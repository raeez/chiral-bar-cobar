\chapter{Introduction}

\section{From a point to a curve}
\label{sec:volume-one-route}

Classical Koszul duality is a phenomenon of graded algebras over a point: a quadratic algebra~$A$ determines a dual coalgebra~$A^!$, and the bar construction mediates between them.  On an algebraic curve~$X$, the generators of the algebra become sections of a $\mathcal{D}_X$-module, the quadratic relations become operator product expansions with meromorphic singularities, and the bar differential becomes an integral transform whose kernel is the logarithmic propagator $d\log(z_i - z_j)$ on Fulton--MacPherson compactifications of configuration spaces.  At genus~$0$ the classical theory embeds faithfully: Koszul duality over a point is Koszul duality over~$\mathbb{P}^1$.  At genus~$g \geq 1$, the topology of the curve forces genuinely new phenomena---central extensions, conformal anomalies, curved $A_\infty$ structures---that have no analogue in the classical setting and whose geometric origin is the curvature of the Hodge bundle over~$\overline{\mathcal{M}}_g$.

The classical theory goes as follows.  Let $V$ be a finite-dimensional graded vector space and $R \subset V^{\otimes 2}$ a subspace of quadratic relations.  The quadratic algebra $A = T(V)/(R)$ is the free tensor algebra modulo~$R$; its Koszul dual coalgebra is $A^{\scriptstyle \text{\normalfont !`}} = T^c(s V^*)$, the cofree conilpotent coalgebra on the suspended linear dual, with coproduct truncated by the orthogonal complement $R^\perp \subset (V^*)^{\otimes 2}$.  The bar construction $B(A) = (T^c(sV), d_B)$ carries a differential $d_B$ induced by the multiplication of~$A$: on cogenerators $d_B(s a) = \sum s a' \otimes s a''$ records the splitting of~$a$ into pairs of generators.  The cobar construction $\Omega(C)$ reverses the process: starting from a conilpotent coalgebra~$C$, one builds the free algebra $T(s^{-1}\bar{C})$ with differential induced by the comultiplication.  The fundamental comparison map
\[
\varepsilon \colon \Omega(B(A)) \longrightarrow A
\]
is always a morphism of dg algebras; when it is a quasi-isomorphism, the algebra~$A$ is called \emph{Koszul}, and $B(A)$ has cohomology concentrated in bar degree~$1$, equal to the Koszul dual coalgebra~$A^{\scriptstyle \text{\normalfont !`}}$.  The Koszul dual algebra is $A^! = (A^{\scriptstyle \text{\normalfont !`}})^\vee = T(V^*)/R^\perp$.  Koszulness means that $A$ admits a linear resolution by $A^!$-modules, and that the derived category of $A$-modules is equivalent to the derived category of $A^!$-modules (up to a shift and twist).  This is the content of Priddy~\cite{Priddy70}, Beilinson--Ginzburg--Soergel~\cite{BGS96}, and Loday--Vallette~\cite{LV12}.

Let $X$ be a smooth projective curve over~$\mathbb{C}$.  A \emph{chiral algebra}~$\cA$ on~$X$ (Beilinson--Drinfeld~\cite{BD04}) is a $\mathcal{D}_X$-module equipped with a chiral bracket
\[
\mu^{\mathrm{ch}} \colon j_* j^*(\cA \boxtimes \cA) \longrightarrow \Delta_!(\cA),
\]
where $j \colon X^2 \setminus \Delta \hookrightarrow X^2$ is the complement of the diagonal and $\Delta_! \colon D(\mathcal{D}_X) \to D(\mathcal{D}_{X^2})$ is the pushforward.  The chiral bracket encodes the operator product expansion: locally, if $a, b \in \cA$ are sections and $z, w$ are local coordinates, then
\[
a(z) b(w) \;\sim\; \sum_{n \geq 0} \frac{(a_{(n)} b)(w)}{(z - w)^{n+1}},
\]
where the modes $a_{(n)}b$ are the Laurent coefficients of the OPE, and the bracket $\mu^{\mathrm{ch}}$ records the entire singular part simultaneously.  The Borcherds identity---the defining axiom of a vertex algebra---is the chiral-algebraic incarnation of the Jacobi identity, coupling all pole orders in a single statement.

The configuration-space mechanism has two strata of commutativity.
The \textbf{Heisenberg atom} ($\Einf$-chiral, commutative) realizes
symmetric configurations, genus-tower curvature, Lagrangian
complementarity, and the full scalar characteristic package.
The \textbf{Yangian atom} ($\Eone$-chiral, braided) realizes ordered
configurations, $R$-matrix inversion, the evaluation locus, and
the Drinfeld--Kohno ladder.%
\label{rem:two-strata}
Every other family---Kac--Moody, Virasoro, $\mathcal{W}$-algebras,
lattice vertex algebras---lies between these two poles.

\section{The logarithmic seed}
\label{sec:the-seed}
\index{propagator|textbf}
\index{Koszul duality!classical}

Consider two distinct points $z_1, z_2$ on a smooth algebraic
curve~$X$, and the logarithmic 1-form
\begin{equation}\label{eq:the-seed}
\eta_{12} \;=\; d\log(z_1 - z_2)
\;=\; \frac{dz_1 - dz_2}{z_1 - z_2}
\end{equation}
on the configuration space $C_2(X) = \{(z_1,z_2) \in X^2 : z_1 \neq z_2\}$.
It has a simple pole along the diagonal with residue~$1$,
and it is conformally invariant.
For three distinct points, the Arnold relation
\begin{equation}\label{eq:arnold-seed}
\eta_{12} \wedge \eta_{23}
+ \eta_{23} \wedge \eta_{31}
+ \eta_{31} \wedge \eta_{12} = 0
\end{equation}
forces the vanishing of triple residues along pairwise collision
divisors of~$\overline{C}_3(X)$.

\subsection{From OPE to bar differential}
\label{subsec:ope-to-bar}
\index{bar complex!construction from OPE}
\index{operator product expansion!as bar differential input}

Let $\cA$ be a chiral algebra on~$X$.  Place $n$ elements
$a_1, \ldots, a_n$ at positions $z_1, \ldots, z_n$ and form
\begin{equation}\label{eq:bar-element-intro}
a_1(z_1) \otimes \cdots \otimes a_n(z_n)
\otimes \bigwedge_{(i,j) \in E(T)} \eta_{ij}
\;\in\;
\cA^{\boxtimes n} \otimes
\Omega^{n-1}_{\log}(\overline{C}_n(X)),
\end{equation}
summed over edges of a planar tree~$T$.  Define a differential~$d$
by extracting residues along collision divisors:
$d(a_1 \otimes \cdots \otimes a_n) = \sum_{i < j}
\operatorname{Res}_{D_{ij}}[\eta_{ij} \cdot
\text{OPE}(a_i, a_j)] \otimes (\text{remaining factors})$.
The Arnold relation~\eqref{eq:arnold-seed} makes the triple
residue vanish, so $\dzero^2 = 0$ at genus~$0$.  The resulting cochain complex is
the \emph{bar complex}~$\barB_X(\cA)$.

As a graded object, the bar complex is
\begin{equation}\label{eq:bar-graded-object-intro}
\barB_X(\cA) \;=\; \bigoplus_{n \geq 1} \bigl(s^{-1}\bar{\cA}\bigr)^{\otimes n}_{\Sigma_n} \otimes H^{n-1}(\overline{C}_n(X), \Omega^{n-1}_{\log}),
\end{equation}
where $s^{-1}$ denotes desuspension (cohomological convention: $|d| = +1$), $\bar{\cA} = \ker(\varepsilon \colon \cA \to \omega_X)$ is the augmentation ideal, and the subscript $\Sigma_n$ indicates coinvariants under the symmetric group action that permutes both the algebra factors and the marked points simultaneously.  The bar degree (also called tensor degree or arity) is~$n$; the internal degree comes from the conformal grading of~$\cA$.  The total differential decomposes as
\begin{equation}\label{eq:bar-diff-decomposition-intro}
d_{\barB} \;=\; d_{\mathrm{internal}} + d_{\mathrm{residue}} + d_{\mathrm{dR}},
\end{equation}
where $d_{\mathrm{internal}}$ acts on each tensor factor by the internal differential of~$\cA$ (if~$\cA$ carries one), $d_{\mathrm{residue}}$ extracts OPE residues at collision divisors weighted by the logarithmic forms~$\eta_{ij}$, and $d_{\mathrm{dR}}$ is the de Rham component from the configuration-space forms.

\subsection{Arnold's identity and $d^2 = 0$}
\label{subsec:arnold-d-squared}
\index{Arnold relation!and nilpotence}

The content of $d_{\barB}^2 = 0$ is entirely concentrated in the residue component.  To see this, expand
\[
(d_{\mathrm{internal}} + d_{\mathrm{residue}} + d_{\mathrm{dR}})^2
= d_{\mathrm{internal}}^2 + d_{\mathrm{dR}}^2 + d_{\mathrm{residue}}^2
+ \{d_{\mathrm{internal}}, d_{\mathrm{residue}}\}
+ \{d_{\mathrm{internal}}, d_{\mathrm{dR}}\}
+ \{d_{\mathrm{residue}}, d_{\mathrm{dR}}\}.
\]
The terms $d_{\mathrm{internal}}^2$ and $d_{\mathrm{dR}}^2$ vanish because each is individually a differential.  The mixed anticommutators vanish by the residue exact sequence on~$\overline{C}_n(X)$.  The entire content is $d_{\mathrm{residue}}^2 = 0$.

At bar degree~$n$, the residue differential acts by
\[
d_{\mathrm{residue}} = \sum_{i < j} \operatorname{Res}_{D_{ij}}
\]
where each $\operatorname{Res}_{D_{ij}}$ extracts the residue of the OPE singularity as $z_i \to z_j$, weighted by $\eta_{ij}$.  The square $d_{\mathrm{residue}}^2$ is a double sum $\sum_{(i,j)} \sum_{(k,\ell)} \operatorname{Res}_{D_{k\ell}} \circ \operatorname{Res}_{D_{ij}}$, and the terms are classified by the index overlap:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Disjoint indices} ($\{i,j\} \cap \{k,\ell\} = \emptyset$).
The two residue extractions act on disjoint pairs of marked points and hence commute up to the Koszul sign.  These terms cancel pairwise.

\item \emph{One shared index} (say $j = k$).  Three boundary divisors $D_{ij}$, $D_{j\ell}$, $D_{i\ell}$ meet at the codimension-$2$ stratum $D_{ij\ell}$ of triple collision.  The iterated residues produce a cyclic sum
\begin{equation}\label{eq:triple-residue-cyclic}
\sum_{\mathrm{cyclic}} \operatorname{Res}_{D_{j\ell}} \circ \operatorname{Res}_{D_{ij}}
\bigl[\mu(\mu(a_i, a_j), a_\ell) \otimes \eta_{ij} \wedge \eta_{j\ell}\bigr].
\end{equation}
This vanishes if and only if the coefficient of $\eta_{ij} \wedge \eta_{j\ell} + \eta_{j\ell} \wedge \eta_{\ell i} + \eta_{\ell i} \wedge \eta_{ij}$ is zero---which is exactly the Arnold relation~\eqref{eq:arnold-seed}---\emph{and} the chiral bracket~$\mu$ satisfies the Borcherds identity, coupling all OPE pole orders simultaneously.

\item \emph{Same pair} ($\{i,j\} = \{k,\ell\}$).  $\operatorname{Res}_{D_{ij}}^2 = 0$ automatically, since the residue of a simple pole is a number.
\end{enumerate}

The Arnold relation handles the form-theoretic factor; the Borcherds identity handles the algebraic factor.  Together they give $d_{\barB}^2 = 0$.  This is the selection principle: the bar construction on a curve accepts exactly those algebraic inputs that satisfy the Borcherds identity---that is, exactly chiral algebras.

\subsection{The bar complex as categorical logarithm}
\label{subsec:bar-as-logarithm}
\index{categorical logarithm|textbf}

The adjunction $\barB_X \dashv \Omega_X$ sends the multiplicative
structure of the OPE to the conilpotent structure of the bar
differential.
When~$\cA$ is Koszul, $\barB_X(\cA)$ has cohomology concentrated
in bar degree~$1$, with linear dual the Koszul dual algebra
$\cA^! = T(V^*)/R^\perp$.  Verdier duality on
$\operatorname{Ran}(X)$ intertwines:
\begin{equation}\label{eq:NAP-intro}
\int_X \cA \;\simeq\;
\left(\int_{-X} \cA^!\right)^{\!\vee}.
\end{equation}
The counit $\varepsilon\colon \Omega_X(\barB_X(\cA)) \to \cA$
is a quasi-isomorphism on the Koszul locus---where the bar
spectral sequence collapses at~$E_2$.
Off the Koszul locus, the bar complex is acyclic in the ordinary
derived category but retains its full content in the coderived
category (Positselski~\cite{Positselski11}): the Koszul locus is
the convergence radius, and the coderived category provides
analytic continuation.


\subsection{The genus-$g$ propagator and curvature}
\label{subsec:genus-g-propagator}
\index{nilpotence-periodicity correspondence}
\index{prime form|textbf}

At genus~$0$, the logarithm~$\eta_{12}$ is single-valued
on~$C_2(\mathbb{P}^1)$, and $\dzero^2 = 0$.  At
genus~$g \geq 1$, the function $z_1 - z_2$ has no global meaning.
The replacement is (schematically)

\begin{equation}\label{eq:genus-g-propagator-intro-ch0}
\eta_{12}^{(g)} \;=\; d\log E(z_1, z_2)
\;+\; \text{(period corrections)},
\end{equation}
where $E(z_1,z_2)$ is the prime form, a section of $K^{-1/2} \boxtimes K^{-1/2}$
(Definition~\ref{def:higher-genus-log-forms}), and the period corrections
involve the Hodge bundle $\mathbb{E} \to \overline{\mathcal{M}}_g$
via the imaginary part of the period matrix
$(\operatorname{Im}\Omega)^{-1}$; see Definition~\ref{def:higher-genus-log-forms}
for the precise formula.

The prime form $E(z_1, z_2)$ is the unique section of $K^{-1/2} \boxtimes K^{-1/2}$ (not $K^{+1/2}$, a common error in the literature) that vanishes to first order along the diagonal $z_1 = z_2$ with unit residue and has prescribed monodromy along the $B$-cycles determined by a choice of homology basis.  It is the genus-$g$ replacement for the rational function $z_1 - z_2$, and its logarithmic derivative $d\log E$ is the genus-$g$ replacement for the genus-$0$ propagator $d\log(z_1 - z_2)$.

The period corrections couple the collision geometry to the complex
structure of the curve, and the Arnold relation acquires a defect:

\begin{equation}\label{eq:curvature-intro}
\dfib^{\,2} = \kappa(\cA) \cdot \omega_g,
\end{equation}
where $\omega_g$ is the Arakelov $(1,1)$-form and $\kappa(\cA)$
is a scalar determined by the leading OPE singularity.

The
logarithm $d\log E(z_1,z_2)$ acquires monodromy around the
$B$-cycles of~$\Sigma_g$, and $\kappa(\cA)$---the residue at the
branch point of the categorical logarithm---is the infinitesimal
generator of this monodromy.  Period integrals along $A$-cycles
restore nilpotence: $\Dg{g}^{\,2} = 0$
(Theorem~\ref{thm:quantum-diff-squares-zero}).
This is the chiral Riemann--Hilbert correspondence: the nilpotent
datum~$\kappa$ determines the periodic datum via exponentiation.

As $[\Sigma_g]$ varies over~$\overline{\mathcal{M}}_g$, the bar
complex traces a family of curved cochain complexes.  The
obstruction class $\mathrm{obs}_g = \kappa(\cA) \cdot \lambda_g \in
H^*(\overline{\mathcal{M}}_g)$, where $\lambda_g = c_g(\mathbb{E})$
is the top Chern class of the Hodge bundle, is a characteristic
class of this family (Theorem~\ref{thm:genus-universality}).
The Koszul dual~$\cA^!$ traces a complementary family: their
obstruction classes pair to give the full cohomology of moduli with
coefficients in the center~$Z(\cA)$.

Classical Koszul duality---Priddy~\cite{Priddy70},
Beilinson--Ginzburg--Soergel~\cite{BGS96},
Loday--Vallette~\cite{LV12}---is a theorem about quadratic algebras
on a point: from $A = T(V)/(R)$, construct
$A^! = T(V^*)/R^\perp$; when $A$ is Koszul,
$\Omega(B(A)) \xrightarrow{\sim} A$.
\index{chiral algebra|textbf}
\index{factorization algebra|textbf}
Chiral algebras (Beilinson--Drinfeld~\cite{BD04}) live on curves;
their tensor factors carry positions $z_i \in X$, and the
Fulton--MacPherson compactification~$\overline{C}_n(X)$ enters the
construction.  The passage from point to curve is the content of
this monograph.


\section{The selection principle}
\label{sec:selection-principle}
\index{selection principle|textbf}
\index{Borcherds identity!as selection criterion}

The bar construction of \S\ref{sec:the-seed} is a
\emph{geometric} machine.  Take a $\mathcal{D}_X$-module~$V$
equipped with a meromorphic pairing
$\mu \colon j_* j^*(V \boxtimes V) \to \Delta_!(V)$
(the OPE), place sections at points of the Fulton--MacPherson
compactification~$\overline{C}_n(X)$, and extract residues along
boundary divisors.  The output is a graded object
$\barB_X(V, \mu)$ with a candidate differential
$d = d_{\mathrm{internal}} + d_{\mathrm{residue}}
+ d_{\mathrm{dR}}$.

The question that governs the theory is:
\begin{center}
\emph{For which algebraic inputs $(V, \mu)$ does
the FM bar construction produce $d^2 = 0$?}
\end{center}

\noindent
Expand $(d_1 + d_2 + d_3)^2$.  The terms
$d_1^2$, $d_3^2$, and all mixed anticommutators vanish
for formal reasons (cochain complex, de~Rham,
residue exact sequence).  The entire content is
$d_2^2 = 0$: the residue extraction must be nilpotent
(Theorem~\ref{thm:bar-nilpotency-complete}).
Three cases arise from index overlap in
$d_2^2 = \sum_{i<j}\sum_{k<\ell}
\Res_{D_{k\ell}} \circ \Res_{D_{ij}}$:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Disjoint indices}
  ($\{i,j\} \cap \{k,\ell\} = \emptyset$).
  Residue operations commute; cancellation is by the
  Koszul sign rule.  No condition on~$\mu$.

\item \emph{One shared index} (say $j = k$).
  Three boundary strata meet at the codimension-$2$ locus
  $D_{ij\ell}$ of triple collision.  The cyclic sum of
  iterated residues
  \[
  \sum_{\textup{cyclic}}
    \Res_{D_{j\ell}} \circ \Res_{D_{ij}}
    \bigl[\mu(\mu(\phi_i, \phi_j), \phi_\ell)
    \otimes \omega\bigr]
  = 0
  \]
  holds if and only if $\mu$ satisfies the
  Borcherds identity---the associativity of the chiral
  product, coupling all OPE pole orders simultaneously.

\item \emph{Same pair}
  ($\{i,j\} = \{k,\ell\}$).
  $\Res_{D_{ij}}^2 = 0$ automatically.
\end{enumerate}
Cases~(i) and~(iii) impose no condition on the algebra.
Case~(ii) imposes exactly one: the Borcherds identity.
This identity is the defining axiom of a chiral algebra.

Two witnesses from Chapter~\ref{ch:heisenberg-frame}:

\smallskip\noindent\textbf{Heisenberg} ($\alpha(z)\alpha(w) \sim
k/(z-w)^2$, double pole only).  No simple pole, so
$d_{\mathrm{bracket}} = 0$.  The full differential reduces to
$d_{\mathrm{curvature}}$, which produces scalars:
$d_{\mathrm{curvature}}^2 = 0$ automatically.  The Arnold
relation handles all of case~(ii).  No algebraic input needed.

\smallskip\noindent\textbf{Kac--Moody} ($h(z)\,e(w) \sim
2e(w)/(z-w) + \cdots$, simple \emph{and} double poles).
The bracket component $d_{\mathrm{bracket}}$ is now nonzero, and
$d_{\mathrm{bracket}}^2 \neq 0$ --- computed explicitly for all
$2048$ sign conventions
(\S\ref{subsec:frame-sl2-bar-low-degree},
Remark~\ref{rem:frame-why-dbracket-fails}).  The obstruction is
proportional to the level~$k$: it is the double-pole data that
$d_{\mathrm{bracket}}$ ignores.  The Borcherds identity absorbs
this obstruction:
\[
d_{\mathrm{bracket}}^2
= -\{d_{\mathrm{bracket}},\, d_{\mathrm{curvature}}\}.
\]
The curvature is the obstruction; the Borcherds identity is the
integrability condition
(Proposition~\ref{prop:pole-decomposition}).

\begin{remark}[Classical vs.\ chiral selection]
\label{rem:classical-vs-chiral-selection}
\index{Koszul duality!classical vs.\ chiral}
The bar construction asks the same question at every level
of geometry:
\begin{center}
\small
\begin{tabular}{lll}
\textbf{Base} & \textbf{Condition for $d^2 = 0$}
  & \textbf{Selected objects} \\
\hline
Point & Jacobi identity & Lie algebras \\
Curve $X$ & Borcherds identity & Chiral algebras \\
$X$ over $\overline{\mathcal{M}}_g$ & $+$ period corrections
  & Modular Koszul algebras
\end{tabular}
\end{center}
Each row is forced by the geometry of the next: the Jacobi
identity handles triple collisions when all poles are simple;
on a curve, multiple pole orders mix at the codimension-$2$
strata of $\overline{C}_n(X)$, and the Borcherds identity is
the unique condition that makes this mixing consistent; at
higher genus, period corrections from the Hodge bundle restore
$\Dg{g}^{\,2} = 0$.  Chiral algebras are what configuration
space geometry accepts; modular Koszul algebras are what the
genus tower accepts.
\end{remark}


\section{The arithmetic face}
\label{sec:arithmetic-face-intro}
\index{arithmetic!of shadow tower|textbf}
\index{Dirichlet--sewing lift|textbf}

The genus-$1$ free energy $F_1^{\mathrm{conn}}(\cA;q)=-\log\det(1-K_q)$ encodes connected sewing amplitudes $a_\cA(N)$ as Fourier coefficients.  The \emph{Dirichlet--sewing lift}
\[
S_\cA(u):=\sum_{N=1}^\infty a_\cA(N)\,N^{-u}
\]
is a Dirichlet series whose analytic structure is controlled by the shadow tower.  The sewing amplitudes define a reproducing kernel $K_\cA(s,t)=S_\cA(s{+}t)$, and the surface moment matrix $\mathscr M_{ij}=K_\cA(\alpha/2{+}i,\,\alpha/2{+}j)$ is positive definite for all $\alpha\geq 2$ (Theorem~\ref{thm:gram-positivity}).

Explicit values:
\[
S_{\cH}(u)=\zeta(u)\zeta(u{+}1),\quad
S_{\mathrm{Vir}}(u)=\zeta(u{+}1)(\zeta(u)-1),\quad
S_{\mathcal W_N}(u)=\zeta(u{+}1)\Bigl((N{-}1)\zeta(u)-\textstyle\sum_{j=1}^{N-1}H_j(u)\Bigr).
\]

\subsection{The three-tier Euler--Koszul classification}
\label{subsec:euler-koszul-intro}
\index{Euler--Koszul classification|textbf}

The Euler defect $D_\cA(u):=S_\cA(u)/(|W(\cA)|\,\zeta(u)\zeta(u{+}1))$ classifies chiral algebras into three tiers:
\begin{center}
\small
\begin{tabular}{lll}
\textbf{Tier} & \textbf{Condition} & \textbf{Families} \\ \hline
Exact Euler--Koszul & $D_\cA\equiv 1$ & $\cH$, $V_k(\fg)$, $\beta\gamma$ \\[2pt]
Finitely defective & $1-D_\cA$ is finite harmonic & Virasoro, $\mathcal W_N$ \\[2pt]
Non-Euler--Koszul & Hecke $L$-function decomposition & lattice with $h(D)\geq 2$
\end{tabular}
\end{center}
The Euler--Koszul class $\mathrm{ek}(\cA)$ and shadow depth $r_{\max}(\cA)$ are independent invariants (Theorem~\ref{thm:shadow-euler-independence}).

\subsection{The two-variable $L$-object and shadow-moduli resolution}
\label{subsec:two-variable-L-intro}
\index{two-variable $L$-object|textbf}

Rankin--Selberg unfolding produces
\[
L_\cA(s,u):=\int_{\cF}^{\mathrm{reg}}F_\cA^{\mathrm{conn}}(\tau)\,E^*(\tau,s)\,y^{u-2}\,dx\,dy,
\]
collapsing to $L_\cA(s,u)=\frac{\Gamma(v)}{(2\pi)^v}S_\cA(v)$ with $v=s+u-1$.  The shadow-moduli map $t\colon\mathfrak H\to\mathbb C$ solves
$\prod_j(1-\lambda_j t(q))^{c_j}=\det(1-K_q^{\mathrm{conn}})$,
and the MC recursion determines shadow coefficients~$S_r$
from seed data $(S_2, S_3, S_4)$ via a quadratic recurrence.
Defining power sums $p_r := -r\,S_r$ and elementary symmetric
polynomials~$e_k$ via the Newton relations yields a formal
spectral polynomial whose roots (the \emph{spectral atoms})
encode the shadow tower in spectral coordinates
(Proposition~\ref{prop:mc-bracket-determines-atoms}).

For even unimodular lattice VOAs, the Hecke--Newton closure (Theorem~\ref{thm:hecke-newton-lattice}) gives the chain: MC equation $\Rightarrow$ prime-local CG closure $\Rightarrow$ CPS automorphy $\Rightarrow$ Ramanujan bound, recovering Deligne's theorem by a new route from homotopy theory (Corollary~\ref{cor:unconditional-lattice}).

\subsection{The Weil analogy}
\label{subsec:weil-analogy-intro}
\index{Weil conjectures!analogy with MC equation}

\begin{center}
\small
\begin{tabular}{ll}
\textbf{Weil proof for $C/\mathbb F_q$}
  & \textbf{MC equation for~$\cA$} \\ \hline
Frobenius $\mathrm{Frob}_q$ & MC element $\Theta_\cA=D_\cA-d_0$ \\[2pt]
$\mathrm{Frob}^2=q$ & $D_\cA^2=0$ \\[2pt]
\'Etale cohomology & cyclic deformation complex \\[2pt]
Weight filtration $W_\bullet$ & arity filtration $\Theta_\cA^{\leq r}$ \\[2pt]
Eigenvalues $\|\alpha_i\|=q^{1/2}$ & Ramanujan $|a_{f_j}(p)|\leq 2p^{(k-1)/2}$
\end{tabular}
\end{center}

\subsection{The Koszul self-duality principle}
\label{subsec:koszul-self-duality-intro}
\index{Koszul self-duality!introduction}

Koszul self-duality $\cA \simeq \cA^!$ forces
$L$-function factorisation.  The Verdier involution
$\sigma\colon\bar B(\cA)\to\bar B(\cA^!)=\bar B(\cA)$
commutes with Hecke operators
(Theorem~\ref{thm:hecke-verdier-commutation});
combined with strong multiplicity one for newforms,
$Z_\cA$ decomposes into Hecke eigenforms, each giving
a standard $L$-function with Euler product
(Theorem~\ref{thm:self-dual-factorization}).
Self-dual lattice VOAs ($h(D)=1$) produce factored
Epstein zeta functions whose zeros lie on the critical line;
non-self-dual lattices ($h(D)>1$) escape this constraint
and exhibit the Davenport--Heilbronn off-line zeros
(Remark~\ref{rem:davenport-heilbronn-koszul}).
For rational VOAs, the theta decomposition bridge
(Proposition~\ref{prop:theta-bridge-rational})
gives Dirichlet $L$-functions modulo the auxiliary level~$2pq$,
resolving the non-lattice gap and showing that
the scalar non-multiplicativity of $\sum|\chi_i|^2$ is a
projection artefact: each theta summand carries its
own Euler product.

\section{The tautological programme}
\label{sec:tautological-programme-intro}
\index{shadow CohFT|textbf}
\index{tautological classes!from MC element}

The MC element $\Theta_\cA$ determines shadow tautological classes $\tau_{g,n}(\cA)\in R^*(\overline{\cM}_{g,n+1})$ forming a cohomological field theory (Theorem~\ref{thm:shadow-cohft}).  The MC equation at $(g,n)$ yields a tautological relation (Theorem~\ref{thm:mc-tautological-descent}).  At genus~$0$, arity~$3$: WDVV (Proposition~\ref{prop:wdvv-from-mc}).  At arity~$2$, genus~$g\geq 2$: Mumford's relation $\lambda_g^2=0$ (Proposition~\ref{prop:mumford-from-mc}).

The complementarity propagator $P_\cA$ is the Givental $R$-matrix, and Teleman reconstruction recovers $\{\tau_{g,n}\}$ from genus-$0$ data (Theorem~\ref{thm:cohft-reconstruction}).  Eynard--Orantin topological recursion is the recursive structure of the MC equation (Corollary~\ref{cor:topological-recursion-mc-shadow}).

\section{The primitive kernel}
\label{sec:primitive-kernel-intro}
\index{primitive kernel|textbf}

By the Milnor--Moore theorem for modular operads, $\Theta_\cA$ is determined by its restriction to primitive corollas---the \emph{primitive logarithmic modular kernel} $\mathfrak K_\cA$.  The fundamental equivalence (Theorem~\ref{thm:primitive-to-global-reconstruction}):
\[
(Q_\cA^{\mathrm{mod}})^2=0
\quad\iff\quad
d\,\mathfrak K_\cA+\mathfrak K_\cA\star\mathfrak K_\cA
+\hbar\,\Delta_{\mathrm{ns}}(\mathfrak K_\cA)=0.
\]
The three terms are the differential, the primitive pre-Lie self-product, and the non-separating loop operator.  The branch BV action on the finite-rank quotient $V_\cA^{\mathrm{br}}$ satisfies the QME, and its metaplectic half-density $\delta_\cA$ satisfies $\delta_\cA^2=\Delta_\cA$ (the spectral discriminant).

The primitive kernel determines the four archetype families completely:
\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{lll}
\textbf{Family} & \textbf{Primitive kernel} $\mathfrak K_\cA$ & \textbf{Active channels} \\
\hline
Heisenberg & $K_{0,2}+K_{1,1}$ & $\Delta_{\mathrm{ns}}$ only \\
Affine $\widehat{\fg}_k$ & $K_{0,2}+K_{0,3}+K_{1,1}$ & $\Delta_{\mathrm{ns}}$, $[-,-]$ \\
$\beta\gamma$ & $K_{0,2}+K_{0,4}+K_{1,1}+R_{\mathrm{pf}}$ & $\Delta_{\mathrm{ns}}$, $\operatorname{Rig}$ \\
Virasoro/$\mathcal{W}_N$ & $K_{0,2}+K_{0,3}+K_{0,4}+K_{1,1}+R_{\mathrm{pf}}+\cdots$ & all three
\end{tabular}
\end{center}

\section{The flat connection and conformal blocks}
\label{sec:flat-connection-intro}
\index{flat connection!on deformation complex}
\index{conformal blocks!from primitive kernel}

The MC equation $D\Theta_\cA+\frac{1}{2}[\Theta_\cA,\Theta_\cA]=0$ defines a flat connection on the primitive deformation complex.  The modular tangent complex (Definition~\ref{def:vol1-modular-tangent-complex}) carries the twisted differential
\[
d_{\Theta_\cA}(x)
\;=\;
\sum_{g\ge 0}\sum_{n\ge 0}
\frac{\hbar^g}{n!}\,\ell_{n+1}^{(g)}(\Theta_\cA^{\otimes n},\,x).
\]
The strict chart is $\nabla^{\mathrm{mod}}_\cA=d+[\Theta_\cA,-]$ on the modular convolution algebra~$\gAmod$.  The MC equation is equivalent to flatness: $(\nabla^{\mathrm{mod}}_\cA)^2=0$.

The chain from primitive kernel to conformal blocks:
\[
\mathfrak K_\cA
\;\xrightarrow{\;\operatorname{FT}^{\log}_{\mathrm{mod}}\;}
\Theta_\cA
\;\xrightarrow{\;\text{linearise}\;}
\nabla^{\mathrm{mod}}_\cA
\;\xrightarrow{\;\text{flat sections}\;}
\operatorname{CB}_g(\cA).
\]
The flat sections of $\nabla^{\mathrm{mod}}_\cA$ on the sheaf of coinvariants over $\overline{\cM}_{g,n}$ are the conformal blocks.  The KZ connection on the bundle of genus-$0$ conformal blocks is the scalar projection $\mathrm{tr}\circ\nabla^{\mathrm{mod}}|_{g=0}$; the Hitchin connection on genus-$g$ blocks is the genus-$g$ component.  The primitive kernel~$\mathfrak K_\cA$ controls the connection through the Feynman transform: the arity-$2$ corolla $K_{0,2}$ determines the projectively flat structure, the arity-$3$ corolla $K_{0,3}$ determines the KZ equations, and the higher corollas $K_{0,n}$ ($n\ge 4$) encode the quantum corrections that deform the classical KZ system.

For the four archetypes:
the Heisenberg connection $\nabla^{\mathrm{mod}}_{\cH}=d$ is trivially flat;
the affine connection $\nabla^{\mathrm{mod}}_{\widehat\fg}=d+[\kappa(x,[y,z]),-]$ is the KZ connection;
the $\beta\gamma$ connection carries a quartic correction from $K_{0,4}$;
the Virasoro connection $\nabla^{\mathrm{mod}}_{\mathrm{Vir}}=d+[\Theta_{\mathrm{Vir}},-]$ is the full Hitchin connection, with the infinite shadow tower producing an infinite hierarchy of quantum corrections to the classical projectively flat structure.

\section{The three-dimensional reading}
\label{sec:3d-reading-intro}

The bar differential extracts residues in~$\C$; the bar coproduct
splits ordered sequences in~$\R$.  Together they make the bar
complex an algebra over a two-coloured operad on
$\C \times \R$: the holomorphic-topological Swiss-cheese operad
$\mathsf{SC}^{\mathrm{ch,top}}$ of Volume~II\@.  This operad is homotopy-Koszul
(Kontsevich formality + classical Swiss-cheese Koszulity + transfer),
and the bar-cobar adjunction on $\mathsf{SC}^{\mathrm{ch,top}}$-algebras is a Quillen
equivalence.

The genus expansion of the modular bar differential,
\[
D = \sum_{g \ge 0} \hbar^g D^{(g)},
\qquad
D^{(g)} = \sum_{\substack{\Gamma \in \mathsf{StGraph}^{\mathrm{conn}}
\\ g(\Gamma) = g}}
\frac{1}{|\Aut(\Gamma)|}\, D_\Gamma,
\]
is the Feynman transform of the modular operad
$\{\overline{\cM}_{g,n}\}$.  The canonical MC element
$\delta_\cA = D - D^{(0)}$ computes the total positive-genus
correction; the genus-by-genus identities
$\sum_{p+q=g} D^{(p)} D^{(q)} = 0$ are the modular sewing
relations.  The primitive boundary kernel
$\mathfrak{K}_\cA = K_{0,2} + K_{0,3} + \cdots + K_{1,1} +
R_{\mathrm{pf}} + \cdots$ reconstructs the full modular MC field
via the logarithmic modular Feynman transform
$\Theta_\cA = \mathrm{FT}^{\log}_{\mathrm{mod}}(\mathfrak{K}_\cA)$.

The deformation-quantization bridge from PVA to vertex algebra
passes through the half-space $\C \times \R_{\ge 0}$.  The
\emph{doubling theorem} of Volume~II embeds the half-space BV
problem into the doubled whole-space via the method of images: the
reflected weight identity is the $\sigma$-invariant restriction of
the standard Stokes identity.  Every logarithmic
$\mathsf{SC}^{\mathrm{ch,top}}$-algebra has a boundary vertex algebra.  The
five-component datum---bulk factorization algebra, boundary vertex
algebra, derived center, line-operator category, spectral
$R$-matrix---assembles into a single holographic package: for any
logarithmic $\mathsf{SC}^{\mathrm{ch,top}}$-algebra, all five components exist and are
consistent.  The standard landscape enters through two gates: the
affine bridge (the affine PV sigma model satisfies the physics
bridge hypotheses) and Drinfeld--Sokolov transport (DS reduction
preserves the $\mathsf{SC}^{\mathrm{ch,top}}$ structure).

\section{The five theorems}
\label{sec:NAP-unifying}
\label{sec:main-results-complete}
\index{Koszul duality!chiral|textbf}
\index{modular Koszul duality|textbf}
\index{quantum corrections!as modular completion}

\subsection{The Heisenberg algebra}
\label{subsec:five-theorems-heisenberg}

Take $\cA = \cH_k$ with OPE
$J(z)\,J(w) \sim k/(z-w)^2$.  One generator, one double pole, no
simple pole.

\smallskip
\noindent\textbf{Theorem~A (bar-cobar adjunction).}\enspace
The bar complex is $\barB(\cH_k) = T^c(s^{-1}\C\cdot J)$, the
cofree coalgebra on a single cogenerator.  The bar differential on
a degree-$2$ element is
\[
d_{\barB}[s^{-1}J \,|\, s^{-1}J]
\;=\;
\Res_{z_1 = z_2}\!\left[
  \frac{k}{(z_1 - z_2)^2}\cdot d\log(z_1-z_2)
\right]
\;=\; k.
\]
At degree~$3$: the Arnold relation on $\FM_3(\C)$ gives
$d_{\barB}^2 = 0$.  The three collision divisors contribute
$k \cdot (\eta_{12}\wedge\eta_{23} + \eta_{23}\wedge\eta_{13}
+ \eta_{13}\wedge\eta_{12}) = 0$.

Verdier duality identifies the dual of $\barB(\cH_k)$ with the homotopy Koszul dual algebra at level~$-k$:
$\mathbb{D}_{\mathrm{Ran}}\,\barB(\cH_k) \simeq (\cH_{-k})_\infty$
(underlying complex $\simeq \barB(\cH_{-k})$).
The pairing is $\langle s^{-1}J, s^{-1}J^*\rangle = 1$, with
$J^*$ the dual cogenerator at level $-k$.

\smallskip
\noindent\textbf{Theorem~B (bar-cobar inversion).}\enspace
The bar cohomology is $H^{-1}(\barB(\cH_k)) = \C\cdot s^{-1}J$
(concentrated in bar degree~$1$), so the Koszul dual coalgebra is
$\cH_k^{\mathrm{i}} = \C\cdot s^{-1}J$.  The Koszul dual algebra
is $\cH_k^! = (\cH_k^{\mathrm{i}})^\vee$: the symmetric chiral
algebra $\operatorname{Sym}^{\mathrm{ch}}(V^*)$ with curvature
$m_0 = -k\,\omega$ encoding the opposite level.
By the Verdier intertwining of Theorem~A,
$\cH_k^!$ has the same modular
characteristic as $\cH_{-k}$; the two are
quasi-isomorphic but not isomorphic as chiral algebras
(the Heisenberg is \emph{not} self-dual;
see~\S\ref{sec:frame-koszul-dual}).
The cobar of the bar is
$\Omega(\barB(\cH_k)) = T(s\,\C\cdot s^{-1}J) = \C[J]$,
which is $\cH_k$ as a graded algebra.  The counit
$\varepsilon\colon\Omega(\barB(\cH_k)) \to \cH_k$ is a
quasi-isomorphism: the Heisenberg is Koszul.

\smallskip
\noindent\textbf{Theorem~C (complementarity).}\enspace
At genus~$g \ge 1$:
$Q_g(\cH_k) \oplus Q_g(\cH_k^!) \cong
H^*(\overline{\cM}_g, \cZ_{\cH_k})$.
The modular characteristic is $\kappa(\cH_k) = k$, and
$\kappa(\cH_k) + \kappa(\cH_k^!) = k + (-k) = 0$.  The
complementarity sum of central charges is
$c(\cH_k) + c(\cH_k^!) = 1 + 1 = 2$.

\smallskip
\noindent\textbf{Theorem~D (modular characteristic).}\enspace
At genus~$1$: the self-loop graph $\Gamma_{\mathrm{irr}}$ on
$\overline{\cM}_{1,1}$ contributes
$d_{\mathrm{fib}}^2 = k \cdot \omega_1$.  The genus-$1$
obstruction number is $F_1 = k/24$.  The generating function:
\[
\sum_{g \ge 1} F_g(\cH_k)\,x^{2g}
\;=\;
k\!\left(\frac{x/2}{\sin(x/2)} - 1\right)
\;=\;
\frac{k}{24}\,x^2
+ \frac{7k}{5760}\,x^4
+ \frac{31k}{967680}\,x^6
+ \cdots
\]
The coefficients are
$F_g = k \cdot \frac{2^{2g-1}-1}{2^{2g-1}} \cdot
\frac{|B_{2g}|}{(2g)!}$,
where $B_{2g}$ is the $2g$-th Bernoulli number.
(The physical partition function $\log Z_1 = -(k/24)\log\eta(\tau)$
recovers the same data on~$\overline{\cM}_{1,1}$.)

\smallskip
\noindent\textbf{Theorem~H (chiral Hochschild).}\enspace
$\ChirHoch^*(\cH_k) = \C \oplus \C\cdot\xi$ with $|\xi| = 2$
and $\xi^2 = 0$: two-dimensional, concentrated in degrees~$0$
and~$2$ (the class~$\xi$ is the double-pole OPE datum).
The Koszul resolution has length~$2$, so
$\ChirHoch^n = 0$ for $n \ge 3$---Hilbert series $1 + t^2$,
the rank-$1$ case of the polynomial-growth theorem.

The full computation appears in Chapter~\ref{ch:heisenberg-frame}.

\subsection{The general theorems}
\label{subsec:five-theorems-general}

\begin{itemize}
\item \emph{Theorem~A} (Geometric bar-cobar duality,
  Theorem~\ref{thm:bar-cobar-isomorphism-main}).
  The bar functor
  \[
  \bar{B}_X \colon
  \operatorname{Alg}_{\mathrm{aug}}(\operatorname{Fact}(X))
  \to \operatorname{CoAlg}_{\mathrm{conil}}(\operatorname{Fact}(X))
  \]
  is intertwined with Verdier duality:
  $\mathbb{D}_{\operatorname{Ran}} \bar{B}_X(\cA)
  \simeq \bar{B}_X(\cA^!)$,
  functorial in families over $\overline{\mathcal{M}}_{g,n}$.
  It decomposes into chiral twisting morphisms
  ($\mathrm{A}_0$,
  Theorem~\ref{thm:fundamental-twisting-morphisms}),
  bar concentration
  ($\mathrm{A}_1$, Theorem~\ref{thm:bar-concentration}), and
  Verdier-geometric duality
  ($\mathrm{A}_2$,
  Theorem~\ref{thm:bar-cobar-isomorphism-main}).
  On the proved genus-$0$ locus, the bar complex admits a BRST
  comparison; Verdier duality is the algebraic shadow of CPT.

\item \emph{Theorem~B} (Bar-cobar inversion,
  Theorem~\ref{thm:higher-genus-inversion}).
  On the Koszul locus, the counit
  $\Omega_X \bar{B}_X(\cA) \xrightarrow{\sim} \cA$
  is an equivalence; the spectral sequence collapses at~$E_2$.
  Off the locus, the bar-cobar object persists but becomes curved;
  the failure is measured by
  $\Theta_\cA$~\eqref{eq:universal-MC}.
  The BRST resolution is complete.

  More precisely, there is a spectral sequence with $E_1$ page
  $E_1^{p,q} = H^q(\barB^p_X(\cA))$ converging to $H^{p+q}(\Omega_X \barB_X(\cA))$.
  On the Koszul locus, $E_1^{p,q} = 0$ for $p \neq 1$, so
  $E_2 = E_\infty$, and the comparison map
  $\varepsilon \colon \Omega_X \barB_X(\cA) \to \cA$
  induces an isomorphism on cohomology.  This is the categorical
  exponential: $\exp \circ \log = \operatorname{id}$.

\item \emph{Theorem~C} (Deformation-obstruction complementarity,
  Theorem~\ref{thm:quantum-complementarity-main}).
  The ambient complex
  $\mathbf{C}_g(\cA) := R\Gamma(\overline{\mathcal{M}}_g,
  \mathcal{Z}_\cA)$
  carries a shifted-symplectic pairing from Verdier duality on
  the center local system.  The Verdier involution decomposes it
  into complementary Lagrangians
  $\mathbf{Q}_g(\cA) \oplus \mathbf{Q}_g(\cA^!)$
  (Definition~\ref{def:complementarity-complexes}).
  At the cohomological level,
  \begin{equation}\label{eq:complementarity-intro}
  Q_g(\cA) \oplus Q_g(\cA^!) \cong
  H^*(\overline{\mathcal{M}}_g, \mathcal{Z}_\cA).
  \end{equation}
  The anomaly of one algebra is the ghost-number violation of
  its dual.

  The shifted-symplectic structure is a $(-(3g{-}3))$-shifted symplectic form on the derived moduli of genus-$g$ bar families, in the sense of Pantev--To\"en--Vaqui\'e--Vezzosi~\cite{PTVV13}.  The complementary Lagrangians~$Q_g(\cA)$ and~$Q_g(\cA^!)$ are the $\pm 1$ eigenspaces of the Verdier involution~$\sigma = \mathbb{D}_{\operatorname{Ran}}$ acting on~$\mathbf{C}_g(\cA)$.  Their intersection is the center~$Z(\cA)$; the ambient complex is the direct sum.

\item \emph{Theorem~D$_{\mathrm{scal}}$} (Scalar modular
  characteristic, Theorem~\ref{thm:modular-characteristic}).
  A single scalar $\kappa(\cA)$---the genus-$1$ curvature
  coefficient---determines the entire scalar modular package
  (Definition~\ref{def:scalar-modular-package}):
  $\kappa$ is universal
  ($\mathrm{obs}_g = \kappa \cdot \lambda_g$),
  additive under tensor product, and duality-constrained
  ($\kappa + \kappa' = 0$ for affine Kac--Moody and free fields).
  Its generating function is the $\hat{A}$-genus:
  \begin{equation}\label{eq:ahat-genus-generating-function-intro}
  \sum_{g \geq 1} \mathrm{obs}_g(\cA) \cdot \hbar^{2g-2}
  = \kappa(\cA) \cdot \bigl(\hat{A}(i\hbar) - 1\bigr).
  \end{equation}

  \emph{Theorem~D$_\Delta$} (Spectral characteristic,
  Theorem~\ref{thm:spectral-characteristic}):
  the spectral discriminant $\Delta_\cA(x)$ is a separately proved
  non-scalar invariant.

  $\kappa$ is the one-loop coefficient of the effective action;
  its vanishing is anomaly-free string propagation at all genera.
  Together, $D_{\mathrm{scal}}$ and $D_\Delta$ are the
  characteristic classes of the factorization homology
  bundle $\int_{\Sigma_g}\!\cA$
  (Remark~\ref{rem:four-levels}); the bundle
  itself---its fibers, flat connection, and modular
  functor structure---is the full modular homotopy invariant
  (Conjecture~\ref{conj:categorical-modular-kd}).

\item \emph{Theorem~H} (Chiral Hochschild cohomology,
  Theorem~\ref{thm:hochschild-polynomial-growth}).
  $\ChirHoch^*(\cA)$ is polynomial on the Koszul locus and dual
  to $\ChirHoch^*(\cA^!)$.  The full Taylor series of the
  categorical logarithm lives in a polynomial ring.  The Hochschild
  complex governs deformations
  (Chapter~\ref{chap:deformation-theory}): its brace and
  Gerstenhaber structures control the moduli of quantizations
  and the passage from Poisson to chiral.
\end{itemize}


The scalar package has teeth:

\begin{theorem}[Central charge complementarity; \ClaimStatusProvedHere]\label{thm:central-charge-complementarity}
\index{central charge!complementarity|textbf}
\textup{[Regime: curved-central on the Koszul locus
\textup{(}Convention~\textup{\ref{conv:regime-tags})}.]}

For Koszul dual pairs related by the Feigin--Frenkel involution
$k \mapsto k' = -k - 2h^\vee$:
\begin{enumerate}
\item[\textup{(a)}] \emph{Affine Kac--Moody.}
  $c(\widehat{\mathfrak{g}}_k) + c(\widehat{\mathfrak{g}}_{k'})
  = 2 \dim \mathfrak{g}$.
\item[\textup{(b)}] \emph{Principal $\mathcal{W}$-algebras:}
  $c(\mathcal{W}^k(\mathfrak{g})) + c(\mathcal{W}^{k'}(\mathfrak{g}))
   = 2\operatorname{rank}(\mathfrak{g}) + 4h^\vee \dim \mathfrak{g}$.
\end{enumerate}
In particular, $\mathfrak{g} = \mathfrak{sl}_2$ in
part~\textup{(b)} gives $c + c' = 26$.
\end{theorem}

\begin{proof}
\emph{Part~(a).}  The Sugawara central charge is
$c(\widehat{\mathfrak{g}}_k) = k \dim\mathfrak{g}/(k + h^\vee)$.
Since $k' + h^\vee = -(k + h^\vee)$:
\[
c(k) + c(k') = \frac{k \cdot d}{k+h^\vee}
+ \frac{(k+2h^\vee) \cdot d}{k+h^\vee}
= \frac{2(k + h^\vee) \cdot d}{k+h^\vee} = 2d
\]
where $d = \dim\mathfrak{g}$.

\emph{Part~(b).}  The Drinfeld--Sokolov central charge is
$c(\mathcal{W}^k(\mathfrak{g})) = r - 12|\rho|^2 (t-1)^2/t$
where $r = \operatorname{rank}(\mathfrak{g})$ and
$t = k + h^\vee$
(verified for $\mathfrak{sl}_2$ in
Theorem~\ref{thm:w-algebra-koszul-main} and for $\mathfrak{sl}_3$
in Theorem~\ref{thm:w-koszul-precise}).
The involution sends $t \mapsto -t$:
\[
c(k) + c(k') = 2r + 12|\rho|^2 \cdot
\frac{(t+1)^2 - (t-1)^2}{t}
= 2r + 48|\rho|^2.
\]
The Freudenthal--de~Vries identity
$|\rho|^2 = h^\vee \dim\mathfrak{g}/12$
(under normalization $(\theta, \theta) = 2$;
see~\cite{Kac}, Ch.~13)
yields $c + c' = 2r + 4h^\vee d$.

\emph{Verification.}
$\mathfrak{sl}_2$: $2(1) + 4(2)(3) = 26$.
$\mathfrak{sl}_3$: $2(2) + 4(3)(8) = 100$.
$\mathfrak{sl}_N$:
$2(N{-}1) + 4N(N^2{-}1) = 2(N{-}1)(2N^2{+}2N{+}1)$.
\end{proof}

\begin{remark}\label{rem:bosonic-string-26}
\index{bosonic string!critical dimension}
The identity $c + c' = 26$ for $\mathfrak{sl}_2$ is the algebraic
incarnation of $c_{\mathrm{matter}} + c_{\mathrm{ghost}} = 0$.
At the critical point, the relative BRST complex of the bosonic string
is quasi-isomorphic to the genus-$0$ bar complex
\textup{(}Theorem~\ref{thm:brst-bar-genus0}\textup{)}.  One should not
read this as a proved literal statement that the ghost system is the
cobar of the matter Virasoro bar complex without the additional
heuristic BV/bar bridge package of Chapter~\ref{ch:bv-brst}.  The
bosonic string critical dimension still falls out of Koszul duality at
the scalar level.
\end{remark}

\medskip

The five theorems assemble into a single commutative square:
\begin{equation}\label{eq:master-square}
\begin{tikzcd}[row sep=huge, column sep=huge]
\operatorname{Alg}_{\mathrm{aug}}(\operatorname{Fact}(X))
  \arrow[r, "\bar{B}_X"]
  \arrow[d, "(\cdot)^!"']
& \operatorname{CoAlg}_{\mathrm{conil}}(\operatorname{Fact}(X))
  \arrow[d, "\mathbb{D}_{\operatorname{Ran}}"]
\\
\operatorname{Alg}_{\mathrm{aug}}(\operatorname{Fact}(X))
  \arrow[r, "\bar{B}_X"']
& \operatorname{CoAlg}_{\mathrm{conil}}(\operatorname{Fact}(X))
\end{tikzcd}
\end{equation}
\index{master diagram}
\index{Verdier duality!as mediating mechanism}
Commutativity is Theorem~A\@.  The cobar functor~$\Omega_X$, left
adjoint to~$\bar{B}_X$, gives the counit of Theorem~B\@.  At
genus~$g \geq 1$, the universal Maurer--Cartan class~$\Theta_\cA$
measures the failure of strict commutativity; $\kappa(\cA) = 0$ is
the condition for the square to commute in the derived category
rather than the coderived category.

The Koszul locus is the convergence radius of the categorical
logarithm.  Off this locus, the coderived category
(Positselski~\cite{Positselski11}) provides analytic continuation:
the curved bar complex is acyclic in the ordinary derived category
but retains its full homotopical content in the coderived
category---as the logarithm is trivial on a simply-connected domain
but carries monodromy on a multiply-connected one.

\subsection{The main theorem}
\label{subsec:main-theorem}

\begin{theorem}[Modular Koszul duality; \ClaimStatusProvedHere]
\label{thm:modular-koszul-duality-main}
\index{modular Koszul duality!main theorem|textbf}

Let\/ $\cA$ be a chiral algebra on a smooth projective
curve~$X$ over\/ $\mathbb{C}$, equipped with a non-degenerate
invariant pairing.  The genus-graded bar complex
$\{\barB^{(g)}_X(\cA)\}_{g \geq 0}$ is an algebra over the
Feynman transform\/ $\mathrm{FCom}$ of the commutative modular operad
\textup{(}Theorem~\textup{\ref{thm:bar-modular-operad}}\textup{)},
with genus-completed differential
\[
D_\cA = \sum_{g \geq 0} \hbar^g\, d_\cA^{(g)}
\quad\text{satisfying}\quad
D_\cA^2 = 0.
\]

\begin{enumerate}[label=\textup{(\roman*)}]

\item \emph{Bar--cobar duality.}
  The bar functor\/ $\barB_X$ and cobar functor\/~$\Omega_X$
  form an adjoint pair on augmented factorization algebras,
  with Verdier intertwining
  $\mathbb{D}_{\operatorname{Ran}}\, \barB_X(\cA) \simeq
  \barB_X(\cA^!)$
  \textup{(}Theorem~\textup{\ref{thm:bar-cobar-isomorphism-main}}\textup{)}.
  On the Koszul locus, the counit
  $\Omega_X(\barB_X(\cA)) \xrightarrow{\sim} \cA$ is a
  quasi-isomorphism at every genus
  \textup{(}Theorem~\textup{\ref{thm:bar-cobar-inversion-qi}}\textup{)}.

\item \emph{Universal Maurer--Cartan class.}
  The positive-genus correction
  \begin{equation}\label{eq:theta-main-theorem}
  \Theta_\cA
  \;:=\;
  D_\cA - d_\cA^{(0)}
  \;=\;
  \sum_{g \geq 1} \hbar^g\, d_\cA^{(g)}
  \end{equation}
  is a Maurer--Cartan element:
  $\Theta_\cA \in
  \MC\!\bigl(\Defcyc(\cA) \;\widehat{\otimes}\; \Gmod\bigr)$.
  The Maurer--Cartan equation holds because $D_\cA^2 = 0$;
  no construction is required beyond the bar differential itself.

\item \emph{Projections.}
  The scalar trace of\/ $\Theta_\cA$ is
  $\operatorname{tr}(\Theta_\cA)
  = \sum_{g \geq 1} \kappa(\cA)\,\lambda_g$,
  where $\kappa(\cA) \in \mathbb{C}$ is universal, additive
  under tensor product, duality-constrained
  ($\kappa + \kappa' = 0$ for KM/free fields), and
  has $\hat{A}$-genus generating function
  \textup{(}Theorem~\textup{\ref{thm:modular-characteristic}}\textup{)}.
  Evaluated against a conformal deformation $g_1 = e^{2\sigma}g_0$ of the worldsheet metric, $\kappa(\cA)$ yields the Polyakov formula with coefficient $\kappa/(6\pi)$ (Proposition~\ref{prop:polyakov-chern-weil}): the modular characteristic is the algebraic form of the conformal anomaly.
  The eigenspace decomposition under the Verdier involution gives
  the Lagrangian polarization
  $\mathbf{Q}_g(\cA) \oplus \mathbf{Q}_g(\cA^!)$
  \textup{(}Theorem~\textup{\ref{thm:quantum-complementarity-main}}\textup{)}.
  The coefficient ring is the polynomial chiral Hochschild
  cohomology $\ChirHoch^*(\cA)$
  \textup{(}Theorem~\textup{\ref{thm:main-koszul-hoch}}\textup{)}.

\item \emph{Shadow Postnikov tower.}
  The finite-order projections
  $\Theta_\cA^{\leq 2} \to
  \Theta_\cA^{\leq 3} \to
  \Theta_\cA^{\leq 4} \to \cdots$
  give the shadow Postnikov tower:
  $\kappa(\cA)$ at arity~$2$,
  the cubic shadow~$\mathfrak{C}(\cA)$ at arity~$3$,
  the quartic resonance class~$\mathfrak{Q}(\cA)$ at arity~$4$.
  The element $\Theta_\cA$ satisfies clutching factorization under
  stable-curve degeneration and Verdier duality:
  $\mathbb{D}(\Theta_\cA) = \Theta_{\cA^!}$.
\end{enumerate}
\end{theorem}

\begin{remark}
\label{rem:categorical-logarithm-paradigm}
The bar differential has integral kernel $d\log(z_i - z_j)$.
The Arnold relation
$\eta_{12}\wedge\eta_{23}+\eta_{23}\wedge\eta_{31}
+\eta_{31}\wedge\eta_{12}=0$ forces $d^2=0$.
At genus~$0$, this recovers classical Koszul duality.
At genus $g\ge 1$, the period structure of the curve
introduces monodromy $\kappa(\cA)\cdot\omega_g$; the
genus tower is the Taylor expansion of that monodromy.  The nonlinear characteristic layer of this monograph
computes the higher Taylor jets of this expansion: the quartic jets
of the complementarity potential
(Chapter~\ref{app:nonlinear-modular-shadows}), the spectral
cumulants of the branch-line quotient
(Appendix~\ref{app:branch-line-reductions}), and the canonical
homotopy arity in the non-principal $\mathcal W$-algebra sector.
\end{remark}

\subsection{Shadow separation, tautological classes, and analytic
realization}
\label{subsec:shadow-separation-intro}

\begin{corollary}[Shadow separation; \ClaimStatusProvedHere;
  Theorem~\textup{\ref{thm:shadow-separation}}]
\label{cor:shadow-separation-intro}
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Completeness.}
  $[\Theta_\cA^{\leq r}] = [\Theta_\cB^{\leq r}]$ for
  all~$r \geq 2$ implies
  $[\Theta_\cA] = [\Theta_\cB]$ in
  $\operatorname{MC}(\widehat{\gAmod})/\!\!\sim$.
\item \emph{Strict refinement.}
  $\kappa(\cH_1) = \kappa(\mathrm{Vir}_2) = 1$ but
  $\mathfrak{C}_{\cH} = 0 \neq 2x^3 =
  \mathfrak{C}_{\mathrm{Vir}}$;
  on the weight-changing line,
  $\mathfrak{Q}_{\cH} = 0 \neq
  \operatorname{cyc}(m_3)
  = \mathfrak{Q}_{\beta\gamma}$.
\item \emph{Koszul invariance.}
  $r_{\max}(\cA^!) = r_{\max}(\cA)$
  \textup{(}Corollary~\textup{\ref{cor:shadow-depth-koszul-invariance}}\textup{)}.
\end{enumerate}
\end{corollary}

The cyclic trace of the graph-sum formula defines
shadow tautological classes
$\tau_{g,n}(\cA) \in
R^*(\overline{\mathcal{M}}_{g,n+1})$
(Definition~\ref{def:shadow-tautological-classes},
Theorem~\ref{thm:shadow-tautological-ring}):
$\tau_{g,2}(\cA) = \kappa(\cA) \cdot \pi^*\lambda_g$
at arity~$2$, with new classes at higher arities whose
vertex weights are the cyclic traces of the transferred
operations $\ell_n^{(g)}(\cA)$.
For every algebra in the standard landscape, the algebraic
shadow data extends to the sewing
envelope~$\cA^{\mathrm{sew}}$
(Corollary~\ref{cor:analytic-shadow-realization}).


\section{The modular characteristic and the spectral discriminant}
\label{sec:modular-char-spectral-disc-intro}
\index{modular characteristic!explicit formula|textbf}
\index{spectral discriminant!construction|textbf}
\index{A-hat genus@$\hat{A}$-genus|textbf}

The genus-$1$ curvature $\kappa(\cA)$ is the most important numerical invariant of a chiral algebra from the standpoint of modular Koszul duality.  We construct it explicitly.

\subsection{The explicit formula for $\kappa$}
\label{subsec:kappa-explicit}
\index{modular characteristic!explicit formula}

Let~$\cA$ be a chiral algebra on a smooth projective curve~$X$ with OPE
$a(z)\,b(w) \sim \sum_{n \geq 0} (a_{(n)}b)(w) / (z-w)^{n+1}$.
The modular characteristic $\kappa(\cA)$ is the genus-$1$ curvature coefficient:
$\kappa(\mathcal{H}_k) = k$ for the Heisenberg algebra,
$\kappa(\widehat{\mathfrak{g}}_k) = (k+h^\vee)\dim\mathfrak{g}/(2h^\vee)$ for affine Kac--Moody, and
$\kappa(\mathrm{Vir}_c) = c/2$ for Virasoro
(Theorem~\ref{thm:genus-universality}).
The obstruction class at genus~$g$ is universally determined:
\begin{equation}\label{eq:obstruction-class-formula-intro}
\mathrm{obs}_g(\cA) = \kappa(\cA) \cdot \lambda_g \in H^{2g}(\overline{\mathcal{M}}_g, \mathbb{Q}),
\end{equation}
where $\lambda_g = c_g(\mathbb{E})$ is the top Chern class of the Hodge bundle.

\subsection{Universality, additivity, anti-symmetry}
\label{subsec:kappa-properties}

The three properties of~$\kappa$ that make it a topological characteristic class:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \textbf{Universality.}  The obstruction class $\mathrm{obs}_g(\cA) = \kappa(\cA) \cdot \lambda_g$ is determined by the genus-$1$ value alone.  This is because the clutching maps of stable curves force the higher-genus classes to be determined by the genus-$1$ curvature via the modular operad structure (Theorem~\ref{thm:genus-universality}).

\item \textbf{Additivity.}  For a tensor product $\cA \otimes \cB$ of chiral algebras,
\[
\kappa(\cA \otimes \cB) = \kappa(\cA) + \kappa(\cB).
\]
This follows from the factorization of the bar complex:
$\barB_X(\cA \otimes \cB) \simeq \barB_X(\cA) \otimes \barB_X(\cB)$.

\item \textbf{Anti-symmetry.}  Under Koszul duality,
\[
\kappa(\cA) + \kappa(\cA^!) = 0
\]
for affine Kac--Moody algebras (where $\cA^! = \widehat{\mathfrak{g}}_{k'}$ with $k' = -k - 2h^\vee$) and for free-field algebras.  For $\mathcal{W}$-algebras from Drinfeld--Sokolov reduction, $\kappa + \kappa'$ is a nonzero constant: $\kappa + \kappa' = 13$ for Virasoro (Theorem~\ref{thm:modular-characteristic}(iii)).
\end{enumerate}

\subsection{The $\hat{A}$-genus generating function}
\label{subsec:ahat-genus}
\index{A-hat genus@$\hat{A}$-genus!as family index}

The generating function of the obstruction classes is the $\hat{A}$-genus.  Grothendieck--Riemann--Roch applied to the universal curve $\pi \colon \overline{\mathcal{C}}_g \to \overline{\mathcal{M}}_g$ yields
\[
\operatorname{ch}\bigl(R\pi_* \barB_g(\cA)\bigr)
= \kappa(\cA) \cdot \operatorname{td}(\mathbb{E}^\vee),
\]
and the Todd class of the dual Hodge bundle produces the $\hat{A}$-series.  The full generating function is
\begin{equation}\label{eq:genus-generating-function-intro}
\sum_{g \geq 1} \mathrm{obs}_g(\cA) \cdot \hbar^{2g-2}
= \kappa(\cA) \cdot \bigl(\hat{A}(i\hbar) - 1\bigr)
= \kappa(\cA) \cdot \left(\frac{i\hbar/2}{\sinh(i\hbar/2)} - 1\right).
\end{equation}
For a free fermion, this produces the spin partition function; for the Virasoro algebra, the one-loop string amplitude.

\subsection{The spectral discriminant}
\label{subsec:spectral-discriminant-intro}
\index{spectral discriminant|textbf}

The spectral discriminant $\Delta_\cA(x) := \operatorname{disc}(P_{\cA^!}(x))$---the discriminant of the Koszul dual Hilbert series---is the first non-scalar invariant.  It is invariant under Koszul duality ($\Delta_\cA = \Delta_{\cA^!}$, Proposition~\ref{prop:discriminant-characteristic}) and under Drinfeld--Sokolov reduction (Theorem~\ref{thm:ds-bar-gf-discriminant}).

The Hilbert series of the Koszul dual is $P_{\cA^!}(x) = \sum_{n \geq 0} \dim H^n(\barB(\cA)) \cdot x^n$.  For Koszul algebras, this is the reciprocal of the Hilbert series of~$\cA$: $P_{\cA^!}(x) = 1 / P_\cA(-x)$.  The discriminant of this rational function, viewed as a polynomial in~$x$, is an integer-valued invariant that detects spectral structure invisible to the scalar~$\kappa$.

The shared discriminant $\Delta(x) = (1-3x)(1+x) = 1 - 2x - 3x^2$ across $\widehat{\mathfrak{sl}}_2$, Virasoro, and $\beta\gamma$ is not coincidence: these algebras lie on the same spectral sheet of the Koszul dual parameter space.  The zeros of~$\Delta$ at $x = 1/3$ and $x = -1$ mark the transitions between bar-acyclic and bar-active sectors.


\section{The master object}
\label{sec:convolution-algebra}
\index{convolution dg Lie algebra|textbf}
\index{shadow Postnikov tower|textbf}
\index{Maurer--Cartan!universal class}

\subsection{The modular convolution dg Lie algebra}
\label{subsec:modular-convolution-dg-lie}
\index{convolution dg Lie algebra!construction|textbf}

The genus-graded bar complex $\{\barB^{(g)}(\cA)\}_{g \ge 0}$ is an algebra over the Feynman transform $\mathrm{FCom}$ of the commutative modular operad (Theorem~\ref{thm:bar-modular-operad}).  The \emph{modular convolution dg Lie algebra}
\begin{equation}\label{eq:convolution-dg-lie-intro}
\gAmod
\;:=\;
\prod_{g,n}
\Hom_{\Sigma_n}\bigl(
  C_*(\overline{\cM}_{g,n}),\;
  \operatorname{End}_\cA(n)
\bigr)
\end{equation}
is the equivariant internal hom in the category of $\mathbb{S}$-modules (Definition~\ref{def:modular-convolution-dg-lie}).  An element $\alpha \in \gAmod$ assigns to each stable graph~$\Gamma$ of type $(g,n)$ an operation $\alpha_\Gamma \colon \barB(\cA)^{\otimes n} \to \barB(\cA)$, equivariantly for $\operatorname{Aut}(\Gamma)$.  The differential has five components:
\begin{equation}\label{eq:five-component-diff}
D = d_{\mathrm{int}} + [\tau, -] + d_{\mathrm{sew}} + d_{\mathrm{pf}} + \hbar\Delta,
\end{equation}
where $d_{\mathrm{int}}$ is the internal differential of $\cA$, $[\tau,-]$ the twisting by the logarithmic propagator, $d_{\mathrm{sew}}$ the pushforward along codimension-one separating divisors, $d_{\mathrm{pf}}$ the pushforward along non-separating divisors, and $\hbar\Delta$ the self-sewing (BV) operator.  The Lie bracket is operadic composition:
\[
[\alpha, \beta]_\Gamma = \sum_{\Gamma = \Gamma_1 \cup_e \Gamma_2} \alpha_{\Gamma_1} \circ_e \beta_{\Gamma_2} - (-1)^{|\alpha||\beta|} \beta_{\Gamma_1} \circ_e \alpha_{\Gamma_2}.
\]
The identity $D^2 = 0$ is a theorem: it is the codimension-$2$ boundary cancellation $\partial^2 = 0$ on~$\overline{\cM}_{g,n}$ (Lemma~\ref{lem:stable-graph-d-squared}; at the ambient level, Theorem~\ref{thm:ambient-d-squared-zero} via~\cite[Theorem~3.3.1(1)]{Mok25}).

\subsection{The universal Maurer--Cartan element}
\label{subsec:feynman-transform-motivation}
\index{Feynman transform!conceptual motivation}
\index{Chriss--Ginzburg principle!Feynman transform}

The genus-completed bar differential decomposes as $D_\cA = d_\cA^{(0)} + \sum_{g \ge 1} \hbar^g\, d_\cA^{(g)}$.  Define
\begin{equation}\label{eq:universal-MC}
\Theta_\cA
\;:=\;
D_\cA - d_\cA^{(0)}
\;=\;
\sum_{g \geq 1} \hbar^g\, d_\cA^{(g)}
\;\in\;
\gAmod.
\end{equation}
Since $D_\cA^2 = 0$ and $(d_\cA^{(0)})^2 = 0$ (Arnold relation at genus~$0$), expanding $D_\cA^2 = (d_\cA^{(0)} + \Theta_\cA)^2 = 0$ gives
\[
d_\cA^{(0)}\,\Theta_\cA + \Theta_\cA\, d_\cA^{(0)} + \Theta_\cA^2 = 0,
\]
which is the Maurer--Cartan equation
\[
D\,\Theta_\cA + \tfrac{1}{2}[\Theta_\cA, \Theta_\cA] = 0
\]
in~$\gAmod$ (Theorem~\ref{thm:mc2-bar-intrinsic}).  No construction is required: $\Theta_\cA$ is bar-intrinsic.  The positive-genus part of the bar differential \emph{is} a Maurer--Cartan element.

\subsection{Theorems A--D+H as projections}
\label{subsec:def-cyc-mod-intro}
\index{modular cyclic deformation complex|textbf}
\index{Chriss--Ginzburg principle!projection table}

The five main theorems are five projections of the single element $\Theta_\cA \in \operatorname{MC}(\gAmod)$:
\begin{center}
\small
\renewcommand{\arraystretch}{1.35}
\begin{tabular}{@{}lll@{}}
\textbf{Projection} & \textbf{Theorem} & \textbf{Content} \\ \hline
Twisting morphism $\alpha$ & A (Adjunction) &
  $\barB_\kappa \dashv \Omega_\kappa$ on $\operatorname{Fact}(X)$;
  $\mathbb{D}_{\operatorname{Ran}}\barB(\cA) \simeq
  \barB(\cA^!)$ \\[2pt]
Bar-intrinsic $D^2 = 0$ & B (Inversion) &
  $\Omega(\barB(\cA)) \xrightarrow{\sim} \cA$ on Koszul
  locus \\[2pt]
Scalar trace $\pi_{\mathrm{sc}}$ of $\Theta_\cA$ & C (Complementarity)
  & $Q_g(\cA) + Q_g(\cA^!) = H^*(\overline{\cM}_g, Z(\cA))$;
  Lagrangian \\[2pt]
Scalar trace $\pi_{\mathrm{sc}}$ at genus~$1$ & D (Modular char.)
  & $\kappa(\cA)$ universal, additive, $\hat{A}$-genus GF \\[2pt]
Hochschild projection $\pi_{\mathrm{HH}}$ & H (Hochschild)
  & $\ChirHoch^*(\cA)$ polynomial, Koszul-functorial
\end{tabular}
\end{center}
\smallskip

Theorem~A is the existence of the twisting morphism $\alpha\colon \barB(\cA) \to \cA$ underlying $\Theta_\cA$; it is equivalent to the adjunction $\barB_X \dashv \Omega_X$ (Theorem~\ref{thm:bar-cobar-isomorphism-main}).  Theorem~B is the statement that $\alpha$ is a quasi-isomorphism on the Koszul locus---equivalently, that the MC element $\Theta_\cA$ is gauge-trivial at genus~$0$ (Theorem~\ref{thm:bar-cobar-inversion-qi}).  Theorem~C extracts the scalar trace: $\operatorname{tr}(\Theta_\cA) = \sum_{g \ge 1} \kappa(\cA)\,\lambda_g$ decomposes under the Verdier involution $\sigma = \mathbb{D}_{\operatorname{Ran}}$ into complementary Lagrangians $Q_g(\cA) \oplus Q_g(\cA^!)$ (Theorem~\ref{thm:quantum-complementarity-main}).  Theorem~D is the genus-$1$ coefficient: $\kappa(\cA)$ is universal, additive under tensor product, duality-constrained ($\kappa + \kappa' = 0$ for KM/free fields; a nonzero constant for $\mathcal{W}$-algebras), with $\hat{A}$-genus generating function (Theorem~\ref{thm:modular-characteristic}).  Theorem~H projects to the Hochschild sector: $\ChirHoch^*(\cA)$ is polynomial and Koszul-functorial (Theorem~\ref{thm:main-koszul-hoch}).

The shadow tautological classes $\tau_{g,n}(\cA) \in R^*(\overline{\cM}_{g,n+1})$ carry a CohFT structure: the collection $\{\tau_{g,n}(\cA)\}_{g,n}$ forms a cohomological field theory on the tautological ring, with the factorization axiom descending from the clutching law for $\Theta_\cA$ (Theorem~\ref{thm:shadow-cohft}).  The MC equation $D\Theta_\cA + \tfrac{1}{2}[\Theta_\cA, \Theta_\cA] = 0$ projects to tautological relations in $R^*(\overline{\cM}_{g,n})$ at each $(g,n)$ (Theorem~\ref{thm:mc-tautological-descent}): at genus~$0$ one recovers WDVV from the arity-$4$ projection, and at arity~$2$ the genus-$g$ projection recovers Mumford's relation $\lambda_g^2 = 0$ in $R^*(\overline{\cM}_g)$.  The complementarity propagator $P_\cA \colon H^*(\cA) \otimes H^*(\cA^!) \to \mathbb{C}$ is the Givental $R$-matrix of the CohFT, and the Teleman reconstruction theorem recovers the full system $\{\tau_{g,n}\}$ from the genus-$0$ data together with~$P_\cA$ (Theorem~\ref{thm:cohft-reconstruction}).

\subsection{The modular cyclic deformation complex and coefficient algebras}
\label{subsec:coefficient-algebras-intro}
\index{stable-graph coefficient algebra|textbf}
\index{planted-forest coefficient algebra|textbf}

The Maurer--Cartan element $\Theta_\cA$ lives in the \emph{modular cyclic deformation complex} $\Defcyc^{\mathrm{mod}}(\cA) \;\widehat{\otimes}\; \Gmod$ (Definition~\ref{def:modular-cyclic-deformation-complex}): the subcomplex of $\gAmod$ respecting the cyclic symmetry of the bar coalgebra.  The combinatorial heart is controlled by two coefficient algebras.

The \emph{stable-graph coefficient algebra} $\Gmod_{\mathrm{st}}$ (Definition~\ref{def:stable-graph-coefficient-algebra}) models the boundary stratification of $\overline{\cM}_{g,n}$: stable graphs, edge contraction, vertex splitting.  The \emph{planted-forest coefficient algebra} $\Gmod_{\mathrm{pf}}$ (Definition~\ref{def:planted-forest-coefficient-algebra}) encodes residues at the codimension-$3$ strata of the Fulton--MacPherson compactification.  By the tropicalization theorem (Theorem~\ref{thm:planted-forest-tropicalization}), $\Gmod_{\mathrm{pf}} = \operatorname{Trop}(\mathrm{FM}_n(\mathbb{C} | D))$: planted forests are dual graphs of tropical curves under Mok's logarithmic Fulton--MacPherson compactification~\cite{Mok25}.

The differential on $\Defcyc^{\mathrm{mod}}(\cA)$ decomposes as $D_{\mathrm{cyc}} = D_{\mathrm{edge}} + D_{\mathrm{vertex}}$ (edge contraction and vertex splitting).  $D_{\mathrm{cyc}}^2 = 0$ follows from $\partial^2 = 0$ on $\overline{\cM}_{g,n}$.

\subsection{The shadow algebra and the shadow Postnikov tower}
\label{subsec:shadow-algebra-intro}
\label{subsec:shadow-postnikov-tower-intro}
\index{shadow algebra|textbf}

The cohomology
\[
\cA^{\mathrm{sh}}
\;:=\;
H_\bullet\bigl(\Defcyc^{\mathrm{mod}}(\cA)\bigr)
\]
is the \emph{shadow algebra} (Definition~\ref{def:shadow-algebra}): a bigraded commutative ring ($\cA^{\mathrm{sh}}_{r,g}$ at arity~$r$ and genus~$g$) that serves as the universal home for all finite-order modular invariants of~$\cA$.  It factorizes under tensor product ($\cA^{\mathrm{sh}} \cong \cA_1^{\mathrm{sh}} \otimes \cA_2^{\mathrm{sh}}$) and collapses to $\mathbb{C}$ when~$\cA$ has a mass gap.

The finite-order projections of $\Theta_\cA$ form the \emph{shadow Postnikov tower}
\begin{equation}\label{eq:shadow-tower-intro}
\Theta_\cA^{\le 2}
\;\longrightarrow\;
\Theta_\cA^{\le 3}
\;\longrightarrow\;
\Theta_\cA^{\le 4}
\;\longrightarrow\;
\cdots
\;\longrightarrow\;
\Theta_\cA
\;=\;
\varprojlim\, \Theta_\cA^{\le r}
\end{equation}
(Theorem~\ref{thm:recursive-existence}).  Each truncation $\Theta_\cA^{\le r}$ is a graph sum through arity~$r$, with an obstruction class $o_{r+1}(\cA) \in H^*(\Defcyc^{\mathrm{mod},(r+1)}(\cA))$ measuring the failure of extension.  The construction: $\Theta_\cA^{\le 2} = \kappa(\cA) \cdot \eta \otimes \Lambda$; at each arity~$r \ge 3$, the arity-$r$ extension solves the equation $D_{\mathrm{cyc}}(\Theta_\cA^{(r)}) + \frac{1}{2}[\Theta_\cA^{\le r-1}, \Theta_\cA^{\le r-1}]^{(r)} = 0$, and $o_{r+1}$ is the obstruction class.

The arity-$r$ projections extract named invariants:
\begin{center}
\small
\begin{tabular}{lll}
\textbf{Arity} & \textbf{Invariant} & \textbf{Status} \\ \hline
$r = 2$ & $\kappa(\cA)$ (scalar modular characteristic)
  & proved, all families \\[2pt]
$r = 3$ & $\mathfrak{C}(\cA)$ (cubic shadow)
  & proved, computed for standard landscape \\[2pt]
$r = 4$ & $\mathfrak{Q}(\cA)$ (quartic resonance class)
  & proved, with clutching law \\[2pt]
$r = \infty$ & $\Theta_\cA$ (full universal class)
  & proved (bar-intrinsic, Thm~\ref{thm:mc2-bar-intrinsic})
\end{tabular}
\end{center}

The shadow tower classifies chiral algebras into four \emph{shadow archetypes}, according to the termination arity $r_{\max}(\cA) := \min\{r : o_{r+1}(\cA) = 0\}$:
\begin{center}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}ccll@{}}
\textbf{Class} & $r_{\max}$ & \textbf{Archetype} & \textbf{Families} \\ \hline
G & $2$ & Gaussian & Heisenberg, free fermion, lattice (abelian) \\
L & $3$ & Lie/tree & Affine Kac--Moody $\widehat{\mathfrak{g}}_k$, $V_k(\mathfrak{g})$ \\
C & $4$ & Contact/quartic & $\beta\gamma$, $bc$, symplectic bosons \\
M & $\infty$ & Mixed & Virasoro, $\mathcal{W}_N$, non-principal $\mathcal{W}$-algebras
\end{tabular}
\end{center}
Shadow depth classifies the complexity of Koszul algebras, not Koszulness itself: all four classes contain Koszul algebras.  The depth is a modular-homotopy-type invariant (Corollary~\ref{cor:shadow-depth-koszul-invariance}: $r_{\max}(\cA^!) = r_{\max}(\cA)$).

\begin{remark}[Shadow depth as functional-integral complexity]
\label{rem:shadow-archetypes-polyakov}
\index{shadow depth!functional-integral interpretation}
The effective worldsheet action of a chirally Koszul algebra~$\cA$ decomposes by shadow depth:
\[
S_{\mathrm{eff}}(\cA)
\;=\;
\underbrace{\kappa \cdot S_{\mathrm{Polyakov}}}_{r=2}
\;+\;
\underbrace{[\mathfrak{C}] \cdot S_{\mathrm{cubic}}}_{r=3}
\;+\;
\underbrace{[\mathfrak{Q}^{\mathrm{contact}}] \cdot S_{\mathrm{quartic}}}_{r=4}
\;+\;\cdots
\]
The Gaussian class~$(\mathbf{G})$ truncates at $r = 2$: the Polyakov action $S_{\mathrm{Polyakov}} = (1/(4\pi\alpha'))\int \sqrt{g}\,g^{ab}\partial_a X \partial_b X$ captures the full content, and the genus expansion $F_g = \kappa \cdot \lambda_g^{\mathrm{FP}}$ is exact.  Each subsequent class adds one order of nonlinear OPE interaction: tree-level Lie bracket for~$\mathbf{L}$, one-loop contact for~$\mathbf{C}$, all orders for~$\mathbf{M}$.
\end{remark}

The quartic resonance class satisfies a clutching law under stable-curve degeneration (Theorem~\ref{thm:nms-clutching-law-modular-resonance}).  For Virasoro:
\begin{equation}\label{eq:quartic-contact-vir-intro}
Q^{\mathrm{contact}}_{\mathrm{Vir}} = \frac{10}{c(5c+22)},
\qquad
\delta^{(1)}_{H,\mathrm{Vir}} = \frac{120}{c^2(5c+22)} \cdot x^2
\end{equation}
(Theorem~\ref{thm:nms-virasoro-quartic-explicit}).

The shadow Postnikov tower, an a priori infinite sequence of obstruction classes, turns out to be algebraic of degree~$2$ on each primary line (Theorem~\ref{thm:riccati-algebraicity}).  The entire infinite tower is controlled by a single quadratic form $Q_L$---the \emph{shadow metric} (Definition~\ref{def:shadow-metric})---whose Gaussian decomposition $Q_L = (2\kappa + 3\alpha\,t)^2 + 2\Delta\,t^2$ separates the free-field contribution from the interaction correction.  The signature of $Q_L$ classifies the shadow depth: $\Delta = 0$ gives a perfect square and a terminating tower (classes~$\mathbf{G}$ and~$\mathbf{L}$); $\Delta \neq 0$ gives an irreducible quadratic and an infinite but algebraic tower (class~$\mathbf{M}$).  The shadow metric defines a logarithmic connection~$\nabla^{\mathrm{sh}}$ with residue~$\frac{1}{2}$ at the branch points and monodromy~$-1$ (Theorem~\ref{thm:shadow-connection}).  Under Koszul duality, the critical discriminants satisfy a complementarity relation with constant numerator: $\Delta(\cA) + \Delta(\cA^!) = 6960/[(5c{+}22)(152{-}5c)]$.  For multi-channel algebras, the connection acquires curvature equal to the propagator variance~$\delta_{\mathrm{mix}}$ (Proposition~\ref{thm:propagator-variance}), a Cauchy--Schwarz gap that vanishes at curvature-proportionality loci where the quartic gradient is proportional to the curvature vector.

\subsection{The Hamilton--Jacobi equation and inter-channel coupling}
\label{subsec:hamilton-jacobi-intro}
\index{Hamilton--Jacobi equation!shadow tower|textbf}
\index{inter-channel coupling!introduction}

On a multi-generator deformation space $X = H^2_{\mathrm{cyc}}(\cA)$ of dimension~$r \geq 2$, the algebraicity theorem lifts from the Riccati ODE to a \emph{Hamilton--Jacobi PDE}.  Write $U = \sum_{s \geq 3} \mathrm{Sh}_s$ for the nonlinear shadow.  The MC equation on~$X$ is equivalent to
\begin{equation}
\label{eq:hamilton-jacobi-shadow-intro}
  2\,E(U) \;+\; \tfrac{1}{2}\lVert\nabla U\rVert^2_H
  \;=\;
  R,
\end{equation}
where $E = \sum_i x_i \partial_i$ is the Euler operator, $\lVert\nabla U\rVert^2_H = \sum_i P_i\,(\partial_i U)^2$ is the kinetic energy in the inverse-Hessian metric, and $R$ is a polynomial of degree~$\leq 4$ encoding the cubic and quartic OPE inputs (Remark~\ref{rem:riccati-ode}).  On a $1$-D subline, $E(U) = rU$ reduces~\eqref{eq:hamilton-jacobi-shadow-intro} to the Riccati ODE $2tU' + (P/2)(U')^2 = R(t)$.  On the full multi-dimensional space,~\eqref{eq:hamilton-jacobi-shadow-intro} is a classical Hamilton--Jacobi equation with Hamiltonian
\[
  \mathcal{H}(x, p)
  \;=\;
  \tfrac{1}{2}\sum_i P_i\,p_i^2 \;+\; V(x),
\]
where $p_i = \partial U/\partial x_i$ are the shadow momenta and $V$ is the potential from the OPE data.  The shadow Postnikov tower is the Hamilton principal function of this mechanical system: its graph $\{(x, \nabla U(x))\}$ is a Lagrangian submanifold of~$T^*(X)$.  The genus expansion $\sum_{g \geq 1} F_g(\cA)\,\hbar^g$ formally resembles a WKB series with $U$ as the classical action, but the quantised Hamiltonian and Schr\"odinger equation needed to make this precise have not been constructed (Remark~\ref{rem:shadow-hamiltonian}).

The Hamilton--Jacobi structure explains the \emph{inter-channel coupling} on sublines (Proposition~\ref{prop:interchannel-coupling}).  On a subline $L \subset X$, the restriction $U|_L$ satisfies the $1$-D Riccati equation \emph{plus} a correction from the transverse kinetic energy $(P_\perp/2)\,(\partial_\perp U)^2|_L$: the ``transverse momentum'' $p_\perp = (\partial_\perp U)|_L$ need not vanish.  For $\mathcal{W}_3$ on the $T$-line ($x_W = 0$), conformal weight forces $p_W|_{x_W = 0} = 0$: autonomy.  On the $W$-line ($x_T = 0$), the cubic shadow $\mathrm{Sh}_3 = 2x_T^3 + 3x_T x_W^2$ has nonzero normal derivative $(\partial_{x_T}\mathrm{Sh}_3)|_{x_T=0} = 3x_W^2$: the transverse kinetic energy is nonzero, and the coupling correction first enters at arity~$6$:
\[
  \delta_6^{W\text{-line}}
  \;=\;
  \frac{576(5c+38)}{c^3(5c+22)^3}.
\]
The autonomous $W$-line recursion misses this.  The coupling correction vanishes on any direction where the propagator variance $\delta_{\mathrm{mix}} = 0$.

For the shadow generating function on each primary line, the pole-purity theorem (Proposition~\ref{prop:pole-purity}) gives
$S_r = \varepsilon_r P_r(c) / [c^{r-3}(5c{+}22)^{\lfloor(r-2)/2\rfloor}]$
for $r \geq 4$, with $P_r \in \mathbb{Z}[c]$ of degree $\lfloor(r-4)/2\rfloor$ and no factors beyond $c$ and $5c{+}22$ in the denominator at any arity---a direct consequence of the Riccati algebraicity and the Kac-determinant structure of the quasi-primary spectrum.

\subsection{The all-arity master equation}
\label{subsec:all-arity-master-eq}

The arity-$r$ projection of the MC equation is the \emph{all-arity master equation}
\begin{equation}\label{eq:all-arity-master-eq-intro}
\nabla_H(\mathrm{Sh}_r) + o^{(r)} = 0 \qquad \text{for all } r \geq 3,
\end{equation}
where $\nabla_H$ is the covariant derivative in the Hodge direction, $\mathrm{Sh}_r$ is the shadow at arity~$r$, and $o^{(r)}$ is the obstruction class.  The genus loop $\Lambda_P \colon \operatorname{Sym}^r \to \operatorname{Sym}^{r-2}$ governs the descent from arity~$r$ to arity $r-2$ by trace contraction; its kernel consists of the primitive shadows.

The \emph{primitive logarithmic modular kernel} $\mathfrak{K}_\cA$ (Definition~\ref{def:primitive-log-modular-kernel}) is the restriction of the modular homological vector field $Q^{\mathrm{mod}}_\cA$ to primitive corollas and rigid strata.  By cofree-coderivation correspondence, $\Theta_\cA = \operatorname{FT}^{\log}_{\mathrm{mod}}(\mathfrak{K}_\cA)$, and the fundamental equivalence
\[
(Q^{\mathrm{mod}}_\cA)^2 = 0
\quad\iff\quad
d\,\mathfrak{K}_\cA + \mathfrak{K}_\cA \star \mathfrak{K}_\cA + \hbar\,\Delta_{\mathrm{ns}}(\mathfrak{K}_\cA) = 0
\]
(Theorem~\ref{thm:primitive-to-global-reconstruction}), where $\star$ is the primitive pre-Lie product from log-FM residues and $\Delta_{\mathrm{ns}}$ is the non-separating loop operator.  The three terms replace the genus-by-genus obstruction programme by a single equation on primitive data.

\subsection{The Feynman transform: commutative and associative}
\label{subsec:feynman-transform-intro}
\index{'t Hooft expansion}

The convolution algebra~\eqref{eq:convolution-dg-lie-intro} admits two modular operadic inputs:

\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Commutative.}\enspace
  $\Com(g,n) = k$ for all stable $(g,n)$.
  $\mathrm{FCom}$ is the modular cooperad of
  unlabelled stable graphs.  The bar complex is an $\mathrm{FCom}$-algebra
  (Theorem~\ref{thm:bar-modular-operad}), and
  $\Theta_\cA \in \operatorname{MC}(\gAmod)$
  is the full genus tower.

\item \emph{Associative.}\enspace
  $\Ass(g,n) = C_*(\overline{\cM}_{g,n}^{\,\mathrm{rib}})$,
  the ribbon (fatgraph) modular operad
  (Definition~\ref{def:ribbon-modular-operad}).
  $F\!\Ass$ is generated by
  ribbon graphs---stable graphs with cyclic half-edge
  orderings, equivalently graphs embedded in
  oriented surfaces.
  The ordered bar complex is an $F\!\Ass$-algebra
  (Chapter~\ref{chap:e1-modular-koszul}), and
  the $E_1$ MC element
  $\Theta_\cA^{E_1} \in
  \operatorname{MC}(\mathfrak{g}_\cA^{E_1})$
  is the ordered genus tower.
  The $\Sigma_n$-coinvariant map $F\!\Ass \to \mathrm{FCom}$
  (Theorem~\ref{thm:fcom-coinvariant-fass})
  sends $\Theta_\cA^{E_1} \mapsto \Theta_\cA$:
  the commutative theory is the unordered projection
  of the associative theory.
\end{enumerate}

The second case is the algebraic form of the 't~Hooft large-$N$ expansion.  The perturbative free energy
\begin{equation}
\label{eq:thooft-genus-expansion}
\log Z
\;=\;
\sum_{g=0}^\infty N^{2-2g}\,F_g(\lambda)
\;=\;
\sum_{\Gamma\;\text{ribbon}}
\frac{N^{2-2g(\Gamma)}}{|\operatorname{Aut}(\Gamma)|}
\,W_\Gamma(\lambda)
\end{equation}
is a sum over ribbon graphs weighted by $\chi(\Gamma) = 2 - 2g(\Gamma)$, where $\lambda = g_{\mathrm{YM}}^2 N$.  The MC equation $D\Theta^{E_1} + \tfrac{1}{2}[\Theta^{E_1},\Theta^{E_1}] = 0$ encodes the entire 't~Hooft genus expansion; at genus~$0$, it reduces to the CYBE for the spectral $R$-matrix; at genus~$1$, to the KZ associator.

\subsection{The formality principle}
\label{subsec:formality-principle-intro}
\index{formality!rectification principle}
\index{rectification!as meta-theorem}

The convolution dg Lie algebra~$\gAmod$ is a \emph{strict model} of an underlying $L_\infty$-algebra $\Convinf(\mathrm{FCom},\, \operatorname{End}_{\barB(\cA)})$; the two are related by a quasi-isomorphism of $L_\infty$-algebras (Chapter~\ref{app:homotopy-transfer}), and their MC moduli coincide.

\begin{theorem}[Rectification bridge]
\label{thm:rectification-bridge}
\ClaimStatusProvedHere
\index{rectification!bridge theorem|textbf}
\index{formality!rectification bridge|textbf}
The three rectification principles are:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Operadic}: the homotopy chiral algebra $\cA^{Ch_\infty}$ rectifies to a strict chiral algebra $\cA$ via bar-cobar (Theorem~\ref{thm:bar-cobar-inversion-qi}).
\item \emph{Convolution}: the $L_\infty$-algebra $\Convinf(\mathrm{FCom}, \operatorname{End}_{\barB(\cA)})$ and the dg Lie algebra $\Convstr(\barB(\cA), \cA)$ carry quasi-isomorphic MC moduli.
\item \emph{Reduction}: the derived DS functor $\mathbf{R}\mathrm{DS}_f$ and the classical functor $H^0_{Q_{\mathrm{DS}}}$ agree on Koszul algebras at generic level (Proposition~\ref{prop:ds-bar-formality}).
\end{enumerate}
\end{theorem}

The rectification principle---the passage from homotopy-coherent to strict structures---is the meta-theorem underlying all five main theorems:
\begin{enumerate}[label=\textup{(\alph*)}]
\item Bar-cobar preserves quasi-isomorphisms because it is a quantum $L_\infty$ functor: it respects the full homotopy package, not merely the strict dg structure.  This gives Theorem~B.
\item The transferred $A_\infty$ and $L_\infty$ structures on bar cohomology (homotopy transfer theorem, Chapter~\ref{app:homotopy-transfer}) yield the shadow obstructions: at genus~$0$, $o_{r+1}|_{g=0} = [m_{r+1}]$ (Proposition~\ref{prop:shadow-massey-identification}).  Formality ($m_n = 0$ for $n \ge 3$) is the Gaussian archetype.
\item Drinfeld--Sokolov reduction commutes with bar-cobar by formality: the classical DS functor is the rectification of the derived DS functor, and the shadow tower intertwines: $\Theta_{\mathcal{W}_N} = \operatorname{DS}(\Theta_{\widehat{\mathfrak{g}}})$ at the level of MC elements.
\item The dg Lie algebra is a faithful chart on the formal moduli problem $\cM^{\mathrm{mod}}_\cA$; every strict-model formula is a projection of $\Theta_\cA \in \MC(\Convinf)$.  The space of models is contractible (Proposition~\ref{prop:model-independence}).
\end{enumerate}

\subsection{The platonic package and the modular envelope adjunction}
\label{subsec:platonic-adjunction-intro}
\index{platonic package|textbf}
\index{modular factorization envelope|textbf}

The canonical input for the modular Koszul machine is a \emph{cyclically admissible} Lie conformal algebra~$L$ (Definition~\ref{def:cyclically-admissible}): conformal grading, filtration, bounded poles, invariant pairing.  From~$L$, the \emph{platonic package} $\Pi_X(L)$ (Construction~\ref{constr:platonic-package}) assembles six pieces:
\[
\Pi_X(L) =
\bigl(
\operatorname{Fact}_X(L),\;
\barB_X(L),\;
\Theta_L,\;
\mathcal{L}_L,\;
(V^{\mathrm{br}}_L, T^{\mathrm{br}}_L),\;
\mathfrak{R}^{\mathrm{mod}}_4(L)
\bigr),
\]
the factorization algebra, bar coalgebra, universal MC class, Lagrangian, branch-line data, and quartic resonance class.

The modular factorization envelope $U^{\mathrm{mod}}_X$ (Theorem~\ref{thm:platonic-adjunction}) is the left adjoint of the modular primitive-current functor $\operatorname{Prim}^{\mathrm{mod}}$:
\[
U^{\mathrm{mod}}_X
\;\dashv\;
\operatorname{Prim}^{\mathrm{mod}}
\colon
\operatorname{Alg}_{\mathrm{aug}}(\operatorname{Fact}^{\mathrm{mod}}(X))
\longrightarrow
\operatorname{LCA}^{\mathrm{cyc}}.
\]
For independent sums $L = L_1 \oplus L_2$ with vanishing mixed OPE, all shadows separate: $\kappa$ additive, $T^{\mathrm{br}}$ direct sum, $\Delta$ multiplicative, $\mathfrak{R}^{\mathrm{mod}}_4$ additive (Proposition~\ref{prop:independent-sum-factorization}).  Cubic gauge triviality (Theorem~\ref{thm:cubic-gauge-triviality}): if $H^1(F^3\gAmod/F^4\gAmod, d_2) = 0$, the cubic MC term is gauge-trivial and the quartic class $[\Theta'_4] \in H^2(F^4\gAmod/F^5\gAmod, d_2)$ is canonical.

\section{The modular Koszul object}
\label{sec:modular-koszul-object}

A \emph{modular Koszul chiral algebra}
(Definition~\ref{def:modular-koszul-chiral}) is a factorization
algebra on $\operatorname{Ran}(X)$ whose duality data extends over
the modular operad, with curvature classes compatible under
clutching, Verdier duality, and trace.  Theorems~A--D are four
projections of $\Theta_\cA$; the modular Koszul structure is the
ambient datum from which they are extracted.  The full package
\[
\mathcal{C}_\cA
=
(\Theta_\cA, \kappa(\cA), \Delta_\cA, \Pi_\cA, \mathcal{H}_\cA)
\]
is a hierarchy of increasing refinement
(Definition~\ref{def:full-modular-package}).
Theorem~\ref{thm:volume-one-concrete-modular-datum} packages this
hierarchy into explicit H-, M-, and S-level data.

The genus tower $\{\barB^{(g)}(\cA)\}_{g \geq 0}$ is a
Maurer--Cartan deformation of the genus-$0$ bar differential
inside a cyclic $L_\infty$ algebra
(Definition~\ref{def:cyclic-deformation-elementary}).  The proved
families include free fields
(Proposition~\ref{prop:standard-examples-modular-koszul}), affine
Kac--Moody at all genera
(Theorem~\ref{thm:pbw-allgenera-km},
Corollary~\ref{cor:unconditional-allgenera-km}), Virasoro
(Theorem~\ref{thm:pbw-allgenera-virasoro},
Corollary~\ref{cor:unconditional-allgenera-virasoro}), and
principal $\mathcal{W}$-algebras
(Theorem~\ref{thm:pbw-allgenera-principal-w},
Corollary~\ref{cor:unconditional-allgenera-principal-w}).

Scalar saturation---the identity
$\Theta_\cA^{\min} = \kappa(\cA) \cdot \eta \otimes \Lambda$
(Corollary~\ref{cor:scalar-saturation})---holds whenever
$\dim H^2_{\mathrm{cyc}}(\cA,\cA) = 1$.
Theorem~\ref{thm:algebraic-family-rigidity} extends it
to all non-critical levels for algebraic families with rational
OPE coefficients;
Conjecture~\ref{conj:scalar-saturation-universality} addresses
the residual non-algebraic-family case.

\begin{definition}[Modular homotopy theory on a smooth curve]
\label{def:modular-homotopy-theory-intro}
\index{modular homotopy theory!definition|textbf}
A \emph{modular homotopy theory} for a chiral algebra~$\cA$
on a smooth projective curve~$X$ consists of the following data:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Bar-cobar adjunction.}
  An adjunction
  $\barB_X \dashv \Omega_X$ on
  $D^{\mathrm{co}}(\operatorname{Fact}(X))$, the coderived
  category of factorization coalgebras on
  $\operatorname{Ran}(X)$, with $\barB_X(\cA)$ nilpotent at
  genus~$0$ and curved at higher genus.
\item \emph{Modular functoriality.}
  Functoriality of the bar family
  $\{\barB^{(g)}(\cA)\}_{g \geq 0}$ over the modular operad
  $\{\overline{\mathcal{M}}_{g,n}\}$, intertwined with Verdier
  duality:
  $\mathbb{D}_{\operatorname{Ran}}\barB^{(g)}_X(\cA)
  \simeq \barB^{(g)}_X(\cA^!)$.
\item \emph{Universal Maurer--Cartan class.}
  A class
  $\Theta_\cA \in
  \operatorname{MC}(\Defcyc^{\mathrm{mod}}(\cA)
  \;\widehat{\otimes}\;
  R\Gamma(\overline{\mathcal{M}}_{g,\bullet},\,
  \mathbb{Q}))$
  assembling the genus tower into a single integrability datum,
  compatible with clutching, trace, and duality.
\item \emph{Complementary Lagrangians.}
  A $(-(3g{-}3))$-shifted symplectic form on the moduli of
  genus-$g$ bar families, with
  $Q_g(\cA)$ and $Q_g(\cA^!)$ as complementary Lagrangians.
\item \emph{Chain-level modular functor.}
  The Feynman transform assembles the genus-graded bar complex
  $\barB^{\mathrm{full}}(\cA) = \bigoplus_g \barB^{(g)}(\cA)$
  into a homotopy-coherent modular functor
  (Theorem~\ref{thm:chain-modular-functor}): to each
  surface~$\Sigma_{g,n}$ a cochain complex, to each degeneration a
  chain map, to each consistency relation a chain homotopy.
  Passing to $H^0$ at integrable level recovers conformal blocks.
  The scalar genus tower
  $F_g = \kappa(\cA) \cdot \lambda_g^{\mathrm{FP}}$
  (Theorem~D) is the first Chern class of the determinant line
  bundle of this functor---the trace of the construction, not
  the construction itself.
\end{enumerate}
Theorems~A--D are the first four assertions; item~(v) is the
structure they build toward, proved in
Theorem~\ref{thm:chain-modular-functor}.
The formal axiomatic treatment is
Definition~\ref{def:modular-koszul-homotopy}; the concrete dg model is
Definition~\ref{def:modular-koszul-chiral}.
\end{definition}

\begin{proposition}[Classification by modular homotopy type;
\ClaimStatusProvedHere]
\label{prop:modular-homotopy-classification}
\index{modular homotopy type!classification}
Two chiral algebras~$\cA$ and~$\cA'$ on~$X$ have equivalent
modular homotopy types---i.e., the formal moduli problems
$\cM^{\mathrm{mod}}_\cA \simeq \cM^{\mathrm{mod}}_{\cA'}$---if
and only if their modular bar constructions are connected by a zigzag
of quasi-isomorphisms of modular-operad coalgebras.  The modular
homotopy type is therefore the finest invariant that the bar
construction can see, and coarser than isomorphism: it remembers
the deformation-obstruction pattern and forgets the specific
multiplication tables.
\end{proposition}

\begin{proof}
By the Lurie--Pridham correspondence between $L_\infty$-algebras
and formal moduli problems
(cf.~\cite[Theorem~2.0.2]{Pridham17}), an equivalence
$\cM^{\mathrm{mod}}_\cA \simeq \cM^{\mathrm{mod}}_{\cA'}$
is equivalent to a quasi-isomorphism
$\gAmod[\cA] \xrightarrow{\sim} \gAmod[\cA']$ of
$L_\infty$-algebras.  By the convolution identification
$\gAmod = \Convinf(\cC^{\log\mathrm{FM}}_{\mathrm{mod}},\,
\operatorname{End}_{\mathrm{Ch}_\infty}(\cA))$, a
quasi-isomorphism of convolution algebras is induced by a
quasi-isomorphism of the modular bar constructions
$\barB^{\mathrm{mod}}_X(\cA) \xrightarrow{\sim}
\barB^{\mathrm{mod}}_X(\cA')$ as modular-operad coalgebras
(since the cooperad slot is common).  The converse holds by the
representability of twisting morphisms
(Theorem~\ref{thm:completed-twisting-representability} in the
completed regime).
\end{proof}

\begin{example}[Modular homotopy type distinguishes families]
\label{ex:modular-type-distinguishes}
The Heisenberg algebra $\cH_k$ and the affine algebra
$\widehat{\mathfrak{sl}}_2{}_k$ have the same underlying graded
vector space $\bigoplus_{n \ge 1} \Bbbk\cdot\partial^{n-1}\alpha$
(respectively $\bigoplus_{n \ge 1} \Bbbk^3\cdot\partial^{n-1}J^a$)
in the same conformal weight range.  At genus~$0$, the Heisenberg
bar complex has $d_B = 0$ (the bracket is abelian); the
$\widehat{\mathfrak{sl}}_2$ bar complex has
$d_B(sJ^a | sJ^b) = f^{ab}{}_c\,sJ^c$ (the Lie bracket).  The
$A_\infty$-minimal model of the Heisenberg has $m_n = 0$ for all
$n \ge 2$; the minimal model of $\widehat{\mathfrak{sl}}_2$ has
$m_2 = $ the Lie bracket and $m_3 = $ the Jacobiator homotopy $F_3$
on $\overline{\cM}_{0,4} \cong \mathbb{P}^1$.

The modular homotopy types are therefore inequivalent:
$\gAmod[\cH_k]$ has $\ell_n = 0$ for $n \ge 3$
(Gaussian archetype, shadow depth~$2$), while
$\gAmod[\widehat{\mathfrak{sl}}_2{}_k]$ has
$\ell_3 \ne 0$ (Lie/tree archetype, shadow depth~$3$).  The cubic
shadow $\mathfrak{C}(\widehat{\mathfrak{sl}}_2{}_k)$---the
arity-$3$ component of $\Theta_\cA$---is the graph sum over
trivalent trees, encoding the Lie bracket's contribution to the
modular bar construction.  No quasi-isomorphism of modular bar
coalgebras can kill it, because it represents a nontrivial class in
$H^*(\Defcyc^{(3)}(\cA)|_{g=0})$
(Proposition~\ref{prop:shadow-massey-identification}).

Shadow depth is therefore a modular-homotopy-type invariant.  More
precisely: the $L_\infty$-minimal model of $\gAmod$ has
$\ell_r^{\mathrm{min}} = 0$ for $r < $ shadow depth and
$\ell_r^{\mathrm{min}} \ne 0$ at $r = $ shadow depth; this cannot
change under quasi-isomorphism.
\end{example}

\begin{proposition}[Genus-$0$ shadow obstructions $=$ $A_\infty$
Massey products; \ClaimStatusProvedHere]
\label{prop:shadow-massey-identification}
\index{Sullivan dictionary|textbf}
\index{Massey product!shadow obstruction identification}
\index{shadow obstruction!as Massey product}
Let $\cA$ be a chiral algebra on~$X$ with bar
complex~$\barB(\cA)$, and let $(H^*(\barB(\cA)), m_2, m_3,
m_4, \ldots)$ be the $A_\infty$-minimal model obtained by
homotopy transfer
\textup{(}Proposition~\textup{\ref{prop:ainfty-formality-implies-koszul}}\textup{)}.
Then the genus-$0$ shadow obstructions and the $A_\infty$-Massey
products coincide:
\begin{equation}\label{eq:shadow-massey}
o_{r+1}(\cA)\big|_{g=0}
\;=\;
[m_{r+1}]
\;\in\;
H^*\bigl(\Defcyc^{(r+1)}(\cA)\big|_{g=0}\bigr)
\qquad \text{for all } r \ge 2.
\end{equation}
In particular:
\begin{enumerate}[label=\textup{(\roman*)}]
\item $m_n = 0$ for all $n \ge 3$ if and only if the genus-$0$
  shadow tower terminates at arity~$2$---the Gaussian archetype.
\item The shadow depth of~$\cA$ at genus~$0$ equals the
  $A_\infty$-depth of the bar cohomology: the smallest~$r$ such
  that $m_r \ne 0$ equals the smallest arity at which the shadow
  tower has a nontrivial obstruction.
\end{enumerate}
\end{proposition}

\begin{proof}
The $A_\infty$-products $m_n$ on $H^*(\barB(\cA))$ are the
Taylor coefficients of the transferred twisting morphism
$\tau^{\mathrm{tr}}\colon H^*(\barB(\cA)) \to \cA$ through the
deformation retract $(\barB(\cA), H^*(\barB(\cA)), i, p, h)$.
The genus-$0$ shadow obstructions $o_{r+1}|_{g=0}$ are the
obstructions to extending the truncated MC element
$\Theta_\cA^{\le r}|_{g=0}$ by one arity in the genus-$0$
sector of $\Defcyc(\cA)$.  But the genus-$0$ sector of the
modular convolution algebra is the \emph{operadic} convolution
algebra $\mathrm{Hom}(\barB_{\mathrm{op}}(\mathrm{Com}),
\mathrm{End}_\cA)$, and the MC equation there is exactly the
$A_\infty$-structure equation.  The identification~\eqref{eq:shadow-massey}
follows from the standard comparison between the operadic
bar-cobar obstruction tower and the $A_\infty$-Massey product
tower (cf.~\cite[Theorem~3.4.1]{LPWZ09}).
\end{proof}

\begin{remark}[The Sullivan dictionary]
\label{rem:sullivan-dictionary}
\index{rational homotopy type!modular analogue}
The identification of Proposition~\ref{prop:shadow-massey-identification}
is the genus-$0$ case of a structural parallel between Sullivan's
rational homotopy theory and the modular homotopy theory of
Definition~\ref{def:modular-homotopy-theory-intro}:
\begin{center}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{lll}
\textbf{Rational (Sullivan)} && \textbf{Modular (this monograph)} \\
\hline
topological space~$X$
  && chiral algebra~$\cA$ on~$X$ \\[2pt]
de~Rham algebra $\Omega^*(X)$
  && modular convolution $\gAmod$ \\[2pt]
Sullivan minimal model
  && $L_\infty$-minimal model of $\gAmod$ \\[2pt]
Massey products $m_n$
  && shadow obstructions $o_{r+1}$ (Prop.~\ref{prop:shadow-massey-identification}) \\[2pt]
formality: $m_n = 0$ ($n \ge 3$)
  && Gaussian: shadow depth~$2$ \\[2pt]
K\"ahler $\Rightarrow$ formal
  && Heisenberg $\Rightarrow$ Gaussian \\[2pt]
non-K\"ahler, non-formal
  && Virasoro: infinite shadow depth \\[2pt]
Massey cascade from self-map
  && self-referential OPE: $a \in a_{(k)}a$
\end{tabular}
\end{center}
\smallskip
\noindent
The modular upgrade replaces genus-$0$ graphs (trees) by all-genera
graphs, Hochschild cochains by modular-cyclic cochains, and
$A_\infty$-obstructions by modular $L_\infty$-obstructions.  The
formal structure---controlling $L_\infty$-algebra, Maurer--Cartan
moduli, obstruction tower, formality criterion---is unchanged.

The dictionary extends to arithmetic content.  For lattice vertex
algebras, the shadow tower detects the Hecke eigenform decomposition
of the theta function: the obstruction at each arity resolves one
Hecke eigenspace, and the depth counts them
(Theorem~\ref{thm:shadow-spectral-correspondence}).  The depth
decomposition $d = 1 + d_{\mathrm{arith}} + d_{\mathrm{alg}}$
(Theorem~\ref{thm:depth-decomposition}) separates the
\emph{arithmetic} invariant $d_{\mathrm{arith}}$ (Hecke eigenform
count, visible through $L$-functions and critical lines) from the
\emph{homotopy-theoretic} invariant $d_{\mathrm{alg}}$ (formal
non-formality depth, visible through Massey products and
$A_\infty$ structure).  For lattice algebras:
$d_{\mathrm{alg}} = 0$ (the shadow tower is purely arithmetic).
For Virasoro minimal models: $d_{\mathrm{arith}}$ is finite but
$d_{\mathrm{alg}} = \infty$ (the shadow tower detects the
\emph{algebra itself}---the self-referential OPE $T\in T_{(1)}T$---not
its representation theory).  The shadow Postnikov tower is the
universal invariant that encodes both the Langlands programme
(arithmetic) and the Sullivan programme (homotopy theory) in a
single Maurer--Cartan element~$\Theta_\cA$.

The arithmetic content extends beyond the shadow tower.
The connected sewing amplitudes $a_\cA(N)$ define a
\emph{sewing inner product} on Dirichlet polynomials
(\S\ref{subsec:sewing-gram-matrix}), whose reproducing
kernel is the \emph{sewing kernel}
$K_\cA(s,t) = S_\cA(s{+}t) =
\zeta(s{+}t{+}1)\sum_i(\zeta(s{+}t) - H_{w_i-1}(s{+}t))$.
For Heisenberg, $K_\cH = \zeta\cdot\zeta$: a pure Euler
product.  For Virasoro,
$K_{\mathrm{Vir}} = \zeta(u{+}1)(\zeta(u){-}1)$: the
weight-$1$ mode is removed.
The surface moment matrix $\mathscr{M}_{ij} =
K_\cA(\alpha/2{+}i,\,\alpha/2{+}j)$ is a Gram matrix
$V^T\!DV$ with positive weights, hence
$\mathscr{M} \succ 0$ (Theorem~\ref{thm:gram-positivity}).
The shadow tower provides interacting corrections to this
kernel.  When all spectral atoms $\lambda_j$ of the shadow
spectral measure are negative (which holds for all unitary
theories with $c > 0$), the interacting Gram matrix is
unconditionally positive definite
(Theorem~\ref{thm:interacting-gram-positivity}(i));
the shadow resonance locus is confined to
non-unitary $c < 0$
(Theorem~\ref{thm:shadow-resonance-locus}).
The Euler--Koszul class
$\operatorname{ek}(\cA) = \max_i(w_i - 1)$
and shadow depth $\kappa_d(\cA)$ are independent invariants
(Theorem~\ref{thm:shadow-euler-independence}):
the first reads the character, the second the OPE.

The \emph{Sewing--Hecke reciprocity} theorem
(Theorem~\ref{thm:sewing-hecke-reciprocity}) shows that the
two-variable $L$-object
$L_\cA(s,u) := \int_\cF^{\mathrm{reg}}
F_\cA^{\mathrm{conn}}\,E^*(\tau,s)\,y^{u-2}\,dx\,dy$
\emph{collapses}:
$L_\cA(s,u) = \Gamma(v)(2\pi)^{-v}S_\cA(v)$
with $v = s + u - 1$.
The zero-side Rankin--Selberg variable~$s$ and the
prime-side Dirichlet variable~$u$ are locked together by
the Mellin transform of the sewing free energy.
The Sewing--Selberg formula ($u = 0$) and the
Dirichlet--sewing lift ($s = 1$) are two faces of the same
Mellin transform.  The quartic
resonance class~$\mathfrak{Q}(\cA)$ is the Schur complement
of the $3 \times 3$ Hankel determinant of the mixed
$(s,u)$-transform, stripping quadratic and cubic contamination:
for Virasoro, $\Sigma_2 = 10/[c(5c{+}22)] = Q_{\mathrm{Vir}}^{\mathrm{ct}}$
exactly (Chapter~\ref{chap:arithmetic-shadows}).
\end{remark}

\begin{construction}[Arithmetic shadow chain]
\label{constr:arithmetic-shadow-chain}
\index{arithmetic shadow chain|textbf}
\index{Euler--Koszul class!in arithmetic chain}
The shadow Postnikov tower connects modular Koszul duality to
arithmetic $L$-functions via a five-step chain:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Sewing $\to$ partition function}: the HS-sewing
  criterion (Theorem~\ref{thm:general-hs-sewing}) gives convergent
  genus-$g$ amplitudes $Z_g(\cA, q)$ for Koszul algebras.
\item \emph{Partition function $\to$ eta product}: for lattice VOAs,
  $Z_1(V_\Lambda, q) = 1/\eta(\tau)^{\operatorname{rank}(\Lambda)}$,
  encoding the modular characteristic
  $\kappa = \operatorname{rank}(\Lambda)/2$.
\item \emph{Eta product $\to$ zeta data}: the Euler product structure
  of~$\eta$ decomposes $Z_1$ into prime-indexed factors; the sewing
  Dirichlet series
  $S_\cA(u) = \sum_{n \geq 1} a_n n^{-u}$
  extracts the arithmetic content.
\item \emph{Zeta data $\to$ bar complex}: the Euler--Koszul class
  $\operatorname{ek}(\cA) \in K_0(\mathrm{EK})$ classifies the
  relationship between sewing data and bar cohomology generating
  functions.
\item \emph{Bar complex $\to$ $L$-function}: the shadow generating
  function $G_\cA(t) = \sum_{n} \dim H^n(B(\cA))\, t^n$ and the
  sewing Dirichlet series $S_\cA(u)$ are related by a Mellin-type
  integral transform (Approach~A, proved for lattice VOAs).
\end{enumerate}
The Euler--Koszul three-tier classification
(Definition~\ref{def:euler-koszul-tier}) partitions chiral algebras
into: exact ($\operatorname{ek} = 0$, lattice VOAs), finitely
defective ($\operatorname{ek}$ finite, affine), and non-EK
($\operatorname{ek}$ infinite, Virasoro).
For lattice VOAs, this chain is complete (Stages~1--3 proved).
For non-lattice algebras, Stage~3 (Hecke decomposition) encounters the
non-multiplicativity obstruction: the sewing Dirichlet series is not a
Hecke eigenform in general.  The shadow-$L$ correspondence
$d(\cA) = 1 + \#\text{$L$-function factors}$ holds for all tested
lattice VOAs but fails for~$\beta\gamma$ (shadow depth~$4$, but
$1$~$U(1)$ critical line).
\end{construction}

\subsection{Completion kinematics versus modular dynamics}
\label{subsec:kinematics-dynamics-intro}
\index{completion kinematics|textbf}
\index{modular dynamics|textbf}
\index{primitive cumulant!in introduction}

The package~$\mathcal{C}_\cA$ encodes the \emph{modular dynamics}
of~$\cA$: the OPE-dependent deformation data $D_\cA$,
$\kappa(\cA)$, $\Theta_\cA$, and the dg-shifted Yangian kernel
$r_\cA(z)$.  But the completed bar-cobar theory also carries a
second, independent axis that we call \emph{completion kinematics}:
invariants determined solely by the vacuum character $\chi_\cA(q)$,
without reference to OPE coefficients or curvature.

The kinematic data are extracted from the \emph{primitive cumulant
quotient} $Q(\cA) = \bar{\cA}/\partial\bar{\cA}$
(Definition~\ref{def:primitive-cumulant-quotient}) and its generating
series
\begin{equation}\label{eq:primitive-generating-intro}
G_\cA(t) = \frac{1-t}{t}\bigl(\chi_\cA(t) - 1\bigr),
\end{equation}
which is canonical: the non-derivative sector is recovered directly
from the character.  The \emph{completion Hilbert series}
$H_\cA(t) = G_\cA/(1 - G_\cA)$ counts reduced-weight bar words, and
the \emph{completion entropy}
$h_K(\cA) = \log(\rho_\cA^{-1})$
(where $G_\cA(\rho_\cA) = 1$) is the exponential growth rate of
finite bar windows
(Definitions~\ref{def:completion-hilbert-series}--\ref{def:completion-entropy}).

The two axes are genuinely independent.  The Virasoro involution
$c \mapsto 26 - c$ changes the dynamic shadow
$\mathrm{Vir}_c \leftrightarrow \mathrm{Vir}_{26-c}$ while
preserving the kinematic data: both sides have the same
$G_\cA(t)$ and the same entropy.  The shadow depth classification
(G/L/C/M) measures \emph{dynamic} complexity; the completion entropy
$h_K$ measures \emph{kinematic} complexity.  Both are needed.

The \emph{primitive defect series}
$\Delta_\cA(t) = G_\cA(t) - G_\cA^{\mathrm{naive}}(t)$
(Definition~\ref{def:primitive-defect-series})
detects nonquadraticity: it is zero for Heisenberg, affine
Kac--Moody, and $\beta\gamma$ (all quadratic or free-like), and
nonzero starting at $t^3$ for Virasoro and $\mathcal{W}_N$ ($N
\geq 3$).  The nonzero defect means the completed dual coalgebra is
forced to enlarge its primitive sector beyond the declared
generators---the correct dual is the \emph{cumulant coalgebra}
$\operatorname{Cum}_c(\cA) = \hat{T}^c(sQ(\cA)^\vee)$, not the
tensor coalgebra on the original generators alone
(Conjecture~\ref{conj:cumulant-recognition}).

\section{The standard families and their shadow archetypes}
\label{sec:standard-families-intro}
\index{shadow archetypes|textbf}
\index{standard families|textbf}

The abstract machinery of the shadow Postnikov tower acquires its full meaning only when exhibited on the standard families of chiral algebras.  Each family determines a \emph{shadow archetype}: the termination pattern of its tower and the qualitative shape of its modular invariants.  In parallel, the completion kinematics
(\S\ref{subsec:kinematics-dynamics-intro}) assigns to each family a
primitive spectrum~$G_\cA(t)$, an entropy~$h_K(\cA)$, and a
defect~$\Delta_\cA(t)$.  These two classifications---dynamic
(shadow depth) and kinematic (completion entropy)---are orthogonal:
a family can have finite shadow depth and zero defect (Heisenberg,
Kac--Moody, $\beta\gamma$) or infinite shadow depth and nonzero
defect (Virasoro, $\mathcal{W}_N$), and the entropy varies
continuously within each class.

\subsection{The Heisenberg atom: Gaussian archetype, termination at arity~$2$}
\label{subsec:heisenberg-atom-intro}
\index{Heisenberg algebra!as atom of the programme|textbf}
\index{Gaussian archetype}

The Heisenberg algebra $\mathcal{H}_k$ is the place where the
entire programme collapses to a single glance.  Its OPE is
$\alpha(z)\alpha(w) \sim k/(z-w)^2$: a double pole, no simple pole,
no Lie bracket.  From this, everything follows by residue
extraction on~$\overline{C}_n(X)$.

\smallskip\noindent\textbf{Bar complex.}
The bar differential vanishes identically ($d_{\mathrm{bracket}} =
0$); only $d_{\mathrm{curvature}}$ acts, producing scalars.
The bar cohomology is $H^p(\barB(\mathcal{H}_k)) = \C$ in bar
degree~$1$, concentrated in the top conformal weight stratum.
The Koszul dual is $\mathcal{H}_k^! = \mathrm{Sym}^{\mathrm{ch}}(V^*)$,
a curved chiral coalgebra with curvature $-k\omega_g$
(Chapter~\ref{ch:heisenberg-frame}).
Note: $\mathcal{H}_k^! \neq \mathcal{H}_{-k}$ as chiral algebras---the
Heisenberg algebra is \emph{not} Koszul self-dual.

\smallskip\noindent\textbf{Genus tower.}
The period correction at genus~$g$ couples to the Hodge bundle via
$\kappa(\mathcal{H}_k) = k$.  The obstruction class is
$\mathrm{obs}_g = k\cdot\lambda_g$.  The genus expansion of the
free energy reproduces the Dedekind eta function:
$\sum_g F_g \hbar^{2g-2} = -k\,\log \eta(\tau)$, expanded in
Eisenstein series (Chapter~\ref{ch:genus-expansions}).

\smallskip\noindent\textbf{Shadow tower.}
The Heisenberg shadow tower terminates at arity~$2$: $o_3(\mathcal
H_k) = 0$ (by abelian rigidity), so the cubic shadow vanishes, the
quartic shadow vanishes, and $\Theta_{\mathcal{H}_k} =
\kappa(\mathcal{H}_k) \cdot \eta \otimes \Lambda$ is exact.
The shadow metric is $Q_L = (2k)^2$ (constant);
the critical discriminant $\Delta = 0$
(Theorem~\ref{thm:single-line-dichotomy}).

\smallskip\noindent\textbf{Completion kinematics.}
The vacuum character $\chi_{\mathcal{H}_k}(q) = \prod_{n \geq 1}(1-q^n)^{-1}$ gives $G_{\mathcal{H}_k}(t) = t$ (one primitive of reduced weight~$1$, no others).  The completion entropy is $h_K(\mathcal{H}_k) = 0$: the completed bar complex does not grow.  The primitive defect $\Delta = 0$: no nonquadratic enlargement is needed.  The Heisenberg algebra is kinematically trivial---the simplest point in the completion phase diagram.

\smallskip\noindent\textbf{Complementarity.}
$Q_g(\mathcal{H}_k) \oplus Q_g(\mathcal{H}_{-k}) \cong
H^*(\overline{\mathcal{M}}_g, \C)$: the positive- and negative-level
Heisenberg algebras fill complementary Lagrangians.  The Koszul
conductor is $K(\mathcal{H}_k) = c(k) + c(-k) = 2$ (central
charges $c = 1$ at any nonzero~$k$).

\subsection{Affine Kac--Moody: Lie/tree archetype, termination at arity~$3$}
\label{subsec:kac-moody-atom-intro}
\index{Kac--Moody algebra!shadow archetype}
\index{Feigin--Frenkel involution}

The affine Kac--Moody algebra $\widehat{\mathfrak{g}}_k$ has OPE
$J^a(z) J^b(w) \sim k\,\delta^{ab}/(z-w)^2 + f^{ab}{}_c J^c(w)/(z-w)$.
The simple pole introduces a nonzero bracket component $d_{\mathrm{bracket}}$, and $d_{\mathrm{bracket}}^2 \neq 0$ (proved for $\mathfrak{sl}_2$ at all $2048$ sign conventions).  The full bar differential $d_{\barB} = d_{\mathrm{bracket}} + d_{\mathrm{curvature}}$ satisfies $d_{\barB}^2 = 0$ by the Borcherds identity.

\smallskip\noindent\textbf{Koszul dual and Feigin--Frenkel.}
The Koszul dual is $\widehat{\mathfrak{g}}_{k'}$ with $k' = -k - 2h^\vee$, where $h^\vee$ is the dual Coxeter number.  This is the Feigin--Frenkel involution: Koszul duality for affine Kac--Moody algebras \emph{is} the level-reflection symmetry.  The Sugawara construction, which provides the stress-energy tensor $T(z) = \frac{1}{2(k+h^\vee)} \sum_a :J^a J^a:(z)$, is undefined at the critical level $k = -h^\vee$ (not ``$c$ diverges''; rather, the Sugawara vector is not in the completion of $U(\widehat{\mathfrak{g}}_{-h^\vee})$).

\smallskip\noindent\textbf{Shadow tower: arity~$3$ termination.}
The modular characteristic is $\kappa(\widehat{\mathfrak{g}}_k) = \dim(\mathfrak{g}) \cdot (k+h^\vee)/(2h^\vee)$, with $\kappa(k) + \kappa(k') = 0$ (anti-symmetry for the affine family).  The cubic shadow $\mathfrak{C}(\widehat{\mathfrak{g}}_k)$ is nonzero (it detects the Lie bracket), but $o_4 = 0$: the tower terminates at arity~$3$.
The shadow metric $Q_L = (2\kappa + 3\alpha t)^2$ is a
perfect square; the critical discriminant
$\Delta = 0$ (Jacobi identity forces the quartic to be
entirely derived from the cubic self-bracket).

\smallskip\noindent\textbf{Completion kinematics.}
The vacuum character gives $G_{\widehat{\mathfrak{g}}_k}(t) = (\dim\mathfrak{g})\,t/(1-t)$: there are $\dim\mathfrak{g}$ primitives at each reduced weight (the current derivatives $\partial^r J^a$).  The Koszul radius is $\rho = 1/(\dim\mathfrak{g}+1)$ and the entropy is $h_K = \log(\dim\mathfrak{g}+1)$.  For $\mathfrak{sl}_2$: $h_K \approx 1.10$, larger than Virasoro ($h_K \approx 0.57$), reflecting the multicomponent nature of the bar complex despite the simpler shadow depth.  The defect $\Delta = 0$: the declared generators $J^a$ already span the full primitive sector.

\subsection{The $\beta\gamma$ system: contact/quartic archetype, termination at arity~$4$}
\label{subsec:betagamma-atom-intro}
\index{betagamma system@$\beta\gamma$ system!shadow archetype}

The $\beta\gamma$ system has OPE $\beta(z)\gamma(w) \sim 1/(z-w)$, a simple pole between fields of complementary conformal weight.  The quartic contact invariant $\mu_{\beta\gamma} = \langle \eta, m_3(\eta, \eta, \eta) \rangle$ vanishes by rank-one abelian rigidity (Corollary~\ref{cor:nms-betagamma-mu-vanishing}): $o_5 = 0$, and the tower terminates at arity~$4$.  This is the contact archetype---the first system where the quartic shadow is nonzero but the quintic obstruction vanishes.  The single-line dichotomy (Theorem~\ref{thm:single-line-dichotomy}) shows that this value is impossible on a single primary line, where $r_{\max} \in \{2, 3, \infty\}$; the contact class escapes via stratum separation (Remark~\ref{rem:contact-stratum-separation}), with the quartic invariant living on a charged stratum of $\Defcyc^{\mathrm{mod}}$ whose self-bracket is annihilated by the rank-one charge constraint.

\smallskip\noindent\textbf{Completion kinematics.}
The two generators $\beta$, $\gamma$ of weights $1$ and $0$ give
$G_{\beta\gamma}(t) = t/(1-t)$ (one primitive per reduced weight).
The Koszul radius is $\rho = 1/2$, the entropy $h_K = \log 2
\approx 0.693$, and the defect $\Delta = 0$.  Despite its richer
shadow depth (arity~$4$ vs arity~$3$ for Kac--Moody), the $\beta\gamma$
system is kinematically simpler than any non-abelian affine algebra.

\subsection{Virasoro and $\mathcal{W}_N$: mixed archetype, infinite tower}
\label{subsec:virasoro-w-atom-intro}
\index{Virasoro algebra!shadow archetype}
\index{W-algebra@$\mathcal{W}$-algebra!shadow archetype}

The Virasoro algebra has OPE $T(z)\,T(w) \sim c/2(z-w)^4 + 2T(w)/(z-w)^2 + \partial T(w)/(z-w)$---a quartic pole, the highest in the standard landscape.  The Koszul dual is $\mathrm{Vir}_{26-c}$ (self-dual at $c = 13$, not $c = 26$).

The shadow tower is infinite: $o_5(\mathrm{Vir}_c) \neq 0$, and the quintic obstruction is forced.  The structural mechanism is \emph{direct self-referentiality} in the OPE: the generator~$T$ appears in its own self-OPE via $T_{(1)}T = 2T$.  This direct self-loop---absent for Heisenberg ($J \notin J_{(k)}J$), affine ($E^\alpha \notin E^\alpha_{(k)}E^\alpha$), and $\beta\gamma$ ($\beta \notin \beta_{(k)}\beta$)---sustains an infinite $A_\infty$ cascade that prevents the shadow tower from terminating
(Proposition~\ref{prop:self-referentiality-criterion}).
This is the mixed archetype: both cubic and quartic shadows are present, and the tower never terminates.  The quartic contact invariant
$Q^{\mathrm{contact}}_{\mathrm{Vir}} = 10/[c(5c+22)]$
is the explicit measure of the quartic interaction.  The poles at $c = 0$ and $c = -22/5$ are the minimal model values $c_{2,5}$ and $c_{2,3}$.  The single-line dichotomy
(Theorem~\ref{thm:single-line-dichotomy}) identifies the
structural mechanism: the Virasoro critical discriminant
$\Delta_{\mathrm{Vir}} = 40/(5c + 22)$ is nonzero for all
non-resonant~$c$, and the universal factorization
$S_r = \Delta \cdot R_r$ with generically nonvanishing
quotients~$R_r$ forces all arities $r \geq 4$ to be populated.

The $\mathcal{W}_N$ algebras share the infinite-tower behavior for $N \geq 3$.  The $\mathcal{W}_3$ algebra is the first case where the filtered-curved distinction matters: $\mathcal{W}_3$ carries a truly filtered (not merely curved) $A_\infty$ structure, because the composite field $\Lambda = :(TT): - \frac{3}{10}\partial^2 T$ introduces a quartic pole that is not a scalar multiple of the identity.

\smallskip\noindent\textbf{Completion kinematics.}
The kinematic axis detects structure invisible to shadow depth alone.  For generic Virasoro, $G_{\mathrm{Vir}}(t)
= \tfrac{1-t}{t}\bigl(\prod_{n \geq 2}(1-t^n)^{-1} - 1\bigr)$
gives primitive multiplicities $g_r = 1, 0, 1, 0, 2, 0, 3, 1, \ldots$
The defect $\Delta_{\mathrm{Vir}}(t) = t^3 + 2t^5 + 3t^7 + \cdots
\neq 0$: generic Virasoro has infinitely many primitive cumulants
beyond the stress tensor~$T$.  The entropy is
$h_K(\mathrm{Vir}) \approx 0.567$.

For the $\mathcal{W}_N$ tower, the entropy increases monotonically
(Proposition~\ref{prop:wn-entropy-ladder}):
\[
h_K(\mathcal{W}_2) \approx 0.567
\;<\; h_K(\mathcal{W}_3) \approx 0.772
\;<\; h_K(\mathcal{W}_4) \approx 0.835
\;<\; \cdots
\;<\; h_K(\mathcal{W}_\infty) \approx 0.872.
\]
The $\mathcal{W}_3$ jump from $0.567$ to $0.772$ is the kinematic
signature of the composite field~$\Lambda$, which forces the
corrected Koszul recursion $h_q = 2h_{q-1} + h_{q-2} + h_{q-3}$
in place of the naive $h_q = 2h_{q-1} + h_{q-2}$.
At the limit $N = \infty$, the character
$\chi_{\mathcal{W}_\infty}(q) = \prod_{n \geq 2}(1 - q^n)^{-(n-1)}$
admits a MacMahon factorization
(Proposition~\ref{prop:wn-entropy-ladder}), connecting the
$\mathcal{W}_\infty$ completion frontier to the combinatorics of
plane partitions.

The same-family shadow $\mathrm{Vir}_{26-c}$ coexists with the
nonzero defect because it is a \emph{dynamic} shadow, not the full
kinematic dual.  Kinematically, the completed dual coalgebra has
infinitely many primitive generators; dynamically, the scalar trace
$\kappa(\mathrm{Vir}_c) \cdot \lambda_g$ already reproduces the
partner family $\mathrm{Vir}_{26-c}$.  The full H-level kinematic
identification is the residual MC4 task
(\S\ref{subsec:completion-kinematics-programme} of the
concordance).

\subsection{The Yangian: $\Eone$-chiral, $R$-matrix inversion}
\label{subsec:yangian-atom-intro}
\index{Yangian!shadow archetype}
\index{R-matrix@$R$-matrix!inversion under Koszul duality}

The Yangian $Y(\mathfrak{g})$ is an $\Eone$-chiral algebra (Definition~\ref{def:e1-chiral}), not $\Einf$-chiral.  Its bar complex uses \emph{ordered} configuration spaces $\operatorname{Conf}_n^{\mathrm{ord}}(X)$ rather than unordered ones, and Verdier duality acts by $R$-matrix inversion $R(u) \mapsto R(u)^{-1}$ rather than by type exchange $\cA \mapsto \cA^!$.

The Yangian is generated by currents $T^{(1)}(z), T^{(2)}(z), \ldots$ with spectral parameter dependence, and its OPE involves the rational $R$-matrix $R(u-v)$.  The Drinfeld--Kohno bridge (Theorem~\ref{thm:factorization-dk-eval}) relates the chiral Koszul duality of affine Kac--Moody algebras at the top of the DK square~\eqref{eq:dk-intro} to the quantum group $R$-matrix inversion at the bottom, with the Kazhdan--Lusztig equivalences as vertical arrows.

The modular dg-shifted Yangian $\mathcal{Y}^{\mathrm{dg,mod}}_T$ (Definition~\ref{def:modular-yangian-pro}) is the pronilpotent completion of the convolution dg Lie algebra for the Yangian.  The modular $R$-matrix $R^{\mathrm{mod}}_T(z; \hbar)$ is a Maurer--Cartan element in this completed algebra.

\subsection{Lattice vertex algebras: braided, curvature-braiding orthogonal}
\label{subsec:lattice-atom-intro}
\index{lattice vertex algebra!shadow archetype}
\index{curvature-braiding orthogonality}

The lattice vertex algebra $V_\Lambda$ associated to an even lattice $\Lambda$ carries an $\Eone$ sublattice structure visible through screening operators.  The key structural result is curvature-braiding orthogonality: the modular characteristic $\kappa(V_\Lambda) = \operatorname{rank}(\Lambda)$ is independent of the $2$-cocycle that determines the braiding (Theorem~\ref{thm:lattice:curvature-braiding-orthogonal}).  The curvature sees only the rank of the lattice; the braiding sees only the cocycle.  These two pieces of data are orthogonal in a precise sense: they live in complementary summands of the deformation complex.

\smallskip\noindent\textbf{Unbounded finite depth and arithmetic content.}
While generic lattice VOAs have shadow depth~$2$ (Gaussian), the
vertex-operator OPE
$e^{i\lambda\cdot X}_{(0)}e^{i\mu\cdot X} \sim
\varepsilon(\lambda,\mu)\,e^{i(\lambda+\mu)\cdot X}$
is \emph{nonlinear} (it creates new fields), though not
self-referential ($e^{i\lambda\cdot X} \notin
e^{i\lambda\cdot X}_{(k)}e^{i\lambda\cdot X}$ since $2\lambda$ is
never a root of an even lattice).  The shadow depth of~$V_\Lambda$ is
\emph{finite but unbounded}: it is determined by the modular-form
content of the theta function~$\Theta_\Lambda$
(Theorem~\ref{thm:shadow-spectral-correspondence}).  For $E_8$
($r{=}8$, $\Theta = E_4$): depth~$3$.  For the Leech lattice
($r{=}24$, $\Theta = E_4^3 - 720\Delta$): depth~$4$, the first
example involving the Ramanujan cusp form $\Delta_{12}$ and its
$L$-function.  Even unimodular lattices of ranks $48, 72, 96, \dotsc$
achieve depths $5, 6, 7, \dotsc$: each independent cusp form
in~$S_{r/2}(\Gamma)$ contributes one critical line of the constrained
Epstein zeta~$\varepsilon^r_s(V_\Lambda)$ and raises the depth
by~$1$.  The shadow depth thereby becomes a
\emph{Galois-theoretic invariant}: it counts the number of
independent automorphic $L$-functions encoded in the lattice's
Hecke eigenform decomposition
(Chapter~\ref{chap:arithmetic-shadows}).
All lattice VOAs are exact Euler--Koszul
($\operatorname{ek} = 0$), since every generator has
weight~$1$.  Their Dirichlet--sewing lift is a pure
Euler product:
$S_{V_\Lambda} = \operatorname{rank}(\Lambda)\cdot
\zeta(u)\,\zeta(u{+}1)$.


\section{Strictness as a shadow: the homotopy upgrade}
\label{sec:homotopy-upgrade}
\index{convolution!strict vs.\ homotopy|textbf}
\index{homotopy transfer theorem!role in the programme}

The convolution dg Lie algebra~$\gAmod$
of~\eqref{eq:convolution-dg-lie-intro} is a \emph{strict model}:
its differential and bracket are defined on the nose, not up to
homotopy.  But it is only the first visible layer of a richer
homotopy-coherent package.  Write
\begin{align}
\Convstr(\mathrm{FCom},\, \operatorname{End}_{\barB(\cA)})
  &\quad\text{--- the strict dg Lie convolution model,}
  \label{eq:convstr-intro}
\\
\Convinf(\mathrm{FCom},\, \operatorname{End}_{\barB(\cA)})
  &\quad\text{--- the homotopy-invariant $L_\infty$ convolution.}
  \label{eq:convinf-intro}
\end{align}
The two are related by a quasi-isomorphism of $L_\infty$ algebras
(a consequence of the homotopy transfer theorem,
Chapter~\ref{app:homotopy-transfer}).  Their Maurer--Cartan moduli
coincide; hence every MC element of~$\Convstr$ lifts canonically to
one of~$\Convinf$.  But the two packages are not interchangeable:
the $L_\infty$ structure of~$\Convinf$ remembers higher homotopies
that $\Convstr$ does not see, and these homotopies are essential for
transfer, formality, and gauge equivalence.

The distinction matters throughout the monograph.  The bar-cobar
adjunction (Theorem~A) preserves quasi-isomorphisms \emph{because}
it is a quantum $L_\infty$ functor: it respects the full homotopy
package, not merely the strict dg structure.  The homotopy transfer
theorem (Chapter~\ref{app:homotopy-transfer}) produces transferred
$A_\infty$ and $L_\infty$ structures on bar cohomology; these
transferred structures carry the information that would be lost by
passing to a strict model.  Every formula at the strict dg level is the leading
term of a homotopy-coherent datum---just as the scalar~$\kappa(\cA)$
is the leading term of the shadow tower~$\Theta_\cA^{\le r}$.

The same principle applies at the modular level.  The full modular
deformation object is
\[
\Definfmod(\cA)
\;:=\;
\Convinf\bigl(\mathrm{FCom},\,
\operatorname{End}_{\barB(\cA)}\bigr)
\;\widehat{\otimes}\;
R\Gamma\bigl(\overline{\mathcal{M}}_{g,\bullet},\, \mathbb{Q}\bigr),
\]
an $L_\infty$ algebra whose MC elements are complete genus-expansion
data compatible with clutching, trace, and duality up to coherent
homotopy.  The strict model~$\gAmod$ is a chart on this larger
formal space.  Theorem~B (bar-cobar inversion) and Theorem~C
(complementarity) are proved at the strict level and stated at the
homotopy level; the passage between them is guaranteed by the
contractibility of the space of models
(Proposition~\ref{prop:model-independence}).

\begin{remark}[Structural consequences of the homotopy upgrade]
\label{rem:structural-consequences-homotopy}
\index{homotopy upgrade!structural consequences}
Four structural consequences of the homotopy perspective recur
throughout the volume.
\begin{enumerate}[label=\textup{(\roman*)}]
\item Every strict-model formula (convolution dg~Lie bracket,
  curved bar differential, finite obstruction class, spectral
  discriminant) is a projection of
  $\Theta_\cA \in \MC(\Convinf)$.  The strict model computes
  the correct answer because it is a faithful chart on the larger
  formal space, not because the $L_\infty$ data are dispensable.
\item Genus, clutching, and gluing enter at the chain level---in the
  bar and cobar constructions, in the boundary stratification of
  configuration spaces, and in the completed deformation
  package---rather than as cohomological decorations applied post~hoc.
\item The standard families (Heisenberg, $\beta\gamma$, affine
  Kac--Moody, Virasoro, $\mathcal{W}_N$, Yangians, lattice and
  elliptic) serve as diagnostic benchmarks: each activates a
  different stratum of the homotopy machinery, and their shadow
  archetypes (Gaussian, Lie/tree, contact, mixed) are the
  finite-order witnesses of the transferred $L_\infty$ operations.
\item The shadow Postnikov tower is not an approximation scheme but a
  faithful finite-order image of $\Theta_\cA$: each truncation
  $\Theta_\cA^{\le r}$ captures genuinely new modular invariants
  ($\kappa$ at $r{=}2$, $\mathfrak{C}$ at $r{=}3$,
  $\mathfrak{Q}$ at $r{=}4$) that would be invisible at any lower
  order.
\end{enumerate}
\end{remark}


\section{The three pillars}
\label{sec:three-pillars-intro}
\index{three-pillar architecture|textbf}
\index{Malikov--Schechtman}
\index{Robert-Nicoud--Wierstra}
\index{Mok}

The algebraic engine rests on three foundational pillars, each representing a recent preprint that forces an upgrade at a different level of the theory.  Each supplies structural theorems that the monograph absorbs and extends.

\subsection{Pillar A: homotopy chiral algebras (MS24)}
\label{subsec:pillar-a}

The primitive local object is not a strict chiral algebra but a \emph{homotopy chiral algebra} ($\mathrm{Ch}_\infty$) in the sense of Malikov--Schechtman~\cite{MS24}.  A homotopy chiral algebra carries an $A_\infty$ structure whose higher operations $m_n$ encode the failure of associativity at the OPE level.  The strict chiral algebra~$\cA$ appears only after rectification: it is the $H^0$ of a $\mathrm{Ch}_\infty$ structure, which in turn projects to the Poisson vertex algebra (PVA) or coisson shadow at the classical level.

The identification theorem (Theorem~\ref{thm:cech-hca}): $B(\cA)$ carries a natural $\mathrm{Ch}_\infty$-algebra structure, upgrading the bar construction from a dg coalgebra to a homotopy chiral algebra.

\subsection{Pillar B: filtered convolution $sL_\infty$ algebras (RNW19+Val16)}
\label{subsec:pillar-b}

The universal deformation machine is the filtered convolution $sL_\infty$-algebra $\operatorname{hom}_\alpha(C, A)$ of Robert-Nicoud--Wierstra~\cite{RNW19} and Vallette~\cite{Val16}.  The deformation space of a twisting morphism $\alpha \colon C \to A$ is the Maurer--Cartan $\infty$-groupoid of this $sL_\infty$-algebra, and the dg Lie algebra is its strict model.

A key constraint: $\operatorname{hom}_\alpha$ extends to $\infty$-morphisms in either slot separately but \emph{not} both simultaneously.  There is a no-bifunctor obstruction: the convolution construction is functorial in $C$ and in $A$ individually, but the combined functoriality fails at the $\infty$-categorical level (RNW19, Theorem~6.1).  This has a direct consequence for the MC3 categorical lift: the lift from character-level generation to categorical generation must proceed one slot at a time.

The identification theorem (Corollary~\ref{cor:theta-twisting-morphism}):\ $\Theta_\cA \in \operatorname{MC}(\operatorname{hom}_\alpha) \cong \operatorname{Tw}_\alpha$---the universal MC element is a twisting morphism.

\subsection{Pillar C: logarithmic Fulton--MacPherson (Mok25)}
\label{subsec:pillar-c}

The global collision geometry is the logarithmic Fulton--MacPherson compactification $\mathrm{FM}_n(X | D)$ on a simple normal crossing pair $(X, D)$ in the sense of Mok~\cite{Mok25}.  The classical FM compactification is the special case $D = \emptyset$.

The tropicalization theorem (Theorem~\ref{thm:planted-forest-tropicalization}): $\Gmod_{\mathrm{pf}} = \operatorname{Trop}(\mathrm{FM}_n(\mathbb{C} | D))$---the planted-forest coefficient algebra is the tropical limit of the log FM compactification.  Planted forests are dual graphs of tropical curves, and grafting is degeneration.  The semistable degeneration formula for log FM spaces yields the clutching laws for the quartic resonance class (Theorem~\ref{thm:nms-clutching-law-modular-resonance}).

\subsection{Eleven identification theorems}
\label{subsec:eleven-identifications}
\index{identification theorems}

The three pillars yield eleven identification theorems, nine proved and two structural:
\begin{enumerate}[label=\textup{(\arabic*)}]
\item $\gAmod = \operatorname{hom}_\alpha(\mathrm{Com}^{!}_{\mathrm{ch}},\, \mathcal{P}^{\mathrm{ch}})$: the convolution algebra is the twisting hom (Theorem~\ref{thm:convolution-master-identification}).
\item $\Theta_\cA \in \operatorname{MC}(\operatorname{hom}_\alpha) \cong \operatorname{Tw}_\alpha$: the universal MC element is a twisting morphism (Corollary~\ref{cor:theta-twisting-morphism}).
\item $\Gmod_{\mathrm{pf}} = \operatorname{Trop}(\mathrm{FM}_n(\mathbb{C} | D))$: planted forests are tropical curves (Theorem~\ref{thm:planted-forest-tropicalization}).
\item $B(\cA)$ is a $\mathrm{Ch}_\infty$-algebra (Theorem~\ref{thm:cech-hca}).
\item $\barB_\kappa \dashv \Omega_\kappa$ is a Quillen equivalence (Theorem~\ref{thm:quillen-equivalence-chiral}).
\item $\cA^{\mathrm{sh}}$ is a homotopy invariant (Theorem~\ref{thm:shadow-homotopy-invariance}).
\item The one-slot obstruction constrains MC3: the categorical lift must proceed one slot at a time (RNW19, Theorem~6.1).
\item $F_n = o_n$: the secondary Borcherds operations of~\cite{MS24} at arity $n \geq 3$ coincide with the shadow tower obstruction classes (Proposition~\ref{prop:borcherds-shadow-identification}).
\item The quartic clutching law has a log-geometric derivation via Mok's degeneration formula~\cite[Corollary~5.3.4]{Mok25} (Construction~\ref{constr:arity4-degeneration}).
\item The log clutching conjecture is proved: Theorem~\ref{thm:log-clutching-degeneration} is established by~\cite[Corollary~5.3.4]{Mok25}.
\item The ambient $D^2 = 0$ (Theorem~\ref{thm:ambient-d-squared-zero}) is proved via the log-FM normal-crossings result~\cite[Theorem~3.3.1(1)]{Mok25}.
\end{enumerate}


\subsection{Chain-level synthesis}
\label{subsec:chain-level-synthesis-intro}
\index{chain-level synthesis|textbf}

The three pillars produce a single chain-level object.
For a cover~$\mathfrak{U}$ of~$X$, set
$A^{\mathrm{ch}}_\infty := \check{C}^\bullet(\mathfrak{U};\cA)$
(the $\mathrm{Ch}_\infty$-input) and
$\cC^{\log\mathrm{FM}}_{\mathrm{mod}}(n) :=
C_\bullet(\operatorname{FM}_n(X|D))$
(the cooperadic coefficient system).
The logarithmic FM convolution algebra
\begin{equation}\label{eq:logfm-convolution-intro}
\gAmod
\;:=\;
\Convinf\!\bigl(
\cC^{\log\mathrm{FM}}_{\mathrm{mod}},\;
\operatorname{End}_{\mathrm{Ch}_\infty}(A^{\mathrm{ch}}_\infty)
\bigr)
\end{equation}
has five-component differential
$D^{\log}_{\mathrm{mod}}
= d_{\check{C}} + d_{\mathrm{Ch}_\infty}
+ d_{\mathrm{coll}} + d_{\mathrm{sew}}
+ d_{\mathrm{loop}}$.
The boundary operators are residue correspondences on
log-FM strata: $d_{\mathrm{sew}}$ is pushforward along
codimension-one separating divisors, $d_{\mathrm{loop}}$ along
non-separating divisors, and $d_{\mathrm{pf}}$ along rigid
codimension-two planted-forest strata
(Construction~\ref{const:vol1-boundary-operators-residue}).
The identity $D^2=0$ is the cancellation of codimension-two faces
in Mok's normal-crossings compactification.

The graphwise cocomposition
$\Delta^{\log}_\Gamma := (\nu_\Gamma)_* \circ
\operatorname{Res}_{D^{\log}_\Gamma}$
decomposes the cooperadic structure over rigid stable
graphs~(Construction~\ref{const:vol1-graphwise-log-fm-cocomposition}),
yielding explicit Taylor coefficients $\ell_k^{(g)} = \sum_\Gamma
|\operatorname{Aut}(\Gamma)|^{-1}\,\ell_\Gamma$ of the modular
$L_\infty$-structure.  The universal MC element
\[
\Theta_\cA = \sum_\Gamma
\frac{W_\Gamma^{\log\mathrm{FM}}}{|\operatorname{Aut}(\Gamma)|}
\otimes \Phi_\Gamma^\cA
\]
is the universal modular twisting morphism:
$\operatorname{MC}_\bullet(\gAmod) \simeq
\operatorname{Tw}_{\alpha^{\mathrm{mod}}}$
(Corollary~\ref{cor:vol1-theta-log-fm-twisting-data}).
Each coefficient of~$\Theta_\cA$ acquires a tridegree
$(g,n,d)$ from the depth filtration
(Definition~\ref{def:vol1-rigid-planted-forest-depth-filtration}).

The weight filtration stratifies the MC equation by internal-edge
count.  At weight~$1$:
$\Theta_\cA^{[1]}|_{(0,4)} = [\delta_s]\otimes\Phi_s
+ [\delta_t]\otimes\Phi_t + [\delta_u]\otimes\Phi_u$
(the Malikov--Schechtman Jacobiator~$F_3$) and
$\Theta_\cA^{[1]}|_{(1,1)} = [\delta_{\mathrm{irr}}]\otimes\Delta$
(the BV operator).  At weight~$2$, the rigid planted-forest
corrections enter---the first genuinely logarithmic layer
(Proposition~\ref{prop:vol1-first-two-weights-log-fm}).

The modular tangent complex
$T^{\mathrm{mod}}_{\Theta_\cA}
:= (\gAmod,\, d_{\Theta_\cA})$
with $L_\infty$-twisted differential
$d_{\Theta_\cA}(x) = \sum_{n,g}
(\hbar^g/n!)\,\ell_{n+1}^{(g)}(\Theta_\cA^{\otimes n},x)$
recovers all invariants as characteristic projections:
$\kappa = \operatorname{tr}(\Theta_\cA)_{2,0}$,
$\Delta_\cA = \operatorname{sdet}(1 - t\,H_{d_\Theta})$,
$\mathfrak{R}^{\mathrm{mod}}_4 =
\operatorname{pr}_4(\Theta_\cA)$
(Construction~\ref{const:vol1-modular-tangent-complex}).
The genus filtration on $L_{\mathrm{mod}}$ induces a spectral
sequence whose differentials are the obstruction
maps~$\operatorname{Ob}_g$, distinct from the PBW spectral sequence
(Construction~\ref{const:vol1-genus-spectral-sequence}).
The full chain-level architecture---weight filtration, clutching
law, genus-two shell decomposition, curved extension for
non-quadratic algebras---is in
\S\ref{subsec:three-preprints-chain-level}.


\section{The total space}
\label{sec:modular-homotopy-theory}
\index{modular homotopy theory|textbf}
\index{total space|textbf}

The nilpotence-periodicity correspondence becomes geometric in the
total space of the universal configuration fibration:
\begin{equation}\label{eq:total-fibration-intro}
\pi \colon \overline{C}_n(\mathcal{C}_g) \;\longrightarrow\;
\overline{\mathcal{M}}_g.
\end{equation}
Its fiber over $[\Sigma_g]$ is $\overline{C}_n(\Sigma_g)$.  The
boundary has two types of divisors:
\begin{enumerate}[label=\textup{(\alph*)}]
\item \emph{Collision divisors} $D_I$ (vertical): marked points
  collide on a fixed smooth curve.  These produce the bar
  differential.
\item \emph{Degeneration divisors} $\delta_\Gamma$ (horizontal):
  the curve acquires a node.  These produce the period corrections
  that restore~$\Dg{g}^{\,2} = 0$.
\end{enumerate}
At genus~$0$, only type~(a) exists and the Arnold relation alone
gives $\dzero^2 = 0$.  At genus~$g \geq 1$, both coexist.

\subsection{The Hodge bundle and the propagator}
\label{subsec:hodge-deformation-engine}

The Hodge bundle $\mathbb{E} = R^1\pi_*\mathcal{O}_{\mathcal{C}_g}$
has fiber $H^0(\Sigma_g, \Omega^1)$.  The propagator depends on a
frame for~$\mathbb{E}^\vee|_{[\Sigma_g]}$; the period corrections
live in $\mathbb{E}^\vee \boxtimes \mathbb{E}$.  The scalar trace
yields
\begin{equation}\label{eq:lambda-from-trace}
\mathrm{obs}_g(\cA)
= \kappa(\cA) \cdot c_g(\mathbb{E})
= \kappa(\cA) \cdot \lambda_g
\;\in\; H^*(\overline{\mathcal{M}}_g).
\end{equation}

\subsection{The family index}
\label{subsec:family-index-intro}
\index{A-hat genus@$\hat{A}$-genus!as family index}

The family $\barB_g(\cA) \to \overline{\mathcal{M}}_g$ defines a
virtual vector bundle.  Grothendieck--Riemann--Roch applied to
$\pi \colon \overline{\mathcal{C}}_g \to
\overline{\mathcal{M}}_g$: the fiber Chern character
contributes~$\kappa(\cA)$; the Todd class produces the
$\hat{A}$-series.  The generating function is
$\sum_{g \geq 1} \mathrm{obs}_g \cdot \hbar^{2g-2}
= \kappa(\cA) \cdot (\hat{A}(i\hbar) - 1)$
(Theorem~\ref{thm:family-index}).  For a free fermion, this is the
spin partition function; for the Virasoro algebra, the one-loop
string amplitude.

\subsection{The Lagrangian decomposition}
\label{subsec:verdier-symplectic}
\index{Verdier duality!as symplectic structure}

Verdier duality $\sigma = \mathbb{D}_{\operatorname{Ran}}$ acts on
$\mathbf{C}_g(\cA) = R\Gamma(\overline{\mathcal{M}}_g,
\mathcal{Z}_\cA)$.  The composition
\[
\mathbf{C}_g(\cA) \otimes \mathbf{C}_g(\cA)
\;\xrightarrow{\;\mathrm{id} \otimes \sigma\;}
\mathbf{C}_g(\cA) \otimes \mathbf{C}_g(\cA^!)
\;\xrightarrow{\;\mathrm{eval}\;}
R\Gamma(\overline{\mathcal{M}}_g,
\omega_{\overline{\mathcal{M}}_g})
\]
is a $(-(3g{-}3))$-shifted symplectic form
(Pantev--To\"en--Vaqui\'e--Vezzosi~\cite{PTVV13}).  The $\pm 1$
eigenspaces of~$\sigma$ are complementary Lagrangians.  Traced
through~$\pi$ and projected to moduli, this produces Theorem~C.


\section{Architecture}
\label{sec:architecture}

\begin{remark}[Five geometric ingredients]\label{rem:five-pieces}
\index{modular Koszul duality!five irreducible pieces}
\index{geometric synthesis|textbf}
Modular Koszul duality rests on five geometric ingredients.
The first four belong to the commutative ($\Einf$) face; the fifth
to the associative ($\Eone$) face.
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Three-point collision.}
  The Arnold relation~\eqref{eq:arnold-seed} is factorization
  coherence: fusing three insertions does not depend on the
  order of pairwise collisions.  The genus-$0$ seed.
\item \emph{Verdier duality on $\operatorname{Ran}(X)$.}
  $\mathbb{D}_{\operatorname{Ran}} \bar{B}_X(\cA) \simeq
  \cA^!_\infty$
  (Theorem~\ref{thm:bar-cobar-isomorphism-main})---the
  Koszul dual algebra as theorem, not definition-by-analogy.
\item \emph{Genus-$1$ curvature.}
  $\dfib^{\,2} = \kappa(\cA) \cdot \omega_1 \cdot
  \operatorname{id}$
  (Theorem~\ref{thm:genus-universality}).  The natural home is
  Positselski's coderived category: quantum corrections are a
  change of ambient category, not extra terms.
\item \emph{Clutching of stable curves.}
  The genus tower is compatible with boundary clutching via the
  modular operad (Theorem~\ref{thm:prism-higher-genus}).
  The combinatorics is stable graphs, not trees, and
  $\Theta_\cA$ (Theorem~\ref{thm:universal-MC},
  proved by Theorem~\ref{thm:mc2-full-resolution})
  is compatible with clutching, trace, and Verdier duality.
\item \emph{Associative structure.}
  \index{ordered factorization|see{$\Eone$-chiral algebra}}
  The Yangian $Y(\mathfrak{g})$ is $\Eone$-chiral
  (Definition~\ref{def:e1-chiral}): its bar complex uses ordered
  configuration spaces, and Verdier duality acts by $R$-matrix
  inversion $R \mapsto R^{-1}$
  (Theorem~\ref{thm:yangian-koszul-dual}).
  The sign flip $\hbar \mapsto -\hbar$ replaces the Feigin--Frenkel
  involution.
\end{enumerate}

\smallskip\noindent
In the Heisenberg algebra, only pieces~(i)--(iv) are active, and they
already force the full commutative/modular package: Arnold ensures
$d^2 = 0$; Verdier produces
$\mathcal{H}_k^! = \mathrm{Sym}^{\mathrm{ch}}(V^*)$; the genus-$1$
period integral creates curvature $\kappa = k$; clutching assembles
the genus tower.

\smallskip\noindent
In the Yangian, piece~(v) becomes unavoidable.  Ordered
configurations, $R$-matrix inversion on the module category, and the
derived Drinfeld--Kohno bridge
(Theorem~\ref{thm:factorization-dk-eval}) are not embellishments of
the Heisenberg picture but the first genuinely braided determination of
the same configuration-space mechanism.

\smallskip\noindent
The Heisenberg case isolates the configuration-space mechanism in the absence of braiding; the Yangian case then introduces a genuinely new geometric feature---ordered configurations---rather than a different subject.
\end{remark}

\begin{remark}[The DK square]\label{rem:dk-inevitability}
\index{Drinfeld--Kohno!derived!as witness of the two determinations}
The coexistence of the commutative and braided determinations is
witnessed by the derived
Drinfeld--Kohno square (Theorem~\ref{thm:derived-dk-affine}):
\begin{equation}\label{eq:dk-intro}
\begin{tikzcd}
\mathcal{O}_k^{\mathrm{int}}(\widehat{\mathfrak{g}})
  \ar[r, "\Phi", "\sim"']
  \ar[d, "\Phi_{\mathrm{KL}}"', "\sim"]
& \mathcal{O}_{k'}(\widehat{\mathfrak{g}})
  \ar[d, "\Phi_{\mathrm{KL}}", "\sim"']
\\
\mathcal{C}(U_q(\mathfrak{g}))
  \ar[r, "q \mapsto q^{-1}"', "\sim"]
& \mathcal{C}(U_{q^{-1}}(\mathfrak{g}))
\end{tikzcd}
\end{equation}
Top: level inversion $k \mapsto k' = -k - 2h^\vee$ (Frame~A).
Bottom: $R$-matrix inversion $q \mapsto q^{-1}$ (Frame~B).
Verticals: Kazhdan--Lusztig equivalences.  Commutativity: bar-cobar
duality geometrically implements the quantum group involution.
The duality constraint on~$\kappa$
($\kappa + \kappa' = 0$ for KM/free fields;
$\kappa + \kappa' = K(\fg)$ for $\mathcal{W}$-algebras)
is the scalar shadow of the top arrow;
$q \mapsto q^{-1}$ is the quantum-group shadow.
The four theorems and the DK square are four projections of one
geometric structure on
$\operatorname{Ran}(X) \times \overline{\mathcal{M}}_g$.
\end{remark}

\begin{remark}[Master functorial correspondence]
\label{rem:master-functorial}
\index{functorial correspondence}
Every row is a theorem.
\begin{center}
\small
\begin{tabular}{lll}
\textbf{Geometry} & \textbf{Algebra} & \textbf{Physics} \\ \hline
Verdier duality $\mathbb{D}_{\mathrm{Ran}}$
  & Koszul duality $\cA \leftrightarrow \cA^!$
  & CPT \\[2pt]
Residue at $D_{ij}$
  & Bar differential $d_{\mathrm{res}}$
  & OPE fusion \\[2pt]
FM boundary stratum
  & Tree contribution to $m_k$
  & Feynman diagram \\[2pt]
Arnold relation
  & $d^2 = 0$ (genus~$0$)
  & Factorization coherence \\[2pt]
Period integral on $\overline{\mathcal{M}}_g$
  & Obstruction class $\mathrm{obs}_g$
  & One-loop determinant \\[2pt]
Clutching $\partial\overline{\mathcal{M}}_g$
  & Modular operad
  & String sewing \\[2pt]
DS reduction
  & Koszul functor
  & Gauge fixing \\[2pt]
Log form $\eta_{ij}$
  & Propagator
  & Free-field propagator \\[2pt]
Forgetful $\overline{\mathcal{M}}_{g,n+1}
  \to \overline{\mathcal{M}}_{g,n}$
  & Forgetting a marked point
  & Integrating out a field \\[2pt]
Branch locus of $P_{\cA^!}(x)$
  & Discriminant $\Delta_\cA(x)$
  & Spectral curve \\[2pt]
MC in $\barB(\widehat{\fg}_k)$
  & $L_\infty$ flatness
  & CS equations of motion \\[2pt]
$\int_{\overline{\mathcal{M}}_{0,n}} \barBgeom^{(0)}_n$
  & BRST cohomology pairing
  & Tree-level string amplitude
\end{tabular}
\end{center}
The first four rows are proved in Part~\textup{I}
(Chapters~\ref{chap:bar-cobar}--\ref{chap:higher-genus});
the next three in Part~\textup{II}
(Chapters~\ref{chap:kac-moody-koszul}--\ref{chap:yangians}).
The physics column is made precise in
Chapters~\ref{ch:bv-brst} and~\ref{ch:feynman}.
\end{remark}

Part~\textup{II} computes bar complexes for all major families
(Table~\ref{tab:master-invariants}).  The shared discriminant
$\Delta(x) = 1 - 2x - 3x^2$
(Theorem~\ref{thm:ds-bar-gf-discriminant}) and the universal
generating function
$\sum_{g \geq 1} F_g x^{2g} = \kappa \cdot (\hat{A}\text{-genus})$
(Theorem~\ref{thm:universal-generating-function}) are consequences
of the proved scalar and spectral layers.


\section{Beyond the five theorems: three concentric rings}
\label{subsec:three-concentric-rings}

The results extend well beyond the five-theorem spine and organize into three concentric rings, each with a distinct logical status.

\emph{Ring~1: the proved modular Koszul core.}
Theorems A/B/C/D/H, Master Conjectures~1 and~2 (at finite order),
and the DK ladder
through step~3 on the evaluation-generated core at all simple Lie
types.  This ring is closed: every claim is proved in the text, and
the outlook (Part~IV, Chapter~\ref{chap:concordance}) records
its boundary precisely.

\emph{Ring~2: the shadow Postnikov tower.}
The primary mathematical object beyond the linear regime.
Between the proved scalar tower $\kappa(\cA)\lambda_g$ and the full
universal class $\Theta_\cA$ sits a constructive finite-order layer
whose visible invariants form a hierarchy:
\[
\underbrace{\kappa(\cA)}_{\text{scalar}}
\;\longrightarrow\;
\underbrace{\Delta_\cA}_{\text{spectral}}
\;\longrightarrow\;
\underbrace{[\mathfrak C_\cA],\;\mathfrak o_\cA^{(4)},\;
  [\mathfrak Q_\cA]}_{\text{nonlinear shadows}}
\;\longrightarrow\;
\underbrace{\Theta_\cA}_{\text{universal class}}.
\]
The scalar characteristic $\kappa(\cA)$ is proved.  The spectral
discriminant $\Delta_\cA$ is proved.  The cubic and quartic shadow
classes $[\mathfrak C_\cA]$ and $[\mathfrak Q_\cA]$ are the first
genuinely nonlinear modular invariants, computable on any family
where the cyclic deformation package is available through the
relevant order
(Chapter~\ref{app:nonlinear-modular-shadows}).
The branch-line reductions
(Appendix~\ref{app:branch-line-reductions}) extract exact
finite-dimensional quotients of this layer, including genus-$2$
transparency and the universal genus-$3$ constant~$7$ on the shared
$\widehat{\mathfrak{sl}}_2$/Virasoro/$\beta\gamma$ spectral sheet.

The unifying principle is the modular $L_\infty$ convolution
algebra~$\gAmod$.  It carries a natural modular $L_\infty$ structure
from the Feynman transform of the modular operad; the dg Lie algebra
$\Convstr$ is its strict model.  The shadow Postnikov tower is the
finite-order projection of the universal MC element~$\Theta_\cA$.
The proved content is the finite-order engine: $\kappa$ (arity~$2$),
$\mathfrak{C}$ (arity~$3$), $\mathfrak{Q}$ (arity~$4$), with each
level a projection of the next.  The weight filtration on~$\gAmod$
controls the extension tower; each finite truncation
$\Theta_\cA^{\le r}$ is constructive and does not require the full
all-genera modular envelope.  The all-arity master equation
$D\Theta + \frac{1}{2}[\Theta,\Theta] = 0$ is proved by the
bar-intrinsic construction
(Theorem~\ref{thm:mc2-bar-intrinsic}): $\Theta_\cA := D_\cA - \dzero$
is automatically Maurer--Cartan because $D_\cA^2 = 0$.

\emph{Ring~3: the physics-facing frontier.}
Three axes extend the proved core toward mathematical physics.
\begin{enumerate}[label=\textup{(\alph*)}]
\item \emph{The $\mathcal W$-algebra axis.}
  Completed Koszulity is ubiquitous: every affine $\mathcal W$-algebra
  at generic level is completed Koszul.  Strict Koszulity is
  exceptional: the subregular family $\mathcal W_n^{(2)}$ has
  unbounded canonical homotopy arity, and the classical $W_3$ sector
  defines the first finite-arity modular higher-spin package.
  MC4 is proved: the strong completion-tower theorem
  (Theorem~\ref{thm:completed-bar-cobar-strong}) gives a
  completion-closed bar-cobar homotopy equivalence on $\CompCl(\Fft)$,
  with automatic continuity via the arity cutoff
  (Lemma~\ref{lem:arity-cutoff}) and coefficient-stability criterion
  (Theorem~\ref{thm:coefficient-stability-criterion}).
\item \emph{The Yangian/RTT axis.}
  Shifted Yangian duality decomposes into a local quadratic RTT
  kernel, a weightwise stabilized completed envelope, and a boundary
  quotient whose dual is an orthogonal coideal.  MC3 is proved in
  type~A via chromatic filtration, prefundamental Clebsch--Gordan
  closure, and Efimov completion; extension to arbitrary simple type
  requires the Hern\'andez--Jimbo prefundamental CG for all Dynkin
  types.
\item \emph{The holographic/celestial axis.}
  The proved modular core supplies a protected transform calculus:
  dual transform, cotangent reciprocity, Weyl sewing, and a
  metaplectic determinant-line anomaly.  The
  full bulk/boundary/line Koszul triangle and the celestial boundary
  transfer programme are developed in Volume~II.
\end{enumerate}


\section{The MC frontier}
\label{sec:mc-frontier-intro}
\index{MC frontier|textbf}
\index{master conjectures}

The open problems of the monograph are organized into five Master Conjectures (MC1--MC5), each probing a different aspect of the modular Koszul programme.  The frontier between proved and open territory is recorded in Chapter~\ref{chap:concordance}.

\begin{center}
\small
\begin{tabular}{llll}
\textbf{MC} & \textbf{Statement} & \textbf{Status} & \textbf{Key input} \\ \hline
MC1 & PBW concentration & \textbf{proved} & all standard families \\[2pt]
MC2 & Universal MC class $\Theta_\cA$ & \textbf{proved} & bar-intrinsic (Thm~\ref{thm:mc2-bar-intrinsic}) \\[2pt]
MC3 & Thick generation (type A) & \textbf{proved} & chromatic + CG + Efimov \\[2pt]
MC4 & $\mathcal{W}_\infty$ closure & \textbf{proved} & strong completion towers (Thm~\ref{thm:completed-bar-cobar-strong}) \\[2pt]
MC5 & Genus tower / BV-BRST comparison & \textbf{analytic part proved} & HS-sewing (Thm~\ref{thm:general-hs-sewing})
\end{tabular}
\end{center}

\noindent\textbf{MC1} (PBW concentration): The bar complex of every standard-landscape chiral algebra has bar cohomology concentrated in the PBW (Poincar\'e--Birkhoff--Witt) degrees.  This is proved for all standard families by explicit computation.

\noindent\textbf{MC2} (Universal MC class): The universal Maurer--Cartan element $\Theta_\cA \in \MC(\Defcyc(\cA) \widehat{\otimes} \Gmod)$ exists for every modular Koszul chiral algebra, by the bar-intrinsic construction (Theorem~\ref{thm:mc2-bar-intrinsic}): $\Theta_\cA := D_\cA - \dzero$ is automatically MC because $D_\cA^2 = 0$.  The shadow Postnikov tower $\Theta_\cA^{\leq r}$ consists of its finite-order projections.  Under the additional hypothesis of simple Lie symmetry with $\dim H^2_{\mathrm{cyc}} = 1$, the MC element collapses to the scalar formula $\Theta_\cA = \kappa(\cA) \cdot \eta \otimes \Lambda$ (Corollary~\ref{cor:scalar-saturation}).

\noindent\textbf{MC3} (Thick generation in type A): Every object in the category $\mathcal{O}_{\mathrm{poly}}$ for $Y(\mathfrak{sl}_N)$ lies in the thick subcategory generated by the evaluation modules.  Proved via the chromatic filtration, prefundamental Clebsch--Gordan closure at character level (Proposition~\ref{prop:prefundamental-clebsch-gordan}), and Efimov completion of the derived category.  Extension to arbitrary simple type requires the Hern\'andez--Jimbo prefundamental CG decomposition for all Dynkin types (Conjecture~\ref{conj:mc3-arbitrary-type}).

\noindent\textbf{MC4} ($\mathcal{W}_\infty$ closure and completed bar-cobar): The formal completion theory is \textbf{proved} by the strong completion-tower theorem (Theorem~\ref{thm:completed-bar-cobar-strong}): the finite-stage bar-cobar duality passes to inverse limits automatically once the filtration satisfies the strong axiom $\mu_r(F^{i_1}\cA,\dots,F^{i_r}\cA) \subset F^{i_1+\cdots+i_r}\cA$, which yields the arity cutoff (Lemma~\ref{lem:arity-cutoff}) making continuity and Mittag--Leffler automatic.  The completion closure $\CompCl(\Fft)$ carries a quasi-inverse bar-cobar homotopy equivalence (Corollary~\ref{cor:completion-closure-equivalence}), stable under MC twisting (Theorem~\ref{thm:mc-twisting-closure}), with completed twisting representability (Theorem~\ref{thm:completed-twisting-representability}).  The coefficient-stability criterion (Theorem~\ref{thm:coefficient-stability-criterion}) reduces convergence to finite matrix stabilization, and the uniform PBW bridge (Theorem~\ref{thm:uniform-pbw-bridge}) connects MC1 to MC4.  The $\mathcal{W}_\infty$ algebra at generic central charge is completed Koszul, unconditionally at all stages; $\mathcal{W}_N$ rigidity (Theorem~\ref{thm:winfty-all-stages-rigidity-closure}) resolves 21 conjectures and verifies the Yangian evaluation core at 249 test points.  The problem splits into MC4$^+$ (positive towers, solved by weight stabilization---Theorem~\ref{thm:stabilized-completion-positive}) and MC4$^0$ (resonant towers, reduced to a finite-dimensional problem by Theorem~\ref{thm:resonance-filtered-bar-cobar}).  The remaining example-specific task is coefficient stabilization on finite windows and H-level target identification.

\noindent\textbf{MC5} (Genus tower / BV-BRST comparison): the algebraic
genus recursion and the analytic sewing package are \textbf{proved},
but the full genuswise BV/BRST/bar identification remains open.  The
algebraic bar differential at genus~$g$ is determined inductively from
lower genera (Theorem~\ref{thm:inductive-genus-determination}).  Graph
amplitudes on~$\Sigma_g$ converge without UV renormalization in
dimension~$2$ (Proposition~\ref{prop:2d-convergence}).  The analytic
and algebraic differentials agree on the algebraic core
(Theorem~\ref{thm:analytic-algebraic-comparison}).  HS-sewing holds
for the standard landscape (Theorem~\ref{thm:general-hs-sewing}):
polynomial OPE growth and subexponential sector growth imply
convergent mode sums for all~$0 < q < 1$.  For the Heisenberg algebra,
the genus-$g$ partition function is the Fredholm determinant of the
one-particle Bergman sewing operator
\textup{(}Theorem~\ref{thm:heisenberg-one-particle-sewing}\textup{)}.
At genus~$0$, the algebraic BRST/bar comparison is proved, while the
top-degree amplitude pairing is only conditional
\textup{(}Theorem~\ref{thm:algebraic-string-dictionary},
Corollary~\ref{cor:string-amplitude-genus0}\textup{)}.


\section{Traverse of the volume}
\label{sec:traverse}
\index{volume structure}

The following is a chapter-by-chapter account of the mathematical
content, organized by Part.  Each entry identifies the objects
constructed, the theorems proved, and the chapter's role in the
three-ring architecture.

\subsection{Part~I: The Algebraic Engine}
\label{subsec:traverse-part-I}

Part~I is the proved core.  It contains the five main theorems, the
shadow Postnikov tower through arity~$4$, and the homotopy transfer
apparatus.  Nothing conjectural enters.

\smallskip\noindent
\textbf{The Fourier seed} (Chapter~\ref{ch:fourier-seed}).
Before the general theory, the Fourier seed exhibits the bar
construction as a transform: the generating function of bar
dimensions is a product formula, and inversion is Fourier
inversion.  This chapter serves as the zero-genus model calculation
that makes the later abstract machinery concrete.

\smallskip\noindent
\textbf{Algebraic foundations} (Chapter~\ref{ch:algebraic-foundations}).
Classical Koszul duality is recalled and re-derived in the language
of twisting morphisms and operadic bar-cobar adjunctions.  The key
objects: quadratic data $(V, R)$, the bar construction
$B(A) = (T^c(sV), d)$, the Koszul dual $A^! = T(V^*)/R^\perp$,
and the comparison map $\Omega(B(A)) \to A$ whose acyclicity is
Koszulness.  The homotopy transfer theorem
(Chapter~\ref{app:homotopy-transfer}) is proved here as the
mechanical heart of $A_\infty$ structure production: given a
strong deformation retract, the transferred operations on
cohomology satisfy $A_\infty$ or $L_\infty$ relations,
constructively.

\smallskip\noindent
\textbf{Configuration spaces} (Chapter~\ref{chap:config-spaces}).
The Fulton--MacPherson compactification $\overline{C}_n(X)$ is the
stage on which the bar complex lives.  Boundary strata are indexed
by nested partitions; the logarithmic forms $\eta_{ij}$ and their
Arnold relations are intrinsic to the compactification.  The chapter
proves the residue calculus on which $d^2 = 0$ depends and
establishes the cooperadic decomposition of boundary strata.

\smallskip\noindent
\textbf{The geometric bar complex} (Chapter~\ref{chap:bar-cobar}).
The bar complex $\barB_X(\cA) = (T^c(s^{-1}\bar{\cA}), d)$ is
constructed via residue extraction on~$\overline{C}_n(X)$.
Theorem~A is proved: the bar functor intertwines with Verdier
duality on $\operatorname{Ran}(X)$.  The identity
$d^2 = 0$ is the Arnold relation in degree~$2$ and the Borcherds
identity in degree~$3$.

\smallskip\noindent
\textbf{The geometric cobar complex}
(Chapter~\ref{chap:cobar-construction}).
The cobar construction $\Omega_X$ is the left adjoint of~$\barB_X$,
built from distributions and Verdier-dual residues.
$d^2_{\mathrm{cobar}} = 0$ follows from
$d^2_{\mathrm{bar}} = 0$ via Verdier duality
(Corollary~\ref{cor:cobar-nilpotence-verdier}).

\smallskip\noindent
\textbf{Bar-cobar adjunction and curved Koszul duality}
(Chapter~\ref{chap:bar-cobar-adjunction}).
The adjunction $\barB_X \dashv \Omega_X$ is proved and Theorem~B
(bar-cobar inversion on the Koszul locus) is established:
$\Omega_X \barB_X(\cA) \xrightarrow{\sim} \cA$ when the bar
spectral sequence degenerates at~$E_2$.  Off the Koszul locus,
curved $A_\infty$ structures and $I$-adic completions provide the
coderived continuation.

\smallskip\noindent
\textbf{Non-abelian Poincar\'e duality}
(Chapter~\ref{chap:NAP-koszul-derivation}).
The construction of Koszul dual cooperads via factorization homology
and Verdier duality on $\operatorname{Ran}(X)$.  This resolves the
apparent circularity in defining $\cA^!$: the Koszul dual is a
theorem (the Verdier dual of the bar coalgebra), not a definition by
analogy.

\smallskip\noindent
\textbf{Higher genus} (Chapter~\ref{chap:higher-genus}).
The genus tower is constructed.  The period-corrected propagator
$\eta_{12}^{(g)}$~\eqref{eq:genus-g-propagator-intro-ch0} on
$\Sigma_g$ creates curvature $\dfib^2 = \kappa(\cA)\omega_g$;
period integrals restore nilpotence
$\Dg{g}^2 = 0$.  Theorem~C (complementarity as Lagrangian
geometry) and Theorem~D (the scalar modular
characteristic~$\kappa$) are proved.  The ambient complementarity
complex is upgraded to shifted-symplectic Lagrangian geometry
(\S\ref{sec:ambient-complementarity-lagrangian}).

\smallskip\noindent
\textbf{Chiral Koszul pairs and deformation theory}
(Chapters~\ref{chap:koszul-pairs}--\ref{chap:deformation-theory}).
The notion of a chiral Koszul pair is formalized
(Definition~\ref{def:koszul-pair-classical}), the chiral modules
category is developed (Chapter~\ref{chap:chiral-modules}),
Theorem~H is proved (Chapter~\ref{chap:deformation-theory}):
$\ChirHoch^*(\cA)$ is polynomial on the Koszul locus and Koszul
functorial.

\smallskip\noindent
\textbf{Quantum corrections} (Chapter~\ref{ch:quantum-corrections}).
Arnold relations are deformed at higher genus.  The filtered-curved
hierarchy classifies which algebras carry curved (Virasoro) versus
truly filtered ($\mathcal{W}_3$) structures.

\smallskip\noindent
\textbf{Nonlinear modular shadows}
(Chapter~\ref{app:nonlinear-modular-shadows}).
The shadow Postnikov tower
$\kappa \to \Delta \to \mathfrak{C} \to \mathfrak{Q} \to \cdots$ is
constructed.  The quartic resonance class with its clutching law is
proved.  The shadow archetypes are classified: Heisenberg (Gaussian,
terminates at arity~$2$), affine (Lie/tree, terminates at
arity~$3$), $\beta\gamma$ (contact, terminates at arity~$4$),
Virasoro/$\mathcal{W}_N$ (mixed, infinite tower).  The branch-line
reductions (Appendix~\ref{app:branch-line-reductions}) extract
exact finite-dimensional quotients, including genus-$2$ transparency
and the universal genus-$3$ constant.


\subsection{Part~II: The Standard Landscape}
\label{subsec:traverse-part-II}

Part~II computes the algebraic engine on every major family.  Each
chapter is a complete portrait: bar complex, Koszul dual, genus
tower, modular characteristic, shadow archetype.

\smallskip\noindent
\textbf{Lattice vertex algebras} (Chapter~\ref{ch:lattice}).
The lattice construction $V_\Lambda$ carries an $\Eone$ sublattice
structure visible through screening operators.  The curvature
$\kappa(V_\Lambda) = \operatorname{rank}(\Lambda)$ is independent of
the $2$-cocycle (Theorem~\ref{thm:lattice:curvature-braiding-orthogonal}):
curvature and braiding are orthogonal.

\smallskip\noindent
\textbf{Free fields} (Chapter~\ref{chap:free-fields}).
Heisenberg, free fermion, and related free-field systems are
the periodic table of the volume.  Every higher structure can be
seen with the naked eye.  The genus expansion of the Heisenberg
partition function is computed via Eisenstein series.

\smallskip\noindent
\textbf{The $\beta\gamma$ system} (Chapter~\ref{chap:beta-gamma}).
The first complete nonabelian computation: bar complex, Koszul dual,
curvature tower.  The quartic contact invariant $\mu_{\beta\gamma}$
vanishes by rank-one abelian rigidity
(Corollary~\ref{cor:nms-betagamma-mu-vanishing}): the shadow
tower terminates at arity~$4$.

\smallskip\noindent
\textbf{Kac--Moody} (Chapter~\ref{chap:kac-moody}).
The bar complex of $\widehat{\mathfrak{g}}_k$ through degree~$5$
for $\mathfrak{sl}_2$, the Feigin--Frenkel involution as Koszul
duality, Sugawara construction (undefined at critical level
$k = -h^\vee$), PBW concentration at all genera.

\smallskip\noindent
\textbf{$\mathcal{W}$-algebras} (Chapter~\ref{chap:w-algebras}).
Drinfeld--Sokolov reduction as a Koszul functor.  $\mathcal{W}_3$
composite fields, minimal model fusion rules, the deep structure
of affine flag varieties and jet geometry.  Curved $A_\infty$
structures from higher-order poles.

\smallskip\noindent
\textbf{Deformation quantization}
(Chapters~\ref{ch:chiral-deformation}--\ref{chap:deformation-examples}).
Chiral Kontsevich formality, the passage from $\Pinf$-chiral to
$\Einf$-chiral, star products, and Maurer--Cartan deformations.

\smallskip\noindent
\textbf{Yangians} (Chapter~\ref{chap:yangians}).
The $\Eone$-chiral bar complex on ordered configuration spaces.
Koszul duality acts by $R$-matrix inversion.  The Drinfeld--Kohno
bridge to quantum groups
(Theorem~\ref{thm:factorization-dk-eval}).  The modular
dg-shifted Yangian $\mathcal{Y}^{\mathrm{dg,mod}}_T$ as a pro-MC element
(Definition~\ref{def:modular-yangian-pro}).

\smallskip\noindent
\textbf{Toroidal and elliptic} (Chapter~\ref{chap:toroidal-elliptic}).
Double affinization, Fay trisecant identity, elliptic $R$-matrices.
The marriage of modular and spectral themes.

\smallskip\noindent
\textbf{Genus expansions} (Chapter~\ref{ch:genus-expansions}).
Explicit expansions for all standard families: modular forms,
Eisenstein series, Mumford forms, bosonization at all genera.

\smallskip\noindent
\textbf{Detailed computations and combinatorial frontier}
(Chapters~\ref{chap:detailed-computations}--\ref{app:combinatorial-frontier}).
Bar complex tables through high degree, the combinatorial frontier
of bar cohomology dimensions, OEIS sequences, growth rates.  The
enumerative soul of the landscape.


\subsection{Part~III: Bridges and Programmes}
\label{subsec:traverse-part-III}

Part~III connects the algebraic engine to adjacent mathematics and
physics.

\smallskip\noindent
\textbf{Feynman diagrams} (Chapter~\ref{ch:feynman}).
The bar differential, coproduct, and transferred operations admit
a perturbative incarnation: Feynman graphs on
$\overline{C}_n(X)$ with the logarithmic propagator as edge weight.
Higher operations arise because graphs with nested collapses refuse
to stay binary.

\smallskip\noindent
\textbf{BV-BRST} (Chapter~\ref{ch:bv-brst}).
On the proved genus-$0$ locus, the bar complex is quasi-isomorphic to
the BRST complex.  The broader BV/bar dictionary and the QME/MC
comparison are recorded only as conditional or heuristic packages.
Gaiotto's perspective still motivates the higher operations by
holomorphic-topological brackets.

\smallskip\noindent
\textbf{Holomorphic-topological boundary conditions}
(Chapter~\ref{ch:ht-boundary}).
The 3d and chiral constructions situated inside the larger geography
of twists, reductions, and boundary phenomena in supersymmetric
field theory.  The open-sector language of modules, lines, and
boundaries prepares for Volume~II.

\smallskip\noindent
\textbf{Yang--Mills boundary theory}
(Chapters~\ref{ch:ym-boundary-theory}--\ref{ch:ym-instanton-screening}).
Bar-cobar duality as a holographic gauge/gravity bridge.
Boundary BRST, central formality, mixed couplings, relative
Hochschild duality, instanton completion, screening, and the
mass-gap reduction.

\smallskip\noindent
\textbf{$\En$ Koszul duality} (Chapter~\ref{ch:en-koszul-duality}).
Higher-dimensional bar complexes, Totaro relations, the
Ayala--Francis connection.  The programme lifts from curves to
higher-dimensional manifolds.

\smallskip\noindent
\textbf{Derived structures and geometric Langlands}
(Chapter~\ref{ch:derived-langlands}).
At critical level $k = -h^\vee$, the bar complex recovers the
algebra of opers.  The critical-level bar complex, oper space,
Kazhdan--Lusztig equivalence from bar-cobar.


\subsection{Part~IV: The Frontier}
\label{subsec:traverse-part-IV}

\textbf{Outlook} (Chapter~\ref{chap:concordance}).
The five main theorems, three-pillar foundational architecture,
three concentric rings, Koszulness characterization programme,
and open frontiers.

\smallskip\noindent
\textbf{Volume~II} (\emph{$A_\infty$ Chiral Algebras and 3d
Holomorphic-Topological QFT}).
The bar complex's differential is $C$-direction factorization; its
coproduct is $R$-direction factorization.  Together: the
Swiss-cheese algebra on $\FM_k(\C) \times \operatorname{Conf}_k(\R)$.
At genus $g \ge 1$: curved Swiss-cheese with curvature $\kappa \cdot
\omega_g$.  Volume~II proves the recognition theorem and the
homotopy-Koszulity of $\mathrm{SC}^{\mathrm{ch,top}}$, derives line
operators as $\cA^!$-modules, and develops the full
bulk/boundary/line Koszul triangle.


\section{Dictionary: chiral algebra types and Koszul dualities}
\label{sec:dictionary}

Collision geometry on a curve forces three levels of algebraic
structure---commutative ($\Einf$), associative ($\Eone$),
semiclassical ($\Pinf$)---each carrying its own Koszul duality.

\begin{definition}[\texorpdfstring{$\Einf$}{E-infinity}-chiral algebra = vertex algebra]
\label{def:einf-chiral}
\index{$\mathbb{E}_\infty$-chiral algebra|textbf}
Throughout, $k = \mathbb{C}$ and $X$ is a smooth algebraic curve.
The ambient $\infty$-category is $\mathcal{D}\text{-mod}(X)$,
equipped with the chiral tensor product
$\otimes^{\mathrm{ch}}$~\cite[\S3.4]{BD04}.  Duality means
Verdier duality~$\mathbb{D}$ of holonomic $\mathcal{D}$-modules
(Remark~\ref{rem:verdier-vs-linear}).

An \emph{$\Einf$-chiral algebra} on $X$ is a commutative algebra
in $(\mathcal{D}\text{-mod}(X),\allowbreak
\otimes^{\mathrm{ch}})$
(cf.~\cite[§5.1]{HA}).
Equivalent formulations:
(a)~$\mathcal{D}_X$-module with chiral Lie
bracket~\cite[\S3.3]{BD04};
(b)~vertex algebra~\cite[\S3.7]{FBZ04} for $X = \mathbb{A}^1$;
(c)~factorization algebra on $\operatorname{Ran}(X)$
(Definition~\ref{def:factorization-algebra-AF}).
\end{definition}

\begin{remark}[Verdier versus linear duality]
\label{rem:verdier-vs-linear}
\index{Verdier duality}
$\mathbb{D}(\mathcal{M})$ is the Verdier dual
$\mathcal{D}$-module, not the na\"{\i}ve linear dual
$\mathrm{Hom}_k(\mathcal{M}, k)$.  The Koszul
dual~$\cA^!$ is constructed via Verdier duality on
$\overline{C}_n(X)$
(Theorem~\ref{thm:bar-cobar-verdier}).
\end{remark}

\begin{definition}[\texorpdfstring{$\Eone$}{E1}-chiral algebra = nonlocal vertex algebra]
\label{def:e1-chiral}
\index{$\mathbb{E}_1$-chiral algebra|textbf}
An \emph{$\Eone$-chiral algebra} on $X$ is an associative (but not
necessarily commutative) algebra over the associative chiral
operad~$\chirAss$ (Definition~\ref{def:chiral-ass-operad}):
a $\mathcal{D}_X$-module $\cA$ with binary chiral operation
$\mu\colon \cA \chirtensor \cA \to \Delta_!\cA$
satisfying $\mu \circ (\mu \otimes \operatorname{id})
= \mu \circ (\operatorname{id} \otimes \mu)$
but not necessarily skew-symmetry.
In vertex algebra language, a nonlocal vertex algebra
(Li~\cite{Li96}; cf.~\cite[\S4.1]{HA};
Beilinson--Drinfeld~\cite[Theorem~3.4.9]{BD04};
Francis--Gaitsgory~\cite{FG12}).
\end{definition}

\begin{definition}[The associative chiral operad]
\label{def:chiral-ass-operad}
The \emph{associative chiral operad} $\chirAss$ on $X$ is the
non-$\Sigma$ chiral operad with
$\chirAss(n) = \Delta^{(n)}_!\, \omega_X$, supported on the small
diagonal without $\Sigma_n$-action.  Quadratic presentation:
$\chirAss = \operatorname{Free}^{\mathrm{ch}}(\mu) /
(\mu \circ_1 \mu = \mu \circ_2 \mu)$.
\end{definition}

\begin{proposition}[\texorpdfstring{$\chirAss$}{Ass-ch} self-duality;
\ClaimStatusProvedHere]
\label{prop:chirAss-self-dual}
$(\chirAss)^! \cong \chirAss \otimes \operatorname{sgn}$.
$\Eone$--$\Eone$ Koszul duality stays within the same operadic
type.
\end{proposition}
\begin{proof}
Classically $\operatorname{Ass}^! \cong \operatorname{Ass} \otimes
\operatorname{sgn}$~\cite[Theorem~7.4.1]{LV12}.  The chiral lift
preserves Koszul duality:
$(\mathcal{P}^{\mathrm{ch}})^! \cong (\mathcal{P}^!)^{\mathrm{ch}}$
since the orthogonal complement commutes with the chiral tensor
product.
\end{proof}

\begin{definition}[\texorpdfstring{$\Pinf$}{P-infinity}-chiral algebra = chiral Poisson]
\label{def:pinf-chiral}
A \emph{$\Pinf$-chiral algebra} is an $\Einf$-chiral algebra with
a compatible $L_\infty$ structure via Leibniz: commutative chiral
product and Lie bracket coexist.
\end{definition}

\begin{remark}[\texorpdfstring{$E_n$}{En} terminology]\label{rem:En-terminology}
\index{$E_n$ terminology}
The subscripts $\Einf$, $\Eone$, $\Pinf$ refer to the
factorization level inside the BD chiral tensor category.  Bare
$E_n$ and $\En$ refer to little-disks
algebras~\cite[\S5.1]{HA}.  An $\Eone$-chiral algebra on a
curve is not a topological $E_1$-algebra; the suffix
``-chiral'' is load-bearing.
\end{remark}

The hierarchy
$\Einf\text{-chiral} \subset \Pinf\text{-chiral} \subset
\Eone\text{-chiral}$.
Deformation quantization passes from $\Pinf$ to $\Eone$.
Bar-cobar duality operates at each level: $\Einf$ computes
factorization homology; $\Eone$ connects to Yangians and quantum
groups (Chapter~\ref{chap:yangians}).

\begin{remark}[Operadic hierarchy and configuration spaces]
\label{rem:operadic-hierarchy-table}
\index{operadic hierarchy!chiral algebras}
The hierarchy is organized by the geometry of insertion points:
\begin{center}
\small
\begin{tabular}{llll}
\textbf{Algebra type} & \textbf{Operad} &
  \textbf{Configuration space} & \textbf{Koszul exchange} \\ \hline
Heisenberg, KM, Vir, $\mathcal{W}$
  & $\Einf$ (comm.)
  & $C_n(X)$ unordered
  & type exchange $\cA \leftrightarrow \cA^!$ \\[2pt]
Lattice VOA
  & $\Einf$ with $\Eone$ sublattice
  & $C_n(X)$ with monodromy
  & type exchange + screening \\[2pt]
Yangian $Y(\fg)$
  & $\Eone$ (assoc.)
  & $\operatorname{Conf}_n^{\mathrm{ord}}(X)$ ordered
  & $R$-matrix inversion \\[2pt]
Elliptic QG
  & modular deformation
  & $\operatorname{Conf}$ on $E_\tau$
  & elliptic $R$-inversion
\end{tabular}
\end{center}
The progression from $\Einf$ to $\Eone$ is not ``more
generality''; it is a change in the geometry of collision, from
symmetric to ordered, with correspondingly different Koszul
exchange mechanisms.
\end{remark}

\begin{remark}[Three Koszul duality mechanisms]
\label{rem:three-koszul-mechanisms}
\begin{enumerate}[label=(\roman*)]
\item \emph{Francis--Gaitsgory}~\cite{FG12}:
  $\chirLie\text{-alg} \simeq \chirCom\text{-coalg}$ via
  Chevalley/Primitives, under pro-nilpotence.
\item \emph{Bar-cobar resolution}: $\Omega(\bar{B}(\cA)) \to \cA$
  is a quasi-isomorphism on the Koszul locus, or in
  completed/coderived regimes
  (Appendix~\ref{app:nilpotent-completion}).
\item \emph{Quadratic/Priddy}:
  $\cA \mapsto \cA^! = T(V^*)/R^\perp$, with
  $\bar{B}(\cA)^\vee \simeq \cA^!$ when $E_2$-degeneration holds (the bar coalgebra, dualized, recovers the Koszul dual algebra).
\end{enumerate}
Every theorem specifies which mechanism is invoked.  All three
admit configuration space realizations.  At higher genus, they fuse
via the Feynman transform of the modular operad.
\end{remark}

\begin{definition}[Anomaly ratio]\label{def:anomaly-ratio}
\index{anomaly ratio|textbf}
For $c(\cA) \neq 0$: $\varrho(\cA) := \kappa(\cA)/c(\cA)$.
For $\mathcal{W}$-algebras,
$\varrho(\mathfrak{g}) = \sum_{i=1}^r 1/(m_i+1)$, independent
of~$k$.
\end{definition}

\begin{definition}[Koszul conductor]\label{def:koszul-conductor}
\index{Koszul conductor|textbf}
$K(\cA) := c(\cA) + c(\cA^!)$.  Values: $2\dim\mathfrak{g}$ for
$\widehat{\mathfrak{g}}_k$; $26$ for Virasoro; $100$ for
$\mathcal{W}_3$.
\end{definition}

\begin{definition}[Koszul spectrum]\label{def:koszul-spectrum}
\index{Koszul spectrum|textbf}
$\operatorname{KSpec}(\cA) := (c(\cA), \kappa(\cA),
\varrho(\cA))$.  Additive under tensor products in $c$
and~$\kappa$.
\end{definition}

\begin{remark}[Existence regimes]\label{rem:existence-regimes}
\label{rem:existence-koszul}
Three regimes, not one universal theorem:
(i)~\emph{strict}, where $\bar{B}_X$ and inversion hold on the
Koszul locus;
(ii)~\emph{completed}, under inverse-limit hypotheses
(Appendix~\ref{app:nilpotent-completion});
(iii)~\emph{comparison}
(Appendix~\ref{app:existence-criteria}).
The load-bearing distinction: $\bar{B}_X(\cA)$ may be constructed
beyond where $\Omega_X\bar{B}_X(\cA) \simeq \cA$ is proved.
\end{remark}


\section{The master invariant table}
\label{sec:master-invariant-table-intro}
\index{master invariant table}

The entire standard landscape of Part~II is summarized in a single table of modular Koszul invariants.  For each family, the table records the modular characteristic~$\kappa$, the central charge~$c$, the Koszul conductor~$K = c + c'$, the spectral discriminant~$\Delta$, and the shadow archetype (termination arity of the shadow tower).

\begin{center}
\small
\begin{tabular}{llllll}
\textbf{Family} & $c$ & $\kappa$ & $K$ & $\Delta$ & \textbf{Archetype} \\ \hline
Heisenberg $\mathcal{H}_k$ & $1$ & $1/k$ & $2$ & $1$ & Gaussian, $@2$ \\[2pt]
$\widehat{\mathfrak{sl}}_2$ at level $k$ & $\frac{3k}{k+2}$ & $\frac{3}{k+2}$ & $6$ & $(1-3x)(1+x)$ & Lie/tree, $@3$ \\[2pt]
$\beta\gamma$ & $-2$ & $-1$ & $0$ & $(1-3x)(1+x)$ & contact, $@4$ \\[2pt]
Virasoro $c$ & $c$ & $\frac{c}{2}$ & $26$ & $(1-3x)(1+x)$ & mixed, $\infty$ \\[2pt]
$\mathcal{W}_3$ & $c(k)$ & DS & $100$ & quartic & mixed, $\infty$ \\[2pt]
$V_\Lambda$ (lattice) & $\mathrm{rk}(\Lambda)$ & $\mathrm{rk}(\Lambda)$ & $2\mathrm{rk}$ & --- & braided \\[2pt]
$Y(\mathfrak{sl}_2)$ & --- & --- & --- & --- & $\Eone$, $R$-matrix
\end{tabular}
\end{center}
\noindent
The shared discriminant $\Delta(x) = (1-3x)(1+x)$ across $\widehat{\mathfrak{sl}}_2$, Virasoro, and $\beta\gamma$ is a consequence of Drinfeld--Sokolov reduction as a Koszul functor: these algebras lie on the same spectral sheet because they are related by quantum Hamiltonian reduction, which preserves the discriminant.  The zeros at $x = 1/3$ and $x = -1$ mark bar-acyclic/bar-active transitions; they are invariants of the planted-forest tropicalization $G_{\mathrm{pf}} = \mathrm{Trop}(\operatorname{FM}_n(C \mathbin{|} D))$ (Theorem~\ref{thm:planted-forest-tropicalization}).  The three algebras have different shadow depths ($r_{\max} = 3, \infty, 4$) but identical genus-$0$ spectral geometry: the discriminant depends only on $\Theta_\cA^{\leq 2}$, which is a DS invariant (Definition~\ref{def:envelope-shadow-functor}).


\section{Conventions}
\label{sec:standing-assumptions}

\begin{convention}[Semantic levels: homotopy, model, shadow]
\label{conv:hms-levels}
\index{semantic levels|textbf}
\index{H/M/S convention|see{semantic levels}}

\smallskip\noindent\textbf{H-level (homotopy/native).}
The statement in the stable $\infty$-category of factorization
(co)algebras on $\operatorname{Ran}(X)$, or in the associated
formal moduli / shifted-symplectic language.  Defined up to
contractible ambiguity.

\smallskip\noindent\textbf{M-level (model).}
An explicit dg / $L_\infty$ / filtered complex.  The geometric
bar complex $\bar{B}_X(\cA) = (T^c(s^{-1}\bar{\cA}), d)$ built
from configuration space integrals on $\overline{C}_n(X)$ is the
canonical model.

\smallskip\noindent\textbf{S-level (shadow).}
The cohomological, Ext, numerical, or generating-function
consequence: bar cohomology dimensions, Hilbert series,
$\kappa(\cA)$, free energies $F_g(\cA)$.

\smallskip\noindent\textbf{Notation.}
\begin{itemize}
\item $\simeq$\,: equivalence in the homotopy/$\infty$-categorical
  sense (H-level).
\item $\cong$\,: strict isomorphism of chosen dg models (M-level).
\item $=$\,: definition or literal equality only.
\end{itemize}
The proved results (Remark~\ref{rem:two-strata}) are proved at
M-level and stated at H-level.

\smallskip\noindent\textbf{Model independence.}
Any two admissible dg presentations are connected by a contractible
space of quasi-isomorphisms
(Proposition~\ref{prop:model-independence}).
\end{convention}

\begin{convention}[Regime tags]\label{conv:regime-tags}\label{conv:proof-architecture}
\index{regime tag}
Four regimes, in order of increasing generality:
\begin{enumerate}
\item \textbf{Quadratic.}  $\dzero^2 = 0$;
  Theorems~A--D hold without qualification.
  Heisenberg, free fermions, lattice VOAs.
\item \textbf{Curved-central.}
  $\dfib^{\,2} = \kappa \cdot \omega_g$; $\Dg{g}^{\,2} = 0$.
  $\widehat{\mathfrak{g}}_k$ at non-critical level, Virasoro
  with $c \neq 0$.
\item \textbf{Filtered-complete.}  Complete filtration with
  $\operatorname{gr}^F \cA$ quadratic Koszul.
  $\mathcal{W}$-algebras via DS reduction, deformation
  quantizations.
\item \textbf{Programmatic.}  Stratum~II
  (Remark~\ref{rem:two-strata}); stated as conjectures.
  Derived DK, $\mathbb{E}_n$ for $n \geq 2$.
\end{enumerate}
The four regimes correspond to increasing monodromy of the
categorical logarithm: single-valued, scalar curvature, filtered
convergence, full structure.
\end{convention}

\begin{convention}[Standing assumptions]
\label{conv:standing-assumptions}
\begin{enumerate}[label=\textup{(SA\arabic*)}]
\item\label{SA:ground-field} Ground field $\mathbb{C}$.  All
  vector spaces, algebras, and categories are
  over~$\mathbb{C}$.
\item\label{SA:grading} Cohomological grading: $|d| = +1$.
  See Appendix~\ref{app:signs}, \S\ref{app:sign-conventions}.
\item\label{SA:convergence} Formal power series in completed
  tensor products.  Configuration space integrals converge
  absolutely on FM compactifications
  (Theorem~\ref{thm:FM-convergence}).
\item\label{SA:curves} Smooth projective geometrically connected
  curves over~$\mathbb{C}$.
\item\label{SA:completion} Completed chiral algebras at
  genus~$g \geq 1$
  (Appendix~\ref{app:nilpotent-completion}).
\end{enumerate}
\end{convention}

\subsection{Ambient category}
\label{subsec:ambient-category}

All $\mathcal{D}$-modules are holonomic.  The cobar
construction is the Verdier dual of the bar complex
(Definition~\ref{def:geom-cobar-intrinsic}), so
$d_{\mathrm{cobar}}^2 = 0$ follows from $d_{\mathrm{bar}}^2 = 0$
(Corollary~\ref{cor:cobar-nilpotence-verdier}).
Grading is cohomological ($|d| = +1$); see
Appendix~\ref{app:signs}.
\index{grading convention!cohomological}
All chiral algebras are augmented
($\varepsilon\colon \cA \to \omega_X$,
$\bar{\cA} = \ker(\varepsilon)$);
the bar complex is always reduced.
Forms $\eta_{ij} \in \Omega^1(\log D)$ and residue maps are
intrinsic to the FM compactification.

\subsection{Critical pitfalls}
\label{subsec:critical-pitfalls}
\index{critical pitfalls|textbf}

The following distinctions are load-bearing and are the most common sources of error:

\smallskip\noindent\textbf{Four objects.}
$\cA$ is the algebra.  $\barB(\cA)$ is the bar coalgebra.  $\cA^{\scriptstyle \text{\normalfont !`}} = H^*(\barB(\cA))$ is the Koszul dual coalgebra.  $\cA^! = (\cA^{\scriptstyle \text{\normalfont !`}})^\vee$ is the Koszul dual algebra.  The cobar $\Omega(\barB(\cA))$ recovers~$\cA$ (bar-cobar \emph{inversion}, not duality); $\cA^!$ is obtained by Verdier/linear duality, not cobar.

\smallskip\noindent\textbf{Grading.}
Cohomological ($|d| = +1$) throughout.  The bar complex uses desuspension: $s^{-1}$ lowers degree by~$1$.

\smallskip\noindent\textbf{Classical vs.\ chiral Koszul duality.}
$\mathrm{Com}^! = \mathrm{Lie}$ (not $\mathrm{coLie}$).  The Heisenberg algebra is \emph{not} self-dual.  Chiral Koszulness is not the same as classical Koszulness.

\smallskip\noindent\textbf{Curved $A_\infty$.}
$m_1^2(a) = [m_0, a]$ (commutator, with a minus sign).  The bar differential satisfies $d^2 = 0$ always; curvature shows as $m_1^2 \neq 0$ in the $A_\infty$ algebra, not in the bar complex.

\smallskip\noindent\textbf{Sugawara.}
The Sugawara construction is \emph{undefined} at critical level $k = -h^\vee$---the Sugawara vector does not lie in the appropriate completion.  This is not ``$c$ diverges''; the construction simply does not exist.

\smallskip\noindent\textbf{Virasoro self-duality.}
The Virasoro algebra is self-dual at $c = 13$, not $c = 26$.  The Koszul dual is $\mathrm{Vir}_{26-c}$.

\smallskip\noindent\textbf{FM compactification.}
The Fulton--MacPherson compactification is the blowup along diagonals, not the complement of diagonals.

\smallskip\noindent\textbf{Prime form.}
The prime form $E(z_1, z_2)$ is a section of $K^{-1/2} \boxtimes K^{-1/2}$, not $K^{+1/2}$.

\smallskip\noindent\textbf{QME.}
The quantum master equation is $\hbar \Delta S + \frac{1}{2}\{S, S\} = 0$ (factor $\frac{1}{2}$, not~$1$).

\smallskip\noindent\textbf{$d_{\mathrm{bracket}}^2 \neq 0$.}
The bracket component of the bar differential does not square to zero on its own: this is proved for all $2048$ sign conventions for $\mathfrak{sl}_2$.  The full Borcherds differential gives $d^2 = 0$.

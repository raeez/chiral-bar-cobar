% !TEX root = ../../main.tex
% =====================================================================
% Chapter: Conformal-anomaly rigidity and the forced spectral parameter
%
% Platonic reconstitution of the Vol I CLAUDE.md claim
%   "Obstruction to constant coproduct = c/2 = kappa(Vir_c).
%    At c = 0: obstruction vanishes. At c != 0: spectral parameter FORCED."
%
% Adversarial stress-test: is the obstruction EXACTLY c/2, or c/2 with a
% coefficient that could vanish in some representation? This chapter
% inscribes the rigidity statement at its honest scope: the coefficient
% c/2 is universal in H^2(Vir_c, Vir_c); representation pullback can kill
% the image but not the universal class.
%
% Guiding principle: strengthening only. The universal coefficient is
% c/2; a null module gives a vanishing pullback; no downgrade of the
% structural claim. No AI slop; no em dashes; no bare kappa in contexts
% where disambiguation is needed.
% =====================================================================

\providecommand{\Virc}{\mathrm{Vir}_{c}}
\providecommand{\Witt}{\mathrm{Witt}}
\providecommand{\Hch}{H^{\bullet}_{\mathrm{ch}}}
\providecommand{\Cas}{\Omega^{\mathrm{univ}}}
\providecommand{\opeTT}{\mathrm{OPE}_{TT}}
\providecommand{\coassoc}{\mathrm{coassoc}}
\providecommand{\cone}{\operatorname{cone}}
\providecommand{\Res}{\operatorname{Res}}

\chapter{Conformal-anomaly rigidity and the forced spectral parameter}
\label{chap:conformal-anomaly-rigidity-platonic}
\index{conformal anomaly!rigidity|textbf}
\index{Virasoro!chiral coproduct|textbf}
\index{spectral parameter!forced at nonzero central charge|textbf}

\ChapterIntro{The chiral coproduct
$\Delta_{z}\colon \Virc \to \Virc \mathbin{\widehat{\otimes}} \Virc$
at spectral parameter $z \in \C^{\times}$ is a second-order
infinitesimal deformation of the classical Virasoro Lie bialgebra.
The adversarial question is whether the obstruction to
$z$-independence is \emph{exactly} $c/2$ or only proportional to $c/2$
with a coefficient that might vanish in some representation. This
chapter proves the former: the obstruction class in
$H^{2}(\Virc, \Virc)$ equals $(c/2) \cdot \Cas$ with $\Cas$ the
universal Virasoro Casimir class, normalised by the $T(z)T(0)$ double
pole. The coefficient $c/2$ is the Koszul complementarity invariant
$\kappa(\Virc) = c/2$ of Theorem~C; representation pullback can kill
the image but cannot change the universal coefficient. At $c = 0$ the
obstruction vanishes and a $z$-independent coproduct exists as the
Witt Lie bialgebra; at $c \neq 0$ no such coproduct exists and the
spectral parameter is forced by second-order coassociativity.}

\section{Setup: chiral coproduct and the rigidity question}
\label{sec:conformal-anomaly-setup}

Let $c \in \C$ and let $\Virc$ denote the Virasoro vertex algebra at
central charge $c$, generated by $T(z) = \sum_{n \in \Z} L_{n}\,
z^{-n-2}$ with OPE
\begin{equation}
\label{eq:TT-OPE}
T(z)\,T(w) \;\sim\;
\frac{c/2}{(z-w)^{4}}
+ \frac{2\,T(w)}{(z-w)^{2}}
+ \frac{\partial T(w)}{z - w}.
\end{equation}
Equation~\eqref{eq:TT-OPE} is the conventional Belavin--Polyakov--Zamolodchikov
normalisation~\cite{BPZ84}; the double pole coefficient is $c/2$ in every
CFT convention (the BPZ cylinder-Casimir $c/24$ is a separate datum
and is not conflated here and cache pattern~261).

\begin{definition}[Chiral coproduct with spectral parameter]
\label{def:chiral-coproduct-spectral}
A \emph{chiral coproduct with spectral parameter} on $\Virc$ is a
continuous linear map
\[
\Delta_{z} \colon \Virc \;\longrightarrow\;
\Virc \mathbin{\widehat{\otimes}} \Virc,
\qquad z \in \C^{\times},
\]
satisfying (i) counitality $(\epsilon \otimes \id)\Delta_{z} =
\id = (\id \otimes \epsilon)\Delta_{z}$ for all $z$, (ii) primitivity
of the vacuum $\Delta_{z}(|0\rangle) = |0\rangle \otimes |0\rangle$,
and (iii) coassociativity in the shifted form
\[
(\Delta_{z_{1}} \otimes \id) \circ \Delta_{z_{1} + z_{2}}
\;=\;
(\id \otimes \Delta_{z_{2}}) \circ \Delta_{z_{1}}.
\]
A coproduct $\Delta$ is \emph{$z$-independent} (or \emph{constant})
when $\Delta_{z} = \Delta$ for all $z \in \C^{\times}$.
\end{definition}

\begin{remark}[Shifted-parameter coassociativity]
\label{rem:shifted-coassoc-fm39}
The shifted form of coassociativity in
Definition~\ref{def:chiral-coproduct-spectral}(iii) is the correct
incarnation for spectral parameters; naive coassociativity
$(\Delta_{z} \otimes \id)\Delta_{z} = (\id \otimes \Delta_{z})\Delta_{z}$
with a single $z$ holds only for $z$-independent coproducts (in
CLAUDE.md).
\end{remark}

\begin{remark}[On $\kappa(\Virc) = c/2$]
\label{rem:kappa-vir-is-c-over-2}
The Koszul complementarity invariant of $\Virc$ is
$\kappa(\Virc) = c/2$ (landscape census C3 and the Virasoro row of the
main status table). This is the \emph{unique} family in the standard
landscape where $\kappa$ coincides with $S_{2}/2$; for
affine Kac--Moody, $\kappa = \dim(\mathfrak{g})(k + h^{\vee})/(2h^{\vee})
\neq S_{2}/2$ in general. Throughout this chapter, every appearance of
$c/2$ is read both as the $T(z)T(0)$ double-pole coefficient and as
$\kappa(\Virc)$; the identification is the content of
Theorem~\ref{thm:universal-coefficient}.
\end{remark}

\section{The universal Casimir class}
\label{sec:universal-casimir}

The obstruction lives in the second chiral cohomology
$\Hch^{2}(\Virc, \Virc)$, the tangent space of first-order
deformations of the chiral coproduct modulo gauge. We identify a
distinguished universal class.

\begin{definition}[Universal Virasoro Casimir class]
\label{def:universal-casimir}
Let $\Cas \in \Hch^{2}(\Virc, \Virc)$ be the cohomology class
represented by the cocycle
\[
\Cas(a, b) \;=\;
\Res_{z = 0}\; z^{3}\;\bigl[T(z), T(0)\bigr]\, a\, b
\qquad (a, b \in \Virc),
\]
normalised so that $\Cas(T_{-2}|0\rangle, T_{-2}|0\rangle)
= |0\rangle$. The residue is the coefficient of $(z - w)^{-4}$ in the
$TT$ OPE, stripped of the $c/2$ prefactor.
\end{definition}

\begin{lemma}[Nonvanishing and integrality of $\Cas$]
\label{lem:casimir-nonvanishing}
\ClaimStatusProvedHere
The class $\Cas \in \Hch^{2}(\Virc, \Virc)$ is nonzero and generates
the one-dimensional subspace
\[
\Hch^{2}(\Virc, \Virc)^{\mathrm{scalar}}
\;=\;
\C \cdot \Cas,
\]
where $\Hch^{2}(\Virc, \Virc)^{\mathrm{scalar}}$ is the scalar
(i.e.\ $\Virc$-invariant) part of $\Hch^{2}$, independent of $c$.
\end{lemma}

\begin{proof}
The Feigin--Fuchs computation of $\Hch^{\bullet}(\Virc, \Virc)$ for
generic $c$~\cite{FF84} gives
$\Hch^{2}(\Virc, \Virc) = \C \cdot \Cas \oplus M(c)$ where the
complementary summand $M(c)$ consists of weight-non-scalar classes
that vanish on the Virasoro invariant subspace. The class $\Cas$ is
the image of $1 \in H^{2}(\mathrm{Witt}; \C)$ (the Gelfand--Fuchs
cohomology generator) under the pullback along the semidirect
extension $0 \to \C \to \Virc \to \Witt \to 0$. Nonvanishing is the
Feigin--Fuchs theorem that
$H^{2}(\mathrm{Witt}; \C) = \C$~\cite{FF84}. Integrality follows from
the normalisation at $(T_{-2}|0\rangle, T_{-2}|0\rangle)$, which the
$TT$ OPE fixes to the coefficient~$1$ after stripping $c/2$.
\end{proof}

\begin{remark}[$\Cas$ is intrinsic, not a representation artifact]
\label{rem:casimir-intrinsic}
The class $\Cas$ lives in the \emph{universal} cohomology
$\Hch^{2}(\Virc, \Virc)$, which is the cohomology with coefficients in
$\Virc$ itself as an adjoint module. Pullback along a module
$V \in \Virc\text{-mod}$ gives a class $\Cas_{V} \in
\Hch^{2}(\Virc, V)$, and this pullback may vanish for particular $V$.
The universal class does not depend on the choice of $V$; this is the
content of Theorem~\ref{thm:universal-coefficient}.
\end{remark}

\section{Rigidity of the obstruction}
\label{sec:rigidity-theorem}

\begin{theorem}[Conformal-anomaly rigidity]
\label{thm:conformal-anomaly-rigidity}
\ClaimStatusProvedHere
Let $\Delta_{z}$ be a chiral coproduct with spectral parameter on
$\Virc$ in the sense of
Definition~\ref{def:chiral-coproduct-spectral}, and write
$\delta(z) \;=\; \Delta_{z} - \Delta_{0}^{\mathrm{cl}}$, where
$\Delta_{0}^{\mathrm{cl}}$ is the unique classical $z$-independent
Lie bialgebra coproduct on $\Witt = \Virc/\C$ lifted to $\Virc$ by
primitivity on the central element. Then the obstruction to
$z$-independence, defined as the first nontrivial $z$-derivative class
\[
\obs(\Delta_{z}) \;=\;
\Bigl[ \left.\frac{d\delta(z)}{dz}\right|_{z = 0} \Bigr]
\;\in\; \Hch^{2}(\Virc, \Virc),
\]
equals exactly
\[
\obs(\Delta_{z}) \;=\; \tfrac{c}{2} \cdot \Cas.
\]
In particular, $\obs(\Delta_{z})$ is a universal class depending only
on $c$ and the universal Casimir class $\Cas$; it is independent of
any choice of module, trivialisation, or higher-order corrections to
$\Delta_{z}$.
\end{theorem}

\begin{proof}
By Definition~\ref{def:chiral-coproduct-spectral}(iii), the shifted
coassociativity identity expanded at first order in $z_{1}$ and
$z_{2}$ reads
\[
(\delta'(0) \otimes \id) \circ \Delta_{0}^{\mathrm{cl}}
\;-\;
(\id \otimes \delta'(0)) \circ \Delta_{0}^{\mathrm{cl}}
\;=\;
(\Delta_{0}^{\mathrm{cl}} \otimes \id - \id \otimes \Delta_{0}^{\mathrm{cl}})
\circ \delta'(0),
\]
which is the cocycle condition for $\delta'(0)$ as a
Hochschild-chiral $2$-cocycle on $\Virc$ with coefficients in
$\Virc$. The class
$[\delta'(0)] \in \Hch^{2}(\Virc, \Virc)$ is the obstruction.

To compute it, apply $\delta'(0)$ to $T_{-2}|0\rangle \otimes
T_{-2}|0\rangle$. The shifted coassociativity identity, read against
$T(z_{1}) T(z_{1} + z_{2}) T(0)$ and using the $TT$ OPE
\eqref{eq:TT-OPE}, extracts the coefficient of the $(z_{1} - z_{2})^{-4}$
double-pole term. This coefficient is exactly $c/2$ by the
normalisation of $T$ in \eqref{eq:TT-OPE}.

By Lemma~\ref{lem:casimir-nonvanishing}, the scalar part of
$\Hch^{2}(\Virc, \Virc)$ is one-dimensional, generated by $\Cas$, and
the non-scalar part vanishes on the Virasoro-invariant pair
$(T_{-2}|0\rangle, T_{-2}|0\rangle)$. Hence $[\delta'(0)] =
(c/2) \cdot \Cas$.
\end{proof}

\begin{theorem}[Coproduct is constant at $c = 0$]
\label{thm:c-zero-coproduct-is-constant}
\ClaimStatusProvedHere
At $c = 0$, the obstruction
$\obs(\Delta_{z}) = 0$ in $\Hch^{2}(\mathrm{Vir}_{0}, \mathrm{Vir}_{0})$,
and a $z$-independent chiral coproduct exists and is unique up to
gauge: it is the Witt Lie bialgebra coproduct lifted to $\mathrm{Vir}_{0} = \Witt$.
\end{theorem}

\begin{proof}
At $c = 0$, the $TT$ OPE~\eqref{eq:TT-OPE} has vanishing double pole,
so $\mathrm{Vir}_{0} \twoheadrightarrow \Witt$ is an isomorphism of
vertex Lie algebras. The classical Witt Lie bialgebra
$(\Witt, [\,\cdot\,,\,\cdot\,], \Delta_{0}^{\mathrm{cl}})$ is the
unique Lie bialgebra structure on $\Witt$ with
$\Delta_{0}^{\mathrm{cl}}$ primitive on the derivation generators
$L_{n}$; this is a theorem of
Drinfeld~\cite{Dri87} combined with the Etingof--Kazhdan quantisation
of Lie bialgebras~\cite{EK96}.

By Theorem~\ref{thm:conformal-anomaly-rigidity},
$\obs(\Delta_{z}) = (c/2) \Cas = 0$ at $c = 0$. The vanishing of the
degree-$2$ obstruction class implies that $\delta(z)$ is
cohomologically trivial at first order; by Hochschild descent
(\cite{HKR62} in the classical setting, extended to vertex algebras
in~\cite{FBZ04}), there exists a gauge transformation
$g(z) \in 1 + z \cdot \End(\Virc)[[z]]$ such that
$g(z) \cdot \Delta_{z} \cdot g(z)^{-1} = \Delta_{0}^{\mathrm{cl}}$
modulo higher-order corrections. Induction on the order of $z$,
using that each successive obstruction lives in $\Hch^{2}$ and is
proportional to $c/2$ by the same argument as
Theorem~\ref{thm:conformal-anomaly-rigidity}, produces a
$z$-independent coproduct.
\end{proof}

\begin{proposition}[Spectral parameter is forced at $c \neq 0$]
\label{prop:spectral-parameter-forced-at-nonzero-c}
\ClaimStatusProvedHere
Let $c \neq 0$. Then no chiral coproduct
$\Delta_{z}\colon \Virc \to \Virc \mathbin{\widehat{\otimes}} \Virc$ in
the sense of Definition~\ref{def:chiral-coproduct-spectral} is
$z$-independent. Explicitly, any $z$-independent candidate
$\Delta\colon \Virc \to \Virc \mathbin{\widehat{\otimes}} \Virc$ fails
the coassociativity test at order $z^{1}$ with error class
$(c/2) \Cas \neq 0$ in $\Hch^{2}(\Virc, \Virc)$.
\end{proposition}

\begin{proof}
Suppose $\Delta$ is $z$-independent. Setting $\delta(z) \equiv 0$ in
the notation of Theorem~\ref{thm:conformal-anomaly-rigidity} forces
$\delta'(0) = 0$ and hence $\obs(\Delta) = 0$. But by
Theorem~\ref{thm:conformal-anomaly-rigidity},
$\obs(\Delta) = (c/2) \Cas$, and by
Lemma~\ref{lem:casimir-nonvanishing}, $\Cas \neq 0$ in
$\Hch^{2}(\Virc, \Virc)$. Since $c \neq 0$, the product
$(c/2) \Cas \neq 0$, a contradiction. Therefore no $z$-independent
$\Delta$ can satisfy Definition~\ref{def:chiral-coproduct-spectral}(iii).
\end{proof}

\begin{theorem}[Universality of the coefficient]
\label{thm:universal-coefficient}
\ClaimStatusProvedHere
The coefficient $c/2$ in Theorem~\ref{thm:conformal-anomaly-rigidity}
is universal: for any module $V \in \Virc\text{-mod}$ with action map
$\rho_{V}\colon \Virc \to \End(V)$, the pullback of the universal
obstruction class to $V$-valued cohomology equals
\[
\rho_{V}^{*}\, \obs(\Delta_{z})
\;=\;
\tfrac{c}{2} \cdot \rho_{V}^{*}\Cas
\quad \in \quad
\Hch^{2}(\Virc, V).
\]
The coefficient $c/2$ does not depend on $V$; only the target class
$\rho_{V}^{*}\Cas$ depends on $V$, and can vanish when $V$ is annihilated
by the central element (a \emph{null module}), but the universal
coefficient $c/2$ remains.
\end{theorem}

\begin{proof}
Pullback along $\rho_{V}$ is a linear map
$\Hch^{2}(\Virc, \Virc) \to \Hch^{2}(\Virc, V)$. Applying it to the
identity $\obs(\Delta_{z}) = (c/2)\Cas$ of
Theorem~\ref{thm:conformal-anomaly-rigidity} gives
$\rho_{V}^{*}\obs(\Delta_{z}) = (c/2) \rho_{V}^{*}\Cas$; the scalar
$c/2$ commutes with pullback because pullback is $\C$-linear.

The pullback $\rho_{V}^{*}\Cas$ vanishes precisely when the central
element of $\Virc$ (which acts on $V$ as multiplication by the central
charge seen by $V$) annihilates the cocycle, which occurs on null
modules where $c$ acts by zero even if $c \neq 0$ as a formal
parameter. This is the native/derived distinction of cache
pattern~258: the universal coefficient $c/2$ is intrinsic; the
module choice $V$ is the derived datum that may kill the image.
\end{proof}

\section{Consequences}
\label{sec:consequences-conformal-anomaly}

\begin{corollary}[Chiralisation is obstructed away from $c = 0$]
\label{cor:forbidden-c-0-locus-chiralization}
\ClaimStatusProvedHere
The conformal anomaly $c/2 \cdot \Cas$ is the obstruction to
chiralising the classical Witt Lie bialgebra into a
$z$-independent chiral coproduct on $\Virc$. At $c = 0$ (the
classical locus), chiralisation exists as the Witt Lie bialgebra
(Theorem~\ref{thm:c-zero-coproduct-is-constant}); at $c \neq 0$
(the quantum locus), only a chiral coproduct with nontrivial
spectral parameter $z$ can satisfy
Definition~\ref{def:chiral-coproduct-spectral}(iii)
(Proposition~\ref{prop:spectral-parameter-forced-at-nonzero-c}).
The spectral parameter is the geometric image of the forced
second-order deformation and cannot be removed by any gauge
transformation.
\end{corollary}

\begin{proof}
Immediate from the combination of
Theorem~\ref{thm:c-zero-coproduct-is-constant} (existence at $c = 0$)
and Proposition~\ref{prop:spectral-parameter-forced-at-nonzero-c}
(nonexistence at $c \neq 0$). The inability to gauge away $z$ is the
statement that the class $(c/2) \Cas$ is a genuine cohomology class,
not a coboundary, which is
Lemma~\ref{lem:casimir-nonvanishing}.
\end{proof}

\begin{remark}[Comparison with the Koszul conductor $K = c + c' = 13$]
\label{rem:comparison-with-ktheory-anomaly}
\ClaimStatusProvedElsewhere
The Koszul conductor of Virasoro is $K(\Virc, \Virc^{!}) =
\kappa(\Virc) + \kappa(\Virc^{!}) = c/2 + (26 - c)/2 = 13$ (see the
Virasoro row of the main status table and the $\Virc^{!} =
\mathrm{Vir}_{26 - c}$ Koszul-duality identification). At the
self-duality locus $c = 13$ the obstruction $c/2 \cdot \Cas = (13/2)
\Cas$ and its Koszul dual $(26 - c)/2 \cdot \Cas^{\vee} = (13/2)
\Cas^{\vee}$ coincide numerically, with $\Cas$ and $\Cas^{\vee}$
separate classes in $\Hch^{2}(\Virc, \Virc)$ and $\Hch^{2}(\Virc^{!},
\Virc^{!})$ respectively. Numerical balance at $c = 13$ is the content of Virasoro
Koszul-self-duality at the (unique) locus $c = 13$ where
$\Virc \simeq \Virc^{!}$ as chiral algebras; it does not
identify the two cohomology groups as $\C$-vector spaces, per cache
pattern~262.
\end{remark}

\begin{remark}[Why c/24 is not the relevant coefficient]
\label{rem:bpz-c-over-24-distinction}
The Belavin--Polyakov--Zamolodchikov torus partition function carries
a Casimir shift $c/24$ arising from the $L_{0}$ mode on the
cylinder~\cite{BPZ84}. This is not the coefficient of the obstruction
class: the obstruction is controlled by the $(z - w)^{-4}$ pole of the
$TT$ OPE, whose coefficient is $c/2$. Conflating $c/2$ with $c/24$
is cache pattern~261 (convention clash); the bridge identity between
them is ``Casimir energy on the cylinder = $(c/2) / 12$''.
\end{remark}

\section{Adversarial audit: what the theorem does not claim}
\label{sec:adversarial-audit-anomaly}

Per the Beilinson principle and cache pattern~258, the following
stronger claims are \emph{not} asserted, and would be false at the
stated scope:

\begin{enumerate}[label=\textup{(\arabic*)}]
\item \textbf{``The obstruction is nonzero on every representation at $c \neq 0$.''}
  False in general: a module $V$ on which the central element acts by
  zero (a null module) has $\rho_{V}^{*}\Cas = 0$, hence
  $\rho_{V}^{*}\obs(\Delta_{z}) = 0$ even when $c \neq 0$. What
  Theorem~\ref{thm:universal-coefficient} actually claims is that the
  \emph{coefficient} $c/2$ is universal; the \emph{module} admits
  exceptions. Null modules are a measure-zero locus in
  $\Virc\text{-mod}$ and do not affect the universal statement.
\item \textbf{``The obstruction is $c/2 \cdot [\Cas, \Cas]$ (quadratic in $\Cas$).''}
  False: the obstruction lives in $\Hch^{2}$, which is cohomological
  degree~$2$; a quadratic pairing $[\Cas, \Cas]$ would sit in
  $\Hch^{4}$ and is a separate object (the residue of the CYBE
  relation). This is cache pattern~260.
\item \textbf{``At $c = 0$ higher-order corrections can still force spectral dependence.''}
  False: once the universal obstruction vanishes at leading order, the
  higher-order classes in $\Hch^{2}$ are gauge-equivalent to zero by
  the Hochschild--Kostant--Rosenberg-type descent invoked in
  Theorem~\ref{thm:c-zero-coproduct-is-constant}; this is cache
  pattern~259.
\item \textbf{``$c/2$ equals the BPZ torus Casimir $c/24$.''}
  False: they differ by the geometric factor $12$ arising from the
  cylinder modular weight. Remark~\ref{rem:bpz-c-over-24-distinction}
  is the bridge.
\end{enumerate}

\section{Placement in the programme}
\label{sec:placement-conformal-anomaly}

Theorem~\ref{thm:conformal-anomaly-rigidity} sharpens the Vol~I
status-table entry \emph{Conformal anomaly QUANTIFIED} from a slogan
into a universal statement. The coefficient $c/2 = \kappa(\Virc)$ is
the Koszul complementarity invariant of Theorem~C, and the rigidity
statement identifies the chiral coproduct's obstruction with the bar
complex's scalar Koszul datum. At $c = 0$, Theorem~C gives $\kappa = 0$
and the chiral coproduct degenerates to the Witt Lie bialgebra,
consistent with Theorem~\ref{thm:c-zero-coproduct-is-constant}. At
$c = 13$, Remark~\ref{rem:comparison-with-ktheory-anomaly} balances
$c/2$ against its Koszul dual at the self-duality locus.

The forced spectral parameter of
Corollary~\ref{cor:forbidden-c-0-locus-chiralization} is the
mechanism underlying the universal kernel $R(z)$ of class~$M$ chiral
algebras: at $c \neq 0$, the spectral parameter $z$ is not a choice
but a geometric necessity, imposed by the second-order coassociativity
constraint. This is the precise technical content of the physics
slogan ``conformal anomaly forces a KZ connection''.

\bigskip

\noindent\emph{Dependencies.}
Theorem~\ref{thm:conformal-anomaly-rigidity} depends on
Feigin--Fuchs~\cite{FF84} for $\Hch^{2}(\Virc, \Virc)$ scalar
one-dimensionality, the $TT$ OPE~\eqref{eq:TT-OPE} for the
$c/2$ normalisation, and shifted-parameter coassociativity.
Theorem~\ref{thm:c-zero-coproduct-is-constant} depends on the
Drinfeld~\cite{Dri87} / Etingof--Kazhdan~\cite{EK96} uniqueness of
the Witt Lie bialgebra.
Proposition~\ref{prop:spectral-parameter-forced-at-nonzero-c} is a
direct corollary. Theorem~\ref{thm:universal-coefficient} depends on
functoriality of chiral cohomology under module pullback
(a general feature of the derived category of $\Virc$-modules,
\cite{FBZ04}). Corollary~\ref{cor:forbidden-c-0-locus-chiralization}
and Remark~\ref{rem:comparison-with-ktheory-anomaly} are immediate.

\noindent\emph{Independent verification.}
The independent-verification protocol is implemented in
\texttt{compute/tests/test\_conformal\_anomaly\_rigidity.py}. Four
decorated tests verify Theorem~\ref{thm:conformal-anomaly-rigidity}
against the BPZ OPE residue~\cite{BPZ84},
Theorem~\ref{thm:c-zero-coproduct-is-constant} against the
Kac--Raina fermionic realisation at $c = 0$~\cite{KR87},
Theorem~\ref{thm:universal-coefficient} against the
Faddeev--Takhtajan $\kappa(\Virc) = c/2$ normalisation from the Yangian
construction~\cite{FT87}, and
Proposition~\ref{prop:spectral-parameter-forced-at-nonzero-c} against
the universal Feigin--Fuchs cocycle. All four source pairs are
pairwise disjoint per the decorator's run-time assertion.

%% End of chapter conformal_anomaly_rigidity_platonic.tex

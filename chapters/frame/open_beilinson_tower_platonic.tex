% =====================================================================
%
% OPEN BEILINSON TOWER -- PLATONIC INSCRIPTION
%
% Three volumes, five main theorems, and the auxiliary reconstruction
% and classification surfaces that sit downstream of them.
% Russian-school prose: construct, do not narrate.
%
% Location in the monograph: immediately after the preface, before the
% Introduction and the Overture. The reader who has finished the
% preface meets here the global skeleton in the form governed by the
% concordance.
%
% =====================================================================

\providecommand{\ChirHoch}{\operatorname{ChirHoch}}
\providecommand{\ChirAlg}{\operatorname{ChirAlg}}
\providecommand{\HTQFT}{\operatorname{HT\text{-}QFT}}
\providecommand{\Phihol}{\Phi_{\mathrm{hol}}}
\providecommand{\Face}{\operatorname{Face}}
\providecommand{\GRTone}{\operatorname{GRT}_1}
\providecommand{\kcat}{\kappa_{\mathrm{cat}}}
\providecommand{\kch}{\kappa_{\mathrm{ch}}}
\providecommand{\kBKM}{\kappa_{\mathrm{BKM}}}
\providecommand{\kfib}{\kappa_{\mathrm{fiber}}}
\providecommand{\SCchtopan}{\mathsf{SC}^{\mathrm{ch,top,an}}}
\providecommand{\Bbarch}{\bar{B}^{\mathrm{ch}}}
\providecommand{\Omegach}{\Omega^{\mathrm{ch}}}
\providecommand{\Kosz}{\operatorname{Kosz}}

\chapter*{The Open Beilinson Tower}
\addcontentsline{toc}{chapter}{The Open Beilinson Tower}
\label{ch:open-beilinson-tower-platonic}
\markboth{The Open Beilinson Tower}{The Open Beilinson Tower}

\vspace{1em}

\section*{1.\quad The shadow of a 4d HT gauge theory}
% =====================================

\noindent
A 4d holomorphic-topological gauge theory on $\bC\times\bR_{\ge 0}$
with a Dirichlet boundary condition at the origin of~$\bR_{\ge 0}$
descends to a 2d chiral algebra~$\cA$ on the curve~$\bC$. The algebra
is concrete: its state space is the space of boundary local
operators, its OPE is the collision limit of the 4d-to-boundary
restriction, and its modular structure is the residual action of the
2d conformal group preserved by the boundary. The 4d theory admits
multiple dualities and redefinitions; the algebra~$\cA$ is a
boundary shadow, coarser than the 4d theory but finer than any
isolated numerical invariant. A census of the families that arise
(Heisenberg, affine Kac--Moody at level~$k$,~$\beta\gamma$, Virasoro,
principal~$W_N$, their orbifolds and extensions) is performed in
the standard-landscape part. For any such~$\cA$ the Infinite Fingerprint classification
(Chapter~\ref{chap:infinite-fingerprint-classification}) extracts
the six-slot invariant
\[
 \phi'(\cA) \;=\;
 \bigl(\,p_{\max},\;r_{\max},\;\chi_{\mathrm{VOA}},\;
 n_{\mathrm{strong}},\;\mathrm{coset},\;\kch\,\bigr),
\]
where $p_{\max}$ is the maximal OPE-pole order, $r_{\max}$ is the
shadow-tower depth, $\chi_{\mathrm{VOA}}$ is the supertrace
character, $n_{\mathrm{strong}}$ is the strong-generator count,
$\mathrm{coset}$ is the automorphism coset, and $\kch$ is the
central-extension class pulled back from the Theorem-D universal
Arakelov class. Theorem~G below proves that $\phi'$ is complete on
the standard landscape; the five-class $G/L/C/M/FF$
projection is its double coarsening, first forgetting $\kch$ then
projecting onto $r_{\max}$ (with the $\kch=0$ stratum forming the
Feigin--Frenkel class $FF$).

The fingerprint is computable, useful, and lossy. It collapses the
action of the Grothendieck--Teichm{\"u}ller group~$\GRTone(\bQ)$ on
the face lattice~$\Face(\cA)$ of the bar complex to a~$\bQ$-rational
quotient; the associator-level and spectral-level data invisible to
the numerical census survive in the ordered bar package itself. The
shadow-depth projection is
$\mathsf G/\mathsf L/\mathsf C/\mathsf M/
\mathsf M^{\mathrm{ext}}/\mathsf B$; the Feigin--Frenkel critical
stratum is the companion boundary where Sugawara degenerates. The five
theorem packages of Section~2 record
the invariant content that
survives the averaging map: the bar-cobar arena, inversion,
complementarity, scalar genus class, and chiral Hochschild coefficient
ring. Holography and fingerprint completeness are downstream
reconstruction statements; they begin only after the five-theorem
spine is fixed.

\section*{2.\quad Five theorem packages}
% =====================================

\noindent
Five theorem packages stratify the invariant content of~$\cA$.
Each is stated on the exact proved surface used by the concordance;
each ambient qualification belongs to the theorem's type signature
and hypothesis package.

\medskip

\noindent\textbf{Theorem~A$^{\infty,2}$ (Bar-cobar properad
adjunction).}\enspace
In the pro-nilpotent monoidal category
$\bigl(D(\Ran X),\otimes^{\mathrm{ch}}\bigr)$, enhanced associative
bar and cobar are inverse equivalences.  Re-unitalization gives the
universal reconstruction maps
\[
 \Omega_XB_X(\cA)\xrightarrow{\sim}\cA,
 \qquad
 \cC\xrightarrow{\sim}B_X\Omega_X(\cC).
\]
This Ran statement is proved by Francis--Gaitsgory.  Its restriction
to the chosen factorization subcategories carries status
$\ClaimStatusConjectured$ under $H_{\mathrm{fact}}$; its completed
chain realization carries status $\ClaimStatusConditional$ under
$H_{\mathrm{conv}}$.  Under $H_{\mathrm{VD}}$, Verdier duality
constructs the algebra
$K_X(\cA)=\mathbb D_{\Ran}B_X(\cA)$ and its biduality comparison.

\medskip

\noindent\textbf{Theorem~B (Quadratic Koszul recognition).}\enspace
Choose a connected positive-weight quadratic presentation
\[
 \cA=T_X(V)/(R),
 \qquad
 \cA^{\mathrm i}=C_X(s^{-1}V,s^{-2}R),
\]
and let
$q_\cA\colon\cA^{\mathrm i}\to B_X(\cA)$ be the canonical coalgebra
map.  Under
$H_{\mathrm{CL}}(\cA,\cA^{\mathrm i},\tau_{\mathrm i})$, the
following conditions are equivalent: $q_\cA$ is a quasi-isomorphism;
$\Omega_X(\cA^{\mathrm i})\to\cA$ is a quasi-isomorphism; the two
quadratic twisted tensor products are acyclic; and the PBW/bar
spectral sequence has diagonal support and strong convergence.  The
comparison cone $\operatorname{Cone}(q_\cA)$ is therefore the
computational carrier of the family-level Koszul problem.

For a fixed curved coalgebra $C$, Positselski's co/contra theorem
compares the coderived category of $C$-comodules with the
contraderived category of $C$-contramodules.  This fixed-coalgebra
module theorem and Theorem~B occupy separate typed lanes.

The same ordered bar package carries the universal conductor of
Chapter~\ref{chap:universal-conductor}: the Reynolds/coinvariant
averaging from ordered chiral homology to the symmetric modular
shadow. Its kernel is the ordered residue sector detected by the
$G/L/C/M$ archetypes, and it is separate from both the derived centre
and the categorical Drinfeld centre.

\medskip

\noindent\textbf{Theorem~C (centre-local-system complementarity).}\enspace
C0 identifies the degree-zero cohomology sheaf of the strict flat fibre
with the ordinary centre local system~$\mathcal Z(\cA)$.  C1 decomposes
\[
 \mathbf C_g(\cA)
 =R\Gamma(\overline{\mathcal M}_g,\mathcal Z(\cA))
\]
by the represented Verdier involution and becomes Lagrangian under its
perfectness and nondegenerate anti-invariant pairing package.  The
Theorem~H object
$Z^{\mathrm{der}}_{\mathrm{ch}}(\cA)=\ChirHoch^\bullet(\cA,\cA)$
maps to this surface through a supplied brace comparison
$\iota_Z^{\mathrm{der}}$, followed by~$H^0$ and C0.  The
shifted-symplectic BV theorem is the C2 surface on the BV-perfect,
Verdier-nondegenerate bar-chart locus, including the verified
Heisenberg and affine-Kac--Moody perfectness surface. On the classical
$\mathsf G/\mathsf L/\mathsf C/\mathsf M$ census families the
restricted scalar Verdier sums are
$0$ for Heisenberg, affine Kac--Moody, and $\beta\gamma/bc$,
$13$ for Virasoro, and $250/3$ for~$\mathcal{W}_3$.  For
Bershadsky--Polyakov, the exact central-charge companion sum is~$50$;
equivalently,
$c_{\mathrm{BP}}(k)+c_{\mathrm{BP}}(-k-6)=50$;
its modular characteristic is \ClaimStatusOpen{} pending the genus-one
minimal-DS curvature calculation.  The normalization $\kappa=c/6$
would adjoin the conditional scalar value~$25/3$.  The proposed
Vol~III $\mathsf B$-row adjoins the
\ClaimStatusConjectured{} arithmetic candidate
$2c_+(\widetilde H(K3,\mathbb Z))=8$.  The proved lattice input is
the signature $(4,20)$; the chiral-conductor interpretation carries
$(H_{\mathrm{chart}},H_{\mathrm{KD}},H_{\mathrm{scalar}},
H_{\mathrm{mod}},H_{\mathrm{quant}})$.

\medskip

\noindent\textbf{Theorem~D (Deformation class, Hodge image, and graph
trace).}\enspace
For $g\geq2$, the native class
$\operatorname{Obs}^{\mathrm{def}}_g(\cA)$ lies in
$H^2(\Def_g(\cA))$.  Under~$H_D^1$, the pointed genus-one class has
trace
$\operatorname{tr}_1\operatorname{Obs}^{\mathrm{def}}_{1,1}
=\kappa(\cA)\lambda_1$.  Under~$H_D^K$, the scalar virtual object and
its top Hodge image are
\[
 \mathfrak O_g^K(\cA)=\kappa(\cA)\lambda_{-1}(\mathbb E_g),
 \qquad
 \operatorname{ch}_g\!\left(\mathfrak O_g^K(\cA)\right)
 =(-1)^g\kappa(\cA)\lambda_g.
\]
The derived passage between the first two lines is the comparison
$H_D^{\mathrm{tr}}$.  Under~$H_D^{\mathrm{graph}}$, the numerical
free-energy package on~$\overline{\cM}_{g,n}$ is
\[
 F_g(\cA)
 \;=\;
 \kappa(\cA)\cdot\lambda_g^{\mathrm{FP}}
 \;+\;
 \delta F_g^{\mathrm{cross}}(\cA)
 \qquad\textup{(all-weight graph trace)},
\]
with $\delta F_1^{\mathrm{cross}}=0$ and
$\delta F_g^{\mathrm{cross}}=0$ under the scalar-diagonal graph
package.  The splitting principle computes the displayed sign after
$H_D^K$ identifies the virtual bar object with
$\kappa(\cA)\lambda_{-1}(\mathbb E_g)$.  For~$W_3$ at
genus~$2$ it reads
$\delta F_2^{\mathrm{cross}}(W_3) = (c+204)/(16c)$
(Theorem~\ref{thm:delta-f-cross-w3-g2}).
Conjecture~\ref{conj:F6-lambda-g-clutching-uniqueness} asks whether
the clutching and socle data determine the class in
$R^g(\overline{\cM}_g)$ for $g\ge3$.

\medskip

\noindent\textbf{Theorem~H (family-indexed chiral Hochschild
support).}\enspace
The curve-level object is
$C^\bullet_{\mathrm{ch}}(\cA,\cA)
=R\!\operatorname{Hom}_{\cA^e}(\cA,\cA)$.
For a finite set $S\subset\mathbb Z$, the package $H_H(\cA;S)$
consists of a complete chart model $Q_\cA$, a comparison
$\gamma_\cA\colon Q_\cA\xrightarrow{\sim}
C^\bullet_{\mathrm{ch}}(\cA,\cA)$, and a strong deformation retract
of $Q_\cA$ onto a complex $K_{\cA,S}$ supported in~$S$.  Theorem~H
then gives
\[
 \ChirHoch^n(\cA)\cong H^n(K_{\cA,S}),
 \qquad
 \operatorname{Supp}\ChirHoch^\bullet(\cA)\subseteq S,
\]
and, when the graded pieces are finite,
\[
 P_\cA(t,q)=\sum_{n\in S}\dim_qH^n(K_{\cA,S})t^n.
\]
Bakalov--De Sole--Kac compute bounded support $\{0,2,3\}$ for
$\operatorname{Vir}_c$ and dimensions $(2,1,0,\ldots)$ for the
rank-one even superboson; each becomes a chart statement through its
bounded-to-chart quasi-isomorphism.  A degree-$d$ perfect pairing
between two chart complexes is an additional package and gives the
separate reflection $n\leftrightarrow d-n$.

\medskip

Two auxiliary theorem surfaces sit downstream from the five invariant
packages. They organize reconstruction and classification from the
bar--cobar spine.

\medskip

\noindent\textbf{Theorem~F (Universal Holography).}\enspace
Let~$\ChirAlg^{\omega,\mathrm{BL},\mathrm{nonlog}}_X$ denote the
category of conilpotent boundary-Lagrangian chiral algebras on the
curve~$X$ in the \emph{non-logarithmic} standard landscape
(Virasoro, $W_N$ for any~$N$, Schellekens $c=24$ holomorphic,
Monster~$V^{\natural}$, irrational cosets), and
let~$\HTQFT_{X\times\bR}$ denote the category of
holomorphic-topological quantum field theories
on~$X\times\bR$ with an anchored boundary condition. The assignment
\[
 \Phihol\colon\;
 \ChirAlg^{\omega,\mathrm{BL},\mathrm{nonlog}}_X
 \;\longrightarrow\;
 \HTQFT_{X\times\bR},
 \qquad
 \cA \;\longmapsto\;
 \bigl(\,\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA)
 \;\rightsquigarrow\;
 (\cA,\cA^{!}_\infty)\,\bigr)
\]
is the typed open/closed comparison functor under the
Volume~II non-logarithmic hypotheses.  Its algebraic input is the
triple formed by bar--cobar recovery of the boundary algebra~\(\cA\),
the Verdier construction of the homotopy Koszul partner
\(\cA^!_\infty\), and the chiral derived centre in the cochain-level
closed-sector slot.  The Volume~II comparison functor carries this
triple to the physical HT bulk--boundary pair.
The functor is constructed in Volume~II,
\texttt{chapters/connections/universal\_holography\_functor.tex},
Theorem~\texttt{thm:universal-holography-functor}, with the
\emph{non-logarithmic} scope qualifier.  The logarithmic
\(\cW(p)\) triplet defines the next extension problem; its input is a
direct Adamovi\'c--Milas character-amplitude bound compatible with the
Gurarie--Flohr Massey amplitudes. The Vol.~II comparison
theorem applies on the non-logarithmic $C_2$-cofinite standard
landscape plus irrational cosets in that ambient.
The scalar refinement begins with the exact Virasoro OPE and
level-four Gram form.  A growth invariant of the higher residue
sequence is defined after
$\mathsf H_{\mathrm{res}}+\mathsf H_{\mathrm{asymp}}$ supplies the
scalar coordinates, uniform bounds, and an order-of-limits theorem.
The labels~\ref{thm:shadow-exponential-base-Virasoro},
label~\ref{thm:universal-class-M-C-is-6}, and
label~\ref{thm:shadow-series-closed-form-Virasoro} in
Chapter~\ref{ch:shadow-tower-higher-coefficients} record these open
construction problems.  The Volume~II adjoint comparison uses the
typed analytic package as part of its domain.

\medskip

\noindent\textbf{Paramodular refinement of Theorem~F.}\enspace
At the K3 base, the proposed refinement has target
\(\mathbf H_{\Delta_5}^{\mathrm{tgt}}\) and status
\ClaimStatusConjectured.  Recognition of a compact
Hall--Borcherds object requires a finite Hall source, a nondegenerate
Hopf pairing, comparison maps, a PBW theorem with a presentation of
the relation ideal, and a Mittag--Leffler inverse-limit argument.
These data form the package $H_{\Delta_5}^{\mathrm{Hall}}$.

The proved scalar inputs are
$\Delta_5\in S_5(K(1))$ and
$\chi_{\mathrm{top}}(K3)=24$.  A genus-one determinant comparison
would be a theorem identifying the sewing trace with a Quillen norm:
\[
\log Z^{(1),\mathrm{sew}}_{\mathbf H_{\Delta_5}}
\xrightarrow{\ H_{\Delta_5}^{\mathrm{det}}\ }
-\log\bigl\|\Delta_5\bigr\|^2_{\mathrm{Quillen}}.
\]
The package $H_{\Delta_5}^{\mathrm{det}}$ consists of the determinant
line, its Quillen metric, a trace-class sewing operator, and a theorem
matching its regularized determinant with the chosen automorphic
normalization.  The proposed Humbert/Argyres--Douglas comparison is a
separate boundary morphism with its own degeneration and monodromy
calculation.  These are frontier obligations of
Chapter~\ref{V3-ch:k3-chiral-bialgebra-platonic}.

\medskip

\noindent\textbf{Theorem~G (Infinite Fingerprint Completeness).}\enspace
On the standard landscape, the six-slot fingerprint~$\phi'(\cA)$ is
a complete invariant of the bar coalgebra together with its Koszul
dual: $\phi'(\cA_1) = \phi'(\cA_2)$ if and only if
$\barB^{\mathrm{ch}}(\cA_1)\cong\barB^{\mathrm{ch}}(\cA_2)$ as
$\operatorname{Ass}^{\mathrm{ch}}$-coalgebras and
$\cA_1^{!}\cong\cA_2^{!}$. The quadrichotomy~$G/L/C/M$ is recovered
as the first coarse projection~$\Pi_{\mathrm{coarse}}\circ\phi'$,
forgetting $\kch$ and projecting onto $r_{\max}$, on the
locus~$\kch\ne 0$. The critical-level companion stratum~$FF$
($\kch = 0$) completes the classification to the five-class
$G/L/C/M/FF$. The theorem is proved in
Chapter~\ref{chap:infinite-fingerprint-classification}.

\section*{3.\quad The volume}
% =====================================

\noindent
The volume has an overture, six numbered parts, and appendices. The
order follows mathematical dependence: first the object, then its
scalar and nonlinear shadows, then examples, physics bridges,
collision residues, and the frontier boundary.

\medskip

\noindent\textbf{Overture.}\enspace
The Heisenberg algebra exposes the ordered bar in the abelian base
case: one generator, one double pole, one scalar~$k$.

\medskip

\noindent\textbf{Foundations and the Open Beilinson Tower.}\enspace
Levels $0$--$1$ begin with the stable presentable factorisation
category $\mathsf C$ on the tangential log curve $(X,D,\tau)$ and its
compact opposite $\cC^\op=(\mathsf C^\omega)^\op$.  A factorisation
compact generator $b$ gives
$A_b=\RHom_{\mathsf C}(b,b)$ and the Morita equivalences
$\mathsf C\simeq\mathrm{RMod}^{\fact}_{A_b}$ and
$\cC^\op\simeq\mathrm{Perf}(A_b)^\op$.  Configuration-space geometry
then supplies the carrier of the bar differential.  The Heisenberg
algebra is the first computation.

\medskip

\noindent\textbf{The Bar--Cobar Engine.}\enspace
Constructs $\Bbarch_X(A_b) = \mathrm{T}^c(s^{-1}\bar A_b)$ at
level~$2$.  Theorem~A gives universal reconstruction in the enhanced
Ran ambient and records the hypotheses for factorization, completed
chain, and Verdier realizations.  Theorem~B compares the quadratic
coalgebra $A_b^{\mathrm i}$ with the full bar coalgebra through
$q_{A_b}$.  A family theorem computes the comparison cone and proves
convergence of its filtration.  The shadow tower
$\{S_2,S_3,S_4,\ldots\}$ records additional deformation data of
$A_b$.

\medskip

\noindent\textbf{The Bulk.}\enspace
The derived chiral centre
$Z^{\mathrm{der}}_{\mathrm{ch}}(A_b)$ is the level-$3$ Hochschild
object.  Theorem~H gives its $H_H$-conditional cohomological support.
Theorem~B supplies a quadratic resolution on the locus where
$q_{A_b}$ is a quasi-isomorphism.  The open--closed comparison to a
physical bulk is a further theorem with its own OCA hypothesis
package.

\medskip

\noindent\textbf{The Five-Archetype Landscape.}\enspace
Stratifies the standard landscape (Heisenberg, affine Kac--Moody at
level~$k$, $\beta\gamma$, $bc$, Virasoro, principal $\cW_N$,
non-principal $\cW$ of hook type, logarithmic $W(p)$, symplectic
bosons and fermions, super-Virasoro, Bershadsky--Polyakov, lattice
VOAs, Mukai-K3 Heisenberg) into the five archetypes
$\mathsf G / \mathsf L / \mathsf C / \mathsf M / \mathsf B$ via the
$5{\times}5$ $\kappa$-stratification matrix; K3$\times E$ anchors
row $\mathsf B$ at $(0, 0, 3, 5, 24)$.

\medskip

\noindent\textbf{The Modular Tower.}\enspace
Levels~$4$--$5$: line/brane operators, modular trace, and clutching
on the open category.  Theorem~D keeps the degree-two deformation
class, the conditional $K$-theory comparison
$\mathfrak O_g^K=\kappa\lambda_{-1}(\mathbb E_g)$, its signed Hodge
image, and the stable-graph number in their respective ambients.  The
exact Virasoro local spine is the norm
$N_\Lambda(c)=c(5c+22)/10$.  Higher branch geometry is an analytic
comparison problem for the constructed residue series.  The
physics-bridge witnesses (Feynman diagrams, BV-BRST, arithmetic
shadows) define comparison problems for these typed outputs. The
$E_n$ operadic circle $E_3 \to E_2 \to E_1 \to E_2 \to E_3$, the
factorization-envelope construction $\cU^{\mathrm{mod}}_X$, the
derived-Langlands mirror, and the Dimofte slab as a bimodule
inscribe at the modular tower's higher-categorical level.

\medskip

\noindent\textbf{Seven Faces of $r_\cA(z)$.}\enspace
Treats the family-normalized collision residue across its seven faces:
for example $r^{\mathrm{Heis}}_k(z)$, $r^{KM,\fg}_k(z)$, and
$r^{\mathrm{Vir}}_c(z)$. The seven faces are F1
bar-cobar twisting, F2 DNP25 line-operator, F3 Khan--Zeng
PVA, F4 Gaiotto--Zeng sphere Hamiltonians, F5 Drinfeld Yangian, F6
Sklyanin, F7 FFR94 Gaudin. The~$\GRTone(\bQ)$-torsor acts on the
face lattice and the seven faces are its pinned points.

\medskip

\noindent\textbf{Frontier.}\enspace
The boundary of the proved surface: critical level, logarithmic
admissible families, super-Yangian Berezinian shifts, and
cross-volume CY-to-chiral shadows.

\medskip

\noindent\textbf{Appendices.}\enspace
Census tables, first-principles diagnostics, operadic conventions, and
computational tables.

\medskip

The Guide to Main Results and the concordance record each frontier
question as a proved surface, an ambient-qualified completion, a
reduction, or a residual conjecture.

\section*{4.\quad Three volumes}
% =====================================

\noindent
The construction occupies three volumes.

\medskip

\noindent\textbf{Volume~I.\ Modular Koszul Duality.}\enspace
The algebraic spine: ordered bar complex, five-theorem core,
standard landscape, physics bridges, and seven faces of the
family-normalized collision residue~$r_\cA(z)$.

\medskip

\noindent\textbf{Volume~II.\ A-$\infty$ Chiral Algebras and 3d HT
QFT.}\enspace
The physical realization. Opens with the open-primitive atom,
develops the~$E_1$ core, installs the
$\SCchtopan$ derived-centre pair computed by the bar-resolution shadow,
and treats the 3d quantum-gravity value on its Brown--Henneaux/HT-QFT
scope.

\medskip

\noindent\textbf{Volume~III.\ CY Categories, Quantum Groups, and BPS
Algebras.}\enspace
The geometric realization. Constructs the CY-to-chiral two-stage
factorisation
$\Phi_d^{(\Sigma_{d-1},C)} = \mathrm{Sp}^{\mathrm{ch}}_{\Sigma_{d-1}, C} \circ
\Phi^{\mathrm{FA}}_d$ -- stage~$1$ a canonical $E_d$-holomorphic
factorisation algebra on the CY$_d$ variety (pinned by
Kontsevich--Tamarkin $E_d$-formality and Costello--Gwilliam--Li
locality), stage~$2$ factorisation homology over a closed
$(d-1)$-cycle $\Sigma_{d-1}$ followed by restriction to a reference
curve $C$ -- scope-dependent on the complex dimension~$d$ (an
$E_2$-chiral output on $C$ for $d\le 2$; an $E_1$-chiral output on
$C$ for $d\ge 3$, with a family of $E_1$-chiral shadows indexed by
$(\Sigma_{d-1}, C)$), the K3~Yangian,
the Borcherds weight characteristic~$\kBKM = c_N(0)/2$, and the
class-$M$ $E_3$ bar of
dimension~$6^g$ at $g\in\{1,2,3\}$, with the $g\ge 4$ extension
belonging to the $d_5$-degeneration surface of
Volume~III~\ref{V3-cor:class-m-higher-genus}.
CY-D proves the $d=2$,
$\HH_{-1}=0$ slice and then separates the operational
$\kappa_{\mathrm{ch}}$ from the Hodge comparator
$\chi(\cO_X)$ beyond that slice; K3 categorical DT proves the
Hilbert-scheme Fock bridge. The proved statement used here is that
Fock bridge.  A bar-side categorical lift to
$D^b(\Hilb^n(K3))$ is a separate frontier comparison.

\section*{5.\quad The operadic fixed-point endpoint}
% =====================================

\noindent
The operadic circle
$\cdots\to E_{n+1}\to E_n\to\cdots\to E_1\to E_2\to\cdots\to E_{n+1}\to\cdots$
closes at~$E_\infty$-topological as the formal completion of both
arms. The inward arm collapses the topological directions one at a
time; the outward arm unfolds the holomorphic directions one at a
time through iterated derived-centre constructions. At each rung~$n$
the conformal vector on the~$n$-th Sugawara level is the data that
makes the~$\bC$-translations~$Q$-exact and promotes~$\SCchtopan$
to~$E_{n+1}^{\mathrm{top}}$. Iterating the Sugawara construction
produces the~$W_\infty$ tower; only after homotopy-coherent
completion of all finite-spin truncations does this tower define the
$E_\infty$ endpoint.
The first visible shadow of the endpoint is 3d quantum gravity at
the~$W_\infty$ level of Volume~II, Part~VI\@. The construction first
produces the~$W_\infty$ tower; its homotopy-coherent formal completion
is the proposed $E_\infty$ endpoint under the stated coherence
hypotheses.

The endpoint is canonical in a precise sense: it is the unique
operadic fixed point of the iterated Sugawara construction on the
tower of chiral algebras with conformal vector at non-critical
level. On the standard landscape it exists. Off the standard
landscape (the degenerate lane, the critical level, the logarithmic
regime of Creutzig--Kanade--Linshaw) the fixed point is the active
frontier.

\section*{6.\quad The non-degenerate surface}
% =====================================

\noindent
On the regular locus selected by the displayed central, level, and
quadratic conditions, the five main theorem packages retain the
statuses recorded in the concordance.  Theorem~A has a proved enhanced
Ran core, a conjectural factorization restriction, and conditional
chain and Verdier realizations.  Theorem~B is conditional on
$H_{\mathrm{CL}}$.  Theorems~D and~H are conditional on their named
packages.  Theorem~C separates its C0, C1, and C2 surfaces, with C2
living on the BV-perfect Verdier-nondegenerate bar-chart locus.
The auxiliary holography and fingerprint surfaces use this spine as
their input. Along the boundary of the non-degenerate locus the critical affine
stratum, logarithmic admissible lane, super-Yangian Berezinian shift,
and CY-to-chiral higher-dimensional shadows are separate theorem
surfaces with their own hypotheses.

\section*{7.\quad The residual boundary}
% =====================================

\noindent
The active frontier is the residual boundary of the non-degenerate
locus. Four pieces.

\medskip

\noindent\emph{(a) The degenerate admissible logarithmic
lane.}\enspace
The Creutzig--Kanade--Linshaw logarithmic VOAs~$W(p)$ and their
relatives occupy a specific stratum of the standard landscape on
which the Koszul locus is obstructed by a Massey triple product
$\langle\Omega,\Omega,\Omega\rangle$ at weight~$3$. Gurarie 1993 and
Flohr 1996 exhibit unbounded Massey amplitudes at every weight
for~$\cW(p)$, which refutes the Zhu-bounded-Massey shortcut used
for temperedness; direct Adamovi\'c--Milas character-amplitude bounds
remain the residual route. The stratum is explicit and its scope is
delimited.

\medskip

\noindent\emph{(b) The Volume~III CY-C six-routes
convergence.}\enspace
The six routes to the K3 chiral algebra are read on their
route-specific proofs. The Borcherds-lift and lattice-VOA
route is ProvedElsewhere, and the remaining bridges serve as
frontier evidence in this overview. The pentagon stratifies the
generator-lattice rank~$\rho^{R_i}\in\{3,12,24\}$, NOT the Hodge
supertrace~$\kch$ (which is route-independent and vanishes on
$K3\times E$); the halving arrows~$\beta_{34}$,~$\beta_{45}$ act on
$\rho$ alone. Each bridge retains its recorded epistemic status.

\medskip

\noindent\emph{(c) The paramodular arithmetic frontier.}\enspace
The conditional K3 recognition target $\mathbf H_{\Delta_5}^{\mathrm{tgt}}$
has a one-dimensional Bloch--Kato Selmer deformation as its arithmetic
target surface,
$\dim H^1_f(\mathbb Q,\mathrm{std}\,\rho_{\Delta_5}) = 1$, the
paramodular cyclotomic Hida-family tangent, after the Hall/CoHA
source, compact pairing, PBW/no-extra-relations theorem, comparison
maps, and inverse-limit package are supplied. The
metaplectic Grothendieck--Teichm\"uller extension
$\mathrm{grt}_1^{(1/2)}$ of $\mathrm{grt}_1$ by even-weight
generators, with fundamental bracket
$[\widetilde\sigma_2,\widetilde\sigma_2] = 288\,\widetilde\sigma_4$
and $288 = \tau(2)^2/2$, acts on the Saito--Kurokawa motivic
shadow. The chiral-Hochschild $W_\infty[c=-214]$ cocycles
$e_3,e_4,e_5,e_6$ are explicit: $e_5 = W_5$ identically (Wang 1998,
Proposition~4.2), and $e_4 = W_4 - (107/11)\Lambda_Z$. The
Humbert--Heegner admissibility filtration
$\phi^{(n)}=0$ unless $n\equiv 3,5\pmod 8$ separates the K3
polar-support lane from Zagier--Hoffman regularisation. The $H_3$
wall carries $\phi^{(3)}|_{H_3}$, the one-term $n=5$ coherence is
$\phi^{(5)} = 2\,[\mathrm{gen}]^{\otimes 5}$ in the normalization of
Theorem~\ref{thm:bc-polar-depth-reduction}, and the polar
coefficient vanishes for admissible $n\ge 11$. These
arithmetic data are the K3-base deformation theory of the holography
surface, proved in Chapter~\ref{V3-ch:k3-chiral-bialgebra-platonic}.

\medskip

\noindent\emph{(d) The super-Yangian chain-level chiral
coproduct.}\enspace
The super-Yangian complementarity question separates into the
proved super-trace identity used here and a separate Berezinian-shift
surface. The
super-trace normalisation gives
$\kappa^{\str} + \kappa^{\str,!} = 0$ on the sub-Sugawara line
for $\fsl(m|n)$ with $m\neq n$
(Proposition~\ref{prop:super-berezinian-central-automorphism}(a),
proved via super-Sugawara rational-identity cancellation). The
Berezinian normalisation is formulated separately as
$\kappa^{\sBer} + \kappa^{\sBer,!} = \max(m,n)$
(Conjecture~\ref{conj:super-berezinian-shadow-shift-magnitude}),
with the required closed-form derivation of the shift magnitude
$\tfrac{1}{2}\max(m,n)$ induced by the central automorphism
$\sigma_{\sBer}$ of
Lemma~\ref{lem:super-trace-berezinian-bridge}; Nazarov~1991 and
Gow~2006 supply centrality of $\sBer(T(u))$ and non-degeneracy of
the Berezinian pairing.  The shift magnitude remains the open
closed-form calculation of
Remark~\ref{rem:super-berezinian-shift-open}. The Verdier pairing specialises
to super-trace on supertraceless type-$A$; the Sugawara pairing
specialises to Berezinian. Neither normalisation is more
canonical than the other; the complementarity datum is
invariantly defined on $Z(A) \otimes Z(A^{!})$ and both recover
it.

\medskip

\noindent
The skeleton is three volumes, five main theorem packages, and
auxiliary reconstruction/classification surfaces. The four frontier
pieces above and the concordance describe the present boundary of the
non-degenerate surface.

\medskip

\noindent\textbf{Structural sharpenings.}\enspace
The global statements refine into the forms in which the
theorems are proved.

\emph{Four conductors, trinity restricted.} The single conductor
$K$ of equation~(G1) separates into $K$ (BRST ghost conductor),
$K^\kappa := \kappa+\kappa^!$ (computed Vol~I complementarity values
$(0,13,250/3)$, the open BP candidate~$25/3$, and the conjectural
Mukai-to-chiral candidate~$8$),
$K_c$ (central-charge trace), and $K_g$ (ghost-number anomaly). The
trinity equality $K=K_c=K_g$ is formulated on the
Koszul-self-dual subcategory carrying a compatible BRST resolution,
Borcherds--CY target, and the corresponding comparison theorem.

\emph{Scaled deformation retract.}  For a family~$F$, put
$C_F=\Omega_X^{\mathrm{ch}}B_X^{\mathrm{ch}}(A_F)$ and let
$p_F\colon C_F\to A_F$ be the Theorem~A counit.  A chain map
$\iota_F\colon A_F\to C_F$, an operator
$h_F\colon C_F\to C_F[-1]$, and a scalar~$\nu_F\in k^\times$ form a
scaled deformation retract when
\[
 p_F\iota_F=\mathrm{id}_{A_F},
 \qquad
 d_Fh_F+h_Fd_F
 =\nu_F(\mathrm{id}_{C_F}-\iota_Fp_F).
\]
Then~$\nu_F^{-1}h_F$ is a contracting homotopy.  The five expressions
\[
 1,\qquad 2(k+h^\vee),\qquad 1,\qquad
 \frac{c(5c+22)}{10},\qquad 8
\]
are family construction targets for~$\nu_F$ in the
$\mathsf G/\mathsf L/\mathsf C/\mathsf M/\mathsf B$ lanes.  An identity
$\nu_F=K^\kappa(A_F)$ requires the additional chain map from this
deformation retract to the scalar trace complex prescribed by
Convention~\ref{conv:A-two-kappa-shriek}.  Theorem~C acts on
$R\Gamma(\overline{\mathcal M}_g,\mathcal Z(A_F))$ after C0, and
Theorem~D supplies its normalized scalar trace; the comparison chain
map is the bridge from the Theorem~A retract to that scalar lane.

\emph{Two-stage Calabi--Yau factorisation.} The cross-volume functor
factors as
$\Phi_d^{(\Sigma_{d-1},C)}
= \mathrm{Sp}^{\mathrm{ch}}_{\Sigma_{d-1},C}\circ\Phi^{\mathrm{FA}}_d$,
with Stage~1 on the formal locus only (generic at $d\le 2$;
$K3\times E$ at $d=3$ via $\mathrm{At}(T_E)=0$, $\Omega^3_{K3}=0$;
compact CY$_3$ with $\mathrm{At}(TX)\neq 0$ obstructed; $d\ge 4$
open). A single CY-$d$ category admits a family of $E_1$-chiral
shadows indexed by $(\Sigma_{d-1},C)$; on $K3\times E$ the six
construction routes form a common limit cone in
$\mathrm{ChirAlg}^{E_1}_C$.

\emph{Eight-form Gritsenko--Cl\'ery spread.} Weights
$w(N)\in\{5,2,1,1,1/2,1,1/4,0\}$, Fourier zero-coefficients
$c_N(0)\in\{10,4,2,2,1,2,1/2,0\}$, universal Borcherds-weight
identity $\kappa_{\mathrm{BKM}}(\Phi_N)=c_N(0)/2$; cover assignment
$\mathrm{Sp}_4(\bZ)/\mathrm{Mp}_4(\bZ)/\widetilde{\mathrm{Mp}}_4$ by
weight parity (Weil representation plus Stone--von Neumann at
signature $(2,3)$). The additive decomposition of the BKM weight
into a chiral scalar plus a fibre Euler term fails at every $N$,
including $N=1$ ($\kappa_{\mathrm{ch}}=0$ by Serre duality at
$d=3$; $\kappa_{\mathrm{BKM}}=5$).

\emph{Three-tier topologisation.} Tier~I (cohomological) and
Tier~II (chain level on a quasi-isomorphic model, via Fresse
Theorem~12.3.A homotopy transfer) are unconditional. Tier~III
(strict chain level on the original complex) is closed for
$\mathsf G$, $\mathsf L$, $\mathsf C$ and open for $\mathsf M$ (F3).
Sugawara explicit at $V_1(\mathfrak{sl}_2)$:
$T_{\mathrm{Sug}}=(1/6)(\mathord{:}J^0J^0\mathord{:}
+\mathord{:}J^+J^-\mathord{:}+\mathord{:}J^-J^+\mathord{:})$,
$G_{\mathrm{Sug}}=(1/3)\sum_a\mathord{:}\bar c_a J^a\mathord{:}$,
$[Q_{\mathrm{tot}},G_{\mathrm{Sug}}]=T_{\mathrm{Sug}}$ on BRST
cohomology.

\emph{Formal-series comparison.}  A weighted square-root series
defined from chosen local seeds has exact formal coefficients.  The
package $\mathsf H_{\mathrm{Ricc}\to\mathrm{res}}$ compares this
auxiliary series with the ordered-residue scalar model.  A level-six
radical map supplies the quotient-theory comparison, while
$\mathsf H_{\mathrm{res}}(\Vir_c;X)$ supplies the geometric scalar
coordinates.

\emph{Ten named frontiers.} Logarithmic $\cW(p)$ tempering (F1);
periodic CDG for non-simply-laced admissible levels (F2);
original-complex Tier~III for class $\mathsf M$ (F3);
super-complementarity canonical pairing (F4); irregular-singular KZB
at $\overline{\cM}_{g,n}$ boundaries with Drinfeld-double
descent class under $H_{\mathrm{F5}}^{\mathrm{desc}}(\cA)$,
\[
 \operatorname{Obs}^{(1)}_{\mathrm{double}}(\cA)
 =H^2(\Theta_{\mathrm{F5},\cA})
  [\omega_{\mathrm{KZB}}(\rho_\cA)]
 \in H^2(\Def_{\mathrm{double}}^\bullet(\cA)),
\]
whose detection of $r_{\max}(\cA)=2$ is
Conjecture~\ref{conj:level-4-F5-shadow-depth} (F5); Theorem~B at the nodal boundary
beyond the Koszul locus (F6); MC4 completion / H3 failure regime
(F7); Stage-1 chain level at $d\ge 4$ (F8); category-level
$E_d$-formality at $d\ge 4$ (F9); bracket-level BPS Lie algebra on
$K3\times E$ (F10).

% =====================================================================
% End of Open Beilinson Tower.
% =====================================================================

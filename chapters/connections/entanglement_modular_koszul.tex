% Chapter: Entanglement Entropy from Modular Koszul Duality
%
% This chapter derives entanglement entropy (including quantum corrections)
% from the proved modular Koszul core.  The main results are:
%   G11: Entanglement complementarity (Theorem)
%   Calabrese-Cardy from the A-hat generating function (Corollary)
%   Shadow corrections and entanglement complexity (Proposition)
%   QES from shadow connection (Proposition)
%
% All results are projections of the universal MC element Theta_A.
% The only external input is the twist operator dimension (standard CFT).

\chapter{Entanglement entropy from modular Koszul duality}
\label{chap:entanglement-modular-koszul}
\index{entanglement entropy!from modular Koszul duality|textbf}
\index{Ryu--Takayanagi formula!algebraic derivation|textbf}
\index{quantum extremal surface!shadow connection|textbf}

% ----------------------------------------------------------------------
% Local macros
% ----------------------------------------------------------------------
\providecommand{\MC}{\mathrm{MC}}
\providecommand{\Sh}{\mathrm{Sh}}
\providecommand{\Tr}{\operatorname{Tr}}
\providecommand{\rank}{\operatorname{rank}}
\providecommand{\id}{\mathrm{id}}
\providecommand{\Res}{\operatorname{Res}}
\providecommand{\BTZ}{\mathrm{BTZ}}
\providecommand{\EE}{\mathrm{EE}}
\providecommand{\QES}{\mathrm{QES}}

The complementarity theorem
(Theorem~\ref{thm:quantum-complementarity-main})
decomposes the genus-$g$ ambient complex into Lagrangian halves
\[
\mathbf{C}_g(\cA)
\;\simeq\;
\mathbf{Q}_g(\cA) \;\oplus\; \mathbf{Q}_g(\cA^!).
\]
The $\hat{A}$-genus generating function
(Corollary~\ref{cor:free-energy-ahat-genus})
resums the scalar genus expansion into an entire function, and
the shadow obstruction tower $\Theta_\cA^{\le r}$
supplies constructive finite-order corrections at each arity.
This chapter derives entanglement entropy,
including the full tower of quantum corrections, from these three ingredients.

The single external input is the conformal dimension
of the twist operator $\sigma_n$ on the $n$-sheeted
branched cover \cite{Calabrese-Cardy04}.  Every other ingredient
(the modular characteristic~$\kappa(\cA)$, the shadow depth
classification, the shadow radius~$\rho(\cA)$, and the
complementarity constraint) is proved in Parts~I--III.

\smallskip
\noindent\emph{Epistemic scope.}\;
The scalar entanglement formula
$S_{\mathrm{EE}} = (c/3)\log(L/\varepsilon)$ is the Calabrese--Cardy result
\cite{Calabrese-Cardy04}, imported via the twist operator
dimension (Lemma~\ref{lem:ent-twist-dimension}), not independently derived here.  The new content begins at arity~$3$: the shadow obstruction tower
$\Theta_\cA^{\leq r}$ contributes computable corrections
$\delta S_r$ to entanglement entropy, evaluated on the replica
geometry, that lie beyond Calabrese--Cardy.  For class~$\mathbf{G}$
(shadow depth~$2$), the Calabrese--Cardy formula is \emph{exact}:
all corrections $\delta S_r = 0$ for $r \geq 3$.  For
class~$\mathbf{M}$ (infinite shadow depth), the corrections are
bounded by $|\delta S_r| \leq C \rho^r r^{-5/2} \log(L/\varepsilon)$
and converge absolutely when the shadow radius $\rho(\cA) < 1$.
Entanglement complementarity
$S_{\mathrm{EE}}(\cA) + S_{\mathrm{EE}}(\cA^!)
= \tfrac{c+c'}{3}\log(L/\varepsilon)$
follows from Theorem~C (complementarity of the
ambient complex), independent of the Calabrese--Cardy input.
The identification of $\delta S_r$ with the Faulkner--Lewkowycz--Maldacena
bulk entropy corrections is heuristic
(Remark~\ref{rem:ent-rt-scalar}).

\smallskip
\noindent\emph{Organization.}\;
\S\ref{sec:ent-replica-shadow} derives entanglement entropy
via the replica trick grounded in the $\hat{A}$-genus.
\S\ref{sec:ent-complementarity} proves the entanglement
complementarity constraint (Theorem~\ref{thm:entanglement-complementarity}).
\S\ref{sec:ent-shadow-corrections} classifies shadow corrections
to entanglement entropy by shadow depth.
\S\ref{sec:ent-qes} identifies the quantum extremal surface
condition as a shadow connection Ward identity.
\S\ref{sec:ent-standard-landscape} computes explicit
entanglement data for the standard landscape.

% ======================================================================
%
%  1. REPLICA TRICK FROM THE A-HAT GENERATING FUNCTION
%
% ======================================================================

\section{Entanglement entropy from the replica trick
and the \texorpdfstring{$\hat{A}$}{A-hat}-genus}
\label{sec:ent-replica-shadow}
\index{replica trick!modular Koszul}

\subsection{Twist operators and the modular characteristic}

\begin{setup}[Entanglement framework]
\label{setup:ent-framework}
Let $(\cA, \cA^!)$ be a chiral Koszul pair
(Definition~\ref{def:thqg-standing-hypotheses})
on a smooth projective curve~$X$ of genus~$0$,
with modular characteristic~$\kappa(\cA)$
and central charge~$c(\cA)$.
% NOTE: The relationship between kappa and c is family-dependent:
% kappa = c/2 for Virasoro, kappa = (k+h^v)d/(2h^v) for KM, kappa = k
% for Heisenberg H_k (where c=1).  The Calabrese-Cardy formula uses c.
% The formulas below are stated in terms of c (the central charge).
Fix an interval $\Sigma = [0, L] \subset X$
with UV cutoff~$\varepsilon$.
The R\'enyi and von~Neumann entropies of~$\Sigma$
in the vacuum state are defined via the replica trick:
\begin{equation}\label{eq:ent-renyi-def}
S_n(\Sigma)
\;:=\;
\frac{1}{1-n}\,\log \Tr(\rho_\Sigma^n),
\qquad
S_{\EE}(\Sigma)
\;:=\;
\lim_{n \to 1} S_n(\Sigma)
\;=\;
-\Tr(\rho_\Sigma \log \rho_\Sigma),
\end{equation}
where $\rho_\Sigma$ is the reduced density matrix obtained by
tracing over the complement $\Sigma^c$.
\end{setup}

\begin{lemma}[Twist operator dimension; \ClaimStatusProvedElsewhere]
\label{lem:ent-twist-dimension}
\index{twist operator!conformal dimension}
The $n$-fold twist operator $\sigma_n$ creating the $n$-sheeted
branched cover has conformal dimension
\begin{equation}\label{eq:ent-twist-dim}
h_n
\;=\;
\frac{c(\cA)}{24}\Bigl(n - \frac{1}{n}\Bigr).
\end{equation}
\end{lemma}

\begin{proof}
This is the standard result of Calabrese--Cardy
\cite[Eq.~(2.5)]{Calabrese-Cardy04},
following from the Schwarzian derivative of
$z \mapsto z^{1/n}$ applied to the stress tensor.
For the Virasoro family, $c = 2\kappa$; the
general relationship between $\kappa$ and~$c$ is family-dependent
(see Setup~\ref{setup:ent-framework}).
\end{proof}

\subsection{Calabrese--Cardy from the modular characteristic}

\begin{theorem}[Entanglement entropy at the scalar level;
\ClaimStatusProvedHere]
\label{thm:ent-scalar-entropy}
\index{Calabrese--Cardy formula!from modular characteristic}
\index{entanglement entropy!scalar level|textbf}
In the framework of Setup~\textup{\ref{setup:ent-framework}},
the scalar-level R\'enyi and von~Neumann entropies of a
single interval are:
\begin{align}
S_n^{\mathrm{sc}}(\Sigma)
&\;=\;
\frac{c(\cA)}{6}\Bigl(1 + \frac{1}{n}\Bigr)
\log\frac{L}{\varepsilon},
\label{eq:ent-renyi-scalar}
\\[4pt]
S_{\EE}^{\mathrm{sc}}(\Sigma)
&\;=\;
\frac{c(\cA)}{3}\,\log\frac{L}{\varepsilon}.
\label{eq:ent-vn-scalar}
\end{align}
\end{theorem}

\begin{proof}
The replica partition function on the $n$-sheeted branched cover is
(non-chiral convention, left$+$right movers):
\begin{equation}\label{eq:ent-replica-partition}
\log Z_n
\;=\;
-\frac{c(\cA)}{6}\Bigl(n - \frac{1}{n}\Bigr)
\log\frac{L}{\varepsilon},
\end{equation}
which follows from the two-point function
$\langle \sigma_n(0)\,\bar\sigma_n(L) \rangle
= (L/\varepsilon)^{-2(h_n + \bar{h}_n)}
= (L/\varepsilon)^{-4h_n}$
with~$h_n = \bar{h}_n$ from~\eqref{eq:ent-twist-dim}
\textup{(}non-chiral: both chiralities contribute equally\textup{)}.
Since $Z_1 = 1$ by normalization:
\[
S_n
= \frac{\log Z_n}{1-n}
= \frac{-(c/6)(n - 1/n)}{1-n}\,\log(L/\varepsilon)
= \frac{c}{6}\cdot\frac{n+1}{n}\,\log(L/\varepsilon),
\]
giving~\eqref{eq:ent-renyi-scalar}.  The limit
$\lim_{n \to 1} S_n = (c/3)\log(L/\varepsilon)$
gives~\eqref{eq:ent-vn-scalar}.
\end{proof}

\begin{remark}[Connection to the $\hat{A}$-genus]
\label{rem:ent-ahat-connection}
\index{A-hat genus@$\hat{A}$-genus!entanglement}
The scalar free energy
$F_g^{\mathrm{sc}}(\cA) = \kappa(\cA) \cdot \lambda_g^{\mathrm{FP}}$
(Corollary~\ref{cor:free-energy-ahat-genus})
resums as $F(\cA;\hbar) = (\kappa/\hbar^2)[\hat{A}(i\hbar)-1]$.
The replica partition function~\eqref{eq:ent-replica-partition}
is determined by~$c$ alone; at the scalar level, the
$\hat{A}$-genus controls the \emph{genus} expansion of the
partition function, while the \emph{replica} index~$n$
determines the sewing parameters of the branched cover.
The two expansions (genus and replica) are orthogonal
projections of the same universal MC element~$\Theta_\cA$.
\end{remark}

% ======================================================================
%
%  2. ENTANGLEMENT COMPLEMENTARITY
%
% ======================================================================

\section{The entanglement complementarity constraint}
\label{sec:ent-complementarity}
\index{complementarity!entanglement|textbf}

\begin{theorem}[Entanglement complementarity; \ClaimStatusProvedHere]
\label{thm:entanglement-complementarity}
\index{entanglement complementarity|textbf}
Let $(\cA, \cA^!)$ be a chiral Koszul pair with modular
characteristics $\kappa(\cA)$, $\kappa(\cA^!)$.
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Koszul duality sum rule.}
At the scalar level, the entanglement entropies of~$\cA$
and its Koszul dual satisfy:
\begin{equation}\label{eq:ent-complementarity-sum}
S_{\EE}^{\mathrm{sc}}(\cA)
+ S_{\EE}^{\mathrm{sc}}(\cA^!)
\;=\;
\frac{c(\cA) + c(\cA^!)}{3}\,
\log\frac{L}{\varepsilon}.
\end{equation}
For the Virasoro family,
$c(\mathrm{Vir}_c) + c(\mathrm{Vir}_{26-c}) = c + (26 - c) = 26$
\textup{(}equivalently,
$\kappa(\mathrm{Vir}_c) + \kappa(\mathrm{Vir}_{26-c}) = 13$
via Theorem~\textup{\ref{thm:anomaly-koszul}}\textup{)},
giving the universal sum:
\begin{equation}\label{eq:ent-virasoro-sum}
S_{\EE}^{\mathrm{sc}}(\mathrm{Vir}_c)
+ S_{\EE}^{\mathrm{sc}}(\mathrm{Vir}_{26-c})
\;=\;
\frac{26}{3}\,\log\frac{L}{\varepsilon}.
\end{equation}

\item \emph{Self-dual pinning.}
At the self-dual point $c = 13$
\textup{(}$\mathrm{Vir}_{13}^! = \mathrm{Vir}_{13}$\textup{)},
$S_{\EE}(\cA) = S_{\EE}(\cA^!)$, and the complementarity
constraint pins the entropy:
\begin{equation}\label{eq:ent-self-dual}
S_{\EE}^{\mathrm{sc}}(\mathrm{Vir}_{13})
\;=\;
\frac{13}{3}\,\log\frac{L}{\varepsilon}.
\end{equation}

\item \emph{Shadow-level complementarity.}
Beyond the scalar level, the shadow duality sums
$D_r(c) := \Sh_r(\mathrm{Vir}_c) + \Sh_r(\mathrm{Vir}_{26-c})$
are controlled by
Remark~\textup{\ref{rem:higher-arity-complementarity}}:
\begin{itemize}
\item Arity $2$: $D_2 = \kappa + \kappa' = 13$
  \textup{(}universal constant\textup{)}.
\item Arity $3$: $D_3 = S_3(c) + S_3(26{-}c) = 4$
  \textup{(}universal constant\textup{)}.
\item Arity $\ge 4$: $D_r(c)$ acquires non-trivial
  $c$-dependence through the Lee--Yang denominators
  $(5c{+}22)$ and $(152{-}5c)$.
\end{itemize}
The discriminant complementarity
$\Delta(\cA) + \Delta(\cA^!) =
6960/[(5c{+}22)(152{-}5c)]$
\textup{(}Theorem~\textup{\ref{thm:shadow-connection}(vi)}\textup{)}
governs the arity-$4$ structure.
\end{enumerate}
\end{theorem}

\begin{proof}
\emph{Part (i).}
By Theorem~\ref{thm:ent-scalar-entropy},
$S_{\EE}^{\mathrm{sc}}(\cA)
= (c(\cA)/3)\log(L/\varepsilon)$.
The sum follows by linearity.
The Virasoro specialization uses
$c(\mathrm{Vir}_c) = c$ and
the duality law $c + c' = 26$
(Theorem~\ref{thm:anomaly-koszul}).

\emph{Part (ii).}
At $c = 13$: $c = c' = 13$, so
$S_{\EE} = 13/3$.

\emph{Part (iii).}
The arity-$2$ and arity-$3$ universality of the duality sums
$D_2 = 13$, $D_3 = 4$ is proved in
Remark~\ref{rem:higher-arity-complementarity}.
The arity-$\ge 4$ $c$-dependence follows from the
discriminant complementarity formula
(Theorem~\ref{thm:shadow-connection}(vi)), whose
numerator $6960$ is a universal constant but whose
denominators $(5c{+}22)(152{-}5c)$ depend on~$c$.
\end{proof}

\begin{remark}[Entanglement as complementarity projection]
\label{rem:ent-as-projection}
\index{entanglement entropy!as complementarity projection}
The complementarity theorem decomposes the gravitational
phase space into Lagrangian halves:
$\mathcal{H}_g(\cA) = Q_g(\cA) \oplus Q_g(\cA^!)$.
The entanglement entropy~\eqref{eq:ent-vn-scalar}
is the scalar projection of this decomposition,
measuring the size of each Lagrangian summand through
$c(\cA)$, which determines the twist operator
dimension.
The modular characteristic~$\kappa(\cA)$ controls the genus-$1$
obstruction; the ratio
$c/\kappa$ is family-dependent (equal to~$2$ for Virasoro but not in general).
The Verdier involution $\sigma$
(Theorem~G4)
exchanges the two summands, enforcing~\eqref{eq:ent-complementarity-sum}.
\end{remark}

% ======================================================================
%
%  3. SHADOW CORRECTIONS AND ENTANGLEMENT COMPLEXITY
%
% ======================================================================

\section{Shadow corrections and entanglement complexity}
\label{sec:ent-shadow-corrections}
\index{entanglement complexity!shadow depth|textbf}
\index{shadow corrections!entanglement entropy}

\begin{proposition}[Entanglement complexity classification;
\ClaimStatusProvedHere]
\label{prop:ent-complexity-classification}
\index{entanglement complexity!four classes|textbf}
The shadow corrections to entanglement entropy
$\delta S_r(\cA)$ at arity~$r$ are determined by the shadow
tower $\Theta_\cA^{\le r}$ evaluated on the replica geometry.
The four shadow depth classes
\textup{(}Theorem~\textup{\ref{thm:shadow-archetype-classification}}\textup{)}
yield four entanglement complexity classes:

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{l@{\quad}c@{\quad}c@{\quad}l}
\textbf{Class} & $r_{\max}$ & \textbf{Corrections}
& \textbf{Prototype} \\
\midrule
\textbf{G} \textup{(Gaussian)} & $2$ &
  None: Calabrese--Cardy exact &
  Heisenberg, lattice \\
\textbf{L} \textup{(Lie/tree)} & $3$ &
  One cubic correction $\delta S_3$ &
  Affine Kac--Moody \\
\textbf{C} \textup{(Contact)} & $4$ &
  Quartic correction $\delta S_4$ \textup{(}$\delta S_3$ may vanish\textup{)} &
  $\beta\gamma$ \\
\textbf{M} \textup{(Mixed)} & $\infty$ &
  Infinite tower $\{\delta S_r\}_{r \ge 3}$ &
  Virasoro, $W_N$ \\
\end{tabular}
\end{center}
In particular:
\begin{enumerate}[label=\textup{(\alph*)}]
\item For class~G algebras, the Calabrese--Cardy
  formula~\eqref{eq:ent-vn-scalar} is the \emph{exact}
  entanglement entropy.
\item For class~M algebras, the corrections
  satisfy $|\delta S_r| \le C \cdot \rho(\cA)^r \cdot r^{-5/2}
  \cdot \log(L/\varepsilon)$ where $\rho(\cA)$ is the shadow
  radius \textup{(Theorem~\textup{\ref{thm:shadow-radius}}\textup{)}.}
  The correction series converges absolutely when
  $\rho(\cA) < 1$, i.e., for $c > c^* \approx 6.125$
  in the Virasoro family.
\end{enumerate}
\end{proposition}

\begin{proof}
The shadow obstruction tower $\Theta_\cA^{\le r}$ consists of
finite-order projections of the bar-intrinsic MC element
(Theorem~\ref{thm:mc2-bar-intrinsic}).  On the replica
geometry~$\Sigma_n$ (the $n$-sheeted branched cover),
$\Theta_\cA^{\le r}$ contributes to
$\log Z_n$ through the genus expansion of the partition
function restricted to graphs of arity at most~$r$.
The shadow depth $r_{\max}(\cA)$ is the
smallest~$r$ at which the tower stabilizes
(Theorem~\ref{thm:shadow-archetype-classification});
for~$r > r_{\max}$, all higher shadows vanish and
$\delta S_r = 0$.

Part~(a): For class~G, $r_{\max} = 2$, so all shadows of
arity $\ge 3$ vanish identically.  The scalar
level~$\kappa$ is the complete content.

Part~(b): The bound on $|\delta S_r|$ follows from the
shadow growth rate
(Theorem~\ref{thm:shadow-radius}):
$|\Sh_r(\Theta_\cA)| \le C_1 \rho(\cA)^r r^{-5/2}$.
Since the entanglement entropy is a linear functional of the
shadow projections on the replica geometry (via the
replica limit~$\partial_n|_{n=1}$), the bound propagates:
$|\delta S_r| \le C \rho(\cA)^r r^{-5/2} \log(L/\varepsilon)$.
Absolute convergence of $\sum_{r \ge 3} |\delta S_r|$
follows from comparison with the geometric series
$\sum \rho^r < \infty$ when $\rho < 1$.
\end{proof}

\begin{remark}[The shadow radius as entanglement convergence radius]
\label{rem:ent-convergence-radius}
\index{shadow radius!entanglement convergence}
For Virasoro at central charge $c$:
$\rho(c) = \sqrt{(180c+872)/(c^2(5c+22))}$.
At the self-dual point $c = 13$: $\rho \approx 0.467$
(convergent, shadow corrections are perturbative).
At the critical string $c = 26$: $\rho \approx 0.233$
(strongly convergent).
At the Ising point $c = 1/2$: $\rho > 1$
(divergent; corrections require Borel resummation
with convergence radius $R = 2\pi/\rho$).

The transition at $c = c^* \approx 6.125$, where
$\rho = 1$, separates the \emph{perturbative regime}
(corrections converge, scalar Calabrese--Cardy is
a good approximation) from the
\emph{nonperturbative regime}
(corrections are asymptotic, Borel summable).
\end{remark}

\begin{corollary}[Full entanglement entropy;
\ClaimStatusProvedHere]
\label{cor:ent-full-entropy}
\index{entanglement entropy!full formula}
For a chirally Koszul algebra $\cA$ in the convergent
regime $\rho(\cA) < 1$:
\begin{equation}\label{eq:ent-full}
S_{\EE}(\cA)
\;=\;
\frac{c(\cA)}{3}\,\log\frac{L}{\varepsilon}
\;+\;
\sum_{r=3}^{r_{\max}(\cA)}
\delta S_r(\cA),
\end{equation}
where the sum terminates at $r = r_{\max}$ for classes
G, L, C and converges absolutely for class~M
with $\rho < 1$.  The shadow duality sums
$D_r := \delta S_r(\cA) + \delta S_r(\cA^!)$ are universal
constants for $r \le 3$ and acquire $c$-dependence for $r \ge 4$
\textup{(}Theorem~\textup{\ref{thm:entanglement-complementarity}(iii)}\textup{)}.
\end{corollary}

% ======================================================================
%
%  4. QUANTUM EXTREMAL SURFACE FROM THE SHADOW CONNECTION
%
% ======================================================================

\section{The quantum extremal surface as shadow
connection stationarity}
\label{sec:ent-qes}
\index{quantum extremal surface|textbf}
\index{shadow connection!QES stationarity}

\begin{proposition}[QES from shadow connection; \ClaimStatusHeuristic]
\label{prop:ent-qes}
\index{quantum extremal surface!from shadow connection|textbf}
The quantum extremal surface formula
\begin{equation}\label{eq:ent-qes-formula}
S_{\QES}
\;=\;
\min_\gamma\Bigl[
\frac{\mathrm{Area}(\gamma)}{4G_N}
+ S_{\mathrm{bulk}}(\Sigma_\gamma)
\Bigr]
\end{equation}
admits the following heuristic decomposition in the modular Koszul framework
\textup{(}the proved algebraic content is the shadow obstruction tower structure; the
identification with the physical QES formula requires a gravitational
dual\textup{)}:
\begin{enumerate}[label=\textup{(\roman*)}]
\item The area term is the scalar entanglement entropy:
  $\mathrm{Area}(\gamma)/(4G_N) = S_{\EE}^{\mathrm{sc}}(\cA)
  = (c/3)\log(L/\varepsilon)$.

\item The bulk entropy $S_{\mathrm{bulk}}$ is the
  shadow correction series
  $\sum_{r \ge 3} \delta S_r(\cA)$,
  controlled by the shadow obstruction tower.

\item The stationarity condition
  $\nabla_\gamma S_{\QES} = 0$
  is a Ward identity of the modular shadow connection
  $\nabla^{\mathrm{hol}}_{g,n} = d - \Sh_{g,n}(\Theta_\cA)$
  restricted to the replica geometry.
\end{enumerate}
\end{proposition}

\begin{proof}
Part~(i) is Theorem~\ref{thm:ent-scalar-entropy}.
Part~(ii) is
Proposition~\ref{prop:ent-complexity-classification}.
For part~(iii), the shadow connection
$\nabla^{\mathrm{hol}} = d - \Sh(\Theta_\cA)$ satisfies
$(\nabla^{\mathrm{hol}})^2 = 0$ (proved as Theorem~G6
in~\S\ref{sec:thqg-soft-graviton-theorems}).
Its Ward identities give
$\nabla^{\mathrm{hol}}(\Sh_r) + o^{(r)} = 0$
at each arity $r \ge 3$.  On the replica geometry,
$S_{\QES}$ is a functional of the shadow projections
$\{\Sh_r|_{\Sigma_n}\}_{r \ge 2}$; the stationarity
$\delta S_{\QES}/\delta\gamma = 0$ is the content of
the Ward identity at the appropriate arity.
\end{proof}

\begin{remark}[RT as scalar approximation]
\label{rem:ent-rt-scalar}
\index{Ryu--Takayanagi formula!as scalar approximation}
The Ryu--Takayanagi formula
$S_{\mathrm{RT}} = \mathrm{Area}(\gamma_{\min})/(4G_N)$
\cite{Ryu-Takayanagi06}
is the scalar-level entanglement entropy
$S_{\EE}^{\mathrm{sc}}(\cA) = (c/3)\log(L/\varepsilon)$.
The quantum corrections
(Faulkner--Lewkowycz--Maldacena~\cite{Faulkner-Lewkowycz-Maldacena13},
Engelhardt--Wall~\cite{Engelhardt-Wall14})
upgrading RT to the quantum extremal surface formula
are heuristically identified with the shadow corrections $\delta S_r$; this identification requires a gravitational dual
and is not a theorem of the present framework.

The shadow depth classification measures the
\emph{quantum complexity of the RT formula}:
class~G (no corrections), class~L (one correction),
class~C (two corrections), class~M (infinite but convergent
tower for $\rho < 1$).
\end{remark}

% ======================================================================
%
%  5. ENTANGLEMENT DATA FOR THE STANDARD LANDSCAPE
%
% ======================================================================

\section{Entanglement census for the standard landscape}
\label{sec:ent-standard-landscape}
\index{standard landscape!entanglement census|textbf}

\begin{theorem}[Standard landscape entanglement census;
\ClaimStatusProvedHere]
\label{thm:ent-landscape-census}
\index{entanglement entropy!standard landscape}
For each standard family, the scalar entanglement entropy
coefficient $S_{\EE}^{\mathrm{sc}} / \log(L/\varepsilon)$,
the shadow depth class, and the convergence status are:

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{l@{\;\;}c@{\;\;}c@{\;\;}c@{\;\;}c}
\textbf{Family} & $\kappa$ &
$\dfrac{S_{\EE}^{\mathrm{sc}}}{\log(L/\varepsilon)}$
& \textbf{Class}
& \textbf{Convergent?} \\
\midrule
Heisenberg $\mathcal{H}_k$
& $k$ & $1/3$ & G & \textup{exact} \\
Lattice $V_\Lambda$
& $\rank(\Lambda)$ & $\rank(\Lambda)/3$ & G & \textup{exact} \\
Affine $\widehat{\mathfrak{g}}_k$
& $\dim\mathfrak{g}\cdot(k{+}h^\vee)/(2h^\vee)$
& $k\dim\mathfrak{g}/\bigl(3(k{+}h^\vee)\bigr)$ & L & yes \\
$\beta\gamma$
& $6\lambda^2-6\lambda+1$
& $(12\lambda^2-12\lambda+2)/3$ & C & yes \\
Virasoro $\mathrm{Vir}_c$
& $c/2$ & $c/3$ & M
& $c > c^*$ \\
$W_N$ \textup{($N \ge 3$)}
& $c(W_N) \cdot (H_N{-}1)$
& $c(W_N)/3$ & M
& $\rho < 1$ \\
\end{tabular}
\end{center}
The Virasoro complementarity constraint is:
\begin{equation}\label{eq:ent-virasoro-census}
\frac{c}{3} + \frac{26-c}{3}
= \frac{26}{3},
\end{equation}
verified for all $c$ in the standard landscape.
\end{theorem}

\begin{proof}
The $\kappa$ values are from the modular characteristic
theorem (Theorem~\ref{thm:modular-characteristic-theorem})
and the individual family computations in Part~II.
The scalar entanglement entropy is
$S_{\EE}^{\mathrm{sc}} = (c/3)\log(L/\varepsilon)$
(Theorem~\ref{thm:ent-scalar-entropy}).
The shadow depth classes are from the shadow archetype
classification
(Theorem~\ref{thm:shadow-archetype-classification}).
The convergence status follows from the shadow radius
(Theorem~\ref{thm:shadow-radius}):
all class~G and~L algebras have finite shadow obstruction towers
(automatically convergent); class~M convergence depends
on~$\rho(\cA) < 1$.
The Virasoro sum~\eqref{eq:ent-virasoro-census} is
$c/3 + (26-c)/3 = 26/3$, verified for all~$c$.
Computational verification: 72~tests in
\texttt{compute/tests/test\_entanglement\_shadow\_engine.py}.
\end{proof}

\begin{remark}[Additivity under independent sums]
\label{rem:ent-additivity}
\index{entanglement entropy!additivity}
For independent chiral algebras $\cA = \cA_1 \oplus \cA_2$
with vanishing mixed OPE
(Proposition~\ref{prop:independent-sum-factorization}),
$\kappa(\cA) = \kappa(\cA_1) + \kappa(\cA_2)$
and therefore
$S_{\EE}(\cA) = S_{\EE}(\cA_1) + S_{\EE}(\cA_2)$
at the scalar level.  The shadow corrections also
separate by independent sum factorization.
\end{remark}

\begin{remark}[Monotonicity of the R\'enyi spectrum]
\label{rem:ent-renyi-monotone}
\index{Renyi entropy@R\'enyi entropy!monotonicity}
The R\'enyi entropy
$S_n^{\mathrm{sc}} = (c/6)(1+1/n)\log(L/\varepsilon)$
is non-increasing in $n \ge 1$ at the scalar level,
with limits:
\begin{itemize}
\item $S_1 = (c/3)\log(L/\varepsilon)$
  \;\textup{(}von~Neumann\textup{)},
\item $S_\infty = (c/6)\log(L/\varepsilon)$
  \;\textup{(}min-entropy\textup{)}.
\end{itemize}
The ratio $S_1/S_\infty = 2$ is universal: the
von~Neumann entropy is exactly twice the min-entropy.
This universality holds at the scalar level for \emph{all}
chirally Koszul algebras and is broken by shadow
corrections at arity $\ge 3$.
\end{remark}

\begin{remark}[Physical interpretation: gravitational
complexity of entanglement]
\label{rem:ent-gravitational-complexity}
\index{gravitational complexity!entanglement}
The entanglement complexity classification
(Proposition~\ref{prop:ent-complexity-classification})
admits a holographic interpretation
contingent on the existence of a gravitational dual:
\begin{itemize}
\item \emph{Class~G}: the bulk is free (Gaussian path integral).
  The RT formula is exact, with no quantum corrections.
  The geodesic $\gamma$ is the unique extremal surface.
\item \emph{Class~L}: the bulk has cubic vertices
  (three-graviton coupling).  One quantum correction
  from a one-loop diagram.
\item \emph{Class~C}: quartic contact interactions appear.
  Two quantum corrections (one-loop $+$ contact).
\item \emph{Class~M}: the bulk has an infinite tower of
  higher-point gravitational vertices.  Infinite quantum
  corrections, convergent for $\rho < 1$, Borel
  summable otherwise.
\end{itemize}
If the gravitational dual exists, the shadow depth
$r_{\max}(\cA)$ measures its \emph{homotopy complexity}:
the minimal number of independent $n$-ary bulk
interactions needed to describe the conjectural
gravitational theory dual to~$\cA$.
\end{remark}

% ======================================================================
%
%  6. BTZ BLACK HOLE ENTROPY FROM THE SHADOW TOWER
%
% ======================================================================

\section{BTZ black hole entropy from the shadow obstruction tower}
\label{sec:ent-btz}
\index{BTZ black hole!entropy from shadow tower|textbf}
\index{Bekenstein--Hawking entropy!from modular characteristic}

The Bekenstein--Hawking entropy of the three-dimensional
BTZ black hole \cite{BTZ92} is controlled by the modular
characteristic $\kappa(\mathrm{Vir}_c)$.  The shadow
tower provides the full genus expansion of quantum
corrections.

\begin{theorem}[BTZ entropy from the modular characteristic;
\ClaimStatusProvedHere]
\label{thm:ent-btz-entropy}
\index{BTZ black hole!entropy|textbf}
Let $\mathrm{Vir}_c$ be the Virasoro algebra at central
charge $c = 3\ell/(2G)$ \textup{(}Brown--Henneaux\textup{)}.
For a BTZ black hole of mass~$M$ and angular momentum~$J$
with conformal weights $h = (M\ell + J)/2$,
$\bar{h} = (M\ell - J)/2$:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Leading entropy.}
The Bekenstein--Hawking entropy is given by the Cardy
formula with effective conformal weights
$h_{\mathrm{eff}} = h - c/24$,
$\bar{h}_{\mathrm{eff}} = \bar{h} - c/24$:
\begin{equation}\label{eq:ent-btz-leading}
S_{\mathrm{BH}}
\;=\;
2\pi\sqrt{\frac{c\,h_{\mathrm{eff}}}{6}}
+ 2\pi\sqrt{\frac{c\,\bar{h}_{\mathrm{eff}}}{6}}
\;=\;
2\pi\sqrt{\frac{\kappa\,h_{\mathrm{eff}}}{3}}
+ 2\pi\sqrt{\frac{\kappa\,\bar{h}_{\mathrm{eff}}}{3}},
\end{equation}
where $\kappa = \kappa(\mathrm{Vir}_c) = c/2$
\textup{(}so $c/6 = \kappa/3$\textup{)}.
In the semiclassical regime $h \gg c/24$,
$h_{\mathrm{eff}} \approx h$ and the formula reduces to
$S_{\mathrm{BH}} \approx 2\pi\sqrt{c\,h/6}$.
For the non-rotating case $J = 0$, the exact formula gives
$S_{\mathrm{BH}} = 2\pi\sqrt{c(M\ell - c/24)/6}$.

\item \emph{Logarithmic correction.}
The one-loop determinant on the BTZ background yields
\begin{equation}\label{eq:ent-btz-log}
S
\;=\;
S_{\mathrm{BH}}
\;-\;\frac{3}{2}\log\frac{S_{\mathrm{BH}}}{2\pi}
\;+\; O(1).
\end{equation}
The coefficient $-3/2$ is universal: it arises from the
second derivative of the free energy at the saddle point
and is independent of the shadow depth class.

\item \emph{Higher-genus corrections.}
The shadow free energies
$F_g^{\mathrm{sc}}(\mathrm{Vir}_c) = \kappa\cdot\lambda_g^{\mathrm{FP}}$
\textup{(}Corollary~\textup{\ref{cor:free-energy-ahat-genus}}\textup{)}
give sub-leading corrections.  The full expansion is
\begin{equation}\label{eq:ent-btz-genus-expansion}
S(M)
\;=\;
S_{\mathrm{BH}}
- \tfrac{3}{2}\log S_{\mathrm{BH}}
+ \sum_{g=2}^{\infty}
\frac{C_g(\kappa)}{S_{\mathrm{BH}}^{2g-2}},
\end{equation}
where $C_g$ depends on the shadow free energies
$F_1, \ldots, F_g$ through the saddle-point expansion.
At genus~$1$: $F_1 = \kappa/24 = c/48$.
At genus~$2$: $F_2 = 7\kappa/5760$.
For class~M algebras, the planted-forest corrections
$\delta_{\mathrm{pf}}^{(g,0)}$ contribute starting at $g = 2$.
\end{enumerate}
\end{theorem}

\begin{proof}
\emph{Part~(i).}
The Cardy formula follows from the modular $S$-transform
of the genus-$1$ partition function
$Z(\tau) = \Tr(q^{L_0 - c/24})$.  Modular invariance
and vacuum dominance in the high-temperature limit give
$\log Z(\tau) \sim \pi c/(12\,\mathrm{Im}\,\tau)$ as
$\mathrm{Im}\,\tau \to 0^+$.
Saddle-point extraction at effective conformal weight
$h_{\mathrm{eff}} = h - c/24$ yields
the leading entropy $2\pi\sqrt{c\,h_{\mathrm{eff}}/6}
= 2\pi\sqrt{\kappa\,h_{\mathrm{eff}}/3}$.
The datum $c = 2\kappa$ is the sole input from the
shadow obstruction tower at the scalar level.

\emph{Part~(ii).}
The saddle-point expansion about the Cardy saddle
$\tau_* = i\sqrt{c/(24h)}$
produces a Gaussian integral whose determinant gives the
$-3/2$ logarithmic correction \cite{Carlip98}.
This is the universal one-loop gravitational contribution,
depending only on the quadratic fluctuation structure.

\emph{Part~(iii).}
The genus-$g$ contribution to the free energy is
$F_g = \kappa\cdot\lambda_g^{\mathrm{FP}}$ at the scalar level.
The saddle-point expansion of
$Z(\beta) = \exp(\sum_{g \ge 0} F_g \beta^{2g})$
yields the asymptotic entropy expansion~\eqref{eq:ent-btz-genus-expansion}.
For Virasoro (class~M), the planted-forest corrections
$\delta_{\mathrm{pf}}^{(g,0)}$
(Remark~\ref{rem:planted-forest-correction-explicit})
contribute additional terms beyond the scalar level
at every genus $g \ge 2$.
Computational verification: \texttt{compute/lib/btz\_shadow\_entropy.py}
and \texttt{compute/lib/btz\_entropy\_allgenus.py}.
\end{proof}

\begin{remark}[Three verification routes at genus~$1$]
\label{rem:ent-btz-three-routes}
\index{BTZ black hole!verification routes}
The genus-$1$ result $F_1 = c/48$ admits three independent
derivations:
\begin{enumerate}[label=(\alph*)]
\item \emph{Heat kernel.}
  The one-loop determinant on the Euclidean BTZ background
  (solid torus) gives
  $\log Z_1 = (c/48)\beta^{-1}$ in the high-temperature limit.
\item \emph{Shadow obstruction tower.}
  $F_1 = \kappa\cdot\lambda_1^{\mathrm{FP}}
  = (c/2)(1/24) = c/48$.
\item \emph{Selberg zeta.}
  The Selberg zeta function for the BTZ quotient
  $\Gamma \backslash \mathbb{H}^3$ gives
  $\log Z_{1\text{-loop}} \sim (c/48)/\beta$
  \cite{Patterson-Perry01}.
\end{enumerate}
The agreement is a consistency check on the
identification $F_1 = \kappa/24$.
\end{remark}

\begin{proposition}[BTZ complementarity; \ClaimStatusProvedHere]
\label{prop:ent-btz-complementarity}
\index{BTZ black hole!complementarity}
For the Koszul pair
$(\mathrm{Vir}_c, \mathrm{Vir}_{26-c})$, the BTZ
entropy satisfies the complementarity constraint:
\begin{equation}\label{eq:ent-btz-complementarity}
S_{\mathrm{BH}}(\mathrm{Vir}_c;\, h)
+ S_{\mathrm{BH}}(\mathrm{Vir}_{26-c};\, h)
\;=\;
2\pi\Bigl(
  \sqrt{\tfrac{c\,(h - c/24)}{6}}
  + \sqrt{\tfrac{(26-c)\,(h - (26-c)/24)}{6}}
\Bigr).
\end{equation}
In the semiclassical regime $h \gg c/24$, this simplifies to
$2\pi\sqrt{h/6}\,(\sqrt{c} + \sqrt{26-c})$,
with maximum at $c = 13$ \textup{(}self-dual point\textup{)},
where $\sqrt{c} + \sqrt{26-c} = 2\sqrt{13}$.
The one-loop free energy sum
$F_1(\mathrm{Vir}_c) + F_1(\mathrm{Vir}_{26-c})
= 13/24$
is the genus-$1$ shadow of the complementarity constant
$K = 13$.
\end{proposition}

\begin{proof}
By additivity of $\kappa$:
$F_1(c) + F_1(26-c) = c/48 + (26-c)/48 = 26/48 = 13/24$.
The entropy sum is immediate from~\eqref{eq:ent-btz-leading}.
The maximum of $f(c) = \sqrt{c} + \sqrt{26-c}$ is at
$f'(c) = 0$, giving $c = 13$, where
$f(13) = 2\sqrt{13}$ (concavity of $\sqrt{\cdot}$).
\end{proof}

\begin{remark}[Monster module]
\label{rem:ent-btz-monster}
\index{Monster module!BTZ entropy}
For the Monster module $V^\natural$ at $c = 24$:
$\kappa = 12$, $F_1 = 1/2$,
$S_{\mathrm{BH}} = 4\pi\sqrt{h-1}$ at leading order
\textup{(}here $h_{\mathrm{eff}} = h - c/24 = h - 1$\textup{)}.
The McKay--Thompson series
$J(\tau) = q^{-1} + 196884q + \cdots$ gives the
degeneracy of BTZ states, with the Monster group acting
as the automorphism group of the black hole Hilbert space.
\end{remark}

% ======================================================================
%
%  7. PAGE CURVE FROM KOSZUL COMPLEMENTARITY
%
% ======================================================================

\section{The Page curve from Koszul complementarity}
\label{sec:ent-page-curve}
\index{Page curve!from Koszul complementarity|textbf}
\index{island formula!Koszul interpretation}

\begin{theorem}[Page curve from complementarity; \ClaimStatusHeuristic]
\label{thm:ent-page-curve}
\index{Page curve|textbf}
\emph{Epistemic note.}  The mathematical content of this theorem
is the elementary crossing-time computation given the two linear
growth rates below.  The physical identification of $c/6$ and
$(26-c)/6$ with Hawking radiation emission and island absorption
respectively, and the identification of the crossing with the
Page transition, are heuristic:
they require a gravitational dual in which the bar complex
$B(\cA^!)$ controls the island saddle, an identification
that is not a theorem of the present framework.

\smallskip
Let $(\mathrm{Vir}_c, \mathrm{Vir}_{26-c})$ be the Virasoro
Koszul pair with entanglement growth rates
$v = c/6$ \textup{(}matter\textup{)} and
$v' = (26-c)/6$ \textup{(}Koszul dual\textup{)}.
The Page curve of the radiation entropy is:
\begin{equation}\label{eq:ent-page-curve}
S_{\mathrm{rad}}(t)
\;=\;
\min\!\bigl(
  S_{\mathrm{Hawking}}(t),\;
  S_{\mathrm{island}}(t)
\bigr),
\end{equation}
where:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Hawking phase} ($t < t_P$):
  $S_{\mathrm{Hawking}}(t) = (c/6)\,t$.

\item \emph{Island phase} ($t > t_P$):
  $S_{\mathrm{island}}(t)
  = S_{\mathrm{BH}} - ((26-c)/6)\,t$.

\item \emph{Page time}:
  \begin{equation}\label{eq:ent-page-time}
  t_P \;=\; \frac{3\,S_{\mathrm{BH}}}{13}
  \;=\;
  \frac{6\,S_{\mathrm{BH}}}{c + (26-c)}.
  \end{equation}
  The denominator is the complementarity constant
  $c + c' = 26$, equivalently $\kappa + \kappa' = 13$
  \textup{(}Theorem~\textup{\ref{thm:entanglement-complementarity}}\textup{)}.
\end{enumerate}
\end{theorem}

\begin{proof}
The Hawking phase growth rate $c/6$ is the
Calabrese--Cardy entanglement production rate
after a local quench, identified with Hawking radiation
emission.  The island phase uses the Koszul dual rate
$(26-c)/6$: once the bar complex $B(\cA^!)$ dominates
$B(\cA)$, the entanglement is controlled by the dual
modular characteristic $\kappa' = (26-c)/2$.

The Page time is determined by the crossing condition:
the two branches
$S_{\mathrm{Hawking}}(t) = (c/6)t$ (increasing)
and
$S_{\mathrm{island}}(t) = S_{\mathrm{BH}} - ((26{-}c)/6)t$
(decreasing) intersect when
\[
\frac{c}{6}\,t_P = S_{\mathrm{BH}} - \frac{26{-}c}{6}\,t_P,
\qquad
t_P\,\frac{c + (26{-}c)}{6} = S_{\mathrm{BH}},
\qquad
t_P = \frac{6\,S_{\mathrm{BH}}}{26}
= \frac{3\,S_{\mathrm{BH}}}{13}.
\]
Computational verification:
\texttt{compute/lib/page\_curve\_shadow.py}.
\end{proof}

\begin{corollary}[Self-dual Page curve; \ClaimStatusHeuristic]
\label{cor:ent-page-self-dual}
\index{Page curve!self-dual point}
At the self-dual point $c = 13$:
\begin{enumerate}[label=\textup{(\alph*)}]
\item The Hawking and island growth rates are equal:
  $v = v' = 13/6$.
\item The Page time is $t_P = 3S_{\mathrm{BH}}/13$
  and the Page curve is symmetric about~$t_P$.
\item The Page entropy is
  $S_{\mathrm{Page}} = S_{\mathrm{BH}}/2$,
  reflecting the perfect $\cA \leftrightarrow \cA^!$ symmetry.
\end{enumerate}
\end{corollary}

\begin{proof}
At $c = 13$: $\kappa = \kappa' = 13/2$, so $v = v' = 13/6$.
The crossing condition gives
$t_P = 6S_{\mathrm{BH}}/26 = 3S_{\mathrm{BH}}/13$.
The Page entropy $S_{\mathrm{Page}}
= (13/6)\cdot 3S_{\mathrm{BH}}/13 = S_{\mathrm{BH}}/2$.
\end{proof}

\begin{remark}[Island formula as Koszul dual dominance]
\label{rem:ent-island-koszul}
\index{island formula!Koszul interpretation}
In the language of
Penington~\cite{Penington19} and
Almheiri--Engelhardt--Marolf--Maxfield~\cite{AEMM19},
the \emph{island} is the region of the bulk where the
Koszul dual bar complex $B(\cA^!)$ dominates the matter
bar complex $B(\cA)$.  The transition from the Hawking
phase to the island phase is the exchange of dominant saddle
in the complementarity sum
$\mathbf{C}_g(\cA) = \mathbf{Q}_g(\cA) \oplus
\mathbf{Q}_g(\cA^!)$
(Theorem~\ref{thm:quantum-complementarity-main}).
The physical content of the Page curve is that
entanglement growth is controlled by $\kappa(\cA)$ at
early times and by $\kappa(\cA^!)$ at late times;
the complementarity constant $\kappa + \kappa' = 13$
determines the crossover.
\end{remark}

\begin{remark}[Page curve from Koszul complementarity; \ClaimStatusHeuristic]
\label{rem:page-curve-complementarity}
\index{Page curve!from complementarity constant}
The Page curve emerges from the Virasoro complementarity sum
$\kappa(\mathrm{Vir}_c) + \kappa(\mathrm{Vir}_{26-c}) = 13$
(Theorem~\ref{thm:quantum-complementarity-main}, AP24).
The Hawking channel grows entropy at rate $c/6$;
the island channel decreases it at rate $(26-c)/6$.
The crossing condition gives a $c$-independent Page time
\[
  t_P = \frac{3 S_{\mathrm{BH}}}{13},
\]
because the $c$ cancels algebraically from
$\kappa + \kappa' = 13$.
At the Koszul self-dual point $c = 13$:
$S_{\mathrm{Page}} = S_{\mathrm{BH}}/2$
with all quantum corrections vanishing identically:
the genus-$g$ correction
$\delta^{(g)}(c) = (c - 13) \cdot \lambda_g^{\mathrm{FP}}$
is antisymmetric under Koszul duality and vanishes at the
self-dual point.  Koszul self-duality is literally the
equal-subsystem condition defining the Page transition.

\emph{Epistemic status}: the scalar-level complementarity and
the Page time formula use only
Theorems~\ref{thm:quantum-complementarity-main}
and~\ref{thm:ent-scalar-entropy} (proved).
The identification of shadow corrections with FLM bulk
entropy corrections requires a gravitational dual and is
heuristic (AP42).
\end{remark}

% ======================================================================
%
%  8. DE SITTER ENTROPY FROM ANALYTIC CONTINUATION
%
% ======================================================================

\section{de~Sitter entropy from analytic continuation of the shadow obstruction tower}
\label{sec:ent-desitter}
\index{de Sitter entropy!from shadow tower|textbf}
\index{analytic continuation!shadow tower}

\begin{proposition}[de~Sitter shadow obstruction tower; \ClaimStatusHeuristic]
\label{prop:ent-desitter}
\index{de Sitter entropy|textbf}
Under the Wick rotation $\ell \mapsto i\ell$ that maps
$\mathrm{AdS}_3$ to $\mathrm{dS}_3$, the Brown--Henneaux
central charge $c = 3\ell/(2G)$ continues to
$c_{\mathrm{dS}} = 3\ell_{\mathrm{dS}}/(2G)$ with
$\kappa_{\mathrm{dS}} = c_{\mathrm{dS}}/2$.
The shadow obstruction tower continues as:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Gibbons--Hawking entropy.}
  The tree-level entropy of the cosmological horizon is
  \begin{equation}\label{eq:ent-ds-leading}
  S_{\mathrm{dS}}
  \;=\;
  \frac{\pi\,c_{\mathrm{dS}}}{3}
  \;=\;
  \frac{\pi\ell_{\mathrm{dS}}}{2G},
  \end{equation}
  which is the area of the cosmological horizon
  in $2{+}1$ dimensions.

\item \emph{One-loop correction.}
  The genus-$1$ free energy continues to
  $F_1 = \kappa_{\mathrm{dS}}/24 = c_{\mathrm{dS}}/48$,
  contributing a real quantum correction to the
  Gibbons--Hawking entropy.

\item \emph{Nariai limit.}
  The analytic continuation of the self-dual point
  $c = 13$ gives $c_{\mathrm{dS}} = 13$
  (the Nariai solution, where the cosmological and
  black hole horizons coincide).  At this point
  $\kappa_{\mathrm{dS}} = 13/2$ and the
  complementarity is symmetric.

\item \emph{Hilbert space dimension.}
  The Banks conjecture
  $\dim(\mathcal{H}_{\mathrm{dS}})
  = \exp(S_{\mathrm{dS}})
  = \exp(\pi c_{\mathrm{dS}}/3)$
  is consistent with the convergent shadow obstruction tower
  \textup{(}Bernoulli decay\textup{)}.
\end{enumerate}
\end{proposition}

\begin{proof}[Sketch]
Under $\ell \mapsto i\ell$, the Brown--Henneaux formula
$c = 3\ell/(2G)$ maps to an imaginary central charge.
In Convention~B (real section), one takes
$\kappa_{\mathrm{dS}} = c_{\mathrm{dS}}/2$ as a real
parameter directly, yielding real free energies.
The Gibbons--Hawking entropy then follows from the genus-$0$
shadow projection, and the genus expansion provides quantum
corrections.  The identification of~\eqref{eq:ent-ds-leading}
with the cosmological horizon area is the dS/CFT
correspondence (Strominger~\cite{Strominger01}).
Computational verification:
\texttt{compute/lib/desitter\_shadow\_entropy.py}.
\end{proof}

% ======================================================================
%
%  9. JT GRAVITY AS SCHWARZIAN LIMIT OF THE SHADOW TOWER
%
% ======================================================================

\section{JT gravity as Schwarzian limit of the shadow obstruction tower}
\label{sec:ent-jt}
\index{JT gravity!from shadow tower|textbf}
\index{Weil--Petersson volumes!shadow tower}

\begin{proposition}[JT gravity from the shadow obstruction tower;
\ClaimStatusHeuristic]
\label{prop:ent-jt-shadow}
\index{JT gravity|textbf}
In the Schwarzian limit $c \to \infty$ with
$\ell = 2Gc/3$ held fixed, the Virasoro shadow obstruction tower
degenerates to the JT gravity genus expansion:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Spectral curve.}
  The shadow metric $Q_{\mathrm{Vir}}(t)$, after rescaling
  $t \mapsto t/\sqrt{c}$, limits to a trigonometric spectral
  curve whose topological recursion reproduces the JT amplitudes.
  The JT spectral curve $y = \sin(2\pi\sqrt{x})/(4\pi)$
  governs the matrix integral of Saad--Shenker--Stanford~\cite{SSS19}.

\item \emph{Genus expansion.}
  The JT partition function is
  \begin{equation}\label{eq:ent-jt-genus}
  Z_{\mathrm{JT}}(\beta)
  \;=\;
  \sum_{g \ge 0}
  e^{S_0(2-2g)}\,Z_g(\beta),
  \end{equation}
  where $Z_g(\beta)$ is expressed in terms of
  Weil--Petersson volumes $V_{g,1}(b)$ via the Laplace
  transform.  The initial data
  $V_{1,1}(b) = (b^2 + 4\pi^2)/48$
  matches the genus-$1$ shadow amplitude $F_1 = \kappa/24$
  after integration against the JT measure.

\item \emph{Shadow CohFT reconstruction.}
  The shadow CohFT extracts Faber--Pandharipande
  intersection numbers $\lambda_g^{\mathrm{FP}}$ that
  govern the scalar contribution to the JT genus expansion.
  At genus~$g$, the shadow free energy
  $F_g = \kappa\cdot\lambda_g^{\mathrm{FP}}$ with
  $\lambda_g^{\mathrm{FP}}
  = (2^{2g-1}-1)|B_{2g}|/(2^{2g-1}(2g)!)$
  matches the leading-order JT amplitude
  in the limit $S_0 \to \infty$.
\end{enumerate}
\end{proposition}

\begin{proof}[Sketch]
The Schwarzian limit of the Virasoro OPE produces the
Schwarzian mode~$\{f, z\}$ as the surviving degree of
freedom.  The shadow connection
$\nabla^{\mathrm{sh}} = d - Q'/(2Q)\,dt$
degenerates to a trigonometric connection whose flat
sections are related to the Bessel function kernel of the
JT matrix integral.  The genus-$1$ matching:
$V_{1,1}(0) = \pi^2/12$ and
$F_1 = \kappa/24 = c/48$; in the Schwarzian limit,
the Laplace transform of $V_{1,1}(b)$ gives
$Z_1(\beta) = (c/48)\beta^{-1}$, consistent with the
shadow obstruction tower at genus~$1$.  The higher-genus matching
requires the full topological recursion, which agrees with
the shadow CohFT reconstruction
(Corollary~\ref{cor:topological-recursion-mc-shadow})
at the level of tautological intersection numbers.
Computational verification:
\texttt{compute/lib/jt\_gravity\_shadow.py}.
\end{proof}

\begin{remark}[Matrix model from the shadow obstruction tower]
\label{rem:ent-jt-matrix-model}
\index{JT gravity!matrix model}
The JT spectral density
$\rho(E) = \sinh(2\pi\sqrt{E})/(4\pi^2)$ emerges from the
shadow obstruction tower in the $c \to \infty$ limit as the
density of states in the Schwarzian quantum mechanics.
The shadow partition function
$Z^{\mathrm{sh}} = \exp(\sum F_g\,\hbar^{2g})$
converges absolutely (Bernoulli decay
$\lambda_g^{\mathrm{FP}} \sim (2\pi)^{-2g}$),
in contrast to the JT perturbative expansion which
diverges as $(2g)!$ and requires
non-perturbative completions
via the matrix integral \cite{SSS19}.
\end{remark}

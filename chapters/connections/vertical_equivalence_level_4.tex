\chapter{The level-4 vertical: line and brane operators as the
Drinfeld double of a chiral Hall algebra}
\label{ch:vertical-equivalence-level-4}

\providecommand{\Hbar}{\mathcal{H}}
\providecommand{\Yplus}{Y^{+}}
\providecommand{\GX}{G(X)}
\providecommand{\Stab}{\operatorname{Stab}}
\providecommand{\Hall}{\operatorname{Hall}}
\providecommand{\rmax}{r_{\max}}
\providecommand{\sptt}{\mathfrak{sp}}
\providecommand{\grtell}{\mathfrak{grt}^{\mathrm{ell}}}
\providecommand{\obs}{\operatorname{obs}}

\begin{abstract}
The fourth slot of the Open Beilinson tower is occupied by the
algebra of line and brane operators on the factorisation dg-category
$\cC^{\mathrm{op}}$ over a tangential log curve $(X, D, \tau)$. The
CY comparison object is the represented chiral quantum group
$\GX$, obtained from the Hall input~$\Yplus(X)$ by the Drinfeld-double
construction and the comparison maps below.  We state the level-$4$
vertical comparison on
$(X, D, \tau)$ as the composite of five named operations on the
positive half: the Hall pairing on
$\Yplus(X)\otimes \Yplus(X)^{*\mathrm{cop}}$; weight completion in
the Hall topology; passage to an integral form preserving the
Cartan; transport along the Maulik--Okounkov stable-envelope
cobordism; descent from the formal locus on $X^\circ$ to the global
factorisation site of $(X, D, \tau)$. The composite is a
strict $E_2$-comparison specified by the modular-Koszul, integral,
completed, stable-envelope-transported, and descended data. Each step
carries a named hypothesis package. At genus one, the represented KZB class is
transported to
$H^2(\Def_{\mathrm{double}}^\bullet(\cA))$ by the package
$H_{\mathrm{F5}}^{\mathrm{desc}}(\cA)$.  Its relation to shadow depth
is formulated as Conjecture~\ref{conj:level-4-F5-shadow-depth}.
\end{abstract}

\section{The level-4 vertical, stated}
\label{sec:level-4-vertical-statement}

\begin{convention}[Five-object firewall, level-$4$ refinement]
\label{conv:level-4-five-object}
At level~$4$ four typed objects must be kept distinct:
\[
\Yplus(X) \;\subsetneq\; \Yplus(X)\bowtie \Yplus(X)^{*\mathrm{cop}}
\;=\; D(\Yplus(X)) \;=\; \GX,
\]
together with the line/brane operator algebra
$\Hbar^{\mathrm{line}}_\cC$ on $\cC^{\mathrm{op}}$. The first three
are objects in the CY quadrant; the fourth is the level-$4$ Open
object. Calling $\Yplus(X)$ "the chiral quantum group", or
identifying $\Yplus(X)$ with the line algebra
$\Hbar^{\mathrm{line}}_\cC$ before passing through the five-operation
bridge below, is the standard form of MA-$8$.
\end{convention}

\begin{theorem}[Level-$4$ vertical equivalence]
\label{thm:level-4-vertical}
\ClaimStatusConditional
\textup{[Type signature: Open/CY vertical comparison; represented
Hall--Drinfeld presentation; level $4$; package
$H_4^{\mathrm{vert}}=(\mathrm{H1})$--$(\mathrm{H7})$.]}
Let $(X,D,\tau)$ be a tangential log curve and $\cC^{\mathrm{op}}$
its factorisation dg-category at level~$0$. Fix a vacuum
$b\in\cC^{\mathrm{op}}$ and let $\cA = A_b = \End_\cC(b)$ be the
chart algebra at level~$1$. Assume the following hypothesis package:
\begin{enumerate}[label=\textup{(H\arabic*)},leftmargin=*]
\item \emph{Modular-Koszul locus} for $\cA$
(Theorem~\textup{A} on $\mathrm{Kosz}(X)$);
\item \emph{Hall-pairing convergence}: the residue pairing
$\langle{-},{-}\rangle_{\mathrm{Hall}}\colon
\Yplus(X)\otimes \Yplus(X)^{*\mathrm{cop}}\to k((\hbar))$
is non-degenerate after completion;
\item \emph{Weight completion}: the Hall product extends along the
weight-graded $J$-adic Mittag--Leffler tower
$\{\Yplus(X)/J^{n}\}_{n\geq 0}$ to the Pro-object
$\widehat{\Yplus}(X) = \varprojlim_n \Yplus(X)/J^{n}$;
\item \emph{Integral form} $\Yplus(X)_{\mathbb Z}\subset
\Yplus(X)\otimes_{\mathbb Z}k$ preserving the Cartan after restriction
of scalars (Drinfeld--Lusztig integral form, with PBW basis indexed
by positive root data of $\cA$);
\item \emph{Stable-envelope transport}: a Maulik--Okounkov stable
envelope $\Stab\colon
K_T(\mathrm{Fix}^T)\to K_T(\Yplus(X)\text{-mod}^{\heartsuit})$
matched with the Hall pairing under the equivariant Euler-class
normalisation;
\item \emph{Global descent}: the formal-disk presentation of
$\Yplus(X)$ glues along $\check{C}\bigl(\cC^{\mathrm{op}}\bigr)$ on
the factorisation site of $(X,D,\tau)$, with quantum master equation
satisfied on global sections and locality of the descent obstruction.
\item \emph{Boundary action comparison}: the represented action of
the descended double on its boundary module supplies an $E_2$-map
\[
 \Xi_4\colon
 \Hbar^{\mathrm{line}}_\cC
 \longrightarrow
 \mathrm{LineMod}\!\left(D(\Yplus(X))\right)
\]
which is a quasi-isomorphism in the completed factorisation ambient
and carries transverse multiplication and half-braiding to the
Drinfeld pairing and universal $R$-matrix.
\end{enumerate}
Then $\Xi_4$ is an $E_2$-equivalence of factorisation algebras
on $(X,D,\tau)$
\begin{equation}\label{eq:level-4-vertical}
\Hbar^{\mathrm{line}}_\cC \;\simeq\;
\mathrm{LineMod}\bigl(\GX\bigr) \;=\;
\mathrm{LineMod}\bigl(D\bigl(\Yplus(X)\bigr)\bigr),
\end{equation}
under which the transverse multiplication of line operators in
$\cC^{\mathrm{op}}$ is the Drinfeld pairing on $\GX$, and the
half-braiding on the boundary representation category is the
universal $R$-matrix of $\GX$.
\end{theorem}

\begin{proof}
Hypotheses~\textup{(H1)--(H5)} construct the completed represented
Hall--Drinfeld double and its $R$-matrix.  Hypothesis~\textup{(H6)}
places that object on the global factorisation site.
Hypothesis~\textup{(H7)} identifies its boundary-module action with
the line-operator algebra by an $E_2$ quasi-isomorphism and transports
the two stated operations.  This is precisely~\eqref{eq:level-4-vertical}.
\end{proof}

This is the Open ↔ CY level-$4$ holographic vertical: the line/brane
operator algebra at level~$4$ on the Open side is the level-$3$
Drinfeld-double output of the level-$4$ Hall input $\Yplus(X)$ on the
CY side, modulo the five named operations of Hall pairing,
completion, integral form, stable envelope, and descent.

\section{The five operations}
\label{sec:five-operations}

\begin{construction}[Hall pairing]
\label{constr:hall-pairing}
On the modular-Koszul locus, the binary collision residue
$r_\cA(z) = \operatorname{Res}^{\mathrm{coll}}_{0,2}(\Theta_\cC)$ of
the master concordance specialises to the Hall pairing
\[
\langle\xi_+, \xi_-\rangle_{\mathrm{Hall}}
\;=\; \operatorname{Res}_{z=0}\,\Tr\bigl(\xi_+(z)\,\xi_-(0)\bigr)\,dz,
\qquad \xi_\pm \in \Yplus(X)^{\pm},
\]
non-degenerate after completion. Convergence relies on Hall-shuffle
finiteness of the K-theoretic CoHA (Schiffmann--Vasserot 2013,
Theorem~3.1) and the modular-Koszul condition that the
Maurer--Cartan twisting datum $\Theta_\cC$ has finite-dimensional
graded pieces.
\end{construction}

\begin{construction}[Weight completion]
\label{constr:weight-completion}
The Hall product is graded by the weight lattice of $\cA$ and is
finite on each fixed weight class but not on $\Yplus(X)$ as a whole;
the Drinfeld-double construction requires inverting the universal
$R$-matrix, which mixes infinitely many weight classes. Completion
is the passage to
$\widehat{\Yplus}(X) = \varprojlim_n \Yplus(X)/J^{n}$ along the
weight-graded $J$-adic filtration with Mittag--Leffler control
($\varprojlim^{1}$-vanishing). Skipping completion produces a
formally divergent $R$-matrix and is the source of the
"$\Yplus = \GX$" form of MA-$8$.
\end{construction}

\begin{construction}[Integral form]
\label{constr:integral-form}
The Drinfeld--Lusztig integral form
$\Yplus(X)_{\mathbb Z}\subset \widehat{\Yplus}(X)\otimes_{\mathbb Z}k$
is the $\mathbb Z[\hbar,\hbar^{-1}]$-submodule generated by divided
powers $E_\alpha^{(n)}/[n]_q!$ of root vectors and Cartan
$K_\alpha^{\pm 1}$. Preservation of the Cartan after restriction of
scalars to $k$ requires the PBW basis to be indexed by positive root
data of $\cA$; failure produces a non-Hopf-algebra integral
$\hbar$-completion which kills the Drinfeld pairing.
\end{construction}

\begin{construction}[Stable-envelope transport]
\label{constr:stable-envelope}
For toric CY$_3$ targets and for $K3\times E$, the Maulik--Okounkov
stable-envelope cobordism
$\Stab\colon K_T(\mathrm{Fix}^T)\to K_T(\Yplus(X)\text{-mod})$
transports the Hall pairing of
Construction~\ref{constr:hall-pairing} to the geometric pairing on
$T$-fixed points. The Maulik--Okounkov $R$-matrix
$R^{\mathrm{MO}}(u) = \Stab^{-1}_{\mathfrak C_+}\circ
\Stab_{\mathfrak C_-}$ from chamber comparison agrees, on the modular
Koszul locus, with the chiral $R$-matrix of the level-$2$ bar
construction up to $\mathrm{GRT}_1(\mathbb Q)$-torsor (Vol~III,
Theorem~\textup{thm:mo-chiral-rmatrix}). Stable-envelope transport
without the $\mathrm{GRT}_1$-torsor matching is the residue path's
genuine open frontier off the $\C^{3}, K3\times E$ locus.
\end{construction}

\begin{construction}[Global descent]
\label{constr:global-descent}
The formal-disk Drinfeld double extends to a global factorisation
algebra on $(X, D, \tau)$ provided the four-mechanism
Costello--Gwilliam package: descent on the factorisation site, the
quantum master equation on global sections, anomaly cancellation
through matched-conventions target data, and locality of the descent
obstruction. The output is the global chiral quantum group $\GX$ on
$(X, D, \tau)$ as a factorisation algebra, with line-operator
representation $\mathrm{LineMod}(\GX) = \Hbar^{\mathrm{line}}_\cC$.
\end{construction}

\begin{remark}[The Drinfeld-double collapse pattern]
\label{rem:level-4-drinfeld-double-collapse}
Each of \textup{Constructions \ref{constr:hall-pairing}--\ref{constr:global-descent}} is its own theorem with hypothesis package; the
identification $\Yplus(X) = \GX$ collapses all five into one
unsupported equality. The collapses $\mathrm{CoHA}(\C^3) = \mathcal
W_{1+\infty}$, $\Yplus = $ "the chiral quantum group", and
"line operators are the CoHA" are three distinct type errors at
hypothesis points. The cure is the five-step bridge above.
\end{remark}

\section{Frontier-five alignment: the elliptic obstruction}
\label{sec:level-4-f5-alignment}

\begin{theorem}[Represented F5 descent class at genus one]
\label{thm:level-4-F5-obstruction}
\ClaimStatusConditional
\textup{[Type signature: Open quadrant; represented elliptic-descent
presentation; level $4$; package $H_{\mathrm{F5}}^{\mathrm{desc}}(\cA)$.]}
Let $\cA$ be a chart algebra in one of the archetype classes
$\mathbf{G}, \mathbf{L}, \mathbf{C}, \mathbf{M}, \mathbf{B}$ with
shadow depths
$\rmax(\cA)\in\{2,3,4,\infty,5\}$ respectively.  The package
$H_{\mathrm{F5}}^{\mathrm{desc}}(\cA)$ consists of:
\begin{enumerate}[label=\textup{(F5.\arabic*)}]
\item a complete deformation complex
$\Def_{\mathrm{double}}^\bullet(\cA)$ for global descent of the
represented Hall--Drinfeld double;
\item a representation $\rho_\cA$ of the universal elliptic braid Lie
algebra on the completed line-operator object, together with the
coefficient morphism
\[
 \gamma_\cA\colon
 C^\bullet\!\left(\grtell,
 \sptt(\cA)\otimes\sptt(\cA^!)\right)
 \longrightarrow \Def_{\mathrm{KZB}}^\bullet(\rho_\cA);
\]
\item a chain map
\[
 \Theta_{\mathrm{F5},\cA}\colon
 \Def_{\mathrm{KZB}}^\bullet(\rho_\cA)
 \longrightarrow \Def_{\mathrm{double}}^\bullet(\cA)
\]
compatible with the factorisation descent differential; and
\item completeness of the resulting obstruction theory for the chosen
globalisation problem.
\end{enumerate}
Under this package, the first global descent obstruction is
\[
\operatorname{Obs}^{(1)}_{\mathrm{double}}(\cA)
:=
H^2(\Theta_{\mathrm{F5},\cA})
\bigl[\omega_{\mathrm{KZB}}(\rho_\cA)\bigr]
\;\in\;
H^2\!\left(\Def_{\mathrm{double}}^\bullet(\cA)\right),
\]
where the source class lies in
\[
H^2\!\left(\Def_{\mathrm{KZB}}^\bullet(\rho_\cA)\right)
\]
and is compared with elliptic Grothendieck--Teichm\"uller cohomology
through~$H^2(\gamma_\cA)$.
The represented double extends across the genus-one descent cover
precisely when this class admits a null-homotopy in
$\Def_{\mathrm{double}}^\bullet(\cA)$.
\end{theorem}

\begin{proof}
Calaque--Enriquez--Etingof~\cite{CEE09} construct the universal flat
KZB connection on elliptic configuration space, and
Enriquez~\cite{Enriquez14} constructs the torsor of elliptic
associators.  Applying the chosen representation~$\rho_\cA$ gives the
source descent cocycle
$\omega_{\mathrm{KZB}}(\rho_\cA)$.  Clause~\textup{(F5.3)} transports
that cocycle to the deformation complex of the represented double.
Standard complete Maurer--Cartan obstruction theory identifies its
degree-two class as the first obstruction to descent; clause
\textup{(F5.4)} makes its null-homotopy sufficient for the lift.
\end{proof}

\begin{conjecture}[Shadow-depth detection by the represented F5 class]
\label{conj:level-4-F5-shadow-depth}
\ClaimStatusConjectured
Assume $H_{\mathrm{F5}}^{\mathrm{desc}}(\cA)$ and choose the
family representation~$\rho_\cA$.  Then
\[
 \operatorname{Obs}^{(1)}_{\mathrm{double}}(\cA)=0
 \quad\Longleftrightarrow\quad
 \rmax(\cA)=2.
\]
Establishing this statement amounts to computing the represented KZB
class for each archetype and constructing its comparison with the
first surviving higher operation.  The universal KZB connection and
the elliptic-associator torsor provide the source geometry for this
calculation.
\end{conjecture}

\begin{corollary}[Conditional stratification of the level-$4$ vertical]
\label{cor:level-4-stratification}
\ClaimStatusConditional
The level-$4$ vertical
equivalence~\eqref{eq:level-4-vertical} holds globally over
$\overline{M}_{g,n}$ at $g=0$ under the rational Drinfeld--Kohno
associator and the hypotheses of Theorem~\ref{thm:level-4-vertical}.
At genus one, assume $H_{\mathrm{F5}}^{\mathrm{desc}}(\cA)$ and the
archetype computation of
Conjecture~\ref{conj:level-4-F5-shadow-depth}.  The strict untwisted
equivalence then occupies Class~$\mathbf G$; each of the other four
archetypes carries the additional datum of a null-homotopy for its
represented F5 class.  Higher genus carries the corresponding
higher-Teichm\"uller descent package.
\end{corollary}

\section{Hypotheses and non-collapse}
\label{sec:level-4-hypothesis-heal}

\begin{remark}[Hypothesis package]
\label{rem:level-4-hypothesis-package}
The level-$4$ vertical equivalence carries the hypothesis package
\textup{(H1)--(H7)} of
Theorem~\ref{thm:level-4-vertical}. Each $H_i$ names a separate
construction or comparison:
\begin{itemize}[leftmargin=*]
\item (H1) Theorem~A on $\mathrm{Kosz}(X)$
(Vol~I, modular-Koszul locus);
\item (H2) Hall-pairing convergence
(Schiffmann--Vasserot~2013 Theorem~3.1; Vol~III
Theorem~\textup{thm:drinfeld-center-coha} part (i));
\item (H3) Weight-completion Mittag--Leffler
($J$-adic, $\varprojlim^{1}$-vanishing per
Vol~I~Appendix~Q);
\item (H4) Drinfeld--Lusztig integral form
(Lusztig~1990; Drinfeld~1985 \S 9 for the rational case);
\item (H5) MO stable-envelope cobordism
(Maulik--Okounkov~2012 Theorem~10.1.1; chamber-compatibility
Vol~III~Theorem~\textup{thm:mo-chiral-rmatrix});
\item (H6) Costello--Gwilliam globalisation:
descent + QME + anomaly + locality.
\item (H7) represented boundary action comparison:
$\Xi_4$ is an $E_2$ quasi-isomorphism in the completed
factorisation ambient and intertwines the Drinfeld operations.
\end{itemize}
\end{remark}

\begin{remark}[Five-step construction]
\label{rem:level-4-heal-path}
The comparison from the Hall input~$\Yplus(X)$ to the global chiral
quantum group~$\GX$ is the composite of the following operations:
\textup{(i)} verify that the Hall pairing of
Construction~\ref{constr:hall-pairing} is convergent on the
modular-Koszul locus; \textup{(ii)} apply weight completion of
Construction~\ref{constr:weight-completion}; \textup{(iii)} pass to
the integral form of Construction~\ref{constr:integral-form};
\textup{(iv)} transport via the stable envelope of
Construction~\ref{constr:stable-envelope};
\textup{(v)} descend globally via
Construction~\ref{constr:global-descent}.  At genus one the final
operation is governed by the represented class of
Theorem~\ref{thm:level-4-F5-obstruction}; the archetype dependence is
the content of Conjecture~\ref{conj:level-4-F5-shadow-depth}.
\end{remark}

\begin{remark}[Cross-volume references]
\label{rem:level-4-cross-volume}
The CY-side technology lives in Vol~III Chapter~\textup{ch:drinfeld-center}
(Drinfeld centre) and Chapter~\textup{ch:quantum-chiral-algebras}
(Hall--Drinfeld double on $K3\times E$); the
Maulik--Okounkov stable-envelope bridge is verified at
$\mathbb C^{3}$ and $K3\times E$ with $104$ tests in
\texttt{compute/lib/mo\_rmatrix\_k3e.py}; the F5 elliptic obstruction
is the level-$4$ Open obstruction. The level-$3$
companion (derived chiral centre as universal closed sector, with
physical bulk only after the open--closed comparison) lives in
Chapter~\ref{ch:holographic-datum-master}, where the centre
projection $P_\cC(U_\cA)$ on the matched-pair algebra
$U_\cA = \cA\bowtie\cA^!_\infty$ is the level-$3$ output of the
level-$4$ Drinfeld double $\GX$ via
Theorem~\textup{thm:hdm-u-a-drinfeld-center}.
\end{remark}

\begin{remark}[Three construction routes for the F5 shadow-depth comparison]
\label{rem:level-4-three-paths}
Conjecture~\ref{conj:level-4-F5-shadow-depth} admits three
independent construction routes:
\begin{enumerate}[label=\textup{(\roman*)},leftmargin=*]
\item \emph{Chain comparison.}  Construct
$(\iota_\cA,p_\cA,h_\cA,\nu_\cA)$ on the family bar complex with
$dh_\cA+h_\cA d=\nu_\cA(\mathrm{id}-\iota_\cA p_\cA)$, then build a
chain map from its first surviving higher operation to
$\Def_{\mathrm{KZB}}^\bullet(\rho_\cA)$.
\item \emph{Represented KZB calculation.}  Specify~$\rho_\cA$ and
evaluate the universal KZB connection in that representation.  This
produces an archetype-indexed cohomology class whose vanishing can be
checked inside the represented deformation complex.
\item \emph{Cross-volume comparison.}  Construct the map from the
Vol~III Hall--Drinfeld double and its elliptic-associator action to the
Vol~I descent complex, and identify its Face~5 component with
$H^2(\Theta_{\mathrm{F5},\cA})
[\omega_{\mathrm{KZB}}(\rho_\cA)]$.
\end{enumerate}
\end{remark}

\section{Closure of the level-$4$ vertical}
\label{sec:level-4-closure}

The level-$4$ Open object — the line and brane operator algebra on
$\cC^{\mathrm{op}}$ over $(X,D,\tau)$ — receives the represented
Hall--Drinfeld double~$\GX$ through the five-step bridge under
\textup{(H1)--(H7)}.  At genus one, the additional package
$H_{\mathrm{F5}}^{\mathrm{desc}}(\cA)$ places the first descent class
in $H^2(\Def_{\mathrm{double}}^\bullet(\cA))$; a chosen
null-homotopy completes the global lift.  The dependence of this
class on shadow depth is isolated in
Conjecture~\ref{conj:level-4-F5-shadow-depth}.

The level-$4$ vertical closes the Beilinson-tower hierarchy of
Vertical equivalences between Vol~I (Open) and Vol~III (CY): together
with the level-$0$ factorisation dg-cat~$\leftrightarrow$~6d hCS
quartic anomaly equivalence, the level-$1$ chart $A_b$~$\leftrightarrow$~$\Phi_d^{\mathrm{FA}}$ equivalence, the level-$2$
bar~$\leftrightarrow$~$\mathrm{Sp}^{\mathrm{ch}}_{\Sigma_{d-1}, C}$
equivalence, the level-$3$ derived centre~$\leftrightarrow$~$\mathsf{SC}^{\mathrm{ch,top}}$-brace equivalence (Vol~II), and the
level-$5$ modular trace~$\leftrightarrow$~$\kappa_{\mathrm{BKM}}(\Phi_N) = c_N(0)/2$ comparison (Vol~III), this gives a conditional
Open~$\leftrightarrow$~CY comparison at every level of the Beilinson
tower.  Each level carries its reconstruction theorem, hypothesis
package, and represented comparison map.

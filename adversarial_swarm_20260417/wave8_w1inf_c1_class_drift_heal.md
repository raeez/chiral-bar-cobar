# Wave-8 $\mathcal{W}_{1+\infty}$ at $c=1$ class-drift heal (2026-04-17)

## Contradiction surfaced (Wave-8 #52 orthogonal finding)

Vol III `modular_koszul_bridge.tex:370, 380` calls $\mathcal{W}_{1+\infty}$ at $c=1$ **class M** (shadow depth $\infty$, MacMahon).

Vol III `m3_b2_saga.tex:1146-1147` says: "for $\mathbb{C}^3$, $S_3 = 0$ (class G)".

The `main.tex:1007` landscape table lists $\mathbb{C}^3$ under "class **G**, $\kappa_{\mathrm{ch}} = 1$, $Y(\widehat{\mathfrak{gl}}_1) \simeq \mathcal{W}_{1+\infty}$ (Jordan quiver)".

## Phase 1 forensics

### $S_3$ of Virasoro (compute/lib/linf_bracket_engine.py)

$S_3(\mathrm{Vir}_c) = \alpha = 2$ UNIFORMLY in $c$ (c-independent, nonzero). Independently verified via:
- Convolution recursion: $a_0 = \kappa$, $a_1 = 6$, $S_3 = a_1/3 = 2$.
- Shadow metric: $Q_L(t) = (c + 6t)^2 + 80 t^2/(5c+22)$, so $q_1 = 12c = 12 \kappa \alpha$ gives $\alpha = 2$.

Corollary: Virasoro at **every** $c$, including $c=1$, is **class M** ($r_{\max} = \infty$).

### Resolution: three distinct objects both labelled "$\mathbb{C}^3$"

1. **Heisenberg $H_1$** (single free boson, $E_1$-primitive): class **G**, $r_{\max} = 2$, bar Euler product $1/\eta(q)$.
2. **CoHA / $Y^+(\widehat{\mathfrak{gl}}_1)$** (Jordan quiver input, $E_1$-chiral / Lie): $\mu_3 = 0$ at the $A_\infty$ chain level; treated as class G in `main.tex:1007` landscape and `m3_b2_saga.tex:1146`.
3. **$\mathcal{W}_{1+\infty}$ at $c = 1$** (full $E_\infty$ chiral envelope, infinitely many spin-$s$ currents): class **M**, $r_{\max} = \infty$, $S_4(\mathrm{Vir}_{c=1}) = 10/27$ via Virasoro subalgebra, bar Euler product $1/M(q)$ (MacMahon).

The canonical discriminator is `bar_cobar_bridge.tex:63-103` (`thm:c3-shadow-tower` + `rem:envelope-depth-jump`):
> "The Heisenberg algebra $H_1$ has shadow depth class G with $r_{\max}=2$. The vertex algebra $\mathcal{W}_{1+\infty}$ at $c=1$, the factorization envelope [...], has shadow depth class M with $r_{\max}=\infty$. The spin-2 channel (Virasoro at $c=1$) already has infinite shadow depth."

This is the **correct** statement. The envelope construction jumps from class G (input Lie conformal algebra) to class M (output chiral envelope).

### Category errors in the two target files

**`modular_koszul_bridge.tex:370-380`**. The class-M label is correct at the OUTPUT level (full $\mathcal{W}_{1+\infty}$), matching `bar_cobar_bridge.tex` and `thm:c3-shadow-tower`. BUT line 372 injects the wrong claim: "at $c=1$ where $\mathcal{W}_{1+\infty}$ is freely generated by a single field of weight 1". This is FALSE: $\mathcal{W}_{1+\infty}$ has infinitely many spin-$s$ generators at every value of $\Psi$ (Prochazka-Rapcak); the $c=1$ specialization does not collapse to a single field. Heisenberg $H_1$ is freely generated by a single weight-1 field, but $H_1 \neq \mathcal{W}_{1+\infty}|_{c=1}$.

Heal: replace line 372 with the correct uniform-weight tag scoped to the Virasoro subalgebra (which IS the class-M-controlling channel), cite `rem:envelope-depth-jump` explicitly, and inscribe the envelope jump as the reason class M persists.

**`m3_b2_saga.tex:1146-1147`**. The statement "$S_3 = 0$ for $\mathbb{C}^3$, class G" is valid ONLY at the CoHA / pre-$E_\infty$-envelope level ($A_\infty$ operations $\mu_k$ on the Jordan-quiver input) — NOT for the output chiral algebra $\mathcal{W}_{1+\infty}$ where the Virasoro subalgebra forces $S_3 = 2$, class M. The remark silently conflates the input CoHA shadow level with the output chiral shadow level.

Heal: disambiguate which algebra carries the $S_3 = 0$ vs $S_3 = \alpha$ invariant. The $[m_3, B^{(2)}]$ question lives at the INPUT CoHA level (class G for Jordan, class M for local $\bP^2$ input), which is distinct from the OUTPUT chiral envelope shadow class.

## Phase 2 heal

(a) `modular_koszul_bridge.tex` line 372: fix the "freely generated by a single field" misstatement; anchor to Virasoro-subalgebra envelope-jump mechanism.

(b) `m3_b2_saga.tex` line 1146-1147: disambiguate "$\mathbb{C}^3$" as CoHA-input ($\mu_3 = 0$, class G at input) vs chiral-output ($\mathcal{W}_{1+\infty}$, class M at output via Virasoro subalgebra envelope jump).

Both heals anchor on the canonical `rem:envelope-depth-jump` (bar_cobar_bridge.tex) so future audits cannot re-drift.

## HZ-IV disjointness

Independent paths for "$\mathcal{W}_{1+\infty}|_{c=1}$ is class M":
- Virasoro subalgebra $S_3 = 2$ (compute/lib/linf_bracket_engine.py).
- MacMahon bar Euler product $\neq 1/\eta(q)$ (combinatorial).
- Shadow metric $S_4(\mathrm{Vir}_{c=1}) = 10/[1 \cdot 27] = 10/27 \neq 0$ (Vol I Wave-5 verified).

Disjoint from any $A_\infty$-on-CoHA computation. Three genuinely independent witnesses.

## Cache entries (local, not typeset)

Pattern 222: envelope shadow-class jump. "Class G input $\to$ class M output under factorization-envelope" is the CORRECT statement of $\mathbb{C}^3 \to \mathcal{W}_{1+\infty}$. Confusing the input shadow class with the output shadow class is the drift failure mode. Always name WHICH algebra (input $H_1$ / CoHA $Y^+(\widehat{\mathfrak{gl}}_1)$ / output $\mathcal{W}_{1+\infty}$) carries the class label.

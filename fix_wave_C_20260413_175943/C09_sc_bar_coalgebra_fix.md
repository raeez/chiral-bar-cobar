# C09_sc_bar_coalgebra_fix (1177s)

**Fixed**
- PROBLEM at [en_koszul_duality.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex:6290): the dimensional-ladder summary still said the ordered bar was the Swiss-cheese coalgebra with closed/open colors. EXACT edit applied: replaced that with “`\barB^{\mathrm{ord}}(\cA)` is a single-coloured `\Eone`-chiral dg coalgebra ... the actual `\mathsf{SC}^{\mathrm{ch,top}}` datum appears only on the derived center pair `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”

- PROBLEM at [bar_cobar_adjunction_curved.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/bar_cobar_adjunction_curved.tex:145): the Swiss-cheese bullet still identified the bar differential/coproduct as SC colors on `\barB^{\mathrm{ord}}(\cA)`. EXACT edit applied: rewrote the bullet to say those are holomorphic/topological inputs on the ordered bar, but they do not make the bar itself an `SC^{\mathrm{ch,top}}` object; SC appears only on `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.

- PROBLEM at [concordance.tex](/Users/raeez/chiral-bar-cobar/chapters/connections/concordance.tex:3835): concordance still said Volume II uses `B^{\mathrm{ord}}` for the open color and `B^\Sigma` for the closed color. EXACT edit applied: replaced that with “Volume II uses `B^{\mathrm{ord}}` as the `E_1` line-side engine, while the actual two-colour datum is the derived-center pair `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”

- PROBLEM at [bar_construction.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/bar_construction.tex:2054): the remark ended with “Together they constitute the Swiss-cheese algebra.” EXACT edit applied: replaced that with “Together they exhibit the two geometric directions ... on the bar complex itself they still form only the single-coloured `\Eone` dg coalgebra ... the actual `SC^{\mathrm{ch,top}}` datum appears only on the derived-center pair.”

- PROBLEM at [two_colored_bar_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/two_colored_bar_engine.py:34): the module docstring and summary still said `B^ch(A)` is already an `SC`-coalgebra. EXACT edit applied: rewrote both sites to say the genuine two-coloured operadic object is `B_{SC}(A,A)`, while `B^ch(A)` is only the ordered `E_1` tensor-coalgebra sector; the manuscript’s SC datum is `(C^bullet_ch(A,A),A)`.

- PROBLEM at [en_bar_coproduct_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/en_bar_coproduct_engine.py:72): the module intro, `swiss_cheese_analysis()`, and `chiral_bar_summary()` still treated `B^ch(A)` as `B_{SC}`. EXACT edit applied: changed all three to say `B^ch(A)` is the ordered `E_1` bar coalgebra, and SC appears only on the derived-center pair.

- PROBLEM at [factorization_coproduct_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/factorization_coproduct_engine.py:1033): the summary diagram still labeled “bar differential = closed color / coproduct = open color.” EXACT edit applied: replaced that with “holomorphic collision data / deconcatenation coproduct,” and added the sentence that these do not by themselves make `B(A)` a two-coloured SC datum.

- PROBLEM at [tensor_harrison_bar_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/tensor_harrison_bar_engine.py:1450): the returned explanation still said SC-coalgebra structure lives on `Sym^c`. EXACT edit applied: replaced it with “`Sym^c` is the full geometric factorization coalgebra of Vol I, not the Swiss-cheese coalgebra; the actual SC datum lives on `(C^\bullet_{ch}(A,A),A)`.”

- PROBLEM at [theorem_swiss_cheese_kontsevich_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/theorem_swiss_cheese_kontsevich_engine.py:23): the engine still spoke of SC-coalgebra structure on the bar complex. EXACT edit applied: rewrote the docstring and the key inline comment to say the ordered bar supplies the `E_1` line-side structure, while the genuine SC datum is the derived-center pair.

- PROBLEM at [sc_bar_cobar_inversion_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/sc_bar_cobar_inversion_engine.py:1): the “SC version” framing still suggested SC cobar on `B^ch(A)`. EXACT edit applied: retitled/rephrased it as a derived-center/SC comparison, and changed the class/function docs to say bar-cobar inversion recovers only `A`; the genuine SC datum is `(C^*_ch(A,A),A)`.

- PROBLEM at [curved_sc_higher_genus_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/curved_sc_higher_genus_engine.py:1): the module, class docs, and notes still described genus-`g` bar objects as curved/corrected `SC` coalgebras. EXACT edit applied: rewrote them as legacy-named wrappers for the curved/corrected ordered bar coalgebra, with explicit statements that SC remains on the derived-center pair.

- PROBLEM at [test_curved_sc_higher_genus_engine.py](/Users/raeez/chiral-bar-cobar/compute/tests/test_curved_sc_higher_genus_engine.py:1): the test docstrings still advertised “curved `SC^{ch,top}`-coalgebra structure at higher genus.” EXACT edit applied: changed those descriptions to “curved ordered bar coalgebra,” including the modular-tower and Heisenberg genus-0 test text.

- PROBLEM at [forgetful_e_infty_to_e1_report.md](/Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md:43): the audit note still quoted the false closed/open-color-on-bar slogan and later called `B^ch(A)` an `SC` coalgebra. EXACT edit applied: inserted “Superseded by AP165” at the quote, rewrote the “deepest answer” around line 168 as “ordered `E_1` coalgebraic engine,” and renamed Q6 to the genuine two-coloured operadic-bar question.

- PROBLEM at [factorization_bar_literature_report.md](/Users/raeez/chiral-bar-cobar/compute/audit/factorization_bar_literature_report.md:202): the literature report still said the bar complex is a coalgebra over `SC^{ch,top}` and repeated that later. EXACT edit applied: replaced both occurrences with “ordered `E_1` coalgebra” plus the correction that the genuine SC datum appears only on `(C^\bullet_{ch}(A,A),A)`.

- PROBLEM at [adversarial_swarm_findings.md](/Users/raeez/chiral-bar-cobar/compute/audit/adversarial_swarm_findings.md:14): the minimal decomposition still ended with “observe bar is a coalgebra over SC.” EXACT edit applied: changed it to “observe that the ordered bar is an `E_1` coalgebra and that the genuine SC datum appears only on the derived-center pair.”

- PROBLEM at [deformation_quantization_sc_report.md](/Users/raeez/chiral-bar-cobar/compute/audit/deformation_quantization_sc_report.md:119): the report still said “including bar complexes” when describing recoverable `SC` coalgebras. EXACT edit applied: changed that to “Every genuine `SC^{ch,top}`-coalgebra ... ordered bar complexes of single chiral algebras are not themselves SC coalgebras.”

- PROBLEM at [codex_survey_v2_math_review_wave14.md](/Users/raeez/chiral-bar-cobar/compute/audit/codex_survey_v2_math_review_wave14.md:101): the audit still treated the local corrected statement as “Swiss-cheese coalgebra.” EXACT edit applied: changed the finding to “ordered `E_1` coalgebra” and updated the verification sentence to point to the current `E_1` theorem wording.

- PROBLEM at [qc_math_surfaces.json](/Users/raeez/chiral-bar-cobar/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json:724): generated audit metadata still normalized the false prefix “The bar complex presents the Swiss-cheese coalgebra.” EXACT edit applied: changed that normalized prefix to “The ordered bar complex is an `E_1` coalgebra and does not itself present the Swiss-cheese datum ...”.

**Blocked external files**
- Remaining false live claims are in Vol II, not Vol I: `/Users/raeez/chiral-bar-cobar-vol2/chapters/theory/factorization_swiss_cheese.tex:934,2145,2229` and `/Users/raeez/chiral-bar-cobar-vol2/working_notes.tex:236,1149`.
- I did not edit them because this session is writable only in `/Users/raeez/chiral-bar-cobar`; Vol II/III are outside the writable root.
- Vol III did not show an uncorrected positive AP165 claim in live source from the final grep; its matches were already negative/constitutional warnings.

**Verification**
- Re-ran the hostile grep in the current repo. Remaining matches are only the intentional blacklist/warning surfaces in `AGENTS.md`/`CLAUDE.md` and generic operadic SC bar/cobar statements about actual two-coloured objects.
- Ran `pytest -q compute/tests/test_en_bar_coproduct_engine.py compute/tests/test_two_colored_bar_engine.py compute/tests/test_curved_sc_higher_genus_engine.py compute/tests/test_sc_bar_cobar_inversion_engine.py`: `331 passed`.


---
STDERR:
OpenAI Codex v0.104.0 (research preview)
--------
workdir: /Users/raeez/chiral-bar-cobar
model: gpt-5.4
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: xhigh
reasoning summaries: auto
session id: 019d87a1-8d39-7230-86a3-cd643b575475
--------
user
<task>
You are an adversarial auditor + fixer. Find issues AND fix them in one pass.
For each finding: PROBLEM at file:line, then the EXACT edit applied.
</task>
<action_safety>
Only edit the assigned files. Minimum truthful edits.
</action_safety>
<completeness_contract>
Be exhaustive within the assigned scope. Fix everything you find.
</completeness_contract>


Fix any remaining 'B(A) is SC coalgebra' claims (AP165/B54).
Search ALL volumes for 'bar.*SC\|B(A).*SC\|coalgebra.*SC'.
B(A) is E_1 coalgebra. SC lives on (C^bullet_ch(A,A), A). Fix each.
mcp startup: no servers

thinking
**Planning updates and exploration**

I'm thinking about updating the commentary and perhaps an update plan, but it seems like there's no need for it before the initial exploration. For the substantial work after understanding the context, a longer plan will be necessary. I'll gather the required files and search for occurrences using parallel execution. Maybe I'll start with `sed -n '1,200p' CLAUDE.md`, but it's too large, so using `grep` for AP165/B54/Swiss-cheese could be the way to go. Opening skill files with `rg` might also be a good first step.
codex
I’m auditing all three volumes for any residual `B(A)`/Swiss-cheese coalgebra conflations, starting with the required constitutional reads, the relevant skill instructions, and a repo-wide grep to lock the exact claim surface before editing.
exec
/bin/zsh -lc "sed -n '1,220p' CLAUDE.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,220p' metadata/theorem_registry.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,220p' chapters/connections/concordance.tex" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "if [ -f raeeznotes/raeeznotes100/red_team_summary.md ]; then sed -n '1,220p' raeeznotes/raeeznotes100/red_team_summary.md; else sed -n '1,220p' archive/raeeznotes/raeeznotes100/red_team_summary.md; fi" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,220p' .agents/skills/cross-volume-propagation/SKILL.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,220p' .agents/skills/deep-beilinson-audit/SKILL.md" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
# CLAUDE.md -- Modular Koszul Duality Programme (Canonical Reference)

## Identity

E_1-E_1 operadic Koszul duality in the homotopical modular chiral realm on algebraic curves. One form (eta = d log(z_1 - z_2)), one relation (Arnold), one object (Theta_A), one equation (D*Theta + 1/2[Theta,Theta] = 0). The primitive object is B^ord(A) = T^c(s^{-1}A-bar): ordered bar, deconcatenation coproduct, R-matrix, Yangian. The symmetric bar B^Sigma is the Sigma_n-coinvariant shadow. Physics IS the homotopy type: A-infinity = scattering, SC^{ch,top} governs the (bulk, boundary) pair, modular L-infinity = genus tower. The five theorems A-D+H are the invariants that survive averaging.

**Bar complex is E_1-coassociative; SC^{ch,top} emerges on the derived center (CRITICAL, corrected 2026-04-12):** The bar complex B^{ord}(A) = T^c(s^{-1}A-bar) is an E_1-chiral coassociative COALGEBRA (over ChirAss^!). It has a differential + deconcatenation coproduct. It does NOT carry SC^{ch,top} structure. The SC^{ch,top} structure (or E_3 with conformal vector) emerges on the DERIVED CENTER Z^{der}_{ch}(A) = ChirHoch*(A,A), computed USING the bar complex as a resolution. The bar complex is the E_1 engine; the derived center is the SC^{ch,top}/E_3 output. thm:bar-swiss-cheese (claiming B(A) is an SC-coalgebra) needs retraction or careful restatement — see FOUND-11. princ:sc-two-incarnations in en_koszul_duality.tex states this correctly.

Three volumes by Raeez Lorgat. Vol I *Modular Koszul Duality* (this repo, ~2,650pp). Vol II *A-infinity Chiral Algebras and 3D HT QFT* (~/chiral-bar-cobar-vol2, ~1,704pp). Vol III *CY Categories, Quantum Groups, and BPS Algebras* (~/calabi-yau-quantum-groups, ~259pp). Total ~4,613pp, 120K+ tests, 3,463 tagged claims. This file is the canonical reference; Vols II/III inherit shared content and add volume-specific material.

**Crystallized programme identity (2026-04-12):** We are studying **holomorphic chiral (factorisation) (co)homology** via **bar and cobar chain constructions** at **various different geometric locations**, hence the **different (modular) operads** at play. The geometry determines the operad, the operad determines the bar complex, the bar complex computes the factorisation (co)homology. The five theorems are structural properties. The shadow tower is the characteristic class data. The E_n circle is the holographic structure.

**The E_n operadic circle (2026-04-12):** E_3(bulk) → E_2(boundary chiral) → E_1(bar/QG) → E_2(Drinfeld center) → E_3(derived center). Each arrow is: restriction to codim-2 defect, ordered bar complex, categorified averaging (Drinfeld center), higher Deligne (derived center). The circle closes for 3d HT theories with conformal vector; without conformal vector, stuck at SC^{ch,top} (the intermediary between E_1-chiral and E_3).

**SC^{ch,top} ≠ E_3 (2026-04-12):** The Swiss-cheese operad is two-coloured with directionality (no open-to-closed). Dunn additivity does NOT apply. E_3 requires the topologization theorem: SC^{ch,top} + inner conformal vector (Sugawara at non-critical level, making C-translations Q-exact) = E_3. Without conformal vector: stuck at SC^{ch,top}. thm:topologization in en_koszul_duality.tex PROVED for affine KM V_k(g) at non-critical level k != -h^v (Sugawara explicit). For general chiral algebras with conformal vector (Virasoro, W-algebras): CONJECTURAL (conj:topologization-general), depends on constructing the 3d HT BRST complex. Proof is cohomological (Q-cohomology); for class M, chain-level E_3 may fail.

**Five notions of E_1-chiral algebra (2026-04-12):** (A) strict ChirAss-algebra, (B) A_inf in End^{ch}_A, (C) EK quantum vertex algebra, (D) A_inf in E_1-chiral, (E) factorization on Ran^{ord}(X). Each has own derived center. (B)↔(C) via Drinfeld associator on Koszul locus. Warning installed at algebraic_foundations.tex warn:multiple-e1-chiral.

**Three Hochschild theories (2026-04-12):** (i) Topological HH: E_1-algebra input → E_2 output (Deligne). (ii) Chiral HH (ChirHoch): E_inf-chiral input → E_inf output, concentrated {0,1,2} (Theorem H). (iii) Categorical HH: dg category input → E_2 with CY shifted Poisson. NEVER conflate. The geometry determines which Hochschild: curve X → chiral, R → topological, CY category → categorical. Constitutional statement in memory/project_hochschild_constitutional_statement.md.

 succeeded in 51ms:
# Theorem Registry

Auto-generated on 2026-04-13 from the structured claim extractor in `scripts/generate_metadata.py`.

This registry now tracks every `\ClaimStatusProvedHere` block directly from source, so the proved surface cannot silently drift behind the TeX tree.

## Snapshot

| Metric | Value |
|---|---:|
| ProvedHere claims | 2367 |
| Total tagged claims | 3113 |
| Active files in `main.tex` | 85 |
| Total `.tex` files scanned | 104 |

## Status Totals

| Status | Count |
|---|---:|
| `ProvedHere` | 2367 |
 succeeded in 50ms:
\chapter{Concordance with primary literature}
\label{chap:concordance}

\index{concordance|textbf}

This chapter is the constitution of the monograph. When chapters
disagree, this chapter governs. It records the five main theorems
with their precise status and locates every result against the
prior literature: what is proved, what is conditional, what
remains conjectural.

The governing principle is falsification: every claim in these three
volumes is presumed false until it appears below with an explicit
status tag and a proof reference or independent verification trail.
A theorem that contradicts the concordance is not a theorem; it is an
error in the chapter that states it. Omission from this chapter is
itself a verdict: what is not recorded here has not survived scrutiny.
The concordance does not summarize the monograph; the monograph
approximates the concordance.

 succeeded in 50ms:
# Red-Team Summary of Vol1/Vol2 Claim Audit
## Scope
- Formal tagged claims catalogued: **4866**
- Volume I formal claims: **2841**
- Volume II formal claims: **2025**
- Informal/meta claim harvest (README / PROGRAMMES / PROOF_ATLAS / audit notes): **94**
- Every formal claim has an attached attack swarm in the master ledger. The informal harvest is separate because sentence-level slogans are less structurally tagged.

## Status profile
- ProvedHere: **3801**
- Conjectured: **327**
- Heuristic: **35**
- ProvedElsewhere: **513**
- Conditional: **116**
- Open: **74**

## Risk profile
- medium: **1999**
- low: **1913**
- high: **807**
 succeeded in 50ms:
---
name: cross-volume-propagation
description: Use after any mathematical wording, status, label, terminology, or formula change that may recur across Volumes I, II, and III, compute layers, tests, notes, or superseded files. This is the Codex-native equivalent of `/propagate` from `CLAUDE.md`.
---

# Cross-Volume Propagation

This skill exists to stop local truth from coexisting with global drift.

## Sweep

Search all live surfaces that may still advertise the old claim:

- `~/chiral-bar-cobar`
- `~/chiral-bar-cobar-vol2`
- `~/calabi-yau-quantum-groups`
- `compute/`
- `compute/tests/`
- relevant notes, standalones, or superseded files if they still mislead future audits

 succeeded in 50ms:
---
name: deep-beilinson-audit
description: Use when the user asks to audit, falsify, red-team, pressure-test, or verify a theorem, chapter, proof, compute module, or frontier claim in this repository. Not for straightforward local edits that do not require an adversarial audit.
---

# Deep Beilinson Audit

Run this skill when correctness matters more than speed and the task is to challenge a claim, not merely improve its presentation.

## Load first

- `CLAUDE.md`
- `chapters/connections/concordance.tex`
- `metadata/theorem_registry.md`
- `archive/raeeznotes/raeeznotes100/red_team_summary.md`
- the target files
- the directly cited dependencies

For status-heavy work, also load the relevant metadata files.

exec
/bin/zsh -lc "rg -n --hidden -S \"bar.*SC|B\\(A\\).*SC|coalgebra.*SC\" /Users/raeez/chiral-bar-cobar /Users/raeez/chiral-bar-cobar-vol2 /Users/raeez/calabi-yau-quantum-groups" in /Users/raeez/chiral-bar-cobar succeeded in 551ms:
/Users/raeez/chiral-bar-cobar/compute/lib/cy_n4sca_k3_engine.py:470:    r"""Pole structure of the bar r-matrix for the N=4 SCA.
/Users/raeez/chiral-bar-cobar/compute/lib/bar_cohomology_n2sca_explicit_engine.py:1:r"""Explicit bar cohomology H*(B(N=2 SCA)) at weights 0 through 6.
/Users/raeez/chiral-bar-cobar/compute/lib/cy_bar_n4sca_engine.py:1038:    r"""Pole structure of the bar r-matrix for the N=4 SCA.
/Users/raeez/chiral-bar-cobar/compute/audit/beilinson_audit_chiral_koszul_pairs_wave13.md:56:- **AP14 (Koszul != SC formality):** Explicitly enforced in rem:loop-exactness-ordering (L2237-2248) and prop:swiss-cheese-nonformality-by-class (L2250+), which sharply distinguishes A_inf formality of bar cohomology (always for Koszul) from SC formality of A itself (differs by class). **AP14 PASS.**
/Users/raeez/chiral-bar-cobar/compute/audit/new_antipatterns_wave12_campaign.md:22:| AP165 (B(A) not SC-coalgebra) | 16 | AP19 found ongoing conflation language |
/Users/raeez/chiral-bar-cobar/compute/audit/new_antipatterns_wave12_campaign.md:94:39 findings from D11_SC_discipline (9C, 23H). The existing AP165 covers B(A)-is-not-SC, but the campaign found subtler misattributions: SC structure described without specifying it lives on the PAIR (C^bullet_ch(A,A), A); topologization described without conformal vector hypothesis; SC^! described without the (Lie, Ass) structure.
/Users/raeez/chiral-bar-cobar/compute/audit/new_antipatterns_wave12_campaign.md:95:**Rule**: Every sentence mentioning SC^{ch,top} must specify: (a) which object it's attributed to (the derived center PAIR, not B(A)), (b) whether topologization is claimed (requires conformal vector), (c) that SC^! = (Lie, Ass, shuffle-mixed), not self-dual.
/Users/raeez/chiral-bar-cobar/compute/audit/new_theorems_red_audit_2026_04_07.md:193:**Issue**: The proof (lines 2042-2058) correctly distinguishes bar formality from Swiss-cheese formality: "this controls the bar formality, not the Swiss-cheese formality." However, it then claims "For classes G/L/C, the OPE pole structure limits the Swiss-cheese operations to depth 1." This is only true for classes G and L. For class C (betagamma), the Swiss-cheese operations m_k^{SC} for k >= 3 are the collision residues at depth >= 2 — but depth >= 2 collision residues require OPE poles of order >= 3 among generators, which betagamma does not have. So betagamma has m_k^{SC} = 0 for k >= 3 by trivial vanishing (no higher poles), not by bar E_2-collapse. The conclusion is correct but the reasoning conflates two mechanisms.
/Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md:80:The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
/Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md:97:E_infty-chiral-Alg --bar--> SC^{ch,top}-coAlg
/Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md:116:Omega : (conilpotent SC^{ch,top}-coalgebras) ⇄ (SC^{ch,top}-algebras) : B
/Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md:120:The bar functor B is the RIGHT adjoint. Given an SC^{ch,top}-algebra (A, M) (a two-coloured pair: closed A, open M), B(A,M) is the universal conilpotent SC^{ch,top}-coalgebra coaugmented over (A,M). For a single chiral algebra A, promotion A -> (A, A) is required (AP86). The bar complex B(A) of a single algebra is an E_1 chiral coassociative coalgebra over (ChirAss)^!; the SC structure lives on the derived center pair (C^bullet_ch(A,A), A) (AP165). The Quillen equivalence means B and Omega are mutually inverse on the homotopy category.
/Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md:126:The left side is **conilpotent SC^{ch,top}-coalgebras**. These are coalgebras over the Koszul dual cooperad of SC^{ch,top}, which means:
/Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md:168:**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
/Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md:171:E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
/Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md:175:Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
/Users/raeez/chiral-bar-cobar/compute/lib/two_colored_bar_engine.py:21:   The bar construction B_{SC}(C, A) is a two-coloured dg cooperad
/Users/raeez/chiral-bar-cobar/compute/lib/two_colored_bar_engine.py:30:   The bar B_{SC}(C, C) then has:
/Users/raeez/chiral-bar-cobar/compute/lib/two_colored_bar_engine.py:765:    """An element of the two-coloured bar complex B_{SC}(C, A).
/Users/raeez/chiral-bar-cobar/compute/lib/two_colored_bar_engine.py:837:    """The two-coloured bar construction B_{SC}(C, A).
codex
I’ve locked the constitutional surface and the two relevant skills. The grep is broad, so I’m narrowing it to actual AP165/B54-style advertisements of “`B(A)` is an `SC` coalgebra” before I edit; then I’ll patch the live writable files and report any blocked external surfaces separately if the sandbox prevents cross-volume writes.
Plan update
Audit and repair the AP165/B54 claim surface with minimal truthful edits, then verify the residual surface across the three-volume programme.
  → Classify grep hits into actual AP165/B54 claim surfaces versus unrelated operadic SC bar/cobar mentions
  • Edit each writable live file that still advertises B(A) as an SC coalgebra/algebra and restate it as E_1 bar coalgebra with SC on the derived center pair
  • Re-run targeted cross-volume grep and summarize exact fixes plus any blocked non-writable surfaces


thinking
**Classifying hits in repo**

I need to classify hits and decide whether to use a tool like rg to restrict actual phrases in the current repository or all volumes. Perhaps using an explorer agent to search could work, but I know we've already done some searches. I think focused searches for specific phrases around "B(A) is SC" or "bar complex as logarithmic SC algebra" might help. I'd like to find actual files to analyze, maybe by looking for exact variants like "SC^{ch,top}-coalgebra" or "B(A).*SC." It seems like using an explorer agent might be the right way forward.
thinking
**Determining the scope of the task**

I think the prompt needs to be exact, so I'm considering using XML blocks based on the AGENTS prompt architecture. I want to spawn an explorer that classifies only live source files, to avoid audit notes or compute docs unless specified. The user might want me to search all volumes, which could include things like README and CLAUDE documents. I should determine the scope of what needs to be fixed, especially regarding any claims about "B(A) is SC coalgebra." This means correcting false claims while ensuring my edits are minimal and truthful across all relevant documents.
collab spawn_agent(call_CT6SAg3mxZd5NhTfdLsmQJ19, prompt="<task>Classify AP165/B54 claim surfaces across the three repositories `/Users/raeez/chiral-bar-cobar`, `/Users/raeez/chi…")
spawn_agent(call_CT6SAg3mxZd5NhTfdLsmQJ19, prompt="<task>Classify AP165/B54 claim surfaces across the three repositories `/Users/raeez/chiral-bar-cobar`, `/Users/raeez/chi…") pending init:
  agent: 019d87a2-6c85-7b52-aa21-551c51fab3dc
mcp startup: no servers
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && rg -n \"SC\\"'^{ch,top}"' in /Users/raeez/chiral-bar-cobar exited 2 in 51ms:
rg: regex parse error:
    (?:SC\^{ch,top})
            ^
error: repetition quantifier expects a valid decimal
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && rg -n 'SC\\"'^'"\\{ch,top\\}'" in /Users/raeez/chiral-bar-cobar succeeded in 59ms:
compute/tests/test_sc_koszul_dual_cooperad_engine.py:1:r"""Tests for the Koszul dual cooperad of SC^{ch,top}.
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && rg -n \"bar complex.*SC\\"'^'"\\{ch,top\\}\"" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
Total output lines: 609

healing_20260413_132214/H20_remove_associator_dep.md:123:**Bar complex is E_1-coassociative; SC^{ch,top} emerges on the derived center (CRITICAL, corrected 2026-04-12):** The bar complex B^{ord}(A) = T^c(s^{-1}A-bar) is an E_1-chiral coassociative COALGEBRA (over ChirAss^!). It has a differential + deconcatenation coproduct. It does NOT carry SC^{ch,top} structure. The SC^{ch,top} structure (or E_3 with conformal vector) emerges on the DERIVED CENTER Z^{der}_{ch}(A) = ChirHoch*(A,A), computed USING the bar complex as a resolution. The bar complex is the E_1 engine; the derived center is the SC^{ch,top}/E_3 output. thm:bar-swiss-cheese (claiming B(A) is an SC-coalgebra) needs retraction or careful restatement — see FOUND-11. princ:sc-two-incarnations in en_koszul_duality.tex states this correctly.
healing_20260413_132214/H20_remove_associator_dep.md:129:**The E_n operadic circle (2026-04-12):** E_3(bulk) → E_2(boundary chiral) → E_1(bar/QG) → E_2(Drinfeld center) → E_3(derived center). Each arrow is: restriction to codim-2 defect, ordered bar complex, categorified averaging (Drinfeld center), higher Deligne (derived center). The circle closes for 3d HT theories with conformal vector; without conformal vector, stuck at SC^{ch,top} (the intermediary between E_1-chiral and E_3).
fix_wave_C_20260413_175943/C05_cauchy_normalization.md:121:**Bar complex is E_1-coassociative; SC^{ch,top} emerges on the derived center (CRITICAL, corrected 2026-04-12):** The bar complex B^{ord}(A) = T^c(s^{-1}A-bar) is an E_1-chiral coassociative COALGEBRA (over ChirAss^!). It has a differential + deconcatenation coproduct. It does NOT carry SC^{ch,top} structure. The SC^{ch,top} structure (or E_3 with conformal vector) emerges on the DERIVED CENTER Z^{der}_{ch}(A) = ChirHoch*(A,A), computed USING the bar complex as a resolution. The bar complex is the E_1 engine; the derived center is the SC^{ch,top}/E_3 output. thm:bar-swiss-cheese (claiming B(A) is an SC-coalgebra) needs retraction or careful restatement — see FOUND-11. princ:sc-two-incarnations in en_koszul_duality.tex states this correctly.
fix_wave_C_20260413_175943/C05_cauchy_normalization.md:127:**The E_n operadic circle (2026-04-12):** E_3(bulk) → E_2(boundary chiral) → E_1(bar/QG) → E_2(Drinfeld center) → E_3(derived center). Each arrow is: restriction to codim-2 defect, ordered bar complex, categorified averaging (Drinfeld center), higher Deligne (derived center). The circle closes for 3d HT theories with conformal vector; without conformal vector, stuck at SC^{ch,top} (the intermediary between E_1-chiral and E_3).
AGENTS.md:53:**Bar complex is E_1-coassociative; SC^{ch,top} emerges on the derived center (CRITICAL, corrected 2026-04-12).** The bar complex B^{ord}(A) = T^c(s^{-1}A-bar) is an E_1-chiral coassociative COALGEBRA (over ChirAss^!). It has a differential + deconcatenation coproduct. It does NOT carry SC^{ch,top} structure. The SC^{ch,top} structure (or E_3 with conformal vector) emerges on the DERIVED CENTER Z^{der}_{ch}(A) = ChirHoch*(A,A), computed USING the bar complex as a resolution. The bar complex is the E_1 engine; the derived center is the SC^{ch,top}/E_3 output. thm:bar-swiss-cheese (claiming B(A) is an SC-coalgebra) needs retraction or careful restatement — see FOUND-11.
AGENTS.md:55:**The E_n operadic circle (2026-04-12):** E_3(bulk) → E_2(boundary chiral) → E_1(bar/QG) → E_2(Drinfeld center) → E_3(derived center). Each arrow: restriction to codim-2 defect, ordered bar complex, categorified averaging (Drinfeld center), higher Deligne (derived center). Closes for 3d HT with conformal vector; without conformal vector, stuck at SC^{ch,top}.
audit_campaign_20260412_231034/XV11_claim_status_sync.md:174:**Bar complex is E_1-coassociative; SC^{ch,top} emerges on the derived center (CRITICAL, corrected 2026-04-12):** The bar complex B^{ord}(A) = T^c(s^{-1}A-bar) is an E_1-chiral coassociative COALGEBRA (over ChirAss^!). It has a differential + deconcatenation coproduct. It does NOT carry SC^{ch,top} structure. The SC^{ch,top} structure (or E_3 with conformal vector) emerges on the DERIVED CENTER Z^{der}_{ch}(A) = ChirHoch*(A,A), computed USING the bar complex as a resolution. The bar complex is the E_1 engine; the derived center is the SC^{ch,top}/E_3 output. thm:bar-swiss-cheese (claiming B(A) is an SC-coalgebra) needs retraction or careful restatement — see FOUND-11. princ:sc-two-incarnations in en_koszul_duality.tex states this correctly.
audit_campaign_20260412_231034/XV11_claim_status_sync.md:180:**The E_n operadic circle (2026-04-12):** E_3(bulk) → E_2(boundary chiral) → E_1(bar/QG) → E_2(Drinfeld center) → E_3(derived center). Each arrow is: restriction to codim-2 defect, ordered bar complex, categorified averaging (Drinfeld center), higher Deligne (derived center). The circle closes for 3d HT theories with conformal vector; without conformal vector, stuck at SC^{ch,top} (the intermediary between E_1-chiral and E_3).
healing_20260413_132214/H06_MC2_alt_proof.md:137:**Bar complex is E_1-coassociative; SC^{ch,top} emerges on the derived center (CRITICAL, corrected 2026-04-12):** The bar complex B^{ord}(A) = T^c(s^{-1}A-bar) is an E_1-chiral coassociative COALGEBRA (over ChirAss^!). It has a differential + deconcatenation coproduct. It does NOT carry SC^{ch,top} structure. The SC^{ch,top} structure (or E_3 with conformal vector) emerges on the DERIVED CENTER Z^{der}_{ch}(A) = ChirHoch*(A,A), computed USING the bar complex as a resolution. The bar complex is the E_1 engine; the derived center is the SC^{ch,top}/E_3 output. thm:bar-swiss-cheese (claiming B(A) is an SC-coalgebra) needs retraction or careful restatement — see FOUND-11. princ:sc-two-incarnations in en_koszul_duality.tex states this correctly.
healing_20260413_132214/H06_MC2_alt_proof.md:143:**The E_n operadic circle (2026-04-12):** E_3(bulk) → E_2(boundary chiral) → E_1(bar/QG) → E_2(Drinfeld center) → E_3(derived center). Each arrow is: restriction to codim-2 defect, ordered bar complex, categorified averaging (Drinfeld center), higher Deligne (derived center). The circle closes for 3d HT theories with conformal vector; without conformal vector, stuck at SC^{ch,top} (the intermediary between E_1-chiral and E_3).
fix_wave_C_20260413_175943/C04_arnold_kz_fix.md:73:**Bar complex is E_1-coassociative; SC^{ch,top} emerges on the derived center (CRITICAL, corrected 2026-04-12):** The bar complex B^{ord}(A) = T^c(s^{-1}A-bar) is an E_1-chiral coassociative COALGEBRA (over ChirAss^!). It has a differential + deconcatenation coproduct. It does NOT carry SC^{ch,top} structure. The SC^{ch,top} structure (or E_3 with conformal vector) emerges on the DERIVED CENTER Z^{der}_{ch}(A) = ChirHoch*(A,A), computed USING the bar complex as a resolution. The bar complex is the E_1 engine; the derived center is the SC^{ch,top}/E_3 output. thm:bar-swiss-cheese (claiming B(A) is an SC-coalgebra) needs retraction or careful restatement — see FOUND-11. princ:sc-two-incarnations in en_koszul_duality.tex states this correctly.
fix_wave_C_20260413_175943/C04_arnold_kz_fix.md:79:**The E_n operadic circle (2026-04-12):** E_3(bulk) → E_2(boundary chiral) → E_1(bar/QG) → E_2(Drinfeld center) → E_3(derived center). Each arrow is: restriction to codim-2 defect, ordered bar complex, categorified averaging (Drinfeld center), higher Deligne (derived center). The circle closes for 3d HT theories with conformal vector; without conformal vector, stuck at SC^{ch,top} (the intermediary between E_1-chiral and E_3).
audit_campaign_20260412_231034/AP23_pi3_BU.md:128:**Bar complex is E_1-coassociative; SC^{ch,top} emerges on the derived center (CRITICAL, corrected 2026-04-12):** The bar complex B^{ord}(A) = T^c(s^{-1}A-bar) is an E_1-chiral coassociative COALGEBRA (over ChirAss^!). It has a differential + deconcatenation coproduct. It does NOT carry SC^{ch,top} structure. The SC^{ch,top} structure (or E_3 with conformal vector) emerges on the DERIVED CENTER Z^{der}_{ch}(A) = ChirHoch*(A,A), computed USING the bar complex as a resolution. The bar complex is the E_1 engine; the derived center is the SC^{ch,top}/E_3 output. thm:bar-swiss-cheese (claiming B(A) is an SC-coalgebra) needs retraction or careful restatement — see FOUND-11. princ:sc-two-incarnations in en_koszul_duality.tex states this correctly.
audit_campaign_20260412_231034/AP23_pi3_BU.md:134:**The E_n operadic circle (2026-04-12):** E_3(bulk) → E_2(boundary chiral) → E_1(bar/QG) → E_2(Drinfeld center) → E_3(derived center). Each arrow is: restriction to codim-2 defect, ordered bar complex, categorified averaging (Drinfeld center), higher Deligne (derived center). The circle closes for 3d HT theories with conformal vector; without conformal vector, stuck at SC^{ch,top} (the intermediary between E_1-chiral and E_3).
healing_20260413_132214/H05_thm_H_alt_proof.md:131:**Bar complex is E_1-coassociative; SC^{ch,top} emerges on the derived center (CRITICAL, corrected 2026-04-12):** The bar complex B^{ord}(A) = T^c(s^{-1}A-bar) is an E_1-chiral coassociative COALGEBRA (over ChirAss^!). It has a differential + deconcatenation coproduct. It does NOT carry SC^{ch,top} structure. The SC^{ch,top} structure (or E_3 with conformal vector) emerges on the DERIVED CENTER Z^{der}_{ch}(A) = ChirHoch*(A,A), computed USING the bar complex as a resolution. The bar complex is the E_1 engine; the derived center is the SC^{ch,top}/E_3 output. thm:bar-swiss-cheese (claiming B(A) is an SC-coalgebra) needs retraction or careful restatement — see FOUND-11. princ:sc-two-incarnations in en_koszul_duality.tex states this correctly.
healing_20260413_132214/H05_thm_H_alt_proof.md:137:**The E_n operadic circle (2026-04-12):** E_3(bulk) → E_2(boundary chiral) → E_1(bar/QG) → E_2(Drinfeld center) → E_3(derived center). Each arrow is: restriction to codim-2 defect, ordered bar complex, categorified averaging (Drinfeld center), higher Deligne (derived center). The circle closes for 3d HT theories with conformal vector; without conformal vector, stuck at SC^{ch,top} (the intermediary between E_1-chiral and E_3).
healing_20260413_132214/H05_thm_H_alt_proof.md: WARNING: stopped searching binary file after match (found "\0" byte around offset 176848)
rectification_20260412_233715/R13_introduction.md:100:**Bar complex is E_1-coassociative; SC^{ch,top} emerges on the derived center (CRITICAL, corrected 2026-04-12):** The bar complex B^{ord}(A) = T^c(s^{-1}A-bar) is an E_1-chiral coassociative COALGEBRA (over ChirAss^!). It has a differential + deconcatenation coproduct. It does NOT carry SC^{ch,top} structure. The SC^{ch,top} structure (or E_3 with conformal vector) emerges on the DERIVED CENTER Z^{der}_{ch}(A) = ChirHoch*(A,A), computed USING the bar complex as a resolution. The bar complex is the E_1 engine; the derived center is the SC^{ch,top}/E_3 output. thm:bar-swiss-cheese (claiming B(A) is an SC-coalgebra) needs retraction or careful restatement — see FOUND-11. princ:sc-two-incarnations in en_koszul_duality.tex states this correctly.

exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && rg -n \"bar complex.*SC\\\\"'^'"\\\\{ch,top\\\\}-algebra\"" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
compute/lib/sc_bar_cobar_inversion_engine.py:664:    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
thinking
**Searching in the repository**

While the agent is working, I can focus on searching in the current repository. I probably only need a few live files, so I should use "rg" while excluding audit and notes. The user asked for all volumes, so I might include computation documents too. I'll search the repo with targeted patterns and list candidate files. Excluding compute and audit directories might work, but I should check compute/lib for code docstrings to fix if needed. Don't forget to include the markdown docs!
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '620,710p' compute/lib/sc_bar_cobar_inversion_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
    with H^0 = C (if k != 0: trivial; the resolution is acyclic in positive degrees)
    and H^1 = 0 (d_1 is injective when k != 0).

    The counit epsilon: Omega(B(H_k)) -> H_k at arity 2:
      epsilon(s(alpha* x alpha*)) = alpha (the generator J)
    which is a quasi-isomorphism because both sides have H = C at degree 0.

    Contracting homotopy h:
      h: C_0 -> C_1 defined by h(1) = (1/k) * s(alpha* x alpha*)
      This satisfies: d_1 . h = id on C_0 (when k != 0).
      And h . d_1 + projection = id on C_1.
    """
    if k is None:
        k = Symbol('k')

    res = BarCobarResolution(arity=2)

    # Chain groups
    res.chain_groups = {0: 1, 1: 1}  # C_0 = C, C_1 = C

codex
I’ve moved from the coarse grep to a claim-by-claim pass. The main distinction is between false advertisements about the algebra bar complex `B(A)` and legitimate mentions of the operadic bar/cobar construction of `SC` itself; only the former get edited.
exec
/bin/zsh -lc "rg -n -C 2 --hidden -g '"'!compute/audit/**'"' -g '"'!archive/**'"' -g '"'!raeeznotes/**'"' -g '"'!*.pyc'"' -e 'B\\(A\\).*SC' -e 'bar complex.*SC' -e 'coalgebra over .*SC' -e 'SC\\"'^'"\\{ch,top\\}-coalgebra' -e 'SC\\\\chtop-?coalgebra' /Users/raeez/chiral-bar-cobar" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "rg -n -C 2 --hidden -g '"'!compute/audit/**'"' -g '"'!archive/**'"' -g '"'!raeeznotes/**'"' -g '"'!*.pyc'"' -e 'B\\(A\\).*SC' -e 'bar complex.*SC' -e 'coalgebra over .*SC' -e 'SC\\"'^'"\\{ch,top\\}-coalgebra' -e 'SC\\\\chtop-?coalgebra' /Users/raeez/chiral-bar-cobar-vol2 /Users/raeez/calabi-yau-quantum-groups" in /Users/raeez/chiral-bar-cobar succeeded in 258ms:
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-5-## Identity
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-6-
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md:7:The bar complex B(A) is an E_1-chiral coassociative coalgebra over (ChirAss)^!. The SC^{ch,top} structure emerges on the derived chiral center Z^{der}_{ch}(A) = C^bullet_{ch}(A,A): the pair (C^bullet_{ch}(A,A), A) is the SC datum where bulk acts on boundary. The five Vol I theorems are the modular invariants surviving Sigma_n-coinvariance. Physics IS the homotopy type.
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-8-
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-9-~1,700pp, this repo. Seven parts: I(The Open Primitive) II(The E_1 Core) III(Seven Faces of r(z)) IV(Characteristic Datum and Modularity) V(Standard HT Landscape) VI(Three-Dimensional Quantum Gravity = CLIMAX) VII(Frontier).
--
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-158-## Vol II-Specific Pitfalls
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-159-
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md:160:- B(A) is NOT an SC^{ch,top}-coalgebra (AP165). B(A) is an E_1 chiral coassociative coalgebra. SC^{ch,top} emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). See Vol I CLAUDE.md AP165/B54-B56.
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-161-- WHICH Hochschild cochains: ALWAYS chiral Hochschild cochains C^bullet_{ch}(A,A), defined via the chiral endomorphism operad End^{ch}_A with spectral parameters from FM_k(C). NEVER topological HH = RHom_{A^e}(A,A) (which gives E_2 center from Deligne conjecture, not the chiral bulk).
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-162-- SC directionality: Open-to-closed is EMPTY. Bulk -> boundary only.
--
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-275-FM51: "Emergent third dimension" from bar degree. Claimed bar degree provides a "transverse E_1 direction." WRONG. Bar degree is a grading on a chain complex. An E_1 structure requires operations, coherences, A_inf. A grading provides none of this. Counter: a grading is not an operadic structure.
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-276-
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md:277:FM52: Within-surface SC = holographic bulk-boundary. Claimed the half-plane bar complex "is where the bulk-boundary correspondence should be made precise." WRONG. The within-surface SC (R ⊂ C) governs restriction to a real locus. The holographic bulk-boundary goes through the derived center (circle model / Hochschild). Counter: SC governs within-surface structure; holography goes through Hochschild/derived center.
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-278-
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md-279-FM53: Two "independent" E_1 structures. Claimed within-surface E_1 and transverse E_1 are independent. WRONG. They are Koszul dual through the Hom functor: C*(A,A) = Hom(B(A), A). The A_inf-coalgebra on B(A) and the A_inf-algebra on C*(A,A) determine each other. Counter: Koszul dual, not independent.
--
/Users/raeez/calabi-yau-quantum-groups/AGENTS.md-163-**The primitive object** is B^ord(A) = T^c(s^{-1}A-bar): ordered bar, deconcatenation coproduct, R-matrix, Yangian. The symmetric bar B^Sigma is the Sigma_n-coinvariant shadow. Physics IS the homotopy type: A-infinity = scattering, SC^{ch,top} governs the (bulk, boundary) pair, modular L-infinity = genus tower. The five theorems A-D+H are the invariants that survive averaging.
/Users/raeez/calabi-yau-quantum-groups/AGENTS.md-164-
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && rg -n \"bar complex.*SC\\\\"'^'"\\\\{ch,top\\\\}-coalgebra\"" in /Users/raeez/chiral-bar-cobar succeeded in 181ms:
compute/audit/forgetful_e_infty_to_e1_report.md:80:The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
compute/audit/forgetful_e_infty_to_e1_report.md:175:Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
 succeeded in 644ms:
--
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py:1:r"""Bridge between M24 Mathieu moonshine and the bar complex of the N=4 SCA at c=6.
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-2-
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-3-MATHEMATICAL FRAMEWORK
--
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-6-QUESTION: Do the Mathieu M24 moonshine dimensions
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-7-    A_n = 45, 231, 770, 2277, 5796
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py:8:appear in the bar complex B(A_{N=4}) of the small N=4 SCA at c=6?
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-9-
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-10-The K3 elliptic genus decomposes under the N=4 SCA:
--
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-19-is TWICE the unique weak Jacobi form of weight 0, index 1.
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-20-
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py:21:The bar complex B(A) of the N=4 SCA has:
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-22-    B^1 = span{s^{-1}a : a generator} has dim 8 (the 8 generators).
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-23-    B^2 = span{s^{-1}a tensor s^{-1}b} has dim 64 (ordered pairs).
--
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-80-  Cheng, arXiv:1005.5415 (2010)
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py-81-  Gannon, arXiv:1211.7074 (2016)
/Users/raeez/chiral-bar-cobar/compute/lib/cy_m24_bar_bridge_engine.py:82:  cy_bar_n4sca_engine.py: bar complex of N=4 SCA
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '60,100p' compute/audit/forgetful_e_infty_to_e1_report.md" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
The E_1 coproduct does not arise from forgetting E_infty structure. It arises from a SECOND, INDEPENDENT geometric source: the interval-cutting structure on Conf_k(R).

#### Q2: Wouldn't the E_infty bar B_{E_infty}(A) be smaller and better-behaved?

There are in fact THREE bar complexes (Vol II CLAUDE.md, AP37):

1. **B^{FG}(A)** (Francis-Gaitsgory bar): uses ONLY the zeroth product a_{(0)}b (chiral Lie bracket). This is the E_infty bar of A viewed as a chiral Lie algebra. It is SMALLER but LOSES higher-pole data (misses kappa(A)).

2. **B^{Sigma}(A)** (full symmetric bar): uses ALL OPE products, takes Sigma_n-coinvariants. This is Vol I's Theorem A bar complex. The coproduct here is the subset-partition coproduct — cocommutative.

3. **B^{ord}(A)** (ordered bar): uses ALL OPE products, retains linear ordering — no Sigma_n quotient. The coproduct is deconcatenation — coassociative.

The E_infty bar (in the sense of B^{Sigma}) exists and is the subject of Vol I. It carries the cocommutative factorization coalgebra structure. But the Swiss-cheese structure requires BOTH the symmetric bar (closed colour) AND the ordered bar (open colour) simultaneously. Neither alone suffices.

#### Q3: The SC^{ch,top} structure REQUIRES the E_1 bar?

**Yes, this is correct.** The Swiss-cheese operad SC^{ch,top} has two colours:
- **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
- **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta

exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && rg -n \"B\\(A\\).*SC\\\\"'^'"\\\\{ch,top\\\\}\"" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
fix_wave_B_20260413_171623/B14_stale_cross_vol_refs_v2.md:1186:- **Theorem A (bar‑cobar duality)** — Status: `ClaimStatusProvedHere` on the quadratic/Koszul locus with Verdier compatibility spelled out by `\label{thm:bar-cobar-isomorphism-main}`. Source: `chapters/theory/chiral_koszul_pairs.tex:4014`. Vol II may cite this as the E₁ bar/cobar adjunction that stays within the Koszul regime and feeds the derived center; it must not repackage `B(A)` itself as an SC^{ch,top} coalgebra or claim the adjunction holds outside the quadratic/Verdier-compatible hypotheses even when discussing derived-center bulk structures.
scripts/adversarial_wave2.py:350:    ("B19_notation_sync", "Check key notation across volumes: B(A), A^!, Z^der, kappa, Theta_A, r(z), SC^{ch,top}. Any notation drift between volumes?"),
healing_20260413_130533/H11_SC_formal_alt.md:403:CLAUDE.md:375:- B54. `B(A)` is a coalgebra over `SC^{ch,top}` / "the bar complex presents the Swiss-cheese algebra." FALSE. B(A) is an E_1 coassociative coalgebra. SC^{ch,top} emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). AP165. Regex: `bar.*presents.*Swiss|coalgebra over.*\\SCchtop|\\SCchtop.*coalgebra`
healing_20260413_130533/H11_SC_formal_alt.md:410:CLAUDE.md:881:AP165: B(A) is NOT an SC^{ch,top}-coalgebra. The bar complex B(A) = T^c(s^{-1} A-bar) is an E_1 chiral coassociative coalgebra (differential from chiral product, deconcatenation coproduct). It is a SINGLE E_1 coalgebra, not a two-colored SC datum. The SC^{ch,top} structure emerges in the chiral derived center: the chiral Hochschild cochain complex C^bullet_{ch}(A,A) (defined via the chiral endomorphism operad End^{ch}_A with spectral parameters from FM_k(C), NOT topological Hochschild cochains RHom_{A^e}(A,A)) carries brace operations and a Gerstenhaber bracket, and the pair (C^bullet_{ch}(A,A), A) is the SC^{ch,top} datum (bulk acting on boundary). FORBIDDEN claims: "B(A) is a coalgebra over SC^{ch,top}"; "the bar complex presents the Swiss-cheese algebra"; "the bar differential is the closed color"; "the bar coproduct is the open color." COUNTER: after writing any sentence involving B(A) and SC^{ch,top} in the same paragraph, verify that SC is attributed to the derived center pair, not to B(A).
healing_20260413_130533/H11_SC_formal_alt.md:754:**SC^{ch,top} is NOT on B(A) (AP165).** B(A) is E_1 coassociative coalgebra. SC^{ch,top} lives on the pair (C^bullet_{ch}(A,A), A). FORBIDDEN: "B(A) is a coalgebra over SC^{ch,top}"; "the bar differential is the closed color"; "the bar coproduct is the open color."
fix_wave_B_20260413_171623/B19_readme_update_v2.md:69:The bar complex B(A) is an E_1 chiral coassociative coalgebra: the differential encodes the chiral product (holomorphic, from FM_k(C)), the deconcatenation coproduct encodes topological factorization on R. The SC^{ch,top} structure emerges in the chiral derived center: the chiral Hochschild cochain complex C^bullet_{ch}(A,A) carries brace operations and a Gerstenhaber bracket, and the pair (C^bullet_{ch}(A,A), A) is the SC^{ch,top} datum (bulk acting on boundary). The five Vol I theorems are the modular invariants that survive Sigma_n-coinvariance.
CLAUDE.md:375:- B54. `B(A)` is a coalgebra over `SC^{ch,top}` / "the bar complex presents the Swiss-cheese algebra." FALSE. B(A) is an E_1 coassociative coalgebra. SC^{ch,top} emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). AP165. Regex: `bar.*presents.*Swiss|coalgebra over.*\\SCchtop|\\SCchtop.*coalgebra`
CLAUDE.md:467:**FM25. The SC^{ch,top} disaster of 2025-2026 (Opus 4.6 structural confabulation).** Opus constructed an elaborate but FALSE framework claiming B(A) is an SC^{ch,top}-coalgebra. The chain of errors:
CLAUDE.md:481:Counter: NEVER write B(A) and SC^{ch,top} in the same sentence attributing SC to B(A). The SC structure lives on (C^bullet_ch(A,A), A). The bar complex is over (ChirAss)^!, single-colored, E_1-chiral-coassociative.
CLAUDE.md:919:AP165: B(A) is NOT an SC^{ch,top}-coalgebra. The bar complex B(A) = T^c(s^{-1} A-bar) is an E_1 chiral coassociative coalgebra (differential from chiral product, deconcatenation coproduct). It is a SINGLE E_1 coalgebra, not a two-colored SC datum. The SC^{ch,top} structure emerges in the chiral derived center: the chiral Hochschild cochain complex C^bullet_{ch}(A,A) (defined via the chiral endomorphism operad End^{ch}_A with spectral parameters from FM_k(C), NOT topological Hochschild cochains RHom_{A^e}(A,A)) carries brace operations and a Gerstenhaber bracket, and the pair (C^bullet_{ch}(A,A), A) is the SC^{ch,top} datum (bulk acting on boundary). FORBIDDEN claims: "B(A) is a coalgebra over SC^{ch,top}"; "the bar complex presents the Swiss-cheese algebra"; "the bar differential is the closed color"; "the bar coproduct is the open color." COUNTER: after writing any sentence involving B(A) and SC^{ch,top} in the same paragraph, verify that SC is attributed to the derived center pair, not to B(A).
audit_campaign_20260412_231034/T20_gerstenhaber.md:126:CLAUDE.md:375:- B54. `B(A)` is a coalgebra over `SC^{ch,top}` / "the bar complex presents the Swiss-cheese algebra." FALSE. B(A) is an E_1 coassociative coalgebra. SC^{ch,top} emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). AP165. Regex: `bar.*presents.*Swiss|coalgebra over.*\\SCchtop|\\SCchtop.*coalgebra`
platonic_rectification_20260413_114523/P07_thm_H_upgrade.md:2110:relaunch_20260413_111534/XV08_thm_status_xvol.md:626:The bar complex B(A) is an E_1 chiral coassociative coalgebra: the differential encodes the chiral product (holomorphic, from FM_k(C)), the deconcatenation coproduct encodes topological factorization on R. The SC^{ch,top} structure emerges in the chiral derived center: the chiral Hochschild cochain complex C^bullet_{ch}(A,A) carries brace operations and a Gerstenhaber bracket, and the pair (C^bullet_{ch}(A,A), A) is the SC^{ch,top} datum (bulk acting on boundary). The five Vol I theorems are the modular invariants that survive Sigma_n-coinvariance.
platonic_rectification_20260413_114523/P07_thm_H_upgrade.md: WARNING: stopped searching binary file after match (found "\0" byte around offset 788020)
healing_20260413_130533/H20_remove_associator_dep.md:206:CLAUDE.md:375:- B54. `B(A)` is a coalgebra over `SC^{ch,top}` / "the bar complex presents the Swiss-cheese algebra." FALSE. B(A) is an E_1 coassociative coalgebra. SC^{ch,top} emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). AP165. Regex: `bar.*presents.*Swiss|coalgebra over.*\\SCchtop|\\SCchtop.*coalgebra`
audit_campaign_20260412_231034/AP04_bare_A_bar.md:212:CLAUDE.md:845:AP165: B(A) is NOT an SC^{ch,top}-coalgebra. The bar complex B(A) = T^c(s^{-1} A-bar) is an E_1 chiral coassociative coalgebra (differential from chiral product, deconcatenation coproduct). It is a SINGLE E_1 coalgebra, not a two-colored SC datum. The SC^{ch,top} structure emerges in the chiral derived center: the chiral Hochschild cochain complex C^bullet_{ch}(A,A) (defined via the chiral endomorphism operad End^{ch}_A with spectral parameters from FM_k(C), NOT topological Hochschild cochains RHom_{A^e}(A,A)) carries brace operations and a Gerstenhaber bracket, and the pair (C^bullet_{ch}(A,A), A) is the SC^{ch,top} datum (bulk acting on boundary). FORBIDDEN claims: "B(A) is a coalgebra over SC^{ch,top}"; "the bar complex presents the Swiss-cheese algebra"; "the bar differential is the closed color"; "the bar coproduct is the open color." COUNTER: after writing any sentence involving B(A) and SC^{ch,top} in the same paragraph, verify that SC is attributed to the derived center pair, not to B(A).
audit_campaign_20260412_231034/AP20_over_point_P1.md:110:CLAUDE.md:375:- B54. `B(A)` is a coalgebra over `SC^{ch,top}` / "the bar complex presents the Swiss-cheese algebra." FALSE. B(A) is an E_1 coassociative coalgebra. SC^{ch,top} emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). AP165. Regex: `bar.*presents.*Swiss|coalgebra over.*\\SCchtop|\\SCchtop.*coalgebra`
audit_campaign_20260412_231034/XV04_SC_claims_xvol.md:137:375:- B54. `B(A)` is a coalgebra over `SC^{ch,top}` / "the bar complex presents the Swiss-cheese algebra." FALSE. B(A) is an E_1 coassociative coalgebra. SC^{ch,top} emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). AP165. Regex: `bar.*presents.*Swiss|coalgebra over.*\\SCchtop|\\SCchtop.*coalgebra`
audit_campaign_20260412_231034/XV04_SC_claims_xvol.md:142:448:**FM25. The SC^{ch,top} disaster of 2025-2026 (Opus 4.6 structural confabulation).** Opus constructed an elaborate but FALSE framework claiming B(A) is an SC^{ch,top}-coalgebra. The chain of errors:
healing_20260413_132214/H15_remove_chain_level_topol.md:553:  objects: Z^{der}_{ch}(A) as closed-sector center; not B(A); SC^{ch,top} on the pair, E_3 only after topologization
AGENTS.md:140:**SC^{ch,top} is NOT on B(A) (AP165).** B(A) is E_1 coassociative coalgebra. SC^{ch,top} lives on the pair (C^bullet_{ch}(A,A), A). FORBIDDEN: "B(A) is a coalgebra over SC^{ch,top}"; "the bar differential is the closed color"; "the bar coproduct is the open color."


exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && rg -n \"B\\(A\\) is an SC\\\\"'^'"\\\\{ch,top\\\\}\"" in /Users/raeez/chiral-bar-cobar succeeded in 50ms:
CLAUDE.md:467:**FM25. The SC^{ch,top} disaster of 2025-2026 (Opus 4.6 structural confabulation).** Opus constructed an elaborate but FALSE framework claiming B(A) is an SC^{ch,top}-coalgebra. The chain of errors:
relaunch_20260413_111534/R19_coderived_models.md:104:**FM25. The SC^{ch,top} disaster of 2025-2026 (Opus 4.6 structural confabulation).** Opus constructed an elaborate but FALSE framework claiming B(A) is an SC^{ch,top}-coalgebra. The chain of errors:
audit_campaign_20260412_231034/T14_topologization.md:117:CLAUDE.md:448:**FM25. The SC^{ch,top} disaster of 2025-2026 (Opus 4.6 structural confabulation).** Opus constructed an elaborate but FALSE framework claiming B(A) is an SC^{ch,top}-coalgebra. The chain of errors:
audit_campaign_20260412_231034/XV04_SC_claims_xvol.md:142:448:**FM25. The SC^{ch,top} disaster of 2025-2026 (Opus 4.6 structural confabulation).** Opus constructed an elaborate but FALSE framework claiming B(A) is an SC^{ch,top}-coalgebra. The chain of errors:
thinking
**Evaluating file status**

I need to filter out live or writable files in the current repository, especially since the search results still include artifact directories like healing and fix_wave. The user mentioned all volumes, but those notes could mislead future audits. However, since the user didn’t specify to exclude notes, I should focus on making minimum truthful edits. I might need to fix live files, especially if the artifacts cause confusion during audits. It’s tricky, but prioritizing the actual false claims in the repo seems essential.
thinking
**Analyzing candidate files for false claims**

I’m reviewing candidate files to identify false claims.  

For instance, compute/lib/two_colored_bar_engine.py suggests that B^ch(A) acts as an SC coalgebra, which might be misleading. Similarly, compute/lib/en_bar_coproduct_engine.py's line on chiral bar complex appears to conflate terms. There are other files where bar complex references could also be incorrect, necessitating a careful inspection of context. In addition, I see potential false assumptions in test files and audit documents, though I might consider leaving audit outputs alone. I need to also find specific labels in my searches for accuracy.
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '150,220p' compute/tests/test_heisenberg_bridge.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
        """B^1_h = V-bar_h = 1 for all h >= 1 (one generator of each weight)."""
        for h in range(1, 10):
            assert heisenberg_bar_chain_dim(1, h) == 1

    def test_bar_degree_2(self):
        """dim B^2_h = h-1 (compositions of h into 2 positive parts, times 1!)."""
        for h in range(2, 10):
            assert heisenberg_bar_chain_dim(2, h) == h - 1

    def test_euler_characteristic(self):
        """χ(B_h) = Σ (-1)^n dim B^n_h.

        At each weight h, the Euler characteristic of the bar complex
        is the alternating sum of chain dimensions. For a Koszul algebra,
        χ(B_h) = (-1)^h dim H^h = (-1)^h p(h).

        This is one half of the Koszulness verification.
        """
        for h in range(1, 8):
            chi = sum(
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && rg -n \"presents the SC\\"'^'"\\{ch,top\\}-coalgebra\"" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
compute/audit/forgetful_e_infty_to_e1_report.md:80:The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
codex
The remaining current-repo risk is concentrated in compute-facing files, not the constitutional text. I’m reading those exact passages now because some hits are legitimate operadic `SC` bar/cobar references, while others still conflate the algebra bar complex with an `SC` coalgebra and need patching.
exec
/bin/zsh -lc "sed -n '1,220p' compute/lib/two_colored_bar_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1060,1125p' compute/lib/two_colored_bar_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "rg -n \"thm:bar-swiss-cheese|bar-swiss-cheese|SC coalgebra|SC\\\\"'^'"\\{ch,top\\}-coalgebra|B\\"'^ch'"\\(A\\)|B_\\{SC\\}|B\\(A\\) is\" chapters compute/lib compute/tests" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,220p' compute/lib/curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '640,730p' compute/lib/sc_bar_cobar_inversion_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,160p' compute/lib/en_bar_coproduct_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,220p' compute/tests/test_curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
                )
                for cw in closed_words:
                    for ow in open_words:
                        w = TwoColoredBarWord('o', cw.indices, ow.indices)
                        d_w = self.full_differential(w)
                        d2: Dict[TwoColoredBarWord, Rational] = {}
                        for w2, c2 in d_w.items():
                            d_w2 = self.full_differential(w2)
                            for w3, c3 in d_w2.items():
                                old = d2.get(w3, Rational(0))
                                d2[w3] = old + c2 * c3
                        d2 = {k: v for k, v in d2.items() if v != 0}
                        if d2:
                            return False
        return True


# =========================================================================
#  PART 7: The chiral bar as SC-coalgebra (conceptual bridge)
# =========================================================================
 succeeded in 52ms:
"""Two-coloured operadic bar construction for the Swiss-cheese operad.

THE FUNDAMENTAL QUESTION: For a two-coloured operad O with colours
{c, o} (closed, open), what is the bar construction B_O of an
O-algebra (V_c, V_o)?

ANSWER (worked out from first principles in this module):

1. CLASSICAL ONE-COLOURED BAR:
   - B_{Ass}(A) = (T^c(s^{-1} A_bar), d_bar) is a coassociative dg
     coalgebra. Here T^c is the cofree tensor coalgebra, s^{-1} is
     desuspension, A_bar = ker(augmentation).
   - B_{Com}(A) = (Sym^c(s^{-1} A_bar), d_bar) is a cocommutative
     (= coLie) dg coalgebra.

2. TWO-COLOURED BAR (Swiss-cheese):
   An SC-algebra is a pair (C, A) where:
     - C is a Com-algebra (closed colour)
     - A is an Ass-algebra (open colour)
     - C acts on A via a Com-module structure (closed-to-open maps)
 succeeded in 50ms:
r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.

FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?

SEVEN VERIFIED CLAIMS (with cross-checks):

1. COASSOCIATIVITY PERSISTS: The deconcatenation coproduct Delta is the tensor
   coalgebra structure, independent of genus. Delta is coassociative at ALL genera.
   (rem:sc-higher-genus, line 1295 of en_koszul_duality.tex)

2. CURVED DG COALGEBRA: d^2 = kappa * omega_g means (B^{(g)}, d, Delta) is NOT
   a dg coalgebra. It is a CURVED dg coalgebra (Positselski). The curvature
   m_0 = kappa * omega_g is a scalar (thm:modular-characteristic), hence CENTRAL
   in the coalgebra.

3. SCALAR CODERIVATION: Since d^2 = kappa * omega_g * id (scalar), the map
   d^2 is a scalar coderivation:
       d^2(Delta(x)) = d^2(x_(1)) tensor x_(2) + x_(1) tensor d^2(x_(2))
   This holds because d^2 = kappa * omega_g * id acts as multiplication by a
 succeeded in 51ms:
compute/tests/test_pva_classical_limit_bar_engine.py:23:    AP25 — gr^F B(A) is bar of classical limit, NOT the Verdier dual or cobar
compute/lib/curved_sc_higher_genus_engine.py:4:d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
compute/lib/curved_sc_higher_genus_engine.py:116:   is a curved SC^{ch,top}-coalgebra.
compute/lib/curved_sc_higher_genus_engine.py:127:   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
compute/lib/curved_sc_higher_genus_engine.py:145:   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
compute/lib/curved_sc_higher_genus_engine.py:161:- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
compute/lib/curved_sc_higher_genus_engine.py:323:    """Data for a curved SC^{ch,top}-coalgebra at genus g.
compute/lib/curved_sc_higher_genus_engine.py:329:    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
compute/lib/curved_sc_higher_genus_engine.py:460:    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
compute/lib/curved_sc_higher_genus_engine.py:469:    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
compute/lib/curved_sc_higher_genus_engine.py:497:        Genus 0: classical dg SC coalgebra (no corrections needed).
compute/lib/curved_sc_higher_genus_engine.py:498:        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
compute/lib/curved_sc_higher_genus_engine.py:602:                 'a coderivation, giving a genuine dg SC coalgebra.'),
compute/lib/curved_sc_higher_genus_engine.py:698:    """The genus tower of SC^{ch,top}-coalgebras.
compute/lib/curved_sc_higher_genus_engine.py:702:    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
compute/lib/curved_sc_higher_genus_engine.py:703:    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
compute/lib/cy_descent_theorem_engine.py:84:AP25: B(A) is a coalgebra; D_Ran(B(A)) = B(A!) is an algebra.
compute/lib/bc_motivic_galois_shadow_engine.py:1232:    For the shadow motive, dim B(A) is related to the fiber functor dimension.
compute/tests/test_theorem_thm_a_bl_rectification_engine.py:171:    """Test that barB(A) is cofibrant in BL-GKD model structure."""
compute/lib/theorem_hca_pillar_a_engine.py:19:    - B(A) is a Ch_infty-algebra (thm:cech-hca)
 succeeded in 50ms:
    # Differential d_1: C_1 -> C_0, matrix [k]
    res.differentials[1] = Matrix([[k]])

    # Contracting homotopy h: C_0 -> C_1
    # h(1) = (1/k) * generator of C_1
    # So d_1 . h = k * (1/k) = 1 = id on C_0.
    if k != 0:
        res.homotopy[0] = Matrix([[Rational(1, 1) / k]])  # symbolic 1/k
        res.homotopy_verified = True
    else:
        res.homotopy_verified = False

    return res


# ============================================================================
# V. Swiss-cheese version: SC cobar
# ============================================================================


 succeeded in 50ms:
r"""E_n bar coproduct engine: explicit coproduct structures on B_{E_n}(A) for varying n.

MATHEMATICAL FRAMEWORK
======================

For a P-algebra A (where P is a quadratic operad), the operadic bar construction is

    B_P(A) = (P^{!,c} circ \bar{A}, d_B)

where P^{!,c} is the Koszul dual cooperad and \bar{A} = ker(epsilon) is the
augmentation ideal.  B_P(A) is a COFREE P^!-coalgebra.  The coproduct on B_P(A)
is the cooperadic cocomposition of P^{!,c}.

The key operadic Koszul dualities (Loday-Vallette, Theorem 7.4.2):

    Ass^! = Ass         (associative is self-dual)
    Com^! = Lie          (commutative <-> Lie)
    Lie^! = Com          (Lie <-> commutative)
    E_n^! = E_n{-n}     (E_n is self-dual up to shift)

 succeeded in 50ms:
r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.

FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
What happens to the SC^{ch,top}-coalgebra structure?

SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):

1. COASSOCIATIVITY: Delta is coassociative at all genera (tensor coalgebra).
2. CURVED STRUCTURE: d_fib^2 = kappa * omega_g (fiberwise curvature).
3. CODERIVATION ANALYSIS: d is a coderivation; d^2 is NOT a coderivation
   (cross terms 2*(d tensor d) are nonzero).
4. CORRECTED DIFFERENTIAL: D^{(g)}^2 = 0 (Fay trisecant identity).
5. HEISENBERG EXPLICIT: d^2[J|J] = (k/24)*[J|J], coderivation discrepancy verified.
6. MULTI-WEIGHT: delta_F_2^cross lives in CLOSED sector; vanishes iff uniform-weight.
7. MODULAR TOWER: genus tower controlled by kappa(A), A-hat generating function.

Cross-checks:
(a) lambda_fp values against hardcoded exact Fractions (3 genera)
(b) Coassociativity at arities 1-5 (combinatorial identity)
(c) Heisenberg curvature coderivation discrepancy: exact factor-2 at interior splits
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && rg -n \"SC\\\\"'^'"\\\\{ch,top\\\\}-coalgebra\" -n compute/audit -g\"*\"" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
compute/audit/forgetful_e_infty_to_e1_report.md:80:The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
compute/audit/forgetful_e_infty_to_e1_report.md:112:#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
compute/audit/forgetful_e_infty_to_e1_report.md:116:Omega : (conilpotent SC^{ch,top}-coalgebras) ⇄ (SC^{ch,top}-algebras) : B
compute/audit/forgetful_e_infty_to_e1_report.md:120:The bar functor B is the RIGHT adjoint. Given an SC^{ch,top}-algebra (A, M) (a two-coloured pair: closed A, open M), B(A,M) is the universal conilpotent SC^{ch,top}-coalgebra coaugmented over (A,M). For a single chiral algebra A, promotion A -> (A, A) is required (AP86). The bar complex B(A) of a single algebra is an E_1 chiral coassociative coalgebra over (ChirAss)^!; the SC structure lives on the derived center pair (C^bullet_ch(A,A), A) (AP165). The Quillen equivalence means B and Omega are mutually inverse on the homotopy category.
compute/audit/forgetful_e_infty_to_e1_report.md:126:The left side is **conilpotent SC^{ch,top}-coalgebras**. These are coalgebras over the Koszul dual cooperad of SC^{ch,top}, which means:
compute/audit/forgetful_e_infty_to_e1_report.md:168:**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
compute/audit/forgetful_e_infty_to_e1_report.md:175:Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
compute/audit/factorization_bar_literature_report.md:216:topological).  Together they form an SC^{ch,top}-coalgebra.
compute/audit/factorization_bar_literature_report.md:331:Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
compute/audit/deformation_quantization_sc_report.md:119:- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '100,140p' compute/audit/forgetful_e_infty_to_e1_report.md" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:

The E_infty structure is NOT lost — it becomes the DIFFERENTIAL. The E_1 structure is NOT derived from E_infty — it is ADDED by the geometric passage to C x R.

#### Q5: Dunn additivity and the Swiss-cheese decomposition

Dunn additivity says E_m ⊗ E_n ≃ E_{m+n}. The relevant statement for Swiss-cheese is different: SC is NOT E_infty ⊗ E_1. The Swiss-cheese operad is a TWO-COLOURED operad with:
- Closed-closed operations: E_2 (or E_infty-chiral in the holomorphic refinement)
- Open-open operations: E_1
- Mixed operations: closed acts on open (bulk-to-boundary), but NOT open-to-closed

The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.

#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?

**Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
```
Omega : (conilpotent SC^{ch,top}-coalgebras) ⇄ (SC^{ch,top}-algebras) : B
```
This is a Quillen equivalence when SC^{ch,top} is homotopy-Koszul (which is PROVED: Theorem thm:homotopy-Koszul via Kontsevich formality + transfer from classical Swiss-cheese Koszulity).

exec
/bin/zsh -lc "sed -n '2728,2795p' chapters/frame/heisenberg_frame.tex" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1418,1455p' chapters/theory/en_koszul_duality.tex" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1260,1355p' chapters/theory/en_koszul_duality.tex" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '6268,6315p' chapters/theory/en_koszul_duality.tex" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
closed colour of~$\mathsf{SC}^{\mathrm{ch,top}}$). The operadic
bar relative to the full two-coloured operad requires a
$\mathsf{SC}^{\mathrm{ch,top}}$-algebra
$(\cA_{\mathrm{cl}},\, \cA_{\mathrm{op}})$ as input
(a closed-colour algebra and an open-colour algebra with
mixed operations). The \emph{promotion functor}
$\cA \mapsto (\cA, \cA)$ uses self-action: every
commutative algebra acts on itself as a module, supplying
the open-colour and mixed data canonically. The operadic bar
$B_{\mathsf{SC}}(\cA, \cA)$ decomposes:
\begin{itemize}
\item Closed sector: $B_{\operatorname{Com}}(\cA)
 = \operatorname{Lie}^c \circ s^{-1}\bar{\cA}$ (Harrison).
\item Open sector: $B_{\operatorname{Ass}}(\cA)
 = T^c(s^{-1}\bar{\cA})$ (tensor).
\item Mixed sector: dimension $(k{-}1)!\,\binom{k+m}{m}$
 at bi-degree $(k,m)$
 (Proposition~\textup{\ref{prop:sc-koszul-dual-three-sectors}}).
\end{itemize}
Theorem~\textup{\ref{thm:bar-swiss-cheese}} is the statement that
 succeeded in 52ms:
the Borcherds identity.


% ----------------------------------------------------------------
\subsection{Swiss-cheese decomposition}
\label{subsec:rosetta-swiss-cheese}
% ----------------------------------------------------------------

The bar complex of any chiral algebra is an $E_1$-chiral
coassociative coalgebra in which the holomorphic bar differential
and the topological deconcatenation coproduct occupy independent
directions (Theorem~\ref{thm:bar-swiss-cheese}). The
$\mathsf{SC}^{\mathrm{ch,top}}$ structure emerges on the derived
center pair, not on the bar complex itself
(\S\ref{sec:operadic-center-theorem}). For Heisenberg, the
holomorphic direction is automatically flat
($d_{\mathrm{curvature}}^2 = 0$ since the bracket vanishes), and
the $E_1$-chiral coalgebra is a direct product of a cochain complex
with a coalgebra. For $\widehat{\mathfrak{sl}}_2$, the holomorphic
direction acquires a genuine obstruction.
 succeeded in 50ms:
carry $\mathsf{SC}^{\mathrm{ch,top}}$ structure.

The $\mathsf{SC}^{\mathrm{ch,top}}$ structure emerges on
the \emph{derived chiral center}
$Z^{\mathrm{der}}_{\mathrm{ch}}(\cA)
= C^{\bullet}_{\mathrm{ch}}(\cA, \cA)$,
computed \emph{using} the bar complex as a resolution.
The derived center is the algebra of bulk observables of
the 3d HT theory on $X \times \bR$:
\begin{itemize}
\item It carries $\Etwo$ structure from
  $\FM_k(\bC)$ (the operadic center theorem,
  Theorem~\textup{\ref{thm:operadic-center-hochschild}}).
\item With an inner conformal vector at non-critical level,
  this $\Etwo$ upgrades on BRST cohomology to $\Ethree$ via the
  topologization theorem
  (Theorem~\textup{\ref{thm:topologization}}).
\item Without a conformal vector,
  $Z^{\mathrm{der}}_{\mathrm{ch}}(\cA)$ carries
  $\mathsf{SC}^{\mathrm{ch,top}}$ structure
 succeeded in 50ms:
read back into explicit bar-complex calculations and obstruction
statements.

\section{The dimensional ladder}
\label{sec:en-summary}

The chapter establishes eight results. The Totaro presentation
of $H^*(\Conf_k(\bR^n))$ generalizes Arnold's presentation from
$n = 2$ to all~$n$ (Theorem~\ref{thm:totaro-presentation}).
The $\Etwo$ bar complex satisfies $d^2 = 0$ via the Totaro
relations (Theorem~\ref{thm:e2-d-squared}).
The chiral bar-cobar duality of Main Theorem~A is recovered
as the $n = 2$ case of $\En$ Koszul duality
(Corollary~\ref{cor:n2-recovery}), and our construction is the
chain-level refinement of Ayala--Francis Poincar\'e--Koszul duality
at $n = 2$ (Proposition~\ref{prop:refines-af}).
The abstract $\En$ Koszul duality, $\En$ formality, and higher
enveloping algebras are established in the literature for all~$n$
(Theorem~\ref{thm:en-koszul-duality},
Theorem~\ref{thm:knudsen-higher-enveloping},
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '200,260p' compute/audit/factorization_bar_literature_report.md" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
The central claim of Vol II (`foundations.tex` lines 1792-1797):

> "The bar complex of Volume I, equipped with its coproduct, is a
> coalgebra over SC^{ch,top}."

The two-coloured Swiss-cheese operad SC^{ch,top} has:
- **Closed colour** from FM_k(C): the differential d_B (from OPE
  residues on holomorphic configuration spaces)
- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
  (from ordered interval-cutting on the real line)

A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
(Vol II, `introduction.tex` lines 204-207).

The differential is the E_infty/E_2 structure (symmetric,
holomorphic).  The coproduct is the E_1 structure (ordered,
topological).  Together they form an SC^{ch,top}-coalgebra.

### Why the E_1 bar is preferred


exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '90,140p' compute/audit/deformation_quantization_sc_report.md" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
**(b) Lift the PVA bracket to A_infty operations on A.**

Given the resolved classical datum, the bar-cobar machinery does this automatically:
- The genus-0 bar differential d_B encodes m_2 (the OPE/lambda-bracket).
- The bar-cobar inversion produces m_k for all k via the cobar differential.
- The SC^{ch,top} structure organizes these into the Swiss-cheese algebra.

The modular PVA quantization chapter (Vol II) makes this precise: the modular deformation complex Def^mod(A_cl(V)) has a complete genus filtration, and the MC obstruction recursion (Theorems `thm:Ob1`, `thm:Obg`) lifts Theta_0 genus by genus. The bar-intrinsic construction bypasses the recursion entirely: Theta_A := D_A - d_0 is automatically MC because D_A^2 = 0.

**(c) Show uniqueness up to A_infty quasi-isomorphism.**

For the Virasoro PVA: the relevant H^1 is one-dimensional (central parameter direction), so every genus-1 lift is gauge-equivalent to a central charge shift (Corollary `cor:virasoro-unique-lift`). For W_3: same structure (Theorem `thm:H1-W3`). For Heisenberg and affine KM: the deformation is unobstructed. **This is proved for all standard families.**

For a general PVA: uniqueness requires that H^1 of the modular deformation complex (the tangent space to the MC moduli) has finite-dimensional "relevant" part. This is expected but not proved in general.

---

### 5. Kontsevich Formality and the Chiral Analogue

**The classical analogue.** Kontsevich's formality theorem says the E_2 operad is formal, so every Poisson bracket on a manifold can be quantized to a star product. The proof uses configuration space integrals on the upper half-plane.
thinking
**Searching for corrections**

I notice that there's a problematic sentence in the dimensional ladder regarding en_koszul_duality, particularly where it refers to dg coalgebra over SC with differential. It mentions closed color and deconcat open color, which doesn't sound right. I’ll need to fix that. I should search the current repository for phrases like "closed color" and "open color," and possibly look into other related references using the rg patterns B54/B55-B56.
exec
/bin/zsh -lc "sed -n '1,140p' compute/lib/theorem_swiss_cheese_kontsevich_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '930,980p' compute/lib/en_bar_coproduct_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
r"""Swiss-cheese Kontsevich conjecture engine: operadic verification and module actions.

CONTEXT:
  De Leger (arXiv:2512.20167, Dec 2025) proves a weakened version of Kontsevich's
  Swiss-cheese conjecture: for any operad P and P-algebra A, there is an operad
  SC(P) with SC(E_n) ~ SC_n and an E_{n+1}-action on Hochschild-Pirashvili
  cochains of order n.  The conjecture (without the universal property) gives a
  combinatorial construction of the Swiss-cheese operad from the little disks.

  Moriwaki (arXiv:2410.02648, Oct 2024) proves that for C_1-cofinite VOA module
  categories, the fundamental groupoid of the Swiss-cheese operad acts on modules,
  all boundary OPEs converge absolutely on upper half-plane configurations, and
  correlation functions are independent of parenthesization order.

  Vol II of the monograph proves:
    - SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul, via Kontsevich
      formality + transfer from classical SC Koszulity [Livernet, GK94])
    - The bar complex B(A) is an E_1 chiral coassociative coalgebra over
      (ChirAss)^!; the SC^{ch,top} structure emerges on the derived center
      pair (C^bullet_ch(A,A), A) (AP165)
exec
/bin/zsh -lc "rg -n -g '"'!compute/audit/**'"' -g '"'!archive/**'"' -g '"'!raeeznotes/**'"' -g '"'!healing_*/**'"' -g '"'!rectification_*/**'"' -g '"'!fix_wave_*/**'"' -g '"'!*.bak*'"' -e 'closed color' -e 'closed colour' -e 'open color' -e 'open colour' -e 'SC\\\\\\"'^'"\\{ch,top\\}-coalgebra' -e 'SC\\\\chtop-?coalgebra' -e 'SC coalgebra' chapters compute/lib compute/tests README.md CLAUDE.md AGENTS.md" in /Users/raeez/chiral-bar-cobar succeeded in 50ms:
                'ratio_E1_Einf': Fraction(dim_e1, dim_einf) if dim_einf > 0 else None,
            }
        table[N] = row
    return table


# ================================================================
# 8. The key theorem: Swiss-cheese decomposition
# ================================================================

def swiss_cheese_analysis() -> Dict[str, object]:
    r"""Analyze the Swiss-cheese structure of the chiral bar complex.

    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
    - Coproduct Delta: from E_1 (topological, Conf_k(R))

    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
    SC is the Swiss-cheese operad.

exec
/bin/zsh -lc "sed -n '300,335p' compute/lib/theorem_swiss_cheese_kontsevich_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
AGENTS.md:140:**SC^{ch,top} is NOT on B(A) (AP165).** B(A) is E_1 coassociative coalgebra. SC^{ch,top} lives on the pair (C^bullet_{ch}(A,A), A). FORBIDDEN: "B(A) is a coalgebra over SC^{ch,top}"; "the bar differential is the closed color"; "the bar coproduct is the open color."
AGENTS.md:287:B54. "B(A) is SC coalgebra"            # FALSE: E_1 coalgebra; SC in derived center pair
CLAUDE.md:376:- B55. "The bar differential is the closed color" / "the bar coproduct is the open color." FALSE. d_B and Delta make B(A) an E_1 dg coalgebra, NOT an SC two-color datum. AP165.
CLAUDE.md:381:- B60. "A^! is an SC-algebra." FALSE. A^! is an SC^!-algebra = (Lie, Ass)-algebra. The closed color is Lie (Sklyanin bracket), not Com. AP172.
CLAUDE.md:919:AP165: B(A) is NOT an SC^{ch,top}-coalgebra. The bar complex B(A) = T^c(s^{-1} A-bar) is an E_1 chiral coassociative coalgebra (differential from chiral product, deconcatenation coproduct). It is a SINGLE E_1 coalgebra, not a two-colored SC datum. The SC^{ch,top} structure emerges in the chiral derived center: the chiral Hochschild cochain complex C^bullet_{ch}(A,A) (defined via the chiral endomorphism operad End^{ch}_A with spectral parameters from FM_k(C), NOT topological Hochschild cochains RHom_{A^e}(A,A)) carries brace operations and a Gerstenhaber bracket, and the pair (C^bullet_{ch}(A,A), A) is the SC^{ch,top} datum (bulk acting on boundary). FORBIDDEN claims: "B(A) is a coalgebra over SC^{ch,top}"; "the bar complex presents the Swiss-cheese algebra"; "the bar differential is the closed color"; "the bar coproduct is the open color." COUNTER: after writing any sentence involving B(A) and SC^{ch,top} in the same paragraph, verify that SC is attributed to the derived center pair, not to B(A).
chapters/connections/thqg_open_closed_realization.tex:365:A \emph{local chiral open/closed pair} with open color~$\cA$
chapters/connections/thqg_open_closed_realization.tex:375:$(\cB, \cA, \iota) \to (\cB', \cA, \iota')$ (with fixed open color)
chapters/connections/thqg_open_closed_realization.tex:509:The open colour $E_1(m)$ is genus-independent: the interval~$\mathbb{R}$
chapters/connections/holographic_datum_master.tex:272:in the 3d HT theory whose closed colour is~$\cA$.
chapters/connections/holographic_datum_master.tex:277:realised as the closed colour of the DNP 3d HT theory, the DNP
chapters/connections/holographic_datum_master.tex:774:$\cA$ realized as the closed colour of a 3d HT theory in the sense
compute/lib/curved_sc_higher_genus_engine.py:142:       which acts on FM_k(C) (closed color).
compute/lib/curved_sc_higher_genus_engine.py:143:   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
compute/lib/curved_sc_higher_genus_engine.py:161:- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
compute/lib/curved_sc_higher_genus_engine.py:497:        Genus 0: classical dg SC coalgebra (no corrections needed).
compute/lib/curved_sc_higher_genus_engine.py:498:        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
compute/lib/curved_sc_higher_genus_engine.py:602:                 'a coderivation, giving a genuine dg SC coalgebra.'),
compute/lib/curved_sc_higher_genus_engine.py:702:    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
compute/lib/curved_sc_higher_genus_engine.py:703:    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
chapters/connections/concordance.tex:3836:the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
 succeeded in 51ms:
    # The interchange law follows from the product structure
    # FM_k(C) x Conf_m^<(R) of the SC operation spaces.
    # The differential d_B acts on FM_k(C) chains; the coproduct
    # Delta acts on Conf_m^<(R) chains. They commute because they
    # act on different factors.

    # More precisely: the SC coalgebra structure on B(A) means
    # (B(A), d_B, Delta) is simultaneously:
    #   - a dg coalgebra over B(E_2) on the closed color
    #   - a conilpotent coalgebra over B(E_1) on the open color
    # and the two structures are compatible via the interchange law.

    interchange_holds = True

    return {
        'interchange_holds': interchange_holds,
        'mechanism': 'product_structure_FM_times_Conf',
        'closed_arity': k,
        'open_arity': m,
        'kappa': kappa,
exec
/bin/zsh -lc 'cd /Users/raeez/chiral-bar-cobar && rg -n "bar complex.*Swiss-cheese"' in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
Total output lines: 194

resume_20260413_163457/S14_standalone_to_main.md:3:- [HIGH] [programme_summary_sections5_8.tex](/Users/raeez/chiral-bar-cobar/standalone/programme_summary_sections5_8.tex:134) — PROBLEM: line 134 says classes `G/L` have “Swiss-cheese-formal bar complexes,” and line 314 later identifies the bar differential/coproduct with the closed/open Swiss-cheese colours. That conflicts with [chiral_koszul_pairs.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/chiral_koszul_pairs.tex:2893), which proves SC-formality iff class `G`, and with [en_koszul_duality.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex:1247), which says the bar complex is only the `E_1` coalgebraic engine while `SC^{ch,top}` lives on `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`. FIX: replace the class sentence by “classes `G` and `L` have finite shadow depth; only class `G` is Swiss-cheese formal,” and replace the colour sentence by “the bar differential and deconcatenation coproduct are the two `E_1` dg-coalgebra operations on `B(\cA)`; the Swiss-cheese closed/open colours belong to the derived-center pair `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”
resume_20260413_163457/S14_standalone_to_main.md:4:- [HIGH] [programme_summary.tex](/Users/raeez/chiral-bar-cobar/standalone/programme_summary.tex:1432) — PROBLEM: this file repeats the same seam error three times: “Swiss-cheese-formal bar complexes” at lines 1432 and 1812, and explicit colour-assignment to the bar differential/coproduct at line 1614, even though the same file already states the corrected rule at lines 1579–1580 (“`SC^{ch,top}` emerges on the derived center pair, not on `B(\cA)` itself”). FIX: make lines 1432/1812 say “finite shadow depth for classes `G/L`, but SC-formality only for class `G`,” and rewrite line 1614 exactly as “the bar complex carries the `E_1` differential and deconcatenation coproduct; the Swiss-cheese structure is on `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”
resume_20260413_163457/S14_standalone_to_main.md:6:- [HIGH] [survey_modular_koszul_duality_v2.tex](/Users/raeez/chiral-bar-cobar/standalone/survey_modular_koszul_duality_v2.tex:1515) — PROBLEM: line 1515 reverses the derived-center pair as `(\cA,\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA))`, and line 3658 later says the bar differential/coproduct encode the closed/open colours. The corrected manuscript surface uses `( \cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA),\cA )` and keeps `SC^{ch,top}` off the bar complex: see [introduction.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/introduction.tex:1466) and [en_koszul_duality.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex:1247). FIX: swap the pair order at line 1515, and replace the line-3658 sentence with “the bar complex is the `E_1` coalgebraic engine; the closed/open Swiss-cheese colours live on `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”
resume_20260413_163457/S14_standalone_to_main.md:7:- [HIGH] [survey_track_b_compressed.tex](/Users/raeez/chiral-bar-cobar/standalone/survey_track_b_compressed.tex:995) — PROBLEM: lines 995–997 say the bar differential “encodes the closed colour” and the coproduct “the open colour.” That is exactly the AP165/ B55–B56 retracted formulation; the corrected rule is in [en_koszul_duality.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex:1257). FIX: replace those lines with “the bar complex classifies twisting morphisms and carries an `E_1` differential plus deconcatenation coproduct; the Swiss-cheese closed/open colours belong to the derived center pair `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”
resume_20260413_163457/S14_standalone_to_main.md:544:/bin/zsh -lc "rg -n -C 2 'bar complex.*Swiss|Swiss-cheese.*bar complex|bar complex.*SC|SC.*bar complex|Swiss-cheese-formal bar|bar complexes; algebras|bar complex is.*coalgebra|bar complex.*coalgebra' standalone/*.tex" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
resume_20260413_163457/S14_standalone_to_main.md:569:I find this interesting! I need to take a closer look at lines 659 and so on. It seems there might be something not stale with the bar complex E1, which could lead to a Swiss-cheese effect emerging at the derived center. I want to carefully inspect those lines to understand what's happening around there. Let's see if I can uncover something important or if my assumptions need adjustment!
resume_20260413_163457/S14_standalone_to_main.md:878:/bin/zsh -lc "rg -n -C 1 'SC-algebra|SC-coalgebra|The bar complex carrying both structures|bar complex.*Swiss-cheese|bar complex.*SC|Swiss-cheese.*pair' standalone/*.tex" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
resume_20260413_163457/S14_standalone_to_main.md:880:standalone/survey_track_b_compressed.tex:1489:bar complex, the shadow tower, the Swiss-cheese structure, the
resume_20260413_163457/S14_standalone_to_main.md:888:standalone/survey_modular_koszul_duality_v2.tex:4152:bar complex, the shadow tower, the Swiss-cheese structure, the
resume_20260413_163457/S14_standalone_to_main.md:1349:- [HIGH] [programme_summary_sections5_8.tex](/Users/raeez/chiral-bar-cobar/standalone/programme_summary_sections5_8.tex:134) — PROBLEM: line 134 says classes `G/L` have “Swiss-cheese-formal bar complexes,” and line 314 later identifies the bar differential/coproduct with the closed/open Swiss-cheese colours. That conflicts with [chiral_koszul_pairs.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/chiral_koszul_pairs.tex:2893), which proves SC-formality iff class `G`, and with [en_koszul_duality.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex:1247), which says the bar complex is only the `E_1` coalgebraic engine while `SC^{ch,top}` lives on `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`. FIX: replace the class sentence by “classes `G` and `L` have finite shadow depth; only class `G` is Swiss-cheese formal,” and replace the colour sentence by “the bar differential and deconcatenation coproduct are the two `E_1` dg-coalgebra operations on `B(\cA)`; the Swiss-cheese closed/open colours belong to the derived-center pair `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”
resume_20260413_163457/S14_standalone_to_main.md:1350:- [HIGH] [programme_summary.tex](/Users/raeez/chiral-bar-cobar/standalone/programme_summary.tex:1432) — PROBLEM: this file repeats the same seam error three times: “Swiss-cheese-formal bar complexes” at lines 1432 and 1812, and explicit colour-assignment to the bar differential/coproduct at line 1614, even though the same file already states the corrected rule at lines 1579–1580 (“`SC^{ch,top}` emerges on the derived center pair, not on `B(\cA)` itself”). FIX: make lines 1432/1812 say “finite shadow depth for classes `G/L`, but SC-formality only for class `G`,” and rewrite line 1614 exactly as “the bar complex carries the `E_1` differential and deconcatenation coproduct; the Swiss-cheese structure is on `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”
resume_20260413_163457/S14_standalone_to_main.md:1352:- [HIGH] [survey_modular_koszul_duality_v2.tex](/Users/raeez/chiral-bar-cobar/standalone/survey_modular_koszul_duality_v2.tex:1515) — PROBLEM: line 1515 reverses the derived-center pair as `(\cA,\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA))`, and line 3658 later says the bar differential/coproduct encode the closed/open colours. The corrected manuscript surface uses `( \cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA),\cA )` and keeps `SC^{ch,top}` off the bar complex: see [introduction.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/introduction.tex:1466) and [en_koszul_duality.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex:1247). FIX: swap the pair order at line 1515, and replace the line-3658 sentence with “the bar complex is the `E_1` coalgebraic engine; the closed/open Swiss-cheese colours live on `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”
resume_20260413_163457/S14_standalone_to_main.md:1353:- [HIGH] [survey_track_b_compressed.tex](/Users/raeez/chiral-bar-cobar/standalone/survey_track_b_compressed.tex:995) — PROBLEM: lines 995–997 say the bar differential “encodes the closed colour” and the coproduct “the open colour.” That is exactly the AP165/ B55–B56 retracted formulation; the corrected rule is in [en_koszul_duality.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex:1257). FIX: replace those lines with “the bar complex classifies twisting morphisms and carries an `E_1` differential plus deconcatenation coproduct; the Swiss-cheese closed/open colours belong to the derived center pair `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”
relaunch_20260413_111534/S14_standalone_to_main.md:13:- [HIGH] `standalone/programme_summary.tex:1414` and `standalone/programme_summary.tex:1783`, plus `standalone/programme_summary_sections5_8.tex:58` — PROBLEM: these summaries assign Swiss-cheese formality to the *bar complex* and even to classes `G/L` together. The chapter surface defines SC-formality as a property of the algebra/open-closed higher operations and singles out only class `G`. FIX: replace “Swiss-cheese-formal bar complex(es)” with “finite shadow depth; SC-formality is stronger and selects exactly class `G`,” and rewrite the gauge-theory item as “class `G` is SC-formal; class `L` has one surviving cubic correction.”
relaunch_20260413_111534/S14_standalone_to_main.md:1276:/bin/zsh -lc "rg -n \"B\\(\\cA\\).*SC|bar complex .*SC|SC.*bar complex|B\\(A\\).*SC|Swiss-cheese.*bar complex|SC-formal bar complexes|bar complexes.*Swiss-cheese\" standalone/*.tex" in /Users/raeez/chiral-bar-cobarexec
relaunch_20260413_111534/S14_standalone_to_main.md:1279:    (?:B\(\cA\).*SC|bar complex .*SC|SC.*bar complex|B\(A\).*SC|Swiss-cheese.*bar complex|SC-formal bar complexes|bar complexes.*Swiss-cheese)
relaunch_20260413_111534/S14_standalone_to_main.md:2232:Another seam class is showing up in the survey derivatives: they still write the line-level shadow-growth formula as an algebra-level invariant `\rho(\cA)`, while the chapter split now reserves that notation for `\sup_L \rho_L`. I’m checking whether there are similarly stale “bar complex is Swiss-cheese-formal” phrasings that survived the structural correction.
relaunch_20260413_111534/S14_standalone_to_main.md:2258:/bin/zsh -lc 'rg -n "bar complex.*Swiss-cheese|Swiss-cheese.*bar complex|SC-formal" chapters/theory/en_koszul_duality.tex chapters/theory/higher_genus_modular_koszul.tex chapters/theory/ordered_associative_chiral_kd.tex chapters/frame/preface.tex' in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '2040,2070p' chapters/theory/bar_construction.tex" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
\begin{remark}[Topological factorization on the $\bR$-direction]
\label{rem:deconcatenation-topological}
\index{deconcatenation coproduct!topological factorization}%
\index{Swiss-cheese!topological factorization on $\bR$}%
The $n+1$ terms of the deconcatenation coproduct on~$T^c$
correspond to the $n+1$ ways of cutting
$\Conf_n^{<}(\bR) = \{t_1 < \cdots < t_n\}$ into two
consecutive segments $\{t_1,\ldots,t_p\} \sqcup
\{t_{p+1},\ldots,t_n\}$. This is topological factorization
on the~$\bR$-direction of~$\bC \times \bR$ in the
Costello--Gwilliam framework~\cite[\S5.4]{CG17}: the bar
differential is holomorphic factorization on~$\FM_k(\bC)$
(Remark~\ref{rem:costello-gwilliam-factorization}); the bar
coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
Together they constitute the Swiss-cheese algebra
$\mathrm{SC}^{\mathrm{ch,top}}$ on
$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
\end{remark}

\begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
exec
/bin/zsh -lc "sed -n '620,700p' compute/tests/test_two_colored_bar_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '330,390p' compute/tests/test_sc_bar_cobar_inversion_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,240p' compute/tests/test_e1_from_chiral_bar_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
        assert w.colour == 'o'
        assert w.closed_degree == 1
        assert w.open_degree == 2
        assert w.total_degree == 3

    def test_two_colored_word_pure_open(self):
        """Pure-open word has only open indices."""
        w = TwoColoredBarWord('o', (), (0, 1))
        assert w.closed_degree == 0
        assert w.open_degree == 2

    def test_no_open_to_closed_in_differential(self):
        """SC operad has no open-to-closed operations.
        This means the differential of a closed-output word
        produces only closed-output words."""
        C = polynomial_algebra(3)
        sc = SCAlgebra.from_self_action(C)
        bc = SwissCheeseBarComplex(sc, 3)
        aug_dim = C.aug_ideal_dim

 succeeded in 51ms:
        assert results['ope_recovered_killing']

    def test_sl2_killing_values(self):
        """Killing form: (h,h) = 2k, (e,f) = k."""
        k = Symbol('k')
        results = verify_cobar_recovers_sl2(k)
        assert results['killing_hh'] == 2 * k
        assert results['killing_ef'] == k


# ============================================================================
# VII. SC^{ch,top} Analysis Tests
# ============================================================================


class TestSCBarCobar:
    """Tests for the SC^{ch,top} cobar analysis.

    Key result: the SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour
    algebra A, NOT the full SC^{ch,top}-algebra (A, A^!).
 succeeded in 51ms:
r"""Tests for E_1 coalgebra structure from the chiral bar complex.

Verifies from first principles:
  (1) Deconcatenation coproduct is coassociative (tensor coalgebra)
  (2) Counit axioms hold
  (3) Bar differential is a coderivation of the deconcatenation
  (4) Logarithmic forms split correctly under the coproduct
  (5) FM configuration spaces do NOT split (but forms do for ordered bar)
  (6) Symmetric bar has cocommutative shuffle coproduct
  (7) Ordered bar has NON-cocommutative deconcatenation
  (8) Arnold relation is a genuine relation (not a formal identity)
  (9) Explicit Heisenberg and sl_2 computations
  (10) Algebraic = geometric reconciliation

Multi-path verification:
  Path 1: Direct algebraic verification (coproduct formulas)
  Path 2: Counting arguments (number of terms, dimensions)
  Path 3: Symmetry properties (cocommutativity yes/no)
  Path 4: Explicit OPE data (Heisenberg, sl_2)
  Path 5: Form-degree analysis (Arnold, Kunneth)
exec
/bin/zsh -lc 'rg -n "chiral_bar_sc_structure_summary|swiss_cheese_analysis|CurvedSCData|CorrectedSCCoalgebra|ModularSCTower|analyze_sc_bar_cobar|SCBarCobarResult|interchange_holds" compute/tests compute/lib' in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
compute/lib/curved_sc_higher_genus_engine.py:322:class CurvedSCData:
compute/lib/curved_sc_higher_genus_engine.py:375:    sc: CurvedSCData,
compute/lib/curved_sc_higher_genus_engine.py:459:class CorrectedSCCoalgebra:
compute/lib/curved_sc_higher_genus_engine.py:697:class ModularSCTower:
compute/lib/curved_sc_higher_genus_engine.py:839:    sc = CurvedSCData(kappa=kappa, genus=genus, algebra_name=algebra_name)
compute/lib/curved_sc_higher_genus_engine.py:840:    corrected = CorrectedSCCoalgebra(kappa=kappa, genus=genus,
compute/lib/en_bar_coproduct_engine.py:940:def swiss_cheese_analysis() -> Dict[str, object]:
compute/lib/two_colored_bar_engine.py:1081:def chiral_bar_sc_structure_summary() -> dict:
compute/lib/theorem_swiss_cheese_kontsevich_engine.py:312:    interchange_holds = True
compute/lib/theorem_swiss_cheese_kontsevich_engine.py:315:        'interchange_holds': interchange_holds,
compute/lib/theorem_swiss_cheese_kontsevich_engine.py:1105:        and interchange['interchange_holds']
compute/lib/sc_bar_cobar_inversion_engine.py:661:class SCBarCobarResult:
compute/lib/sc_bar_cobar_inversion_engine.py:686:def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
compute/lib/sc_bar_cobar_inversion_engine.py:710:    result = SCBarCobarResult()
compute/lib/sc_bar_cobar_inversion_engine.py:1008:    heis_analysis = analyze_sc_bar_cobar(heisenberg_ope())
compute/lib/sc_bar_cobar_inversion_engine.py:1009:    sl2_analysis = analyze_sc_bar_cobar(affine_sl2_ope())
compute/tests/test_sc_bar_cobar_inversion_engine.py:60:    SCBarCobarResult,
compute/tests/test_sc_bar_cobar_inversion_engine.py:61:    analyze_sc_bar_cobar,
compute/tests/test_sc_bar_cobar_inversion_engine.py:354:        result = analyze_sc_bar_cobar(heisenberg_ope())
compute/tests/test_sc_bar_cobar_inversion_engine.py:359:        result = analyze_sc_bar_cobar(heisenberg_ope())


thinking
**Classifying SC algebra claims in files**

I’m gathering exact line numbers and forming a classification for each instance where files claim the bar complex is an SC^{ch,top} algebra, verifying if these surfaces still hold the uncorrected AP165 claim, paying close attention to files flagged across volumes while sorting out which are still relevant.
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && nl -ba compute/audit/forgetful_e_infty_to_e1_report.md | sed -n '70,90p'" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
    70	3. **B^{ord}(A)** (ordered bar): uses ALL OPE products, retains linear ordering — no Sigma_n quotient. The coproduct is deconcatenation — coassociative.
    71	
    72	The E_infty bar (in the sense of B^{Sigma}) exists and is the subject of Vol I. It carries the cocommutative factorization coalgebra structure. But the Swiss-cheese structure requires BOTH the symmetric bar (closed colour) AND the ordered bar (open colour) simultaneously. Neither alone suffices.
    73	
    74	#### Q3: The SC^{ch,top} structure REQUIRES the E_1 bar?
    75	
    76	**Yes, this is correct.** The Swiss-cheese operad SC^{ch,top} has two colours:
    77	- **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
    78	- **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
    79	
    80	The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
    81	
    82	The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
    83	- In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
    84	- In the Sigma_g-direction: chiral Koszul duality (bar-cobar for chiral/factorization algebras, Vol I Theorem A)
    85	- Combined: Swiss-cheese Koszul duality
    86	
    87	#### Q4: The bar construction CONVERTS E_infty (algebra) structure into E_1 (coalgebra) structure?
    88	
    89	**This is the correct high-level picture, but it needs precision.** The bar construction does not "convert" E_infty to E_1. Rather:
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && nl -ba compute/audit/forgetful_e_infty_to_e1_report.md | sed -n '160,190p'" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
   160	- The E_1 shadows (r-matrix, associator, quartic R-matrix) are the ordered data that project to the E_infty shadows (kappa, C, Q) under coinvariants
   161	
   162	---
   163	
   164	### 4. The Deepest Answer
   165	
   166	The bar complex does not "forget" E_infty down to E_1. The correct statement is:
   167	
   168	**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
   169	
   170	```
   171	E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
   172	                                                  ---forget closed--> E_1-coAlg
   173	```
   174	
   175	Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
   176	
   177	This is the content of the "Steinberg principle" (Vol II preface, line 72):
   178	
   179	> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
thinking
**Focusing on manuscript files**

I think I need to focus more on live manuscript files, keeping an eye on any bad claims aside from the en_koszul_duality summary and considering the frame or preface too. I should inspect the preface lines closely. Maybe I'll search the current repository while excluding anything to do with compute. I could use rg to look for instances of "bar complex.*SC" since it seems there might only be a few relevant occurrences to consider.
exec
/bin/zsh -lc "sed -n '4128,4148p' chapters/frame/preface.tex" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '3828,3843p' chapters/connections/concordance.tex" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "rg -n -C 2 -g '"'!*.bak*'"' -g '"'!healing_*/**'"' -g '"'!rectification_*/**'"' -g '"'!fix_wave_*/**'"' -g '"'!compute/**'"' -g '"'!archive/**'"' -g '"'!raeeznotes/**'"' -e 'SC\\\\\\"'^'"\\{ch,top\\}.*bar complex' -e 'bar complex.*SC' -e 'coalgebra over .*SC' -e 'differential is the closed' -e 'coproduct is the open' -e 'is a dg coalgebra over the holomorphic--topological Swiss-cheese operad' -e 'SC coalgebra structure at genus 0' chapters README.md main.tex appendices" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
 the $E_\infty$ shadow is the coinvariant of the $E_1$ shadow.
\end{enumerate}

\paragraph{Which is used where.}
The modular convolution algebra $\gAmod$
(Definition~\ref{def:modular-convolution-dg-lie}) uses the
symmetric bar~$B^\Sigma$. The
Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
$B^\Sigma$ for the closed colour. The Harrison
bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
and controls the Francis--Gaitsgory chiral Lie Koszul duality.
The three bar complexes produce three
different Koszul duals: $B^{\mathrm{Lie}}$ gives the chiral Lie
dual, $B^\Sigma$ gives $\cA^!_{\mathrm{ch}}$ (Volume~I,
Theorem~A), and $B^{\mathrm{ord}}$ gives the ordered/line-side
 succeeded in 52ms:
full open/closed package.

The operad $\SCchtop$ is homotopy-Koszul (proved). The operation spaces
are $\SCchtop(\mathrm{ch}^k;\mathrm{ch})=\operatorname{FM}_k(\mathbb C)$,
$\SCchtop(\mathrm{ch}^k,\mathrm{top}^m;\mathrm{top})
=\operatorname{FM}_k(\mathbb C)\times E_1(m)$, and
$\SCchtop(\ldots,\mathrm{top},\ldots;\mathrm{ch})=\varnothing$:
open-to-closed is forbidden. An $\SCchtop$-operation of degree~$(k,m)$
lives on
$\operatorname{FM}_k(\mathbb C)\times E_1(m)$;
the holomorphic factor governs the closed colour and the topological
factor governs the open colour. The bar complex $\barB(\cA)$ itself
is a single-coloured $\Eone$-chiral dg coalgebra over
$(\mathrm{ChirAss})^!$: its differential extracts OPE residues along
collision divisors of $\operatorname{FM}_k(\mathbb C)$, and its
deconcatenation coproduct is the cofree coassociative structure.
The $\SCchtop$ structure emerges on the derived center pair
$(\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA), \cA)$, not on $\barB(\cA)$.
Kontsevich formality supplies a
quasi-isomorphism $\Phi\colon\SCchtop\xrightarrow{\sim}\mathsf{SC}$ from
 succeeded in 51ms:
chapters/theory/introduction.tex-2231- Diagonal bicomodule; Hochschild-coHochschild dictionary; open trace formalism; chiral--quantum group equivalence (Thm~\ref{thm:chiral-qg-equiv}) \\
chapters/theory/introduction.tex-2232-$\En$ Koszul duality (Ch.~\ref{ch:en-koszul-duality}) &
chapters/theory/introduction.tex:2233: Higher-dimensional bar complexes; Totaro relations; Ayala--Francis; topologization $\mathsf{SC}^{\mathrm{ch,top}} + T_{\mathrm{Sug}} \Rightarrow \Ethree$ (Thm~\ref{thm:topologization}, proved for affine KM; Conj~\ref{conj:topologization-general}, general) \\[6pt]
chapters/theory/introduction.tex-2234-\multicolumn{2}{@{}l}{\textbf{Part~\ref{part:standard-landscape}: The Standard Landscape} \emph{(complete portrait of each family)}} \\[3pt]
chapters/theory/introduction.tex-2235-\hline
--
chapters/theory/en_koszul_duality.tex-1249-\label{princ:sc-two-incarnations}
chapters/theory/en_koszul_duality.tex-1250-\index{Swiss-cheese operad!on derived center, NOT on bar}
chapters/theory/en_koszul_duality.tex:1251:\index{bar complex!is E1 coassociative, NOT SC}
chapters/theory/en_koszul_duality.tex-1252-The ordered bar complex
chapters/theory/en_koszul_duality.tex-1253-$\barB^{\mathrm{ord}}(\cA) = T^c(s^{-1}\bar{\cA})$
--
chapters/theory/bar_cobar_adjunction_curved.tex-147- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
chapters/theory/bar_cobar_adjunction_curved.tex-148- the closed/$\Einf$ colour on holomorphic configurations, and the
chapters/theory/bar_cobar_adjunction_curved.tex:149: deconcatenation coproduct is the open/$\Eone$ colour on the
chapters/theory/bar_cobar_adjunction_curved.tex-150- topological interval. The symmetric bar retains only the closed
chapters/theory/bar_cobar_adjunction_curved.tex-151- colour; the ordered bar retains both.
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && nl -ba compute/audit/factorization_bar_literature_report.md | sed -n '202,230p'" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
   202	> "The bar complex of Volume I, equipped with its coproduct, is a
   203	> coalgebra over SC^{ch,top}."
   204	
   205	The two-coloured Swiss-cheese operad SC^{ch,top} has:
   206	- **Closed colour** from FM_k(C): the differential d_B (from OPE
   207	  residues on holomorphic configuration spaces)
   208	- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
   209	  (from ordered interval-cutting on the real line)
   210	
   211	A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
   212	(Vol II, `introduction.tex` lines 204-207).
   213	
   214	The differential is the E_infty/E_2 structure (symmetric,
   215	holomorphic).  The coproduct is the E_1 structure (ordered,
   216	topological).  Together they form an SC^{ch,top}-coalgebra.
   217	
   218	### Why the E_1 bar is preferred
   219	
   220	The ordered bar B^ord(A) carries STRICTLY MORE information than the
   221	symmetric bar B^Sigma(A).  The descent map B^ord -> B^Sigma takes
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '5410,5455p' chapters/theory/en_koszul_duality.tex" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
  $\Pthree$ bracket and the curvature together deform the
  commutative structure to a filtered $\Ethree$-chiral
  algebra.
  The deformation parameter is $(k+h^\vee) \in
  (k+h^\vee)\,H^3(\fg)[[k+h^\vee]]$,
  matching the deformation space of
  Theorem~\textup{\ref{thm:e3-cs}(ii)}.
\item \textup{(Factorisation property.)}
  The $\Ethree$-chiral algebra
  $\CE^{\mathrm{ch}}_k(\fg)$ is a factorisation algebra on
  $X$ in the sense that the restriction to disjoint
  opens $U_1 \sqcup U_2 \hookrightarrow X$ induces
  \begin{equation}\label{eq:factorisation}
    \CE^{\mathrm{ch}}_k(\fg)\big|_{U_1 \sqcup U_2}
    \;\simeq\;
    \CE^{\mathrm{ch}}_k(\fg)\big|_{U_1}
    \otimes
    \CE^{\mathrm{ch}}_k(\fg)\big|_{U_2},
  \end{equation}
  where the tensor product is in the derived category of

thinking
**Investigating bar_cobar claims**

I'm finding something interesting about the bar_cobar_adjunction_curved line 149 which might be incorrect. I need to check the context more closely. The concordance shows that the SC two-colour uses ordered bars for open colour and B^Sigma for closed colour, but that might also be wrong. Also, the derived center pair rather than bar complexes relates to the SC. The closed colour should actually be the derived center, not B^Sigma. I'll need to look at the broader context and search the manuscript further.
exec
/bin/zsh -lc 'cd /Users/raeez/chiral-bar-cobar && rg -n "Swiss-cheese" chapters/theory/en_koszul_duality.tex' in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
1196:\section{The Swiss-cheese case: holomorphic--topological
1199:\index{Swiss-cheese operad|textbf}
1217:\emph{two-colored} operad: the Swiss-cheese operad
1220:\begin{definition}[Holomorphic--topological Swiss-cheese operad]
1222:\index{Swiss-cheese operad!definition}
1250:\index{Swiss-cheese operad!on derived center, NOT on bar}
1416:\index{Swiss-cheese operad!promotion from chiral algebra}
1466:\begin{remark}[Curved Swiss-cheese at higher genus, and the
1469:\index{Swiss-cheese operad!curved|textbf}
1524:The Swiss-cheese operad $\mathsf{SC}^{\mathrm{ch,top}}$ is the
1530:classical Swiss-cheese of Voronov~\cite{Voronov99} is the
1543:\index{Swiss-cheese operad!center theorem}
1549:center} of an open-color algebra~$\cA$ in the Swiss-cheese operad
1554:classical Swiss-cheese operad. The chiral version requires the
1591:The Swiss-cheese operad $\mathsf{SC}^{\mathrm{ch,top}}$
1599:\index{Swiss-cheese operad!Koszul dual cooperad|textbf}
1600:\index{Koszul dual cooperad!Swiss-cheese three sectors}
1799:of the holomorphic--topological Swiss-cheese operad, with
2227:This identification is \emph{inevitable}: once the Swiss-cheese
2259:closed-sector algebra acting on~$\cA$ through the Swiss-cheese
exec
/bin/zsh -lc 'cd /Users/raeez/chiral-bar-cobar && rg -n -C2 "bar complex.*Swiss-cheese" chapters/theory/en_koszul_duality.tex' in /Users/raeez/chiral-bar-cobar exited 1 in 52ms:

codex
I’ve now found genuine manuscript residue, not just compute-doc drift. The main live prose errors are the old “closed color/open color on the bar complex” formulations in [chapters/theory/en_koszul_duality.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex:6291), [chapters/theory/bar_cobar_adjunction_curved.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/bar_cobar_adjunction_curved.tex:147), and [chapters/connections/concordance.tex](/Users/raeez/chiral-bar-cobar/chapters/connections/concordance.tex:3836). I’m patching those together with the compute-facing docstrings that still advertise the false SC-coalgebra picture.
exec
/bin/zsh -lc "sed -n '136,158p' chapters/theory/bar_cobar_adjunction_curved.tex" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '3828,3844p' chapters/connections/concordance.tex" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '640,710p' compute/tests/test_en_bar_coproduct_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '940,980p' compute/tests/test_en_bar_coproduct_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '220,340p' compute/lib/curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '590,615p' compute/tests/test_two_colored_bar_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '320,520p' compute/lib/curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '688,735p' compute/lib/curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
 the $E_\infty$ shadow is the coinvariant of the $E_1$ shadow.
\end{enumerate}

\paragraph{Which is used where.}
The modular convolution algebra $\gAmod$
(Definition~\ref{def:modular-convolution-dg-lie}) uses the
symmetric bar~$B^\Sigma$. The
Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
$B^\Sigma$ for the closed colour. The Harrison
bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
and controls the Francis--Gaitsgory chiral Lie Koszul duality.
The three bar complexes produce three
different Koszul duals: $B^{\mathrm{Lie}}$ gives the chiral Lie
dual, $B^\Sigma$ gives $\cA^!_{\mathrm{ch}}$ (Volume~I,
Theorem~A), and $B^{\mathrm{ord}}$ gives the ordered/line-side
dual $\cA^!_{\mathrm{line}}$.
 succeeded in 51ms:
 Yang--Baxter equation. Its $\Sigma_2$-coinvariant is the scalar
 $\kappa(\cA) = \operatorname{av}(r(z))$
 \textup{(}equation~\eqref{eq:intro-kappa-from-r}\textup{)}: the
 full spectral data collapses to a single number.
\item \emph{The braided monoidal category.} The deconcatenation
 coproduct on $\barB^{\mathrm{ord}}(\cA)$ controls the tensor
 product on the category of ordered factorization modules
 $\Factord(\cA)$. The $R$-matrix provides the braiding.
 After coinvariants, only the symmetric monoidal shadow survives.
\item \emph{The Swiss-cheese directionality.} The
 $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
 $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
 the closed/$\Einf$ colour on holomorphic configurations, and the
 deconcatenation coproduct is the open/$\Eone$ colour on the
 topological interval. The symmetric bar retains only the closed
 colour; the ordered bar retains both.
\end{enumerate}
The five main theorems~A--H are invariants of the symmetric bar
and therefore survive averaging. The Yangian $R$-matrix, the
Drinfeld--Kohno comparison, and the full braided monoidal category
 succeeded in 51ms:
    def test_kxy_arity_3(self):
        """A = k[x,y], d=2 at arity 3."""
        result = polynomial_algebra_bar(d=2, max_arity=4)
        assert result[3]['E_1_dim'] == 8   # 2^3
        assert result[3]['E_inf_dim'] == 4  # C(4, 3) = 4

    def test_kxyz_dimensions(self):
        """A = k[x,y,z], d=3."""
        result = polynomial_algebra_bar(d=3, max_arity=4)
        for n in range(1, 5):
            assert result[n]['E_1_dim'] == 3 ** n
            assert result[n]['E_inf_dim'] == comb(3 + n - 1, n)

    def test_harrison_and_symmetric_both_leq_e1(self):
        """Both Harrison and symmetric coalgebra have dim <= E_1 dim = d^n."""
        result = polynomial_algebra_bar(d=3, max_arity=5)
        for n in range(1, 6):
            assert result[n]['Harrison_dim'] <= result[n]['E_1_dim'], \
                f"arity {n}: Harrison > E_1"
            assert result[n]['E_inf_dim'] <= result[n]['E_1_dim'], \
 succeeded in 51ms:
class TestTypeChecking:
    """Test the conceptual type-checking of the SC bar construction."""

    def test_chiral_bar_summary_keys(self):
        """The summary should contain all expected keys."""
        summary = chiral_bar_sc_structure_summary()
        expected_keys = [
            "closed_sector", "open_sector", "compatibility",
            "input", "output", "key_fact", "type_resolution",
            "manuscript_ref",
        ]
        for key in expected_keys:
            assert key in summary

    def test_chiral_bar_summary_manuscript_ref(self):
        """Summary references the correct theorem."""
        summary = chiral_bar_sc_structure_summary()
        assert "thm:bar-swiss-cheese" in summary["manuscript_ref"]

    def test_two_colored_word_closed(self):
 succeeded in 51ms:
            ratio = Fraction(d ** n, comb(d + n - 1, n))
            # Should be close to 6
            assert abs(float(ratio) - 6.0) < 6.0 * n / d

    def test_necklace_sum_recovers_tensor_dim(self):
        """Witt formula: fundamental cross-check between Harrison and E_1."""
        d = 4
        for n in [1, 2, 3, 4, 5, 6]:
            assert sum(j * necklace_polynomial(j, d) for j in range(1, n + 1)
                       if n % j == 0) == d ** n

    def test_chiral_bar_uses_e1_coproduct(self):
        """Verify the claim that the chiral bar uses E_1 coproduct.

        The Swiss-cheese analysis should report E_1 for the coproduct source.
        """
        analysis = swiss_cheese_analysis()
        assert 'E_1' in analysis['coproduct_source']
        assert 'coassociative' in analysis['coalgebra_type']
        assert 'NOT cocommutative' in analysis['coalgebra_type']
 succeeded in 50ms:

@dataclass
class CurvedSCData:
    """Data for a curved SC^{ch,top}-coalgebra at genus g.

    At genus g >= 1 with kappa != 0:
    - d is a coderivation of Delta (always, by tensor coalgebra universality).
    - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
    - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.

    Attributes:
        kappa: modular characteristic kappa(A)
        genus: genus g
        algebra_name: name of the algebra
    """
    kappa: Fraction
    genus: int
    algebra_name: str = ""

 succeeded in 51ms:
        sum_{g>=1} F_g(A) * x^{2g} = kappa(A) * ((x/2)/sin(x/2) - 1)

    So the coefficient of x^{2g} is lambda_g^FP (by Theorem D).
    This provides an independent verification path.
    """
    return lambda_fp(g)


# ============================================================================
# 1. BAR ELEMENTS AND DECONCATENATION COPRODUCT
# ============================================================================

@dataclass(frozen=True)
class BarElement:
    """A bar element [a_1 | a_2 | ... | a_n] in the tensor coalgebra.

    Each a_i is represented by a label (string) and a conformal weight (int).
    The bar degree (arity/tensor length) is n.

    Attributes:
 succeeded in 50ms:
        return "closed_sector"
    return "closed_sector"  # Always closed: graph sums are geometric


# ============================================================================
# 6. MODULAR OPERAD COMPATIBILITY
# ============================================================================

@dataclass
class ModularSCTower:
    """The genus tower of SC^{ch,top}-coalgebras.

    {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
    At each genus, the SC structure is:
    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
    - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.

    The compatibility between FCom structure and SC structure arises because:
    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '5120,5155p' chapters/connections/3d_gravity.tex" in /Users/raeez/chiral-bar-cobar exited 1 in 52ms:
sed: chapters/connections/3d_gravity.tex: No such file or directory
exec
/bin/zsh -lc 'cd /Users/raeez/chiral-bar-cobar && ls chapters' in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
connections
examples
frame
theory
exec
/bin/zsh -lc 'cd /Users/raeez/chiral-bar-cobar && ls chapters/connections' in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
arithmetic_shadows.tex
bv_brst.tex
concordance.tex
editorial_constitution.tex
entanglement_modular_koszul.tex
feynman_connection.tex
feynman_diagrams.tex
frontier_modular_holography_platonic.tex
genus1_seven_faces.tex
genus_complete.tex
holographic_codes_koszul.tex
holographic_datum_master.tex
master_concordance.tex
outlook.tex
poincare_computations.tex
semistrict_modular_higher_spin_w3.tex
subregular_hook_frontier.tex
thqg_entanglement_programme.tex
thqg_introduction_supplement.tex
thqg_introduction_supplement_body.tex
exec
/bin/zsh -lc 'cd /Users/raeez/chiral-bar-cobar && rg -n -i "Swiss-cheese.*bar complex" -g"*.tex" chapters' in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
chapters/theory/introduction.tex:1467:(Theorem~\ref{thm:thqg-swiss-cheese}); the bar complex itself
collab wait(call_7It3g0m1j1ZdVMLolwRwsHJ9)
  receivers: 019d87a2-6c85-7b52-aa21-551c51fab3dc
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '1450,1495p' chapters/theory/introduction.tex" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
appears, the line/boundary direction is ordered before any
$\Sigma_n$-symmetrization is taken.

\textsc{Categorical primacy.} The categorical objects of
representation theory are ordered. Quantum groups, Yangians, and
braided tensor categories are the natural framework for chiral
algebras coupled to a spectral parameter; their structure
constants are matrix-valued and obey ordered identities (CYBE,
pentagon, hexagon, mixed Yang--Baxter). Symmetric monoidal
categories are obtained from braided ones by the
$\Sigma_n$-coinvariant procedure on the braid groupoid; this is
exactly the categorical analogue of the averaging
map~\eqref{eq:intro-averaging}.

\textsc{Operadic primacy of Swiss-cheese.} The Swiss-cheese
operad $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ governs the
open/closed derived center pair $(\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA), \cA)$
(Theorem~\ref{thm:thqg-swiss-cheese}); the bar complex itself
is a single-coloured $\Eone$-chiral coassociative coalgebra
over $(\mathrm{ChirAss})^!$. The SC structure emerges on the
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && sed -n '920,960p' compute/lib/en_bar_coproduct_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
        row = {}
        for arity in range(1, 7):
            dim_e1 = gen_dim ** arity
            # For even desuspended degree generators:
            dim_einf = comb(gen_dim + arity - 1, arity)
            dim_harrison = necklace_polynomial(arity, gen_dim)
            row[arity] = {
                'E_1': dim_e1,
                'E_inf': dim_einf,
                'Harrison': dim_harrison,
                'ratio_E1_Einf': Fraction(dim_e1, dim_einf) if dim_einf > 0 else None,
            }
        table[N] = row
    return table


# ================================================================
# 8. The key theorem: Swiss-cheese decomposition
# ================================================================

exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && nl -ba compute/lib/en_bar_coproduct_engine.py | sed -n '930,960p'" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
   930	                'ratio_E1_Einf': Fraction(dim_e1, dim_einf) if dim_einf > 0 else None,
   931	            }
   932	        table[N] = row
   933	    return table
   934	
   935	
   936	# ================================================================
   937	# 8. The key theorem: Swiss-cheese decomposition
   938	# ================================================================
   939	
   940	def swiss_cheese_analysis() -> Dict[str, object]:
   941	    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
   942	
   943	    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
   944	    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
   945	    - Coproduct Delta: from E_1 (topological, Conf_k(R))
   946	
   947	    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
   948	    SC is the Swiss-cheese operad.
   949	

thinking
**Reviewing bar complex references across volumes**

I'm checking references to the bar complex and Swiss-cheese structures, especially around line 664 in a key compute file, to identify relevant mentions like AP165. To keep this manageable, I'll include what I've found and then search the second volume for similar live surface references using targeted pattern searches.
exec
/bin/zsh -lc 'cd /Users/raeez/chiral-bar-cobar-vol2 && rg -n -i "bar complex.*Swiss-cheese"' in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
AGENTS.md:71:- "the bar complex presents the Swiss-cheese algebra"
main.tex:1078:$A_\infty$~chiral algebra, bar complex, Swiss-cheese operad,
working_notes.tex:157:they make the bar complex a coalgebra over the Swiss-cheese
working_notes.tex:1149:The bar complex presents the Swiss-cheese algebra, as the
chapters/connections/spectral-braiding-frontier.tex:379:\,d\lambda$ produces the infinitesimal braiding, and the full quantum $R(z)$ produces the finite braiding. For the affine lineage, this is proved unconditionally: one-loop exactness collapses the $A_\infty$ tower, on the reduced evaluation comparison surface the reduced HT monodromy identifies with the KZ monodromy, and the affine Drinfeld--Kohno theorem compares that monodromy with the corresponding braided tensor-product representation of $\mathrm{Rep}_q(\fg)$ there (Theorem~\ref{thm:affine-monodromy-identification}). On the $\mathfrak{sl}_2$ weight system, the bar complex integrals over $\FM_n(\C) \times \Conf_n(\R)$ compute the colored Jones polynomial (Corollary~\ref{cor:jones-polynomial}), recovering the Reshetikhin--Turaev invariant directly from the Swiss-cheese structure. The all-types categorical Clebsch--Gordan and
chapters/theory/factorization_swiss_cheese.tex:2145:The bar complex presents the Swiss-cheese coalgebra in the same sense
chapters/theory/factorization_swiss_cheese.tex:2229:Thus: the bar complex presents the Swiss-cheese coalgebra, as the
chapters/connections/bv_brst.tex:2048:compared is not ``the bar complex as a Swiss-cheese algebra'': the
chapters/theory/introduction.tex:48:MC element, bar complex, and Swiss-cheese structure exist
chapters/theory/foundations.tex:200:\index{three bar complexes!Swiss-cheese decomposition}
chapters/theory/foundations.tex:1350:\index{three bar complexes!Swiss-cheese provenance}%
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar-vol2 && sed -n '2120,2235p' chapters/theory/factorization_swiss_cheese.tex" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
\ClaimStatusProvedHere{}
The analogy between the classical and modular Steinberg varieties is
exact in the following sense.
\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{lll}
\textbf{Classical} & \textbf{Modular} & \textbf{Output} \\
\hline
$\widetilde{\cN} = T^*(\cG/\cB)$ &
  $\Mbar_{g_1,n_1+1}$ &
  Resolution of singularities \\
$\fg$ &
  $\Mbar_{g,n}$ &
  Total space \\
$\widetilde{\cN} \times_\fg \widetilde{\cN}$ &
  $Z_{\mathrm{sep}} \times_{\Mbar_{g,n}} Z_{\mathrm{sep}}$ &
  Steinberg variety \\
$H_*^{\cG}(\mathrm{St})$ &
  $H_*(\mathsf{St}^{\mathrm{mod}})$ &
  Convolution algebra \\
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar-vol2 && nl -ba chapters/theory/factorization_swiss_cheese.tex | sed -n '2135,2235p'" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
  2135	  $Z_{\mathrm{sep}} \times_{\Mbar_{g,n}} Z_{\mathrm{sep}}$ &
  2136	  Steinberg variety \\
  2137	$H_*^{\cG}(\mathrm{St})$ &
  2138	  $H_*(\mathsf{St}^{\mathrm{mod}})$ &
  2139	  Convolution algebra \\
  2140	Hecke algebra $\cH(W)$ &
  2141	  $L_{\mathrm{mod}}$ &
  2142	  Algebraic output \\
  2143	\end{tabular}
  2144	\end{center}
  2145	The bar complex presents the Swiss-cheese coalgebra in the same sense
  2146	that the Steinberg variety presents the Hecke algebra: both are
  2147	convolution structures on self-intersection varieties of resolution
  2148	correspondences.
  2149	
  2150	The non-separating clutching extends the parallel: the
  2151	non-separating correspondence
  2152	\begin{equation}\label{eq:nonsep-clutching-span}
  2153	\begin{tikzcd}[column sep=3em]
  2154	  \Mbar_{g,n+2}
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar-vol2 && sed -n '360,410p' chapters/connections/spectral-braiding-frontier.tex" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
$w_3 = \sum f^{abc}f^{abc} = 8$ matches the degree-$3$
coefficient $v_3(3_1) = 1$.

\medskip
\noindent\textbf{Status.}
The degree-by-degree matching at $n \le 3$ is unconditional
for the $\mathfrak{sl}_2$ weight system: it uses the proved
spectral $R$-matrix
(Corollary~\ref{cor:jones-polynomial}), the proved one-loop
exactness, and the proved Arnold cancellation
(Theorem~\ref{thm:Arnold_full_proof}). The full
identification $Z_{A_\infty}(K) = Z(K)$ at all orders
remains conjectural (Conjecture~\ref{conj:bar-kontsevich}).
\end{example}

\begin{remark}[Relation to the Drinfeld--Kohno ladder]
\label{rem:dk-kontsevich}
The classical $r$-matrix
$r(z) = \int_0^\infty e^{-\lambda z}\,\{{\cdot}_\lambda{\cdot}\}
\,d\lambda$ produces the infinitesimal braiding, and the full quantum $R(z)$ produces the finite braiding. For the affine lineage, this is proved unconditionally: one-loop exactness collapses the $A_\infty$ tower, on the reduced evaluation comparison surface the reduced HT monodromy identifies with the KZ monodromy, and the affine Drinfeld--Kohno theorem compares that monodromy with the corresponding braided tensor-product representation of $\mathrm{Rep}_q(\fg)$ there (Theorem~\ref{thm:affine-monodromy-identification}). On the $\mathfrak{sl}_2$ weight system, the bar complex integrals over $\FM_n(\C) \times \Conf_n(\R)$ compute the colored Jones polynomial (Corollary~\ref{cor:jones-polynomial}), recovering the Reshetikhin--Turaev invariant directly from the Swiss-cheese structure. The all-types categorical Clebsch--Gordan and
wait(call_7It3g0m1j1ZdVMLolwRwsHJ9) timed out:
collab wait(call_4lyJk4xF3RFq2QYZANDEO8hO)
  receivers: 019d87a2-6c85-7b52-aa21-551c51fab3dc
exec
/bin/zsh -lc 'cd /Users/raeez/calabi-yau-quantum-groups && rg -n -i "bar complex.*Swiss-cheese"' in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
compute/lib/swiss_cheese_cy3_e1.py:4:of Vol III. The E_1 bar complex has a Swiss-cheese algebra structure
notes/physics_bv_brst_cy.tex:84:We develop the BV-BRST interpretation of the bar complex of a quantum chiral algebra arising from a Calabi--Yau category. The $\Ainf$-operations of the CY category define an open string field theory in the Batalin--Vilkovisky formalism; the CY pairing provides the BV bracket; the quantum master equation is identified with the Maurer--Cartan equation for $\Theta_A$ from Volume~I. The bar complex $B(A)$ is the BV-BRST complex, with the bar differential equal to $Q_{\BRST}$. We explain how the genus-$g$ curvature $d_B^2 = \kappa_{\mathrm{ch}} \cdot \omega_g$ is the BRST anomaly, and how anomaly cancellation ($\kappa_{\mathrm{ch}} = 0$) corresponds to an ``untwisted'' BKM algebra. The bar complex $B(A)$ is an $E_1$ dg coassociative coalgebra (deconcatenation coproduct, bar differential); the Swiss-cheese structure $\SC^{\mathrm{ch,top}}$ of Volume~II emerges on the open-closed \emph{pair} $(C^\bullet_{\mathrm{ch}}(A,A),\, A)$, where the chiral Hochschild cochain complex $C^\bullet_{\mathrm{ch}}(A,A)$ (the bulk/closed sector) acts on $A$ (the boundary/open sector). For CY3 manifolds $X$, the full BV-BRST complex of the topological B-model is the bar complex of the quantum vertex chiral group $G(X)$.
notes/physics_bv_brst_cy.tex:407:The bar complex $B(A)$ is an $E_1$ dg coassociative coalgebra. It is \emph{not} an $\SC^{\mathrm{ch,top}}$-algebra: the Swiss-cheese operad is two-coloured, requiring both an open (boundary) and a closed (bulk) sector. The $\SC^{\mathrm{ch,top}}$ structure emerges on the pair $(C^\bullet_{\mathrm{ch}}(A,A),\, A)$, not on $B(A)$ alone.
exec
/bin/zsh -lc "cd /Users/raeez/calabi-yau-quantum-groups && sed -n '1,120p' compute/lib/swiss_cheese_cy3_e1.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
r"""Swiss-cheese structure of CY3-derived E_1 chiral algebras.

Connects Vol II's SC^{ch,top} theory to the CY3-derived E_1 algebras
of Vol III. The E_1 bar complex has a Swiss-cheese algebra structure
where the E_1 (associative/ordered) direction is the R-direction
(boundary) and the chiral direction is the C-direction (bulk).

MATHEMATICAL BACKGROUND:
========================

1. SC^{E_1,ch} OPERAD FOR E_1-CHIRAL ALGEBRAS FROM CY3.
   Vol II proves (thm:homotopy-Koszul, def:SC-operations) that B(A) is
   an algebra over SC^{ch,top} = FM(C) x Conf(R). For CY3-derived E_1
   algebras, the E_1 direction is PRIMITIVE data: the factorization on
   ordered configurations comes from the CoHA multiplication (critical
   equivariant cohomology), NOT from a symmetric OPE.

   The SC^{E_1,ch} operad has operation spaces:
     SC^{E_1,ch}(n_c, n_o) = FM_{n_c}(C) x Conf^{ord}_{n_o}(R)
   where n_c = number of closed (bulk/chiral) inputs,

thinking
**Reviewing Swiss-cheese algebra claims in Volume III**

I'm sorting through conflicting claims about whether the bar complex B(A) has a Swiss-cheese algebra structure in Volume III. Some parts say yes, others explicitly disagree, so I'll note which files and statements to include or exclude for classification based on accuracy and context. I'll also run a focused search to catch all relevant occurrences in the volume.
exec
/bin/zsh -lc 'cd /Users/raeez/calabi-yau-quantum-groups && rg -n -i "Swiss-cheese.*bar complex"' in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
working_notes.tex:1713:\subsection{Swiss-cheese and the $\Etwo$ bar complex}
working_notes.tex:1716:The Vol~II Swiss-cheese structure $\mathrm{SC}^{\mathrm{ch,top}}$ (Theorem~H) provides the operadic framework for the $\Etwo$ bar complex of Section~\ref{wn:sec:e2-bar}. The three bar complexes $(B_{\Eone}, B_{\Etwo}, B_{\Einf})$ correspond to the three sectors:
chapters/theory/e1_chiral_algebras.tex:290:Volume~II resolves the ordered side of the programme. Its seven parts develop the Swiss-cheese operad, the ordered bar complex, the classical $r(z)$-matrix at level $k$, the seven faces of $r(z)$, and the derived center of an $E_1$-chiral algebra. Vol~III uses that machinery through the CY interface fixed here.
chapters/theory/e1_chiral_algebras.tex:1585:Four facts control the later parts of Vol~III. First, factorization algebras on a complex curve are $E_2$ topologically but specialize to $E_1$ when holomorphy fixes an ordering, and the Swiss-cheese splitting $E_1(C) \times E_2(\mathrm{top})$ makes that specialization precise. Second, the ordered bar complex $B^{\mathrm{ord}}(A) = T^c(s^{-1}\bar A)$ with deconcatenation coproduct is the natural $E_1$ primitive, and averaging to the symmetric bar $B^{\Sigma}(A)$ is lossy because it kills the ordered $R$-matrix data. Third, the CY-to-chiral functor $\Phi$ at $d=2$ produces an ordered bar whose Euler character is the Borcherds denominator and whose first shadow invariant is $\kappa_{\mathrm{ch}}$, distinct from $\kappa_{\mathrm{BKM}}$, $\kappa_{\mathrm{cat}}$, and $\kappa_{\mathrm{fiber}}$. Fourth, the $E_2$ enhancement requires the Drinfeld center construction, the categorified averaging map $E_1\text{-Cat} \to E_2\text{-Cat}$; at $d=3$ the enhancement is conditional on the CY-A$_3$ programme.
notes/physics_bv_brst_cy.tex:84:We develop the BV-BRST interpretation of the bar complex of a quantum chiral algebra arising from a Calabi--Yau category. The $\Ainf$-operations of the CY category define an open string field theory in the Batalin--Vilkovisky formalism; the CY pairing provides the BV bracket; the quantum master equation is identified with the Maurer--Cartan equation for $\Theta_A$ from Volume~I. The bar complex $B(A)$ is the BV-BRST complex, with the bar differential equal to $Q_{\BRST}$. We explain how the genus-$g$ curvature $d_B^2 = \kappa_{\mathrm{ch}} \cdot \omega_g$ is the BRST anomaly, and how anomaly cancellation ($\kappa_{\mathrm{ch}} = 0$) corresponds to an ``untwisted'' BKM algebra. The bar complex $B(A)$ is an $E_1$ dg coassociative coalgebra (deconcatenation coproduct, bar differential); the Swiss-cheese structure $\SC^{\mathrm{ch,top}}$ of Volume~II emerges on the open-closed \emph{pair} $(C^\bullet_{\mathrm{ch}}(A,A),\, A)$, where the chiral Hochschild cochain complex $C^\bullet_{\mathrm{ch}}(A,A)$ (the bulk/closed sector) acts on $A$ (the boundary/open sector). For CY3 manifolds $X$, the full BV-BRST complex of the topological B-model is the bar complex of the quantum vertex chiral group $G(X)$.
notes/physics_bv_brst_cy.tex:563: \item \textbf{Swiss-cheese = open-closed BV.} The $\SC^{\mathrm{ch,top}}$ structure on the pair $(C^\bullet_{\mathrm{ch}}(A,A),\, A)$ is the BV structure on the combined open-closed string field theory. The bar complex $B(A)$ is the $E_1$ dg coassociative coalgebra (the open sector); the chiral derived center $C^\bullet_{\mathrm{ch}}(A,A)$ is the closed sector.
notes/physics_bv_brst_cy.tex:586: \item \textbf{The closed string field theory and $\mathcal{W}$-algebras.} For CY3, the closed string sector is controlled by the BCOV theory, whose algebraic structure is a $\mathcal{W}$-algebra (the vertex algebra of polyvector fields with Schouten bracket and $\partial$ operator). The full open-closed BV-BRST complex should be the Swiss-cheese bar complex $B_{\SC}(A_\cC)$, combining the $\mathcal{W}$-algebra (bulk) with $G(X)$ (boundary) via the open-closed coupling.
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar-vol2 && sed -n '2030,2065p' chapters/connections/bv_brst.tex" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:

\begin{remark}[Alternative approach via operadic Koszul duality]
\label{rem:bv-bar-coderived-operadic}%
\index{BV algebra!bar complex identification!operadic route}%
\index{Koszul duality!BV/bar comparison}%
There is a second route to
Theorem~\ref{thm:bv-bar-coderived}. By
Principle~\ref{princ:sc-two-incarnations}, the ordered bar complex
$\barB^{\mathrm{ord}}(\cA)$ is the open
$\Eone$-chiral coalgebra engine, while the derived center
$Z^{\mathrm{der}}_{\mathrm{ch}}(\cA)
= C^{\bullet}_{\mathrm{ch}}(\cA,\cA)$ is the closed object carrying
the $\mathsf{SC}^{\mathrm{ch,top}}$ structure. In this two-coloured
package, the BV operator $\Delta_{\mathrm{BV}}$ is the closed-colour
contraction induced by the Swiss-cheese pairing, whereas the bar
differential $d_{\barB}$ is the open-colour coderivation induced by
the chiral product. The two operators therefore come from the same
operadic bar-cobar datum, but they live in different colours. What is
compared is not ``the bar complex as a Swiss-cheese algebra'': the
Swiss-cheese object is the pair
exec
/bin/zsh -lc "cd /Users/raeez/chiral-bar-cobar && nl -ba chapters/theory/bar_construction.tex | sed -n '2038,2060p'" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
  2038	\end{remark}
  2039	
  2040	\begin{remark}[Topological factorization on the $\bR$-direction]
  2041	\label{rem:deconcatenation-topological}
  2042	\index{deconcatenation coproduct!topological factorization}%
  2043	\index{Swiss-cheese!topological factorization on $\bR$}%
  2044	The $n+1$ terms of the deconcatenation coproduct on~$T^c$
  2045	correspond to the $n+1$ ways of cutting
  2046	$\Conf_n^{<}(\bR) = \{t_1 < \cdots < t_n\}$ into two
  2047	consecutive segments $\{t_1,\ldots,t_p\} \sqcup
  2048	\{t_{p+1},\ldots,t_n\}$. This is topological factorization
  2049	on the~$\bR$-direction of~$\bC \times \bR$ in the
  2050	Costello--Gwilliam framework~\cite[\S5.4]{CG17}: the bar
  2051	differential is holomorphic factorization on~$\FM_k(\bC)$
  2052	(Remark~\ref{rem:costello-gwilliam-factorization}); the bar
  2053	coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
  2054	Together they constitute the Swiss-cheese algebra
  2055	$\mathrm{SC}^{\mathrm{ch,top}}$ on
  2056	$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
  2057	\end{remark}
codex
Writable here
- `compute/audit/forgetful_e_infty_to_e1_report.md:80` — Q3’s answer says “The bar complex presents the `SC^{ch,top}`-coalgebra,” directly promoting the ordered bar `B(A)` to the two-coloured Swiss-cheese coalgebra (AP165 forbids this attribution).
- `compute/audit/forgetful_e_infty_to_e1_report.md:168` — The “Deepest Answer” paragraph repeats the claim “the bar construction REFINES `E_infty` to `SC^{ch,top}` and produces an `SC^{ch,top}`-coalgebra `B(A)`,” so the bar complex itself is still treated as the Swiss-cheese object.
- `compute/audit/forgetful_e_infty_to_e1_report.md:179` — The “Steinberg principle” analogy proclaims “The bar complex presents the Swiss-cheese algebra,” reinforcing the idea that `B(A)` carries the SC structure rather than the derived-center pair.
- `compute/audit/factorization_bar_literature_report.md:202` — The opening statement “The bar complex … is a coalgebra over `SC^{ch,top}`” and the paragraph below that the differential/coproduct “together form an `SC^{ch,top}`-coalgebra” leave no doubt that the literature review still attributes the Swiss-cheese algebra directly to `B(A)`.
- `chapters/theory/bar_construction.tex:2054` — Remark \ref{rem:deconcatenation-topological} says the bar differential and coproduct “together constitute the Swiss-cheese algebra `SC^{ch,top}` on `\FM_k(\bC)\times\Conf_n^{<}(\bR)`,” so the bar complex is again the full SC object instead of just the `E_1` engine.
- `compute/lib/en_bar_coproduct_engine.py:943` — The Swiss-cheese decomposition docstring states “The chiral bar complex `B^ch(A)` is a Swiss-cheese coalgebra,” explicitly identifying the ordered bar with the SC operad and continuing to conflate `B(A)` with the prohibited two-colour structure.

External volumes
- `chapters/theory/factorization_swiss_cheese.tex:2145` — The Steinberg analogy in this Vol II chapter repeats “The bar complex presents the Swiss-cheese coalgebra,” so the bar coalgebra is still treated as the Swiss-cheese datum (the same phrasing is echoed again around line 2229).
- `compute/lib/swiss_cheese_cy3_e1.py:1` — The Vol III compute note opens “The E₁ bar complex has a Swiss-cheese algebra structure,” naming the ordered bar itself as the `SC^{ch,top}`-algebra and thus continuing the AP165/B54 conflation.
wait(call_4lyJk4xF3RFq2QYZANDEO8hO) 1 agents complete:
  019d87a2-6c85-7b52-aa21-551c51fab3dc completed: "Writable here
- `compute/audit/forgetful_e_infty_to_e1_report.md:80` — Q3’s answer says “The bar complex presents the `S…"



thinking
**Evaluating repo files**

I've found a live file, bar_construction.tex, and some audit docs. I need to inspect bar_construction.tex because it's editable. I'll consider checking certain lines manually and also look at the audit docs. They seem relevant since they're marked as writable, but I'm unsure if I should edit them. The user wants a thorough search across all volumes, and I wonder if we should patch some if they're inaccurate. The situation feels a little overwhelming, so I'll have to decide on the scope of changes carefully.
thinking
**Inspecting repo files**

I’m considering whether to build or test, but maybe I can just patch the live source and audit docs. I need to inspect bar_construction.tex and the audit documents carefully. There might also be compute/audit docs in the current repo and external volumes, but I can't edit the external ones. I wonder if I should verify whether we can apply patches outside the root. Let’s focus on inspecting specific lines in bar_construction and the audit docs, and check for any false claims in the compute files too.
thinking
**Inspecting documents**

I’m going to start by inspecting bar_construction.tex and the audit documents. They seem crucial to ensure everything's accurate and up-to-date. I'll look for any potential issues or necessary edits that need to be made. It’s important to take my time here so that I don’t miss anything significant. I want to make sure I provide the user with precise and clear information moving forward based on what I find in these documents.
codex
The narrowed list now includes one more live theorem chapter and a few audit notes that still advertise the false picture. I’m patching the live manuscript and compute modules first, then I’ll decide whether to patch the audit notes in-place or leave them as explicitly historical if that is the more truthful minimal treatment.
exec
/bin/zsh -lc "sed -n '2038,2066p' chapters/theory/bar_construction.tex" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '160,186p' compute/audit/forgetful_e_infty_to_e1_report.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '70,95p' compute/audit/forgetful_e_infty_to_e1_report.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '196,210p' compute/audit/factorization_bar_literature_report.md" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
\end{remark}

\begin{remark}[Topological factorization on the $\bR$-direction]
\label{rem:deconcatenation-topological}
\index{deconcatenation coproduct!topological factorization}%
\index{Swiss-cheese!topological factorization on $\bR$}%
The $n+1$ terms of the deconcatenation coproduct on~$T^c$
correspond to the $n+1$ ways of cutting
$\Conf_n^{<}(\bR) = \{t_1 < \cdots < t_n\}$ into two
consecutive segments $\{t_1,\ldots,t_p\} \sqcup
\{t_{p+1},\ldots,t_n\}$. This is topological factorization
on the~$\bR$-direction of~$\bC \times \bR$ in the
Costello--Gwilliam framework~\cite[\S5.4]{CG17}: the bar
differential is holomorphic factorization on~$\FM_k(\bC)$
(Remark~\ref{rem:costello-gwilliam-factorization}); the bar
coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
Together they constitute the Swiss-cheese algebra
$\mathrm{SC}^{\mathrm{ch,top}}$ on
$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
\end{remark}
 succeeded in 51ms:
- The E_1 shadows (r-matrix, associator, quartic R-matrix) are the ordered data that project to the E_infty shadows (kappa, C, Q) under coinvariants

---

### 4. The Deepest Answer

The bar complex does not "forget" E_infty down to E_1. The correct statement is:

**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:

```
E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
                                                  ---forget closed--> E_1-coAlg
```

Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.

This is the content of the "Steinberg principle" (Vol II preface, line 72):

> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
 succeeded in 51ms:
3. **B^{ord}(A)** (ordered bar): uses ALL OPE products, retains linear ordering — no Sigma_n quotient. The coproduct is deconcatenation — coassociative.

The E_infty bar (in the sense of B^{Sigma}) exists and is the subject of Vol I. It carries the cocommutative factorization coalgebra structure. But the Swiss-cheese structure requires BOTH the symmetric bar (closed colour) AND the ordered bar (open colour) simultaneously. Neither alone suffices.

#### Q3: The SC^{ch,top} structure REQUIRES the E_1 bar?

**Yes, this is correct.** The Swiss-cheese operad SC^{ch,top} has two colours:
- **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
- **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta

The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.

The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
- In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
- In the Sigma_g-direction: chiral Koszul duality (bar-cobar for chiral/factorization algebras, Vol I Theorem A)
- Combined: Swiss-cheese Koszul duality

#### Q4: The bar construction CONVERTS E_infty (algebra) structure into E_1 (coalgebra) structure?

**This is the correct high-level picture, but it needs precision.** The bar construction does not "convert" E_infty to E_1. Rather:
 succeeded in 51ms:
coassociative coproduct compatible with the cogenerators.

### The Swiss-cheese structure unifies both

The central claim of Vol II (`foundations.tex` lines 1792-1797):

> "The bar complex of Volume I, equipped with its coproduct, is a
> coalgebra over SC^{ch,top}."

The two-coloured Swiss-cheese operad SC^{ch,top} has:
- **Closed colour** from FM_k(C): the differential d_B (from OPE
  residues on holomorphic configuration spaces)
- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
  (from ordered interval-cutting on the real line)



thinking
**Checking manuscript files**

I need to check certain files like `chapters/connections/concordance.tex` at line 3836 and `chapters/theory/bar_construction.tex` at line 2054, as they appear to have issues. I might also want to look at `chapters/theory/en_koszul_duality.tex` at line 6290, just to be thorough. Additionally, I’ll consider `chapters/frame/heisenberg_frame.tex` for references to theorems.

It seems necessary to examine specific compute libraries like `two_colored_bar_engine.py`, `en_bar_coproduct_engine.py`, and others too.
thinking
**Inspecting algebra concepts**

I’m pondering the concept of an SC^{ch,top}-algebra and whether it connects to AP165. If input A is a pair in SC algebra, I find that the current repository focuses on Volume I, but B^ch(A) pertains to the bar complex of chiral algebra instead of SC algebra. It seems this module deals with SC bar-cobar inversion, which might not be correct conceptually. I think I need to check the top of the file for clarity.
exec
/bin/zsh -lc "sed -n '1,85p' compute/lib/sc_bar_cobar_inversion_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.

THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.

This engine makes that theorem COMPUTATIONAL by building the cobar complex
Omega(B^ch(A)) explicitly at low arities for Heisenberg H_k and affine
sl_2 at level k, verifying:

  1. The bar complex B^ch(A) at arities 1-3 with explicit differentials.
  2. The cobar complex Omega(B^ch(A)) with the cobar differential
     d_Omega = d_coQ + d_cores + d_co-Ainf (Verdier dual of bar differential).
  3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
     (chain map inducing iso on cohomology).
  4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?

MATHEMATICAL CONTENT:

  Bar complex B^ch(H_k):
exec
/bin/zsh -lc "sed -n '680,715p' compute/lib/sc_bar_cobar_inversion_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,90p' compute/lib/theorem_swiss_cheese_kontsevich_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,110p' compute/tests/test_sc_bar_cobar_inversion_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
    recovers_open_colour: bool = False
    recovers_full_sc: bool = False
    closed_colour_algebra: str = ""
    explanation: str = ""


def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
    """Analyze what the SC cobar recovers.

    For any SC^{ch,top}-algebra (A, A_boundary):
      The SC bar B_SC(A) encodes both colours.
      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).

    The open colour (boundary/A^!) is NOT recovered by the cobar.
    It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
    The FULL SC structure requires BOTH operations.

    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
    so the bar-cobar adjunction is a Quillen equivalence.
    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
 succeeded in 51ms:
r"""Tests for SC bar-cobar inversion engine: Omega(B^ch(A)) recovers A.

THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.

FIVE VERIFICATION PATHS (3+ per claim, per CLAUDE.md multi-path mandate):

  Path 1 (Bar d^2=0):     Arnold relation forces d^2 = 0 at all degrees
  Path 2 (Cobar recovery): Omega(B(A)) ~ A via counit quasi-isomorphism
  Path 3 (SC analysis):    SC cobar recovers closed colour, NOT full SC
  Path 4 (Homotopy):       Contracting homotopy exists at arity 2
  Path 5 (Three functors): Omega != D_Ran != Z^der_ch (AP25, AP34)

Cross-checks:
  (a) Heisenberg and sl_2 give consistent results
  (b) Level k passes through bar-cobar without loss
  (c) Numerical evaluation at multiple k values
  (d) Bar cohomology matches thm:frame-heisenberg-bar
  (e) Koszul dual is NOT the cobar output (AP25, AP33)

 succeeded in 51ms:
r"""Swiss-cheese Kontsevich conjecture engine: operadic verification and module actions.

CONTEXT:
  De Leger (arXiv:2512.20167, Dec 2025) proves a weakened version of Kontsevich's
  Swiss-cheese conjecture: for any operad P and P-algebra A, there is an operad
  SC(P) with SC(E_n) ~ SC_n and an E_{n+1}-action on Hochschild-Pirashvili
  cochains of order n.  The conjecture (without the universal property) gives a
  combinatorial construction of the Swiss-cheese operad from the little disks.

  Moriwaki (arXiv:2410.02648, Oct 2024) proves that for C_1-cofinite VOA module
  categories, the fundamental groupoid of the Swiss-cheese operad acts on modules,
  all boundary OPEs converge absolutely on upper half-plane configurations, and
  correlation functions are independent of parenthesization order.

  Vol II of the monograph proves:
    - SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul, via Kontsevich
      formality + transfer from classical SC Koszulity [Livernet, GK94])
    - The bar complex B(A) is an E_1 chiral coassociative coalgebra over
      (ChirAss)^!; the SC^{ch,top} structure emerges on the derived center
      pair (C^bullet_ch(A,A), A) (AP165)
exec
/bin/zsh -lc "sed -n '1028,1044p' compute/lib/factorization_coproduct_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "rg -n -g '"'!compute/audit/**'"' -g '"'!archive/**'"' -g '"'!raeeznotes/**'"' -g '"'!healing_*/**'"' -g '"'!rectification_*/**'"' -g '"'!fix_wave_*/**'"' -g '"'!*.bak*'"' -e 'Swiss-cheese coalgebra' -e 'SC\\"'^'"\\{ch,top\\}-coalgebra' -e 'presents the Swiss-cheese' -e 'closed colour.*differential' -e 'coproduct.*open colour' -e 'bar differential is.*closed' -e 'bar coproduct.*open' -e 'B\\"'^ch'"\\(A\\) is a Swiss-cheese' chapters compute/lib compute/tests" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:

    The two coproducts are NOT two descriptions of the same thing.
    They are two DIFFERENT structures living on two DIFFERENT
    directions of the Swiss-cheese product:

    FM_k(C) x Conf_k^<(R)
      |                  |
      v                  v
    Bar differential   Deconcatenation
    (closed color)     coproduct (open color)
    Cocommutative      Non-cocommutative
    Factorization      E_1 coalgebra
    on Ran(X)          on ordered configs

    The unordered (factorization) coproduct on Ran(X) is the
    Sigma_k-symmetrization of the ordered (deconcatenation)
    coproduct.  Both are coassociative.  Only the ordered one
 succeeded in 51ms:
compute/tests/test_curved_sc_higher_genus_engine.py:1:r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
compute/tests/test_curved_sc_higher_genus_engine.py:4:What happens to the SC^{ch,top}-coalgebra structure?
compute/tests/test_curved_sc_higher_genus_engine.py:548:    """Verify the genus tower of SC^{ch,top}-coalgebras."""
compute/lib/curved_sc_higher_genus_engine.py:1:r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
compute/lib/curved_sc_higher_genus_engine.py:4:d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
compute/lib/curved_sc_higher_genus_engine.py:116:   is a curved SC^{ch,top}-coalgebra.
compute/lib/curved_sc_higher_genus_engine.py:127:   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
compute/lib/curved_sc_higher_genus_engine.py:145:   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
compute/lib/curved_sc_higher_genus_engine.py:323:    """Data for a curved SC^{ch,top}-coalgebra at genus g.
compute/lib/curved_sc_higher_genus_engine.py:329:    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
compute/lib/curved_sc_higher_genus_engine.py:460:    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
compute/lib/curved_sc_higher_genus_engine.py:469:    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
compute/lib/curved_sc_higher_genus_engine.py:698:    """The genus tower of SC^{ch,top}-coalgebras.
compute/lib/tensor_harrison_bar_engine.py:1451:            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
compute/lib/sc_homotopy_koszul_engine.py:64:    derived category of SC^{ch,top}-coalgebras.
compute/lib/en_bar_coproduct_engine.py:943:    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
compute/lib/two_colored_bar_engine.py:41:   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
compute/lib/two_colored_bar_engine.py:1082:    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
compute/lib/two_colored_bar_engine.py:1095:       = the SC^{ch,top}-coalgebra compatibility
exec
/bin/zsh -lc "sed -n '1,90p' compute/tests/test_en_bar_coproduct_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
r"""Tests for en_bar_coproduct_engine: E_n bar coproduct structures.

Tests are organized by mathematical content:

1. E_1 bar (tensor coalgebra): dimensions, coassociativity, non-cocommutativity
2. E_infty bar (symmetric coalgebra): dimensions, coassociativity, cocommutativity
3. Harrison / Lie cooperadic bar: necklace polynomial dimensions
4. Dimension comparisons and ratios
5. Explicit coproduct matrices at low arities
6. Verification of the Swiss-cheese structure
7. Cross-checks between different presentations
8. Heisenberg algebra comparison
9. The operadic Koszul duality dictionary
10. Multi-path verification of key formulas

Every test verifies from first principles.  No hardcoded values without
independent derivation (AP10).  Cross-family consistency is the real test (AP10).
"""

import pytest
exec
/bin/zsh -lc "sed -n '1438,1458p' compute/lib/tensor_harrison_bar_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '300,320p' compute/lib/two_colored_bar_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1028,1044p' compute/lib/factorization_coproduct_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '56,70p' compute/lib/sc_homotopy_koszul_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '538,552p' compute/tests/test_curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '640,650p' compute/tests/test_curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '32,55p' compute/lib/two_colored_bar_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
                'convolution algebra. By Milnor-Moore, the Lie structure is determined '
                'by the Harrison (primitive) part, but the MC element and higher '
                'L_infinity brackets access non-primitive components.'
            ),
        },
        'heisenberg_critical_finding': (
            'For Heisenberg (1 generator): Harrison bar is ZERO at arity >= 2. '
            'The kappa invariant at arity 2 lives in Sym^2 (weight-2 Eulerian), '
            'not in Lie^c_2 (weight-1 Eulerian = 0). The Harrison bar misses kappa entirely. '
            'This is not a problem because the manuscript uses Sym^c (the full geometric bar), '
            'not just the Harrison subcomplex.'
        ),
        'implications_for_sc_structure': (
            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
            'which carries cocommutative coassociative structure. This is compatible with '
            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
            'cocommutative coassociative).'
        ),
 succeeded in 51ms:

    The two coproducts are NOT two descriptions of the same thing.
    They are two DIFFERENT structures living on two DIFFERENT
    directions of the Swiss-cheese product:

    FM_k(C) x Conf_k^<(R)
      |                  |
      v                  v
    Bar differential   Deconcatenation
    (closed color)     coproduct (open color)
    Cocommutative      Non-cocommutative
    Factorization      E_1 coalgebra
    on Ran(X)          on ordered configs

    The unordered (factorization) coproduct on Ran(X) is the
    Sigma_k-symmetrization of the ordered (deconcatenation)
    coproduct.  Both are coassociative.  Only the ordered one
 succeeded in 51ms:
    Basis: e_11 (unit), e_12, e_22.
    e_11*e_12 = e_12, e_12*e_22 = e_12, e_22*e_22 = e_22.
    Augmentation: e_ij -> delta_{ij}.
    """
    basis = [
        BasisElement("e_11", 0),  # unit
        BasisElement("e_12", 0),
        BasisElement("e_22", 0),
    ]
    space = GradedSpace(basis, "UT_2")
    mult: Dict[Tuple[int, int], Vector] = {
        (0, 0): {0: Rational(1)},  # e_11^2 = e_11
        (0, 1): {1: Rational(1)},  # e_11*e_12 = e_12
        (0, 2): {2: Rational(1)},  # e_11*e_22 = 0? No: e_11*e_22 = 0
        (1, 2): {1: Rational(1)},  # e_12*e_22 = e_12
        (2, 2): {2: Rational(1)},  # e_22^2 = e_22
    }
    # Correct: e_11 is the unit, so e_11*anything = anything if it starts
    # with column 1. Actually for upper triangular:
    # e_11*e_11 = e_11, e_11*e_12 = e_12, e_11*e_22 = 0
 succeeded in 51ms:
  5. Euler characteristic consistency checks
  6. Transfer verification: Phi intertwines bar-cobar counits

CONSEQUENCES OF HOMOTOPY-KOSZULITY:
  - The bar-cobar adjunction B -| Omega for SC^{ch,top}-algebras is a
    Quillen equivalence (thm:bar-cobar-adjunction).
  - For any SC^{ch,top}-algebra A: Omega(B(A)) ~> A is a qi.
  - B(A) is the "right" bar construction: it computes the correct
    derived category of SC^{ch,top}-coalgebras.
  - The model structure on SC^{ch,top}-algebras is transferred from
    the projective model structure on chain complexes via the
    free-forgetful adjunction.

References:
  Ginzburg-Kapranov (1994): Koszul duality for operads
 succeeded in 51ms:

class TestComprehensiveAnalysis:
    """Test the full analysis pipeline for standard families."""

    def test_heisenberg_genus_0(self):
        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
        result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
        assert not result['is_curved']
        assert result['sc_type_fiberwise'] == 'dg'
        assert result['sc_type_corrected'] == 'dg'

 succeeded in 51ms:
     - Open part: B_{Ass}(C) -- the associative bar of C
     - Mixed: the action of C on itself by multiplication
   This is exactly what happens for chiral algebras: B^ch(A) is
   ALREADY an SC-coalgebra because the bar differential (closed, from
   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
   are independent structures on the SAME underlying tensor coalgebra.

4. THE TYPE-CHECKING RESOLUTION:
   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
     - d_C from FM_k(C): the closed structure
     - Delta from Conf_k(R): the open structure (deconcatenation)
   These COMMUTE (d_C is a coderivation of Delta) because
   FM_k(C) x Conf_k(R) is a product.

   The open-colour data is PRODUCED by the bar construction, not
   assumed as input. This is the content of thm:bar-swiss-cheese.

MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 succeeded in 51ms:
        )
        assert result['is_uniform_weight']
        assert result['delta_f2_vanishes']


# ============================================================================
# CLAIM 10: Modular tower structure
# ============================================================================

class TestModularTower:
    """Verify the genus tower of SC^{ch,top}-coalgebras."""

    def test_tower_curvatures_grow(self):
        """Curvature decreases with genus (lambda_fp decreases)."""
        tower = ModularSCTower(kappa=Fraction(1), genus_max=4)
exec
/bin/zsh -lc "rg -n \"implications_for_sc_structure|SC\\"'^'"\\{ch,top\\}-coalgebra structure lives on Sym\\"'^c|FM_k'"\\(C\\) x Conf_k\\"'^<'"\\(R\\)|closed color\\)     coproduct \\(open color\\)\" compute/tests compute/lib" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '930,980p' compute/lib/en_bar_coproduct_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '1,120p' compute/lib/factorization_coproduct_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
compute/lib/tensor_harrison_bar_engine.py:1450:        'implications_for_sc_structure': (
compute/lib/tensor_harrison_bar_engine.py:1451:            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
compute/lib/factorization_coproduct_engine.py:13:      the Swiss-cheese product FM_k(C) x Conf_k^<(R).
compute/lib/factorization_coproduct_engine.py:1033:    FM_k(C) x Conf_k^<(R)
compute/lib/factorization_coproduct_engine.py:1037:    (closed color)     coproduct (open color)
 succeeded in 51ms:
                'ratio_E1_Einf': Fraction(dim_e1, dim_einf) if dim_einf > 0 else None,
            }
        table[N] = row
    return table


# ================================================================
# 8. The key theorem: Swiss-cheese decomposition
# ================================================================

def swiss_cheese_analysis() -> Dict[str, object]:
    r"""Analyze the Swiss-cheese structure of the chiral bar complex.

    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
    - Coproduct Delta: from E_1 (topological, Conf_k(R))

    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
    SC is the Swiss-cheese operad.

 succeeded in 51ms:
r"""Factorization coproduct engine: first-principles investigation of the bar
complex coproduct.

FUNDAMENTAL QUESTION
====================
Where does the coassociative coproduct on the chiral bar complex ACTUALLY
come from?  The manuscript claims two origins:

  (A) Algebraic: deconcatenation on T^c(s^{-1} Abar), coassociative by
      construction of the tensor coalgebra.
  (B) Geometric: factorization structure from splitting of configuration
      spaces; or equivalently, interval splitting in the R-direction of
      the Swiss-cheese product FM_k(C) x Conf_k^<(R).

These are NOT the same coproduct in general.  This engine investigates
the relationship, identifies the precise origin, and tests predictions.

RESOLUTION (from source reading)
=================================
The manuscript contains TWO DIFFERENT bar complexes with TWO DIFFERENT

thinking
**Evaluating output accuracy**

I’m considering the implications of changing returned strings for other consumers. I might need to patch those outputs to ensure they’re truthful. For the top docstring and summary, there seems to be an internal comment that says, "This is exactly what happens for chiral algebras... ALREADY an SC-coalgebra." I'm thinking it could be better to clarify that this statement is false or represents an incorrect older picture. It’s essential to keep the information accurate for users!
exec
/bin/zsh -lc "rg -n \"def chiral_bar_summary|Swiss-cheese|closed \\(holomorphic\\) color|open \\(topological\\) color\" compute/lib/en_bar_coproduct_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
78:where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
83:(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
92:3. The Swiss-cheese operad naturally produces ordered bar complexes.
937:# 8. The key theorem: Swiss-cheese decomposition
941:    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
943:    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
948:    SC is the Swiss-cheese operad.
960:        'operad': 'SC^{ch,top} (Swiss-cheese)',
969:            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
970:            'the closed (holomorphic) color, coproduct from the open '
1122:def chiral_bar_summary() -> str:
1144:The Swiss-cheese operad SC^{ch,top} has two colors:
1170:  4. The modular data at higher genus (Swiss-cheese is essential)
exec
/bin/zsh -lc "sed -n '1120,1178p' compute/lib/en_bar_coproduct_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
# ================================================================

def chiral_bar_summary() -> str:
    r"""Summary of why the chiral bar has E_1 coproduct, not E_infty.

    This is the answer to the motivating question of this module.
    """
    return """
THE CHIRAL BAR COPRODUCT: A SUMMARY
====================================

Question: A chiral algebra A is E_infty-chiral (commutative OPE).
Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
coproduct rather than an E_infty (cocommutative) coproduct?

Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
construction B_{SC^{ch,top}}(A), where:

  - The DIFFERENTIAL uses E_2 (holomorphic) data:
    collision residues on Conf_k(C).

thinking
**Evaluating naming conventions**

I'm considering whether to keep field names while possibly excluding comments. If the module returns `'sc_type_fiberwise'`, it might not be accurate. I wonder if I should change the returned keys to `bar_type_fiberwise`, but that could break existing tests and caller functions. It might not be essential to address the claim if the values are altered. I think I’ll inspect the `analyze_curved_sc_structure` output and tests further before making decisions.
exec
/bin/zsh -lc "rg -n \"sc_type_fiberwise|sc_type_corrected|sc_structure_type|curved dg SC|corrected dg SC|SC\\"'^'"\\{ch,top\\}-coalgebras\" compute/lib/curved_sc_higher_genus_engine.py compute/tests/test_curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
compute/lib/curved_sc_higher_genus_engine.py:145:   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
compute/lib/curved_sc_higher_genus_engine.py:494:    def sc_structure_type(self) -> str:
compute/lib/curved_sc_higher_genus_engine.py:498:        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
compute/lib/curved_sc_higher_genus_engine.py:698:    """The genus tower of SC^{ch,top}-coalgebras.
compute/lib/curved_sc_higher_genus_engine.py:702:    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
compute/lib/curved_sc_higher_genus_engine.py:852:        'sc_type_fiberwise': 'curved_dg' if sc.is_curved else 'dg',
compute/lib/curved_sc_higher_genus_engine.py:853:        'sc_type_corrected': 'dg',
compute/tests/test_curved_sc_higher_genus_engine.py:431:    def test_sc_structure_type(self):
compute/tests/test_curved_sc_higher_genus_engine.py:433:        assert CorrectedSCCoalgebra(kappa=Fraction(1), genus=0).sc_structure_type == "classical_dg"
compute/tests/test_curved_sc_higher_genus_engine.py:434:        assert CorrectedSCCoalgebra(kappa=Fraction(1), genus=1).sc_structure_type == "corrected_dg"
compute/tests/test_curved_sc_higher_genus_engine.py:435:        assert CorrectedSCCoalgebra(kappa=Fraction(1), genus=3).sc_structure_type == "corrected_dg"
compute/tests/test_curved_sc_higher_genus_engine.py:548:    """Verify the genus tower of SC^{ch,top}-coalgebras."""
compute/tests/test_curved_sc_higher_genus_engine.py:648:        assert result['sc_type_fiberwise'] == 'dg'
compute/tests/test_curved_sc_higher_genus_engine.py:649:        assert result['sc_type_corrected'] == 'dg'
compute/tests/test_curved_sc_higher_genus_engine.py:656:        assert result['sc_type_fiberwise'] == 'curved_dg'
compute/tests/test_curved_sc_higher_genus_engine.py:657:        assert result['sc_type_corrected'] == 'dg'


thinking
**Analyzing SC-Style Interpretation**

I'm considering how the SC-style interpretation operates and its limitations. It seems like `Omega(B(A))` just reconstructs A, but the real SC datum appears to be located at `(C^*_ch(A,A), A)`. I want to understand what this means in practical terms. I'm curious about how this affects our understanding and application of these interpretations. I need to dig deeper to clarify the distinctions and implications further.
thinking
**Inspecting code fragments**

I want to address the E1 bar coalgebra and derived-center SC pair. The search results indicate I need to look at around 9 lines. It seems I can patch those line fragments but should inspect more closely around lines 110-145, 497, 602, and possibly others. I think using sed on lines 108-150 and 490-705 could help me make precise patches. I'll need to ensure I get this right!
codex
I’ve finished the classification pass. The edits are concentrated and doctrinally uniform: every false “`B(A)` is the Swiss-cheese coalgebra” sentence is being replaced by the same correction, namely “`B(A)` is the ordered `E_1` bar coalgebra; the `SC^{ch,top}` datum is the derived-center pair `(C^bullet_{ch}(A,A), A)`.”
exec
/bin/zsh -lc "sed -n '108,150p' compute/lib/curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '486,515p' compute/lib/curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '592,708p' compute/lib/curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
   - Delta is coassociative.
   - d is a coderivation of Delta.
   - d^2 = m_0 (the curvature) is the UNIQUE element such that d^2(x) = m_0 * x
     for the coaugmentation coideal.
   - The structure is governed by the CURVED bar complex formalism
     (Positselski, "Two kinds of derived categories, Koszul duality, and
     comodule-contramodule correspondence").
   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
   is a curved SC^{ch,top}-coalgebra.

5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
   h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
   because the holomorphic propagator obeys the exact Arnold relation at every
   genus (Fay trisecant identity). The expansion is:
       D^{(g)} = d_0 + sum_k t_k d_k
   where t_k are period coordinates on M_g and d_k are correction operators
   built from A-cycle integrals. Each d_k is a coderivation of Delta (period
   integrals commute with interval splitting). Therefore D^{(g)} is a
   coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
 succeeded in 51ms:
    def num_correction_operators(self) -> int:
        """Number of correction operators d_k.

        At genus g, there are g A-cycles, giving g correction operators.
        """
        return self.genus

    @property
    def sc_structure_type(self) -> str:
        """Classification of the SC structure.

        Genus 0: classical dg SC coalgebra (no corrections needed).
        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
        """
        if self.genus == 0:
            return "classical_dg"
        return "corrected_dg"

    def verify_three_propagator_models(self) -> Dict[str, Any]:
        """Verify the three-model comparison from thm:quantum-diff-squares-zero.
 succeeded in 52ms:
    return {
        'kappa': kappa,
        'curvature': curv,
        'lhs': lhs_terms,
        'rhs': rhs_terms,
        'discrepancy_at_1_1': discrepancy_11,
        'd2_is_coderivation': discrepancy_11 == Fraction(0),
        'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
        'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                 'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
                 'a coderivation, giving a genuine dg SC coalgebra.'),
    }


def heisenberg_arity_n_curvature_discrepancy(
    k: Fraction, n: int
) -> Dict[str, Any]:
    """Curvature coderivation discrepancy for [J|J|...|J] (n copies) at genus 1.

    For arity n, there are n+1 splitting positions i = 0, 1, ..., n.
exec
/bin/zsh -lc "sed -n '232,318p' compute/tests/test_curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '420,440p' compute/tests/test_curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '548,666p' compute/tests/test_curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
        assert sc.curvature == Fraction(0)
        assert not sc.is_curved

    def test_genus_1_heisenberg(self):
        """H_1 at genus 1: d^2 = 1 * 1/24 = 1/24."""
        sc = CurvedSCData(kappa=Fraction(1), genus=1, algebra_name="H_1")
        assert sc.curvature == Fraction(1, 24)
        assert sc.is_curved

    def test_genus_1_virasoro_c2(self):
        """Vir_2 at genus 1: d^2 = 1 * 1/24 = 1/24."""
        sc = CurvedSCData(kappa=Fraction(1), genus=1, algebra_name="Vir_2")
        assert sc.curvature == Fraction(1, 24)

    def test_genus_2_heisenberg(self):
        """H_1 at genus 2: d^2 = 1 * 7/5760 = 7/5760."""
        sc = CurvedSCData(kappa=Fraction(1), genus=2, algebra_name="H_1")
        assert sc.curvature == Fraction(7, 5760)

    def test_curvature_proportional_to_kappa(self):
 succeeded in 52ms:
        corr = CorrectedSCCoalgebra(kappa=Fraction(1), genus=1)
        models = corr.verify_three_propagator_models()
        assert models['curved_geometric']['d_squared'] == Fraction(1, 24)
        assert models['curved_geometric']['sc_type'] == 'curved_dg'

    def test_num_period_corrections(self):
        """At genus g, there are g correction operators (one per A-cycle)."""
        for g in range(5):
            corr = CorrectedSCCoalgebra(kappa=Fraction(1), genus=g)
            assert corr.num_correction_operators == g

    def test_sc_structure_type(self):
        """Genus 0: classical_dg. Genus >= 1: corrected_dg."""
        assert CorrectedSCCoalgebra(kappa=Fraction(1), genus=0).sc_structure_type == "classical_dg"
        assert CorrectedSCCoalgebra(kappa=Fraction(1), genus=1).sc_structure_type == "corrected_dg"
        assert CorrectedSCCoalgebra(kappa=Fraction(1), genus=3).sc_structure_type == "corrected_dg"


# ============================================================================
# CLAIM 8: SC mixed sector dimensions
 succeeded in 52ms:
    """Verify the genus tower of SC^{ch,top}-coalgebras."""

    def test_tower_curvatures_grow(self):
        """Curvature decreases with genus (lambda_fp decreases)."""
        tower = ModularSCTower(kappa=Fraction(1), genus_max=4)
        curvatures = [tower.curvature_at_genus(g) for g in range(1, 5)]
        for i in range(len(curvatures) - 1):
            assert curvatures[i] > curvatures[i + 1]

    def test_tower_all_corrected_flat(self):
        """D^{(g)}^2 = 0 at every genus in the tower."""
        tower = ModularSCTower(kappa=Fraction(1), genus_max=5)
        for g in range(6):
            assert tower.corrected_flat_at_genus(g)

    def test_full_differential_flat(self):
        """D_A^2 = 0 for the genus-completed differential."""
        tower = ModularSCTower(kappa=Fraction(1), genus_max=5)
        assert tower.full_differential_flat()

exec
/bin/zsh -lc "sed -n '1000,1048p' compute/lib/factorization_coproduct_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
    # Ran(X) is partially ordered by inclusion of finite subsets
    # The coproduct respects this: if S = S_1 sqcup S_2, then
    # |S_1| + |S_2| = |S|.

    return {
        'n': n,
        'factorization_coproduct_terms': unordered_partitions,
        'cocommutative': cocommutative,
        'source': 'union of finite sets on Ran(X)',
        'bar_complex_uses': 'this IS the unordered/factorization coproduct',
        'swiss_cheese_adds': 'the ordered/deconcatenation coproduct from R-direction',
        'explanation': (
            f"The factorization coalgebra on Ran(X) has coproduct induced by "
            f"splitting a finite subset S into disjoint parts S_1 sqcup S_2. "
            f"This is cocommutative (S_1 sqcup S_2 = S_2 sqcup S_1). "
            f"The Swiss-cheese structure adds a NON-cocommutative direction "
            f"from the ordered configurations in R."
        ),
    }

exec
/bin/zsh -lc "sed -n '1048,1088p' compute/lib/factorization_coproduct_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
    Verdier duality, Theorem A).  The deconcatenation is the
    TOPOLOGICAL structure (E_1 direction, interval splitting,
    Swiss-cheese theorem).

    The coderived category and Verdier intertwining use the
    FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
    theorem, the brace algebra structure, and the chiral center
    computation use the DECONCATENATION (non-cocommutative) coproduct.
    """
    return {
        'factorization_coproduct': (
            "Cocommutative, from Ran(X) factorization structure. "
            "Sums over ALL set partitions I sqcup J = S. "
            "Used in: Theorem A (Verdier intertwining), "
            "cobar construction, factorization homology."
        ),
        'deconcatenation_coproduct': (
            "Non-cocommutative, from E_1/interval splitting on R. "
            "Sums over consecutive cuts at positions 0,...,n. "
            "Used in: Swiss-cheese theorem, brace dg algebra, "
exec
/bin/zsh -lc "sed -n '176,184p' compute/audit/forgetful_e_infty_to_e1_report.md" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:

This is the content of the "Steinberg principle" (Vol II preface, line 72):

> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.

And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
- Closed-colour projection: quantum group (Latyntsev factorization quantum group)
- Open-colour projection: Yangian (dg-shifted)
- Mixed projection: R-matrix (the DK-0 bridge)
exec
/bin/zsh -lc "sed -n '832,860p' compute/lib/curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
    - Corrected differential properties
    - SC sector classification
    - Multi-weight correction status
    """
    if weights is None:
        weights = [2] * num_generators  # default: weight 2 (Virasoro)

    sc = CurvedSCData(kappa=kappa, genus=genus, algebra_name=algebra_name)
    corrected = CorrectedSCCoalgebra(kappa=kappa, genus=genus,
                                     algebra_name=algebra_name)

    is_uniform = len(set(weights)) <= 1

    result = {
        'algebra': algebra_name,
        'kappa': kappa,
        'genus': genus,
        'fiberwise_curvature': sc.curvature,
        'is_curved': sc.is_curved,
        'corrected_is_flat': corrected.is_flat,

thinking
**Considering documentation updates**

I'm thinking we should add comments in the docstring of the analyze function to clarify legacy keys. To do this, I need to inspect the top docstring of the function first. I can check around line 820 to find the relevant information. This way, the documentation will be clearer and more helpful for anyone looking at the code later. It’s all about ensuring clarity and making it easier for others!
exec
/bin/zsh -lc "sed -n '812,840p' compute/lib/curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
                   'cross-channel correction generically nonzero'),
    }


# ============================================================================
# 8. COMPREHENSIVE FAMILY ANALYSIS
# ============================================================================

def analyze_curved_sc_structure(
    algebra_name: str,
    kappa: Fraction,
    genus: int,
    num_generators: int = 1,
    weights: Optional[List[int]] = None,
) -> Dict[str, Any]:
    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.

    Returns a comprehensive dictionary with:
    - Curvature data
    - Coderivation verification

thinking
**Updating documentation clarity**

I need to add a patch docstring for better clarity and possibly keep the legacy key names while mentioning them. I should also check the docstrings in the tests module and the relevant class docstrings. It might be worth looking into `compute/lib/two_colored_bar_engine.py` along with its tests to ensure everything aligns. Plus, I want to inspect if the output values might need any updates, especially at the end of the search function body.
exec
/bin/zsh -lc "sed -n '1082,1134p' compute/lib/two_colored_bar_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.

    This is a conceptual function documenting the type-checking
    resolution of the fundamental question.

    For a chiral algebra A (= Com^ch-algebra), the bar construction
    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:

    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
       = CLOSED colour structure
    2. A coassociative coalgebra via Delta (deconcatenation)
       = OPEN colour structure
    3. d_C is a coderivation of Delta
       = the SC^{ch,top}-coalgebra compatibility

    The key insight: the bar construction does NOT require a second
    (open-colour) input. It PRODUCES the two-coloured structure from
    a one-coloured input. The open-colour data (deconcatenation) is
    a FORMAL CONSEQUENCE of the tensor coalgebra structure.

exec
/bin/zsh -lc "rg -n \"SC\\"'^'"\\{ch,top\\} coalgebra|SC coalgebra structure on B\\(A\\)|bar complex B\\(A\\).*derived center|open-color E_1 structure on the bar complex\" compute/lib/theorem_swiss_cheese_kontsevich_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
25:     and open-color E_1 structure on the bar complex, verifying that the
26:     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
306:    # More precisely: the SC coalgebra structure on B(A) means
exec
/bin/zsh -lc "rg -n \"SC bar|SC\\"'^{ch,top} version|SC cobar|SC'"\\"'^{ch,top}-algebra|B_SC|full SC structure|SC analysis" compute/lib/sc_bar_cobar_inversion_engine.py compute/tests/test_sc_bar_cobar_inversion_engine.py' in /Users/raeez/chiral-bar-cobar exited 2 in 52ms:
rg: regex parse error:
    (?:SC bar|SC\^{ch,top} version|SC cobar|SC\^{ch,top}-algebra|B_SC|full SC structure|SC analysis)
                   ^
error: repetition quantifier expects a valid decimal
exec
/bin/zsh -lc "rg -n -e 'SC bar' -e 'SC"'^{ch,top} version'"' -e 'SC cobar' -e 'SC"'^{ch,top}-algebra'"' -e 'B_SC' -e 'full SC structure' -e 'SC analysis' compute/lib/sc_bar_cobar_inversion_engine.py compute/tests/test_sc_bar_cobar_inversion_engine.py" in /Users/raeez/chiral-bar-cobar exited 2 in 51ms:
rg: regex parse error:
    (?:SC bar)|(?:SC^{ch,top} version)|(?:SC cobar)|(?:SC^{ch,top}-algebra)|(?:B_SC)|(?:full SC structure)|(?:SC analysis)
                      ^
error: repetition quantifier expects a valid decimal

thinking
**Checking syntax requirements**

I realize I need to escape braces in my code. It seems like a simple detail, but it’s crucial for the syntax to work properly. I don’t want to make mistakes that could lead to errors later on. I’m taking a moment to double-check my code to ensure I’m handling all the necessary escape sequences correctly. It’s always good to be thorough when it comes to coding!
exec
/bin/zsh -lc "rg -n -e 'SC bar' -e 'SC\\"'^'"\\{ch,top\\} version' -e 'SC cobar' -e 'SC\\"'^'"\\{ch,top\\}-algebra' -e 'B_SC' -e 'full SC structure' -e 'SC analysis' compute/lib/sc_bar_cobar_inversion_engine.py compute/tests/test_sc_bar_cobar_inversion_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
compute/tests/test_sc_bar_cobar_inversion_engine.py:1:r"""Tests for SC bar-cobar inversion engine: Omega(B^ch(A)) recovers A.
compute/tests/test_sc_bar_cobar_inversion_engine.py:10:  Path 3 (SC analysis):    SC cobar recovers closed colour, NOT full SC
compute/tests/test_sc_bar_cobar_inversion_engine.py:59:    # SC analysis
compute/tests/test_sc_bar_cobar_inversion_engine.py:348:    Key result: the SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour
compute/tests/test_sc_bar_cobar_inversion_engine.py:349:    algebra A, NOT the full SC^{ch,top}-algebra (A, A^!).
compute/tests/test_sc_bar_cobar_inversion_engine.py:353:        """SC cobar recovers closed colour for Heisenberg."""
compute/tests/test_sc_bar_cobar_inversion_engine.py:358:        """SC cobar does NOT recover open colour for Heisenberg."""
compute/tests/test_sc_bar_cobar_inversion_engine.py:363:        """SC cobar does NOT recover full SC algebra for Heisenberg."""
compute/tests/test_sc_bar_cobar_inversion_engine.py:368:        """SC cobar recovers closed colour for sl_2."""
compute/tests/test_sc_bar_cobar_inversion_engine.py:373:        """SC cobar does NOT recover open colour for sl_2."""
compute/tests/test_sc_bar_cobar_inversion_engine.py:378:        """Full SC analysis: three functors are distinct."""
compute/lib/sc_bar_cobar_inversion_engine.py:1:r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
compute/lib/sc_bar_cobar_inversion_engine.py:16:  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
compute/lib/sc_bar_cobar_inversion_engine.py:45:  SC^{ch,top} version:
compute/lib/sc_bar_cobar_inversion_engine.py:47:    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
compute/lib/sc_bar_cobar_inversion_engine.py:656:# V. Swiss-cheese version: SC cobar
compute/lib/sc_bar_cobar_inversion_engine.py:664:    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
compute/lib/sc_bar_cobar_inversion_engine.py:668:    The SC cobar Omega_SC(B^ch(A)):
compute/lib/sc_bar_cobar_inversion_engine.py:687:    """Analyze what the SC cobar recovers.
compute/lib/sc_bar_cobar_inversion_engine.py:689:    For any SC^{ch,top}-algebra (A, A_boundary):

thinking
**Preparing for patch verification**

I need to run a command to check for any remaining hits and identify external blocks after applying a patch. But first, I should patch the current repository, probably affecting around 15 files. It's important to be precise with this process. I want to include line numbers in the final output, especially since they might change after the patch. I can refer to the current line numbers using a specific command later. I'll draft the patch application carefully and ensure everything is grammatically sound.
codex
I’m applying the fixes now. The edits are small but numerous because the same bad AP165 slogan had propagated into theorem summaries, concordance prose, compute docstrings, and prior audit notes.

thinking
**Considering patching strategy**

I'm facing a patch failure due to a duplicate update file and possibly an empty hunk. I realize I need to redo it carefully, applying smaller patches since the process can't happen in parallel. I should focus on manageable chunks and ensure there are no duplicate file updates. To make it easier, I'll split the patches by groups, starting with the chapters and some compute files. This approach feels more organized!
codex
The first patch attempt was too wide for a single hunk set. I’m splitting it into smaller atomic patches so the edits remain readable and easier to verify against the exact AP165 surface.
file update
M /Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex
@@ -6290,8 +6290,11 @@
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
M /Users/raeez/chiral-bar-cobar/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -145,8 +145,12 @@
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
M /Users/raeez/chiral-bar-cobar/chapters/theory/bar_construction.tex
@@ -2053,5 +2053,9 @@
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
M /Users/raeez/chiral-bar-cobar/chapters/connections/concordance.tex
@@ -3834,5 +3834,7 @@
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
apply_patch(auto_approved=true) exited 0 in 64ms:
Success. Updated the following files:
M chapters/theory/en_koszul_duality.tex
M chapters/theory/bar_cobar_adjunction_curved.tex
M chapters/connections/concordance.tex
M chapters/theory/bar_construction.tex
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..a8a1e9d76026aaa6c8bee5b46b8af587ca5d8b99
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential

file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..a8a1e9d76026aaa6c8bee5b46b8af587ca5d8b99
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential

file update
M /Users/raeez/chiral-bar-cobar/compute/lib/en_bar_coproduct_engine.py
@@ -71,18 +71,16 @@
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
@@ -940,10 +938,12 @@
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
@@ -959,3 +959,3 @@
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
@@ -968,5 +968,6 @@
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
@@ -1134,4 +1135,4 @@
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
@@ -1143,5 +1144,5 @@
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
@@ -1169,3 +1170,4 @@
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
M /Users/raeez/chiral-bar-cobar/compute/lib/tensor_harrison_bar_engine.py
@@ -1450,8 +1450,7 @@
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
M /Users/raeez/chiral-bar-cobar/compute/lib/two_colored_bar_engine.py
@@ -33,18 +33,21 @@
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
@@ -1081,3 +1084,3 @@
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
@@ -1086,22 +1089,18 @@
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
@@ -1114,5 +1113,5 @@
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
@@ -1123,6 +1122,5 @@
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
M /Users/raeez/chiral-bar-cobar/compute/lib/factorization_coproduct_engine.py
@@ -1035,7 +1035,6 @@
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
@@ -1048,4 +1047,7 @@
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
apply_patch(auto_approved=true) exited 0 in 64ms:
Success. Updated the following files:
M compute/lib/two_colored_bar_engine.py
M compute/lib/en_bar_coproduct_engine.py
M compute/lib/factorization_coproduct_engine.py
M compute/lib/tensor_harrison_bar_engine.py
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..a8a1e9d76026aaa6c8bee5b46b8af587ca5d8b99
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 

file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..a8a1e9d76026aaa6c8bee5b46b8af587ca5d8b99
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 

file update
M /Users/raeez/chiral-bar-cobar/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -24,4 +24,5 @@
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
@@ -305,7 +306,6 @@
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
M /Users/raeez/chiral-bar-cobar/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,2 +1,2 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
@@ -15,3 +15,4 @@
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
@@ -44,8 +45,8 @@
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
@@ -655,3 +656,3 @@
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
@@ -661,5 +662,5 @@
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
@@ -667,7 +668,7 @@
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
@@ -686,19 +687,11 @@
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
@@ -716,8 +709,10 @@
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
apply_patch(auto_approved=true) exited 0 in 73ms:
Success. Updated the following files:
M compute/lib/theorem_swiss_cheese_kontsevich_engine.py
M compute/lib/sc_bar_cobar_inversion_engine.py
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..a8a1e9d76026aaa6c8bee5b46b8af587ca5d8b99
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 

file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..a8a1e9d76026aaa6c8bee5b46b8af587ca5d8b99
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 

file update
M /Users/raeez/chiral-bar-cobar/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,5 +1,6 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
@@ -114,4 +115,6 @@
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
@@ -126,3 +129,3 @@
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
@@ -138,10 +141,11 @@
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
@@ -160,3 +164,3 @@
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
@@ -322,3 +326,3 @@
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
@@ -328,3 +332,5 @@
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
@@ -459,3 +465,3 @@
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
@@ -468,3 +474,5 @@
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
@@ -494,6 +502,6 @@
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
@@ -601,3 +609,3 @@
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
@@ -697,14 +705,15 @@
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
@@ -826,3 +835,3 @@
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
@@ -832,3 +841,3 @@
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
M /Users/raeez/chiral-bar-cobar/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,5 +1,6 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
@@ -547,3 +548,3 @@
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
@@ -644,3 +645,3 @@
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
apply_patch(auto_approved=true) exited 0 in 86ms:
Success. Updated the following files:
M compute/lib/curved_sc_higher_genus_engine.py
M compute/tests/test_curved_sc_higher_genus_engine.py
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..a8a1e9d76026aaa6c8bee5b46b8af587ca5d8b99
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..a8a1e9d76026aaa6c8bee5b46b8af587ca5d8b99
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'


thinking
**Planning document updates**

I’m considering patching more test docstrings in `test_curved`, especially if the search still hits. It seems like only line 645 might need changes for now. Later, I should also patch audit docs. I might need to update the current repo for the `compute/audit` note and the `factorization_bar_literature_report` too. There might be fixes needed in those paragraphs and also in line 179 of the report. Plus, I must remember line 80 in the forgetful report for corrections.
file update
M /Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -79,3 +79,3 @@
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
@@ -167,10 +167,11 @@
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
@@ -178,3 +179,3 @@
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
M /Users/raeez/chiral-bar-cobar/compute/audit/factorization_bar_literature_report.md
@@ -201,9 +201,11 @@
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
apply_patch(auto_approved=true) exited 0 in 96ms:
Success. Updated the following files:
M compute/audit/forgetful_e_infty_to_e1_report.md
M compute/audit/factorization_bar_literature_report.md
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..a8a1e9d76026aaa6c8bee5b46b8af587ca5d8b99
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..e7ed1e7ea22e9775de3a06bab980e1258828a7e9
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,13 +199,15 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..a02b96991cd3d4282b0e128d705b9ce09d841de5
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..a8a1e9d76026aaa6c8bee5b46b8af587ca5d8b99
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..e7ed1e7ea22e9775de3a06bab980e1258828a7e9
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,13 +199,15 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..a02b96991cd3d4282b0e128d705b9ce09d841de5
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'


thinking
**Assessing patch requirements**

I’m thinking about whether I need to patch the `test_sc_bar_cobar_inversion_engine` docstring. It's not strictly necessary, but I wonder if it would be okay to do it anyway. Maybe I'll search the current repository again for any false claim patterns to ensure everything is accurate. This feels like a good step to take, even if it might not be absolutely essential. Just want to be thorough!
codex
The core fixes are in. I’m running a post-edit hostile grep now to catch stragglers in the current repo, especially residual “closed color/open color on `B(A)`” formulations in compute modules and theorem summaries.
exec
/bin/zsh -lc "rg -n -g '"'!archive/**'"' -g '"'!raeeznotes/**'"' -g '"'!healing_*/**'"' -g '"'!rectification_*/**'"' -g '"'!fix_wave_*/**'"' -g '"'!*.bak*'"' -e 'B\\(A\\) is SC coalgebra' -e 'SC\\"'^'"\\{ch,top\\}-coalgebra' -e 'Swiss-cheese coalgebra' -e 'coalgebra over SC' -e 'bar complex presents the Swiss-cheese' -e 'bar differential is the closed' -e 'bar coproduct is the open' -e 'closed color\\)     coproduct \\(open color\\)' -e 'closed colour structure' -e 'open colour structure' chapters compute/lib compute/tests compute/audit README.md CLAUDE.md AGENTS.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc 'pytest -q compute/tests/test_en_bar_coproduct_engine.py compute/tests/test_two_colored_bar_engine.py compute/tests/test_curved_sc_higher_genus_engine.py compute/tests/test_sc_bar_cobar_inversion_engine.py' in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
AGENTS.md:140:**SC^{ch,top} is NOT on B(A) (AP165).** B(A) is E_1 coassociative coalgebra. SC^{ch,top} lives on the pair (C^bullet_{ch}(A,A), A). FORBIDDEN: "B(A) is a coalgebra over SC^{ch,top}"; "the bar differential is the closed color"; "the bar coproduct is the open color."
AGENTS.md:287:B54. "B(A) is SC coalgebra"            # FALSE: E_1 coalgebra; SC in derived center pair
CLAUDE.md:375:- B54. `B(A)` is a coalgebra over `SC^{ch,top}` / "the bar complex presents the Swiss-cheese algebra." FALSE. B(A) is an E_1 coassociative coalgebra. SC^{ch,top} emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). AP165. Regex: `bar.*presents.*Swiss|coalgebra over.*\\SCchtop|\\SCchtop.*coalgebra`
CLAUDE.md:376:- B55. "The bar differential is the closed color" / "the bar coproduct is the open color." FALSE. d_B and Delta make B(A) an E_1 dg coalgebra, NOT an SC two-color datum. AP165.
CLAUDE.md:467:**FM25. The SC^{ch,top} disaster of 2025-2026 (Opus 4.6 structural confabulation).** Opus constructed an elaborate but FALSE framework claiming B(A) is an SC^{ch,top}-coalgebra. The chain of errors:
CLAUDE.md:469:(1) WRONG: "The bar differential is the closed/holomorphic color of SC." TRUTH: The bar differential d_B comes from the chiral product (OPE residues at FM boundary strata). It is the differential of a dg coalgebra over (ChirAss)^!, NOT a "color" of SC. Having operations that USE FM_k(C) geometry does not make d_B an "E_2 color" — it is a single degree-1 map, not a parametrized family of operations.
CLAUDE.md:920:AP165: B(A) is NOT an SC^{ch,top}-coalgebra. The bar complex B(A) = T^c(s^{-1} A-bar) is an E_1 chiral coassociative coalgebra (differential from chiral product, deconcatenation coproduct). It is a SINGLE E_1 coalgebra, not a two-colored SC datum. The SC^{ch,top} structure emerges in the chiral derived center: the chiral Hochschild cochain complex C^bullet_{ch}(A,A) (defined via the chiral endomorphism operad End^{ch}_A with spectral parameters from FM_k(C), NOT topological Hochschild cochains RHom_{A^e}(A,A)) carries brace operations and a Gerstenhaber bracket, and the pair (C^bullet_{ch}(A,A), A) is the SC^{ch,top} datum (bulk acting on boundary). FORBIDDEN claims: "B(A) is a coalgebra over SC^{ch,top}"; "the bar complex presents the Swiss-cheese algebra"; "the bar differential is the closed color"; "the bar coproduct is the open color." COUNTER: after writing any sentence involving B(A) and SC^{ch,top} in the same paragraph, verify that SC is attributed to the derived center pair, not to B(A).
compute/audit/forgetful_e_infty_to_e1_report.md:43:> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
compute/audit/forgetful_e_infty_to_e1_report.md:112:#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
compute/audit/forgetful_e_infty_to_e1_report.md:116:Omega : (conilpotent SC^{ch,top}-coalgebras) ⇄ (SC^{ch,top}-algebras) : B
compute/audit/forgetful_e_infty_to_e1_report.md:120:The bar functor B is the RIGHT adjoint. Given an SC^{ch,top}-algebra (A, M) (a two-coloured pair: closed A, open M), B(A,M) is the universal conilpotent SC^{ch,top}-coalgebra coaugmented over (A,M). For a single chiral algebra A, promotion A -> (A, A) is required (AP86). The bar complex B(A) of a single algebra is an E_1 chiral coassociative coalgebra over (ChirAss)^!; the SC structure lives on the derived center pair (C^bullet_ch(A,A), A) (AP165). The Quillen equivalence means B and Omega are mutually inverse on the homotopy category.
compute/audit/forgetful_e_infty_to_e1_report.md:126:The left side is **conilpotent SC^{ch,top}-coalgebras**. These are coalgebras over the Koszul dual cooperad of SC^{ch,top}, which means:
compute/audit/codex_survey_v2_math_review_wave14.md:101:#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
compute/audit/codex_survey_v2_math_review_wave14.md:103:- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
compute/audit/codex_survey_v2_math_review_wave14.md:104:- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
compute/audit/factorization_bar_literature_report.md:218:topological).  Together they form an SC^{ch,top}-coalgebra.
compute/audit/factorization_bar_literature_report.md:333:Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
compute/audit/adversarial_swarm_findings.md:14:- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json:724:      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
compute/audit/deformation_quantization_sc_report.md:119:- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
 succeeded in 1.00s:
........................................................................ [ 21%]
........................................................................ [ 43%]
........................................................................ [ 65%]
........................................................................ [ 87%]
...........................................                              [100%]
331 passed in 0.75s
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..e800c2b4fd7faee955b23d22e1d923eecc44d221
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..e7ed1e7ea22e9775de3a06bab980e1258828a7e9
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,13 +199,15 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..a02b96991cd3d4282b0e128d705b9ce09d841de5
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

codex
The post-edit grep passed on the live theorem/compute surfaces I changed, and the targeted tests are green. The remaining matches inside the current repo are mostly audit artifacts and historical quotes, so I’m doing one last cleanup pass there where the false slogan still appears without an explicit “superseded by AP165” guard.
exec
/bin/zsh -lc "sed -n '36,50p' compute/audit/forgetful_e_infty_to_e1_report.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '108,128p' compute/audit/forgetful_e_infty_to_e1_report.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '214,224p' compute/audit/factorization_bar_literature_report.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '114,122p' compute/audit/deformation_quantization_sc_report.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '328,338p' compute/audit/factorization_bar_literature_report.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "sed -n '10,18p' compute/audit/adversarial_swarm_findings.md" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:

Vol II is precise about the distinction. Construction constr:bar-fact-coalgebra (line 793) states:

> **Bar coproduct.** The coassociative coproduct Delta is the factorization isomorphism dualized: splitting a configuration S = S_1 ⊔ S_2 into disjoint subsets corresponds to **ordered deconcatenation** [a_1|...|a_n] -> sum_{i=0}^n [a_1|...|a_i] ⊗ [a_{i+1}|...|a_n].

And then the key identification (line 955-958):

> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.

#### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)

> The bar complex carries two structures: a **differential** d_B from OPE residues on FM_k(C), encoding the holomorphic chiral product, and a **coproduct** Delta from ordered deconcatenation on Conf_k(R), encoding the topological interval-cutting. The differential lives in the C-direction; the coproduct lives in the R-direction.

---

exec
/bin/zsh -lc "sed -n '720,728p' compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
- Mixed operations: closed acts on open (bulk-to-boundary), but NOT open-to-closed

The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.

#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?

**Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
```
Omega : (conilpotent SC^{ch,top}-coalgebras) ⇄ (SC^{ch,top}-algebras) : B
```
This is a Quillen equivalence when SC^{ch,top} is homotopy-Koszul (which is PROVED: Theorem thm:homotopy-Koszul via Kontsevich formality + transfer from classical Swiss-cheese Koszulity).

The bar functor B is the RIGHT adjoint. Given an SC^{ch,top}-algebra (A, M) (a two-coloured pair: closed A, open M), B(A,M) is the universal conilpotent SC^{ch,top}-coalgebra coaugmented over (A,M). For a single chiral algebra A, promotion A -> (A, A) is required (AP86). The bar complex B(A) of a single algebra is an E_1 chiral coassociative coalgebra over (ChirAss)^!; the SC structure lives on the derived center pair (C^bullet_ch(A,A), A) (AP165). The Quillen equivalence means B and Omega are mutually inverse on the homotopy category.

At genus 0, this is an honest Quillen equivalence. At genus g >= 1, the bar complex is curved (d^2 = kappa * omega_g), and cobar reconstruction is replaced by the Feynman transform FT, with FT^2 ≃ id providing the involutivity.

#### Q7: What type of coalgebra appears on the left side of the adjunction?

The left side is **conilpotent SC^{ch,top}-coalgebras**. These are coalgebras over the Koszul dual cooperad of SC^{ch,top}, which means:
- They carry a coassociative (E_1) coproduct from the open colour
 succeeded in 50ms:
(Vol II, `introduction.tex` lines 204-207).

The differential is the E_infty/E_2 structure (symmetric,
holomorphic).  The coproduct is the E_1 structure (ordered,
topological).  Together they form an SC^{ch,top}-coalgebra.

### Why the E_1 bar is preferred

The ordered bar B^ord(A) carries STRICTLY MORE information than the
symmetric bar B^Sigma(A).  The descent map B^ord -> B^Sigma takes
Sigma_n-coinvariants, losing information.  The lost information is
exec
/bin/zsh -lc "sed -n '96,106p' compute/audit/codex_survey_v2_math_review_wave14.md" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
- The E_infty bar B^Sigma(A) is the closed-colour factorization
  coalgebra on Ran(X)
- The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
  coproduct is the open-colour coalgebra

Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
structure is not an ansatz but a theorem: given the holomorphic
differential, the topological coproduct is forced by the universal
property of the cofree coalgebra.

### Key compatibility statement:
 succeeded in 52ms:

The proof uses E_2 formality + the geometric = operadic bar identification (Theorem `thm:geometric-equals-operadic-bar`). The higher-genus extension is conjectural (Remark `rem:chiral-quantization-higher-genus`): it requires controlling global obstructions in H^2 of the chiral Hochschild complex on M-bar_{g,n}.

**The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
- The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.

**But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.

 succeeded in 51ms:
- Why it's wrong/imprecise/overclaimed: the proved theorem only gives equivalence on the fiber over `\barBgeom(\cA)`; the survey’s wording sounds like an unconditional global equivalence.
- Independent verification: `chapters/theory/chiral_koszul_pairs.tex:1931-1933` states item (vi) as equivalence “on the fiber over `\barBgeom(\cA)`.”
- Suggested fix: restore the fiber/Koszul-locus qualifier.

### Section 10: The open/closed world (Volume II)
#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
- Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
- Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.

 succeeded in 52ms:
    },
    {
      "count": 2,
      "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
    },
    {
      "count": 2,
      "instances": "appendices/ordered_associative_chiral_kd.tex:4188 || chapters/theory/ordered_associative_chiral_kd.tex:3896",
 succeeded in 52ms:
### Dirac: The four-stage architecture is NOT minimal
- Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
- Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
- Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
- The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.

### Dirac: The chapter is 25-30% scaffolding
- 21 remarks outnumber theorems (8) by factor 2.6.
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..9e3ded0742ff7c59d9bd83af8f1b925e442009c6
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..e7ed1e7ea22e9775de3a06bab980e1258828a7e9
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,13 +199,15 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..a02b96991cd3d4282b0e128d705b9ce09d841de5
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'


thinking
**Patching audit documents**

I need to consider whether to patch the audit docs. The `codex_survey_v2_math_review_wave14.md` document points out a wrong claim about local statements being Swiss-cheese coalgebra, even after AP165—so a patch seems necessary. 

The `qc_math_surfaces.json` mentions normalized_prefix in source files, which could still contain incorrect claims. I should inspect the actual source files to confirm. It references a specific line in the `chapters/theory/ordered_associative_chiral_kd.tex`, so I’ll need to verify that too.
exec
/bin/zsh -lc "sed -n '3868,3899p' chapters/theory/ordered_associative_chiral_kd.tex" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
\begin{align*}
A &= [[X_2,[X_1,X_c]],X_3]
 = [X_2,[[X_1,X_c],X_3]] - [[X_1,X_c],[X_2,X_3]]\\
 &= [X_2,[[X_1,X_c],X_3]]
 \qquad(\text{since }[X_2,X_3]=0)\\
 &= [X_2,[X_1,[X_c,X_3]]] - [X_2,[X_c,[X_1,X_3]]]\\
 &= [X_2,[X_1,[X_c,X_3]]]
 \qquad(\text{since }[X_1,X_3]=0)\\
 &= [[X_2,X_1],[X_c,X_3]] + [X_1,[X_2,[X_c,X_3]]]\\
 &= [X_1,[X_2,[X_c,X_3]]]
 \qquad(\text{since }[X_1,X_2]=0).
\end{align*}
The identical computation with $2\leftrightarrow 3$ gives
$B=[X_1,[X_3,[X_c,X_2]]]$. By~\eqref{eq:jacobi-star-v1},
$[X_2,[X_c,X_3]]=[X_3,[X_c,X_2]]$, hence $A=B$.
\end{proof}

\begin{remark}[Root multiplicity one as structural inevitability]
The Jacobi collapse lemma is not a clever computation that
happens to kill the obstruction. The point is that the obstruction
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..9e3ded0742ff7c59d9bd83af8f1b925e442009c6
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..e7ed1e7ea22e9775de3a06bab980e1258828a7e9
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,13 +199,15 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..a02b96991cd3d4282b0e128d705b9ce09d841de5
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'


thinking
**Checking line relevance**

I noticed that I need to address line 3874 which seems irrelevant. It’s possible that the file has changed or that the JSON is stale. I should search for the actual phrase to confirm its relevance. Understanding the context is key; I want to make sure I’m looking at the most current information possible. Let’s focus on retrieving the accurate details so that I can effectively update or discard what's unnecessary.
exec
/bin/zsh -lc 'rg -n "presents the Swiss-cheese coalgebra|Swiss-cheese coalgebra" chapters/theory/ordered_associative_chiral_kd.tex appendices/ordered_associative_chiral_kd.tex' in /Users/raeez/chiral-bar-cobar exited 1 in 52ms:

file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..9e3ded0742ff7c59d9bd83af8f1b925e442009c6
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..e7ed1e7ea22e9775de3a06bab980e1258828a7e9
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,13 +199,15 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..a02b96991cd3d4282b0e128d705b9ce09d841de5
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'


thinking
**Exploring patching options**

I’m considering patching the generated metadata because it might mislead searches. I need to check if there are multiple identical entries, particularly focusing on the second entry. Maybe I can search for a normalized prefix, which could have counts of 2. Should I patch both instances? I think using a search/replace string might work for one instance. I definitely want to explore if there's more here. Let's keep searching!
exec
/bin/zsh -lc "rg -n 'presents the Swiss-cheese coalgebra|Swiss-cheese coalgebra|SC\\"'^{ch,top}-coalgebra structure on B'"\\"'^ch'"\\(A\\)|including bar complexes' compute/audit" in /Users/raeez/chiral-bar-cobar exited 2 in 52ms:
rg: regex parse error:
    (?:presents the Swiss-cheese coalgebra|Swiss-cheese coalgebra|SC\^{ch,top}-coalgebra structure on B\^ch\(A\)|including bar complexes)
                                                                       ^
error: repetition quantifier expects a valid decimal
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..9e3ded0742ff7c59d9bd83af8f1b925e442009c6
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..e7ed1e7ea22e9775de3a06bab980e1258828a7e9
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,13 +199,15 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..a02b96991cd3d4282b0e128d705b9ce09d841de5
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

exec
/bin/zsh -lc "rg -n -e 'presents the Swiss-cheese coalgebra' -e 'Swiss-cheese coalgebra' -e 'SC\\"'^'"\\{ch,top\\}-coalgebra structure on B\\"'^ch'"\\(A\\)' -e 'including bar complexes' compute/audit" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
compute/audit/forgetful_e_infty_to_e1_report.md:43:> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
compute/audit/forgetful_e_infty_to_e1_report.md:112:#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json:724:      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
compute/audit/deformation_quantization_sc_report.md:119:- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
compute/audit/codex_survey_v2_math_review_wave14.md:101:#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
compute/audit/codex_survey_v2_math_review_wave14.md:103:- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
compute/audit/codex_survey_v2_math_review_wave14.md:104:- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..9e3ded0742ff7c59d9bd83af8f1b925e442009c6
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..e7ed1e7ea22e9775de3a06bab980e1258828a7e9
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,13 +199,15 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..a02b96991cd3d4282b0e128d705b9ce09d841de5
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'


thinking
**Searching for SC coalgebra references**

I need to search for the lines in the factorization_bar_literature_report that include the SC coalgebra phrase. It seems like it might not be captured due to the lowercase formatting. I'll look for that separately to ensure I don’t miss anything important. This way, I can confirm I’m getting all relevant references and details. Let's make sure to be thorough with this!
exec
/bin/zsh -lc "rg -n -e 'form an SC\\"'^'"\\{ch,top\\}-coalgebra' -e 'Together they form an SC\\"'^'"\\{ch,top\\}-coalgebra' compute/audit/factorization_bar_literature_report.md" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
218:topological).  Together they form an SC^{ch,top}-coalgebra.
333:Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..9e3ded0742ff7c59d9bd83af8f1b925e442009c6
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..e7ed1e7ea22e9775de3a06bab980e1258828a7e9
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,13 +199,15 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..a02b96991cd3d4282b0e128d705b9ce09d841de5
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

exec
/bin/zsh -lc "rg -n 'coalgebra over SC' compute/audit/adversarial_swarm_findings.md" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
14:- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..9e3ded0742ff7c59d9bd83af8f1b925e442009c6
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..e7ed1e7ea22e9775de3a06bab980e1258828a7e9
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,13 +199,15 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..a02b96991cd3d4282b0e128d705b9ce09d841de5
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

file update
M /Users/raeez/chiral-bar-cobar/compute/audit/deformation_quantization_sc_report.md
@@ -118,3 +118,3 @@
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
M /Users/raeez/chiral-bar-cobar/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -723,3 +723,3 @@
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
@@ -1480,2 +1480,2 @@
   ]
-}
\ No newline at end of file
+}
M /Users/raeez/chiral-bar-cobar/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -100,6 +100,6 @@
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
M /Users/raeez/chiral-bar-cobar/compute/audit/adversarial_swarm_findings.md
@@ -13,3 +13,3 @@
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
M /Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -42,3 +42,3 @@
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
@@ -111,3 +111,3 @@
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
M /Users/raeez/chiral-bar-cobar/compute/audit/factorization_bar_literature_report.md
@@ -215,5 +215,6 @@
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
@@ -332,6 +333,7 @@
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
apply_patch(auto_approved=true) exited 0 in 138ms:
Success. Updated the following files:
M compute/audit/forgetful_e_infty_to_e1_report.md
M compute/audit/factorization_bar_literature_report.md
M compute/audit/adversarial_swarm_findings.md
M compute/audit/deformation_quantization_sc_report.md
M compute/audit/codex_survey_v2_math_review_wave14.md
M compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..de837b8eeb21c3132d237ede82e5c59b67dfd943
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
@@ -10344,8 +10346,8 @@
  \emph{Strategy from $\Theta^{\mathrm{oc}}$.}
  The open sector of $\Theta^{\mathrm{oc}}_\cA$
  provides boundary-marked tautological classes
- $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which are
- strictly richer than the closed-sector classes.
+ $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which contain
+ more data than the closed-sector classes.
  The four-sector decomposition of the MC equation
  produces relations with both interior and boundary
  markings. These additional relations may generate
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/adversarial_swarm_findings.md b/compute/audit/adversarial_swarm_findings.md
index 8b21045dcb458bbde649dbf1dd57f0932f6fc7bf..f443cdeb991e059d2fb313c46537f01d812dc530
--- a/compute/audit/adversarial_swarm_findings.md
+++ b/compute/audit/adversarial_swarm_findings.md
@@ -11,7 +11,7 @@
 - Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
 - Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
 
 ### Dirac: The chapter is 25-30% scaffolding
diff --git a/compute/audit/codex_survey_v2_math_review_wave14.md b/compute/audit/codex_survey_v2_math_review_wave14.md
index 145deca09b82fea3bde918dd52d437d6d83af6e9..404bca6d23b917288ed143bfd2326f14e12557ac
--- a/compute/audit/codex_survey_v2_math_review_wave14.md
+++ b/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -98,10 +98,10 @@
 - Suggested fix: restore the fiber/Koszul-locus qualifier.
 
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
 
 ### Section 11: Modular PVA quantization
diff --git a/compute/audit/deformation_quantization_sc_report.md b/compute/audit/deformation_quantization_sc_report.md
index 348d677e8cdf6d6d8d6f17823f74c02de8e35223..3ae5d6a0b2122544db601f7c99e9d9523dc81568
--- a/compute/audit/deformation_quantization_sc_report.md
+++ b/compute/audit/deformation_quantization_sc_report.md
@@ -116,7 +116,7 @@
 
 **The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
 **But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.
 
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..74ccd525f9a9dde13904542b99001780e992065c
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,21 +199,24 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
 (Vol II, `introduction.tex` lines 204-207).
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
 ### Why the E_1 bar is preferred
 
@@ -328,10 +331,11 @@
 - The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
   coproduct is the open-colour coalgebra
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
 ### Key compatibility statement:
 
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..7938371b9e61e104fcf333181c69bd0bef379710
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -40,7 +40,7 @@
 
 And then the key identification (line 955-958):
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 #### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
 
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -109,7 +109,7 @@
 
 The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
 **Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
 ```
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
index 054c461c27dabe1c2e8c414def1274e4b32b62f7..a29a5437545f21ab552f86d19007b9528339cf15
--- a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
+++ b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -721,7 +721,7 @@
     {
       "count": 2,
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
     {
       "count": 2,
@@ -1478,4 +1478,4 @@
       "path": "chapters/theory/ordered_associative_chiral_kd.tex"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..de837b8eeb21c3132d237ede82e5c59b67dfd943
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
@@ -10344,8 +10346,8 @@
  \emph{Strategy from $\Theta^{\mathrm{oc}}$.}
  The open sector of $\Theta^{\mathrm{oc}}_\cA$
  provides boundary-marked tautological classes
- $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which are
- strictly richer than the closed-sector classes.
+ $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which contain
+ more data than the closed-sector classes.
  The four-sector decomposition of the MC equation
  produces relations with both interior and boundary
  markings. These additional relations may generate
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/adversarial_swarm_findings.md b/compute/audit/adversarial_swarm_findings.md
index 8b21045dcb458bbde649dbf1dd57f0932f6fc7bf..f443cdeb991e059d2fb313c46537f01d812dc530
--- a/compute/audit/adversarial_swarm_findings.md
+++ b/compute/audit/adversarial_swarm_findings.md
@@ -11,7 +11,7 @@
 - Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
 - Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
 
 ### Dirac: The chapter is 25-30% scaffolding
diff --git a/compute/audit/codex_survey_v2_math_review_wave14.md b/compute/audit/codex_survey_v2_math_review_wave14.md
index 145deca09b82fea3bde918dd52d437d6d83af6e9..404bca6d23b917288ed143bfd2326f14e12557ac
--- a/compute/audit/codex_survey_v2_math_review_wave14.md
+++ b/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -98,10 +98,10 @@
 - Suggested fix: restore the fiber/Koszul-locus qualifier.
 
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
 
 ### Section 11: Modular PVA quantization
diff --git a/compute/audit/deformation_quantization_sc_report.md b/compute/audit/deformation_quantization_sc_report.md
index 348d677e8cdf6d6d8d6f17823f74c02de8e35223..3ae5d6a0b2122544db601f7c99e9d9523dc81568
--- a/compute/audit/deformation_quantization_sc_report.md
+++ b/compute/audit/deformation_quantization_sc_report.md
@@ -116,7 +116,7 @@
 
 **The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
 **But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.
 
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..74ccd525f9a9dde13904542b99001780e992065c
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,21 +199,24 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
 (Vol II, `introduction.tex` lines 204-207).
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
 ### Why the E_1 bar is preferred
 
@@ -328,10 +331,11 @@
 - The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
   coproduct is the open-colour coalgebra
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
 ### Key compatibility statement:
 
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..7938371b9e61e104fcf333181c69bd0bef379710
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -40,7 +40,7 @@
 
 And then the key identification (line 955-958):
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 #### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
 
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -109,7 +109,7 @@
 
 The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
 **Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
 ```
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
index 054c461c27dabe1c2e8c414def1274e4b32b62f7..a29a5437545f21ab552f86d19007b9528339cf15
--- a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
+++ b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -721,7 +721,7 @@
     {
       "count": 2,
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
     {
       "count": 2,
@@ -1478,4 +1478,4 @@
       "path": "chapters/theory/ordered_associative_chiral_kd.tex"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

exec
/bin/zsh -lc "rg -n -g '"'!archive/**'"' -g '"'!raeeznotes/**'"' -g '"'!healing_*/**'"' -g '"'!rectification_*/**'"' -g '"'!fix_wave_*/**'"' -g '"'!*.bak*'"' -e 'B\\(A\\) is SC coalgebra' -e 'SC\\"'^'"\\{ch,top\\}-coalgebra' -e 'Swiss-cheese coalgebra' -e 'coalgebra over SC' -e 'bar complex presents the Swiss-cheese' -e 'bar differential is the closed' -e 'bar coproduct is the open' -e 'closed color\\)     coproduct \\(open color\\)' -e 'closed colour structure' -e 'open colour structure' chapters compute/lib compute/tests compute/audit README.md CLAUDE.md AGENTS.md" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc 'pytest -q compute/tests/test_en_bar_coproduct_engine.py compute/tests/test_two_colored_bar_engine.py compute/tests/test_curved_sc_higher_genus_engine.py compute/tests/test_sc_bar_cobar_inversion_engine.py' in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
AGENTS.md:140:**SC^{ch,top} is NOT on B(A) (AP165).** B(A) is E_1 coassociative coalgebra. SC^{ch,top} lives on the pair (C^bullet_{ch}(A,A), A). FORBIDDEN: "B(A) is a coalgebra over SC^{ch,top}"; "the bar differential is the closed color"; "the bar coproduct is the open color."
AGENTS.md:287:B54. "B(A) is SC coalgebra"            # FALSE: E_1 coalgebra; SC in derived center pair
CLAUDE.md:375:- B54. `B(A)` is a coalgebra over `SC^{ch,top}` / "the bar complex presents the Swiss-cheese algebra." FALSE. B(A) is an E_1 coassociative coalgebra. SC^{ch,top} emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). AP165. Regex: `bar.*presents.*Swiss|coalgebra over.*\\SCchtop|\\SCchtop.*coalgebra`
CLAUDE.md:376:- B55. "The bar differential is the closed color" / "the bar coproduct is the open color." FALSE. d_B and Delta make B(A) an E_1 dg coalgebra, NOT an SC two-color datum. AP165.
CLAUDE.md:467:**FM25. The SC^{ch,top} disaster of 2025-2026 (Opus 4.6 structural confabulation).** Opus constructed an elaborate but FALSE framework claiming B(A) is an SC^{ch,top}-coalgebra. The chain of errors:
CLAUDE.md:469:(1) WRONG: "The bar differential is the closed/holomorphic color of SC." TRUTH: The bar differential d_B comes from the chiral product (OPE residues at FM boundary strata). It is the differential of a dg coalgebra over (ChirAss)^!, NOT a "color" of SC. Having operations that USE FM_k(C) geometry does not make d_B an "E_2 color" — it is a single degree-1 map, not a parametrized family of operations.
CLAUDE.md:920:AP165: B(A) is NOT an SC^{ch,top}-coalgebra. The bar complex B(A) = T^c(s^{-1} A-bar) is an E_1 chiral coassociative coalgebra (differential from chiral product, deconcatenation coproduct). It is a SINGLE E_1 coalgebra, not a two-colored SC datum. The SC^{ch,top} structure emerges in the chiral derived center: the chiral Hochschild cochain complex C^bullet_{ch}(A,A) (defined via the chiral endomorphism operad End^{ch}_A with spectral parameters from FM_k(C), NOT topological Hochschild cochains RHom_{A^e}(A,A)) carries brace operations and a Gerstenhaber bracket, and the pair (C^bullet_{ch}(A,A), A) is the SC^{ch,top} datum (bulk acting on boundary). FORBIDDEN claims: "B(A) is a coalgebra over SC^{ch,top}"; "the bar complex presents the Swiss-cheese algebra"; "the bar differential is the closed color"; "the bar coproduct is the open color." COUNTER: after writing any sentence involving B(A) and SC^{ch,top} in the same paragraph, verify that SC is attributed to the derived center pair, not to B(A).
compute/audit/forgetful_e_infty_to_e1_report.md:116:Omega : (conilpotent SC^{ch,top}-coalgebras) ⇄ (SC^{ch,top}-algebras) : B
compute/audit/forgetful_e_infty_to_e1_report.md:120:The bar functor B is the RIGHT adjoint. Given an SC^{ch,top}-algebra (A, M) (a two-coloured pair: closed A, open M), B(A,M) is the universal conilpotent SC^{ch,top}-coalgebra coaugmented over (A,M). For a single chiral algebra A, promotion A -> (A, A) is required (AP86). The bar complex B(A) of a single algebra is an E_1 chiral coassociative coalgebra over (ChirAss)^!; the SC structure lives on the derived center pair (C^bullet_ch(A,A), A) (AP165). The Quillen equivalence means B and Omega are mutually inverse on the homotopy category.
compute/audit/forgetful_e_infty_to_e1_report.md:126:The left side is **conilpotent SC^{ch,top}-coalgebras**. These are coalgebras over the Koszul dual cooperad of SC^{ch,top}, which means:
compute/audit/deformation_quantization_sc_report.md:119:- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
compute/lib/tensor_harrison_bar_engine.py:1452:            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
compute/lib/sc_homotopy_koszul_engine.py:64:    derived category of SC^{ch,top}-coalgebras.
 succeeded in 997ms:
........................................................................ [ 21%]
........................................................................ [ 43%]
........................................................................ [ 65%]
........................................................................ [ 87%]
...........................................                              [100%]
331 passed in 0.72s
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..de837b8eeb21c3132d237ede82e5c59b67dfd943
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
@@ -10344,8 +10346,8 @@
  \emph{Strategy from $\Theta^{\mathrm{oc}}$.}
  The open sector of $\Theta^{\mathrm{oc}}_\cA$
  provides boundary-marked tautological classes
- $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which are
- strictly richer than the closed-sector classes.
+ $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which contain
+ more data than the closed-sector classes.
  The four-sector decomposition of the MC equation
  produces relations with both interior and boundary
  markings. These additional relations may generate
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/adversarial_swarm_findings.md b/compute/audit/adversarial_swarm_findings.md
index 8b21045dcb458bbde649dbf1dd57f0932f6fc7bf..f443cdeb991e059d2fb313c46537f01d812dc530
--- a/compute/audit/adversarial_swarm_findings.md
+++ b/compute/audit/adversarial_swarm_findings.md
@@ -11,7 +11,7 @@
 - Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
 - Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
 
 ### Dirac: The chapter is 25-30% scaffolding
diff --git a/compute/audit/codex_survey_v2_math_review_wave14.md b/compute/audit/codex_survey_v2_math_review_wave14.md
index 145deca09b82fea3bde918dd52d437d6d83af6e9..404bca6d23b917288ed143bfd2326f14e12557ac
--- a/compute/audit/codex_survey_v2_math_review_wave14.md
+++ b/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -98,10 +98,10 @@
 - Suggested fix: restore the fiber/Koszul-locus qualifier.
 
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
 
 ### Section 11: Modular PVA quantization
diff --git a/compute/audit/deformation_quantization_sc_report.md b/compute/audit/deformation_quantization_sc_report.md
index 348d677e8cdf6d6d8d6f17823f74c02de8e35223..3ae5d6a0b2122544db601f7c99e9d9523dc81568
--- a/compute/audit/deformation_quantization_sc_report.md
+++ b/compute/audit/deformation_quantization_sc_report.md
@@ -116,7 +116,7 @@
 
 **The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
 **But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.
 
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..74ccd525f9a9dde13904542b99001780e992065c
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,21 +199,24 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
 (Vol II, `introduction.tex` lines 204-207).
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
 ### Why the E_1 bar is preferred
 
@@ -328,10 +331,11 @@
 - The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
   coproduct is the open-colour coalgebra
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
 ### Key compatibility statement:
 
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..7938371b9e61e104fcf333181c69bd0bef379710
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -40,7 +40,7 @@
 
 And then the key identification (line 955-958):
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 #### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
 
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -109,7 +109,7 @@
 
 The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
 **Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
 ```
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
index 054c461c27dabe1c2e8c414def1274e4b32b62f7..a29a5437545f21ab552f86d19007b9528339cf15
--- a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
+++ b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -721,7 +721,7 @@
     {
       "count": 2,
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
     {
       "count": 2,
@@ -1478,4 +1478,4 @@
       "path": "chapters/theory/ordered_associative_chiral_kd.tex"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

codex
The current writable repo is clean on the targeted AP165 surface apart from the intentional constitutional warnings and generic operadic `SC` bar/cobar statements. I’m doing one last cross-volume grep now so I can report the exact external files that still need the same correction but are blocked by the sandbox.
exec
/bin/zsh -lc "rg -n -g '"'!archive/**'"' -g '"'!raeeznotes/**'"' -g '"'!healing_*/**'"' -g '"'!rectification_*/**'"' -g '"'!fix_wave_*/**'"' -g '"'!*.bak*'"' -e 'B\\(A\\) is SC coalgebra' -e 'SC\\"'^'"\\{ch,top\\}-coalgebra' -e 'Swiss-cheese coalgebra' -e 'coalgebra over SC' -e 'bar complex presents the Swiss-cheese' -e 'bar differential is the closed' -e 'bar coproduct is the open' -e 'closed color\\)     coproduct \\(open color\\)' -e 'closed colour structure' -e 'open colour structure' /Users/raeez/chiral-bar-cobar-vol2 /Users/raeez/calabi-yau-quantum-groups" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
/Users/raeez/calabi-yau-quantum-groups/AGENTS.md:723:"B(A) is SC coalgebra"                # FALSE: E_1 coalgebra; SC in derived center pair (AP165)
/Users/raeez/calabi-yau-quantum-groups/AGENTS.md:749:**AP165: B(A) is NOT an SC^{ch,top}-coalgebra.** The bar complex is an E_1 chiral coassociative coalgebra (differential + deconcatenation). It is a SINGLE E_1 coalgebra, not a two-colored SC datum. The SC^{ch,top} structure emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). FORBIDDEN: "B(A) is a coalgebra over SC^{ch,top}"; "the bar differential is the closed color"; "the bar coproduct is the open color."
/Users/raeez/chiral-bar-cobar-vol2/CLAUDE.md:160:- B(A) is NOT an SC^{ch,top}-coalgebra (AP165). B(A) is an E_1 chiral coassociative coalgebra. SC^{ch,top} emerges in the chiral derived center pair (C^bullet_{ch}(A,A), A). See Vol I CLAUDE.md AP165/B54-B56.
/Users/raeez/chiral-bar-cobar-vol2/AGENTS.md:70:- "B(A) is a coalgebra over SC^{ch,top}"
/Users/raeez/chiral-bar-cobar-vol2/AGENTS.md:71:- "the bar complex presents the Swiss-cheese algebra"
/Users/raeez/chiral-bar-cobar-vol2/AGENTS.md:72:- "the bar differential is the closed color"
/Users/raeez/chiral-bar-cobar-vol2/AGENTS.md:73:- "the bar coproduct is the open color"
/Users/raeez/chiral-bar-cobar-vol2/AGENTS.md:261:- AP165: B(A) is NOT an SC^{ch,top}-coalgebra. B(A) is E_1 chiral coassociative. SC^{ch,top} emerges in the derived center pair (C^bullet_{ch}(A,A), A). FORBIDDEN: "B(A) coalgebra over SC", "bar presents Swiss-cheese", "bar differential is closed color", "bar coproduct is open color."
/Users/raeez/calabi-yau-quantum-groups/chapters/theory/braided_factorization.tex:259:\begin{remark}[$B_{E_2}(\cA)$ is not a Swiss-cheese coalgebra]
/Users/raeez/chiral-bar-cobar-vol2/compute/lib/sc_bar_cobar_engine.py:7:NOTE (AP165): B(A) is NOT an SC^{ch,top}-coalgebra. The SC^{ch,top}
/Users/raeez/chiral-bar-cobar-vol2/chapters/theory/factorization_swiss_cheese.tex:934:the factorization-level Swiss-cheese coalgebra.
/Users/raeez/chiral-bar-cobar-vol2/chapters/theory/factorization_swiss_cheese.tex:2145:The bar complex presents the Swiss-cheese coalgebra in the same sense
/Users/raeez/chiral-bar-cobar-vol2/chapters/theory/factorization_swiss_cheese.tex:2229:Thus: the bar complex presents the Swiss-cheese coalgebra, as the
/Users/raeez/chiral-bar-cobar-vol2/working_notes.tex:236:splittings).  The bar differential is the closed colour.  The bar
/Users/raeez/chiral-bar-cobar-vol2/working_notes.tex:1149:The bar complex presents the Swiss-cheese algebra, as the
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..de837b8eeb21c3132d237ede82e5c59b67dfd943
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
@@ -10344,8 +10346,8 @@
  \emph{Strategy from $\Theta^{\mathrm{oc}}$.}
  The open sector of $\Theta^{\mathrm{oc}}_\cA$
  provides boundary-marked tautological classes
- $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which are
- strictly richer than the closed-sector classes.
+ $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which contain
+ more data than the closed-sector classes.
  The four-sector decomposition of the MC equation
  produces relations with both interior and boundary
  markings. These additional relations may generate
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/adversarial_swarm_findings.md b/compute/audit/adversarial_swarm_findings.md
index 8b21045dcb458bbde649dbf1dd57f0932f6fc7bf..f443cdeb991e059d2fb313c46537f01d812dc530
--- a/compute/audit/adversarial_swarm_findings.md
+++ b/compute/audit/adversarial_swarm_findings.md
@@ -11,7 +11,7 @@
 - Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
 - Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
 
 ### Dirac: The chapter is 25-30% scaffolding
diff --git a/compute/audit/codex_survey_v2_math_review_wave14.md b/compute/audit/codex_survey_v2_math_review_wave14.md
index 145deca09b82fea3bde918dd52d437d6d83af6e9..404bca6d23b917288ed143bfd2326f14e12557ac
--- a/compute/audit/codex_survey_v2_math_review_wave14.md
+++ b/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -98,10 +98,10 @@
 - Suggested fix: restore the fiber/Koszul-locus qualifier.
 
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
 
 ### Section 11: Modular PVA quantization
diff --git a/compute/audit/deformation_quantization_sc_report.md b/compute/audit/deformation_quantization_sc_report.md
index 348d677e8cdf6d6d8d6f17823f74c02de8e35223..3ae5d6a0b2122544db601f7c99e9d9523dc81568
--- a/compute/audit/deformation_quantization_sc_report.md
+++ b/compute/audit/deformation_quantization_sc_report.md
@@ -116,7 +116,7 @@
 
 **The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
 **But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.
 
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..74ccd525f9a9dde13904542b99001780e992065c
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,21 +199,24 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
 (Vol II, `introduction.tex` lines 204-207).
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
 ### Why the E_1 bar is preferred
 
@@ -328,10 +331,11 @@
 - The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
   coproduct is the open-colour coalgebra
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
 ### Key compatibility statement:
 
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..7938371b9e61e104fcf333181c69bd0bef379710
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -40,7 +40,7 @@
 
 And then the key identification (line 955-958):
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 #### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
 
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -109,7 +109,7 @@
 
 The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
 **Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
 ```
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
index 054c461c27dabe1c2e8c414def1274e4b32b62f7..a29a5437545f21ab552f86d19007b9528339cf15
--- a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
+++ b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -721,7 +721,7 @@
     {
       "count": 2,
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
     {
       "count": 2,
@@ -1478,4 +1478,4 @@
       "path": "chapters/theory/ordered_associative_chiral_kd.tex"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'



thinking
**Gathering edit details**

I'm facing a restriction with editing outside writable roots, and I need to mention that. The user is asking for a summary that includes each finding, detailing the problem with the file and line number, along with the exact edit applied. I need to gather line numbers for each changed file and the specific edits made. I'll also consider using tools like `rg -n` or `nl -ba` to get those details organized. It's all about being concise while still including everything necessary!
thinking
**Utilizing tools for changes**

I'm thinking of using `git diff --stat` to get a quick summary of the changes in the repository. This will help me see an overview of what's been modified. Additionally, I'll use `rg -n` to gather line numbers for key inserted phrases, which is essential for pinpointing exact changes. Combining these tools should provide me with a clearer view of the edits and help compile the necessary information more effectively. Let's get to it!
codex
I’m collecting exact post-edit line numbers now so the close-out can be a true audit report, not a vague changelog. The final response will list each current-repo finding as `PROBLEM at file:line` followed by the exact replacement I applied, then the externally blocked Vol II surfaces.
exec
/bin/zsh -lc "rg -n -e 'single-coloured \\\\Eone-chiral dg coalgebra' -e 'derived center pair \\"'$'"\\(C\\"'^'"\\\\bullet_\\{\\\\mathrm\\{ch\\}\\}' -e 'ordered E_1 bar coalgebra' -e 'derived-center pair \\"'$'"\\(C\\"'^'"\\\\bullet_\\{ch\\}' chapters/theory/en_koszul_duality.tex chapters/theory/bar_cobar_adjunction_curved.tex chapters/connections/concordance.tex chapters/theory/bar_construction.tex" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc 'git diff --stat' in /Users/raeez/chiral-bar-cobar exited 1 in 50ms:

exec
/bin/zsh -lc "rg -n -e 'ordered bar B\\"'^ch'"\\(A\\) = T\\"'^c'"' -e 'SC datum lives on the derived-center pair' -e 'actual SC\\"'^'"\\{ch,top\\} datum is the derived-center pair' -e 'ordered line-side data needed before passing' -e 'do NOT by themselves make B\\(A\\) a two-coloured SC datum' -e 'derived-center pair \\(C\\"'^bullet_ch'"\\(A,A\\), A\\)' compute/lib/two_colored_bar_engine.py compute/lib/en_bar_coproduct_engine.py compute/lib/factorization_coproduct_engine.py compute/lib/tensor_harrison_bar_engine.py compute/lib/theorem_swiss_cheese_kontsevich_engine.py compute/lib/sc_bar_cobar_inversion_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
compute/lib/en_bar_coproduct_engine.py:971:            'actual SC^{ch,top} datum is the derived-center pair '
compute/lib/en_bar_coproduct_engine.py:1171:  4. The ordered line-side data needed before passing to the
compute/lib/factorization_coproduct_engine.py:1050:    they do NOT by themselves make B(A) a two-coloured SC datum;
compute/lib/two_colored_bar_engine.py:1104:    the SC datum lives on the derived-center pair
compute/lib/tensor_harrison_bar_engine.py:1453:            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
exec
/bin/zsh -lc "rg -n -e 'curved ordered bar coalgebra' -e 'genuine dg E_1 bar coalgebra' -e 'Legacy-named genus tower of ordered bar coalgebras' compute/lib/curved_sc_higher_genus_engine.py compute/tests/test_curved_sc_higher_genus_engine.py" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
compute/tests/test_curved_sc_higher_genus_engine.py:1:r"""Tests for the curved ordered bar coalgebra at higher genus.
compute/lib/curved_sc_higher_genus_engine.py:327:    """Legacy-named data for the curved ordered bar coalgebra at genus g.
compute/lib/curved_sc_higher_genus_engine.py:333:    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
compute/lib/curved_sc_higher_genus_engine.py:475:    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
compute/lib/curved_sc_higher_genus_engine.py:610:                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
compute/lib/curved_sc_higher_genus_engine.py:706:    """Legacy-named genus tower of ordered bar coalgebras.
 succeeded in 345ms:
 AGENTS.md                                          |   12 +-
 CLAUDE.md                                          |   24 +-
 ...al_Bar_Cobar_Duality__Geometric_Realization.pdf |  Bin 14461072 -> 14241684 bytes
 Makefile                                           |   28 +-
 N1_koszul_meta.pdf                                 |  Bin 356279 -> 356279 bytes
 N2_mc3_all_types.pdf                               |  Bin 369242 -> 369242 bytes
 N3_e1_primacy.pdf                                  |  Bin 367368 -> 367368 bytes
 N4_mc4_completion.pdf                              |  Bin 333361 -> 333361 bytes
 N5_mc5_sewing.pdf                                  |  Bin 345516 -> 345516 bytes
 N6_shadow_formality.pdf                            |  Bin 341744 -> 341744 bytes
 README.md                                          |   24 +-
 appendices/_sl2_yangian_insert.tex                 |    2 +-
 appendices/nonlinear_modular_shadows.tex           |    2 +-
 appendices/ordered_associative_chiral_kd.tex       |   27 +-
 appendices/spectral_higher_genus.tex               |    2 +-
 bibliography/references.tex                        |  107 +
 bp_self_duality.pdf                                |  Bin 304709 -> 304709 bytes
 chapters/connections/arithmetic_shadows.tex        |   28 +-
 chapters/connections/bv_brst.tex                   |   30 +-
 .../connections/casimir_divisor_core_transport.tex | 1511 -------
exec
/bin/zsh -lc "rg -n -e 'Superseded by AP165' -e 'ordered bar complex itself is therefore an E_1 coalgebra' -e 'genuine SC\\"'^'"\\{ch,top\\} datum appears only on the derived-center pair' -e 'single-coloured ordered "'`E_1` coalgebra'"' -e 'ordered bar complexes of single chiral algebras are not themselves SC coalgebras' -e 'observe that the ordered bar is an E_1 coalgebra' -e 'does not itself present the Swiss-cheese datum' compute/audit/forgetful_e_infty_to_e1_report.md compute/audit/factorization_bar_literature_report.md compute/audit/adversarial_swarm_findings.md compute/audit/deformation_quantization_sc_report.md compute/audit/codex_survey_v2_math_review_wave14.md compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json:724:      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
compute/audit/codex_survey_v2_math_review_wave14.md:103:- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
compute/audit/deformation_quantization_sc_report.md:119:- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
compute/audit/adversarial_swarm_findings.md:14:- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
compute/audit/factorization_bar_literature_report.md:202:> Superseded by AP165: the bar complex of Volume I, equipped with its
compute/audit/factorization_bar_literature_report.md:218:the genuine SC^{ch,top} datum appears only on the derived-center pair
compute/audit/forgetful_e_infty_to_e1_report.md:43:> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
compute/audit/forgetful_e_infty_to_e1_report.md:80:The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
compute/audit/forgetful_e_infty_to_e1_report.md:180:> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..de837b8eeb21c3132d237ede82e5c59b67dfd943
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
@@ -10344,8 +10346,8 @@
  \emph{Strategy from $\Theta^{\mathrm{oc}}$.}
  The open sector of $\Theta^{\mathrm{oc}}_\cA$
  provides boundary-marked tautological classes
- $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which are
- strictly richer than the closed-sector classes.
+ $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which contain
+ more data than the closed-sector classes.
  The four-sector decomposition of the MC equation
  produces relations with both interior and boundary
  markings. These additional relations may generate
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/adversarial_swarm_findings.md b/compute/audit/adversarial_swarm_findings.md
index 8b21045dcb458bbde649dbf1dd57f0932f6fc7bf..f443cdeb991e059d2fb313c46537f01d812dc530
--- a/compute/audit/adversarial_swarm_findings.md
+++ b/compute/audit/adversarial_swarm_findings.md
@@ -11,7 +11,7 @@
 - Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
 - Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
 
 ### Dirac: The chapter is 25-30% scaffolding
diff --git a/compute/audit/codex_survey_v2_math_review_wave14.md b/compute/audit/codex_survey_v2_math_review_wave14.md
index 145deca09b82fea3bde918dd52d437d6d83af6e9..404bca6d23b917288ed143bfd2326f14e12557ac
--- a/compute/audit/codex_survey_v2_math_review_wave14.md
+++ b/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -98,10 +98,10 @@
 - Suggested fix: restore the fiber/Koszul-locus qualifier.
 
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
 
 ### Section 11: Modular PVA quantization
diff --git a/compute/audit/deformation_quantization_sc_report.md b/compute/audit/deformation_quantization_sc_report.md
index 348d677e8cdf6d6d8d6f17823f74c02de8e35223..3ae5d6a0b2122544db601f7c99e9d9523dc81568
--- a/compute/audit/deformation_quantization_sc_report.md
+++ b/compute/audit/deformation_quantization_sc_report.md
@@ -116,7 +116,7 @@
 
 **The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
 **But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.
 
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..74ccd525f9a9dde13904542b99001780e992065c
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,21 +199,24 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
 (Vol II, `introduction.tex` lines 204-207).
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
 ### Why the E_1 bar is preferred
 
@@ -328,10 +331,11 @@
 - The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
   coproduct is the open-colour coalgebra
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
 ### Key compatibility statement:
 
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..7938371b9e61e104fcf333181c69bd0bef379710
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -40,7 +40,7 @@
 
 And then the key identification (line 955-958):
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 #### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
 
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -109,7 +109,7 @@
 
 The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
 **Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
 ```
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
index 054c461c27dabe1c2e8c414def1274e4b32b62f7..a29a5437545f21ab552f86d19007b9528339cf15
--- a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
+++ b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -721,7 +721,7 @@
     {
       "count": 2,
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
     {
       "count": 2,
@@ -1478,4 +1478,4 @@
       "path": "chapters/theory/ordered_associative_chiral_kd.tex"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'


thinking
**Finding chapter patterns**

It seems like my search with the regex patterns for the chapters returned nothing. I’m thinking maybe the patterns I used didn't match what I expected. I need to try something else, like using line numbers for identifying chapter patches. Maybe I should look for unique phrases to make my search more targeted. It feels a bit frustrating, but I’ll keep refining my approach until I get the results I need!
exec
/bin/zsh -lc "rg -n -e 'single-coloured \\\\Eone-chiral dg coalgebra' -e 'actual Swiss-cheese datum emerges only on the derived center' -e 'actual two-colour datum is the derived-center pair' -e 'Together they exhibit the two geometric directions' chapters/theory/en_koszul_duality.tex chapters/theory/bar_cobar_adjunction_curved.tex chapters/connections/concordance.tex chapters/theory/bar_construction.tex" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
chapters/theory/bar_construction.tex:2054:Together they exhibit the two geometric directions that later
chapters/theory/bar_cobar_adjunction_curved.tex:154: actual Swiss-cheese datum emerges only on the derived center
chapters/connections/concordance.tex:3837:actual two-colour datum is the derived-center pair
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..de837b8eeb21c3132d237ede82e5c59b67dfd943
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
@@ -10344,8 +10346,8 @@
  \emph{Strategy from $\Theta^{\mathrm{oc}}$.}
  The open sector of $\Theta^{\mathrm{oc}}_\cA$
  provides boundary-marked tautological classes
- $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which are
- strictly richer than the closed-sector classes.
+ $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which contain
+ more data than the closed-sector classes.
  The four-sector decomposition of the MC equation
  produces relations with both interior and boundary
  markings. These additional relations may generate
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/adversarial_swarm_findings.md b/compute/audit/adversarial_swarm_findings.md
index 8b21045dcb458bbde649dbf1dd57f0932f6fc7bf..f443cdeb991e059d2fb313c46537f01d812dc530
--- a/compute/audit/adversarial_swarm_findings.md
+++ b/compute/audit/adversarial_swarm_findings.md
@@ -11,7 +11,7 @@
 - Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
 - Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
 
 ### Dirac: The chapter is 25-30% scaffolding
diff --git a/compute/audit/codex_survey_v2_math_review_wave14.md b/compute/audit/codex_survey_v2_math_review_wave14.md
index 145deca09b82fea3bde918dd52d437d6d83af6e9..404bca6d23b917288ed143bfd2326f14e12557ac
--- a/compute/audit/codex_survey_v2_math_review_wave14.md
+++ b/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -98,10 +98,10 @@
 - Suggested fix: restore the fiber/Koszul-locus qualifier.
 
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
 
 ### Section 11: Modular PVA quantization
diff --git a/compute/audit/deformation_quantization_sc_report.md b/compute/audit/deformation_quantization_sc_report.md
index 348d677e8cdf6d6d8d6f17823f74c02de8e35223..3ae5d6a0b2122544db601f7c99e9d9523dc81568
--- a/compute/audit/deformation_quantization_sc_report.md
+++ b/compute/audit/deformation_quantization_sc_report.md
@@ -116,7 +116,7 @@
 
 **The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
 **But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.
 
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..74ccd525f9a9dde13904542b99001780e992065c
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,21 +199,24 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
 (Vol II, `introduction.tex` lines 204-207).
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
 ### Why the E_1 bar is preferred
 
@@ -328,10 +331,11 @@
 - The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
   coproduct is the open-colour coalgebra
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
 ### Key compatibility statement:
 
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..7938371b9e61e104fcf333181c69bd0bef379710
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -40,7 +40,7 @@
 
 And then the key identification (line 955-958):
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 #### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
 
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -109,7 +109,7 @@
 
 The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
 **Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
 ```
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
index 054c461c27dabe1c2e8c414def1274e4b32b62f7..a29a5437545f21ab552f86d19007b9528339cf15
--- a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
+++ b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -721,7 +721,7 @@
     {
       "count": 2,
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
     {
       "count": 2,
@@ -1478,4 +1478,4 @@
       "path": "chapters/theory/ordered_associative_chiral_kd.tex"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

exec
/bin/zsh -lc "rg -n -e 'single-coloured \\\\Eone-chiral dg coalgebra' -e 'derived center pair' chapters/theory/en_koszul_duality.tex" in /Users/raeez/chiral-bar-cobar succeeded in 52ms:
6298:derived center pair
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..de837b8eeb21c3132d237ede82e5c59b67dfd943
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
@@ -10344,8 +10346,8 @@
  \emph{Strategy from $\Theta^{\mathrm{oc}}$.}
  The open sector of $\Theta^{\mathrm{oc}}_\cA$
  provides boundary-marked tautological classes
- $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which are
- strictly richer than the closed-sector classes.
+ $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which contain
+ more data than the closed-sector classes.
  The four-sector decomposition of the MC equation
  produces relations with both interior and boundary
  markings. These additional relations may generate
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/adversarial_swarm_findings.md b/compute/audit/adversarial_swarm_findings.md
index 8b21045dcb458bbde649dbf1dd57f0932f6fc7bf..f443cdeb991e059d2fb313c46537f01d812dc530
--- a/compute/audit/adversarial_swarm_findings.md
+++ b/compute/audit/adversarial_swarm_findings.md
@@ -11,7 +11,7 @@
 - Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
 - Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
 
 ### Dirac: The chapter is 25-30% scaffolding
diff --git a/compute/audit/codex_survey_v2_math_review_wave14.md b/compute/audit/codex_survey_v2_math_review_wave14.md
index 145deca09b82fea3bde918dd52d437d6d83af6e9..404bca6d23b917288ed143bfd2326f14e12557ac
--- a/compute/audit/codex_survey_v2_math_review_wave14.md
+++ b/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -98,10 +98,10 @@
 - Suggested fix: restore the fiber/Koszul-locus qualifier.
 
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
 
 ### Section 11: Modular PVA quantization
diff --git a/compute/audit/deformation_quantization_sc_report.md b/compute/audit/deformation_quantization_sc_report.md
index 348d677e8cdf6d6d8d6f17823f74c02de8e35223..3ae5d6a0b2122544db601f7c99e9d9523dc81568
--- a/compute/audit/deformation_quantization_sc_report.md
+++ b/compute/audit/deformation_quantization_sc_report.md
@@ -116,7 +116,7 @@
 
 **The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
 **But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.
 
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..74ccd525f9a9dde13904542b99001780e992065c
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,21 +199,24 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
 (Vol II, `introduction.tex` lines 204-207).
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
 ### Why the E_1 bar is preferred
 
@@ -328,10 +331,11 @@
 - The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
   coproduct is the open-colour coalgebra
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
 ### Key compatibility statement:
 
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..7938371b9e61e104fcf333181c69bd0bef379710
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -40,7 +40,7 @@
 
 And then the key identification (line 955-958):
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 #### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
 
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -109,7 +109,7 @@
 
 The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
 **Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
 ```
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
index 054c461c27dabe1c2e8c414def1274e4b32b62f7..a29a5437545f21ab552f86d19007b9528339cf15
--- a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
+++ b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -721,7 +721,7 @@
     {
       "count": 2,
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
     {
       "count": 2,
@@ -1478,4 +1478,4 @@
       "path": "chapters/theory/ordered_associative_chiral_kd.tex"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

exec
/bin/zsh -lc "nl -ba chapters/theory/en_koszul_duality.tex | sed -n '6288,6303p'" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
  6288	Proposition~\ref{prop:en-formality};
  6289	see also Remark~\ref{rem:en-scope}).
  6290	The \emph{ordered} bar complex
  6291	$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
  6292	single-coloured $\Eone$-chiral dg coalgebra: the differential
  6293	comes from holomorphic collision residues on $\FM_k(\bC)$ and
  6294	the deconcatenation coproduct comes from ordered interval
  6295	splitting on $\Conf_k(\bR)$
  6296	(Theorem~\ref{thm:bar-swiss-cheese}). The actual
  6297	$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
  6298	derived center pair
  6299	$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
  6300	$\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
  6301	$\Sigma_n$-coinvariants and uses the coshuffle coproduct
  6302	instead. At higher genus the fibrewise differential
  6303	$\dfib$ is curved with $\dfib^{\,2}=\kappa(\cA)\cdot\omega_g$
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..de837b8eeb21c3132d237ede82e5c59b67dfd943
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
@@ -10344,8 +10346,8 @@
  \emph{Strategy from $\Theta^{\mathrm{oc}}$.}
  The open sector of $\Theta^{\mathrm{oc}}_\cA$
  provides boundary-marked tautological classes
- $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which are
- strictly richer than the closed-sector classes.
+ $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which contain
+ more data than the closed-sector classes.
  The four-sector decomposition of the MC equation
  produces relations with both interior and boundary
  markings. These additional relations may generate
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/adversarial_swarm_findings.md b/compute/audit/adversarial_swarm_findings.md
index 8b21045dcb458bbde649dbf1dd57f0932f6fc7bf..f443cdeb991e059d2fb313c46537f01d812dc530
--- a/compute/audit/adversarial_swarm_findings.md
+++ b/compute/audit/adversarial_swarm_findings.md
@@ -11,7 +11,7 @@
 - Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
 - Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
 
 ### Dirac: The chapter is 25-30% scaffolding
diff --git a/compute/audit/codex_survey_v2_math_review_wave14.md b/compute/audit/codex_survey_v2_math_review_wave14.md
index 145deca09b82fea3bde918dd52d437d6d83af6e9..404bca6d23b917288ed143bfd2326f14e12557ac
--- a/compute/audit/codex_survey_v2_math_review_wave14.md
+++ b/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -98,10 +98,10 @@
 - Suggested fix: restore the fiber/Koszul-locus qualifier.
 
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
 
 ### Section 11: Modular PVA quantization
diff --git a/compute/audit/deformation_quantization_sc_report.md b/compute/audit/deformation_quantization_sc_report.md
index 348d677e8cdf6d6d8d6f17823f74c02de8e35223..3ae5d6a0b2122544db601f7c99e9d9523dc81568
--- a/compute/audit/deformation_quantization_sc_report.md
+++ b/compute/audit/deformation_quantization_sc_report.md
@@ -116,7 +116,7 @@
 
 **The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
 **But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.
 
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..74ccd525f9a9dde13904542b99001780e992065c
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,21 +199,24 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
 (Vol II, `introduction.tex` lines 204-207).
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
 ### Why the E_1 bar is preferred
 
@@ -328,10 +331,11 @@
 - The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
   coproduct is the open-colour coalgebra
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
 ### Key compatibility statement:
 
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..7938371b9e61e104fcf333181c69bd0bef379710
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -40,7 +40,7 @@
 
 And then the key identification (line 955-958):
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 #### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
 
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -109,7 +109,7 @@
 
 The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
 **Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
 ```
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
index 054c461c27dabe1c2e8c414def1274e4b32b62f7..a29a5437545f21ab552f86d19007b9528339cf15
--- a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
+++ b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -721,7 +721,7 @@
     {
       "count": 2,
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
     {
       "count": 2,
@@ -1478,4 +1478,4 @@
       "path": "chapters/theory/ordered_associative_chiral_kd.tex"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

exec
/bin/zsh -lc "nl -ba chapters/theory/bar_cobar_adjunction_curved.tex | sed -n '145,156p'" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "nl -ba chapters/connections/concordance.tex | sed -n '3834,3841p'" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "nl -ba chapters/theory/bar_construction.tex | sed -n '2048,2060p'" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
   145	\item \emph{The Swiss-cheese directionality.} The
   146	 $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
   147	 holomorphic collision data on~$\FM_k(\C)$ with topological
   148	 interval splitting on~$\Conf_k(\R)$. On the ordered bar these
   149	 appear as the bar differential and the deconcatenation
   150	 coproduct, but they do \emph{not} make
   151	 $\barB^{\mathrm{ord}}(\cA)$ itself an
   152	 $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
   153	 bar remains a single-coloured $\Eone$-chiral coalgebra, and the
   154	 actual Swiss-cheese datum emerges only on the derived center
   155	 pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
   156	\end{enumerate}
 succeeded in 51ms:
  3834	symmetric bar~$B^\Sigma$. The
  3835	Swiss-cheese analysis of Volume~II uses the ordered bar
  3836	$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
  3837	actual two-colour datum is the derived-center pair
  3838	$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
  3839	bar resolution. The Harrison
  3840	bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
  3841	and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 succeeded in 51ms:
  2048	\{t_{p+1},\ldots,t_n\}$. This is topological factorization
  2049	on the~$\bR$-direction of~$\bC \times \bR$ in the
  2050	Costello--Gwilliam framework~\cite[\S5.4]{CG17}: the bar
  2051	differential is holomorphic factorization on~$\FM_k(\bC)$
  2052	(Remark~\ref{rem:costello-gwilliam-factorization}); the bar
  2053	coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
  2054	Together they exhibit the two geometric directions that later
  2055	feed the Swiss-cheese story. On the bar complex itself they still
  2056	form only the single-coloured $\Eone$ dg coalgebra
  2057	$T^c(s^{-1}\bar{\cA})$; the actual
  2058	$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
  2059	derived-center pair
  2060	$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
exec
/bin/zsh -lc "nl -ba compute/lib/two_colored_bar_engine.py | sed -n '32,52p'; nl -ba compute/lib/two_colored_bar_engine.py | sed -n '1082,1128p'" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
    32	     - Open part: B_{Ass}(C) -- the associative bar of C
    33	     - Mixed: the action of C on itself by multiplication
    34	   For chiral algebras, the analogous construction requires care.
    35	   The promoted self-action pair (A, A) has a genuine two-coloured
    36	   operadic bar B_{SC}(A, A), but the ordered chiral bar
    37	   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
    38	   sector. It is not itself the full SC-coalgebra.
    39	
    40	4. THE TYPE-CHECKING RESOLUTION:
    41	   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
    42	   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
    43	     - d_C from FM_k(C): the holomorphic bar differential
    44	     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
    45	   These COMMUTE (d_C is a coderivation of Delta) because the
    46	   holomorphic and topological directions are independent.
    47	
    48	   This does NOT by itself produce a two-coloured SC-coalgebra.
    49	   The genuine SC datum arises either from the promoted pair (A, A)
    50	   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
    51	   Vol I/II architecture, on the derived-center pair
exec
/bin/zsh -lc "nl -ba compute/lib/tensor_harrison_bar_engine.py | sed -n '1448,1456p'" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "nl -ba compute/lib/en_bar_coproduct_engine.py | sed -n '72,96p'; nl -ba compute/lib/en_bar_coproduct_engine.py | sed -n '940,973p'; nl -ba compute/lib/en_bar_coproduct_engine.py | sed -n '1128,1174p'" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "nl -ba compute/lib/factorization_coproduct_engine.py | sed -n '1031,1053p'" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "nl -ba compute/lib/theorem_swiss_cheese_kontsevich_engine.py | sed -n '23,30p'; nl -ba compute/lib/theorem_swiss_cheese_kontsevich_engine.py | sed -n '304,312p'" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "nl -ba compute/lib/sc_bar_cobar_inversion_engine.py | sed -n '1,22p'; nl -ba compute/lib/sc_bar_cobar_inversion_engine.py | sed -n '660,707p'" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "nl -ba compute/tests/test_curved_sc_higher_genus_engine.py | sed -n '1,8p'; nl -ba compute/tests/test_curved_sc_higher_genus_engine.py | sed -n '548,555p'; nl -ba compute/tests/test_curved_sc_higher_genus_engine.py | sed -n '644,650p'" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "nl -ba compute/audit/forgetful_e_infty_to_e1_report.md | sed -n '40,45p'; nl -ba compute/audit/forgetful_e_infty_to_e1_report.md | sed -n '78,82p'; nl -ba compute/audit/forgetful_e_infty_to_e1_report.md | sed -n '112,121p'; nl -ba compute/audit/forgetful_e_infty_to_e1_report.md | sed -n '176,181p'" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "nl -ba compute/lib/curved_sc_higher_genus_engine.py | sed -n '1,20p'; nl -ba compute/lib/curved_sc_higher_genus_engine.py | sed -n '323,340p'; nl -ba compute/lib/curved_sc_higher_genus_engine.py | sed -n '465,476p'; nl -ba compute/lib/curved_sc_higher_genus_engine.py | sed -n '706,719p'; nl -ba compute/lib/curved_sc_higher_genus_engine.py | sed -n '823,832p'" in /Users/raeez/chiral-bar-cobarexec
/bin/zsh -lc "nl -ba compute/audit/factorization_bar_literature_report.md | sed -n '200,220p'; nl -ba compute/audit/factorization_bar_literature_report.md | sed -n '328,336p'; nl -ba compute/audit/adversarial_swarm_findings.md | sed -n '12,16p'; nl -ba compute/audit/deformation_quantization_sc_report.md | sed -n '116,120p'; nl -ba compute/audit/codex_survey_v2_math_review_wave14.md | sed -n '100,105p'; nl -ba compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json | sed -n '722,726p'" in /Users/raeez/chiral-bar-cobar succeeded in 51ms:
  1031	    directions of the Swiss-cheese product:
  1032	
  1033	    FM_k(C) x Conf_k^<(R)
  1034	      |                  |
  1035	      v                  v
  1036	    Holomorphic        Deconcatenation
  1037	    collision data     coproduct
  1038	    for d_bar          for the ordered bar
  1039	    on FM_k(C)         on ordered configs
  1040	
  1041	    The unordered (factorization) coproduct on Ran(X) is the
  1042	    Sigma_k-symmetrization of the ordered (deconcatenation)
  1043	    coproduct.  Both are coassociative.  Only the ordered one
  1044	    is non-cocommutative.
  1045	
  1046	    The factorization coproduct is the CHIRAL structure (Ran(X),
  1047	    Verdier duality, Theorem A).  The deconcatenation is the
  1048	    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
  1049	    These two directions later feed the Swiss-cheese picture, but
  1050	    they do NOT by themselves make B(A) a two-coloured SC datum;
 succeeded in 52ms:
  1448	            'not just the Harrison subcomplex.'
  1449	        ),
  1450	        'implications_for_sc_structure': (
  1451	            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
  1452	            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
  1453	            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
  1454	            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
  1455	            'supplies the E_1 line-side shadow.'
  1456	        ),
 succeeded in 52ms:
    72	A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
    73	complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
    74	coproduct from the R-direction, NOT an E_infty coproduct.  Its
    75	bar differential still uses holomorphic collision data from FM_k(C).
    76	This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
    77	is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
    78	SC^{ch,top} datum in the manuscript lives on the derived-center pair
    79	(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
    80	
    81	This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
    82	E_1 bar that preserves tensor ordering.  Concretely:
    83	
    84	    B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
    85	    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
    86	
    87	The manuscript uses the ordered (tensor coalgebra) structure because:
    88	1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
    89	2. The Yangian/quantum group structure requires the non-cocommutative part.
    90	3. The Swiss-cheese operad naturally produces ordered bar complexes.
    91	
 succeeded in 51ms:
    23	WHAT THIS ENGINE TESTS:
    24	  1. SC operad action on Heisenberg modules: closed-color chiral operations
    25	     together with the ordered bar's E_1 line-side structure, verifying
    26	     that the genuine SC^{ch,top} datum on the derived-center pair
    27	     reproduces the brace dg algebra.
    28	  2. Groupoid action on module categories: the fundamental groupoid of
    29	     SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
    30	     transport of the KZ connection; different parenthesizations give
   304	    # Delta acts on Conf_m^<(R) chains. They commute because they
   305	    # act on different factors.
   306	
   307	    # More precisely: the holomorphic and topological directions are compatible,
   308	    # but on B(A) they still give only the ordered E_1 bar coalgebra.
   309	    # The genuine two-coloured SC datum is the derived-center pair
   310	    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
   311	
   312	    interchange_holds = True
 succeeded in 50ms:
     1	r"""Bar-cobar inversion engine with the derived-center/SC comparison.
     2	
     3	THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
     4	the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
     5	
     6	This engine makes that theorem COMPUTATIONAL by building the cobar complex
     7	Omega(B^ch(A)) explicitly at low arities for Heisenberg H_k and affine
     8	sl_2 at level k, verifying:
     9	
    10	  1. The bar complex B^ch(A) at arities 1-3 with explicit differentials.
    11	  2. The cobar complex Omega(B^ch(A)) with the cobar differential
    12	     d_Omega = d_coQ + d_cores + d_co-Ainf (Verdier dual of bar differential).
    13	  3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
    14	     (chain map inducing iso on cohomology).
    15	  4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
    16	  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
    17	     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
    18	
    19	MATHEMATICAL CONTENT:
    20	
 succeeded in 50ms:
     1	r"""Tests for the curved ordered bar coalgebra at higher genus.
     2	
     3	FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
     4	What happens to the ordered E_1 bar coalgebra, and where does the
     5	SC^{ch,top} datum live?
     6	
     7	SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
     8	
   548	class TestModularTower:
   549	    """Verify the genus tower of ordered bar coalgebras."""
   550	
   551	    def test_tower_curvatures_grow(self):
   552	        """Curvature decreases with genus (lambda_fp decreases)."""
   553	        tower = ModularSCTower(kappa=Fraction(1), genus_max=4)
   554	        curvatures = [tower.curvature_at_genus(g) for g in range(1, 5)]
   555	        for i in range(len(curvatures) - 1):
   644	
   645	    def test_heisenberg_genus_0(self):
   646	        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
   647	        result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
 succeeded in 51ms:
    40	
    41	And then the key identification (line 955-958):
    42	
    43	> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
    44	
    45	#### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
    78	- **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
    79	
    80	The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
    81	
    82	The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
   112	#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
   113	
   114	**Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
   115	```
   116	Omega : (conilpotent SC^{ch,top}-coalgebras) ⇄ (SC^{ch,top}-algebras) : B
   117	```
   118	This is a Quillen equivalence when SC^{ch,top} is homotopy-Koszul (which is PROVED: Theorem thm:homotopy-Koszul via Kontsevich formality + transfer from classical Swiss-cheese Koszulity).
   119	
   120	The bar functor B is the RIGHT adjoint. Given an SC^{ch,top}-algebra (A, M) (a two-coloured pair: closed A, open M), B(A,M) is the universal conilpotent SC^{ch,top}-coalgebra coaugmented over (A,M). For a single chiral algebra A, promotion A -> (A, A) is required (AP86). The bar complex B(A) of a single algebra is an E_1 chiral coassociative coalgebra over (ChirAss)^!; the SC structure lives on the derived center pair (C^bullet_ch(A,A), A) (AP165). The Quillen equivalence means B and Omega are mutually inverse on the homotopy category.
 succeeded in 50ms:
   200	The central claim of Vol II (`foundations.tex` lines 1792-1797):
   201	
   202	> Superseded by AP165: the bar complex of Volume I, equipped with its
   203	> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
   204	> datum appears only on the derived-center pair
   205	> $(C^\bullet_{ch}(A,A), A)$.
   206	
   207	The two-coloured Swiss-cheese operad SC^{ch,top} has:
   208	- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   209	  residues on holomorphic configuration spaces)
   210	- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   211	  (from ordered interval-cutting on the real line)
   212	
   213	A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
   214	(Vol II, `introduction.tex` lines 204-207).
   215	
   216	The differential is the holomorphic/E_2 input and the coproduct is
   217	the ordered E_1 input. Together they form the ordered bar coalgebra;
   218	the genuine SC^{ch,top} datum appears only on the derived-center pair
   219	$(C^\bullet_{ch}(A,A), A)$.
 succeeded in 50ms:
     1	r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
     2	
     3	FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
     4	d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
     5	and where does the SC^{ch,top} datum live?
     6	
     7	SEVEN VERIFIED CLAIMS (with cross-checks):
     8	
     9	1. COASSOCIATIVITY PERSISTS: The deconcatenation coproduct Delta is the tensor
    10	   coalgebra structure, independent of genus. Delta is coassociative at ALL genera.
    11	   (rem:sc-higher-genus, line 1295 of en_koszul_duality.tex)
    12	
    13	2. CURVED DG COALGEBRA: d^2 = kappa * omega_g means (B^{(g)}, d, Delta) is NOT
    14	   a dg coalgebra. It is a CURVED dg coalgebra (Positselski). The curvature
    15	   m_0 = kappa * omega_g is a scalar (thm:modular-characteristic), hence CENTRAL
    16	   in the coalgebra.
    17	
    18	3. SCALAR CODERIVATION: Since d^2 = kappa * omega_g * id (scalar), the map
    19	   d^2 is a scalar coderivation:
    20	       d^2(Delta(x)) = d^2(x_(1)) tensor x_(2) + x_(1) tensor d^2(x_(2))
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..de837b8eeb21c3132d237ede82e5c59b67dfd943
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
@@ -10344,8 +10346,8 @@
  \emph{Strategy from $\Theta^{\mathrm{oc}}$.}
  The open sector of $\Theta^{\mathrm{oc}}_\cA$
  provides boundary-marked tautological classes
- $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which are
- strictly richer than the closed-sector classes.
+ $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which contain
+ more data than the closed-sector classes.
  The four-sector decomposition of the MC equation
  produces relations with both interior and boundary
  markings. These additional relations may generate
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/adversarial_swarm_findings.md b/compute/audit/adversarial_swarm_findings.md
index 8b21045dcb458bbde649dbf1dd57f0932f6fc7bf..f443cdeb991e059d2fb313c46537f01d812dc530
--- a/compute/audit/adversarial_swarm_findings.md
+++ b/compute/audit/adversarial_swarm_findings.md
@@ -11,7 +11,7 @@
 - Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
 - Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
 
 ### Dirac: The chapter is 25-30% scaffolding
diff --git a/compute/audit/codex_survey_v2_math_review_wave14.md b/compute/audit/codex_survey_v2_math_review_wave14.md
index 145deca09b82fea3bde918dd52d437d6d83af6e9..404bca6d23b917288ed143bfd2326f14e12557ac
--- a/compute/audit/codex_survey_v2_math_review_wave14.md
+++ b/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -98,10 +98,10 @@
 - Suggested fix: restore the fiber/Koszul-locus qualifier.
 
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
 
 ### Section 11: Modular PVA quantization
diff --git a/compute/audit/deformation_quantization_sc_report.md b/compute/audit/deformation_quantization_sc_report.md
index 348d677e8cdf6d6d8d6f17823f74c02de8e35223..3ae5d6a0b2122544db601f7c99e9d9523dc81568
--- a/compute/audit/deformation_quantization_sc_report.md
+++ b/compute/audit/deformation_quantization_sc_report.md
@@ -116,7 +116,7 @@
 
 **The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
 **But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.
 
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..74ccd525f9a9dde13904542b99001780e992065c
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,21 +199,24 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
 (Vol II, `introduction.tex` lines 204-207).
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
 ### Why the E_1 bar is preferred
 
@@ -328,10 +331,11 @@
 - The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
   coproduct is the open-colour coalgebra
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
 ### Key compatibility statement:
 
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..7938371b9e61e104fcf333181c69bd0bef379710
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -40,7 +40,7 @@
 
 And then the key identification (line 955-958):
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 #### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
 
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -109,7 +109,7 @@
 
 The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
 **Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
 ```
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
index 054c461c27dabe1c2e8c414def1274e4b32b62f7..a29a5437545f21ab552f86d19007b9528339cf15
--- a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
+++ b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -721,7 +721,7 @@
     {
       "count": 2,
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
     {
       "count": 2,
@@ -1478,4 +1478,4 @@
       "path": "chapters/theory/ordered_associative_chiral_kd.tex"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'


thinking
**Considering text replacement**

I need to think about including the actual replacement text. Maybe I can paraphrase it, using something like, “Replaced with: ...” while making sure it's clear enough. Keeping it concise is key; I might not need to include a whole multi-sentence paragraph. Using the phrase “exact edit applied” implies that I’ve replaced a specific sentence. So, I should definitely include the new sentence or sentences as well.
codex
**Fixed**
- PROBLEM at [en_koszul_duality.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex:6290): the dimensional-ladder summary still said the ordered bar was the Swiss-cheese coalgebra with closed/open colors. EXACT edit applied: replaced that with “`\barB^{\mathrm{ord}}(\cA)` is a single-coloured `\Eone`-chiral dg coalgebra ... the actual `\mathsf{SC}^{\mathrm{ch,top}}` datum appears only on the derived center pair `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”

- PROBLEM at [bar_cobar_adjunction_curved.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/bar_cobar_adjunction_curved.tex:145): the Swiss-cheese bullet still identified the bar differential/coproduct as SC colors on `\barB^{\mathrm{ord}}(\cA)`. EXACT edit applied: rewrote the bullet to say those are holomorphic/topological inputs on the ordered bar, but they do not make the bar itself an `SC^{\mathrm{ch,top}}` object; SC appears only on `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.

- PROBLEM at [concordance.tex](/Users/raeez/chiral-bar-cobar/chapters/connections/concordance.tex:3835): concordance still said Volume II uses `B^{\mathrm{ord}}` for the open color and `B^\Sigma` for the closed color. EXACT edit applied: replaced that with “Volume II uses `B^{\mathrm{ord}}` as the `E_1` line-side engine, while the actual two-colour datum is the derived-center pair `(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)`.”

- PROBLEM at [bar_construction.tex](/Users/raeez/chiral-bar-cobar/chapters/theory/bar_construction.tex:2054): the remark ended with “Together they constitute the Swiss-cheese algebra.” EXACT edit applied: replaced that with “Together they exhibit the two geometric directions ... on the bar complex itself they still form only the single-coloured `\Eone` dg coalgebra ... the actual `SC^{\mathrm{ch,top}}` datum appears only on the derived-center pair.”

- PROBLEM at [two_colored_bar_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/two_colored_bar_engine.py:34): the module docstring and summary still said `B^ch(A)` is already an `SC`-coalgebra. EXACT edit applied: rewrote both sites to say the genuine two-coloured operadic object is `B_{SC}(A,A)`, while `B^ch(A)` is only the ordered `E_1` tensor-coalgebra sector; the manuscript’s SC datum is `(C^bullet_ch(A,A),A)`.

- PROBLEM at [en_bar_coproduct_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/en_bar_coproduct_engine.py:72): the module intro, `swiss_cheese_analysis()`, and `chiral_bar_summary()` still treated `B^ch(A)` as `B_{SC}`. EXACT edit applied: changed all three to say `B^ch(A)` is the ordered `E_1` bar coalgebra, and SC appears only on the derived-center pair.

- PROBLEM at [factorization_coproduct_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/factorization_coproduct_engine.py:1033): the summary diagram still labeled “bar differential = closed color / coproduct = open color.” EXACT edit applied: replaced that with “holomorphic collision data / deconcatenation coproduct,” and added the sentence that these do not by themselves make `B(A)` a two-coloured SC datum.

- PROBLEM at [tensor_harrison_bar_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/tensor_harrison_bar_engine.py:1450): the returned explanation still said SC-coalgebra structure lives on `Sym^c`. EXACT edit applied: replaced it with “`Sym^c` is the full geometric factorization coalgebra of Vol I, not the Swiss-cheese coalgebra; the actual SC datum lives on `(C^\bullet_{ch}(A,A),A)`.”

- PROBLEM at [theorem_swiss_cheese_kontsevich_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/theorem_swiss_cheese_kontsevich_engine.py:23): the engine still spoke of SC-coalgebra structure on the bar complex. EXACT edit applied: rewrote the docstring and the key inline comment to say the ordered bar supplies the `E_1` line-side structure, while the genuine SC datum is the derived-center pair.

- PROBLEM at [sc_bar_cobar_inversion_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/sc_bar_cobar_inversion_engine.py:1): the “SC version” framing still suggested SC cobar on `B^ch(A)`. EXACT edit applied: retitled/rephrased it as a derived-center/SC comparison, and changed the class/function docs to say bar-cobar inversion recovers only `A`; the genuine SC datum is `(C^*_ch(A,A),A)`.

- PROBLEM at [curved_sc_higher_genus_engine.py](/Users/raeez/chiral-bar-cobar/compute/lib/curved_sc_higher_genus_engine.py:1): the module, class docs, and notes still described genus-`g` bar objects as curved/corrected `SC` coalgebras. EXACT edit applied: rewrote them as legacy-named wrappers for the curved/corrected ordered bar coalgebra, with explicit statements that SC remains on the derived-center pair.

- PROBLEM at [test_curved_sc_higher_genus_engine.py](/Users/raeez/chiral-bar-cobar/compute/tests/test_curved_sc_higher_genus_engine.py:1): the test docstrings still advertised “curved `SC^{ch,top}`-coalgebra structure at higher genus.” EXACT edit applied: changed those descriptions to “curved ordered bar coalgebra,” including the modular-tower and Heisenberg genus-0 test text.

- PROBLEM at [forgetful_e_infty_to_e1_report.md](/Users/raeez/chiral-bar-cobar/compute/audit/forgetful_e_infty_to_e1_report.md:43): the audit note still quoted the false closed/open-color-on-bar slogan and later called `B^ch(A)` an `SC` coalgebra. EXACT edit applied: inserted “Superseded by AP165” at the quote, rewrote the “deepest answer” around line 168 as “ordered `E_1` coalgebraic engine,” and renamed Q6 to the genuine two-coloured operadic-bar question.

- PROBLEM at [factorization_bar_literature_report.md](/Users/raeez/chiral-bar-cobar/compute/audit/factorization_bar_literature_report.md:202): the literature report still said the bar complex is a coalgebra over `SC^{ch,top}` and repeated that later. EXACT edit applied: replaced both occurrences with “ordered `E_1` coalgebra” plus the correction that the genuine SC datum appears only on `(C^\bullet_{ch}(A,A),A)`.

- PROBLEM at [adversarial_swarm_findings.md](/Users/raeez/chiral-bar-cobar/compute/audit/adversarial_swarm_findings.md:14): the minimal decomposition still ended with “observe bar is a coalgebra over SC.” EXACT edit applied: changed it to “observe that the ordered bar is an `E_1` coalgebra and that the genuine SC datum appears only on the derived-center pair.”

- PROBLEM at [deformation_quantization_sc_report.md](/Users/raeez/chiral-bar-cobar/compute/audit/deformation_quantization_sc_report.md:119): the report still said “including bar complexes” when describing recoverable `SC` coalgebras. EXACT edit applied: changed that to “Every genuine `SC^{ch,top}`-coalgebra ... ordered bar complexes of single chiral algebras are not themselves SC coalgebras.”

- PROBLEM at [codex_survey_v2_math_review_wave14.md](/Users/raeez/chiral-bar-cobar/compute/audit/codex_survey_v2_math_review_wave14.md:101): the audit still treated the local corrected statement as “Swiss-cheese coalgebra.” EXACT edit applied: changed the finding to “ordered `E_1` coalgebra” and updated the verification sentence to point to the current `E_1` theorem wording.

- PROBLEM at [qc_math_surfaces.json](/Users/raeez/chiral-bar-cobar/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json:724): generated audit metadata still normalized the false prefix “The bar complex presents the Swiss-cheese coalgebra.” EXACT edit applied: changed that normalized prefix to “The ordered bar complex is an `E_1` coalgebra and does not itself present the Swiss-cheese datum ...”.

**Blocked external files**
- Remaining false live claims are in Vol II, not Vol I: `/Users/raeez/chiral-bar-cobar-vol2/chapters/theory/factorization_swiss_cheese.tex:934,2145,2229` and `/Users/raeez/chiral-bar-cobar-vol2/working_notes.tex:236,1149`.
- I did not edit them because this session is writable only in `/Users/raeez/chiral-bar-cobar`; Vol II/III are outside the writable root.
- Vol III did not show an uncorrected positive AP165 claim in live source from the final grep; its matches were already negative/constitutional warnings.

**Verification**
- Re-ran the hostile grep in the current repo. Remaining matches are only the intentional blacklist/warning surfaces in `AGENTS.md`/`CLAUDE.md` and generic operadic SC bar/cobar statements about actual two-coloured objects.
- Ran `pytest -q compute/tests/test_en_bar_coproduct_engine.py compute/tests/test_two_colored_bar_engine.py compute/tests/test_curved_sc_higher_genus_engine.py compute/tests/test_sc_bar_cobar_inversion_engine.py`: `331 passed`.
file update:
diff --git a/chapters/connections/concordance.tex b/chapters/connections/concordance.tex
index 894e647c6ec119034b741308e95613d92219cfe4..de837b8eeb21c3132d237ede82e5c59b67dfd943
--- a/chapters/connections/concordance.tex
+++ b/chapters/connections/concordance.tex
@@ -332,7 +332,7 @@
  first Chern class of the factorization homology bundle.
  The full genus-$g$ invariant, the cohomology
  $H^*(\barB^{(g)}(\cA), \Dg{g})$ and its variation over
- $\mathcal{M}_g$, is categorically richer
+ $\mathcal{M}_g$, is a chain-level categorical invariant
  (Theorem~\ref{thm:chain-modular-functor}).
 
 \item \emph{Deformation-obstruction complementarity
@@ -718,7 +718,7 @@
  produce the same genus-$g$ obstruction class.
 \item \emph{BV-BRST.}
 The CG BV formalism motivates the BRST/bar comparison package in
-Chapter~\ref{ch:bv-brst}. The BV/bar identification is resolved
+Chapter~\ref{ch:v1-bv-brst}. The BV/bar identification is resolved
 in the coderived category $D^{\mathrm{co}}$ for all shadow classes
 (Theorem~\ref{thm:bv-bar-coderived}); at the chain level, the
 Heisenberg scalar identification is proved at all genera
@@ -968,7 +968,7 @@
 construction (Theorem~\ref{thm:mc2-bar-intrinsic}) produces
 $\Thetaenv(R)$ from this input. The super
 extension (Neveu--Schwarz, $\mathcal{N}=2$) suggests the
-same mechanism organizes the nonlinear chiral landscape.
+same construction for nonlinear chiral families.
 \end{remark}
 
 \begin{remark}[Conjectural frontier]
@@ -3412,7 +3412,7 @@
 level is the possible failure of $E_1$-degeneration at
 rational~$k$ (admissible/critical levels), where vacuum null
 vectors can produce genuine higher differentials.
-The hook-type corridor is the first natural audit surface for this
+The hook-type corridor is the initial audit surface for this
 route, but the manuscript currently contains incompatible summaries
 of the type-$A$ abelian-$\mathfrak{n}_+$ locus, so the hook-wide
 extension is not used as unconditional input. Accordingly the
@@ -3832,9 +3832,11 @@
 The modular convolution algebra $\gAmod$
 (Definition~\ref{def:modular-convolution-dg-lie}) uses the
 symmetric bar~$B^\Sigma$. The
-Swiss-cheese two-colour structure $\SCchtop$ of Volume~II uses
-the ordered bar~$B^{\mathrm{ord}}$ for the open colour and
-$B^\Sigma$ for the closed colour. The Harrison
+Swiss-cheese analysis of Volume~II uses the ordered bar
+$B^{\mathrm{ord}}$ as the $\Eone$ line-side engine, while the
+actual two-colour datum is the derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$ computed from that
+bar resolution. The Harrison
 bar~$B^{\mathrm{Lie}}$ appears as the primitives of~$B^\Sigma$
 and controls the Francis--Gaitsgory chiral Lie Koszul duality.
 The three bar complexes produce three
@@ -5905,7 +5907,7 @@
 function (Remark~\ref{rem:two-discriminant-families}).
 
 \emph{Why not the Kodaira--Spencer operator on $H^1$?}
-A natural first guess is
+A first guess is
 $\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
 where $T_{\mathrm{KS}}$ acts on
 $H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
@@ -6368,7 +6370,7 @@
 and the family index theorem follows.
 
 \smallskip\noindent\emph{Open problems.}
-The following extensions use the same master diagram in richer
+The following extensions use the same master diagram in broader
 geometric settings:
 $C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
 derived Drinfeld--Kohno, and the elliptic extension.
@@ -7130,7 +7132,7 @@
  & MC-shadow recursion in the Koszul case
  \textup{(}Cor.~\ref{cor:topological-recursion-mc-shadow}\textup{)};
  full EO identification remains conditional on
- Conj.~\ref{conj:bar-worldline}
+ Conj.~\ref{conj:v1-bar-worldline}
  & MC tautological relation $+$ Chern--Weil transform \\
 \hline
 \multicolumn{3}{l}{\textit{%
@@ -7651,7 +7653,7 @@
 bar concentration) constrains the spectral Drinfeld
 class relevant to~\textbf{P3}.
 \textbf{P3} does not feed back into the other two.
-P2~is the \emph{single deepest problem}: it is the
+P2~is the \emph{main unresolved problem}: it is the
 only one whose failure could invalidate the downstream
 conclusions.
 
@@ -8225,7 +8227,7 @@
 $\operatorname{Spec}(\mathcal{O}_F)$ and the
 factorization structure is replaced by adelic
 automorphic forms. The descent from function field to
-number field is the deepest open problem in the
+number field is a major open problem in the
 Langlands programme and has been open since
 Langlands's original letter to Weil (1967).
 
@@ -9314,7 +9316,7 @@
  conjectural (\ClaimStatusConjectured).
 \item The periodic CDG structure
  (Conjecture~\ref{conj:periodic-cdg})
- is the deepest layer: it asserts that the
+ is the last layer: it asserts that the
  cyclotomic grading of the boundary chart
  is reflected in the periodicity of the bar
  differential on the simple quotient.
@@ -10344,8 +10346,8 @@
  \emph{Strategy from $\Theta^{\mathrm{oc}}$.}
  The open sector of $\Theta^{\mathrm{oc}}_\cA$
  provides boundary-marked tautological classes
- $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which are
- strictly richer than the closed-sector classes.
+ $\operatorname{taut}_{g,n}^{\mathrm{oc}}$, which contain
+ more data than the closed-sector classes.
  The four-sector decomposition of the MC equation
  produces relations with both interior and boundary
  markings. These additional relations may generate
diff --git a/chapters/theory/bar_cobar_adjunction_curved.tex b/chapters/theory/bar_cobar_adjunction_curved.tex
index 7542bc1c14634a315d6fae05888863fa7e4e5fd9..5d5a6a1c3941f53bbeb0324886728d1d7f5518e4
--- a/chapters/theory/bar_cobar_adjunction_curved.tex
+++ b/chapters/theory/bar_cobar_adjunction_curved.tex
@@ -143,12 +143,16 @@
  $\Factord(\cA)$. The $R$-matrix provides the braiding.
  After coinvariants, only the symmetric monoidal shadow survives.
 \item \emph{The Swiss-cheese directionality.} The
- $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~operad decomposes as
- $\Eone \times \Einf$ on $\R \times \C$: the bar differential is
- the closed/$\Einf$ colour on holomorphic configurations, and the
- deconcatenation coproduct is the open/$\Eone$ colour on the
- topological interval. The symmetric bar retains only the closed
- colour; the ordered bar retains both.
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$~picture couples
+ holomorphic collision data on~$\FM_k(\C)$ with topological
+ interval splitting on~$\Conf_k(\R)$. On the ordered bar these
+ appear as the bar differential and the deconcatenation
+ coproduct, but they do \emph{not} make
+ $\barB^{\mathrm{ord}}(\cA)$ itself an
+ $\mathrm{SC}^{\mathrm{ch},\mathrm{top}}$ object: the ordered
+ bar remains a single-coloured $\Eone$-chiral coalgebra, and the
+ actual Swiss-cheese datum emerges only on the derived center
+ pair $(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{enumerate}
 The five main theorems~A--H are invariants of the symmetric bar
 and therefore survive averaging. The Yangian $R$-matrix, the
diff --git a/chapters/theory/bar_construction.tex b/chapters/theory/bar_construction.tex
index 4380d797f695cc4da62f2bbe7678b65931aa2d63..89a68bbedbaa7084c689770a6e8e6606d28c729b
--- a/chapters/theory/bar_construction.tex
+++ b/chapters/theory/bar_construction.tex
@@ -2051,9 +2051,13 @@
 differential is holomorphic factorization on~$\FM_k(\bC)$
 (Remark~\ref{rem:costello-gwilliam-factorization}); the bar
 coproduct is topological factorization on~$\Conf_n^{<}(\bR)$.
-Together they constitute the Swiss-cheese algebra
-$\mathrm{SC}^{\mathrm{ch,top}}$ on
-$\FM_k(\bC) \times \Conf_n^{<}(\bR)$.
+Together they exhibit the two geometric directions that later
+feed the Swiss-cheese story. On the bar complex itself they still
+form only the single-coloured $\Eone$ dg coalgebra
+$T^c(s^{-1}\bar{\cA})$; the actual
+$\mathrm{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived-center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$.
 \end{remark}
 
 \begin{remark}[Conventions for this chapter]\label{rem:three-bar-conventions}
diff --git a/chapters/theory/en_koszul_duality.tex b/chapters/theory/en_koszul_duality.tex
index eaa746faa2151c9b1e19d89dde714b6ef1432430..880a4e2fb68efaa3624880b0fdde2b4f1ce91703
--- a/chapters/theory/en_koszul_duality.tex
+++ b/chapters/theory/en_koszul_duality.tex
@@ -6288,12 +6288,15 @@
 Proposition~\ref{prop:en-formality};
 see also Remark~\ref{rem:en-scope}).
 The \emph{ordered} bar complex
-$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a dg coalgebra
-over the holomorphic--topological Swiss-cheese operad
-$\mathsf{SC}^{\mathrm{ch,top}}$: the differential is the
-closed $(n=2)$ color, the deconcatenation coproduct is the
-open $(n=1)$ color
-(Theorem~\ref{thm:bar-swiss-cheese}); the symmetric bar
+$\barB^{\mathrm{ord}}(\cA)=T^c(s^{-1}\bar\cA)$ is a
+single-coloured $\Eone$-chiral dg coalgebra: the differential
+comes from holomorphic collision residues on $\FM_k(\bC)$ and
+the deconcatenation coproduct comes from ordered interval
+splitting on $\Conf_k(\bR)$
+(Theorem~\ref{thm:bar-swiss-cheese}). The actual
+$\mathsf{SC}^{\mathrm{ch,top}}$ datum appears only on the
+derived center pair
+$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\,\cA)$; the symmetric bar
 $\barB^{\Sigma}(\cA)$ of Theorem~A is recovered by $R$-twisted
 $\Sigma_n$-coinvariants and uses the coshuffle coproduct
 instead. At higher genus the fibrewise differential
diff --git a/compute/audit/adversarial_swarm_findings.md b/compute/audit/adversarial_swarm_findings.md
index 8b21045dcb458bbde649dbf1dd57f0932f6fc7bf..f443cdeb991e059d2fb313c46537f01d812dc530
--- a/compute/audit/adversarial_swarm_findings.md
+++ b/compute/audit/adversarial_swarm_findings.md
@@ -11,7 +11,7 @@
 - Stages 1 and 2 are the same observation (Morita theory). One definition suffices.
 - Stage 3 is a conjecture, not a theorem. Cannot be a "stage" of a proved architecture.
 - Stage 4 is already implicit in the definition of C (axiom (vi): clutching).
-- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe bar is a coalgebra over SC.
+- The MINIMAL decomposition has TWO stages: (1) define C with clutching, (2) observe that the ordered bar is an E_1 coalgebra and that the genuine SC datum appears only on the derived-center pair.
 - The "corrected big picture" remark has 6 points but only 1 is independent. The rest are consequences.
 
 ### Dirac: The chapter is 25-30% scaffolding
diff --git a/compute/audit/codex_survey_v2_math_review_wave14.md b/compute/audit/codex_survey_v2_math_review_wave14.md
index 145deca09b82fea3bde918dd52d437d6d83af6e9..404bca6d23b917288ed143bfd2326f14e12557ac
--- a/compute/audit/codex_survey_v2_math_review_wave14.md
+++ b/compute/audit/codex_survey_v2_math_review_wave14.md
@@ -98,10 +98,10 @@
 - Suggested fix: restore the fiber/Koszul-locus qualifier.
 
 ### Section 10: The open/closed world (Volume II)
-#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is a Swiss-cheese coalgebra (line 3424)
+#### [CRITICAL] The bar complex is called an `\mathsf{SC}^{\mathrm{ch,top}}`-algebra when the proved local statement is an ordered `E_1` coalgebra (line 3424)
 - Claim as written: “The bar complex with both structures is a `\SCchtop`-algebra.”
-- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a Swiss-cheese coalgebra, not as an algebra over the operad.
-- Independent verification: `chapters/theory/en_koszul_duality.tex:1222-1237` states “Bar complex as Swiss-cheese coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct.
+- Why it's wrong/imprecise/overclaimed: this flips the operadic variance on the bar side. The bar construction carries differential plus deconcatenation coproduct and is formulated locally as a single-coloured ordered `E_1` coalgebra, not as an algebra over the operad.
+- Independent verification: `chapters/theory/en_koszul_duality.tex` now states “Bar complex as `E_1`-chiral coassociative coalgebra” and identifies `\barB^{\mathrm{ord}}(\cA)` as a dg coalgebra with deconcatenation coproduct, with `\mathsf{SC}^{\mathrm{ch,top}}` moved to the derived-center pair.
 - Suggested fix: replace “algebra” by “coalgebra” here and in the surrounding discussion.
 
 ### Section 11: Modular PVA quantization
diff --git a/compute/audit/deformation_quantization_sc_report.md b/compute/audit/deformation_quantization_sc_report.md
index 348d677e8cdf6d6d8d6f17823f74c02de8e35223..3ae5d6a0b2122544db601f7c99e9d9523dc81568
--- a/compute/audit/deformation_quantization_sc_report.md
+++ b/compute/audit/deformation_quantization_sc_report.md
@@ -116,7 +116,7 @@
 
 **The SC^{ch,top} contribution.** The homotopy-Koszulity of SC^{ch,top} (proved in Vol II via Kontsevich formality + transfer from classical Swiss-cheese) gives a bar-cobar Quillen equivalence. This means:
 - The bar functor B: SC^{ch,top}-alg -> SC^{ch,top}-coalg and cobar functor Omega: SC^{ch,top}-coalg -> SC^{ch,top}-alg form an adjoint equivalence on the homotopy categories.
-- Every SC^{ch,top}-coalgebra (including bar complexes) can be recovered by the cobar functor up to quasi-isomorphism.
+- Every genuine SC^{ch,top}-coalgebra can be recovered by the cobar functor up to quasi-isomorphism; ordered bar complexes of single chiral algebras are not themselves SC coalgebras but E_1 coalgebras feeding the derived-center construction.
 
 **But this does not solve the DQ problem directly.** The DQ problem asks: given a PVA (which lives at the cohomological level, below the SC^{ch,top} world), construct an SC^{ch,top}-algebra. The bar-cobar equivalence operates at the chain level, not at the cohomological level. To go from PVA to SC^{ch,top}-algebra, one must first LIFT the PVA to a chain-level object (the resolved classical datum), and then apply the bar-cobar machinery.
 
diff --git a/compute/audit/factorization_bar_literature_report.md b/compute/audit/factorization_bar_literature_report.md
index 3d1622766bebea28d3a93957795660735b20f444..74ccd525f9a9dde13904542b99001780e992065c
--- a/compute/audit/factorization_bar_literature_report.md
+++ b/compute/audit/factorization_bar_literature_report.md
@@ -199,21 +199,24 @@
 
 The central claim of Vol II (`foundations.tex` lines 1792-1797):
 
-> "The bar complex of Volume I, equipped with its coproduct, is a
-> coalgebra over SC^{ch,top}."
+> Superseded by AP165: the bar complex of Volume I, equipped with its
+> coproduct, is an ordered E_1 coalgebra. The genuine SC^{ch,top}
+> datum appears only on the derived-center pair
+> $(C^\bullet_{ch}(A,A), A)$.
 
 The two-coloured Swiss-cheese operad SC^{ch,top} has:
-- **Closed colour** from FM_k(C): the differential d_B (from OPE
+- **Holomorphic direction** from FM_k(C): the differential d_B (from OPE
   residues on holomorphic configuration spaces)
-- **Open colour** from E_1(m) = Conf_m^<(R): the coproduct Delta
+- **Topological/E_1 direction** from E_1(m) = Conf_m^<(R): the coproduct Delta
   (from ordered interval-cutting on the real line)
 
 A bar element of degree k is parametrised by FM_k(C) x Conf_k^<(R)
 (Vol II, `introduction.tex` lines 204-207).
 
-The differential is the E_infty/E_2 structure (symmetric,
-holomorphic).  The coproduct is the E_1 structure (ordered,
-topological).  Together they form an SC^{ch,top}-coalgebra.
+The differential is the holomorphic/E_2 input and the coproduct is
+the ordered E_1 input. Together they form the ordered bar coalgebra;
+the genuine SC^{ch,top} datum appears only on the derived-center pair
+$(C^\bullet_{ch}(A,A), A)$.
 
 ### Why the E_1 bar is preferred
 
@@ -328,10 +331,11 @@
 - The E_1 bar B^ord(A) = T^c(s^{-1} A-bar) with deconcatenation
   coproduct is the open-colour coalgebra
 
-Together they form an SC^{ch,top}-coalgebra.  The Swiss-cheese
-structure is not an ansatz but a theorem: given the holomorphic
-differential, the topological coproduct is forced by the universal
-property of the cofree coalgebra.
+Together they form the ordered E_1 bar coalgebra. The Swiss-cheese
+story is not an ansatz but a derived-center theorem: the holomorphic
+and topological bar data feed the bulk/boundary pair
+$(C^\bullet_{ch}(A,A), A)$ rather than making $B(A)$ itself
+two-coloured.
 
 ### Key compatibility statement:
 
diff --git a/compute/audit/forgetful_e_infty_to_e1_report.md b/compute/audit/forgetful_e_infty_to_e1_report.md
index bc152f9c6b3082ac675e0cef84a71d3816bf00e5..7938371b9e61e104fcf333181c69bd0bef379710
--- a/compute/audit/forgetful_e_infty_to_e1_report.md
+++ b/compute/audit/forgetful_e_infty_to_e1_report.md
@@ -40,7 +40,7 @@
 
 And then the key identification (line 955-958):
 
-> The bar differential encodes the holomorphic (closed) colour. The bar coproduct encodes the topological (open) colour. Together, B(F) is a factorization coalgebra with two colours, the factorization-level Swiss-cheese coalgebra.
+> Superseded by AP165: the bar differential comes from holomorphic collision data and the bar coproduct comes from the topological/E_1 direction. Together they make the ordered bar an E_1 coalgebra; the genuine Swiss-cheese datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 #### 1c. Vol II CLAUDE.md (the authoritative conceptual statement)
 
@@ -77,7 +77,7 @@
 - **Closed colour**: operations parametrized by FM_k(C) — these produce the differential d_B
 - **Open colour**: operations parametrized by Conf_k(R) — these produce the coproduct Delta
 
-The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The bar complex presents the SC^{ch,top}-coalgebra, so it necessarily carries E_1 coalgebra structure on the open colour.
+The open colour IS E_1 by definition (locally constant factorization algebras on R are E_1-algebras, by Lurie HA Theorem 5.4.5.9). The ordered bar complex itself is therefore an E_1 coalgebra. It does \emph{not} present the full SC^{ch,top} datum; that two-colour structure appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$.
 
 The manuscript's CG Koszul duality remark (rem:CG-koszul, factorization_swiss_cheese.tex line 1611) decomposes this precisely:
 - In the R-direction: E_1 Koszul duality (bar-cobar for associative algebras)
@@ -109,7 +109,7 @@
 
 The directionality constraint (no open-to-closed operations) makes SC genuinely different from E_2 ⊗ E_1 or E_3. Dunn additivity applies to single-colour operads; SC is bicoloured with asymmetric mixed operations.
 
-#### Q6: Is the SC^{ch,top}-coalgebra structure on B^ch(A) the consequence of a UNIVERSAL PROPERTY?
+#### Q6: Is the two-coloured SC^{ch,top} operadic bar the consequence of a UNIVERSAL PROPERTY?
 
 **Yes.** The bar-cobar adjunction (Vol II, thm:bar-cobar-adjunction, cited throughout) gives:
 ```
@@ -165,18 +165,19 @@
 
 The bar complex does not "forget" E_infty down to E_1. The correct statement is:
 
-**The bar construction REFINES E_infty to SC^{ch,top}.** It takes an E_infty-chiral algebra A (a factorizable D-module on Ran(X)) and produces an SC^{ch,top}-coalgebra B(A) (a factorization coalgebra on Ran(X) x Ran(R)). The SC^{ch,top} structure is RICHER than E_infty, not poorer:
+**The ordered bar construction adds an E_1 line-side direction to the E_\infty shadow.** It takes an E_infty-chiral algebra A and produces the ordered bar coalgebra $B^{\mathrm{ord}}(A)$, an E_1 coalgebra with holomorphic differential and deconcatenation coproduct. The genuine SC^{ch,top} structure is richer than either shadow alone and appears only on the derived-center pair $(C^\bullet_{ch}(A,A), A)$:
 
 ```
-E_infty-chiral-Alg ---bar---> SC^{ch,top}-coAlg ---forget open---> E_infty-chiral-coAlg
-                                                  ---forget closed--> E_1-coAlg
+E_infty-chiral-Alg ---bar---> E_1-bar-coAlg
+                                   |
+                                   +-- derived center --> SC^{ch,top}-datum on (C^bullet_ch(A,A), A)
 ```
 
-Forgetting the open colour recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Forgetting the closed colour recovers the associative coalgebra (the Yangian side). Neither alone is the full bar complex. The SC^{ch,top}-coalgebra structure is the UNIVERSAL object from which both projections are shadows.
+Passing to Sigma_n-coinvariants recovers the chiral factorization coalgebra B^{Sigma}(A) of Vol I Theorem A. Retaining the ordering recovers the associative coalgebra/Yangian side. Neither alone is the full Swiss-cheese datum; that datum sits on the derived-center pair computed from the ordered bar.
 
 This is the content of the "Steinberg principle" (Vol II preface, line 72):
 
-> The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra. [...] three functors extract three objects: cobar Omega(B^ch(A)) ≃ A recovers the original algebra, Verdier duality D_Ran(B^ch(A)) ≃ B^ch(A!) produces the line-side Koszul dual, and chiral Hochschild cochains C^bullet_ch(A,A) compute the bulk observables.
+> Superseded by AP165: the bar complex is the ordered E_1 coalgebraic engine, not the Swiss-cheese algebra itself. The genuine SC datum is the derived-center pair $(C^\bullet_{ch}(A,A), A)$; three functors still extract the original algebra, the line-side Koszul dual, and the bulk observables.
 
 And the colour-projection theorem (thm:latyntsev-colour-projections, factorization_swiss_cheese.tex line 2278):
 - Closed-colour projection: quantum group (Latyntsev factorization quantum group)
diff --git a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
index 054c461c27dabe1c2e8c414def1274e4b32b62f7..a29a5437545f21ab552f86d19007b9528339cf15
--- a/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
+++ b/compute/audit/vol1_full_audit_2026_04_08/qc_math_surfaces.json
@@ -721,7 +721,7 @@
     {
       "count": 2,
       "instances": "appendices/ordered_associative_chiral_kd.tex:4166 || chapters/theory/ordered_associative_chiral_kd.tex:3874",
-      "normalized_prefix": "The bar complex presents the Swiss-cheese coalgebra (Theorem~REF). The complete strictification theorem says that, once "
+      "normalized_prefix": "The ordered bar complex is an E_1 coalgebra and does not itself present the Swiss-cheese datum (Theorem~REF). The complete strictification theorem says that, once "
     },
     {
       "count": 2,
@@ -1478,4 +1478,4 @@
       "path": "chapters/theory/ordered_associative_chiral_kd.tex"
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/compute/lib/curved_sc_higher_genus_engine.py b/compute/lib/curved_sc_higher_genus_engine.py
index 7e0b27a1c1e30872555097e0265be7a287a69fe1..d43056928a1db619f7a3ba986b1253639ab0eb31
--- a/compute/lib/curved_sc_higher_genus_engine.py
+++ b/compute/lib/curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Curved Swiss-cheese coalgebra at higher genus: the modular SC^{ch,top} structure.
+r"""Curved ordered bar coalgebra at higher genus and the derived-center SC output.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex B^{(g)}(A) is curved:
-d^2 = kappa(A) * omega_g != 0. What happens to the SC^{ch,top}-coalgebra structure?
+d^2 = kappa(A) * omega_g != 0. What happens to the ordered E_1 bar coalgebra,
+and where does the SC^{ch,top} datum live?
 
 SEVEN VERIFIED CLAIMS (with cross-checks):
 
@@ -112,8 +113,10 @@
    - The structure is governed by the CURVED bar complex formalism
      (Positselski, "Two kinds of derived categories, Koszul duality, and
      comodule-contramodule correspondence").
-   The SC^{ch,top} structure persists in the CURVED sense: (B^{(g)}, d, Delta)
-   is a curved SC^{ch,top}-coalgebra.
+   The ordered bar structure persists in the CURVED sense:
+   (B^{(g)}, d, Delta) is a curved E_1 dg coalgebra. The actual
+   SC^{ch,top} datum remains the derived-center pair
+   (C^\bullet_{ch}(A,A), A).
 
 5. THE CORRECTED DIFFERENTIAL D^{(g)}: Built from the holomorphic propagator
    h_{ij} = partial_{z_i} log E(z_i, z_j) (prime form). Satisfies D^{(g)}^2 = 0
@@ -124,7 +127,7 @@
    built from A-cycle integrals. Each d_k is a coderivation of Delta (period
    integrals commute with interval splitting). Therefore D^{(g)} is a
    coderivation of Delta, and D^{(g)}^2 = 0, so (B^{(g)}, D^{(g)}, Delta) is
-   a genuine (non-curved) dg SC^{ch,top}-coalgebra.
+   a genuine (non-curved) dg E_1 bar coalgebra.
 
 6. MULTI-WEIGHT CORRECTION: delta_F_g^cross lives in the CLOSED sector of
    the SC cooperad. It arises from mixed-channel boundary graphs of M_{g,0}
@@ -136,14 +139,15 @@
 
 7. MODULAR EXTENSION: The genus tower {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra
    over FCom (Feynman transform of commutative modular operad) by
-   thm:bar-modular-operad. The SC^{ch,top} structure at each genus is
+   thm:bar-modular-operad. The ordered bar structure at each genus is
    compatible with the modular operad composition maps because:
    (a) Edge contraction (modular composition) uses the propagator P_A,
-       which acts on FM_k(C) (closed color).
-   (b) Deconcatenation Delta acts on Conf_k(R) (open color).
+       which acts on FM_k(C).
+   (b) Deconcatenation Delta acts on Conf_k(R).
    (c) These are independent (product structure FM x Conf).
-   The genus tower of (corrected) SC^{ch,top}-coalgebras is controlled by
-   the modular characteristic kappa(A) (thm:modular-characteristic).
+   The genus tower of corrected ordered bar coalgebras is controlled by
+   the modular characteristic kappa(A) (thm:modular-characteristic),
+   and the SC^{ch,top} datum is recovered only on the derived-center pair.
 
 Conventions
 -----------
@@ -158,7 +162,7 @@
 
 References
 ----------
-- thm:bar-swiss-cheese (en_koszul_duality.tex): SC coalgebra structure at genus 0
+- thm:bar-swiss-cheese (en_koszul_duality.tex): E_1 bar coalgebra structure at genus 0
 - rem:sc-higher-genus (en_koszul_duality.tex): Curved SC at higher genus
 - thm:quantum-diff-squares-zero (higher_genus_complementarity.tex): D^{(g)}^2 = 0
 - thm:bar-modular-operad (bar_cobar_adjunction_curved.tex): FCom-algebra structure
@@ -320,13 +324,15 @@
 
 @dataclass
 class CurvedSCData:
-    """Data for a curved SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named data for the curved ordered bar coalgebra at genus g.
 
     At genus g >= 1 with kappa != 0:
     - d is a coderivation of Delta (always, by tensor coalgebra universality).
     - d^2 = kappa * omega_g (curvature) as a fiberwise operator.
     - The corrected differential D^{(g)} satisfies D^{(g)}^2 = 0.
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The actual SC^{ch,top} datum is the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     Attributes:
         kappa: modular characteristic kappa(A)
@@ -457,7 +463,7 @@
 
 @dataclass
 class CorrectedSCCoalgebra:
-    """The corrected (flat) SC^{ch,top}-coalgebra at genus g.
+    """Legacy-named corrected (flat) ordered bar coalgebra at genus g.
 
     At genus g, the corrected differential D^{(g)} is built from the
     holomorphic propagator h_{ij} = partial_{z_i} log E(z_i, z_j)
@@ -466,7 +472,9 @@
     Properties:
     - D^{(g)}^2 = 0 (Fay trisecant identity = higher-genus Arnold relation).
     - D^{(g)} is a coderivation of Delta (period integrals commute with splitting).
-    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg SC^{ch,top}-coalgebra.
+    - (B^{(g)}, D^{(g)}, Delta) is a genuine dg E_1 bar coalgebra.
+    - The SC^{ch,top} datum remains the derived-center pair
+      (C^bullet_{ch}(A,A), A).
 
     The corrected differential has the expansion:
         D^{(g)} = d_0 + sum_k t_k d_k
@@ -492,10 +500,10 @@
 
     @property
     def sc_structure_type(self) -> str:
-        """Classification of the SC structure.
+        """Classification of the corrected bar structure.
 
-        Genus 0: classical dg SC coalgebra (no corrections needed).
-        Genus >= 1: corrected dg SC coalgebra (period integral corrections).
+        Genus 0: classical dg ordered bar coalgebra (no corrections needed).
+        Genus >= 1: corrected dg ordered bar coalgebra (period integral corrections).
         """
         if self.genus == 0:
             return "classical_dg"
@@ -599,7 +607,7 @@
         'corrected_is_flat': True,  # D^{(1)}^2 = 0 always
         'note': ('The fiberwise d^2 is NOT a coderivation: factor-2 discrepancy '
                  'at the (1,1) splitting. The corrected D^{(1)} is flat and IS '
-                 'a coderivation, giving a genuine dg SC coalgebra.'),
+                 'a coderivation, giving a genuine dg E_1 bar coalgebra.'),
     }
 
 
@@ -695,18 +703,19 @@
 
 @dataclass
 class ModularSCTower:
-    """The genus tower of SC^{ch,top}-coalgebras.
+    """Legacy-named genus tower of ordered bar coalgebras.
 
     {B^{(g,n)}(A)}_{2g-2+n>0} is an algebra over FCom (thm:bar-modular-operad).
-    At each genus, the SC structure is:
-    - Fiberwise: curved dg SC coalgebra (d_fib^2 = kappa * omega_g).
-    - Corrected: genuine dg SC coalgebra (D^{(g)}^2 = 0).
+    At each genus, the bar structure is:
+    - Fiberwise: curved dg ordered bar coalgebra (d_fib^2 = kappa * omega_g).
+    - Corrected: genuine dg ordered bar coalgebra (D^{(g)}^2 = 0).
     - Modular: the genus-completed D_A = sum hbar^g d^{(g)} satisfies D_A^2 = 0.
 
-    The compatibility between FCom structure and SC structure arises because:
-    - FCom composition = edge contraction via propagator (CLOSED color, FM_k(C)).
-    - SC coproduct = deconcatenation (OPEN color, Conf_k(R)).
+    The compatibility between FCom structure and the ordered bar arises because:
+    - FCom composition = edge contraction via propagator on FM_k(C).
+    - The bar coproduct = deconcatenation on Conf_k(R).
     - These act on independent factors in FM_k(C) x Conf_k(R).
+    The genuine SC^{ch,top} datum is recovered only on the derived-center pair.
     """
     kappa: Fraction
     genus_max: int
@@ -824,13 +833,13 @@
     num_generators: int = 1,
     weights: Optional[List[int]] = None,
 ) -> Dict[str, Any]:
-    """Complete analysis of the curved SC^{ch,top} structure for a given algebra.
+    """Complete analysis of the curved ordered bar structure for a given algebra.
 
     Returns a comprehensive dictionary with:
     - Curvature data
     - Coderivation verification
     - Corrected differential properties
-    - SC sector classification
+    - Legacy-named SC-sector bookkeeping for the derived-center output
     - Multi-weight correction status
     """
     if weights is None:
diff --git a/compute/lib/en_bar_coproduct_engine.py b/compute/lib/en_bar_coproduct_engine.py
index bb3bda7718fa9a4eb7107ef373902a3fb79ac0fd..0529bfbb856d9d5528a79f0286859507b218fc40
--- a/compute/lib/en_bar_coproduct_engine.py
+++ b/compute/lib/en_bar_coproduct_engine.py
@@ -69,22 +69,20 @@
 
 THE CHIRAL CASE:
 
-A chiral algebra A on C is E_infty-chiral.  The manuscript's bar complex
-B^ch(A) carries an E_1 (coassociative) coproduct from the R-direction,
-NOT an E_infty coproduct.  This is because:
-
-    The chiral bar complex = B_{SC^{ch,top}}(A)
+A chiral algebra A on C is E_infty-chiral.  The manuscript's ordered bar
+complex B^ch(A) = T^c(s^{-1}\bar A) carries an E_1 (coassociative)
+coproduct from the R-direction, NOT an E_infty coproduct.  Its
+bar differential still uses holomorphic collision data from FM_k(C).
+This mixed provenance explains the Swiss-cheese picture, but B^ch(A)
+is NOT itself the full two-coloured SC^{ch,top}-bar object.  The actual
+SC^{ch,top} datum in the manuscript lives on the derived-center pair
+(C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
-where SC^{ch,top} is the holomorphic-topological Swiss-cheese operad.
-The differential (C-direction) uses E_2/chiral structure.
-The coproduct (R-direction) uses E_1/associative structure.
-
-This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is a MIXED
-(two-colored) construction.  The Swiss-cheese bar complex is LARGER than
-B_{E_2}(A) because it retains the ordered tensor structure.  Concretely:
+This is NOT the same as B_{E_2}(A) or B_{Com}(A).  It is the ordered
+E_1 bar that preserves tensor ordering.  Concretely:
 
     B_{E_2}(A): elements are symmetric (up to homotopy) in the E_2 sense
-    B_{SC}(A):  elements are ORDERED (the R-direction ordering is physical)
+    B^ch(A):    elements are ORDERED (the R-direction ordering is physical)
 
 The manuscript uses the ordered (tensor coalgebra) structure because:
 1. The R-matrix R(z) lives in End(V tensor V), which requires ordering.
@@ -938,14 +936,16 @@
 # ================================================================
 
 def swiss_cheese_analysis() -> Dict[str, object]:
-    r"""Analyze the Swiss-cheese structure of the chiral bar complex.
+    r"""Analyze how the chiral bar complex sits inside the Swiss-cheese picture.
 
-    The chiral bar complex B^ch(A) is a Swiss-cheese coalgebra:
-    - Differential d_B: from E_2 (holomorphic, Conf_k(C))
-    - Coproduct Delta: from E_1 (topological, Conf_k(R))
+    The ordered chiral bar complex B^ch(A) is an E_1 bar coalgebra:
+    - Differential d_B: from E_2/holomorphic collision data on Conf_k(C)
+    - Coproduct Delta: from E_1/topological ordering on Conf_k(R)
 
-    This means B^ch(A) is NOT B_{E_2}(A).  It is B_{SC}(A) where
-    SC is the Swiss-cheese operad.
+    This means B^ch(A) is NOT B_{E_2}(A).  It is the ordered E_1 bar
+    whose mixed holomorphic/topological provenance is later used to
+    compute the genuine SC^{ch,top} datum on the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The dimensional consequences:
     - B_{E_2}(A) would have dim involving Arnold algebra / S_n coinvariants
@@ -957,7 +957,7 @@
     result = {
         'differential_source': 'E_2 (holomorphic, Conf_k(C))',
         'coproduct_source': 'E_1 (topological, Conf_k(R))',
-        'operad': 'SC^{ch,top} (Swiss-cheese)',
+        'operad': 'SC^{ch,top} appears on the derived-center pair, not on B^ch(A) itself',
         'coalgebra_type': 'coassociative (NOT cocommutative)',
         'explanation': (
             'A chiral algebra A on C is E_infty-chiral (commutative OPE), '
@@ -966,9 +966,10 @@
             'the differential (collision residues on Conf_k(C)).  The coproduct '
             'is the deconcatenation Delta on the ordered tensor coalgebra, '
             'corresponding to cutting the R-line at a point.  '
-            'This is the Swiss-cheese operad SC^{ch,top}: differential from '
-            'the closed (holomorphic) color, coproduct from the open '
-            '(topological) color.'
+            'These two directions explain the Swiss-cheese geometry, but '
+            'B^ch(A) remains a single-coloured E_1 bar coalgebra; the '
+            'actual SC^{ch,top} datum is the derived-center pair '
+            '(C^bullet_ch(A,A), A).'
         ),
         'consequence_for_quantum_groups': (
             'The non-cocommutative E_1 coproduct is ESSENTIAL for the '
@@ -1132,8 +1133,8 @@
 Why does B^ch(A) carry an E_1 (coassociative, non-cocommutative)
 coproduct rather than an E_infty (cocommutative) coproduct?
 
-Answer: The bar complex is NOT B_{E_infty}(A).  It is a SWISS-CHEESE
-construction B_{SC^{ch,top}}(A), where:
+Answer: The bar complex is NOT B_{E_infty}(A).  It is an ordered
+E_1 bar construction whose geometry later feeds the Swiss-cheese story:
 
   - The DIFFERENTIAL uses E_2 (holomorphic) data:
     collision residues on Conf_k(C).
@@ -1141,9 +1142,9 @@
   - The COPRODUCT uses E_1 (topological) data:
     deconcatenation = interval-splitting on Conf_k(R).
 
-The Swiss-cheese operad SC^{ch,top} has two colors:
-  closed (holomorphic, contributing to the differential)
-  open (topological, contributing to the coproduct)
+These two directions do NOT make B^ch(A) the full Swiss-cheese datum.
+The actual SC^{ch,top} structure lives on the derived-center pair
+  (C^bullet_ch(A,A), A).
 
 Why E_1 and not E_2 for the coproduct?
 The coproduct comes from the R-direction in C x R.  Points on R
@@ -1167,7 +1168,8 @@
   1. The R-matrix (lives in ordered tensors)
   2. The quantum group (requires non-cocommutative coproduct)
   3. The Yang-Baxter equation (needs ordered triple tensor products)
-  4. The modular data at higher genus (Swiss-cheese is essential)
+  4. The ordered line-side data needed before passing to the
+     derived-center SC^{ch,top} pair
 
 The CLASSICAL r-matrix r(z) is the symmetrization of R(z):
 it lives in the E_infty quotient.  But the full quantum R-matrix,
diff --git a/compute/lib/factorization_coproduct_engine.py b/compute/lib/factorization_coproduct_engine.py
index 57f810688ba511c5281a66a55da06a29ad48fb68..ee25cbae5cba614ff827a3febc6f3007117a311b
--- a/compute/lib/factorization_coproduct_engine.py
+++ b/compute/lib/factorization_coproduct_engine.py
@@ -1033,11 +1033,10 @@
     FM_k(C) x Conf_k^<(R)
       |                  |
       v                  v
-    Bar differential   Deconcatenation
-    (closed color)     coproduct (open color)
-    Cocommutative      Non-cocommutative
-    Factorization      E_1 coalgebra
-    on Ran(X)          on ordered configs
+    Holomorphic        Deconcatenation
+    collision data     coproduct
+    for d_bar          for the ordered bar
+    on FM_k(C)         on ordered configs
 
     The unordered (factorization) coproduct on Ran(X) is the
     Sigma_k-symmetrization of the ordered (deconcatenation)
@@ -1046,8 +1045,11 @@
 
     The factorization coproduct is the CHIRAL structure (Ran(X),
     Verdier duality, Theorem A).  The deconcatenation is the
-    TOPOLOGICAL structure (E_1 direction, interval splitting,
-    Swiss-cheese theorem).
+    TOPOLOGICAL/E_1 structure (interval splitting on the ordered bar).
+    These two directions later feed the Swiss-cheese picture, but
+    they do NOT by themselves make B(A) a two-coloured SC datum;
+    that attribution belongs to the derived-center pair
+    (C^bullet_ch(A,A), A).
 
     The coderived category and Verdier intertwining use the
     FACTORIZATION (cocommutative) coproduct.  The Swiss-cheese
diff --git a/compute/lib/sc_bar_cobar_inversion_engine.py b/compute/lib/sc_bar_cobar_inversion_engine.py
index 683ec3136dcb8ee44f44fe75992df854b17f08d8..2d12b1c4e8ee819e272eb08d3cc699a9fb39b460
--- a/compute/lib/sc_bar_cobar_inversion_engine.py
+++ b/compute/lib/sc_bar_cobar_inversion_engine.py
@@ -1,4 +1,4 @@
-r"""SC bar-cobar inversion engine: explicit Omega(B^ch(A)) for standard families.
+r"""Bar-cobar inversion engine with the derived-center/SC comparison.
 
 THEOREM B (thm:bar-cobar-inversion-qi): For a chirally Koszul algebra A,
 the counit epsilon: Omega^ch(B^ch(A)) -> A is a quasi-isomorphism.
@@ -13,7 +13,8 @@
   3. The counit epsilon: Omega(B^ch(A)) -> A is a quasi-isomorphism
      (chain map inducing iso on cohomology).
   4. The contracting homotopy h: A -> Omega(B^ch(A)) at arity 2.
-  5. The SC^{ch,top} version: does the SC cobar recover (A, A) or only A?
+  5. The SC^{ch,top} comparison: why bar-cobar inversion recovers only A,
+     while the genuine SC datum lives on (C^bullet_ch(A,A), A).
 
 MATHEMATICAL CONTENT:
 
@@ -42,12 +43,12 @@
     d = d_bracket + d_residue
     d^2 = 0 by Arnold + Jacobi + Killing symmetry
 
-  SC^{ch,top} version:
-    The bar complex carries BOTH differential (hol) and coproduct (top).
-    The SC cobar Omega_SC(B^ch(A)) recovers the CLOSED-colour algebra A,
-    not the full SC algebra (A, A). The open colour is the coproduct
-    direction, which is encoded in the coalgebra structure of B^ch(A),
-    not in the cobar of B^ch(A).
+  Derived-center/SC comparison:
+    The bar complex carries BOTH the holomorphic differential and the
+    E_1 deconcatenation coproduct. This does NOT make B^ch(A) the full
+    SC datum. Bar-cobar inversion still recovers only the original
+    algebra A. The genuine SC^{ch,top} structure sits on the derived-center
+    pair (C^bullet_ch(A,A), A), computed using B^ch(A) as a resolution.
 
 CRITICAL DISTINCTIONS (from CLAUDE.md):
   - Omega(B(A)) = A (bar-cobar INVERSION, recovers original) [AP25]
@@ -653,23 +654,23 @@
 
 
 # ============================================================================
-# V. Swiss-cheese version: SC cobar
+# V. Derived-center/SC comparison
 # ============================================================================
 
 
 @dataclass
 class SCBarCobarResult:
-    """Result of the SC^{ch,top} bar-cobar analysis.
+    """Result of the derived-center/SC comparison.
 
-    The bar complex B^ch(A) of an SC^{ch,top}-algebra carries:
+    The ordered bar complex B^ch(A) carries:
       - Differential d_B from OPE residues (holomorphic/C-direction)
       - Coproduct Delta from deconcatenation (topological/R-direction)
 
-    The SC cobar Omega_SC(B^ch(A)):
-      - Recovers the CLOSED-colour algebra A (the chiral algebra)
-      - Does NOT recover the full SC-algebra (A, A^!)
-      - The OPEN-colour structure is encoded in the coproduct of B,
-        which becomes the R-matrix/braiding of the cobar resolution.
+    Bar-cobar inversion Omega(B^ch(A)):
+      - Recovers the original chiral algebra A
+      - Does NOT by itself produce the genuine SC datum
+      - Leaves the actual SC^{ch,top} attribution on the derived-center pair
+        (C^bullet_ch(A,A), A)
 
     The key distinction (AP34, AP-OC):
       Omega(B(A)) = A (inversion, recovers original)
@@ -684,23 +685,15 @@
 
 
 def analyze_sc_bar_cobar(ope: OPEData) -> SCBarCobarResult:
-    """Analyze what the SC cobar recovers.
+    """Analyze what bar-cobar inversion does and does not recover.
 
-    For any SC^{ch,top}-algebra (A, A_boundary):
-      The SC bar B_SC(A) encodes both colours.
-      The SC cobar Omega_SC(B_SC(A)) recovers A (closed colour).
+    For the ordered bar B^ch(A) of a chiral algebra:
+      Omega(B^ch(A)) recovers A.
 
-    The open colour (boundary/A^!) is NOT recovered by the cobar.
+    The line-side dual A^! is NOT recovered by the cobar.
     It is obtained by Verdier duality: D_Ran(B(A)) = B(A!).
-    The FULL SC structure requires BOTH operations.
-
-    The SC^{ch,top} is homotopy-Koszul (thm:homotopy-Koszul in Vol II),
-    so the bar-cobar adjunction is a Quillen equivalence.
-    This means: Omega(B(A)) ~ A as SC^{ch,top}-algebras.
-    But this recovers the CLOSED-colour structure of A as an
-    SC^{ch,top}-algebra, which includes the A_inf operations m_k
-    from the homotopy transfer. It does NOT produce A^! as the
-    open sector.
+    The genuine SC^{ch,top} structure is different again:
+    it lives on the derived-center pair (C^*_ch(A,A), A).
 
     The open/closed architecture (AP34):
       Functor (1): Omega(B(A)) = A (reconstruction)
@@ -714,12 +707,14 @@
     result.closed_colour_algebra = ope.name
 
     result.explanation = (
-        f"The SC cobar Omega_SC(B^ch({ope.name})) recovers the CLOSED-colour "
-        f"algebra {ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
-        f"The OPEN colour (boundary conditions, A^!) is obtained by Verdier "
-        f"duality D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
-        f"The BULK (universal bulk algebra) is Z^der_ch(A) = C^*_ch(A, A), "
-        f"yet another functor. Three functors, three outputs (AP25, AP34)."
+        f"Bar-cobar inversion Omega(B^ch({ope.name})) recovers the algebra "
+        f"{ope.name} via the bar-cobar quasi-isomorphism (Theorem B). "
+        f"The line-side dual A^! is obtained by Verdier duality "
+        f"D_Ran(B(A)) = B(A!), a DIFFERENT functor. "
+        f"The genuine SC bulk/boundary datum is "
+        f"(C^*_ch(A, A), {ope.name}), with bulk algebra "
+        f"Z^der_ch(A) = C^*_ch(A, A). Three functors, three outputs "
+        f"(AP25, AP34)."
     )
     return result
 
diff --git a/compute/lib/tensor_harrison_bar_engine.py b/compute/lib/tensor_harrison_bar_engine.py
index 58c869ec424dc311c48c85b662af9fe56c1bbfa3..06e6d97076f3a7174886314021b8e0c2d5566b32
--- a/compute/lib/tensor_harrison_bar_engine.py
+++ b/compute/lib/tensor_harrison_bar_engine.py
@@ -1448,11 +1448,10 @@
             'not just the Harrison subcomplex.'
         ),
         'implications_for_sc_structure': (
-            'The SC^{ch,top}-coalgebra structure lives on Sym^c (the FULL geometric bar), '
-            'which carries cocommutative coassociative structure. This is compatible with '
-            'both the operadic bar for Com^{ch} (via primitive projection) and for Ass^{ch} '
-            '(via ordered embedding). The E_1 structure on B^{geom} IS the cocommutative '
-            'coalgebra structure, which is STRICTLY STRONGER than coLie (it is coLie + '
-            'cocommutative coassociative).'
+            'Sym^c is the FULL geometric factorization coalgebra of Vol I, not the '
+            'Swiss-cheese coalgebra. The actual SC^{ch,top} datum lives on the '
+            'derived-center pair (C^bullet_ch(A,A), A); Sym^c is its '
+            'Sigma_n-coinvariant closed shadow, while the ordered tensor bar '
+            'supplies the E_1 line-side shadow.'
         ),
     }
diff --git a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
index 945adcf9dd7730e6b854c04dd93f44773246ae40..2107fb24ff5ff6c6cb22881666b0d6100126fae0
--- a/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
+++ b/compute/lib/theorem_swiss_cheese_kontsevich_engine.py
@@ -22,8 +22,9 @@
 
 WHAT THIS ENGINE TESTS:
   1. SC operad action on Heisenberg modules: closed-color chiral operations
-     and open-color E_1 structure on the bar complex, verifying that the
-     SC^{ch,top} coalgebra structure reproduces the brace dg algebra.
+     together with the ordered bar's E_1 line-side structure, verifying
+     that the genuine SC^{ch,top} datum on the derived-center pair
+     reproduces the brace dg algebra.
   2. Groupoid action on module categories: the fundamental groupoid of
      SC_2 = SC^{ch,top} acts on Heisenberg Fock modules via parallel
      transport of the KZ connection; different parenthesizations give
@@ -303,11 +304,10 @@
     # Delta acts on Conf_m^<(R) chains. They commute because they
     # act on different factors.
 
-    # More precisely: the SC coalgebra structure on B(A) means
-    # (B(A), d_B, Delta) is simultaneously:
-    #   - a dg coalgebra over B(E_2) on the closed color
-    #   - a conilpotent coalgebra over B(E_1) on the open color
-    # and the two structures are compatible via the interchange law.
+    # More precisely: the holomorphic and topological directions are compatible,
+    # but on B(A) they still give only the ordered E_1 bar coalgebra.
+    # The genuine two-coloured SC datum is the derived-center pair
+    # (C^bullet_ch(A,A), A), where closed-to-open operations act.
 
     interchange_holds = True
 
diff --git a/compute/lib/two_colored_bar_engine.py b/compute/lib/two_colored_bar_engine.py
index 1fe5c82bb57670bd4c2d2fb87023909cd25f16e6..6d5433c492123281b4829e13c8d00b6acffd66a5
--- a/compute/lib/two_colored_bar_engine.py
+++ b/compute/lib/two_colored_bar_engine.py
@@ -31,22 +31,25 @@
      - Closed part: B_{Com}(C) -- the commutative bar
      - Open part: B_{Ass}(C) -- the associative bar of C
      - Mixed: the action of C on itself by multiplication
-   This is exactly what happens for chiral algebras: B^ch(A) is
-   ALREADY an SC-coalgebra because the bar differential (closed, from
-   FM_k(C)) and the deconcatenation coproduct (open, from Conf_k(R))
-   are independent structures on the SAME underlying tensor coalgebra.
+   For chiral algebras, the analogous construction requires care.
+   The promoted self-action pair (A, A) has a genuine two-coloured
+   operadic bar B_{SC}(A, A), but the ordered chiral bar
+   B^ch(A) = T^c(s^{-1}\bar A) records only the open tensor-coalgebra
+   sector. It is not itself the full SC-coalgebra.
 
 4. THE TYPE-CHECKING RESOLUTION:
-   A chiral algebra A is a Com^ch-algebra (one-coloured). To get
-   an SC^{ch,top}-coalgebra, we do NOT need a separate open input.
-   The bar construction B^ch(A) = T^c(s^{-1} A_bar) carries:
-     - d_C from FM_k(C): the closed structure
-     - Delta from Conf_k(R): the open structure (deconcatenation)
-   These COMMUTE (d_C is a coderivation of Delta) because
-   FM_k(C) x Conf_k(R) is a product.
+   A chiral algebra A is a Com^ch-algebra (one-coloured). Its ordered
+   bar B^ch(A) = T^c(s^{-1} A_bar) carries:
+     - d_C from FM_k(C): the holomorphic bar differential
+     - Delta from Conf_k(R): the E_1 deconcatenation coproduct
+   These COMMUTE (d_C is a coderivation of Delta) because the
+   holomorphic and topological directions are independent.
 
-   The open-colour data is PRODUCED by the bar construction, not
-   assumed as input. This is the content of thm:bar-swiss-cheese.
+   This does NOT by itself produce a two-coloured SC-coalgebra.
+   The genuine SC datum arises either from the promoted pair (A, A)
+   in the operadic bar B_{SC}(A, A), or, in the manuscript's actual
+   Vol I/II architecture, on the derived-center pair
+   (C^bullet_ch(A,A), A).
 
 MATHEMATICAL FRAMEWORK (Loday-Vallette, Chapter 13):
 
@@ -1079,31 +1082,27 @@
 # =========================================================================
 
 def chiral_bar_sc_structure_summary() -> dict:
-    """Summarise how B^ch(A) is an SC^{ch,top}-coalgebra.
+    """Summarise the relation between B^ch(A) and the SC^{ch,top} bar story.
 
     This is a conceptual function documenting the type-checking
     resolution of the fundamental question.
 
-    For a chiral algebra A (= Com^ch-algebra), the bar construction
-    B^ch(A) = T^c(s^{-1} A_bar) is SIMULTANEOUSLY:
+    For a chiral algebra A (= Com^ch-algebra), the ordered bar
+    B^ch(A) = T^c(s^{-1} A_bar) carries:
 
-    1. A dg coalgebra via d_C (the bar differential from FM_k(C))
-       = CLOSED colour structure
-    2. A coassociative coalgebra via Delta (deconcatenation)
-       = OPEN colour structure
-    3. d_C is a coderivation of Delta
-       = the SC^{ch,top}-coalgebra compatibility
+    1. A holomorphic bar differential d_C from FM_k(C)
+    2. An E_1 coassociative coproduct Delta from deconcatenation
+    3. The coderivation compatibility d_C o Delta
+       = (d_C tensor id + id tensor d_C) o Delta
 
-    The key insight: the bar construction does NOT require a second
-    (open-colour) input. It PRODUCES the two-coloured structure from
-    a one-coloured input. The open-colour data (deconcatenation) is
-    a FORMAL CONSEQUENCE of the tensor coalgebra structure.
+    These structures make B^ch(A) a single-coloured E_1 bar coalgebra.
+    They do NOT make B^ch(A) the full two-coloured SC datum.
 
-    In the Loday-Vallette framework: this corresponds to the fact
-    that the forgetful functor from SC-coalgebras to Com-coalgebras
-    (forgetting the open sector) has a LEFT ADJOINT that freely
-    generates the open-colour structure. The bar construction
-    computes this left adjoint on the nose.
+    In the genuine two-coloured operadic bar story, one instead starts
+    from a promoted self-action pair (A, A) and forms B_{SC}(A, A).
+    The manuscript's actual SC^{ch,top} attribution is different again:
+    the SC datum lives on the derived-center pair
+    (C^bullet_ch(A,A), A), computed using the ordered bar as a resolution.
 
     Comparison with the classical case:
     - Classical SC (Voronov): (C, A) with C = Com-algebra, A = Ass-algebra
@@ -1112,19 +1111,18 @@
         closed = B_{Com}(C) = coLie coalgebra
         open = B_{Ass}(C) = coAss coalgebra
         mixed = action-induced maps
-    - For chiral: A single chiral algebra gives rise to the self-action
-      SC-algebra (A, A) where A acts on itself, and B_{SC}(A, A)
-      decomposes exactly as described in thm:bar-swiss-cheese.
+    - For chiral: the promoted self-action pair (A, A) has a genuine
+      two-coloured operadic bar B_{SC}(A, A), while B^ch(A) itself is
+      only the ordered tensor-coalgebra sector.
     """
     return {
         "closed_sector": "B_{Com}(A) = coLie coalgebra from FM_k(C)",
         "open_sector": "B_{Ass}(A) = coAss coalgebra from Conf_k(R)",
         "compatibility": "d_C coderivation of Delta",
         "input": "Single Com-algebra A",
-        "output": "SC-coalgebra (B_{Com}(A), B_{Ass}(A), mixed)",
-        "key_fact": "Open sector is PRODUCED, not assumed",
-        "type_resolution": "Forgetful SC-coalg -> Com-coalg has left adjoint; "
-                           "bar computes this adjoint",
+        "output": "B^ch(A) is the ordered E_1 bar coalgebra; the full two-coloured object is B_{SC}(A,A), and the manuscript's SC datum is (C^bullet_ch(A,A), A)",
+        "key_fact": "The ordered bar supplies the open/bar sector, not the whole SC datum",
+        "type_resolution": "Promotion to (A, A) gives the genuine two-coloured operadic bar; the derived-center pair gives the manuscript's SC attribution",
         "manuscript_ref": "thm:bar-swiss-cheese (en_koszul_duality.tex)",
     }
 
diff --git a/compute/tests/test_curved_sc_higher_genus_engine.py b/compute/tests/test_curved_sc_higher_genus_engine.py
index 7448047fc14de69abf669d448b5f74c4e8af15ab..fb0acc8fd86dc90f51577e5e1923c316c2d799d3
--- a/compute/tests/test_curved_sc_higher_genus_engine.py
+++ b/compute/tests/test_curved_sc_higher_genus_engine.py
@@ -1,7 +1,8 @@
-r"""Tests for the curved SC^{ch,top}-coalgebra structure at higher genus.
+r"""Tests for the curved ordered bar coalgebra at higher genus.
 
 FRONTIER QUESTION: At genus g >= 1, the bar complex is CURVED: d^2 = kappa * omega_g.
-What happens to the SC^{ch,top}-coalgebra structure?
+What happens to the ordered E_1 bar coalgebra, and where does the
+SC^{ch,top} datum live?
 
 SEVEN CLAIMS VERIFIED (3+ paths per claim, per CLAUDE.md multi-path mandate):
 
@@ -545,7 +546,7 @@
 # ============================================================================
 
 class TestModularTower:
-    """Verify the genus tower of SC^{ch,top}-coalgebras."""
+    """Verify the genus tower of ordered bar coalgebras."""
 
     def test_tower_curvatures_grow(self):
         """Curvature decreases with genus (lambda_fp decreases)."""
@@ -642,7 +643,7 @@
     """Test the full analysis pipeline for standard families."""
 
     def test_heisenberg_genus_0(self):
-        """Heisenberg at genus 0: uncurved, classical dg SC coalgebra."""
+        """Heisenberg at genus 0: uncurved, classical dg ordered bar coalgebra."""
         result = analyze_curved_sc_structure("H_1", Fraction(1), 0)
         assert not result['is_curved']
         assert result['sc_type_fiberwise'] == 'dg'

tokens used
265,777

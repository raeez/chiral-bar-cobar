# Wave 12 — AP253 programme-interior self-cite execution (2026-04-18)

## Mission

AP253 (Inter-volume dependency graph for "one programme" claim) flags
programme-interior self-cites where `\cite{LorgatVolI}` /
`\cite{Vol1}` etc. are used in typeset chapter prose. These must be
rewritten as `\ref{V1-...}` cross-volume pointers via the canonical
HZ-11 phantomsection alias pattern (Vol II `main.tex` has ~476
`V1-*` aliases; Vol I `main.tex` has 2 `V2-*` aliases).

Standalones (external arXiv PDFs) are CASE C — they legitimately use
`\cite{}` against the parent monograph with a proper local `\bibitem`.
AP253 scope here is CHAPTER-LEVEL only.

## Programme-wide enumeration

### Vol I — chapters

| File:Line | Citation | Classification |
|---|---|---|
| `chapters/theory/en_koszul_duality.tex:6317` | `\cite{Lorgat26I}, §5.11` | Vol I self-cite (pure AP253) |
| `chapters/theory/en_koszul_duality.tex:6545` | `\cite{Lorgat26II}, App.A Prop A.5` | Vol I → Vol II (no target label) |
| `chapters/theory/en_koszul_duality.tex:6601` | `\cite{Lorgat26II}, App.A Prop A.6` | Vol I → Vol II (no target label) |

### Vol II — chapters

| File:Line | Citation | Classification |
|---|---|---|
| `chapters/theory/bp_chain_level_strict_platonic.tex:814` | `\cite{Vol1-C31}` | Vol II → Vol I `thm:bp-koszul-conductor-polynomial` |
| `chapters/connections/ym_boundary_theory.tex:631` | `\cite{LorgatVolI}` | Vol II → Vol I `chap:universal-conductor` |

### Vol III — chapters

Zero hits. Clean.

### Standalones (Vol I, II, III) — CASE C (external, remain `\cite{}`)

~40 hits across Vol I `standalone/*.tex` (LorgatVolI, Lorgat26, Lorgat26I,
Lorgat26II, LorgatMKD, LorgatMKD1, LorgatI, LorgatVirR, LorgatSevenFaces,
Lorgat2026a/b/c, LorgatShadow, LorgatCYChiral) and 3 hits in Vol II
`standalone/bar_chain_models_chiral_quantum_groups.tex` (Vol1, Vol3).
Each has a corresponding `\bibitem{...}` in the standalone's bibliography
block. Legitimate for arXiv submission. NOT healed this wave.

### Staging / backup files (excluded)

`staging/*.tex`, `staging/ordered_chiral_homology_BACKUP_*.tex`,
`compute/audit/standalone_paper/paper.tex`, `tmp_standalone_audit/*`:
build-artefact or backup; not part of any live `\input` chain. Excluded.

## Rewrites applied

### Vol I `chapters/theory/en_koszul_duality.tex`

Three self-cites in the PVA `\lambda`-bracket / Jacobi / Leibniz
derivation for the chiral $P_3$ bracket on $V_k(\mathfrak{g})$. The PVA
bracket, $(-1)$-shifted Jacobi identity, and right Leibniz rule are
textbook (De Sole--Kac). Rewritten as direct attribution:

1. Line 6317: `(see \cite{Lorgat26I}, §5.11)` →
   `is the standard bracket generated by the Kac--Moody OPE`.
2. Line 6545: `(see~\cite{Lorgat26II}, Appendix~A, Proposition~A.5)` →
   `standard ... Jacobi identity of De Sole--Kac`.
3. Line 6601: `(see~\cite{Lorgat26II}, Appendix~A, Proposition~A.6)` →
   `(De Sole--Kac axiom)`.

No `\ref{V2-...}` alias path used: neither Vol II chapter nor appendix
inscribes PVA Jacobi with a clean label target; the cleanest heal is
textbook attribution.

### Vol II `chapters/theory/bp_chain_level_strict_platonic.tex:814`

`$\thm{koszul-conductor-bp}$ \cite{Vol1-C31}` → `\Cref{V1-thm:bp-koszul-conductor-polynomial}`.
The `\thm{...}` macro call was also redundant (legacy artefact);
`\Cref` via phantomsection alias replaces both constructs.

### Vol II `chapters/connections/ym_boundary_theory.tex:631`

`the universal-conductor chapter of \cite{LorgatVolI}` →
`\Cref{V1-chap:universal-conductor}`. Alias already exists in Vol II
`main.tex:416`.

## Phantomsection aliases added to Vol II main.tex

| Alias | Location |
|---|---|
| `V1-thm:bp-koszul-conductor-polynomial` | inserted alphabetically between `V1-thm:bar-semi-infinite-w` and `V1-thm:bordered-fm-properties` at `main.tex:608` |

Target resolves in Vol I to `chapters/examples/bershadsky_polyakov.tex:205`
(canonical inscription) and `standalone/bp_self_duality.tex:288`
(standalone duplicate).

## Residual / open

1. **Vol I → Vol II cites** (en_koszul_duality:6545, :6601) healed via
   textbook attribution rather than `\ref{V2-...}` because Vol II carries
   no labelled PVA Jacobi/Leibniz target. If Vol II later inscribes
   `prop:pva-jacobi` / `prop:pva-leibniz`, Vol I can be retrofitted:
   add `\phantomsection\label{V2-prop:pva-jacobi}` etc. to Vol I
   `main.tex` (currently only 2 `V2-*` aliases; scaffolding minimal).
2. **Vol I `main.tex` V2/V3 scaffolding is thin** (2 aliases vs Vol II's
   476). Symmetric cross-volume alias infrastructure is a future
   campaign (AP253 continuation) — outside this wave's scope.
3. **Standalone self-cites** (Vol I ~40 sites, Vol II 3 sites) retained
   as CASE C (external arXiv submissions). Each has local `\bibitem`.
4. **Vol I bibliography/references.tex:1652-1655** contains
   `\bibitem{Lorgat26I}` / `\bibitem{Lorgat26II}` entries. Now orphaned
   for chapter consumption (no remaining chapter `\cite{Lorgat26*}`),
   but retained for standalone-include flows where this file may be
   pulled in. Triage candidate for AP281 continuation.

## Commit plan

Zero commits this session (mission constraint: no commits). Diff
summary for operator review:

- `/Users/raeez/chiral-bar-cobar/chapters/theory/en_koszul_duality.tex`
  (3 edits, all citation-hygiene rewrites, no math change)
- `/Users/raeez/chiral-bar-cobar-vol2/main.tex` (1 phantomsection
  alias addition, alphabetical-order-preserving)
- `/Users/raeez/chiral-bar-cobar-vol2/chapters/theory/bp_chain_level_strict_platonic.tex`
  (1 edit, `\cite → \Cref`)
- `/Users/raeez/chiral-bar-cobar-vol2/chapters/connections/ym_boundary_theory.tex`
  (1 edit, `\cite → \Cref`)

Build-gate: verify `make fast` Vol I clean (PVA prose change is local
and removes an undefined bibkey target — should improve not regress);
verify `make` Vol II clean (new `V1-thm:bp-koszul-conductor-polynomial`
phantomsection resolves `\Cref` at both new consumer sites; existing
`V1-chap:universal-conductor` already resolves).

## Verification grep (post-heal)

```
grep -rn '\\cite{(Lorgat|Vol[123]|LorgatMKD|LorgatVol|LorgatI|LorgatCY|Lorgat2026|LorgatVirR|LorgatSevenFaces|LorgatShadow|MKD[0-9]*|V[123]-)' \
  /Users/raeez/chiral-bar-cobar/chapters \
  /Users/raeez/chiral-bar-cobar-vol2/chapters \
  /Users/raeez/calabi-yau-quantum-groups/chapters
```

Result: zero chapter-level programme-interior self-cites across all
three volumes. AP253 chapter scope: CLOSED.

% Editorial companion: archive-only concordance and constitution chapters.
%
% Scope. This editorial track gathers the two archive-only chapters of
% Volume~I that carry no new mathematical content but function as the
% operational constitution of the programme:
%
% 1. concordance.tex -- canonical notation, convention bridge, and
% theorem-status discipline (the epistemic hierarchy, the five-object
% discipline, the status tags ProvedHere/ProvedElsewhere/Conjectured/
% Heuristic, the kappa family registry, the label-uniqueness protocol
% across all three volumes).
% 2. editorial_constitution.tex -- the author's working constitution:
% the Beilinson principle, the E_1-first prose architecture, the
% pre-edit verification templates, the hot-zone anti-pattern registry,
% the prohibition on AI attribution, and the three-volume architecture.
%
% Reading order. The concordance is load-bearing for the manuscript;
% the constitution is load-bearing for the authorial process. Readers
% of the theorem statements can skip this track. Editors and referees
% should read it first.
%
% 2026-04-17 Beilinson-rectified state. The editorial track inherits
% the Beilinson audit verdicts: programme-climax scope qualifiers are
% pinned to the non-logarithmic C_2-cofinite standard landscape plus
% irrational cosets; logarithmic W(p) tempering is open; the CY-C
% pentagon stratifies generator rank rho^{R_i}, not kappa_ch (Hodge
% supertrace route-independent = 0); super-Yangian complementarity is
% max(m,n), not the naive additive zero; the Kummer-irregular prime
% labels 1423, 3067, 23, 43, 419 are retracted at primary-source
% verification and appear in S_r numerators as Riccati-arithmetic
% characteristic primes only.
\def\archivebuild{1}
\def\mainpreambleonly{1}
\IfFileExists{main.tex}{\input{main.tex}}{\input{../main.tex}}

\newcommand{\repoinput}[1]{%
 \IfFileExists{#1.tex}{\input{#1}}{\input{../#1}}%
}

\begin{document}
\frontmatter
\tableofcontents
\mainmatter
\repoinput{chapters/connections/concordance}
\repoinput{chapters/connections/editorial_constitution}
\backmatter
\repoinput{bibliography/references}
\end{document}

# Linear Read Audit — Vol I: Modular Koszul Duality
# Started: 2026-03-25
# Method: Page-by-page, linear, 6-examiner adversarial panel
# Examiners: Beilinson, Witten, Costello, Gaiotto, Drinfeld, Kontsevich

---

## FINDINGS REGISTER

### Pages 1-4 (Abstract)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F1 | p.3 | **CRITICAL** | Gaiotto | κ(W_N) formula WRONG: (N³-N)k/(k+N) → c·(H_N-1). Also CLAUDE.md had c·H_N → c·(H_N-1). | **FIXED** |
| F2 | p.2 | SERIOUS | Beilinson | CG formula "(all n)" → "(type A, generic parameters)". | **FIXED** |
| F3 | p.1 | SERIOUS | Witten | "Recovers Fourier-Mukai kernel" → "genus expansion controlled by Eisenstein series". | **FIXED** |
| F4 | p.1-2 | ~~SERIOUS~~ | Witten | ~~Modular invariance of F_g never verified.~~ Transformation laws ARE recorded (prop:eisenstein-modular). F_g coefficients are modular forms by construction. | **CLOSED** |
| F5 | p.2 | ~~MODERATE~~ | Costello | ~~6-component decomposition vs 3-5.~~ All 6 components named, defined, and described in preface §3.6 and main.tex lines 714-727. | **CLOSED** |
| F6 | p.2 | ~~MODERATE~~ | Drinfeld | ~~def:modular-yangian-pro missing.~~ EXISTS at yangians_drinfeld_kohno.tex:5577. | **CLOSED** |
| F7 | p.3 | ~~MODERATE~~ | Kontsevich | ~~"Postnikov" terminologically incorrect.~~ Metaphorical, well-documented (def:shadow-postnikov-tower line 10109); identification with L∞ obstructions proved at low arity. Standard usage. | **CLOSED** |
| F8 | p.3 | ~~MODERATE~~ | Kontsevich | ~~A∞/L∞ conflation.~~ Meta-theorem item (iii) precisely says "A∞-model of B̄(A) formal: m_n=0 for n≥3." This IS A∞-formality. Shadow-formality (prop:shadow-formality-low-arity) is a separate identification. No conflation in formal statements. | **CLOSED** |
| F9 | p.2 | ~~MODERATE~~ | Kontsevich | ~~"Forces" presented as one-way.~~ Standard mathematical usage for "is the mechanism ensuring." | **CLOSED** |
| F10 | p.2 | ~~MODERATE~~ | Costello | ~~Verdier-Quillen without proof.~~ Correctly styled as \begin{remark}, not a theorem. Standard practice. | **CLOSED** |
| F11 | p.3 | ~~MODERATE~~ | Drinfeld | ~~"Arnold implies CYBE" misleading.~~ Text says MC equation "reduces to" CYBE at genus 0. Correctly framed as genus reduction. | **CLOSED** |
| F12 | p.4 | ~~MODERATE~~ | Witten | ~~Physical meaning of α, Δ never explained.~~ Defined formally at def:shadow-metric (line 12972/12988). Physical roles explained in body chapters. Not expected in abstract. | **CLOSED** |
| F13 | p.4 | ~~MODERATE~~ | Witten | ~~Holographic datum overclaim.~~ Frontier section uses programme language ("governs", "recognition theorem identifies"). Appropriately cautious. | **CLOSED** |
| F14 | p.4 | ~~MINOR~~ | Witten | ~~HS-sewing Moriwaki dependency.~~ General HS-sewing (thm:general-hs-sewing) is self-contained. Only Heisenberg envelope clause (i) depends on Moriwaki; text separates these explicitly. | **CLOSED** |
| F15 | p.2 | ~~MINOR~~ | Kontsevich | ~~FCom homotopy upgrade only cited.~~ Theorem thm:bar-modular-operad is ProvedHere with full proof. GK98 cited only for FCom definition. Standard practice. | **CLOSED** |

### Pages 106-110 (Preface §7: standard landscape — Heisenberg, KM, W₃, βγ families)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F63 | p.108 | **SERIOUS** | Gaiotto | W₃ WW OPE coefficient 16/(22+5c) at line 5054, but body (w_algebras.tex:240,1638) and same preface line 5070 use 32/(22+5c). Internal inconsistency. | **FIXED**: 16→32 in preface OPE |
| F64 | p.109 | **SERIOUS** | Beilinson | βγ central charge formula 1-3(2λ-1)² = 12λ²-12λ+2: algebraic error. 1-3(2λ-1)² = -12λ²+12λ-2 (this is c_bc, not c_βγ). Correct: 3(2λ-1)²-1 = 12λ²-12λ+2. | **FIXED** |
| F65 | p.107 | **MODERATE** | Drinfeld | "symmetry c(k)=c(-k-2h∨)" FALSE. c(k)≠c(-k-2h∨) in general (e.g. sl₂: c(2)=3/2, c(-6)=9/2). Correct: c(k)+c(-k-2h∨)=2 dim g. | **FIXED** |
| F66 | p.100 | **SERIOUS** | Costello | bc ghosts: ORIGINAL classification as Contact (C, depth 4) was CORRECT. F66 incorrectly "fixed" this to Gaussian (G, depth 2) by majority vote across stale sources (AP3). The bc system has two generators and the same stratum separation mechanism as βγ; fermionic antisymmetry kills S_3 only for single-generator systems (free fermion), not for two-generator bc. | **REVERTED**: bc restored to class C (depth 4) in all locations |
| — | p.106-110 | VERIFIED | All | Heisenberg OPE, κ=k, Koszul dual Sym^ch(V*), depth G. KM OPE, κ=(k+h∨)dim g/(2h∨), FF duality k↔-k-2h∨. W₃ quasi-primary Λ = :TT:-3/10 ∂²T, c=-22/5 degeneration. βγ OPE, depth C, genus-2 shell profile. ✓ |

### Pages 96-105 (Preface §5.5-6.8: genus spectral sequence, shadow tower, depth classes, genus-2 shells)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F62 | p.97 | **SERIOUS** | Beilinson | Spectral sequence bidegree: d₁: E₁^{0,*}→E₁^{1,*-1} inconsistent with general formula d_r bidegree (r,-r+1). For r=1: d₁ has bidegree (1,0), so d₁: E₁^{0,*}→E₁^{1,*}. Same error in body (higher_genus_modular_koszul.tex:10320). | **FIXED** (both files) |
| — | p.96-105 | VERIFIED | All | D₁²=0 (BV identity), total weight w=2g-2+r+d, obstruction classes always exact (bar-intrinsic), cubic gauge triviality, canonical quartic Q^contact_Vir=10/[c(5c+22)], G/L/C/M depth classification (verified census), genus loop Λ_P computation (120/[c²(5c+22)]), loop ratio ρ^(1)=240/[c³(5c+22)], quintic forcing o^(5)=480/[c²(5c+22)]x⁵, independent sum factorization. ✓ |

### Pages 93-96 (Preface §5: tangent complex, Chern-Weil, κ table, spectral discriminant, quartic shadow)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F54 | p.94 | **SERIOUS** | Gaiotto | κ(ψ) = 1/2 WRONG (landscape_census confirms 1/4). Free fermion c=1/2, κ=c/2=1/4. | **FIXED** |
| F55 | p.95 | **SERIOUS** | Gaiotto | βγ Hilbert series 1/(1-q)² and Δ=(1-x)² WRONG. Correct: h=√((1+q)/(1-3q)), Δ=(1-3x)(1+x). Confirmed by free_fields.tex:122 and beta_gamma.tex:18. | **FIXED** |
| F57 | p.95 | **MODERATE** | Beilinson | Chern character formula: [(-1)^{r-1}/(r-1)!] d^r log Δ ≠ (1/r)tr(T^r). Correct: [1/r!] d^r (-log Δ) = (1/r)tr(T^r). | **FIXED** |
| F61 | p.95 | MINOR | Kontsevich | det vs sdet inconsistency: formal definition (l.3839) used det, but l.1117 and l.3958 use sdet. | **FIXED**: changed to sdet |
| F58 | p.96 | MINOR | Costello | "cubic also vanishes" for βγ — wrong. Cubic is nonzero but gauge-trivial (thm:cubic-gauge-triviality). | **FIXED** |
| — | p.93-96 | VERIFIED | All | κ table (H, KM, Vir, βγ, lattice, W_N), Δ self-duality, sl₃ eigenvalues (8,(3±√13)/2), Chern-Weil dictionary, genus spectral sequence intro. ✓ |

### Pages 92-93 (Preface §4: Γ-amplitudes, bar-intrinsic construction, weight filtration, all-genus recursion)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F56 | p.92 | **SERIOUS** | Kontsevich | ℏ double-counting: ℓ₁^(1) and ℓ₂^(1) defined WITH ℏ, but full sum ℓ = Σ ℏ^g ℓ_k^(g)/k! adds another ℏ^g. BV operator would appear at O(ℏ²) not O(ℏ). | **FIXED**: removed ℏ from ℓ₁^(1) and ℓ₂^(1) definitions |
| F53 | p.92 | **MODERATE** | Gaiotto | "fifteen trees, five trivalent topologies, each with three planar labelings" — wrong. Two unlabeled topologies: balanced (3 labelings) + caterpillar (12). Also "five faces of K₄" → vertices. | **FIXED** |
| F59 | p.93 | **MODERATE** | Drinfeld | Recursion ordering: "2g'-2+n' < 2g-2+n" wrong (BV term has same EC). Correct: lexicographic in (g,n). | **FIXED** |
| F60 | p.93 | MINOR | Witten | "pentagon identity for the A∞ structure" — should be L∞ (10 divisors = symmetric, not planar). | **FIXED** |
| — | p.92-93 | VERIFIED | All | MC expansion D²=0 → [d₀,Θ]+½[Θ,Θ]=0. Weight filtration tower. (0,4) Borcherds identity Φ_s+Φ_t+Φ_u=F₃. (1,1) Casimir element. Clutching identity factorization. Tridegree, depth filtration, algebraic convergence. ✓ |

### Pages 87-91 (Preface §3: homotopy chiral algebras, log-FM, convolution L∞, assembly, bar functor, D²=0)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F51 | p.90 | **SERIOUS** | Witten | "Non-separating edges: cutting increases b₁ by one" — WRONG. Cutting DECREASES b₁; CREATION increases it. The ℏΔ operator creates, not cuts. | **FIXED**: "whose creation increases b₁ by one" |
| F52 | p.87 | **SERIOUS** | Beilinson | "(n-2)-associahedron" — off by 2. M̄_{0,n+1} ≅ K_n (Stasheff). For n=3: K₁ is a point but M̄_{0,4}=P¹ is 1-dim. | **FIXED**: "the associahedron K_n" |
| — | p.87-91 | VERIFIED | All | Čech totalisation, Ch∞ structure (Malikov-Schechtman), F₂=bracket, F₃ from M̄_{0,4}, HTT for higher F_n, rectification (Vallette), log-FM compactification, planted-forest types, tropicalization, Mok codimension formula, graphwise cocomposition, convolution L∞ formula, one-slot functoriality, strict chart MC moduli agreement, six-component differential (d_Č+d_{Ch∞}+d_coll+d_sew+d_pf+ℏΔ), orientation line det(kE)⊗det(H₁)^{-1}, coinvariants, D²=0 three mechanisms (vertex/one-edge/codim-2). ✓ |

### Pages 90-91 (Preface: clutching identity, modular tangent complex, κ table, three projections)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F34 | p.91 | **CRITICAL** | Gaiotto | W_N κ formula in table WRONG: stated (N-1)(N²k+N²-1)/(N+k), correct is c·(H_N-1). Sum rule also wrong: stated 0, correct is K_N·(H_N-1) ≠ 0. Same-family AP1 error. | **FIXED**: table now uses c·(H_N-1), c'·(H_N-1), K_N·(H_N-1). |
| — | p.88-89 | VERIFIED | All | Weight filtration, MC tower, boundary divisor counts (M̄_{0,4}: 3, M̄_{0,5}: 10 Petersen), planted-forest layer. ✓ |
| — | p.86-87 | VERIFIED | All | Explicit L∞ brackets, bar-intrinsic construction, universal twisting morphism. All correct. ✓ |
| — | p.80-85 | VERIFIED | All | Complementarity, FF duality, total space, Ch∞, log-FM, six-component differential. ✓ |

### Pages 78-79 (Preface: genus-g propagator, curvature, period integrals, genus tower, Â-genus, complementarity)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F33 | p.79 | **CRITICAL** | Beilinson/Gaiotto | F_g signs WRONG: listed F₁=-κ/24, F₂=+7κ/5760, F₃=-31κ/967680 (alternating from Â(x)). Correct: ALL POSITIVE (from Â(iℏ) = (ℏ/2)/sin(ℏ/2)). Compute tests confirm F₁=+κ/24. Also pattern "(-1)^g B_{2g}/(2g)!" wrong — missing (2^{2g-1}-1)/2^{2g-1} factor. | **FIXED**: corrected signs to all positive + correct FP pattern formula. |
| — | p.78-79 | VERIFIED | Witten | D²_g = d²_fib + [d_fib,∇^GM] + (∇^GM)² = κω_g - κω_g + 0 = 0. ✓ |
| — | p.78 | VERIFIED | Kontsevich | Genus-g propagator with (Im Ω)^{-1} correction is standard Arakelov. ✓ |
| — | p.78 | VERIFIED | All | κ(ĝ_k) = (k+h∨)dim g/(2h∨), κ=0 at critical level. ✓ |

### Pages 76-77 (Preface: Koszul dual construction, Heisenberg atom, KM OPE, prime form)

| — | p.76-77 | VERIFIED | All 6 | All formulas correct: residue sign, KM decomposition (dη distributional), prime form K^{-1/2}, FM contractibility, Heisenberg OPE, curved A∞ commutator. ✓ |

### Pages 74-75 (Preface: bar complex on X, d²=0 proof, categorical logarithm, Verdier duality)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F32 | p.74 | SERIOUS | Kontsevich | Bar complex formula used H^{n-1}(FM̄_n, Ω^{n-1}_log) but body text uses Γ (global sections). Bar differential acts on FORMS, not cohomology classes. H^{n-1} is wrong. | **FIXED**: changed to Γ in both occurrences (preface.tex:2259, 2454). |
| — | p.74-75 | VERIFIED | Beilinson | d²=0 nine-term proof structure (3 diagonal + 3 cross + 3 cancellations). ✓ |
| — | p.75 | VERIFIED | Gaiotto | Same-pair residue d²_res^{(ij)} = 0 (vacuous after collision). ✓ |
| — | p.75 | VERIFIED | Drinfeld | D_Ran B̄(A) ≃ A^!_∞ correctly stated (hypotheses implicit). ✓ |

### Pages 72-73 (Preface: Section 1 categorical logarithm, bar/cobar formulas, quadratic duality, chiral algebras)

| — | p.72-73 | VERIFIED | All 6 | Bar/cobar signs, Borcherds identity, Com^!=Lie, chiral algebra def. All correct. ✓ |

### Pages 70-71 (Preface: quartic closure, tautological classes, CohFT, primitive kernel, BV action)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F31 | p.70 | MODERATE | Gaiotto | Preface says "Mumford's relation λ_g²=0" but body proposition (prop:mumford-from-mc) proves a clutching law Σ ξ_sep(λ_{g₁}·λ_{g₂})+ξ_nsep(λ_{g-1})=0. Not the same statement. | **FIXED**: changed to "Mumford λ-class clutching relation". |
| — | p.70 | VERIFIED | Beilinson | Three conjectures (M.13.9, M.13.31, M.13.27) properly tagged \ClaimStatusConjectured. ✓ |
| — | p.70 | VERIFIED | Kontsevich | MC→WDVV at genus 0 correct (prop:wdvv-from-mc). ✓ |
| — | p.70 | VERIFIED | Witten | EO recursion = MC shadow proved for Koszul case. ✓ |
| — | p.71 | VERIFIED | Costello | Pre-Lie product ⋆ correctly defined; primitive master eq correct. ✓ |
| — | p.71 | VERIFIED | Drinfeld | Branch BV action standard; kinetic term ½Ω(K^br x,x) correct. ✓ |

### Pages 68-69 (Preface: Weil analogy, Polyakov dictionary, arithmetic packet, Miura defect)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F29 | p.68 | MODERATE | Witten | Polyakov dictionary "Ghost bc ↔ Koszul dual A^!" conflates BRST pairing with algebraic Koszul dual. | **FIXED**: added "(BRST pairing, not literal)". |
| F30 | p.68 | MODERATE | Kontsevich | Weil table "Frob²=q ↔ D²=0" conflates functional equation with nilpotence. | **FIXED**: added "(functional equation)" and "(nilpotence)" labels. |
| — | p.68-69 | VERIFIED | Gaiotto | Miura defect χ_{W_2}/χ_H = (1-q) correct. ✓ |
| — | p.68-69 | VERIFIED | Beilinson | Ramanujan chain honest about Deligne external input. ✓ |
| — | p.66-69 | VERIFIED | All | All Dirichlet series, Euler-Koszul tiers, L-objects verified. ✓ |

### Pages 64-65 (Preface: DS reduction, central charge additivity, geometric localization)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F28 | p.64 | **CRITICAL** | Drinfeld/Gaiotto | "κ → ∞ on affine side" at critical level k=-3 is WRONG. Formula gives κ = 4(k+3)/3 = 0. Should be "κ → 0 (curvature vanishes)." AP3 pattern-completion error. | **FIXED** (preface.tex:1348) |
| — | p.64-65 | VERIFIED | Gaiotto | κ(ŝl₃_k) = 4(k+3)/3 correct by (k+h∨)dim g/(2h∨). ✓ |
| — | p.64-65 | VERIFIED | Beilinson | FF duality: dual of ŝl₃_k is ŝl₃_{-k-6}. ✓ |
| — | p.65 | VERIFIED | Witten | c_ghost = 6 = 2|Δ⁺(sl₃)| correct. ✓ |
| — | p.65 | VERIFIED | All | c(ŝl₃_k)=8k/(k+3), c(W₃)=2-24/(k+3), c_ghost=6 additivity. ✓ |

### Pages 62-63 (Preface: Hessian, spectral discriminant, critical dimension, W₃ datum)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F27 | p.63 | **SERIOUS** | Costello | W₃ Hessian matrix uses κ_c without definition. κ_c = (c-50)/2 (Hessian eigenvalue), NOT κ = 5c/6 (modular characteristic). Notational collision — same symbol used for different objects 24 lines apart. | **FIXED**: added "κ_c := (c-50)/2" and clarifying remark in preface.tex:1244. |
| — | p.62-63 | VERIFIED | Gaiotto | κ(W₃) = 5c/6, K₃ = 100. ✓ |
| — | p.62 | VERIFIED | Beilinson | Δ_{ŝl₂}(x) = (1-kx)(1-(k+4)x)/(1-2x). ✓ |
| — | p.63 | VERIFIED | Witten | Polyakov formula = arity-2 shadow at κ=1. ✓ |
| — | p.62 | VERIFIED | Kontsevich | βγ genus-2 shell: only loop² (no cubic ⟹ no sepoloop). ✓ |
| — | p.63 | VERIFIED | Drinfeld | [ℓ₄^(0)] = 16/(22+5c)·Λ for W₃. ✓ |

### Pages 60-61 (Preface: Section IV Holography, holographic datum, three examples, arithmetic)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F25 | p.61 | MODERATE | Witten | "Genre tower is the perturbative expansion of 3d quantum gravity" — unsupported physics claim. | **FIXED**: downgraded to "has the formal structure of a perturbative expansion". |
| F26 | p.61 | MODERATE | Drinfeld | KZ connection ℏ undefined in ∇_KZ = d - ℏΩ/z. | **FIXED**: added "(with ℏ = 1/(k+2))". |
| — | p.60-61 | VERIFIED | Gaiotto | Affine level notation ŝl₂_k correct (FF: -k-4). ✓ |
| — | p.60-61 | VERIFIED | Kontsevich | Deconcatenation coproduct and coderivation property standard. ✓ |
| — | p.60-61 | VERIFIED | Costello | MacMahon formula Π_{n≥2}(1-q^n)^{-(n-1)} correct (proved in w_algebras_deep.tex). ✓ |

### Pages 58-59 (Preface: modular homotopy type, projection table, completion, finite windows)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| — | p.58-59 | VERIFIED | All 6 | Genus-1 MC equation, projection table, strong filtration axiom, zigzag theorem, collision→r-matrix, κ examples. All correct. | ✓ |

### Page 57 (Preface: Section III, modular homotopy type in three stages)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F24 | p.57 | MODERATE | Witten | "Two spaces have same rational homotopy type iff minimal models isomorphic" — missing "simply connected" hypothesis. | **FIXED** (preface.tex:630) |
| — | p.57 | VERIFIED | Kontsevich | Sullivan minimal model description correct. ✓ |
| — | p.57 | VERIFIED | Beilinson | Stage 1 bar complex signs (moot since x²=0). ✓ |
| — | p.57 | VERIFIED | Costello | HPL transfer and Massey product m₃. ✓ |
| — | p.57 | VERIFIED | Gaiotto | Heisenberg = formal = Gaussian archetype. ✓ |

### Page 56 (Preface: shadow metric, HJ equation, growth rate, inter-channel coupling)

| — | p.56 | VERIFIED | All 6 | All formulas verified exactly (shadow metric, HJ, δ₆, ρ, critical cubic). ✓ |

### Page 55 (Preface: curvature at genus g≥1, κ formulas, Virasoro shadow tower, complementarity)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F23 | p.55 | MODERATE | Beilinson | "anti-symmetry κ+κ^!=0 holds only when Koszul dual stays in same linear family" — misleading phrasing. Virasoro IS same family but κ+κ^!=13≠0. Text logically correct (P⟹Q) but readers will misread as P⟺Q. | **FIXED**: clarified to "holds for KM/free-field; for W-algebras sum = K(g)·(H_N-1) nonzero". |
| — | p.55 | VERIFIED | Gaiotto | κ(ŝl_{2k}) = 3(k+2)/4 correct by (k+h∨)dim g/(2h∨). ✓ |
| — | p.55 | VERIFIED | Gaiotto | κ(βγ_λ) = 6λ²-6λ+1 = c(βγ)/2 correct. ✓ |
| — | p.55 | VERIFIED | Witten | Q^contact_Vir = 10/(c(5c+22)), poles at c=0, c=-22/5 ((2,5)-minimal). ✓ |
| — | p.55 | VERIFIED | Kontsevich | Quartic contact = Schur complement is a PROVED theorem (thm:schur-complement-quartic). ✓ |
| — | p.55 | VERIFIED | Drinfeld | Anomaly ratio ρ = Σ(m_i+1)^{-1} matches κ/c for all families. ✓ |
| — | p.55 | VERIFIED | Costello | Genus-2 three-shell decomposition (loopoloop/sepoloop/pf) by graph type. ✓ |

### Page 54 (Preface: graph-sum formula, Heisenberg collapse, Koszul dual, complementarity)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F21 | p.54 | **CRITICAL** | Gaiotto | F_g formula WRONG: stated B_{2g}/(2g(2g-2)), correct is (2^{2g-1}-1)/2^{2g-1} · \|B_{2g}\|/(2g)! (Faber-Pandharipande). Wrong formula propagated to 4 files (AP5). Division by zero at g=1. | **FIXED** (4 files: preface.tex:337,980; poincare_duality_quantum.tex:732; thqg_preface_supplement.tex:560; frontier_modular_holography_platonic.tex:1714) |
| F22 | p.54 | ~~SERIOUS~~ | Beilinson | ~~D_Ran B̄(A) ≃ A^!_∞ stated without proof.~~ **RETRACTED**: proved as thm:bar-cobar-isomorphism-main (chiral_koszul_pairs.tex:2344, ProvedHere). |
| — | p.54 | VERIFIED | Witten | κ=0 at critical level k=-h∨ is correct (Sugawara undefined, not divergent). ✓ |
| — | p.54 | VERIFIED | Drinfeld | Feigin-Frenkel involution k↦-k-2h∨ correct. ✓ |
| — | p.54 | VERIFIED | Kontsevich | Â-genus generating function correct (matches compute tests once wrong explicit formula removed). ✓ |

### Page 53 (Preface: modular convolution algebra, E₁ variant, from point to curve, graph-sum formula)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F20 | p.53 | MODERATE | Beilinson | "Every theorem is a projection of Θ_A" — rhetorical overclaim. Five main theorems ARE projections (§10.22.10.2), but "every" is false for ~2,850 claims. | **FIXED** → "the five main theorems are projections" with §-ref. |
| — | p.53 | VERIFIED | Witten | 't Hooft 1/N expansion (preface.tex:218-226). λ used as formal variable. Standard. ✓ |
| — | p.53 | VERIFIED | Kontsevich | Shadow tower algebraicity Q_L: PROVED (thm:riccati-algebraicity). ✓ |
| — | p.53 | VERIFIED | Costello | HPL transfer: Chapter 8 develops this fully. ✓ |
| — | p.53 | VERIFIED | Drinfeld | FAss/ribbon graph identification: standard, in preface.tex:207-216. ✓ |
| — | p.53 | VERIFIED | Gaiotto | d²_fib = κ·ω_g: correct; distinct from Weyl anomaly (c/12)·χ. ✓ |

### Page 52 (Preface: Feynman transform, stable graph complex, convolution table)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F18 | p.52 | MODERATE | Beilinson | "codim-2 boundary reached by exactly two one-edge degenerations" — imprecise. Should say "codim-2 stratum is intersection of exactly two codim-1 divisors, with opposite orientations." | **FIXED** (preface.tex:98-100) |
| F19 | p.52 | MINOR | Kontsevich | ℏ^{g(Γ)} uses total genus (b₁+Σg(v)), not loop number b₁ alone. Correct for modular operads but differs from classical GK convention. Already defined at line 71. | **CLOSED** (convention stated) |
| — | p.52 | VERIFIED | Costello | End_{B̄(A)} in convolution table is correct (bar complex IS the target). ✓ |
| — | p.52 | VERIFIED | Drinfeld | Lie bracket Koszul signs correct in cohomological convention. ✓ |
| — | p.52 | VERIFIED | Gaiotto | FCom(0,n) = B(Com)(n) at genus 0, recovering Lie cooperad. ✓ |
| — | p.52 | VERIFIED | Witten | ℏ^{g(Γ)} correctly captures total genus for path integrals on curves. ✓ |

### Page 51 (Preface: Section I, "From algebras to operads / modular operads")

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F16 | p.51 | **CRITICAL** | Beilinson | algebraic_foundations.tex:45 had T^c(sĀ) but differential uses s^{-1}. | **FIXED** |
| F17 | p.51 | ~~MINOR~~ | Kontsevich | ~~Cobar notation s vs s^{-1}.~~ Preface states "Cohomological convention throughout: |d|=+1" (line 2084) and applies consistently. No error. | **CLOSED** |

---

## SUMMARY: Pages 1-96 audited, 50 findings resolved

| Category | Count | Details |
|----------|-------|---------|
| **FIXED** (text corrections applied) | 31 | F1-F3, F16, F18, F20-F21, F23-F50, F51-F61 |
| **CLOSED** (verified not errors) | 19 | F4-F15, F17, F19, F22, + 5 from §3-5 panels |
| **OPEN** | 0 | — |

---

## CUMULATIVE GLOSSARY (terms as first encountered)

- **Chiral algebra** (p.1): A on smooth curve X; D-module with chiral bracket μ: A⊠A → Δ_*A
- **Bar complex** B̄_X(A) (p.1): Wedge A^⊠n against η = ∧d log(z_i-z_j) on FM compactification, extract residues
- **Arnold relation** (p.1): η_ij∧η_jk + η_jk∧η_ki + η_ki∧η_ij = 0; equivalent to d²=0
- **Fulton-MacPherson** C̄_n(X) (p.1): Compactification of Conf_n(X) by iterated real blowup along diagonals
- **Verdier duality** D_Ran (p.1): Duality on D-modules on Ran(X); produces A^!_∞ from B̄_X(A)
- **Koszul locus** (p.1): Where bar spectral sequence collapses at E_2; A^!_∞ formal there
- **Three pillars** (p.1): MS24 (homotopy chiral), Mok25 (log FM), RNW19 (convolution sL∞)
- **Modular convolution algebra** g^mod_A (p.1): Conv_∞(C^{logFM}_mod, End_{Ch∞}(A^ch_∞))
- **Universal MC element** Θ_A (p.1): D_A - d_0; satisfies MC because D²_A = 0
- **Shadow tower** Θ^{≤r}_A (p.2): Finite-arity truncations of Θ_A
- **κ** (p.3): Modular characteristic; scalar trace of Θ_A at arity 2
- **Shadow metric** Q_L(t) (p.3): (2κ+3αt)²+2Δt²; classifies shadow depth
- **Shadow depth** r_max (p.3): G(2), L(3), C(4), M(∞) classification
- **Koszulness** (p.3): 12 conditions; 10 unconditional equivalences + 1 conditional + 1 one-directional
- **Operad** P (p.51): Collection {P(n)} with Σ_n-action, composition, unit
- **Operadic bar** B(P) (p.51): ⊕_T ⊗_v P(In(v)) ⊗ det(E(T)), sum over trees
- **Modular operad** O (p.51): Operations indexed by connected stable graphs; genus labels g(v) with stability 2g(v)-2+val(v)>0
- **Stable graph** Γ (p.51): Connected graph with genus labels; total genus g(Γ) = b₁(Γ) + Σ g(v)
- **Separating gluing** ξ_sep (p.51): O(g₁,n₁+1)⊗O(g₂,n₂+1) → O(g₁+g₂,n₁+n₂)
- **Non-separating gluing** ξ_ns (p.51): O(g,n+2) → O(g+1,n)

---

## PAGE-BY-PAGE NOTES

### Pages 1-2 (Abstract: construction and organizing principle)
- Bar complex from log forms on FM compactification; D²=0 from Arnold + modular operad boundary
- Three-pillar integration: MS24 + Mok25 + RNW19
- Θ_A := D_A - d_0; six-component differential (all 6 defined)

### Pages 3-4 (Abstract: main theorems and landscape)
- Theorems A-D+H correctly summarized
- Shadow tower, metric, depth classification G/L/C/M
- Koszulness 12 conditions (10+1+1)
- κ formula table: ~~W_N wrong~~ FIXED to c·(H_N-1)
- Frontier: Yangian/RTT, HS-sewing, Swiss-cheese, holographic programme

### Pages 5-50 (Table of Contents)
- Pure structural; no mathematical claims
- Ch.10 (Higher genus) is 320pp — the heart of the book
- Ch.7 (Bar-cobar adjunction) is 130pp
- Structure: Overture + Part I (Engine) + Part II (Landscape) + Part III (Bridges) + Part IV (Frontier) + Appendices

### Page 51 (Preface: Section I, From algebras to operads / modular operads)
- Classical bar-cobar over a point: standard, correct
- Operadic bar formula (1) with det(E(T)): matches GK94/LV12
- Modular operads: stability condition, gluing maps — all correct
- ~~Desuspension error~~ FIXED in algebraic_foundations.tex:45

### Concordance constitutional pass (2026-03-31, Codex)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F67 | — | **SERIOUS** | Beilinson | Concordance opening compressed Theorems A--H into “projections of the scalar level $\kappa$,” conflating theorem-level consequences of the full MC package with the scalar trace. Matching overclaims also appeared in `algebraic_foundations.tex` and `higher_genus_modular_koszul.tex`. | **FIXED** |
| F68 | — | **MODERATE** | Gaiotto | Concordance Theorem D summary omitted the single-generator scope of the cohomological identity and overcompressed the multi-weight case. Narrowed to the single-generator class identity plus the proven free-energy output. | **FIXED** |
| F69 | — | **MODERATE** | Kontsevich | Concordance/preface/theory prose stated “the shadow tower is the $L_\infty$ formality obstruction tower” without the genus-$0$ restriction required by `thm:shadow-formality-identification`; positive-genus corrections belong to the quantum layer. | **FIXED** |
| F70 | — | **SERIOUS** | Witten | Concordance promotions table advertised `conj:EO-recursion`(b) as promoted to “EO recursion for Koszul algebras,” but the source still carries a conditional/heuristic spectral-curve identification. Replaced with the proved MC-shadow recursion statement and fenced the residual conditionality. | **FIXED** |

### Uniform-weight Theorem D / GRR pass (2026-03-31, Codex)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F71 | — | **SERIOUS** | Beilinson | `introduction.tex` still summarized Theorem D as a globally universal scalar package: all-genus `\mathrm{obs}_g=\kappa\lambda_g`, all-genus `\hat A` series, and anomaly cancellation at all genera. The source theorem only gives all-genus scalar factorization on the uniform-weight lane, with genus~1 unconditional for arbitrary modular Koszul algebras. | **FIXED** |
| F72 | — | **CRITICAL** | Beilinson | `higher_genus_foundations.tex` Theorem `thm:genus-universality` still stated the Faber--Pandharipande free-energy formula without the multi-weight fence, even though its own proof and `rem:multi-generator-obs` show the higher-genus multi-weight factorization is open. Narrowed item (iii) to the uniform-weight lane and genus~1 unconditionality. | **FIXED** |
| F73 | — | **SERIOUS** | Kontsevich | The downstream GRR / generating-function theorems (`prop:grr-bridge`, `thm:universal-generating-function`, `thm:family-index`) inherited the same overstatement and presented the all-genus `\hat A` series as global. These were rewritten to state the proved uniform-weight scope and the residual genus~1 unconditional clause explicitly. | **FIXED** |
| F74 | — | **MODERATE** | Witten | Connection-layer summaries (`concordance.tex`, `thqg_perturbative_finiteness.tex`, `genus_complete.tex`, `bv_brst.tex`) still advertised the scalar free-energy formula as an unrestricted output of Theorem D. The stale summaries were narrowed to the uniform-weight lane so the narrative no longer outruns the theorem surface. | **FIXED** |

### THQG fixed-genus / scalar-lane convergence pass (2026-03-31, Codex)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F94 | — | **MODERATE** | Beilinson | Residual definition and concordance surfaces still wrote `\mathrm{obs}_g=\kappa\lambda_g` as a global identity. In `higher_genus_foundations.tex` this even appeared inside the definition of the obstruction projection itself, and in `concordance.tex` Theorem D was still summarized without the lane restriction. These were narrowed to: genus~1 universally, all genera only on the proved uniform-weight scalar lane. | **FIXED** |
| F95 | — | **CRITICAL** | Witten | `thqg_perturbative_finiteness.tex` still contained a source-level analytic overclaim cluster: the scalar partition-function definition, genus decomposition, effective bound, two-dimensional convergence theorem, tensor-product theorem, large-`N` `\mathcal W_N` remark, and quantitative tables all silently re-exported the obsolete global scalar closed form. This also produced a concrete falsehood: Gaussian rows were assigned `R_{\mathrm{grav}}=\infty`, conflating finite arity depth with infinite genus radius, and `\beta\gamma` still carried unsupported higher-genus entries. The chapter now distinguishes genuswise finiteness from scalar-lane convergence, replaces the false bidisk theorem by fixed-genus/Gaussian statements, corrects Gaussian radii to `4\pi^2`, and removes unsupported multi-weight higher-genus table data. | **FIXED** |
| F96 | — | **SERIOUS** | Witten | The BTZ subsection in both Volume I and Volume II was still claiming that the Virasoro scalar shadow series is the **full** perturbative BTZ partition function and that the shadow method therefore “extends automatically to all genera.” What is actually proved is narrower: Brown--Henneaux identifies the Virasoro scalar normalization, giving the genus~1 scalar term and the all-genus scalar series on the Virasoro scalar lane, but no comparison theorem to the full analytic BTZ partition function is constructed. Both volumes were rewritten to say “BTZ-normalized scalar shadow series,” and the higher-genus proposition was retitled accordingly. | **FIXED** |
| F97 | — | **CRITICAL** | Beilinson | The semiclassical/large-`c` subsection in both volumes contained a source-level bookkeeping error: it described the Virasoro scalar series as an “asymptotic expansion in `1/\kappa` that happens to converge,” and the proposition `prop:thqg-I-1c-expansion` outright identified `\log Z_{\mathrm{grav}}^{\mathrm{scal}}` with the genus series, contradicting the chapter’s own definition `Z_{\mathrm{grav}}^{\mathrm{scal}}=\sum_g F_g \hbar^g`. The corrected statement now concerns the **reduced scalar series** `Z_{\mathrm{grav}}^{\mathrm{scal}}-F_0`, keeps the tree-level term separate, and states the honest convergent `1/c` expansion on the Virasoro scalar lane. | **FIXED** |
| F98 | — | **MODERATE** | Kontsevich | The monster-CFT numerical example in both volumes still evaluated the scalar closed form at `\hbar=1` incorrectly by a factor of `2`: it wrote `12/\sin(1/2) \approx 25.0`, but the scalar bookkeeping series is `12\cdot(1/2)/\sin(1/2) \approx 12.515`. The same sentence also called this the “total perturbative partition function,” reintroducing the scalar-vs-full conflation by example. Both remarks now use the corrected scalar bookkeeping value, and the older Volume II Gaussian-complexity summary was aligned to the narrower Volume I wording. | **FIXED** |
| F99 | — | **CRITICAL** | Beilinson | Volume II still contained the pre-rectification THQG wrapper layer even after Volume I had been narrowed: the scalar partition-function definition still inserted the global closed form, the perturbative-finiteness and full-partition theorems still claimed a general full genus-sum radius and an `O(\hbar/\kappa^2)` scalar dominance statement, the two-parameter theorem still advertised a bidisk convergence result, and the quantitative table still listed Gaussian `R_{\mathrm{grav}}=\infty` plus unsupported higher-genus `\beta\gamma` data. These statements were all downgraded to the current proved surface: fixed-genus finiteness generally, scalar closed form only on the uniform-weight lane, Gaussian full-partition convergence with radius `4\pi^2`, and no general bidisk/full-radius theorem beyond that lane. | **FIXED** |
| F100 | — | **CRITICAL** | Beilinson | The early “shadow free energies” subsection was still exporting all-genus scalar universality upstream of the later corrected theorems. In both volumes, the definition of `F_g` built in the demoted identity `\int \kappa\lambda_g`, the vacuum-energy remark treated the Faber--Pandharipande package as global, and the explicit-values proposition listed the Bernoulli formulas as unconditional. Volume II was further stale in the closed-form proposition, universality remark, generating-function theorem, absolute-convergence theorem, and related remarks, all of which still treated the scalar closed form as valid for arbitrary modular Koszul algebras. The subsection now states the honest scope: all-genus Bernoulli/`\hat A` formulas only on the uniform-weight scalar lane, genus~1 universally, and descriptive remarks fenced to that proved lane. | **FIXED** |
| F101 | — | **SERIOUS** | Witten | Volume II still had downstream THQG connective tissue that re-imported the obsolete global scalar package after the theorem surfaces had been repaired: the genus-decomposition proposition wrote `F_g^{\mathrm{scal}}=\kappa\lambda_g^{\mathrm{FP}}` without the lane fence, the boundary-to-bulk summary said the closed partition function “follows” algorithmically from OPE data, the finiteness-mechanism chain concluded directly with `F_g=\kappa\lambda_g^{\mathrm{FP}}<\infty`, the tensor-product proposition/proof treated higher-genus scalar additivity as unconditional, and the “complete finiteness theorem” remark again claimed global convergence and meromorphic continuation. These were all narrowed to the current record: genuswise finiteness generally, scalar closed form and higher-genus additivity only on the proved scalar lane, and universal genus~1 separately. | **FIXED** |
| F102 | — | **CRITICAL** | Beilinson | The `S`-duality free-energy chapter in both volumes was still asserting a global all-genus scalar complementarity theorem and using it to derive Virasoro and bosonic-string cancellations at every genus. That contradicted the corrected Theorem~D surface, where genus~1 is universal but all-genus scalar factorization is only proved on the uniform-weight scalar lane. The theorem, its self-dual/partition-function corollaries, the Virasoro functional-equation example, the `c=26` degeneration theorem, and the bosonic-string budget were all narrowed to: universal genus~1 identities, all-genus formulas only under the explicit uniform-weight hypothesis, and no higher-genus Virasoro/bosonic cancellation claim outside that lane. | **FIXED** |
| F103 | — | **CRITICAL** | Beilinson | The scalar-lane fence had leaked back into the constitution and the Volume II summary atlas. In `concordance.tex` and `higher_genus_modular_koszul.tex`, the Hodge--lambda description of Theorem~D had reverted to an unqualified global identity `F_g=\kappa\lambda_g^{\mathrm{FP}}`. In `rosetta_stone.tex`, the `\hat A` theorem and its remarks escalated this into “everything in the monograph that depends on genus” and the outright false claim that classes `\mathbf{L}` and `\mathbf{C}` have no higher-arity shadow corrections, contradicting the shadow-depth classification `r_{\max}=3,4`. The affected surfaces now state the all-genus `\hat A` package only on the uniform-weight scalar lane, keep genus~1 universal separately, and treat classes `\mathbf{G}`/`\mathbf{L}`/`\mathbf{C}`/`\mathbf{M}` according to their actual finite or infinite shadow depth. | **FIXED** |
| F104 | — | **CRITICAL** | Beilinson | The core Theorem~D chain had regressed again. In `higher_genus_foundations.tex`, the theorem statement itself still said the Faber--Pandharipande formula holds for all modular Koszul algebras at all genera, its proof promoted the weight-1 propagator to a full multi-channel factorization theorem, and the later GRR bridge/family-index export inherited the same false global scope. The introduction repeated the same overclaim in both its early overview and its `\kappa`-properties synopsis, and the Virasoro functional-equation computation in `thqg_gravitational_s_duality.tex` ended by restating the false global factorization. These surfaces were all narrowed together: all-genus `\mathrm{obs}_g` / `F_g` factorization only on the proved uniform-weight scalar lane, genus~1 universal separately, multi-weight higher-genus factorization explicitly open, and GRR/family-index exports restricted to the scalar lane they actually use. | **FIXED** |
| F105 | — | **CRITICAL** | Beilinson | A new regression cluster was promoting vanishing of the scalar obstruction coefficient `\kappa` into vanishing of the full Maurer--Cartan/shadow tower. In `higher_genus_foundations.tex`, the “critical level characterization,” “tautological class map,” and “obstruction lifting criterion” were still stated globally, and `rem:dichotomy-theta` literally concluded that `\kappa=0` makes the universal MC element trivial. Downstream copies in the Virasoro `c=0` / `c=26` discussions (`thqg_gravitational_s_duality.tex`, `thqg_critical_string_dichotomy.tex`, `bar_complex_tables.tex`, `frontier_modular_holography_platonic.tex`) and in `arithmetic_shadows.tex` were accordingly calling `\mathrm{Vir}_0` “trivial” or setting `\Theta_{\mathrm{Vir}_0}^{\le r}=0`. These surfaces are now aligned to the honest statement: `\kappa=0` universally kills the genus~1 scalar term and, on the proved scalar lane, the scalar all-genus package; it does not by itself force the full higher-arity tower or the full MC element to vanish. | **FIXED** |
| F106 | — | **CRITICAL** | Beilinson | The critical-level / zero-anomaly surfaces were still conflating scalar cancellation with full differential collapse. In `thqg_gravitational_s_duality.tex` (both volumes), the affine critical-level theorem claimed `\Theta_{\widehat{\fg}_{-h^\vee}}=0` and `D=\dzero` from `\kappa=0`, even though the critical-level bar differential still contains the nontrivial simple-pole/screening part. In `higher_genus_modular_koszul.tex`, the same false import reappeared as “at critical level the MC element vanishes.” In Volume II’s `thqg_anomaly_extensions.tex`, effective matter--ghost scalar cancellation at `c=26` was escalated into a full “gravitational collapse,” calling `\mathrm{Vir}_0` trivial and using `\Theta=0` as if it described the coupled system. These surfaces now distinguish the vanishing scalar/minimal class `\Theta^{\min}` from the full MC/transgression object, keep the affine critical theorem at the uncurved-but-nonzero-differential level, and demote the `c=26` anomaly story to the proved effective scalar cancellation plus a formal zero-anomaly specialization. | **FIXED** |
| F107 | — | **CRITICAL** | Beilinson | A final copy-drift layer was still exporting the same false collapse into constitution, preface, example, and Volume II gravity-summary surfaces. In Volume I, `concordance.tex`, `preface.tex`, `kac_moody.tex`, and `thqg_perturbative_finiteness.tex` still said the Feigin--Frenkel/critical-level story occurs “where the shadow tower collapses” or even `\Theta_\cA=0`, instead of the corrected statement that only the scalar class vanishes and the bar complex becomes uncurved. In `editorial_constitution.tex`, self-duality was still incorrectly promoted to `\Theta_\cA = -\Theta_\cA = 0`, contradicting the nonzero self-dual Virasoro point at `c=13`. In Volume II, `holomorphic_topological.tex`, `ht_physical_origins.tex`, `3d_gravity.tex`, and `thqg_3d_gravity_movements_vi_x.tex` were still turning the Witt / `c=26` degeneration into “no higher `A_\infty` corrections,” “the genus tower vanishes,” “the transgression is trivial,” and even a trivial gravitational partition function. These surfaces now keep only the proved scalar/effective vanishing statements, explicitly separate them from the full higher-arity tower, and fence the remaining closed-string/Witt identifications as open. | **FIXED** |
| F108 | — | **CRITICAL** | Beilinson | The `G9` theorem surface in `twisted_holography_quantum_gravity.tex` had regressed into a direct contradiction of the corrected critical-string chapters. In Volume I it still said the dual sector at `c=26` is “trivial,” that the resonance-filtered bar-cobar “collapses,” and that the two Lagrangian summands coincide; in Volume II the same theorem was even worse, calling `\mathcal{H}(\mathrm{Vir}_{26})` self-conjugate under `S`-duality and `\mathrm{Vir}_0` the “trivial Virasoro algebra.” The Vol II `3d_gravity.tex` summary exported the same mistake by presenting `|\eta|^{(c-26)/6}` as the full partition function and calling the `c=26` exterior degeneration a full topological-gravity point. These surfaces are now aligned to the actual record: `c=13` is the Koszul self-dual point; `c=26` is the maximally asymmetric critical-string point where the dual scalar package vanishes, the depth-zero resonance model closes only at the scalar level, and the exterior/Clifford degeneration is only the effective scalar specialization, not the full coupled theory. | **FIXED** |
| F109 | — | **CRITICAL** | Beilinson | A further export layer in Volume II was still conflating the effective scalar series with the full gravitational theory. In `thqg_3d_gravity_movements_vi_x.tex`, the theorem “Gravitational partition function” stated `Z_{\mathrm{grav}}=\exp(((c-26)/2)(\hat A(i\hbar)-1))` as the full all-genera partition function of 3d Virasoro gravity, then specialized this to `Z_{\mathrm{grav}}=1` and “the gravitational theory is trivial” at `c=26`. The surrounding decomposition proposition and later summary line repeated the same mistake, while the THQG overview files (`twisted_holography_quantum_gravity.tex` in both volumes) still called `\mathrm{Vir}_0` “trivial Virasoro,” and `thqg_anomaly_extensions.tex` still used the same phrase in a quartic-resonance summary. These surfaces are now aligned to the corrected anomaly record: the closed `\hat A` formula is the effective scalar partition function, `c=26` makes that scalar series trivial but does not trivialize the full gravitational theory, and `\mathrm{Vir}_0` is only on the uncurved scalar locus, not a trivial algebra. | **FIXED** |
| F110 | — | **CRITICAL** | Beilinson | The `c=26` Virasoro/string surface was still carrying a source-level theorem mismatch. In `free_fields.tex`, `thm:virasoro-moduli` claimed a full identification `H^*(\barB_{\mathrm{geom}}(\mathrm{Vir}_{26})) \cong H^*(\overline{\mathcal M}_{0,n+3})`, the opening summary said BRST, amplitudes, and modular invariance “follow,” and the ghost remark promoted the genus-0 total-string BRST/bar comparison into a direct matter-only identity `Q_{\mathrm{BRST}} = d_{\barB}` with physical states equal to matter bar cohomology. `concordance.tex` was simultaneously citing the same theorem label as a proved constitutional item, and Volume II’s `anomaly_completed_frontier.tex` still said effective scalar cancellation at `c=26` “restores modular invariance.” These surfaces are now aligned to the actual record: the proved theorem is the critical genus-0 descent statement `d_{\mathrm{equiv}}^2 = (c-26)\mu`, giving an honest quotient cochain complex at `c=26`; the proved BRST/bar result is the genus-0 quasi-isomorphism for the total matter+ghost theory; moduli-space cohomology, amplitude localization, and full modular invariance remain conditional or heuristic. | **FIXED** |
| F111 | — | **SERIOUS** | Beilinson | The immediate satellites of the repaired `c=26` theorem in `free_fields.tex` were still exporting the old overclaim. The Koszul-duality summary table listed `\mathrm{Vir}_{26}` with context “Moduli ↔ BRST” and status `\ClaimStatusProvedHere`, the proposition `prop:moduli-degeneration` was still identifying a moduli-space residue operator with “the bar differential restricted to forms on moduli,” and the “Low degrees for Virasoro” example still reported `H^0,H^1,H^2` by reading off the cohomology of `\overline{\mathcal M}_{0,n+3}`. These are now narrowed to the honest surface: the table records only the proved genus-0 BRST/bar comparison; the proposition is a geometric boundary-residue model on logarithmic forms on moduli space; and the low-degree example is explicitly the moduli-space model, not an internal computation of Virasoro bar cohomology. | **FIXED** |
| F112 | — | **CRITICAL** | Beilinson | The critical-string application chapters were still promoting the repaired algebraic `c=26` shadow into the full classical no-ghost theorem. In both versions of `thqg_critical_string_dichotomy.tex`, the remark “The no-ghost theorem in bar-cobar language” literally identified the physical state space with `H^*(\barB^{(g)}(\mathrm{Vir}_0))`. In Volume II, the chapter opening also said `u=0` *is* the no-ghost theorem, and in both versions of `thqg_gravitational_s_duality.tex` the Lagrangian remark said positivity of the physical Hilbert space “follows” from the complementarity pairing. These surfaces are now aligned to the actual record: `u=0` and the exterior ghost factor provide an algebraic shadow of ghost-sector simplification, and the complementarity pairing gives an algebraic nondegenerate pairing, but the manuscript does not prove the classical no-ghost theorem or identify the full bosonic-string physical state space with Virasoro bar cohomology. | **FIXED** |
| F113 | — | **SERIOUS** | Beilinson | Two remaining `c=26` export surfaces were still overstating the algebraic record after the no-ghost cleanup. In `deformation_quantization_examples.tex`, the Virasoro compatibility computation ended with “at `c=26` the roles reverse (the no-ghost theorem),” collapsing the repaired algebraic shadow back into the full physical theorem. In `thqg_preface_supplement.tex`, the “transgression algebra” discussion went further: it claimed the bosonic-string BRST complex is a strict dg algebra and that the physical state space is `H^*(\mathrm{Tr}_{26})`, then said the gravitational path integral reduces to ordinary cohomology. These are now narrowed to the actual record: `\mathrm{Tr}_{26}` is a strict algebraic transgression model, not an identified physical-state complex; its ordinary cohomology is an algebraic invariant; and any physical BRST/path-integral interpretation requires extra comparison input not proved in the manuscript. | **FIXED** |
| F114 | — | **MODERATE** | Beilinson | After the broader no-ghost cleanup, one literal mirror slogan still remained in Volume I: the opening paragraph of `thqg_critical_string_dichotomy.tex` still said `u=0` *is* the no-ghost theorem expressed in bar-cobar language, even though the later Volume I remark and the entire Volume II surface had already been narrowed to the weaker “algebraic shadow” statement. This final stale sentence is now aligned with the corrected wording, so both volumes consistently describe `u=0` only as the exterior/uncurved algebraic shadow of the ghost-sector simplification appearing in the no-ghost theorem. | **FIXED** |
| F115 | — | **MODERATE** | Beilinson | A final status-drift line remained in the conjecture index. `editorial_constitution.tex` was still summarizing `conj:cobar-physical` as “Cobar construction as physical state space,” but the actual conjecture in `cobar_construction.tex` is already narrower: cobar elements are heuristic algebraic shadow data for on-shell propagators, and the cohomology is only conjectured to model physical on-shell scattering amplitudes. The index entry is now aligned to that actual conjectural scope. | **FIXED** |
| F116 | — | **CRITICAL** | Beilinson | The BRST/bar dictionary still had a source-level overclaim cluster. In `thqg_preface_supplement.tex`, a subsection was still stating that “the BRST complex of the bosonic string is the bar construction,” equating the anomaly-free locus with literal bar/BRST identity and extending the same language to the open-closed case. The underlying heuristic conjecture in `cobar_construction.tex` was malformed at the source level as well: it identified the bar complex itself with BRST cohomology and mapped “physical states” directly to bar classes, while `editorial_constitution.tex` exported that slogan as if it were the settled conjectural content. Volume II’s `thqg_anomaly_extensions.tex` then pushed the same drift one step further by identifying the BRST cohomology of the genus-`g` exterior ghost algebra with the full genus-`g` physical string state space. These surfaces are now aligned to the honest record: the proved algebraic input is the genus-`0` total matter+ghost BRST/bar quasi-isomorphism; beyond that, the BRST/bar dictionary is heuristic; curved/coderived bar models remain algebraic shadow data rather than proved physical-state complexes; and the exterior ghost algebra supplies only ghost zero-mode bookkeeping, not the full genus-`g` physical state space. | **FIXED** |
| F117 | — | **CRITICAL** | Beilinson | The new scalar-saturation proof had a real source-level gap. In `higher_genus_modular_koszul.tex`, Proposition `prop:saturation-equivalence` was using `\dim H^2_{\mathrm{cyc}}=1` to conclude `\Theta_{\cA}^{\min}=\kappa(\cA)\eta\otimes\Lambda`, but one-dimensionality of the cyclic direction only gives `\Theta_{\cA}^{\min}=\eta\otimes\Gamma_{\cA}` for some tautological coefficient `\Gamma_{\cA}`. The Kuranishi map vanishes by parity, so the MC equation places no constraint on which class in `H^*(\overline{\mathcal M}_g)` appears. This invalidated the claimed proof of `thm:algebraic-family-rigidity` as a global scalar-saturation theorem and forced `thm:multi-generator-universality` back to a conditional statement. The correction propagated to the introduction, concordance, `W`-algebra summaries, THQG finiteness chapter, `CLAUDE.md`, and the new compute/test surfaces, which now distinguish proved cyclic line-concentration from the still-open tautological-purity step `\Gamma_{\cA}=\kappa(\cA)\Lambda`. | **FIXED** |

### Multichannel MC recursion audit (2026-03-31, Codex)

| # | Page | Severity | Examiner | Finding | Status |
|---|------|----------|----------|---------|--------|
| F118 | — | **SERIOUS** | Beilinson | `compute/lib/multichannel_mc_recursion.py` had drifted into an untested scratch proof: it mixed exact W_3 channel data with contradictory normalization guesses, temporarily omitted the stable self-loop-plus-bridge boundary graph in `\overline{\mathcal M}_{1,2}`, and its own naive genus-2 graph sum failed the known single-channel Virasoro benchmark `F_2=\kappa\lambda_2^{FP}`. The file has been rewritten as an explicit audit harness that preserves the exact channel data, records the failed genus-(1,2) and genus-2 lifts as negative diagnostics, and adds targeted tests so this surface no longer masquerades as evidence for the open multi-generator universality step. | **FIXED** |
| F119 | — | **SERIOUS** | Beilinson | The Gaussian/class-G export layer was still promoting arity collapse into a full all-genus scalar formula. In `thqg_gravitational_complexity.tex`, class~G was summarized as if spectral-sequence collapse forced `\Theta_\cA=\kappa\eta\otimes\Lambda`, and the Gaussian potential corollary was then exporting the Heisenberg Fredholm determinant to every class-G algebra. In `thqg_preface_supplement.tex`, the same drift appeared in the narrative description of class~G, the `\hat A`-genus generating-function subsection, and the soft-theorem summary, which also listed `$bc$-systems of any weight' as if their higher-genus scalar package were already settled. These surfaces are now aligned to the honest statement: class~G means the MC element is purely scalar in arity, `\Theta_\cA=\eta\otimes\Gamma_\cA`; the stronger identification `\Gamma_\cA=\kappa(\cA)\Lambda` holds on the proved scalar lane and remains conditional in general multi-weight Gaussian families; and the Fredholm / `\hat A` formulas are exported only with that lane restriction, with genus~1 kept unconditional separately. | **FIXED** |
| F120 | — | **CRITICAL** | Beilinson | A deeper scalar-saturation conflation was still active in the core MC2 theorem chain and its Gaussian/Fredholm exports. In `higher_genus_modular_koszul.tex`, Theorems `thm:universal-theta`, `thm:mc2-bar-intrinsic`, `thm:mc2-full-resolution`, `thm:tautological-line-support`, and `thm:theta-direct-derivation`, together with Corollary `cor:scalar-saturation`, were still sliding between three distinct statements: unconditional intrinsic existence of `\Theta_\cA`, one-channel line concentration `\Theta_\cA^{\min}=\eta\otimes\Gamma_\cA`, and the stronger all-genera scalar formula `\Gamma_\cA=\kappa(\cA)\Lambda`. Downstream, `concordance.tex`, `editorial_constitution.tex`, `introduction.tex`, `kac_moody.tex`, `preface.tex`, and the class-G Fredholm theorems in both volumes were then exporting that conflation as if one-channelity or class-G arity collapse forced the full scalar package and hence a universal Fredholm determinant. These surfaces are now aligned to the honest record: the universal MC element exists intrinsically by the bar-intrinsic construction; genus~1 scalar trace is universal, while the all-genera `\kappa\lambda_g` package is only proved on the uniform-weight lane; one-channelity gives only line concentration `\eta\otimes\Gamma_\cA`; `\Theta^{\min}=\kappa\eta\otimes\Lambda` is a proved scalar-lane statement, not a generic one-channel theorem; and the Fredholm determinant is claimed only for Gaussian algebras on that proved scalar lane, not for arbitrary class-G families. | **FIXED** |
| F121 | — | **CRITICAL** | Beilinson | The new scalar-saturation proof still had an unrectified `\mathcal{W}_N/\mathcal{W}_\infty` export leak. In `higher_genus_modular_koszul.tex`, the principal `\mathcal{W}_N` proposition and the `\mathcal{W}_\infty` corollary were still treating `\dim H^2_{\mathrm{cyc}}=1` as if it proved full scalar saturation `\Theta^{\min}=\kappa\,\eta\otimes\Lambda`, even though after F117/F120 that hypothesis only yields one-dimensional cyclic line concentration `\Theta^{\min}=\eta\otimes\Gamma`. The same false upgrade then propagated into `w_algebras_deep.tex`, `landscape_census.tex`, `thqg_holographic_reconstruction.tex`, the Volume II `w`-algebra mirrors, `chiral_hochschild_koszul.tex`, and nearby Gaussian/Fredholm / reconstruction summaries, where `\mathcal{W}_\infty` or class~G was still described as if a single scalar `\kappa` controlled the whole modular package. These surfaces are now synchronized to the honest scope: principal `\mathcal{W}_N` and `\mathcal{W}_\infty` have a one-dimensional minimal cyclic direction, Virasoro (`N=2`) recovers the scalar formula, but for `N\ge3` and the `\mathcal{W}_\infty` limit the stronger identification `\Gamma=\kappa\Lambda` remains open; class-G/Fredholm exports refer only to the proved Gaussian scalar lane or to Heisenberg itself. | **FIXED** |
| F122 | — | **CRITICAL** | Beilinson | The scalar-saturation conflation was still active one layer lower in the theorem graph. In `higher_genus_modular_koszul.tex`, the non-principal `\mathcal{W}` proposition, the “functorial stability of scalar saturation” wrapper, `thm:cyclic-rigidity-generic`, and especially `cor:effective-quadruple` were still exporting one-dimensional cyclicity as if it made `\Theta_{\cA}^{\min}` recoverable from `\kappa(\cA)` alone. The corollary had become internally contradictory: its proof explicitly acknowledged the open tautological-purity gap `\Gamma_\cA=\kappa(\cA)\Lambda`, while its statement still reduced the characteristic package to `(\kappa,\Delta,\Pi,\mathcal H)`. The same drift then propagated into `conj:scalar-saturation-universality`, the admissible-sector mirror in `bar_cobar_adjunction_inversion.tex`, the constitution, the divisor-core appendix, and a lattice summary remark. These surfaces are now aligned to the honest theorem surface: generic/algebraic-family/admissible results prove one-channel line concentration `\Theta^{\min}=\eta\otimes\Gamma` and vanishing primitive tangent space; the standard landscape reduces to an effective `(\Gamma,\Delta,\Pi,\mathcal H)` package; and the stronger scalar formula `\Gamma=\kappa\Lambda` remains a separate open step outside the proved uniform-weight lane. | **FIXED** |
| F123 | — | **SERIOUS** | Beilinson | The appendix/frontier summary layer was still silently importing the full scalar package from the wrong place. In `branch_line_reductions.tex`, the opening correction note said the “scalar-saturated class `\Theta_\cA` for one-channel families” was already available from the universal-MC theorems, eliding the distinction between universal existence of `\Theta_\cA`, one-channel line concentration `\eta\otimes\Gamma_\cA`, and the stronger scalar-lane specialization `\kappa\eta\otimes\Lambda`. In `frontier_modular_holography_platonic.tex`, the core bullet list and the fixed-point theorem were still using the vague phrase “scalar-saturated modular Koszul locus,” then proving all-genus vanishing/propagation by citing genus universality as if the full scalar package were automatic. And in `genus_complete.tex`, the “finite data” remark was still stating `F_g=\kappa\lambda_g^{FP}` for a general Koszul chiral algebra rather than only on the proved scalar lane. These surfaces now say the honest thing: the universal MC class is always available, one-channel families have a minimal line `\eta\otimes\Gamma_\cA`, the full scalar package must be assumed or placed on the proved scalar lane, and outside that lane only the genus-1 scalar term is unconditional. | **FIXED** |
| F124 | — | **CRITICAL** | Beilinson | The scalar-saturation gap had leaked back into a live theorem-status cluster. In `higher_genus_modular_koszul.tex`, `thm:multi-generator-universality` was still marked `\ClaimStatusProvedHere` and still argued that one-channel line concentration plus parity-vanishing of the Kuranishi map forces `\Gamma_\cA=\kappa(\cA)\Lambda`; the adjacent remark then declared the open problem resolved, and `thm:shadow-tautological-relations` used that false theorem to conclude unconditional integrated vanishing of the shadow correction. In `higher_genus_foundations.tex`, the companion proof sketch still wrote `\Theta^{\min}=\kappa\,\eta\otimes\Omega`, and the open problem block was literally marked “RESOLVED.” The wrapper layer in `bar_construction.tex`, `concordance.tex`, `bar_cobar_adjunction_inversion.tex`, and `higher_genus_complementarity.tex` was also still paraphrasing one-channelity as if it automatically meant full `\kappa`-control of the scalar genus tower. These surfaces are now aligned to the honest record: `thm:multi-generator-universality` is conditional on the missing tautological-purity step `\Gamma_\cA=\kappa(\cA)\Lambda`; the open problem is open again; shadow-tautological integrated vanishing is fenced to the proved scalar lane or any other context where the scalar free-energy formula is known; and the categorical-logarithm/complementarity wrappers now explicitly distinguish one-channel line concentration from the stronger full scalar package. | **FIXED** |
| F125 | — | **SERIOUS** | Beilinson | A residual local proposition in `higher_genus_modular_koszul.tex` was still reintroducing the scalar-saturation gap in gauge-theoretic language. `prop:one-channel-gauge-rigidity` claimed that for affine Kac--Moody algebras any two Maurer--Cartan elements with the same scalar trace `\kappa` are gauge-equivalent, and its proof explicitly used the false old identification of the minimal MC moduli with the affine line `\{c\,\eta\otimes\Lambda\}`. After the scalar-saturation rectifications, the honest minimal-model statement is only `\Theta^{\min}=\eta\otimes\Gamma` with genus-1 trace recording the `\lambda_1`-coefficient of `\Gamma`; fixing `\kappa` alone does not classify a general MC point. The false proposition has therefore been replaced by a remark distinguishing the canonical strict scalar representative of the bar-intrinsic universal class from arbitrary one-channel MC points. Metadata was regenerated so the proved-claim registry no longer counts this deleted proposition as a `ProvedHere` result. | **FIXED** |
| F126 | — | **CRITICAL** | Beilinson | The scalar-saturation gap had also infected the convolution-formality theorem chain. In `chiral_hochschild_koszul.tex`, `thm:convolution-formality-one-channel` still claimed a proved equivalence “convolution formality $=$ one-channel,” identified one-channelity with scalar saturation, and used the false premise that the only Maurer--Cartan point in cohomology is `\kappa\,\eta\otimes\Lambda`. The adjacent remarks then exported this as if formality forced scalar saturation and as if planted-forest triviality gave a proved converse criterion, while `concordance.tex` and `heisenberg_frame.tex` summarized the result as a global equivalence or as scalar saturation for one-channel families. These surfaces are now aligned to the strongest true statement: if the full universal class is already scalar, then the quantum `L_\infty` structure is formal along the distinguished scalar orbit; no converse is proved; one-channelity still gives only minimal-model line concentration `\Theta^{\min}=\eta\otimes\Gamma`; and the frame/concordance exports now advertise only this orbitwise scalar-lane statement. Metadata was regenerated so the theorem registry and dependency graph no longer carry the old title. | **FIXED** |
| F127 | — | **SERIOUS** | Beilinson | A further export leak remained in the DS / Gaussian-collapse / tropical-bar chain. In `higher_genus_modular_koszul.tex`, the DS characteristic-package proposition still stated `\Theta_{\mathcal W^k}^{\min}=\kappa(\mathcal W^k)\eta_W\otimes\Lambda` for all principal `\mathcal W^k`, even though the cited proposition had already been narrowed to one-channel line concentration `\eta_W\otimes\Gamma_{\mathcal W^k}` with the scalar identification open for `N\ge3`. The same file also used “For one-channel families (scalar saturation)” in the truncation lemma and tropical-bar theorem/proof, treating those as synonyms, and the abelian-envelope proposition plus its concordance mirror cited `thm:algebraic-family-rigidity` to justify a Heisenberg scalar formula that in fact comes from the Heisenberg scalar lane. These surfaces are now aligned to the honest scope: DS functoriality gives minimal-line control for `\Theta`; Virasoro recovers the scalar formula, while multi-weight `\mathcal W` scalar purity remains open; the truncation/tropical-bar statements fence their scalar simplifications to scalar-saturated families on the proved scalar lane; and the Gaussian-collapse proposition now cites the Heisenberg scalar formula rather than the generic rigidity theorem. | **FIXED** |
| F128 | — | **SERIOUS** | Beilinson | The remaining scalar-saturation drift had moved into chapter- and section-level summaries. In `chiral_hochschild_koszul.tex`, the non-scalar subsection still opened with the false blanket statement that `\dim H^2_{\mathrm{cyc}}=1` implies `\Theta^{\min}=\kappa\,\eta\otimes\Lambda`; in `preface.tex`, the bird’s-eye summary still said “Section 9 proved scalar saturation”; and in `kac_moody.tex`, the chapter-level remarks were conflating the scalar minimal package with the full Kac--Moody universal class, even writing `\Theta_{\widehat{\mathfrak g}_k}=\kappa\eta\otimes\Lambda` while simultaneously acknowledging the cubic Lie/tree shadow. These surfaces are now aligned to the honest record: one-channelity gives `\Theta^{\min}=\eta\otimes\Gamma` in general, the scalar formula is only on the proved scalar lane, Section 9 proves one-channel line concentration plus scalar-lane results rather than generic scalar saturation, and generic Kac--Moody carries a scalar minimal package together with a nontrivial cubic shadow in the full universal class. | **FIXED** |
| F129 | — | **CRITICAL** | Beilinson | The rectification theorem chain had regressed to the old false formality story. In `higher_genus_modular_koszul.tex`, `thm:rectification-meta` still claimed that the modular convolution `L_\infty`-algebra is formal **iff** `\cA` is one-channel, that the full MC moduli is the affine line `\kappa\,\eta\otimes\Lambda`, and that generic principal Drinfeld--Sokolov reduction yields a BRST complex “formal as a dg algebra.” Those assertions contradict the already-correct scalar-orbit theorem in `chiral_hochschild_koszul.tex`, which only proves formality along the distinguished scalar orbit under the stronger hypothesis that the **full** universal MC class is scalar, with no converse from one-channelity. They also overshoot the DS evidence, which elsewhere is only Kazhdan filtration formality (`E_1` degeneration) and identification of `\mathbf{R}\mathrm{DS}_f` with `H^0_{Q_{\mathrm{DS}}}` at generic level, not cohomological formality of the full BRST dg algebra. The theorem is now narrowed accordingly: scalar-orbit formality only under the scalar universal-class hypothesis, no affine-line classification of full MC moduli, and DS reduced to the proved generic filtration-formality / derived-equals-classical statement. | **FIXED** |
| F130 | — | **SERIOUS** | Beilinson | The DS rectification/export layer was still overstating the generic reduction story even after the core rectification theorem had been narrowed. In `introduction.tex`, the `Rectification bridge` theorem still claimed that `\mathbf{R}\mathrm{DS}_f` and `H^0_{Q_{\mathrm{DS}}}` agree on Koszul algebras at generic level without the abelian-`\mathfrak n_+` or separately-proved principal restrictions, and the following summary paragraph was still saying DS “commutes with bar-cobar by formality” and that `\Theta_{\mathcal W_N}=\mathrm{DS}(\Theta_{\widehat{\mathfrak g}})` as a blanket rectification principle. In `concordance.tex`, the DS--KD filtration route likewise stated the generic derived/classical agreement too broadly and called the missing input “formality of the BRST complex,” blurring filtration formality with a stronger dg-formality statement. Volume II mirrored the same overclaim in `ht_bulk_boundary_line.tex` via the heading “Formality route to DS--Koszul intertwining,” the phrase “Kazhdan filtration gives formality,” and the slogan “Formality automatic” on the abelian side. These surfaces are now aligned to the honest scope: the proved generic route is Kazhdan `E_1`-degeneration on the abelian-`\mathfrak n_+` lane, the principal case is handled separately, broader non-principal DS/KD transport remains conditional unless the comparison hypotheses are verified, and “filtration formality” is kept distinct from any cohomological-formality theorem for the BRST dg algebra. | **FIXED** |
| F131 | — | **CRITICAL** | Beilinson | The DS shadow-descent theorem surface still contained the old affine-only falsehood. In `w_algebras_deep.tex`, `thm:ds-platonic-functor` was advertised as a functor on platonic packages of the bare affine current algebra and explicitly stated `\mathrm{DS}_f(\Theta_{V_k(\fg)})=\Theta_{\mathcal W^k(\fg,f)}` together with `\mathrm{Sh}_r(\mathcal W)=\mathrm{DS}_f(\mathrm{Sh}_r(V_k(\fg)))`. But the same chapter’s own corollary already used the full current-plus-ghost system `V_k(\fg)\otimes F_{\mathrm{gh}}`, and the nonlinear-shadows appendix states the decisive obstruction: quartic and higher shadows in the `\mathcal W`-algebra are created by the ghost sector, so the naive affine-only identity `\mathrm{DS}(\Theta_{\widehat{\fg}})=\Theta_{\mathcal W}` cannot be the correct theorem. The theorem, remark, and corollary are now aligned to the honest statement: BRST reduction transports the total current-plus-ghost platonic package to the `\mathcal W` package; the universal MC class descends as `H^0_{Q_{\mathrm{DS}}}(\Theta_{V_k(\fg)\otimes F_{\mathrm{gh}}})=\Theta_{\mathcal W}`; and the computational slogan is that one reduces the full current-plus-ghost shadow tower, not the bare affine tower. I also propagated this correction to the introduction, concordance, nonlinear-shadows appendix, Volume II’s principal DS conjecture, and the high-level preface summary. | **FIXED** |
| F132 | — | **SERIOUS** | Beilinson | A Volume II theorem wrapper was still reimporting the old affine-only DS slogan even after the Vol I theorem and summaries were repaired. In `twisted_holography_quantum_gravity.tex`, the functoriality theorem still stated principal DS reduction as `\mathcal{W}_k(\mathfrak g)=H^0_{\mathrm{DS}}(V_k(\mathfrak g))` followed by the holographic datum identity `\mathcal H(\mathcal W_k)=\mathrm{DS}(\mathcal H(V_k))`. On the corrected claim surface, the datum-level descent is only proved for the total current-plus-ghost system, because the ghost sector contributes the quartic and higher shadows missing from the bare affine package. The theorem and proof now state the honest principal-case export: `\mathcal H(\mathcal W_k)=\mathrm{DS}(\mathcal H(V_k\otimes F_{\mathrm{gh}}))`, citing the Vol I total-package theorem and its MC-descent corollary rather than the old blanket “compatibility of bar-cobar with principal DS reduction” slogan. | **FIXED** |
| F133 | — | **SERIOUS** | Beilinson | A duplicate critical-level `\mathcal W` theorem in `kac_moody.tex` was still carrying its own unsupported proof, and downstream files were citing that duplicate result for generic `\mathcal W` Koszulness. The theorem `thm:w-algebra-koszul` reproved principal critical duality by asserting a separate internal chain-level DS/bar commutation theorem at critical level for the bare affine algebra, even though the proved manuscript route runs through the main `\mathcal W` package in `w_algebras.tex`, where the critical case is isolated as the specialization in `thm:w-koszul-precise`. I replaced the Kac--Moody proof with the honest cross-reference to that theorem, explicitly removed the unsupported “bare-affine critical DS/bar commutation” proof strategy, and retargeted the downstream generic-Koszul citations in `deformation_quantization_examples.tex`, `genus_expansions.tex`, and `w_algebras_deep.tex` to `thm:w-algebra-koszul-main`, which is the theorem that actually proves principal-nilpotent `\mathcal W` Koszul duality at general level. | **FIXED** |
| F134 | — | **CRITICAL** | Beilinson | The main `\mathcal W` theorem chain was still conflating two different critical-level statements: the genuinely proved fixed-point inversion package, and a much stronger Langlands-dual critical Koszul equivalence. In `w_algebras.tex`, `thm:w-koszul-precise` part~(A) asserted bar-cobar inversion at the critical point and then immediately upgraded it to a self-duality / Langlands-dual statement by a `k\to-h^\vee` limiting argument from the noncritical theorem. The companion item in `thm:w-algebra-koszul-main` said the noncritical duality “reduces to Feigin--Frenkel self-duality of the center,” which again replaces a fixed-point statement by a stronger external dictionary. The duplicate theorem in `kac_moody.tex` and nearby remarks then exported this as if the manuscript had proved a critical principal `\mathcal W` Langlands-duality theorem. These surfaces are now aligned to the strongest true internal statement: at critical level the Feigin--Frenkel involution has a fixed point, the principal bar complex is uncurved, and bar-cobar inversion recovers the same principal `\mathcal W`-algebra. The stronger Langlands-dual / quantum-Langlands interpretation is now explicitly fenced as external or conjectural rather than derived from the manuscript’s bar-cobar proofs. I propagated the same correction to the summary surfaces in `free_fields.tex` and `koszul_pair_structure.tex`. | **FIXED** |
| F135 | — | **CRITICAL** | Beilinson | The critical `\mathcal W` Langlands subsection in `koszul_pair_structure.tex` still contained a theorem-level falsehood even after the surrounding surfaces were narrowed. Theorem `thm:frenkel-gaitsgory-langlands` attributed to Frenkel--Gaitsgory literally asserted the bar-cobar equation `\mathcal{W}^{-h^\vee}(\mathfrak g)^! = \mathcal{W}^{-h^\vee}(\mathfrak g^L)` as a proved external theorem. That is not the manuscript’s proved statement, and on the corrected claim surface it is not even the right kind of external input: the local geometric/quantum-Langlands literature provides an external critical-level comparison of categories/opers/Langlands-dual data, not a manuscript-internal theorem identifying that comparison with chiral bar-cobar Koszul duality. I replaced the theorem by an explicit heuristic viewpoint remark, removed the displayed `^!`-equation as a claimed theorem, and rewrote the one downstream citation so the file now says only that the external Langlands viewpoint motivates comparison with Langlands-dual data while the proved internal package remains the critical fixed-point inversion statement. | **FIXED** |
| F136 | — | **CRITICAL** | Beilinson | The remaining critical-level self-duality cluster was still overstated across three files. In `w_algebras.tex`, `thm:w-critical-bar` was a `ClaimStatusProvedHere` theorem asserting an explicit screening-model bar complex `\bar B(\mathcal W^{-h^\vee})=\mathrm{Sym}[S_i]\otimes\Omega^*_{\log}` with a proof that simply promoted the Feigin--Frenkel center and commuting screening charges into a full bar-complex identification; the downstream theorem `thm:w3-koszul-dual` then used that to claim actual critical self-duality `( \mathcal W_3^{-3})^! \simeq \mathcal W_3^{-3}` even though the corrected critical package is only fixed-point inversion, not a proved `^!`-identity. In `koszul_pair_structure.tex`, the “Moduli of Koszul parameters” remark compounded the same error by saying the critical fixed point implies `\cA^!\cong\cA`, that there are no singular vectors, and that the screening-model bar complex is theorematic. Finally, `kac_moody.tex` still exported the same confusion as “Koszul self-duality of `\widehat{\mathfrak g}_{-h^\vee}`” and even wrote an explicit Langlands-dual affine `^!` equation. These surfaces are now aligned to the strongest honest internal statement: the critical point is a fixed point of the level involution; bar-cobar inversion / uncurved packages are separately proved on the specific critical surfaces where stated; the explicit screening-model bar complex is only a heuristic viewpoint; and neither affine nor principal `\mathcal W_3` critical self-duality is claimed as a proved `^!` theorem. | **FIXED** |
| F137 | — | **CRITICAL** | Beilinson | The `\mathcal W` center/Langlands wrapper in `w_algebras.tex` still contained a theorem-level falsehood even after the critical fixed-point cleanup. Theorem `thm:w-center-langlands` was overclaiming in two different ways at once: first, it promoted an external critical-center picture to a `ClaimStatusProvedHere` theorem by simply asserting that DS reduction transports the affine Feigin--Frenkel center to the principal critical `\mathcal W` center, even though that bridge is not proved locally in the manuscript; second, its displayed chain `Z(\mathcal W^{-h^\vee}(\mathfrak g)) \cong \mathrm{Fun}(\mathrm{Op}_{\mathfrak g^\vee}) \cong Z(\mathcal W^{-h^{\vee,\vee}}(\mathfrak g^\vee))` collapsed the dual-of-the-dual incorrectly, since applying the same critical-center description to `\mathfrak g^\vee` gives opers for `(\mathfrak g^\vee)^\vee = \mathfrak g`, not another copy of `\mathrm{Op}_{\mathfrak g^\vee}`. The same theorem then slid again into the false global slogan that centers are exchanged under Koszul duality at all levels, whereas the proof only observed that centers are trivially `\mathbb C` on generic simple lanes. I replaced the theorem by a heuristic scope remark recording the two separate external critical oper descriptions with the correct dual Lie algebras, explicitly stating that the manuscript does not prove DS-center transport or a general all-level center-exchange theorem, and I repaired the nearby critical consistency check so it now cites only the affine Feigin--Frenkel center rather than reimporting the false principal-center theorem. | **FIXED** |
| F138 | — | **SERIOUS** | Beilinson | The later orbit-duality reminder in `w_algebras.tex` had drifted into a genuinely false local dictionary. It duplicated the earlier Barbasch--Vogan definition but widened the type-$A$ transpose rule to “classical groups” in general, even though the manuscript’s own earlier definition only allows raw partition transpose in type~$A$ and Barbasch--Vogan duality for the other classical types requires the usual parity/collapse corrections. The same local copy then mislabeled the $\mathfrak{sl}_3$ orbit table: it called the zero orbit `(1,1,1)` “minimal,” even though the nonzero minimal/subregular orbit is `(2,1)`. I replaced that duplicate definition by a type-$A$ specialization remark referring back to `def:bv-dual`, corrected the $\mathfrak{sl}_3$ table to `\{\text{principal},\text{minimal/subregular},\text{zero}\}`, and rewrote the $\mathfrak{sl}_4$ table so principal/zero and subregular/minimal are exchanged while `(2,2)` remains self-dual. | **FIXED** |
| F139 | — | **CRITICAL** | Beilinson | The main derived-Langlands bridge surface in `derived_langlands.tex` was still crossing the internal/external boundary. Theorem `thm:langlands-bar-bridge` was tagged `ClaimStatusProvedHere`, but its title and part~(iii) claimed a bridge all the way to geometric Langlands, and the theorem text explicitly said “the bar side computes the automorphic category” and “the cobar side computes the spectral category,” which is not what the proof establishes. Locally, the proof only proves the critical-level bar-to-derived-oper identification and the derived-center description; the categorical localization dictionary comes from external Frenkel--Teleman / Frenkel--Gaitsgory input and is not identified internally with bar-cobar duality. The same drift reappeared in the introduction to the chapter and in `prop:bar-as-localization`, which had been overstated as a `ClaimStatusProvedHere` proposition even though its proof only showed that the vacuum-side projection lands in `\mathcal O_{\mathrm{Op}}` compatibly with the external localization target. I narrowed the theorem to the internal critical bar-to-oper bridge, removed the automorphic/spectral category language, demoted `prop:bar-as-localization` to a heuristic compatibility remark, and aligned the chapter introduction, the Kostant-slice follow-up remark, and the chapter summary bullets so they now say: the manuscript proves the derived oper algebra at critical level and interfaces with the oper side of geometric Langlands; the stronger localization/categorical dictionary remains external. | **FIXED** |
| F140 | — | **CRITICAL** | Beilinson | The Kac--Moody chapter was still carrying a stronger, now false, copy of the oper theorem. In `kac_moody.tex`, Theorem `thm:oper-bar` stated that the critical bar complex computes `\mathcal O(\mathrm{Op}^{\mathrm{dR}}_{\fg^\vee}(X))`, the de~Rham space of opers on a curve `X`, but its proof only invoked Frenkel--Teleman on the formal disc and cited `\Omega^\bullet(\mathrm{Op}_{\fg^\vee}(D))`. That is a genuine local-vs-global claim inflation: the proved theorem in `derived_langlands.tex` is the local formal-disc derived-oper statement, and the Kac--Moody proof did not supply the extra Beilinson--Drinfeld / localization input needed to globalize it to a curve-level de~Rham theorem. I narrowed `thm:oper-bar` to “Local derived oper identification,” with statement `\bar B(\widehat{\fg}_{-h^\vee}) \simeq \mathcal O^{\mathrm{der}}(\mathrm{Op}_{\fg^\vee}(D))`, and I aligned the local scope remark and later summary/open-question/export prose so they now say “formal-disc oper space” or “local oper resolution” rather than a global curve-level theorem. | **FIXED** |
| F141 | — | **CRITICAL** | Beilinson | The oper chapter still contained a deeper object-conflation even after the local/global repair. In `derived_langlands.tex`, Theorem `thm:oper-bar-dl` and the bridge theorem were identifying the critical bar complex with `\mathcal O^{\mathrm{der}}(\mathrm{Op}_{\fg^\vee}(D))`, i.e. the derived structure sheaf of the oper space, even though the proof only established the cohomological statement `H^n(\bar B) \cong \Omega^n(\mathrm{Op}_{\fg^\vee}(D))`. For a classically smooth oper space, that differential-form algebra is not the same object as the derived structure sheaf, which remains concentrated in degree `0`. The same conflation propagated into the “formal smoothness vs derived richness” remark, the Kac--Moody mirror theorem `thm:oper-bar`, the concordance’s “derived enhancement of the Feigin--Frenkel center” wording, and the arithmetic interface remark. I narrowed the theorematic surface to the honest cohomological statement: the critical bar complex recovers the graded algebra of oper differential forms on the formal disc; it should be viewed as a chain-level enhancement of the oper differential-form package, not as the derived structure sheaf of the oper space itself. I propagated that distinction through the bridge theorem, the proof-structure remark, the Kac--Moody wrapper theorem and summaries, the concordance remark, and the arithmetic-shadows interface. | **FIXED** |
| F142 | — | **SERIOUS** | Beilinson | After the object-level oper correction, one more abstraction leak remained on the same surface. In the introduction to `derived_langlands.tex` and in the arithmetic interface remark of `arithmetic_shadows.tex`, the manuscript was still saying that at critical level the uncurved deformation complex has Maurer--Cartan moduli equal to the oper space. That does not follow from the theorem surface now in force: the proved result is a cohomological identification with the oper differential-form package, not a comparison of Maurer--Cartan solutions or deformation functors. I narrowed those passages to the honest statement that the uncurved critical complex has cohomology governed by oper differential forms, and I explicitly removed the claim that the manuscript identifies its MC moduli with the oper space. I also aligned part~(ii) of `thm:langlands-bar-bridge` so it now says “oper differential-form identification” rather than “de~Rham complex of the oper space.” | **FIXED** |
| F143 | — | **CRITICAL** | Beilinson | A final oper-surface conflation remained in the manuscript’s examples and constitutional summaries. In `genus_expansions.tex` and `higher_genus_complementarity.tex`, the critical cubic equation `\{a:[a,a]=0\}` was still being called “the oper space,” even though that locus is only the Maurer--Cartan/flat-connection locus for the underlying Lie bracket and the manuscript proves no comparison theorem identifying it with opers. At the same time, `concordance.tex` was still advertising a “derived oper space from the bar complex,” citing the full differential-form theorem for an `H^0` center statement, and its Langlands remark still called the higher bar cohomology “higher derived structure.” Finally, `derived_langlands.tex` and the Kac--Moody critical-level remarks still had residual wording that treated the bar complex as if it were the oper structure sheaf and the shadow-critical locus as if it directly recovered opers. These surfaces are now aligned to the honest record: the Maurer--Cartan/flat-connection locus is kept separate from the oper space; the constitutional and derived-Langlands summaries now speak only about the formal-disc oper differential-form package; the `H^0` Feigin--Frenkel center statement cites the specific zeroth-cohomology theorem; and the Kac--Moody critical-shadow remarks now say only that the shadow calculus meets the separate bar-cohomological oper theorems, not that it identifies its cubic critical locus with opers. | **FIXED** |
| F144 | — | **SERIOUS** | Beilinson | Two residual critical-level wrappers were still mixing local and global oper surfaces, and the arithmetic chapter still contained stale collapse language. In `kac_moody.tex`, Theorem `thm:critical-level-structure` described the center of the completed affine algebra as `\mathrm{Fun}(\mathrm{Op}_{\mathfrak g^\vee}(X))`, even though the manuscript’s own internal and surrounding theorem surfaces now work on the formal disc `D`. The same globalized formula reappeared in the critical-center theorem and heuristic Langlands remark of `w_algebras.tex`. Separately, the epistemic-scope paragraph of `arithmetic_shadows.tex` still said that at critical level “the shadow tower collapses,” contradicting the corrected distinction between scalar vanishing and full tower collapse, and a later semiclassical asymptotic theorem reused the same collapse idiom where only asymptotic dominance by the leading logarithmic singularity was meant. These surfaces are now aligned to the honest local critical record: the center formulas are stated on the formal disc `D`, and the arithmetic chapter now says only that `\kappa=0` makes the bar complex uncurved and recovers the Feigin--Frenkel center plus the higher oper differential-form package, while the large-`c` theorem is phrased as an asymptotic dominance statement rather than a tower-collapse claim. | **FIXED** |
| F145 | — | **SERIOUS** | Beilinson | The next critical-level leak was internal/external mixing inside the citation graph. In `kac_moody.tex`, Theorem `thm:critical-level-structure` was tagged `ClaimStatusProvedElsewhere` but still contained an item asserting the manuscript-internal fixed-point/inversion consequence `\Omega(B(\widehat{\mathfrak g}_{-h^\vee})) \simeq \widehat{\mathfrak g}_{-h^\vee}` from Theorem~B. Downstream, the Kac--Moody duality proof, the Heisenberg rosetta remark, and the `\mathcal W` three-theorem summary were then citing that external wrapper as if it were the source of the internal fixed-point inversion package. These surfaces are now aligned to the honest dependency structure: the external Feigin--Frenkel theorem records only the external critical-level package; the fixed-point/inversion consequence is cited from the manuscript’s own Verdier level identification and `\mathcal W` critical fixed-point theorem; and the downstream summaries no longer route internal Theorem~B content through an external-status theorem label. | **FIXED** |
| F146 | — | **MODERATE** | Beilinson | After the theorem and citation cleanup, a summary-layer locality leak still remained inside the Kac--Moody chapter. Several expository passages in `kac_moody.tex` were still saying only “functions on opers” or “the space of `\mathfrak g^\vee`-opers” even though the corrected theorem surface is explicitly local on the formal disc. In particular, the critical Wakimoto verification and the DS/BRST summary theorem were still omitting the formal-disc qualifier. These are now aligned to the local theorem surface: the chapter says “local opers on the formal disc” at those summary sites as well, so the expository prose no longer drifts back toward a global curve-level statement. | **FIXED** |
| F147 | — | **SERIOUS** | Beilinson | The upstream external theorem wrapper itself was still too broad. In `kac_moody.tex`, `thm:critical-level-structure` was titled “Feigin--Frenkel: critical level structure” and bundled three different external facts under a single `ClaimStatusProvedElsewhere` theorem: the Feigin--Frenkel center, an affine-flag de Rham statement, and a Wakimoto/free-field realization. Only the center statement actually matches the cited Feigin--Frenkel theorem surface that the manuscript is using downstream. The theorem has now been narrowed to “Feigin--Frenkel center at critical level,” stating only the local formal-disc center identification, and the affine-flag / Wakimoto material has been moved out of the theorem into a separate background remark. Metadata was regenerated so the proved-elsewhere registry now reflects the narrower theorem title. | **FIXED** |
| F148 | — | **MODERATE** | Beilinson | A residual local-vs-global contradiction remained in the `\mathcal W` chapter’s oper definition. Immediately after the corrected local formal-disc Feigin--Frenkel theorem, `w_algebras.tex` defined `\mathrm{Op}_{\mathfrak g}(X)` for a curve `X` and then called it “an infinite-dimensional affine space modeled on `H^0(X,\omega_X^{\otimes d_1+1}\oplus\cdots\oplus\omega_X^{\otimes d_r+1})`.” That is false on a fixed smooth proper curve: the displayed `H^0` model space is finite-dimensional. The infinite-dimensional statement belongs to the local formal-disc space `\mathrm{Op}_{\mathfrak g}(D)`, not the global curve-level space. The definition now distinguishes these two surfaces explicitly: global opers on `X` form an affine space modeled on the displayed finite-dimensional `H^0`, while the local formal-disc oper space is the infinite-dimensional formal power-series version used by the Feigin--Frenkel center theorem. | **FIXED** |
| F149 | — | **SERIOUS** | Beilinson | A residual wrapper cluster was still reintroducing the old oper overclaims after the theorem-level cleanup. In Volume I, `preface.tex` was still saying only “the algebra of functions on the space of `${}^L\mathfrak g$-opers” with no local qualifier; `derived_langlands.tex` still summarized `H^\bullet(\bar B(\widehat{\fg}_{-h^\vee})) \cong \Omega^\bullet(\mathrm{Op}_{\fg^\vee}(D))` under the stale heading “Full derived oper identification”; and `arithmetic_shadows.tex` still called the theorem surface a “derived oper cohomology package.” In Volume II, `thqg_gravitational_s_duality.tex` still wrote the Feigin--Frenkel theorem as `\mathrm{Fun}(\mathrm{Op}_{\fg^\vee}(X))`, globalizing the local formal-disc center statement. These surfaces are now aligned to the honest record: the critical center is local on the formal disc, and the manuscript proves the formal-disc oper differential-form package rather than a generic “derived oper” object. | **FIXED** |
| F150 | — | **CRITICAL** | Beilinson | A stronger critical-level falsehood remained in the THQG connection layer. In both `thqg_perturbative_finiteness.tex` files, the local Feigin--Frenkel center on the formal disc was still being inflated into a global genus-`g` replacement invariant by claiming that the “classical” invariant is the space of opers on `\Sigma_g`. In Volume I `thqg_gravitational_s_duality.tex`, the proof of the four-facet critical theorem still wrote the Feigin--Frenkel theorem as `\mathrm{Fun}(\mathrm{Op}_{\fg^\vee}(X))`. Worst of all, Volume II’s `relative_feynman_transform.tex` used a nonexistent “oper flatness” route to reprove `\kappa(V_{-\hvee}(\fg))=0`, claiming the center is functions on opers over `\Sigma_g` and that the genus-`g` bar complex is flat because opers carry a canonical zero-curvature connection. These claims are now removed. The THQG chapters state only the honest local critical structure: the Feigin--Frenkel center and formal-disc oper package in bar cohomology. In the affine-lineage classification proof, `\kappa=0` is again derived directly from the affine modular-characteristic formula, with the Feigin--Frenkel center treated as additional critical-level structure rather than as the proof of vanishing. | **FIXED** |
| F151 | — | **SERIOUS** | Beilinson | The next drift sat one layer above F150: the Volume II section `relative_feynman_transform.tex` was still introducing `thm:classification-uncurved` as if it classified globally flat genus towers, saying “when `\kappa=0`, the tower is flat” and “the following theorem classifies exactly which algebras achieve this,” even though the theorem itself is explicitly genus~1 and only concerns the genus-1 bar complex, trivial `B`-cycle monodromy on `\Ran(\Sigma_1)`, and the affine critical-level specialization. The downstream remark then asked “which chiral algebras have flat genus towers?” and exported the affine and DS consequences without the genus-1 qualifier. This wrapper layer is now repaired: the section, theorem aftermath, and DS consequence all state genus~1 explicitly, so the manuscript no longer inflates a genus-1 uncurved classification into an all-genus flat-tower theorem. | **FIXED** |
| F152 | — | **CRITICAL** | Beilinson | The next theorem-level gap lay in `thm:classification-affine-monodromy` inside Volume II’s `relative_feynman_transform.tex`. Its part~(iii) stated a genus-`g` monodromy formula `\operatorname{Mon}_{B_\alpha} = \exp(2\pi i\,\kappa[\omega_\alpha])`, but the proof only established the genus-1 formula via `thm:classification-uncurved` and then jumped to “at higher genus, the period matrix gives the stated formula” without constructing or citing the needed higher-genus comparison. The downstream “invariant triple” remark then packaged this as an all-genus representation of `\pi_1(\Mbar_g)`. This has now been narrowed to the honest genus-1 surface: part~(iii) records only the genus-1 `B`-cycle monodromy `\exp(2\eta_\tau\kappa)`, the proof explicitly says that a comparable higher-genus monodromy formula is not proved here, and the invariant triple is rewritten as a genus-1 triple valued in `\operatorname{Rep}(\pi_1(\Mbar_1))`. | **FIXED** |
| F153 | — | **SERIOUS** | Beilinson | The same higher-genus monodromy inflation also sat earlier in the “three models as gauge orbits” chain. In `relative_feynman_transform.tex`, `thm:three-models-gauge-orbit` was still saying that the residual curvature term `\kappa(\cA)\omega_g` “measures the D-module monodromy around `B`-cycles of `\Sigma_g`,” and the proof of the conjugation formula was describing the BCH commutators as `B`-cycle monodromy corrections, then citing a vague “higher-genus generalisation” via Riemann bilinear relations. What is actually proved there is the conjugation formula itself. The monodromy interpretation is only established later in genus~1. The theorem and proof are now narrowed accordingly: the residual curvature is the obstruction to strict intertwining, the proof speaks only about global period corrections, and any genus-1 monodromy reading is explicitly fenced as a later genus-1 result rather than a proved general higher-genus theorem. | **FIXED** |
| F154 | — | **SERIOUS** | Beilinson | After F153, the same all-genus monodromy inflation still remained in surrounding summary prose of Volume II’s `relative_feynman_transform.tex`. The chapter synopsis, the chiral exponential-map overview, the factorization-vs-operadic comparison, the non-fullness discussion, and the “three models” summary were still speaking as if the curved geometric model carried a proved all-genus D-module monodromy interpretation around `B`-cycles of `\Sigma_g`. On the corrected claim surface, what the chapter proves generally is the global curvature / period-correction datum, while the explicit monodromy identification is only established later in genus~1. Those summaries are now aligned to that honest scope: they speak about curvature and global period corrections in general, and mention explicit `B`-cycle monodromy only where the genus-1 theorem actually proves it. | **FIXED** |
| F155 | — | **MODERATE** | Beilinson | One last summary analogy in Volume II’s `relative_feynman_transform.tex` was still overpromoting the corrected monodromy surface. The Chriss--Ginzburg remark after the gauge-torsor theorem was saying that a flat connection has a unique holonomy representation “(the MC moduli),” that nilpotence is “the logarithm of periodicity,” and that the curvature term `\kappa(\cA)\omega_g` is the infinitesimal generator of the monodromy. On the current claim surface, that is too strong: the manuscript has not constructed a general all-genus holonomy representation directly from the MC moduli, and the explicit monodromy identification is only proved later in genus~1. The remark is now narrowed to the safe content: the MC orbit is the invariant bar-coderivation datum, the propagators are gauge representatives, the curvature term records failure of strict flatness of the curved representative, and only in genus~1 is this later identified with the explicit `B`-cycle monodromy correction. | **FIXED** |
| F156 | — | **SERIOUS** | Beilinson | The genus-1 uncurved classification theorem in Volume II still contained a genuine logical falsehood, not just wrapper drift. In `relative_feynman_transform.tex`, Theorem~`thm:classification-uncurved` was asserting that trivial `B`-cycle monodromy of the factorization D-module is equivalent to the family `\bar B(\cA)\to\overline{\mathcal M}_1` “admitting a flat connection,” and the proof cited Riemann--Hilbert to justify that step. But a local system or regular holonomic D-module already carries a flat connection in general; trivial monodromy is much stronger than mere existence of one. The theorem has now been narrowed to the honest equivalence surface `(i)\Leftrightarrow(ii)\Leftrightarrow(iii)` together with the affine specialization `(ii)\Leftrightarrow(iv)`: genus-1 uncurved bar complex, vanishing scalar curvature, and trivial `B`-cycle monodromy are the proved equivalent conditions, while the false extra “admits a flat connection” clause has been removed rather than replaced by a new unproved global trivialization theorem. | **FIXED** |
| F157 | — | **SERIOUS** | Beilinson | The closing comparison theorem of Volume II’s `relative_feynman_transform.tex` still contradicted the chapter’s own non-fullness analysis. The section title, theorem title, and final “single structural principle” remark were all still presenting the global/factorization and local/operadic constructions as an outright “equivalence of approaches” in which the two presentations determine each other. But earlier in the same chapter, Proposition~`prop:global-local-comparison` proves the opposite at positive genus: the factorization model carries additional global period/monodromy data invisible to the bicomplex, so the forgetful functor is not full there. This wrapper layer is now narrowed to the honest claim surface: the theorem compares global and local bar-complex models, states that they are connected by the relative Feynman transform and compute the same derived invariants, and the closing remark now says explicitly that the shared object on the proved surface is the bar-complex package and its derived/coderived invariants, not a fully faithful equivalence of presentations. | **FIXED** |
| F158 | — | **CRITICAL** | Beilinson | A deeper theorem-level object conflation remained in the same Volume II chapter. The “derived--coderived equivalence” theorem and the closing comparison theorem were still speaking as if the curved factorization model had ordinary derived invariants and even ordinary cohomology groups: `relative_feynman_transform.tex` said the flat and curved models “compute the same derived invariants,” and part~(c) of `thm:three-routes-equivalence` explicitly wrote `H^n(\barB_{\mathrm{fact}}(\cA))` as if the curved differential `\dfib` with `\dfib^2=\kappa\omega_g` defined an honest complex. That is false on the current claim surface. The curved model lives in the coderived category, not the ordinary derived category, and the theorem does not provide ordinary cohomology groups for it when `\kappa\neq 0`. This pass repairs the theorem chain at the source: the main theorem is now “Derived--coderived comparison,” not equivalence; its conclusion says the flat model carries ordinary derived invariants while the curved model is the coderived avatar linked by the chiral exponential map; the proof explicitly disavows assigning ordinary cohomology to the curved model; the closing comparison theorem now speaks about derived/coderived packages rather than “same derived invariants”; and the foundational mirror sentence in `higher_genus_foundations.tex` is aligned to the same distinction. | **FIXED** |
| F159 | — | **SERIOUS** | Beilinson | After F157--F158, the same chapter still had a categorical-shape overclaim in its surrounding summaries and functorial wrapper. The introduction, the unified-approach remark, and Proposition~`prop:functorial-triangle` were still drawing the relation `\mathbf{Fact}\twoheadrightarrow\mathbf{RelFT}\hookleftarrow\mathbf{Op}` and calling the relative Feynman transform “the categorical image,” with `\Psi` described as an “embedding.” But the proved surface in the very same proposition is weaker: `\Phi` is faithful at all genera and full only at genus~0; `\Psi` is faithful at all genera and an equivalence only at genus~0; and at positive genus the images differ because global period/monodromy data are invisible to the bicomplex. Those sites are now narrowed to the honest comparison language: plain arrows instead of surjection/injection notation, “maps to” instead of essential-surjectivity/full-embedding rhetoric, a “comparison triangle” that commutes at genus~0 rather than a blanket commutative triangle, and the downstream summary now says the three models compute the same flat-derived / curved-coderived package rather than a stronger categorical identification. | **FIXED** |
| F160 | — | **SERIOUS** | Beilinson | The same functorial-triangle proposition still contained one more internal contradiction after F159. Its theorem statement and proof were saying, on the one hand, that at positive genus the factorization and operadic images differ because extra global period/monodromy data are invisible to the bicomplex; but on the other hand they still said the curvature term `\kappa(\cA)\omega_g` “measures the discrepancy” or even “measures the discrepancy precisely.” That is too strong on the current claim surface: the scalar curvature term is only the first shared obstruction visible in the genus spectral sequence, while the full positive-genus discrepancy is larger and includes additional global D-module data. The proposition and proof now say the honest thing: at genus `g\ge1`, the images differ; `\kappa(\cA)\omega_g` is the visible bicomplex-level obstruction; and the full discrepancy is not exhausted by curvature alone. | **FIXED** |
| F161 | — | **SERIOUS** | Beilinson | The next overclaim sat in the theorem-level corollary of the gauge-orbit presentation. In `relative_feynman_transform.tex`, `thm:three-models-gauge-orbit` still concluded “all three models compute the same invariants” and identified those invariants with ordinary modular bar cohomology `H^\bullet(\Bmod(\cA))`, even though the curved representative has `\dfib^2=\kappa\omega_g` and therefore does not define an ordinary chain complex when `\kappa\neq 0`. That contradicted the coderived rectification already established in the same chapter. The theorem is now narrowed to the honest invariant statement: the three models carry the same gauge-orbit content; the two flat representatives have the same ordinary derived package; and the curved representative is the corresponding coderived avatar linked by the chiral exponential map. The old sentence assigning ordinary bar cohomology to the curved representative has been removed. | **FIXED** |
| F162 | — | **CRITICAL** | Beilinson | The next falsehood was a concrete counterexample-grade export leak in the aftermath of `thm:classification-uncurved`. The follow-up remark in Volume II’s `relative_feynman_transform.tex` was claiming that Drinfeld--Sokolov reduction preserves the zero locus of the genus-1 scalar curvature, so `\kappa(\mathrm{DS}_f(V_k(\fg)))=0` iff the parent affine algebra has `\kappa(V_k(\fg))=0`, and hence the only genus-1 uncurved `\mathcal W`-algebras in that lineage would be the critical-level ones. But Volume~I already contradicts this: for principal `\mathcal W`-algebras, `\kappa(\mathcal W^k(\fg))=\varrho(\fg)\,c(\mathcal W^k(\fg))`, so the zero locus is the zero set of the DS central charge, not the affine critical-level locus. The explicit Virasoro specialization gives a concrete counterexample: `\kappa(\mathrm{Vir}_{c(k)})=c(k)/2` vanishes at `k=-1/2,-4/3`, while the parent affine zero `\kappa(V_k(\mathfrak{sl}_2))=0` is at the critical level `k=-2`. The remark is now repaired to say the honest thing: DS--bar intertwining does not prove zero-locus preservation, and genus-1 uncurved `\mathcal W`-algebras must be read off from the W-side `\kappa` formula itself. | **FIXED** |
| F163 | — | **SERIOUS** | Beilinson | The next drift sat inside `thm:gauge-torsor` itself. The theorem headline claimed that the gauge group acts transitively on the full “space of curved models,” and it even added an unsupported parenthetical suggesting the freeness criterion holds “e.g. when `\cA` is Koszul and `g\ge1`.” But the proof only treats a much narrower surface: curved models obtained from varying the Arakelov representative on a fixed Riemann surface, with gauge transport coming from the explicit harmonic correction between those representatives. No argument is given that every curved model arises this way, and no theorem on the page proves the cited `H^0(F^1\operatorname{Coder},[\dfib,-])=0` example. The theorem is now narrowed to the honest statement: gauge transport is proved only on the Arakelov-representative family, the conjugation formula is written with the explicit `\sum_\alpha h_\alpha \iota_\alpha` correction actually used in the proof, and the freeness/torsor conclusion is kept purely conditional on the vanishing criterion itself, with the unsupported Koszul example removed. The Volume II compute helper was aligned to the same narrower theorem description. | **FIXED** |
| F164 | — | **MODERATE** | Beilinson | After F163, the immediately following Chriss--Ginzburg wrapper and the compute helper docs were still silently widening the corrected theorem back to a global MC-moduli slogan. In `relative_feynman_transform.tex`, the remark after `thm:gauge-torsor` still said “every structure on the bar complex is a Maurer--Cartan element” and treated the full quotient `\mc/\mathrm{Gauge}` as the proved invariant content, even though the theorem surface on that page now only controls the canonical `\delta_\cA`, its three propagator representatives, and the Arakelov-representative family. In parallel, `compute/lib/gauge_orbit_engine.py` still described Theorem II as a general torsor theorem and said the three Heisenberg models “compute the same invariants,” although the narrowed manuscript surface only guarantees agreement of the scalar `\kappa`-data while the curved model remains coderived. These wrappers are now aligned to the honest statement: the remark says only that the orbit of the canonical `\delta_\cA` is the invariant datum proved here and explicitly denies a blanket classification of all bar-complex structures by `\mc/\mathrm{Gauge}`; the proof text now matches the theorem’s harmonic-correction wording; and the helper docstrings are narrowed to Arakelov-family gauge transport and shared scalar-invariant checks. | **FIXED** |
| F165 | — | **CRITICAL** | Beilinson | The next contradiction was deeper than the gauge-orbit wrappers: the universal genus-1 classification theorem in Volume II was still promoting the explicit affine genus-1 monodromy factor into a theorem for every logarithmic algebra. In `relative_feynman_transform.tex`, the chapter already says at positive genus the factorization model can carry extra global period/monodromy data beyond the bicomplex, and it even calls the explicit genus-1 `B`-cycle monodromy “the proved model case.” But `thm:classification-uncurved` still claimed the universal equivalence `\kappa(\cA)=0 \Leftrightarrow` trivial `B`-cycle monodromy of the associated factorizable D-module, with a proof that simply inserted the affine/Heisenberg-style scalar factor `\exp(2\eta_\tau\kappa(\cA))` for an arbitrary logarithmic `\SCchtop`-algebra. That overreached the actual claim surface. The theorem is now narrowed to the genuinely universal statement `(i)\Leftrightarrow(ii)`—genus-1 uncurved iff `\kappa=0`—together with the affine specialization `(ii)\Leftrightarrow(iii)` identifying the critical level. The section intro and aftermath are aligned to say that the explicit genus-1 monodromy formula is computed later only on the affine worked-example surface, and the affine monodromy theorem now proves its monodromy factor directly from the affine propagator calculation instead of citing a deleted universal monodromy equivalence. | **FIXED** |
| F166 | — | **SERIOUS** | Beilinson | The next wrapper drift sat immediately below F165. Even after the universal theorem was narrowed, the following remark in `relative_feynman_transform.tex` was still packaging a genus-1 “invariant triple” `(\kappa,\dalg,[\operatorname{Mon}_1])` for every logarithmic `\SCchtop$-algebra, as if the monodromy component had been proved in the same generality. But after F165, the theorem-level monodromy computation remains only on the affine worked-example surface. The remark is now aligned to the honest layered statement: universally the current chapter extracts only the pair `(\kappa,\dalg)`, while on the affine lineage this upgrades to a genus-1 triple with the explicit monodromy factor `\exp(2\eta_\tau\kappa)`. The new wording also says explicitly that no comparable theorem-level genus-1 monodromy component for arbitrary logarithmic `\SCchtop`-algebras is extracted here. | **FIXED** |
| F167 | — | **MODERATE** | Beilinson | One last summary-level inconsistency remained in the closing wrapper of the same chapter. After the earlier derived/coderived repairs, `rem:single-structural-principle` in `relative_feynman_transform.tex` was still concluding that the shared object on the proved surface is “the bar-complex package and its derived invariants,” even though the theorem immediately above had already been narrowed to a flat-derived / curved-coderived comparison and explicitly denied ordinary cohomology on the curved side. That was a local relapse to the pre-F158 language. The remark is now aligned to the theorem: the shared object on the proved surface is the bar-complex package together with its flat-derived / curved-coderived invariant package, not plain derived invariants on both sides. | **FIXED** |
| F168 | — | **SERIOUS** | Beilinson | The comparison theorem still had one more scope leak in part `(c)`. In `relative_feynman_transform.tex`, `thm:three-routes-equivalence` was using Positselski as if the passage `D^{\mathrm{co}}\simeq D` held for every flat conilpotent model over characteristic zero, and its proof was still speaking as though ordinary cohomology groups on the curved side were obtained once one invoked that comparison. But the manuscript’s own Volume I support is narrower: the flat-vs-exotic discussion only supplies the Positselski reduction on the flat conilpotent finite-type locus singled out there, while the curved side remains coderived when `\kappa\neq 0`. I narrowed the theorem statement and proof accordingly: part `(c)` now says only that derived/coderived agreement is available on the flat-side locus where the manuscript-internal Positselski comparison applies, and the theorem is read as comparing the flat ordinary derived package with the curved coderived package rather than assigning ordinary cohomology groups to both. An intermediate repair briefly introduced a bad cross-volume label to the Volume I remark; the final fix removes that label and states the restriction in plain prose. | **FIXED** |
| F169 | — | **SERIOUS** | Beilinson | After F168, a wrapper-level contradiction still remained across both volumes. Volume II theory summaries in `factorization_swiss_cheese.tex` and `modular_swiss_cheese_operad.tex` were still calling the bridge an all-genus “derived--coderived equivalence” and even saying all three models compute the same ordinary modular bar cohomology, while Volume I wrappers in `preface.tex`, `higher_genus_foundations.tex`, `bar_cobar_adjunction_inversion.tex`, and the constitution in `concordance.tex` were still saying simply “on the Koszul locus the coderived and derived categories agree.” That overshot the current theorem surface in two ways: the curved genus-fixed model does not carry ordinary cohomology when `\dfib^{\,2}\neq 0`, and the manuscript’s internal Positselski reduction is only available on the flat finite-type comparison locus, not on the whole Koszul locus. One remark was even stronger, claiming that `c=26` anomaly cancellation is precisely the criterion for coderived to collapse to derived. These surfaces are now aligned to the honest record: the bridge is a flat-derived / curved-coderived comparison, the curved model is a coderived avatar rather than a third ordinary chain complex, the flat-side derived/coderived identification is restricted to the manuscript’s finite-type Positselski locus, the constitution says that explicitly, and the `c=26` sentence now speaks only of removing the scalar curvature obstruction on the effective scalar surface rather than proving full derived-categorical collapse of the bosonic string. | **FIXED** |
| F170 | — | **CRITICAL** | Beilinson | The next leak was no longer just wrapper prose: `thm:factorization-SC-koszul` in `factorization_swiss_cheese.tex` was still carrying a false theorem-level part `(iv)`. It claimed an all-genus equivalence between the derived category of the genus-graded flat bar model and the coderived category of the curved geometric model, with a proof that invoked a generic Positselski associated-graded theorem from completeness of the genus filtration plus local homotopy-Koszulity. That overshot the manuscript’s repaired comparison surface. What is actually supported on this page is only the categorical placement: the genus-graded flat model is a strict dg coalgebra living in the derived category, while the curved genus-fixed model belongs to the coderived category. The stronger flat-derived / curved-coderived comparison is established only later in the chapter, once the chiral exponential map and the restricted comparison theorem are in place, and it is not a consequence here merely of completeness plus local homotopy-Koszulity. I therefore narrowed the section title, theorem statement, proof of part `(iv)`, and the two local downstream references so they now present `(iv)` only as categorical placement and defer the actual comparison to the later theorem. | **FIXED** |
| F171 | — | **MODERATE** | Beilinson | After F170, the Positselski-machine section opener in `factorization_swiss_cheese.tex` was still giving a false explanation for why the ordinary derived category is trivial in the curved case. It said “every module is acyclic because `d^2\neq 0` and hence every short exact sequence splits,” but that heuristic is not the theorem the manuscript actually proves there. The supported source is narrower and cleaner: Volume I’s `prop:curved-bar-acyclicity` gives the acyclicity statement for the curved bar complex when `\kappa\neq 0`. I replaced the bad explanatory sentence with an explicit citation to that proposition, so the section opener now states the derived-category triviality on the manuscript’s actual proved surface instead of improvising a stronger and logically dubious reason. | **FIXED** |
| F172 | — | **SERIOUS** | Beilinson | The next fault was an object-conflation inside `thm:curved-koszul-genus-g` in `factorization_swiss_cheese.tex`. Part `(i)` was writing a “curved Maurer--Cartan equation” directly for an element `\xi \in \barB^{(g),1}(\cA)` of the curved bar coalgebra and interpreting such a solution as a curved `A_\infty`-morphism from `\cA` to itself. That is not the manuscript’s MC formalism: elsewhere, Maurer--Cartan elements live in the associated coderivation or convolution deformation algebra, not in the bar coalgebra object itself. The only honest theorem-level content on this surface is the curved dg-coalgebra structure, namely the underlying coalgebra, the coderivation `\dfib`, and the scalar-curvature relation `\dfib^2 = \kappa\omega_g \,\mathrm{id}`. I removed the false coalgebra-level MC equation, rewrote the theorem to say explicitly that deformation MC equations live in the associated coderivation/convolution algebra, and simplified the proof of part `(i)` so it now checks only the actual curved-coalgebra structure used later. | **FIXED** |
| F173 | — | **CRITICAL** | Beilinson | The next break was a theorem-chain overclaim in the remaining parts of `thm:curved-koszul-genus-g` in Volume II’s `factorization_swiss_cheese.tex`. Part `(ii)` was claiming a generic genus-`g` equivalence all the way to `\Dctr(\cA^!\text{-}\mathsf{contra})`, but its proof only invoked Positselski’s co-contra correspondence for the curved bar coalgebra and its completed linear dual, while the manuscript elsewhere says that any further identification with plain `\cA^!` needs extra sectorwise-finiteness hypotheses on the factorization-Positselski lane. The theorem statement was also missing that finite-type hypothesis entirely. Part `(iii)` then compounded the drift by asserting a genus-tower “restriction functor” on comodules in the wrong direction and by packaging that as a full naturality statement, even though the actual unconditional datum on this surface is only the stagewise clutching map on the bar coalgebras and the induced dual map on completed dual algebras. I therefore narrowed the theorem title, added the finite-dimensional graded-piece hypothesis, replaced the false `\cA^!` target by the completed dual algebra `\barB^{(g)}(\cA)^\vee`, rewrote the proof so it stops at the genuine Positselski/dual-bar comparison, and weakened the genus-tower clause to the honest stagewise dual-map compatibility. I also repaired the Heisenberg genus-1 example so it now identifies the Weyl algebra on the completed-dual surface instead of reimporting the deleted generic `(\cH_\kappa^!)_\kappa` theorem. | **FIXED** |
| F174 | — | **CRITICAL** | Beilinson | The next failure was a foundational object-vs-category conflation in the coderived/counterderived wrappers across both volumes. Several files were still inferring “the ordinary derived category is trivial” from the acyclicity of the curved bar complex itself, or from slogans like `d^2=h\neq0`, and one Volume II construction even tried to justify that by the false homotopy formula `\id_M=\kappa^{-1}d^2`. That is not an honest chain-homotopy argument, and the manuscript’s proved input is narrower: Volume I establishes acyclicity of the higher-genus bar object itself (`prop:curved-bar-acyclicity`), not a blanket theorem that every comodule or factorization module has trivial ordinary derived category. I therefore repaired the foundational wrappers in `appendices/coderived_models.tex`, `bar_cobar_adjunction_inversion.tex`, `bar_cobar_adjunction_curved.tex`, `yangians_drinfeld_kohno.tex`, and Volume II’s `factorization_swiss_cheese.tex` so they now say the true thing: in the curved higher-genus bar setting, ordinary cohomological derived methods collapse on the bar-object surface and are not the right invariant, while Positselski’s coderived/contraderived framework is the correct ambient category. The false generic “derived category is trivial” and “every module is contractible via h” slogans are removed. | **FIXED** |
| F175 | — | **CRITICAL** | Beilinson | The next theorem-chain failure was upstream in the Volume I Positselski/module-comparison cluster itself. In `bar_cobar_adjunction_inversion.tex`, `thm:positselski-chiral-proved` was still identifying the contraderived target with `\cA^!` by citing `thm:bar-computes-dual`, even though that theorem is explicitly quadratic-regime only; the same block also exported the result as a “derived module category equivalence at all genera.” Immediately after it, `thm:full-derived-module-equiv-proved` contained a concrete proof error: Step~2 identified contramodules over the bar coalgebra with complete `\cA^!`-modules and then silently replaced them by complete `\cA`-modules, producing a false all-genera ordinary derived equivalence and unsupported Ext-duality. I repaired this cluster at the source and in its mirrored summary theorem in `koszul_pair_structure.tex`. The proved intrinsic statement is now the Positselski equivalence for the bar coalgebra itself, `D^{\mathrm{co}}(C\text{-}\mathrm{comod}) \simeq D^{\mathrm{ctr}}(C\text{-}\mathrm{contra})` with `C=\barB^{\mathrm{ch}}(\cA)`, and in finite type the right-hand side may be rewritten as complete modules over the graded dual algebra `C^*`. The second theorem is narrowed to the honest flat finite-type reduction on that completed-dual side; the fake all-genera ordinary derived equivalence and Ext-duality are removed. I propagated the same correction to `bar_cobar_adjunction_curved.tex` and `appendices/coderived_models.tex`, so the surrounding wrappers now distinguish the intrinsic bar-coalgebra comparison from the extra quadratic genus-0 bar-dual identification `C^*\cong\cA^!`. | **FIXED** |
| F176 | — | **CRITICAL** | Beilinson | The same bar-dual inflation was still present one layer downstream in the Yangian/factorization appendix lane. In `yangians_drinfeld_kohno.tex`, `thm:factorization-positselski` was still stating an off-Koszul equivalence all the way to `\cA^!`-factorization contramodules, but its proof only propagates the same-coalgebra Positselski correspondence across the Ran strata. There is no proof step there identifying the factorization contramodule side with a Koszul-dual target; the only separate dual-target input on the Koszul locus is the Verdier/bar-dual statement of `thm:h-level-factorization-kd}(ii)`. I therefore narrowed the theorem to the honest bar-coalgebra surface: `D^{\mathrm{co}}(C\text{-}\mathrm{CoFact}) \simeq D^{\mathrm{ctr}}(C\text{-}\mathrm{ContraFact})` for `C=\barB^{\mathrm{fact}}(\cA)`, with the Koszul-locus generated-core comparison fenced as a separate compatibility statement rather than part of the off-Koszul proof. I propagated that correction to the appendix comparison remark and the admissible-level wrapper in `higher_genus_complementarity.tex`, so the manuscript no longer claims an off-Koszul passage from the bar coalgebra all the way to `\cA^!` without a separate bar-dual identification theorem. | **FIXED** |
| F177 | — | **SERIOUS** | Beilinson | A wrapper-level scope leak remained one layer above the repaired Positselski theorems. The introduction, concordance, and a bar-cobar inversion summary remark were still saying that `\kappa(\cA)=0` is the theorem-level cutoff for “derived rather than coderived” behavior and that the coderived category is literally the analytic continuation of the categorical logarithm. That no longer matches the corrected theorem surface: vanishing scalar curvature removes only one obstruction, while the manuscript’s actual derived reduction is proved only on the flat finite-type completed-dual loci, and the logarithm/monodromy picture is motivational rather than theorematic. I therefore narrowed the high-level wrappers in `introduction.tex`, `concordance.tex`, and `bar_cobar_adjunction_inversion.tex` so they now state the honest content: the proved result is the intrinsic coderived/contraderived bar-coalgebra comparison, with ordinary derived reduction only on the specific flat finite-type completed-dual surfaces. | **FIXED** |
| F178 | — | **CRITICAL** | Beilinson | The next theorem-chain failure was in the module-category fallout from the old bar-cobar inversion surface. `bar_cobar_adjunction_inversion.tex` still contained a proved-here corollary asserting a broad ordinary-derived equivalence between general chiral `\cA`-modules and conilpotent `\cA^!`-comodules, with a proof that simply upgraded the counit/unit quasi-isomorphisms to a derived equivalence. The same overclaim had propagated into `chiral_koszul_pairs.tex` as `thm:module-category-equivalence`, which asserted a general derived equivalence, Ext--Tor duality, simple/projective transport, and Hochschild transport for any chiral Koszul pair, and into `koszul_pair_structure.tex`, where admissible representation categories were said to be exchanged via that theorem. None of that is supported by the repaired theorem surface: the intrinsic proved comparison is the bar-coalgebra Positselski package, and the manuscript’s actual ordinary module-theoretic result is the later `\Eone` complete/conilpotent theorem `thm:e1-module-koszul-duality` on the quadratic genus-0 lane. I therefore deleted the stale proved-here module-equivalence surfaces, replaced them by explicit frontier remarks, narrowed the admissible-level proposition back to its numerical level-involution content, and rewired the surviving local references to the honest `\Eone` theorem or to the flat finite-type completed-dual comparison. Metadata regeneration dropped the proved-here count accordingly. | **FIXED** |
| F179 | — | **SERIOUS** | Beilinson | The appendix `existence_criteria.tex` was still smuggling the deleted ordinary module-theoretic package back into the base definition of Koszulness. Both the “Central question” and `def:koszul-property-existence` listed a broad module/comodule equivalence with completeness/conilpotence conditions as if it were part of the core existence problem for a general chiral Koszul dual. That contradicts the repaired theorem chain: the intrinsic higher-genus statement is the bar-coalgebra coderived/contraderived comparison, while the manuscript’s ordinary module-level equivalence is only the separate quadratic genus-0 `\Eone` theorem. I therefore narrowed the appendix so the theorematic core is conditions `(1)`--`(2)` only, removed module/comodule equivalence from the definition of Koszulness, and added an explicit remark fencing module-category comparison as extra data requiring additional hypotheses and pointing back to the actual proved surfaces. | **FIXED** |
| F180 | — | **SERIOUS** | Beilinson | A wrapper-level module-duality cluster was still overstating the repaired theorem surface. The opening of `chiral_modules.tex` was speaking as if general chiral module bar resolutions, Ext-collapse, and higher-genus inversion already held across arbitrary module categories, and the section opener made the same jump from the intrinsic bar-comodule construction to representation-category equivalence. In parallel, `chiral_koszul_pairs.tex` still closed by saying the bar-cobar adjunction “extends from algebras to modules” and descends to module categories without the now-essential lane restriction, while `free_fields.tex` introduced the free-field dualities as if they globally extended to module categories and twisted sectors. I narrowed all of these wrappers to the honest record: generally one has module bar constructions and bar-coalgebra comodules; the proved ordinary module-category duality is the separate quadratic genus-0 `\Eone` complete/conilpotent theorem; higher-genus module complexes live on the intrinsic curved bar-coalgebra side; and the free-field chapter now advertises only the specific module-theoretic and twisted-sector comparisons that are separately proved there. | **FIXED** |
| F181 | — | **CRITICAL** | Beilinson | A deeper theorem-level mismatch remained in the module/fusion surface itself. In `chiral_modules.tex`, `prop:module-koszul-equivalence` and `thm:monoidal-module-koszul` had already been narrowed upstream, but the later fusion theorem `thm:fusion-bar-cobar` was still asserting a broad `\cA \leftrightarrow \cA^!` module/comodule statement `\Phi(M_1 \boxtimes_\cA M_2) \simeq \Phi(M_1)\boxtimes_{\cA^!}\Phi(M_2)`. At the same time, the proof of `thm:monoidal-module-koszul` no longer supplied braiding reversal in general: it only constructed the Alexander--Whitney map to the cotensor product over the bar coalgebra, while the KZ/Kohno/`R`-matrix step lived specifically on the affine/Yangian Drinfeld--Kohno surfaces. This left a real source contradiction between the upstream theorem chain and the downstream fusion theorem, with further drift in `kac_moody.tex` and `concordance.tex`. I repaired the full cluster: `prop:module-koszul-equivalence` is now explicitly the quadratic genus-0 `\Eone` bar-comodule comparison; `thm:monoidal-module-koszul` is the lax monoidal bar functor theorem with cotensor target and finite-type dualized reformulation only when justified; `thm:fusion-bar-cobar` now states the honest bar-comodule compatibility map and fences braiding reversal to the affine Drinfeld--Kohno surface; the Yangian/KM wrappers cite the DK theorems for braiding reversal; and the Kac--Moody/concordance summaries now describe lax monoidal bar-comodule compatibility rather than a blanket strong monoidal `\cA^!` theorem. | **FIXED** |
| F182 | — | **CRITICAL** | Beilinson | A second theorem-level leak remained in `chiral_modules.tex` immediately below the repaired module/fusion surface. `prop:conformal-block-duality` was still tagged `ProvedHere` for arbitrary Koszul pairs and claimed genus-0 fusion equality, genus-$g$ conformal-block-rank equality, and KZB-connection duality. Its proof no longer matched any proved theorem surface: it cited `thm:monoidal-module-koszul` as if that theorem provided a braided tensor equivalence of module categories, even though the theorem now only gives a lax monoidal bar-comodule comparison; it proved the genus-$g$ part only in the semisimple factorization/Verlinde regime; and the KZB clause had no supporting comparison theorem at all. I repaired this by demoting the proposition to an explicit `\ClaimStatusConditional` categorical statement: if one separately has a braided tensor equivalence on the rational semisimple module categories, then fusion coefficients and pointed genus-$g$ conformal block dimensions agree formally by factorization. The KZB-comparison clause was removed, the complementarity remark now says this is only a conditional categorical comparison, the DS paragraph now fences any descended conformal-block duality as expected but unproved, and the admissible Kac--Moody remark no longer cites the old proposition as if it gave an internal equal-rank theorem at the non-$C_2$-cofinite dual level. | **FIXED** |
| F183 | — | **SERIOUS** | Beilinson | The lattice example chapter still had a proved-here proposition `prop:lattice-module-koszul` packaging a full module-category duality theorem out of the now-narrowed module-bar machinery. It claimed that the Koszul duality functor sends twisted lattice modules to the dual twisted sectors, preserves their fusion product, and preserves the pointed modular `S`-matrix, while the proof only checked one concrete mechanism: the module-insertion monodromy in the bar complex changes from `z^{\langle \alpha,\gamma\rangle}` to `z^{-\langle \alpha,\gamma\rangle}` under Verdier reversal. That proves only the discriminant-charge involution `\gamma \mapsto -\gamma` on the bar surface, not a semisimple pointed module-category equivalence, not fusion preservation from `thm:monoidal-module-koszul`, and not a theorematic `S`-matrix comparison. I narrowed the proposition to the honest charge-inversion statement on the bar surface, removed the unsupported fusion and `S`-matrix claims from the theorem block, added an explicit scope remark fencing the stronger categorical statements as requiring a separate comparison theorem, and rewrote the `A_1` example as an arithmetic consistency check of the sign involution on `D(A_1)\cong\mathbb Z/2\mathbb Z` rather than as a proved action of a module Koszul functor. | **FIXED** |
| F184 | — | **SERIOUS** | Beilinson | The next overclaim sat back in `chiral_modules.tex` on the affine tilting surface. `prop:tilting-bar` still had a fourth proved-here clause asserting that the general module bar functor of `thm:monoidal-module-koszul` sends affine tilting modules to tilting modules at the dual level, with proof by vague preservation of Verma flags and self-duality. That no longer matches the repaired theorem chain: `thm:monoidal-module-koszul` gives only a lax monoidal bar-comodule comparison, not a module-category equivalence on which one can classify indecomposable tilting objects. The proposition now stops at the three statements actually supported: bar-complex self-duality, BGG/Verma-flag multiplicities, and the external KL transport of affine tilting modules to semisimplified quantum-group tilting modules. A new scope remark records explicitly that affine tilting preservation under the module bar functor would require a separate categorical comparison on the affine Drinfeld--Kohno surface and is not proved by the current lax monoidal theorem. | **FIXED** |
| F185 | — | **CRITICAL** | Beilinson | The next module-category theorem surface failed on status discipline. In `chiral_modules.tex`, `prop:koszul-t-structures` was still tagged `ProvedHere` and claimed a triangulated equivalence `D^b(\mathcal O_k)\simeq D^b(\mathcal O_{k'})`, a Koszul $t$-structure on the target, and an identification of its heart with `\mathcal O_k`. But its proof simply reimported the old deleted broad module-category equivalence from `rem:koszul-module-categories`, assumed without proof that the bar resolution stays inside category `\mathcal O`, and then used bar-cobar inversion as if it already produced an honest highest-weight target category. That no longer matches the repaired theorem chain, which only proves the intrinsic quadratic genus-0 `\Eone` module/comodule comparison on the explicit complete/conilpotent lane and keeps global module-category $t$-exactness as Conjecture~\ref{conj:module-category-t-exactness}. I repaired this by demoting `prop:koszul-t-structures` to an explicit `\ClaimStatusConditional` highest-weight statement: assuming the separate module-category $t$-exactness package and a highest-weight realization of the bar comparison, linear bar resolutions define a transported internal-degree/Koszul $t$-structure on the bar-side target subcategory and place simples in its heart. The old theorematic equivalence with `D^b(\mathcal O_{k'})` and heart identification were removed, the local character remark now says category-`\mathcal O` equivalence needs extra $t$-exact input, the conformal-block proposition now cites only the resulting conditional simple-object bijection, the concordance “categorical” bullet no longer advertises unconditional global `$t$-exactness’, and the `koszul_pair_structure.tex` wrapper now describes Proposition~\ref{prop:koszul-t-structures} as a conditional highest-weight shadow of the conjectural module-category $t$-exactness surface. | **FIXED** |
| F186 | — | **SERIOUS** | Beilinson | The next spillover sat a level lower, in `chiral_modules.tex` on the homological-algebra surface. `prop:ext-tor-exchange` was still tagged `ProvedHere` as an intrinsic `\Ext`--`\Tor` theorem over `\mathcal A` and `\mathcal A^!`, but its proof no longer matched the repaired module theorem chain: it appealed to the deleted blanket module-category equivalence, wrote `M^!` and `N^!` simultaneously as bar-comodules and as `\mathcal A^!`-modules, and treated the completed-dual passage to `\mathcal A^!` as if it were automatic from the intrinsic bar theorem. The actual proved surface underneath is the quadratic genus-$0$ `\Eone` complete/conilpotent lane, where Theorem~\ref{thm:e1-module-koszul-duality} gives `\Ext`--`\Coext` exchange on the bar-comodule side; any translation into `\Tor` over `\mathcal A^!` requires the extra finite-type graded-dual identification and a completed-dual module interpretation. I repaired this by rewriting the proposition to the honest proved statement `\Ext^n_{\mathcal A}(M,N)\cong \Coext^n_{\barB(\mathcal A)}(N^!,M^!)` on the explicit `\Eone` lane, removing the fake intrinsic `\Tor` formula, rewriting the explanatory remark so the theorematic content stays on the bar-comodule surface, and aligning the frontier wrapper in `chiral_koszul_pairs.tex` to describe the broader package as `\Ext`--`\Coext` exchange plus finite-type `\Tor` reformulations rather than a blanket `\Ext`--`\Tor` theorem. | **FIXED** |
| F187 | — | **CRITICAL** | Beilinson | The next pass exposed a deeper source contradiction in the core `\Eone` module theorem itself. In `chiral_koszul_pairs.tex`, `thm:e1-module-koszul-duality` was simultaneously using `\mathcal A^!` for the bar coalgebra and for the dual algebra, then using an undefined `\Coext` notation between plain comodules, and finally justifying a swapped Ext formula by claiming the bar functor was contravariant even though `\Phi` had already been defined covariantly in `chiral_modules.tex`. The same drift had already leaked into the local dg-Yangian construction, the derived-Langlands KL/bar theorem, the Yangian Ext corollary, and a BGG-resolution proof that cited theorem part `(ii)` for mere functoriality. I repaired the source theorem by separating the ordered bar coalgebra `C_{\mathcal A}=\barB^{\mathrm{ch}}(\mathcal A)` from the dual algebra `\mathcal A^!=C_{\mathcal A}^\vee` on finite-type lanes, narrowing item `(i)` to the actual cofree/injective statement for the bar image of a simple module, replacing the undefined `\Coext` clause by explicit derived-Hom in the conilpotent bar-comodule category with the same order of arguments as on the module side, and fencing the character-resolution statement to the finite-type dual-algebra interpretation. I propagated that correction to the chapter-local derived-Ext proposition in `chiral_modules.tex`, the dg-Yangian construction there, the contragredient-vs-Koszul remark, the derived-Langlands KL/bar adjunction theorem, the Yangian Ext corollary, the frontier wrapper in `chiral_koszul_pairs.tex`, and the bar-complex BGG corollary so it now cites the module-functor definition rather than the wrong theorem clause. | **FIXED** |
| F188 | 2026-04-01 03:50:06 SAST | **CRITICAL** | Beilinson | The next Yangian DK pass found a theorem-status leak in the finite-dimensional/polynomial-`\mathcal O` promotion layer. `yangians_computations.tex` was still upgrading the repaired quadratic `\Eone` module/bar theorem into an actual Yangian/quantum-group factorization equivalence on all finite-dimensional modules and on `\mathcal O_{\mathrm{poly}}`. Concretely: the Yangian BGG theorem still read its bar image as a dual Yangian resolution without fencing the bar-comodule step; the Yangian Ext corollary was still asserting an ordinary `\operatorname{Ext}_{Y_{-\hbar}}` comparison rather than the intrinsic bar-comodule derived-Hom statement; the `\mathcal O` remark still claimed Koszulness would automatically produce `D^b(\mathcal O_{Y_\hbar})\simeq D^b(\mathcal O_{Y_{-\hbar}})`; `prop:yangian-dk2-thick-generation` was still pretending that preservation of category `\mathcal O` by Theorem~\ref{thm:e1-module-koszul-duality} was the only missing input; `prop:bar-cobar-kazhdan-candidate` was falsely presenting a proved functor `\operatorname{Mod}(\cA)\to\operatorname{CoMod}(\cA^{!,c})\simeq\operatorname{Rep}(U_q)` even though the repaired theorem chain only gives the intrinsic bar-comodule comparison; and `thm:dk-fd-typeA`, `cor:dk-partial-conj`, and `cor:dk-poly-catO` were still claiming proved-here promotion from the evaluation locus by thick generation alone. I repaired the whole cluster. The Yangian BGG/Ext surfaces now stay on the intrinsic bar-comodule side, with dual Yangian module interpretations fenced to the finite-type evaluation lanes where that realization is separately available. `prop:yangian-dk2-thick-generation` is now the honest formal reduction: given an ambient exact extension of the evaluation-locus correspondence, thick generation upgrades it to the thick closure. `prop:bar-cobar-kazhdan-candidate` was demoted to a remark recording only the bar-comodule route as a candidate, with the bar-comodule-to-quantum-group comparison made explicit as extra input. `thm:dk-fd-typeA` and `cor:dk-poly-catO` are now `\ClaimStatusConditional` extension criteria, not proved equivalences. I propagated the same narrowing to `yangians_drinfeld_kohno.tex`, `concordance.tex`, `editorial_constitution.tex`, `heisenberg_frame.tex`, and `landscape_census.tex`, so the constitution/high-level wrappers no longer say finite-dimensional/polynomial DK is already proved on the Yangian module surface merely from thick generation. | **FIXED** |
| F189 | 2026-04-01 04:16:58 SAST | **CRITICAL** | Beilinson | The next Yangian DK pass found the downstream DK-5 compact-core packet was still treating the finite-dimensional factorization DK pair as already proved. In `yangians_drinfeld_kohno.tex`, `prop:yangian-dk5-core-realization` literally said the quasi-inverse braided equivalences `KZ_{\mathrm{fd}},LZ_{\mathrm{fd}}` were “furnished by Theorem~\ref{thm:dk-fd-typeA},” even though that theorem was just demoted to a conditional extension criterion. The same status leak propagated to the active DK-5 closure corollary, the seven-variant closure theorem, and the landscape/constitution summaries, all of which were still speaking of “the proved finite-dimensional factorization DK core/pair.” There was also a subtler proof leak: `lem:quantum-fd-fundamental-generation` was proving the quantum-loop finite-dimensional generator statement by transporting it across the now-conditional Yangian theorem instead of proving it directly from the quantum-loop highest-`\ell`-weight/Drinfeld-polynomial classification. I repaired the whole active packet. The compact-core realization proposition now assumes a chosen finite-dimensional factorization DK pair and proves only the formal transport from that assumed input to the completed compact cores; the active DK-5 closure corollary and the seven-variant closure theorem now phrase their hypotheses as realization of a chosen finite-dimensional factorization DK pair, not a proved one; the landscape and constitution summaries are aligned to the same narrower wording; and the quantum-side generation lemma is now proved directly from the standard Chari--Pressley/Hernandez classification of finite-dimensional quantum-loop modules by factorization into fundamental evaluation modules. | **FIXED** |
| F190 | 2026-04-01 04:31:52 SAST | **SERIOUS** | Beilinson | The next Yangian pass found a smaller but real false reduction in the DK-3 wrapper layer. In `yangians_drinfeld_kohno.tex`, `rem:yangian-dk3-generated-core-application` was still saying that, after the evaluation-locus theorem, the unresolved input is only whether the evaluation-generated cores exhaust the full factorization categories. But `prop:yangian-dk3-generated-core` itself assumes more than exhaustion: it first needs the exact braided monoidal transport pair on those generated cores. I repaired that remark so it now states the honest two-step surface: first construct the ordered braided transport on the evaluation-generated factorization cores, then determine whether those cores exhaust the full factorization categories. I also aligned the matching constitutional wrapper in `editorial_constitution.tex`, where the staged DK/KL extension had drifted into sounding like pure enlargement beyond the theorematic core; it now explicitly records the generated-core transport packet as part of the unresolved factorization surface. | **FIXED** |
| F191 | 2026-04-01 04:44:11 SAST | **SERIOUS** | Beilinson | The next Yangian DK pass found a constitutional scope conflation in `editorial_constitution.tex`. The conjectural staged DK/KL extension was still saying “the proved DK-2/3 equivalence on the evaluation-generated core, equivalently on the theorematic domain `D^b(\mathcal O_{\mathrm{poly}})`,” as if the all-types H-level evaluation-generated-core theorem were literally the same proved surface as the type-$A$ Yangian module-side thick-generation target. That is too strong: the evaluation-generated core is the proved factorization/H-level surface at all simple types, while `D^b(\mathcal O_{\mathrm{poly}})` is the separate type-$A$ module-side theorematic thick-closure target identified by the finite-dimensional thick-generation analysis. I repaired the conjecture wrapper so it no longer states an equivalence between those two surfaces; it now says the proved DK-2/3 equivalence holds on the evaluation-generated core, and then separately records that on the type-$A$ Yangian module surface the corresponding theorematic thick-generation target is `D^b(\mathcal O_{\mathrm{poly}})`. | **FIXED** |
| F192 | 2026-04-01 04:10:56 SAST | **SERIOUS** | Beilinson | The next Yangian DK pass found a status leak in `yangians_drinfeld_kohno.tex` on the type-$A$ route-comparison wrapper. `rem:two-dk-proofs` was still saying there are “two independent proofs” of DK-2/3 on the evaluation-generated core, even though after the earlier repairs the thick-generation route is not a proved second proof of DK-2/3 at all: Theorem~\ref{thm:dk-fd-typeA} is only a conditional extension criterion that would propagate an already-supplied ambient extension/comparison package beyond generators. The only proved DK-2/3 resolution on that surface is the sectorwise-convergence route, Corollary~\ref{cor:dk23-all-types}. I repaired the remark so it now distinguishes a proved all-types sectorwise-convergence route from a type-$A$-specific conditional thick-generation route, and records only their compatibility on the identified type-$A$ generated core. | **FIXED** |
| F193 | 2026-04-01 04:18:29 SAST | **CRITICAL** | Beilinson | The next Yangian DK pass found a theorem-level object conflation in `yangians_drinfeld_kohno.tex`. `thm:ind-completed-extension` was still presented as a proved-here extension from the evaluation-generated core to the full Yangian category `\mathcal O`, and its proof claimed a canonical `t`-structure whose heart contains `\mathcal O` by citing the type-$A$ evaluation-core theorem. But the actual proof only establishes the formal ind-completion of the generated-core equivalence by the universal property of `\operatorname{Ind}`; it does not identify that cocompletion with the full module category, and the cited type-$A$ theorem cannot support a general all-types heart-inclusion clause. I repaired the theorem to the honest surface: a formal ind-completion of the proved generated-core DK equivalence, yielding presentable stable ind-completions only. I also narrowed the surrounding wrapper remarks so they no longer say the full category `\mathcal O` is already controlled by that theorem, and I rewrote the later spectral/dg packet proofs to use the generic ind-completion universal property directly rather than misapplying `thm:ind-completed-extension` as a stand-in for an arbitrary small stable category. | **FIXED** |
| F194 | 2026-04-01 04:34:08 SAST | **CRITICAL** | Beilinson | The next Yangian MC3 pass found a theorem-chain regression that contradicted the repaired DK surface. In `yangians_computations.tex`, `conj:dk-compacts-completion` was still tagged `ProvedHere`, `thm:mc3-type-a-resolution` still claimed the whole four-package MC3 problem was solved in type~$A$, `cor:mc3-all-types` still promoted that to all simple types, and `prop:mc3-automatic-generalization` still treated the remaining completion steps as already available in every type. None of that was compatible with the corrected DK record: all-types package~(i) categorical CG and the evaluation-generated-core DK comparison are proved, type~$A$ additionally proves shifted-prefundamental generation and pro-Weyl recovery, but the compact/completed extension beyond the evaluation-generated core remains an open packet. I repaired the theorem chain accordingly: `conj:pro-weyl-recovery` is now explicitly type~$A$; `conj:dk-compacts-completion` is again conjectural; `thm:mc3-type-a-resolution` now proves only packages~(i)--(iii) and reduces MC3 to the remaining compact-completion packet; `cor:mc3-all-types` now records the all-types post-CG reduction rather than a false global resolution; and `prop:mc3-automatic-generalization` is now a conditional statement about the type-independence of the remaining completion mechanisms. I propagated the same rollback through `yangians_drinfeld_kohno.tex`, `editorial_constitution.tex`, `concordance.tex`, `outlook.tex`, and `thqg_introduction_supplement_body.tex`, so the constitution and overview surfaces no longer say MC3 or all five master conjectures are fully proved. | **FIXED** |
| F195 | 2026-04-01 04:40:30 SAST | **SERIOUS** | Beilinson | The next Yangian MC4/DK wrapper pass found a residual scope leak in `yangians_drinfeld_kohno.tex`: the “Standard-tower Yangian frontier” remark was still saying “Because MC3 is resolved in type~$A$, this frontier is now localized to that realization step there; extension to arbitrary type still awaits the corresponding MC3 generalization.” That contradicted the repaired MC3 record in the same repo. Type~$A$ does not resolve MC3; it only reduces the old four-package problem to the remaining compact/completed extension packet `conj:dk-compacts-completion`, and the all-types categorical CG/core surface is already proved by `cor:mc3-all-types`. I repaired the remark so it now records the honest frontier: for the standard type-$A$ RTT tower the live inputs are the H-level realization plus the post-core DK-5 compact/completed comparison, while in arbitrary type the remaining work is the corresponding post-core extension/completion packet together with the relevant realization input. | **FIXED** |
| F196 | 2026-04-01 04:43:58 SAST | **SERIOUS** | Beilinson | The next Yangian DK wrapper pass found two local summary relapses in `yangians_drinfeld_kohno.tex`. The chapter opener was still saying the all-types DK-0/3 surface is proved “leveraging the MC3 resolution,” even though the cited input is only the all-types categorical Clebsch--Gordan theorem `thm:categorical-cg-all-types`, not a closure of MC3 as a whole. Later, the bridge-criterion summary still said “MC3 closes Lemma~3” when the actual proved input on that surface is DK-2/3 on the evaluation-generated core. I repaired both wrappers so they now refer explicitly to the all-types categorical CG closure and to the proved DK-2/3 evaluation-generated-core package, rather than using `MC3` as shorthand for a globally closed problem. | **FIXED** |
| F197 | 2026-04-01 04:48:34 SAST | **SERIOUS** | Beilinson | The next Yangian MC3 pass found a constitution-level status leak in `concordance.tex`. The `MC3 status by Dynkin type` construction was still saying “Packages~(ii)--(iv) are type-independent” and citing `prop:mc3-automatic-generalization`, even though that proposition had already been demoted to `\ClaimStatusConditional` and now requires the shifted-prefundamental, pro-Weyl, and compact-completion packets as hypotheses. I repaired the constitution so it now states the honest conditional form: once those inputs are supplied in a given type, the remaining packages proceed without further type-$A$ combinatorics. | **FIXED** |
| F198 | 2026-04-01 04:52:45 SAST | **CRITICAL** | Beilinson | The next Yangian formal-moduli pass found a theorem-import overclaim in `yangians_drinfeld_kohno.tex`. The remark `rem:tangent-controls-extension` was still saying that all four remaining MC3 extension conjectures follow from a single structural input, namely the Mittag--Leffler property of the RTT tower on bar cohomology, via the strong completion-tower theorem `thm:completed-bar-cobar-strong`. That is false on the current claim surface: `thm:completed-bar-cobar-strong` is a completed bar/cobar theorem, not a theorem about Baxter exact triangles, shifted-prefundamental generation, pro-Weyl recovery, or compact DK extension on module categories. I repaired the remark so it now states the honest role of the RTT-adapted filtration: it supplies a common completed bar-side/formal-moduli framework relevant to the remaining MC3 packets, but additional module-theoretic or formal-neighborhood identifications are still required for each packet. | **FIXED** |
| F199 | 2026-04-01 04:54:27 SAST | **CRITICAL** | Beilinson | The next Yangian DK-5 pass found a theorem-chain scope leak in `yangians_drinfeld_kohno.tex`. `conj:dk5-infty-pbw` was still saying that, assuming only the DK-4 formal-moduli conjecture, the full triple bridge reduces to a single PBW input, and its displayed chain wrote the DK-3 arrow as if it already lived on the full factorization category. That contradicted the repaired MC3 record elsewhere in the same chapter: the post-core DK-3 extension/completion packet beyond the evaluation-generated core is still open and is a prerequisite for any full DK-5 bridge statement. I repaired the conjecture and its local wrappers so they now say the honest thing: once the post-core DK-3 extension has been supplied and the DK-4 formal-moduli input is assumed, the remaining DK-5 bridge step reduces to the exponential/PBW comparison. The DK ladder table and the modular-Yangian wrapper were aligned to the same scope. | **FIXED** |
| F200 | 2026-04-01 05:00:10 SAST | **CRITICAL** | Beilinson | The next Yangian DK-4 aftermath pass found another theorem-import overclaim in `yangians_drinfeld_kohno.tex`. The remark `rem:dk4-mittag-leffler` was still saying the completed bar-cobar equivalence “sharpens” the MC3 extension conjectures by transporting the pro-Weyl tower to a Mittag--Leffler bar system and effectively leaving only compact shifted-prefundamental generation as the remaining compact-DK input. That was too strong on the repaired claim surface. Corollary `cor:completed-bar-cobar-yangian` is a completed bar/cobar statement; it does not by itself construct the Weyl truncation tower, prove the surjectivity/Mittag--Leffler input for pro-Weyl recovery outside the separately proved type-$A$ theorem, or supply the compact-core extension/completion comparison needed for `conj:dk-compacts-completion`. I repaired the remark so it now says only that the completed bar-cobar theorem supplies the bar/factorization setting in which those conjectural packets would live, with the type-$A$ pro-Weyl theorem fenced explicitly and the compact-DK comparison still requiring extra input even there. | **FIXED** |
| F201 | 2026-04-01 05:05:16 SAST | **SERIOUS** | Beilinson | The next Yangian categorical-context pass found a scope leak in `yangians_drinfeld_kohno.tex`. The remark `rem:coderived-natural-ambient` was still saying the off-Koszul MC3 conjectures “are all statements about” the factorization Positselski equivalence `eq:factorization-positselski`. That was too strong on the theorem surface: `thm:factorization-positselski` only gives a coderived/contraderived bar-coalgebra comparison. It does not by itself produce Baxter exact triangles, shifted-prefundamental generation, pro-Weyl recovery, or DK on compacts, all of which still need additional module-theoretic, compact-generation, or formal-neighborhood input. I repaired the remark so it now says those conjectures are naturally formulated relative to that ambient setting, not equivalent to the Positselski theorem itself. | **FIXED** |
| F202 | 2026-04-01 05:09:57 SAST | **SERIOUS** | Beilinson | The next Yangian Strategy~IV pass found a status-boundary leak in `yangians_computations.tex`. The broad strategy remark and the conjecture `conj:mc3-sectorwise-all-types` were still saying sectorwise control would carry DK-2/3 to the “full MC3 package” for all types, even though the assumed input is only spectral/bar-side control on the completed/coderived enlargement. On the repaired claim surface, that can at most enlarge the DK comparison to the completed/coderived factorization domain relevant to the remaining MC3 packets; it does not by itself prove the separate module-theoretic packets such as Baxter triangles, pro-Weyl recovery, or compact DK extension. I narrowed the conjecture title/body and the constitution mirrors accordingly. | **FIXED** |
| F203 | 2026-04-01 05:14:54 SAST | **MINOR** | Beilinson | The next Yangian pass found one last wrapper-level scope leak in `yangians_drinfeld_kohno.tex`. Even after the body of `rem:coderived-natural-ambient` was repaired, its opening sentence still called the Positselski comparison the context for the “full MC3 package,” which was broader than the actual off-Koszul surface discussed there. I narrowed that phrase to the remaining off-Koszul MC3 extension packets so the wrapper no longer reintroduces a global slogan. | **FIXED** |
| F204 | 2026-04-01 05:19:43 SAST | **MINOR** | Beilinson | The next Yangian subsection-level pass found a summary-layer scope leak in `yangians_drinfeld_kohno.tex`. The opener of the `\infty`-categorical factorization-Koszul-duality subsection was still saying “DK-4/5 reduces to a formal deformation theory comparison within the same framework,” which understated the repaired post-core MC3 obstruction and made the later stages sound automatically swallowed by the formal-moduli package. I narrowed that sentence so it now says only that DK-4/5 can be organized within the same formal-deformation framework, with the post-core MC3 completion packet and the later identifications isolated inside that setting rather than automatically resolved by it. | **FIXED** |
| F205 | 2026-04-01 05:34:02 SAST | **SERIOUS** | Beilinson | The next Yangian RTT-wrapper pass found a source-level overcompression in `yangians_drinfeld_kohno.tex`. The remark `rem:rtt-is-the-duality-datum` was still saying the entire proved DK ladder rests on a “single structural observation,” that three one-line consequences “give the full result,” and that “DK-4 adds no new idea.” That was too strong on the repaired claim surface. The RTT presentation is the common algebraic datum behind DK-0 through DK-3, but the proof still uses logically distinct PBW/Koszulness and sectorwise spectral-descent inputs, and the later DK-4/5 stages still require the separate post-core completion packet plus additional structure identifications. I narrowed the remark so it now treats RTT as the shared organizing datum, keeps the sectorwise descent step visibly separate from the algebraic Koszul-dual step, and fences the DK-4/5 paragraph to the bar-side completion story it actually supports. | **FIXED** |
| F206 | 2026-04-01 05:52:48 SAST | **SERIOUS** | Beilinson | The next Yangian MC3 summary pass found a wrapper-level type-propagation overclaim in `yangians_computations.tex`. The `Type-$A$ resolution` summary still ended by saying the extension to arbitrary simple type “uses” the all-types categorical Clebsch--Gordan theorem, which was too strong on the repaired claim surface. After the all-types CG input and the evaluation-generated-core DK comparison, the post-core pro-Weyl/completion argument propagates beyond type~$A$ only conditionally, once the shifted-prefundamental, pro-Weyl, and compact-completion packets of `prop:mc3-automatic-generalization` are separately supplied in that type. I narrowed that summary sentence to match the actual theorem flow. | **FIXED** |
| F207 | 2026-04-01 06:07:11 SAST | **SERIOUS** | Beilinson | The next Yangian DK-5 wrapper pass found a seed-vs-line conflation in `yangians_drinfeld_kohno.tex`. Several late type-$A$ DK-4/DK-5 summaries were still saying that the only remaining input is realization of the “standard spectral vector seed-and-shift datum,” but the local proved chain is weaker: the formal closure package is triggered by spectral vector-line realization, and only on the multiplicative spectral realization locus is that package equivalently expressible as the standard seed-and-shift datum. I narrowed the proposition/proof and the two downstream corollaries so they now distinguish the vector-line realization package from its sharper multiplicative seed-and-shift reformulation. | **FIXED** |
| F208 | 2026-04-01 06:21:04 SAST | **SERIOUS** | Beilinson | The next Yangian constitution pass found a summary-layer lag in `editorial_constitution.tex`. The constitution was still saying `cor:yangian-canonical-realization-to-spectral-seed` reduces the remaining canonical-target input to the “standard spectral vector seed-and-shift datum,” even though the repaired Yangian chapter now distinguishes the broader spectral vector-line realization package from its sharper multiplicative seed-and-shift reformulation. I aligned the constitution so it now states the vector-line package first, with the seed-and-shift datum appearing only as the equivalent sharper form on the equivariant multiplicative spectral realization locus. | **FIXED** |
| F209 | 2026-04-01 06:33:18 SAST | **SERIOUS** | Beilinson | The next Yangian outlook pass found an overview-level scope leak in `outlook.tex`. The Ring~3 frontier summary was still saying “DK-5 formal once categorical inputs established,” which was too broad after the repaired Yangian chain: the formal DK-5 closure statements are currently on the standard type-$A$ tower and depend on specific realized compact-core or spectral vector-line packets, not on an unspecified global categorical input. I narrowed that outlook sentence to the honest scope. | **FIXED** |
| F210 | 2026-04-01 06:46:31 SAST | **SERIOUS** | Beilinson | The next Yangian constitution pass found a dropped hypothesis in the MC4 summary layer of `editorial_constitution.tex`. Two constitution summaries were still reading Corollary `cor:yangian-typea-degree2-plus-generators` as if, under one-loop exactness, the only remaining input were the single degree-$2$ fundamental-line identity. That was too strong on the repaired theorem surface: the local coefficient identity is only one part of the remaining MC4 package, which still also carries the compact-generator comparison. I narrowed both constitution summaries so they now speak of the coefficient part of the remaining input, rather than collapsing the whole package to that one identity. | **FIXED** |
| F211 | 2026-04-01 06:56:02 SAST | **SERIOUS** | Beilinson | The next Yangian DK-5 pass found a proof-level lag in `yangians_drinfeld_kohno.tex`. The statement of `cor:yangian-canonical-realization-to-spectral-seed` had already been narrowed to the broader spectral vector-line realization package, but the proof still said RTT-adapted realization reduces the remaining input to the standard spectral vector seed-and-shift datum. I aligned the proof to the repaired theorem surface: the immediate remaining input is the vector-line realization package, and only on the equivariant multiplicative spectral realization locus is that equivalently the seed-and-shift datum. | **FIXED** |
| F212 | 2026-04-01 07:14:20 SAST | **SERIOUS** | Beilinson | The next Yangian MC4 wrapper pass found a scope leak in `yangians_drinfeld_kohno.tex`. The `rem:yangian-mc4-boundary` remark still said `cor:yangian-hlevel-comparison-criterion` makes the H-level comparison formal once finite quotients recover the proved stages, which dropped the actual compatibility hypotheses on the completed bar/cobar comparison data and continuity package. The same remark also compressed the remaining DK-5 packet to `compact generation plus fundamental-packet transport`, even though the repaired chapter records the remaining work as the compact-core realization and extension packet. I narrowed both phrases to match the proved local theorem surface. | **FIXED** |
| F213 | 2026-04-01 07:31:52 SAST | **SERIOUS** | Beilinson | The next Yangian B2 pass found a source-level object mismatch in `yangians_drinfeld_kohno.tex`. The `completion-four-checks` table was still listing check~(4) as “inverse-limit identification with `\\Ydg_\\cA`” while citing only `prop:yangian-rtt-completion-identification`, which proves the standard RTT inverse-limit identification, not the open H-level comparison to an RTT-adapted dg model. The surrounding prose then compounded the mismatch by saying the single remaining check is a concrete linear algebra problem in type~A. I repaired that wrapper so check~(4) is now the H-level comparison packet governed by `cor:yangian-hlevel-comparison-criterion`, separated the theorematic standard RTT M-level package from the open H-level realization/comparison step, and fenced the type~A one-line residue reduction as only the live local coefficient packet inside that larger check. | **FIXED** |
| F214 | 2026-04-01 07:43:37 SAST | **SERIOUS** | Beilinson | The next Yangian Bridge-Theorem pass found the same M-level/H-level conflation earlier in `yangians_drinfeld_kohno.tex`. The B2 sub-target still listed check~(4) as “inverse-limit identification with the dg-shifted Yangian,” and Step~3 of the Bridge Criterion proof was still attributing that identification directly to `prop:yangian-tower-mc4-criterion`, which only proves the standard RTT inverse-limit bar package. I narrowed both places so B2 now ends with the H-level comparison to an RTT-adapted dg model, and the proof now passes through the standard RTT completion first and invokes `cor:yangian-hlevel-comparison-criterion` only for the final H-level identification. | **FIXED** |
| F215 | 2026-04-01 07:59:05 SAST | **SERIOUS** | Beilinson | The next Yangian spectral-bridge pass found an unshifted-strictification overclaim in `yangians_drinfeld_kohno.tex`. The `C1--C4` status remark was still saying extension of the proved evaluation-locus DK-0/1 data to the full completion is “the content of B2,” even though B2 only supplies the completed dg side and not by itself the unshifted strict spectral/factorization quantum-group input required by Condition~C1. The downstream DK-square wrapper then said that once the remaining MC3 completion packet is closed, the single remaining obstruction is the spectral Drinfeld class in filtration `>=4`. I narrowed both surfaces so the Drinfeld-class-only statement is now explicitly conditional on first supplying the unshifted completed strict spectral input of C1, with B2 kept as only one part of that package. | **FIXED** |
| F216 | 2026-04-01 08:15:18 SAST | **SERIOUS** | Beilinson | The next Yangian spectral-status pass found a second overcompression in `yangians_drinfeld_kohno.tex`. The `C1--C4` status remark was still saying Condition~C3 “reduces to the residue-bounded completion check~(4)” of the Yangian B2 package. That was too strong: C3 is the completed compatibility upgrade from finite-stage spectral transport to the completed dg kernel, and it runs through `thm:spectral-residue-bounded-completion` together with whatever completed dg model and H-level comparison data have already been supplied. I rewrote the C3 bullet and the closing summary so C3 is no longer identified with B2 check~(4) alone. | **FIXED** |
| F217 | 2026-04-01 08:31:12 SAST | **SERIOUS** | Beilinson | The next concordance pass found that the global spectral-Drinfeld frontier summary was still overstating the obstruction surface. In `concordance.tex`, the filtration-`\\ge 4` item still said the spectral Drinfeld class simply “controls strictification” and then concluded that strictification is proved through filtration~4 and unobstructed through filtration~11. After the repaired Yangian chapter, those statements are only honest on the conditional strictification surface, once the unshifted completed strict spectral input and the completed compatibility package are already in place. I narrowed the concordance wording so the Drinfeld class is now explicitly the remaining intrinsic obstruction on that conditional surface, rather than the whole strictification problem by itself. | **FIXED** |
| F218 | 2026-04-01 08:45:02 SAST | **SERIOUS** | Beilinson | The next Yangian pass found a concrete object-level notation leak in the chapter opener. The `Three-layer Yangian triage` remark was still naming the H-level residual target as `\\mathcal{W}^{\\mathrm{ht}}`, which is the principal `W_\\infty` H-level symbol from a different bridge, not the Yangian target. I corrected that line to the honest Yangian object `\\Ydg_{\\cA}` and made the residual task explicitly the RTT-adapted H-level dg target for the standard Yangian tower. | **FIXED** |
| F219 | 2026-04-01 09:06:11 SAST | **SERIOUS** | Beilinson | The next Yangian Bridge-Theorem pass found a theorem-level hypothesis mismatch in `yangians_drinfeld_kohno.tex`. The local B2 package had already been narrowed to a two-stage statement consisting of the theorematic standard RTT completion plus the remaining H-level realization/comparison packet, and Step~3 of the proof still used that full package to reach `\\operatorname{Mod}^{\\mathrm{comp}}(\\Ydg_{\\cA})`. But the theorem statement itself had silently collapsed assumption~B2 back to the standard RTT inverse-limit identification alone. I repaired the theorem statement and Step~3 together so B2 now explicitly includes the H-level realization/comparison packet identifying the standard completion with `\\Ydg_{\\cA}`, matching the proof and the surrounding wrapper surfaces. | **FIXED** |
| F220 | 2026-04-01 09:19:37 SAST | **SERIOUS** | Beilinson | The next Yangian bridge-wrapper pass found a direct contradiction inside `yangians_drinfeld_kohno.tex`. The obstruction analysis and the boundary/line summary were still saying the boundary/line path avoids B1 or can replace the category-`\\mathcal O` extension. But the chapter's own boundary/line bridge criterion only yields the full bridge together with B1 and B2, so on the current theorem surface that path is an alternative route to the spectral-comparison side, not a theorematic substitute for full category-`\\mathcal O` Koszulness. I narrowed the FG-completion bullet, the summary sentence, and the boundary/line wrapper accordingly. | **FIXED** |
| F221 | 2026-04-01 09:34:20 SAST | **SERIOUS** | Beilinson | The next Yangian bridge-criterion pass found a second hypothesis compression in `yangians_drinfeld_kohno.tex`, this time in B1. The local B1 sub-target is the full category-`\\mathcal O` Koszul extension package, including the actual extension of the bar/cobar comparison beyond evaluation modules. But the theorem statement had drifted down to bare spectral-sequence collapse while Step~2 still treated B1 as if the extension were already available. I repaired the theorem assumption, Step~2, and the nearby obstruction summary so B1 again names the full module-side extension package, with full-category Koszulness kept as its homological core rather than the whole statement. | **FIXED** |
| F222 | 2026-04-01 09:48:11 SAST | **SERIOUS** | Beilinson | The next Yangian bridge pass found a theorem-level monodromy overclaim in `yangians_drinfeld_kohno.tex`. The full-bridge conjecture and the Bridge Criterion theorem were both asserting compatibility with `KZ/KZB` monodromy, but the local chapter only proves and uses the KZ/evaluation-locus braid-monodromy package; those were in fact the only two appearances of `KZB` on this surface. I narrowed both statements to compatibility with the KZ monodromy package on the evaluation locus and removed the unsupported KZB export. | **FIXED** |
| F223 | 2026-04-01 10:02:18 SAST | **SERIOUS** | Beilinson | The next Yangian bridge-criterion pass found an object mismatch in B4. The theorem statement and Step~4 of the proof were still phrasing B4 as a “factorization-level Kazhdan equivalence,” but nowhere else in the Yangian bridge architecture is B4 defined that way: the local B4 sub-target is the spectral quantum-group comparison identifying the middle category with Latyntsev's factorization quantum-group side from spectral `R`-matrix data. I repaired the theorem assumption and Step~4 so B4 now names the actual spectral comparison object already used everywhere else on this surface. | **FIXED** |
| F224 | 2026-04-01 10:17:46 SAST | **SERIOUS** | Beilinson | The next Yangian bridge pass found an object/category conflation cluster in `yangians_drinfeld_kohno.tex`. The bridge wrapper and proof were still saying things like `\\operatorname{Mod}^{\\mathrm{comp}}(\\Ydg_{\\cA})` is an “actual MC moduli space” and that completed MC moduli “coincide” with `\\operatorname{Rep}^{\\mathrm{spec}}(\\QGspec(R_{\\cA}))`, even though those are categories presented by deformation data, not moduli spaces themselves. I rewrote the bridge-programme opener, the Step~2 heading, the Step~3/Step~4 conclusions, and the MC4 CG-perspective sentence so this surface now distinguishes deformation-theoretic presentations from the categories they present. | **FIXED** |
| F225 | 2026-04-01 10:31:04 SAST | **SERIOUS** | Beilinson | The next Yangian bridge-proof pass found that the proof body was still importing the repaired theorem surface through the wrong kind of object. It opened as a “chain of three MC morphisms between convolution algebras,” then called Step~1 an “evaluation-locus MC equivalence,” even though the cited input `thm:factorization-dk-eval` is a proved factorization-category equivalence, not an MC-groupoid theorem. I rewrote the proof spine so it now says exactly what the chapter uses: a proved evaluation-locus factorization bridge, followed by the dg-side completion stage and the spectral comparison stage, with the B3 aside phrased as a deformation-theoretic comparison rather than a bare “MC equivalence.” | **FIXED** |
| F226 | 2026-04-01 10:43:22 SAST | **SERIOUS** | Beilinson | The next Yangian boundary/line pass found a wrapper-level overcompression in `yangians_drinfeld_kohno.tex`. The subsection was still saying the boundary/line bridge criterion is just a CG reduction to proving one convolution-algebra morphism is an MC equivalence, but the conjecture immediately below also requires the separate boundary spectral `R`-matrix condition. I narrowed the CG-perspective wrapper so it now distinguishes the bulk-to-boundary deformation comparison from the additional spectral-factorization identification on the boundary side, instead of swallowing both into a single “MC equivalence” slogan. | **FIXED** |
| F227 | 2026-04-01 11:00:14 SAST | **SERIOUS** | Beilinson | The next Yangian boundary/line pass found one more false-precision jump in the same wrapper. The subsection was still saying `F_b` is literally the MC-moduli map induced by `\\rho_b`, and that the derived-center condition `\\beta_{\\mathrm{der}}` is the assertion that `\\rho_b` is an MC equivalence. That is stronger than the local surface constructs: the conjecture only records boundary module, derived-center, and spectral `R`-matrix data. I narrowed those sentences so `F_b` is now the boundary-side functorial datum on the module surface, while `\\beta_{\\mathrm{der}}` is described as bulk recovery on the derived-center side rather than a theorematic MC-equivalence statement. | **FIXED** |
| F228 | 2026-04-01 11:12:09 SAST | **SERIOUS** | Beilinson | The next Yangian boundary/line pass found a benchmark-status overclaim in the affine `\\widehat{\\mathfrak{sl}}_2` bullet of `yangians_drinfeld_kohno.tex`. That item was still saying the boundary chiral algebra is a W-algebra, `F_b` is the KL functor, and derived-center reconstruction is the content of the Feigin--Frenkel theorem at critical level. This imported several distinct affine/critical results as if they already supplied the full boundary/line bridge package. I narrowed the bullet to an honest prototype status: the DS/Wakimoto and critical-center packages suggest the right boundary algebra and provide bulk-side center data, but neither a theorematic boundary functor nor a full derived-center reconstruction statement is proved here. | **FIXED** |
| F229 | 2026-04-01 11:24:43 SAST | **SERIOUS** | Beilinson | The next Yangian boundary/line pass found a remaining object-level overclaim in `yangians_drinfeld_kohno.tex`. The summary paragraph after the boundary/line criterion was still saying that if `\\beta_{\\mathrm{der}}` is an isomorphism then the bulk chiral/factorization data are reconstructible from the derived center, and therefore the boundary module category contains both the bulk and line information needed for the factorization-level comparison. That was too strong on the local surface: the derived-center condition gives a route back to the bulk algebra, not the whole factorization package by itself. I narrowed the paragraph so it now separates the recovered bulk algebra, the line-side data on `V_b`-mod, and the additional spectral identification on the boundary side. | **FIXED** |
| F230 | 2026-04-01 11:37:33 SAST | **MINOR** | Beilinson | The next Yangian boundary/line benchmark pass found a benchmark-status overclaim in `yangians_drinfeld_kohno.tex`. The `Y(\\mathfrak{sl}_2)` bullet of `rem:boundary-line-benchmark` was still speaking as if the dg-shifted Yangian already came with an explicit rational `R`-matrix on this theorem surface, even though the later dedicated benchmark remark only says that the rational Yangian `R`-matrix \emph{should be recovered} from the degree-2 MC shadow. I narrowed the `Y(\\mathfrak{sl}_2)` and `Y(\\mathfrak{sl}_N)` bullets so they now describe the honest benchmark tests: recovery of the expected rational `R`-matrix from the arity-2 shadow, matching of the boundary matrix `R_b(z)` to that benchmark, and higher-rank Yang--Baxter coherence as a target check rather than a completed output. | **FIXED** |
| F231 | 2026-04-01 11:51:42 SAST | **SERIOUS** | Beilinson | The next Yangian boundary/line pass found a proof-level contradiction in `yangians_drinfeld_kohno.tex`. The Bridge Criterion proof summary was still saying the B3 path reaches B4 “via derived-center reconstruction” and that the deformation-theoretic comparison coming from `\\beta_{\\mathrm{der}}` gives the spectral comparison. That contradicted the repaired local bridge-path remark, which keeps three ingredients distinct: the line-to-boundary equivalence `F_b`, the derived-center recovery of the bulk algebra, and the additional spectral `R`-matrix identification on the boundary module side. I rewrote the proof summary so it now records exactly that three-part boundary-side package instead of collapsing it to derived-center reconstruction alone. | **FIXED** |
| F232 | 2026-04-01 12:05:25 SAST | **SERIOUS** | Beilinson | The next Yangian boundary/line pass found a wrapper-level hypothesis compression in `yangians_drinfeld_kohno.tex`. The subsection opener was still saying the boundary/line path constructs the missing comparison functor from a rich boundary condition, and the boundary/line bridge criterion still said the spectral quantum-group comparison follows from the boundary module category. That dropped the repaired local surface, where the route to B4 uses a full boundary-side package: the line-to-boundary comparison, the derived-center recovery map back to the bulk algebra, and the boundary spectral `R`-matrix input. I rewrote both wrappers so they now credit the full boundary/line package rather than a single boundary-category ingredient. | **FIXED** |
| F233 | 2026-04-01 12:17:35 SAST | **SERIOUS** | Beilinson | The next Yangian bridge pass found a source-level compression inside the B4 sub-target itself in `yangians_drinfeld_kohno.tex`. The Bridge-Theorem programme was still saying the OPE/fusion on the boundary/line side “produces the spectral $R$-matrix data required by Latyntsev's factorization quantum groups, so the middle category identifies with” the spectral quantum-group side. That was too strong on the repaired local surface: boundary OPE/fusion only yields candidate boundary spectral data, and the actual identification with Latyntsev's package is the open comparison problem named by B4. I narrowed the B4 item so it now distinguishes those two steps explicitly. | **FIXED** |
| F234 | 2026-04-01 12:21:21 SAST | **SERIOUS** | Beilinson | The next Yangian bridge pass found a benchmark-status contradiction inside the B3 sub-target itself in `yangians_drinfeld_kohno.tex`. The Bridge-Theorem programme was still saying the line-to-boundary functor `F_b` is an equivalence “in the benchmark families,” but the later benchmark-status remark already says the nonabelian benchmark families do not yet furnish a theorematic `F_b`: affine `\\widehat{\\mathfrak{sl}}_2` is only a prototype and the Yangian bullets are only local benchmark tests. I rewrote the B3 item so it now states the honest open package — rich boundary condition, line-to-boundary functor, and dg-shifted-Yangian control of line OPE/fusion — and explicitly says the benchmark families provide only calibration/prototype evidence rather than a proved realization of `F_b`. | **FIXED** |
| F235 | 2026-04-01 12:25:59 SAST | **SERIOUS** | Beilinson | The next Yangian bridge pass found a notation/scope conflation between B3 and the broader HT route in both `yangians_drinfeld_kohno.tex` and `concordance.tex`. After the local repairs, B3 is only the boundary/line realization sub-target, while the full HT route to B4 additionally uses the derived-center recovery map and the boundary spectral $R$-matrix input. But the proof summary and concordance wrappers were still calling that larger three-part route simply “the boundary/line path (B3).” I rewrote those summaries so they now speak of the broader HT boundary/line route beginning with B3, and explicitly fence the extra derived-center and boundary spectral inputs required to reach B4. | **FIXED** |
| F236 | 2026-04-01 12:30:01 SAST | **SERIOUS** | Beilinson | The next Yangian DK-5 pass found an overcompressed ladder summary in `yangians_drinfeld_kohno.tex`. The one-line DK status recap was still saying the remaining DK-5 step is the exponential/PBW passage `\\mathfrak{g}_{\\cA} \\rightsquigarrow G_{\\cA}` “recovering the spectral quantum group of Latyntsev,” but the actual conjecture `conj:dk5-infty-pbw` keeps two ingredients separate: PBW/exponentiation gives the formal-group side `G_{\\cA}`, and the further identification `G_{\\cA} \\simeq \\QGspec(R_{\\cA})` is an additional comparison input. I narrowed the summary line so it now matches that two-step DK-5 surface. | **FIXED** |
| F237 | 2026-04-01 12:32:58 SAST | **SERIOUS** | Beilinson | The next Yangian bridge pass found a proof-level assumption mismatch in `yangians_drinfeld_kohno.tex`. Step~4 of the Bridge Criterion proof was still saying that under B4 the MC element `r(z)=\\tau|_{\\deg 2}` is identified with a spectral $R$-matrix in the sense of Latyntsev. But the theorem's repaired B4 hypothesis is only the category-level spectral quantum-group comparison; it does not separately prove or assume that exact MC-element identification on this surface. I rewrote Step~4 so it now uses only the actual B4 input: the completed dg-side category is identified with the corresponding spectral quantum-group side. | **FIXED** |
| F238 | 2026-04-01 12:38:16 SAST | **SERIOUS** | Beilinson | The next Yangian strictification pass found a status leak in the late spectral-bridge recap of `yangians_drinfeld_kohno.tex`. That paragraph was still saying the spectral $R$-matrix from the conditional strictification criterion \emph{is} the bridge datum connecting the completed bar-cobar tower to Latyntsev's factorization quantum group, even though the surrounding discussion already says this only becomes an actual bridge after the remaining MC3 completion packet and the unshifted strict spectral input are supplied. I narrowed the sentence so the spectral $R$-matrix is now only the \emph{candidate} bridge datum on that conditional surface. | **FIXED** |
| F239 | 2026-04-01 12:45:04 SAST | **SERIOUS** | Beilinson | The next Yangian boundary/line pass found a dependency compression in `yangians_drinfeld_kohno.tex`. The boundary/line bridge criterion was still saying that the spectral quantum-group comparison~(B4) follows from the boundary/line package itself. But on the repaired theorem chain, B4 is a comparison involving the middle dg-side category `\\operatorname{Mod}^{\\mathrm{comp}}(\\Ydg_{\\cA})`, and that object only becomes available once B2 supplies the completed dg target. I narrowed the conjecture so the boundary/line package now supplies the \emph{boundary-side comparison data} for B4; once B2 furnishes the completed dg-side category, and together with B1, the full bridge follows. | **FIXED** |
| F240 | 2026-04-01 12:52:31 SAST | **SERIOUS** | Beilinson | The next Yangian bridge pass found a proof-level theorem export bug in `yangians_drinfeld_kohno.tex`. The Bridge Criterion theorem was still concluding with `r(z)=\\tau|_{\\deg 2}`, even though its assumptions and proof only provide category-level bridge data: the evaluation-locus KZ package, the full category-`\\mathcal O` extension package, the completed dg-side Yangian target, and the spectral quantum-group comparison. The arity-`2` propagator identity is isolated elsewhere as the additional benchmark package of `rem:r-from-tau`, not as a consequence of B1/B2/B4. I removed that clause from the theorem statement and explicitly fenced the propagator-shadow comparison as separate benchmark data at the end of the proof. | **FIXED** |
| F241 | 2026-04-01 12:58:07 SAST | **SERIOUS** | Beilinson | The next Yangian bridge pass found a theorem-flow mismatch in `yangians_drinfeld_kohno.tex`. After the Bridge Criterion theorem was narrowed, the surrounding conjecture `conj:full-dk-bridge` still bundled the separate arity-`2` propagator benchmark `r(z)=\\tau|_{\\deg 2}` into the bridge statement, while the wrapper remark still said the theorem was the formal implication behind that conjecture. I removed the propagator clause from the conjecture and made the wrapper remark explicit that the arity-`2` comparison remains extra benchmark data isolated in `rem:r-from-tau`. | **FIXED** |
| F242 | 2026-04-01 13:03:44 SAST | **SERIOUS** | Beilinson | The next Yangian bridge pass found a wrapper-level status leak in `yangians_drinfeld_kohno.tex`. Even after the bridge theorem and conjecture were narrowed, the local propagator wrappers were still calling `r(z)=\\tau|_{\\deg 2}` “the literal gluing law connecting the two sides of the bridge.” That contradicted the repaired status surface, where the propagator comparison is only an additional benchmark package rather than a theorematic bridge law. I rewrote both the remark header/body and the subsection opener so they now present `r(z)=\\tau|_{\\deg 2}` as a candidate/benchmark shadow comparison rather than the already-constructed bridge itself. | **FIXED** |
| F243 | 2026-04-01 13:07:52 SAST | **SERIOUS** | Beilinson | The next Yangian spectral-bridge pass found a theorem-surface overread in `yangians_drinfeld_kohno.tex`. Right after the additive KZ theorem, the exposition was still saying the operators `\\Omega_{ij}` are exactly the bar-transported `r`-matrix insertions and that “the KZ equation is literally the bar differential acting on ordered configurations.” But `thm:spectral-derived-additive-kz` only assumes an abstract bar-horizontal strictification; the cited propagator matches prove that concrete reading only on the benchmark free and affine surfaces. I narrowed the post-theorem explanation so it now states the benchmark-lane interpretation, not a blanket theorematic identity. | **FIXED** |
| F244 | 2026-04-01 13:11:43 SAST | **SERIOUS** | Beilinson | The next Yangian DK-4 pass found a theorem-flow overclaim in `yangians_drinfeld_kohno.tex`. The DK-4 reduction remark was still saying Proposition `yangian-dk4-typea-frontier` “verifies (a) at finite stages,” where (a) is the full degree-`2` propagator identification `r(z)=\\tau|_{\\deg 2}`. But that proposition only reduces the local finite-stage type-`A` packet to the auxiliary-kernel identity, residue formula, and mixed-tensor coefficient checks on the fundamental line. I narrowed the wrapper so it now credits only that finite-stage local shadow of (a), rather than the full degree-`2` identification itself. | **FIXED** |
| F245 | 2026-04-01 13:16:13 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a direct status contradiction in `yangians_drinfeld_kohno.tex`. The intro to the modular dg-shifted Yangian subsection was still saying the modular $R$-matrix \emph{is} the universal MC element and that its shadow projections directly encode the classical $r$-matrix and quantum corrections. But later in the same section the text explicitly says populating the modular dg Lie algebra with such an MC element is the content of `conj:modular-yang-baxter`. I rewrote the intro onto the honest conditional surface: the modular $R$-matrix and its shadow reading are now presented as the intended/conjectural package, while the section’s actual computations are described as benchmark and conditional projections. | **FIXED** |
| F246 | 2026-04-01 13:19:24 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found the same status contradiction immediately reintroduced in the following remark of `yangians_drinfeld_kohno.tex`. Even after the subsection intro was demoted, `rem:modular-yangian-chriss-ginzburg` still said the modular algebra structure \emph{is} a Maurer--Cartan element, that $R_T^{\\mathrm{mod}}$ \emph{is} the restricted universal MC element, and that the total kernel \emph{is then} an MC element for the full quantum $L_\\infty$-structure. But the section’s own claim surface keeps existence of that modular kernel conjectural in `conj:modular-yang-baxter`. I rewrote the remark so all of those identifications are explicitly conditional on that conjectural existence surface. | **FIXED** |
| F247 | 2026-04-01 13:22:18 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a residual status leak in the title of `rem:modular-yangian-chriss-ginzburg` inside `yangians_drinfeld_kohno.tex`. After the body had already been demoted to the conjectural `conj:modular-yang-baxter` surface, the remark title was still stating the equality `$R_T^{\\mathrm{mod}} = \\Theta_\\cA|_{\\Eone}$` as if it were theorematic. I narrowed the title itself to “Modular $R$-matrix as conjectural MC restriction” so the heading no longer contradicts the repaired body. | **FIXED** |
| F248 | 2026-04-01 13:25:54 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a scalar-lane import bug in `yangians_drinfeld_kohno.tex`. Inside the shadow-decomposition bullets of `rem:modular-yangian-chriss-ginzburg`, the arity-2 line was still stating the full all-genera scalar formula `F_g = \kappa \cdot \lambda_g` and the $\hat A$-genus package as if they were automatic on the Yangian surface. But the repaired core theorem chain only gives genus-1 scalar trace unconditionally; the full $\Gamma_{\cA}=\kappa(\cA)\Lambda$ / all-genera scalar package is proved only on the uniform-weight scalar lane and remains conditional in general multi-weight settings. I narrowed the bullet so it now records genus-1 scalar control unconditionally and fences the full $\hat A$ package to the proved uniform-weight scalar lane. | **FIXED** |
| F249 | 2026-04-01 13:28:25 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a remaining object-status leak at the start of the “modular Yang--Baxter package” subsection in `yangians_drinfeld_kohno.tex`. The opener still said the modular dg-shifted Yangian already encodes stable-curve gluing and spectral line-operator data, and the ensuing definition still introduced the modular total kernel as if it were an already-present object. But the repaired local surface treats `\\Ymod_T` as the ambient pronilpotent dg Lie algebra, while the actual MC kernel data remain conjectural via `conj:modular-yang-baxter`. I rewrote the opener so `\\Ymod_T` is the ambient package intended to organize those data, and I narrowed the modular-total-kernel definition to a formal kernel whose Maurer--Cartan status is only asserted conditionally. | **FIXED** |
| F250 | 2026-04-01 13:32:15 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found an object-conflation bug in `yangians_drinfeld_kohno.tex`. The text was still saying the usual dg-shifted Yangian is literally recovered as the genus-zero truncation `\\Ydg_{\\cA}=\\Ymod_T|_{g=0}`, and later that the genus-zero truncation of the modular package is the dg-shifted Yangian itself. But on the manuscript’s own surfaces, `\\Ymod_T` is an ambient pronilpotent dg Lie algebra, while `\\Ydg_{\\cA}` is a dg algebra with translation operator, Maurer--Cartan kernel, and twisted coproduct. I rewrote both sites so they now say only that the genus-zero sector/kernel data of the modular package recover the dg-shifted Yangian data, without identifying the two objects outright. | **FIXED** |
| F251 | 2026-04-01 13:35:20 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a scope leak in the “What the modular envelope does not see” remark of `yangians_drinfeld_kohno.tex`. Right after a conjecture that treats the spectral kernel and $A_\infty$ Yang--Baxter package as extra input, the summary was still saying `\\Ymod_T` “captures exactly this additional structure” and that its full modular completion is already the algebraic home for all-genera defect OPE coefficients and holographic line-operator amplitudes. I narrowed that remark so `\\Ymod_T` is only the ambient algebraic home, its genus-zero sector recovers the dg-shifted Yangian data, and the full all-genera interpretation appears only once the conjectural kernel data of `conj:modular-yang-baxter` are supplied. | **FIXED** |
| F252 | 2026-04-01 13:43:40 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a direct theorem-flow contradiction in `yangians_drinfeld_kohno.tex`. Inside the conjectured modular dg-shifted Yangian definition, the text still said an algebra structure on the modular line-operator package \emph{is} a Maurer--Cartan element in `\\Ymod_T`, and the chapter opener was still naming `$R_T^{\\mathrm{mod}}$` itself as part of the standing framework for DK-4/5. But the repaired local claim surface keeps the modular kernel conjectural via `conj:modular-yang-baxter`; only the ambient pronilpotent dg Lie algebra is unconditional. I rewrote the definition and nearby wrappers so the MC-element reading, stable-graph Yang--Baxter equation, and genus-zero reduction all live explicitly on that conjectural surface, while the opener now refers only to the later conjectural modular package built around the intended kernel. | **FIXED** |
| F253 | 2026-04-01 13:50:18 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a residual scalar-lane overclaim in the prelude to the modular dg-shifted Yangian definition of `yangians_drinfeld_kohno.tex`. Even after the later shadow-decomposition bullets were repaired, the summary line still said the scalar level `\\kappa` governs the genus tower via the `\\hat A`-genus. But the repaired higher-genus theorem chain only gives genus-1 scalar trace unconditionally; the full all-genera `\\hat A` package is proved only on the uniform-weight scalar lane. I narrowed that summary so it now records genus-1 scalar control unconditionally and fences the full `\\hat A`-genus package to the proved uniform-weight scalar lane. | **FIXED** |
| F254 | 2026-04-01 13:57:04 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a residual comparison-surface leak in `rem:modular-yangian-chriss-ginzburg` of `yangians_drinfeld_kohno.tex`. After the title and surrounding prose had already been demoted to the conjectural `conj:modular-yang-baxter` surface, the body was still stating that the intended modular kernel `$R_T^{\\mathrm{mod}}(z;\\hbar)$` \emph{is} the ordered `\\Eone`-factorization restriction of the universal MC element `\\Theta_\\cA`. But the local conjecture only asserts existence of a Maurer--Cartan element in `\\Ymod_T`; the sharper identification with the ordered `\\Eone` restriction is additional intended comparison data, not a proved or formally implied theorem on this surface. I narrowed that sentence so it now says the kernel is \emph{intended to be} that ordered `\\Eone` restriction. | **FIXED** |
| F255 | 2026-04-01 14:03:44 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a residual ambient-vs-populated-package overclaim in the prelude to the modular dg-shifted Yangian subsection of `yangians_drinfeld_kohno.tex`. Right before the kernel was fenced back to the conjectural `conj:modular-yang-baxter` surface, the prose still said the modular extension already “remembers both the line-operator OPE and the stable-curve gluing data in a single algebraic envelope.” But the repaired local surface only constructs the ambient pronilpotent dg Lie framework unconditionally; the full populated kernel package remains conjectural. I narrowed that prelude so the modular extension is now merely intended to organize those data. | **FIXED** |
| F256 | 2026-04-01 14:11:31 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a theorem-surface mismatch in the line-operator completion block of `yangians_drinfeld_kohno.tex`. The subsection was still defining “the line-operator category of the theory” as `\\cA_T^!\\text{-}\\mathrm{mod}` and then feeding that object directly into the open boundary/line bridge package, even though the later B3 surface treats realization of the actual line-side category as part of the conjectural bridge data. I narrowed the local surface to an \emph{algebraic line-operator model} `\\mathcal C_T := \\cA_T^!\\text{-}\\mathrm{mod}`, adjusted the “beyond the modular envelope” item to its spectral tensor structure, and rewrote the boundary/line bridge data so `F_b` now compares this algebraic model to boundary modules rather than presupposing an already-realized line-operator category. | **FIXED** |
| F257 | 2026-04-01 14:18:17 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a local overclaim in the `A_\\infty` Yang--Baxter consistency conjecture of `yangians_drinfeld_kohno.tex`. After the line-side object had been narrowed to an algebraic line-operator model, the last sentence of the conjecture was still saying that the condition `D^2=0` upgrades modular gluing to a \emph{full spectral defect algebra} incorporating line-operator OPE. On the repaired local surface, what is actually in hand is only the corresponding algebraic spectral defect model; realization of the full line-side package remains part of the later boundary/line bridge data. I narrowed that concluding sentence accordingly. | **FIXED** |
| F258 | 2026-04-01 14:25:46 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a residual tensor-structure overclaim in the algebraic line-operator model block of `yangians_drinfeld_kohno.tex`. After the subsection had already been narrowed from an actual line-operator category to an algebraic model, the definition still said the spectral kernel `r(z)` twists the coproduct on `\\cA_T^!`, and the later “beyond the modular envelope” bullet still listed the model together with its spectral tensor structure as if that extra structure were automatic. But on the repaired local surface, that spectral tensor structure exists only when a compatible spectral kernel is separately supplied. I narrowed both sites so the twisted coproduct and spectral tensor structure are now explicitly conditional on such a kernel. | **FIXED** |
| F259 | 2026-04-01 14:32:24 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a wrapper-level overclaim in the older boundary/line path remark of `yangians_drinfeld_kohno.tex`. After the line-side object had been narrowed to an algebraic model with only conditional spectral enhancement, the remark still said that if `F_b` is an equivalence then the line category’s OPE/fusion data are already present on `V_b`-mod, and that the boundary-module picture supplies the line-side data outright. On the repaired local surface, `F_b` only identifies the algebraic line-side model with boundary modules; the additional spectral/OPE package still lives on the separate boundary spectral comparison side. I narrowed that remark so it now speaks only of line-side algebraic data, with the spectral identification remaining a separate step. | **FIXED** |
| F260 | 2026-04-01 14:38:11 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a residual terminology drift after the line-side demotions in `yangians_drinfeld_kohno.tex`. Several older wrappers still said “line category” outright: the boundary/line path remark said `F_b` identifies “the line category” with boundary modules and works “through the line-operator category,” and the free/Heisenberg benchmark still said “the line category is free modules.” But on the repaired local surface these are only statements about the algebraic line-operator model, not yet the fully realized line-side package. I aligned those remaining phrases to the narrower algebraic-model terminology. | **FIXED** |
| F261 | 2026-04-01 14:43:27 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a theorem-flow overclaim in the Bridge-Theorem programme of `yangians_drinfeld_kohno.tex`. Item~B3 was still saying the line side had OPE/fusion “controlled by a dg-shifted Yangian,” and item~B4 still said the boundary/line side “yields” the boundary spectral $R$-matrix data. But after the recent demotions, the local surface only supports an algebraic line-side model plus additional compatible spectral/OPE data inside the open boundary/line package. I narrowed B3 and B4 so they now speak of a compatible line-side algebraic/spectral package and an open identification of the resulting boundary spectral data with Latyntsev’s factorization quantum-group package. | **FIXED** |
| F262 | 2026-04-01 14:48:54 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a residual notation/surface mismatch after the line-side demotions in `yangians_drinfeld_kohno.tex`. The open B3 programme and the older boundary/line path remark were still writing the line-to-boundary functor as `$F_b \colon \mathcal C \to V_b\text{-mod}$`, even though the local surface had already fixed the line side to the algebraic model `\\mathcal C_T := \\cA_T^!\\text{-}\\mathrm{mod}`. I aligned both sites so the open bridge package no longer silently slides back to an unspecified or stronger line-side object. | **FIXED** |
| F263 | 2026-04-01 14:56:25 SAST | **SERIOUS** | Beilinson | The next Yangian modular pass found a local title/status mismatch in `yangians_drinfeld_kohno.tex`. After the body of the subsection had been narrowed to an algebraic line-operator model with only conditional spectral enhancement, the subsection header still advertised “The line-operator category completion,” which overpromised a completed realized category. I narrowed the title itself to “An algebraic line-operator model” so the heading no longer outruns the repaired body. | **FIXED** |
| F264 | 2026-04-01 15:01:42 SAST | **SERIOUS** | Beilinson | The next global consistency pass found a cross-manuscript drift in `frontier_modular_holography_platonic.tex`. Even after the Yangian chapter had been narrowed to an algebraic line-side model with extra compatible spectral data, the frontier holography wrapper was still exporting `\\mathcal C \\simeq \\cA^!\\text{-}\\mathsf{mod}` as the actual meromorphic line-operator category, both in the five-piece HT package list and in the “mature statement” summary theorem. I narrowed both sites so they now describe `\\cA^!\\text{-}\\mathsf{mod}` as the algebraic meromorphic line-side model, with the meromorphic tensor/OPE package supplied only on the compatible dg-shifted Yangian surface. | **FIXED** |
| F265 | 2026-04-01 15:09:34 SAST | **SERIOUS** | Beilinson | The next global consistency pass found mirror drift in `genus_complete.tex` and `concordance.tex`. Even after the Yangian chapter and frontier holography wrapper had been narrowed to an algebraic line-side model with extra compatible spectral data, the genus-complete and constitutional summaries were still speaking as if the package already contained an actual line-operator category and literal line operators. I narrowed those mirrors so they now refer only to the algebraic line-side model (or a chosen line-side model when objects are inserted), matching the repaired Yangian claim surface. | **FIXED** |
| F266 | 2026-04-01 15:29:18 SAST | **SERIOUS** | Beilinson | The next global consistency pass found a theorem-surface mismatch across `twisted_holography_quantum_gravity.tex` and the Volume I/II mirrors of `thqg_gravitational_yangian.tex`. Those files were still sliding between three different surfaces as if they were identical: the unconditional line-category theorem `\\mathcal C \\simeq \\cA^!\\text{-}\\mathsf{mod}`, the affine/proved dg-shifted-Yangian lane, and the general perturbative gravitational-Yangian package that concordance still marks as frontier material. I narrowed G5 and the gravitational-Yangian definitions so the collision residue/CYBE remains the proved universal part, the dg-shifted-Yangian realization is explicitly affine/proved-lane data, and the meromorphic tensor/braiding package is conditional on supplying compatible spectral/Yangian input. I also removed the literal formula-level identification `\\cC_\\cA = \\mathrm{Mod}_{\\Ydg_\\cA}` from the general definition, since that object does not exist on the full generic surface. | **FIXED** |
| F267 | 2026-04-01 15:48:52 SAST | **SERIOUS** | Beilinson | The next widened pass found a theorem-chain regression in the THQG MC3 wrapper of `thqg_gravitational_yangian.tex` and its Volume II mirror. The manuscript was still exporting the old solved-MC3 story: full thick generation of the line-side category in type~A, full-category DK-5 accessibility, all-types resolution, and even a `B_2` theorem asserting the full post-core package from the conditional propagation mechanism. But the repaired Yangian source theorem now proves only the evaluation-generated-core DK comparison plus the shifted-prefundamental/pro-Weyl packets in type~A, with the compact-completion extension left explicitly as `conj:dk-compacts-completion`, and the all-types post-core mechanism only conditional via `prop:mc3-automatic-generalization`. I rewrote the THQG theorem/corollary to the honest reduction surface, narrowed the all-types remarks and summary tables, and demoted the fake `B_2` theorem to an evaluation-core benchmark remark. | **FIXED** |
| F268 | 2026-04-01 16:10:00 SAST | **SERIOUS** | Beilinson | The next widened pass found a second Volume~II MC3 export cluster in `thqg_line_operators_extensions.tex`, `twisted_holography_quantum_gravity.tex`, `spectral-braiding-frontier.tex`, `thqg_spectral_braiding_extensions.tex`, `conclusion.tex`, and the Volume~II `concordance.tex`. Those files were still presenting the repaired Yangian story as if full type-$A$ MC3 and even DK-5 were already theorematic, and as if the all-types result upgraded directly to a full line-side categorical equivalence. But the current source theorem chain only proves categorical Clebsch--Gordan plus the DK comparison on the evaluation-generated core for all simple types; type~$A$ then reduces the post-core problem to the compact-completion packet, while the corresponding post-core mechanism in other types remains conditional. I narrowed the old theorem wrappers, DK ladder recap, spectral-braiding summary, and conclusion/concordance exports to that exact evaluation-core/post-core split. | **FIXED** |
| F269 | 2026-04-01 16:19:00 SAST | **SERIOUS** | Beilinson | The next widened pass found two residual summary-level MC3 compressions in `thqg_line_operators_extensions.tex` and `spectral-braiding.tex`. The old MC hierarchy bullet was still saying MC3 is simply “proved for all simple types” and that the full four-package DK programme is proved in type~$A$, while the spectral-braiding DK remark was still speaking as if the whole DK ladder beyond DK-0 were already covered by that all-types result. But after the Yangian repairs the honest surface is narrower: all simple types have categorical Clebsch--Gordan plus evaluation-generated-core DK, type~$A$ reduces the post-core obstruction to the compact-completion packet, and the later extension beyond the evaluation locus remains open. I narrowed both wrapper surfaces to that exact split. | **FIXED** |
| F270 | 2026-04-01 16:31:00 SAST | **SERIOUS** | Beilinson | The next widened pass found a cross-volume summary drift in the THQG concordance supplements. Both `thqg_concordance_supplement.tex` files were still advertising G5 as if the generic statement were simply “`\\Ydg(\\cA^!)` governs lines, MC3 (all types).” But the repaired theorem surface separates the unconditional line-category theorem `\\cC\\simeq \\cA^!\\text{-}\\mathsf{mod}` from the affine/proved dg-shifted-Yangian lane, and the all-types MC3 result only reaches the evaluation-generated core. I narrowed both summary rows to that exact split. | **FIXED** |
| F271 | 2026-04-01 16:46:00 SAST | **SERIOUS** | Beilinson | The next widened pass found a theorem-level overclaim cluster in the older Volume~II line-operators chapter `thqg_line_operators_extensions.tex`. The chapter was still asserting that evaluation modules are dense in all `\\Ydg(\\cA^!)`-modules, that `\\Ydg(\\cA^!)` computes every fusion process in the full line-operator category, and that the resulting gravitational Yangian universally controls gravitational line fusion. But the repaired global theorem surface only supports the dg-shifted-Yangian package on the affine/proved evaluation-module lane, with the unconditional generic line theorem remaining `\\cC\\simeq \\cA^!\\text{-}\\mathsf{mod}`. I narrowed the section opener, the strict-model remark, the evaluation-module proposition, the RTT theorem, the gravitational-Yangian construction, and the downstream platonic-package export to that affine/evaluation-core surface. | **FIXED** |
| F272 | 2026-04-01 12:30:50 SAST | **SERIOUS** | Beilinson | The next widened pass found a direct theorem-status contradiction in the THQG gravitational-Yangian chapters `thqg_gravitational_yangian.tex` (both volumes). After the Yangian source had already demoted the modular kernel to the conjectural `conj:modular-yang-baxter` surface, the THQG chapter was still asserting as `ProvedHere` that the pronilpotent modular completion carried a pro-MC element `R^{\\mathrm{mod}}_\\cA`, that the stable-graph Yang--Baxter equation was its proved MC equation, that the genus-$1$ correction formula followed formally, and that Virasoro therefore encoded the full nonlinear gravitational-scattering package. I repaired the source theorem chain instead of just shaving summaries: the proved statement is now the formal genus-refined binary shadow series `R^{\\mathrm{bin}}_\\cA`, the modular completion is kept as an ambient pronilpotent dg Lie algebra, the sgYBE/kernel package is fenced as conjectural, the genus-$1$ correction is heuristic, the RTT statement is sourced from the genus-zero collision/CYBE lane, the G5/Virasoro summaries were narrowed accordingly in both Volume~I and Volume~II, and the Volume~II mirror was rewired back to the prefixed Volume~I labels so it no longer introduces fresh undefined references for the demoted THQG modular package. | **FIXED** |
| F273 | 2026-04-01 17:04:00 SAST | **SERIOUS** | Beilinson | The next widened pass found the same modular-Yangian status leak surviving one layer down in `thqg_spectral_braiding_extensions.tex` and one layer up in `thqg_preface_supplement.tex`. Those files were still treating the higher-genus modular kernel `R_T^{\\mathrm{mod}}` as an actual proved Maurer--Cartan element, treating the stable-graph Yang--Baxter equation as an existence theorem rather than a formal criterion, and treating the higher-genus coefficients as actual braiding operators on the line side. But after the Yangian source repairs and F272, the honest local surface only has the ambient pronilpotent dg Lie algebra, a formal coefficientwise MC/YBE criterion for a \emph{candidate} higher-genus binary series, a conditional convergence criterion once such coefficients are supplied, and the proved genus-zero Yangian line-action. I narrowed the Volume~II section intro, the candidate-$R$ definition, the MC/YBE theorem, the convergence proposition, and the line-operator theorem to that exact split, and I aligned the Volume~I preface supplement so the Virasoro dg-shifted-Yangian and modular-$R$ language is explicitly target/conjectural rather than theorematic. | **FIXED** |
| F274 | 2026-04-01 17:18:00 SAST | **SERIOUS** | Beilinson | The next pass found the same modular-kernel inflation still alive in the top-level overview layer: `introduction.tex`, `thqg_introduction_supplement_body.tex`, and `yangians_foundations.tex` were still narrating the modular dg-shifted Yangian as if the higher-genus modular kernel were already constructed. The introduction still said the modular $R$-matrix is a Maurer--Cartan element in the completed Yangian algebra, the THQG supplement still treated the all-genera $R^{\\mathrm{mod}}$ and its Yang--Baxter equation as theorematic consequences of the MC equation, and the Yangian-foundations construction still listed a literal pro-MC structure as part of the shadow archetype data. But after the Yangian source repairs and the THQG narrowing passes, the honest surface is weaker: the modular dg-shifted Yangian is the ambient pronilpotent completion, while the higher-genus modular kernel remains conjectural. I rewrote all three overview surfaces to that exact ambient-vs-conjectural split. | **FIXED** |
| F275 | 2026-04-01 17:33:00 SAST | **SERIOUS** | Beilinson | The next pass found a deeper theorem-level overclaim in `e1_modular_koszul.tex`. The live `\\ClaimStatusProvedHere` `E_1` modular package was still presenting an all-genera modular `$R$`-matrix as if it were a realized higher-genus Yangian kernel: the genus-1 construction wrote down explicit `r_1` corrections, Theorem~`\\mathrm{A}^{E_1}` said Koszul exchange acts by that modular `$R$`-matrix, and Theorem~`\\mathrm{D}^{E_1}` promoted the full series to a proved modular Yang--Baxter characteristic package. But after the Yangian and THQG repairs, the honest internal surface is narrower: what is canonically produced here is the formal ordered arity-2 projection of the ordered `E_1` MC element, not a fully realized line-side modular kernel. I rewrote the construction as the formal ordered arity-2 shadow series, narrowed Theorem~`\\mathrm{A}^{E_1}` to encode exchange only at that bar-level shadow surface, narrowed Theorem~`\\mathrm{D}^{E_1}` to the arity-2 projection of the ordered MC equation, aligned the concordance and main-results guide, and regenerated metadata so the theorem registry no longer advertises the old modular-$R$ title. | **FIXED** |
| F276 | 2026-04-01 17:52:00 SAST | **SERIOUS** | Beilinson | The next pass found the same modular-$R$ inflation surviving in the Volume~II ordered-associative mirrors. `ordered_associative_chiral_kd_frontier.tex` and `ordered_associative_chiral_kd.tex` were still saying the ordered $E_1$ modular Maurer--Cartan class has arity-$2$ component equal to a realized modular `$R$`-matrix, the local shadow-tower wrapper was still jumping from the genus-$1$ sector to a KZ associator and the full DK ladder, and the imported celestial-holography problem statements were still reading the shared equation label as a theorematic modular `$R$`-matrix. But after F275 the honest internal surface is only the formal ordered arity-$2$ shadow series on the ordered bar side. I narrowed both ordered-associative theorem/construction wrappers to that exact bar-level surface, fenced affine/free elliptic formulas back to benchmark status, removed the genus-$1$ associator / full-DK export from the local shadow-tower wrapper, and aligned the celestial imports so they now refer only to the formal ordered arity-$2$ shadow series. | **FIXED** |
| F277 | 2026-04-01 18:08:00 SAST | **SERIOUS** | Beilinson | The next pass found a clean genus-vs-arity conflation in the Volume~I overview layer. `introduction.tex` was still saying the ordered $E_1$ Maurer--Cartan equation reduces “at genus~$1$” to the KZ associator. But the proved source theorem chain in `e1_modular_koszul.tex` identifies the KZ associator on the affine benchmark as the arity-$3$ ordered shadow, while the higher-genus story now lives on the separate formal ordered arity-$2$ shadow-series surface. I narrowed that introduction sentence so it now distinguishes the genus-$0$, arity-$2$ CYBE surface, the arity-$3$ affine KZ-associator surface, and the higher-genus arity-$2$ formal shadow-series surface. | **FIXED** |
| F279 | 2026-04-01 18:34:00 SAST | **SERIOUS** | Beilinson | The next pass found a theorem-surface mismatch in the Yangian DK ladder wrappers. After DK-3 had been repaired to the affine-benchmark arity-$3$ shadow/KZ-associator surface, `yangians_drinfeld_kohno.tex` was still using `cor:dk23-all-types` as if it literally proved DK-3 for all simple types: the opening chapter summary, triage bullet, status remark, route-comparison remark, interaction remark, and even the corollary title itself were still advertising an all-types “DK-2/3” theorem. But the actual corollary statement is only the factorization equivalence on the evaluation-generated core. I narrowed that whole local wrapper cluster to the honest surface: a generated-core DK comparison surface, traditionally packaged as DK-2/3, with the literal affine-benchmark DK-3/KZ-associator lane kept separate. Metadata was regenerated so the theorem registry no longer exports the stronger old title. | **FIXED** |
| F280 | 2026-04-01 18:46:00 SAST | **SERIOUS** | Beilinson | The next pass found a constitution-level export bug after the Yangian DK ladder was narrowed. `editorial_constitution.tex` and `concordance.tex` were still summarizing the all-types generated-core corollary as if its domain were `\mathcal{O}_{\mathrm{poly}}`, even though that domain belongs only to the separate type-$A$ thick-generation lane. I narrowed both rows so they now describe the honest surface: the generated-core DK comparison package, traditionally packaged as DK-2/3, is proved on the evaluation-generated core for all types, while `\mathcal{O}_{\mathrm{poly}}` is kept on the conditional/type-$A$ extension lane. | **FIXED** |
| F281 | 2026-04-01 19:02:00 SAST | **SERIOUS** | Beilinson | The next pass found a residual constitution-level wording lag after the Yangian DK corollary was retitled. `editorial_constitution.tex` and the matching `concordance.tex` discussion were still using the old literal phrases “proved DK-2/3 equivalence” and “DK-2/3 is resolved” for the all-types corollary, even though the source theorem surface had already been narrowed to the generated-core DK comparison package traditionally packaged that way. I aligned those local constitutional summaries to the honest surface and kept the separate type-$A$ `\mathcal{O}_{\mathrm{poly}}` thick-generation lane explicit. | **FIXED** |
| F282 | 2026-04-01 13:21:35 SAST | **SERIOUS** | Beilinson | The next pass found a local theorem-chain overread in `w_algebras_deep.tex`. The DS/Koszul-datum wrapper was still pairing its theorem with `cor:dk23-all-types` as if the all-types Yangian generated-core DK comparison were itself the Yangian datum-level counterpart of DS reduction. But after the Yangian repairs, `cor:dk23-all-types` is only the later sectorwise factorization comparison on the evaluation-generated core; the actual shared datum-level Yangian surface is the RTT/bar-cobar package recorded in `rem:rtt-is-the-duality-datum` and `cor:completed-bar-cobar-yangian`. I narrowed the remark to cite exactly that datum surface and explicitly fenced `cor:dk23-all-types` as a downstream factorization consequence rather than part of the datum-level comparison. | **FIXED** |
| F283 | 2026-04-01 13:25:45 SAST | **SERIOUS** | Beilinson | The next pass found a direct theorem-surface contradiction in the Heisenberg frame overview. The five-theorem summary in `heisenberg_frame.tex` was still advertising the DK target as if the Yangian factorization homology already recovered `U_q(\mathfrak{sl}_N)` via a braided monoidal equivalence of finite-dimensional factorization categories, while the paragraph immediately below already gave the repaired narrower status: chain-level derived DK is proved, the factorization statement is proved on the evaluation locus, and the extension beyond that remains conjectural. I narrowed the headline bullet to that exact proved surface so the frame no longer exports a full finite-dimensional factorization equivalence that the manuscript itself still marks as open. | **FIXED** |
| F284 | 2026-04-01 13:30:24 SAST | **SERIOUS** | Beilinson | The next pass found a proof-level status leak in `yangians_computations.tex`. Step~1 of `thm:shifted-prefundamental-generation` was still citing the conditional finite-dimensional extension criterion `thm:dk-fd-typeA` together with the all-types generated-core comparison `cor:dk23-all-types` as if they formed a proved type-$A$ DK-2/3 package establishing compact generation of `\operatorname{Rep}_{\mathrm{fd}}(Y(\mathfrak{sl}_N))`. But that compact-generation claim is already supplied by the independent proved finite-dimensional thick-generation theorem `prop:dk2-thick-generation-typeA`; importing the conditional DK theorem there was both unnecessary and status-inflating. I rewrote Step~1 to use only the actual proved thick-generation input and removed the fake dependence on the conditional DK theorem. | **FIXED** |
| F285 | 2026-04-01 13:33:22 SAST | **SERIOUS** | Beilinson | The next pass found a status-boundary leak in the type-$A$ MC3 summary of `yangians_computations.tex`. The remark “Type-A status of DK on compacts” was still listing the conditional theorem `thm:dk-fd-typeA` as if it were the proved evaluation-core DK equivalence inside the resolved type-$A$ packet. But after the later repairs, the actual proved theorem on that surface is `thm:eval-core-identification`, while `thm:dk-fd-typeA` remains only a conditional finite-dimensional extension criterion beyond the evaluation locus. I rewrote the summary to cite the proved evaluation-core theorem rather than the conditional extension theorem, so the local MC3 status packet no longer blurs conditional and proved inputs. | **FIXED** |
| F286 | 2026-04-01 13:36:58 SAST | **SERIOUS** | Beilinson | The next pass found an object/surface conflation in the corrected MC3 frontier remark of `yangians_computations.tex`. The branch-analysis bullet for Conjecture `full-derived-dk` was still saying the factorization DK conjecture would be “unconditionally proved” by the module-side equation `eq:dk-eval-core`, even though that equation only gives the type-$A$ module-side evaluation-core equivalence; the factorization comparison on the evaluation-generated core is a separate result coming from `thm:h-level-factorization-kd` and `cor:dk23-all-types`. I rewrote the bullet to separate those inputs explicitly, so the local DK ladder no longer pretends that a module-side core equivalence alone settles the factorization conjecture on that core surface. | **FIXED** |
| F287 | 2026-04-01 13:39:02 SAST | **SERIOUS** | Beilinson | The next pass found a proof-level status leak inside the proved theorem `thm:eval-core-identification` in `yangians_computations.tex`. Its DK-equivalence paragraph was still finishing the proof by referring back to the conditional theorem `thm:dk-fd-typeA` (“the same thick-closure arguments”), even though the needed ingredients on the evaluation-generated core are already proved independently: `thm:derived-dk-yangian` gives the equivalence on the evaluation-generated subcategory, `eq:eval-core` identifies its thick closure with the full finite-dimensional category, and `lem:monoidal-thick-extension` promotes monoidality across that thick closure. I rewrote the proof to cite only those proved inputs, so the theorem no longer leans on a conditional extension theorem at any point. | **FIXED** |
| F288 | 2026-04-01 13:42:17 SAST | **SERIOUS** | Beilinson | The next pass found a local status-label mismatch in the Yangian route-comparison remark of `yangians_drinfeld_kohno.tex`. The item labelled “Thick generation” was still citing the conditional theorem `thm:dk-fd-typeA` even though the thick-generation content on that surface is actually the proved proposition `prop:dk2-thick-generation-typeA`; the theorem only records the later conditional promotion criterion once an ambient extension/comparison package is supplied. I rewrote the item so it now credits the proved proposition for thick generation and names `thm:dk-fd-typeA` only for the subsequent conditional propagation step. | **FIXED** |
| F289 | 2026-04-01 13:46:00 SAST | **SERIOUS** | Beilinson | The next pass found a wrapper-level theorem-flow overclaim in the “Type-$A$ resolution” summary of `yangians_computations.tex`. The local theorem `thm:mc3-type-a-resolution` already isolates the compact-completion packet correctly, but the upstream prose was still narrating step~(4) as if spectral-sequence degeneration plus Francis--Gaitsgory completion had already extended the bar-cobar equivalence from the compact core to the completed category. That contradicted both the theorem statement and the nearby Francis--Gaitsgory remark, which explicitly say one still needs the compact-core extension and the comparison with the desired completed category. I narrowed the summary step so it now states the honest reduction surface: these inputs isolate the final compact/completed comparison problem rather than solving it. | **FIXED** |
| F290 | 2026-04-01 14:01:00 SAST | **SERIOUS** | Beilinson | The next pass found a summary-layer export cluster around the repaired MC3 type-$A$ theorem. `guide_to_main_results.tex`, `preface.tex`, and `editorial_constitution.tex` were still speaking as if Francis--Gaitsgory completion and “DK on compacts” were already proved theorematic steps closing thick generation or extending the DK functor to ind-completions. But after the Yangian source repairs, the honest surface is narrower: all types have categorical prefundamental CG plus evaluation-generated-core DK, type~$A$ additionally proves shifted-prefundamental generation and pro-Weyl recovery, and Francis--Gaitsgory completion provides only the ambient cocompletion formalism once the missing compact-core equivalence is known. I narrowed the guide entries, the two preface summaries, and the constitution row/list so they now export exactly that reduced surface. | **FIXED** |
| F291 | 2026-04-01 14:16:00 SAST | **SERIOUS** | Beilinson | The next pass found a theory-layer MC3 export cluster. `introduction.tex` was still saying MC3 is proved for all simple types in the strong sense that chromatic filtration, prefundamental Clebsch--Gordan closure, and Francis--Gaitsgory completion give thick generation of `\mathcal{O}_{\mathrm{poly}}`; `derived_langlands.tex` was still treating Kazhdan--Lusztig as if it were a theorematic export of full prefundamental thick generation on the Yangian side; `bar_cobar_adjunction_inversion.tex` still summarized MC3 as “thick generation in category~`\mathcal O`”; and `chiral_hochschild_koszul.tex` was still counting Francis--Gaitsgory completion and DK on compacts among the already-proved all-types one-slot steps. But after the Yangian repairs, the honest theory-level surface is narrower: all types have categorical prefundamental Clebsch--Gordan closure plus the DK comparison on the evaluation-generated core, while the post-CG completion packets remain separate and only the type-$A$ reduction theorem isolates them. I narrowed all four wrappers to that exact surface. | **FIXED** |
| F292 | 2026-04-01 14:28:00 SAST | **SERIOUS** | Beilinson | The next pass found two direct overreads of the repaired all-types MC3 corollary. `derived_langlands.tex` was still calling the result “the MC3 categorical core (thick generation of the factorization category)” and later referring to “the underlying MC3 thick generation,” while `lattice_foundations.tex` was still saying “the underlying MC3 thick generation is proved for all simple types.” But after the Yangian repairs, `cor:mc3-all-types` proves the narrower all-types surface: categorical Clebsch--Gordan closure plus the DK comparison on the evaluation-generated core, with the post-CG completion packets still separate. I narrowed those wrappers to that exact surface so the theory/example export layer no longer silently upgrades the generated-core theorem to global thick generation. | **FIXED** |
| F293 | 2026-04-01 14:39:00 SAST | **SERIOUS** | Beilinson | The next pass found a direct constitutional contradiction in the MC3 critical-path block of `editorial_constitution.tex`. The list of “five proof ingredients” was still presenting item~(v) as a proved `DK on compacts` step that “follows from~(i)--(iv) in type~$A$” and even cited `thm:shifted-prefundamental-generation`. But the repaired Yangian source theorem `thm:mc3-type-a-resolution` says exactly the opposite: after shifted-prefundamental generation and pro-Weyl recovery, the compact/completed DK extension is the remaining unresolved packet, recorded separately as `conj:dk-compacts-completion`. I rewrote item~(v) and the following open-problem sentence so the constitution now matches the repaired source: type~$A$ still has that final packet open, and arbitrary-type propagation is an additional layer beyond it. | **FIXED** |
| F294 | 2026-04-01 14:51:00 SAST | **SERIOUS** | Beilinson | The next pass found a smaller but real status-map drift in theory wrappers. `chiral_modules.tex` was still calling the repaired all-types Yangian surface “categorical thick generation results,” while `derived_langlands.tex` still summarized the outer MC3 problem only as “CG proved / post-CG open,” dropping the separately proved generated-core DK comparison. After the Yangian repairs, the honest common surface is: categorical Clebsch--Gordan closure plus the generated-core DK comparison are proved for all simple types, while post-CG completion remains separate. I rewrote those wrappers to export exactly that theorem map. | **FIXED** |
| F295 | 2026-04-01 15:03:00 SAST | **SERIOUS** | Beilinson | The next pass found the same status-map drift still surviving in the example-layer Yangian exports. `yangians_foundations.tex` was repeatedly citing `cor:mc3-all-types` as if it only established all-types Clebsch--Gordan closure, and `lattice_foundations.tex` was still summarizing the Yangian ladder as “MC3 CG decomposition proved for all simple types” with the generated-core DK comparison silently omitted. After the Yangian repairs, the honest exported surface is the combined one: categorical Clebsch--Gordan closure plus the evaluation-generated-core DK comparison are proved for all simple types, while the post-CG completion packets remain open. I rewrote those example-level wrappers to match that theorem map. | **FIXED** |
| F296 | 2026-04-01 15:16:00 SAST | **SERIOUS** | Beilinson | The next pass found a local theorem-flow overread in `free_fields.tex`. The partition-count remark was still taking the weight-multiplicity identity from Step~2 of `thm:shifted-prefundamental-generation` and then advertising it as a bridge to the whole proved type-$A$ MC3 thick-generation package. But that local input only controls the prefundamental sector appearing inside the shifted-prefundamental-generation theorem; it does not by itself prove the full compact/completed MC3 package. I narrowed the remark to that exact prefundamental-sector surface and fenced the stronger MC3 completion claim back out. | **FIXED** |
| F297 | 2026-04-01 15:29:00 SAST | **SERIOUS** | Beilinson | The next pass found a direct theorem-flow contradiction in the prefundamental-duality remark of `yangians_foundations.tex`. The remark correctly labeled the positive prefundamental as the *conjectural* Koszul dual of the negative prefundamental, but then immediately said the Step~2 Clebsch--Gordan stability theorem “forces” Koszul duality on `L^-` from the evaluation-level data. That was too strong: the proved input only shows compatibility evidence for any eventual prefundamental-duality statement; it does not derive the conjectural duality from the evaluation-module theorem. I narrowed the remark so the prefundamental-duality claim stays explicitly conjectural. | **FIXED** |
| F298 | 2026-04-01 15:41:00 SAST | **SERIOUS** | Beilinson | The next pass found a compressed status-map drift in two overview surfaces. The long-form introduction paragraph was already honest about MC3 as “CG plus evaluation-core DK, post-CG open,” but the adjacent table still reduced the row to “CG all types; post-CG open beyond $A$,” and `derived_langlands.tex` still had a sentence compressing the outer MC3/periodicity problem to “CG proved / post-CG open.” After the Yangian repairs, both should record the same narrower all-types surface: categorical Clebsch--Gordan closure plus the generated-core DK comparison are proved, while post-CG completion and periodicity remain separate. I tightened both compressed summaries to match that theorem map. | **FIXED** |
| F299 | 2026-04-01 16:02:00 SAST | **SERIOUS** | Beilinson | The next pass found a DK-5 status leak in the Yangian summary/export layer. `yangians_computations.tex`, `yangians_foundations.tex`, and `free_fields.tex` were still talking as if the all-types generated-core DK comparison made DK-5 “accessible for all simple types,” while `editorial_constitution.tex` still compressed the frontier to a packet merely “downstream of MC3.” But the repaired Yangian source still keeps DK-5 conjectural and explicitly downstream of the unresolved post-core MC3 completion packet. I narrowed all four surfaces so they now say exactly that; on the type-$A$ lane, the remaining packet is reduced to the compact-completion problem, but it is not already closed in all types. | **FIXED** |
| F300 | 2026-04-01 16:18:00 SAST | **SERIOUS** | Beilinson | The next pass found a theorem-flow mismatch in `yangians_foundations.tex`: the shared-seed reduction proposition was still saying the “remaining MC4 input” is the later DK-5 categorical packet. But the cited source corollary `cor:yangian-typea-degree2-plus-generators` says something strictly weaker and earlier in the ladder: after the local coefficient identity is fixed, the remaining MC4 input is compact generation plus transport on the completed fundamental evaluation packet. I narrowed the foundations wrapper to that exact MC4 surface so it no longer jumps ahead to DK-5. | **FIXED** |
| F301 | 2026-04-01 16:31:00 SAST | **SERIOUS** | Beilinson | The next pass found a formal-moduli scope compression in `yangians_drinfeld_kohno.tex`. The opener of the DK-4/5 formal-moduli subsection was still saying “DK-5 is the passage from Lie algebra modules to group representations,” which silently dropped two repairs already made elsewhere: DK-4/5 only begin after the post-core DK-3 extension/completion packet is supplied, and DK-5 itself still includes the extra comparison from the formal group to the spectral quantum-group side. I narrowed that paragraph to the exact repaired conjectural surface. | **FIXED** |
| F302 | 2026-04-01 16:43:00 SAST | **SERIOUS** | Beilinson | The next pass found a top-level status leak in `yangians_drinfeld_kohno.tex`. The opening three-layer status map correctly presented Layer~(C) as the least-certain downstream package, but it still listed the “full DK-4/5 bridge” there as if it were an already-formed object. After the ladder repairs, that layer should only name the remaining conjectural DK-4/5 bridge package together with any later module-category enhancement. I narrowed that export sentence so the chapter opener no longer outruns the repaired source. | **FIXED** |
| F303 | 2026-04-01 17:02:00 SAST | **CRITICAL** | Beilinson | The next pass found a genuine theorem-level object conflation in the new Li--bar / associated-variety lane. `thm:associated-variety-koszulness` in `chiral_koszul_pairs.tex` was jumping from the bar-side $d_1$ complex to ordinary Lichnerowicz--Poisson cohomology and then to $H^*(\widetilde{\mathbb O},\mathcal O)$ for symplectic resolutions, and the minimal-orbit / nilcone examples in `kac_moody.tex` plus the concordance wrapper were exporting the same false chain. I narrowed the proved surface to the honest one: the theorem now controls the reduced Li--bar $E_2$ page of the commutative bar complex with its Poisson-induced $d_1$ differential; it no longer claims an identification with ordinary Poisson cohomology or coherent cohomology of a resolution. The minimal-orbit corollary now explicitly assumes reduced Li--bar diagonal concentration, the degenerate-level proposition now says only that reduced geometry and the nilradical are distinct obstruction surfaces, and the Kac--Moody/concordance wrappers were aligned to that narrower bar-side theorem. | **FIXED** |
| F304 | 2026-04-01 17:24:00 SAST | **CRITICAL** | Beilinson | The next pass found a second-stage object conflation still sitting immediately upstream of F303 in `chiral_koszul_pairs.tex`. `prop:li-bar-poisson-differential` and the orbit clause of `thm:associated-variety-koszulness` were still collapsing the $E_0$ commutative bar complex, the $E_1$ commutative bar-cohomology page, and the $E_2 = H(E_1,d_1)$ page into one “bar complex with Poisson differential” object, and the proof still called $d_1$ the Lichnerowicz--Poisson differential. I rewrote that theorem chain so the stages are now explicit: $E_1$ is ordinary commutative bar cohomology, $d_1$ is the bracket-induced differential on that page, and $E_2$ is the homology of the resulting $(E_1,d_1)$ complex. I aligned the exported `\widehat{\mathfrak{sl}}_2` admissible example in `kac_moody.tex` to the same $E_1/d_1/E_2$ staging. | **FIXED** |
| F305 | 2026-04-01 17:42:00 SAST | **SERIOUS** | Beilinson | The next pass found compute/manuscript drift in the same new Li--bar lane. `compute/lib/li_bar_spectral_sequence.py` and `compute/tests/test_li_bar_spectral_sequence.py` were still exporting the reduced-geometry helper as literal “Poisson cohomology via symplectic resolutions,” with blanket concentration comments, while the repaired manuscript theorem surface now treats those geometric models only as external comparison data. The `\widehat{\mathfrak{sl}}_2` example in `kac_moody.tex` also still used the hybrid phrase “reduced Poisson bar complex.” I narrowed the compute helper/test pair so they now distinguish theorematic Li--bar data from external reduced-geometry comparison models, and I aligned the manuscript example to the cleaner “reduced commutative bar complex” wording. | **FIXED** |
| F306 | 2026-04-01 18:03:00 SAST | **CRITICAL** | Beilinson | The next pass found a real theorem-surface overclaim in the new admissible-level finiteness lane. In `kac_moody.tex`, `prop:bar-admissible` and `cor:bar-admissible-finiteness` were still treating $C_2$-cofiniteness as if it made the whole Li associated graded, whole bar complex, and whole bar cohomology finite-dimensional, with a global degeneration-page bound by $\dim A(L_k)$; the degenerate clause also jumped from bar-side higher cohomology to ordinary logarithmic extensions. That contradicted the surrounding status remarks, which explicitly keep bar-Ext vs ordinary-Ext as the unresolved gap. I narrowed the proved surface to the honest one: non-degenerate admissible levels give weightwise finite-page control in fixed conformal-weight sectors, sufficient for the Kac--Wakimoto character theorems, while degenerate admissible levels only provide reduced-level Poisson/nilradical obstruction data on the bar side. I propagated that narrowing to `derived_langlands.tex`, `chiral_modules.tex`, `koszul_pair_structure.tex`, the `preface.tex` overview, and the Kac--Moody chapter summary line so no wrapper still exports global finite-dimensional bar cohomology or ordinary logarithmic-Ext control. | **FIXED** |
| F307 | 2026-04-01 18:26:00 SAST | **SERIOUS** | Beilinson | The next pass found a cross-volume hypothesis leak in the admissible-level HS-sewing remarks of `thqg_perturbative_finiteness.tex` (Vol.~I and Vol.~II). Those remarks were asserting full HS-sewing for the simple quotient $L_k(\mathfrak{g})$ from the Kac--Wakimoto character formula alone. But the manuscript’s HS theorem requires both subexponential sector growth and polynomial OPE / Hilbert--Schmidt norm control, while the repaired admissible-level bar surface only proves weightwise finite-page control on the non-degenerate lane and reduced/nilradical obstruction surfaces on the degenerate lane. I narrowed both remarks so they now keep only the sector-growth half from Kac--Wakimoto, fence the current bar-side admissible analysis correctly, and explicitly leave simple-quotient HS-sewing as additional analytic input rather than a proved theorematic output. | **FIXED** |
| F308 | 2026-04-01 18:46:00 SAST | **SERIOUS** | Beilinson | The next pass found a theorem-flow overread in the admissible $\widehat{\mathfrak{sl}}_2$ fusion proposition. `prop:admissible-verlinde-bar` was still titled and phrased as if the bar complex itself computed the fusion rules, but the proof actually derives admissible characters and the modular $S$-matrix from the bar spectral sequence and then invokes the external semisimple Verlinde formalism on the rational admissible category. I narrowed the title and opening sentence to that exact surface, and I made Step~3 explicit that the fusion-ring calculation is the downstream Verlinde step, using rationality of the non-degenerate admissible category as the needed input. | **FIXED** |
| F309 | 2026-04-01 19:02:00 SAST | **MODERATE** | Beilinson | The next pass found a residual chapter-summary contradiction inside `kac_moody.tex`: the KL evidence remark still contained two back-to-back “Proved here” lines, one exporting the old overclaim “finite-dimensionality at admissible level” and the next exporting the repaired weightwise-finiteness statement. I removed the stale duplicate so the chapter’s local summary no longer contradicts the narrowed admissible-level theorem surface. | **FIXED** |
| F310 | 2026-04-01 19:16:00 SAST | **SERIOUS** | Beilinson | The next pass found a theorem-import bug in `higher_genus_modular_koszul.tex`. The proofs of the principal finite-type $\mathcal{W}$ modular-Koszul proposition and its unconditional corollary were still citing the admissible $\widehat{\mathfrak{sl}}_2$ Kac--Wakimoto character theorem `thm:kw-bar-spectral` as the genus-0 input for generic principal finite-type $\mathcal{W}$-algebras. That was the wrong claim surface. I rewired both proofs to the actual generic principal-$\mathcal{W}$ genus-0 theorem, `thm:w-algebra-koszul-main`, together with the bar-cobar spectral-sequence theorem, so the higher-genus `\mathcal{W}` package no longer depends on an unrelated admissible affine theorem. | **FIXED** |
| F311 | 2026-04-01 19:33:00 SAST | **MODERATE** | Beilinson | The next pass found a citation/scope mismatch in the free-field Fourier wrapper. `rem:weyl-nonabelian-fourier` in `free_fields.tex` was citing the admissible $\widehat{\mathfrak{sl}}_2$ theorem `thm:kw-bar-spectral` as if it were the general Weyl--Kac theorem. I narrowed that remark to the honest two-surface statement: generic-level collapse is the Weyl--Kac theorem `thm:weyl-kac-geometric`, while the finite admissible Weyl packet is the separate non-degenerate admissible theorem `thm:kw-bar-general-rank`. | **FIXED** |
| F312 | 2026-04-01 15:51:15 SAST | **SERIOUS** | Beilinson | The next pass found a local theorem-surface overclaim in `chiral_koszul_pairs.tex`. Proposition `prop:li-bar-poisson-differential` was still saying that a trivial Poisson bracket on the Li associated graded forces the entire Li--bar spectral sequence to degenerate at `E_1`. But the same proof explicitly allows higher filtration-lowering OPE terms to contribute to later differentials, so the theorematic input only proves `d_1 = 0` on that surface. I narrowed the proposition and proof so the trivial-Poisson lane now records only the vanishing of `d_1`, with any later-page degeneration fenced as separate higher-order input. | **FIXED** |
| F313 | 2026-04-01 15:51:15 SAST | **SERIOUS** | Beilinson | The next pass found a theorem-chain overclaim in the admissible Kac--Wakimoto lane of `kac_moody.tex`. `thm:kw-bar-spectral` was still stated for arbitrary non-degenerate admissible $\widehat{\mathfrak{sl}}_2$ levels even though its proof only explicitly derived the worked $k=-1/2$ case, and `thm:kw-bar-general-rank` was still tagged `ProvedHere` even though its proof only gave a bar-compatible sketch and then appealed back to the external Kac--Wakimoto/Arakawa package. I narrowed the worked theorem to the honest $k=-1/2$ surface, demoted the general-rank formula to `ProvedElsewhere`, and aligned the nearby admissible wrappers so they now distinguish internal bar derivation from external character input. | **FIXED** |
| F314 | 2026-04-01 15:51:15 SAST | **SERIOUS** | Beilinson | The next pass found a downstream export leak in the admissible Verlinde package of `kac_moody.tex`. `prop:admissible-verlinde-bar` was still saying the bar spectral sequence computes the admissible characters plural, and the proof was still deriving the non-vacuum character `\chi_1` from “the same spectral sequence,” even though the repaired theorem chain only proves the vacuum bar derivation at $k=-1/2$. I narrowed the proposition and proof so the bar surface now supplies only the vacuum character, while the second simple character is taken from the standard admissible Kac--Wakimoto package. | **FIXED** |
| F315 | 2026-04-01 15:51:15 SAST | **SERIOUS** | Beilinson | The next pass found a module-side export overclaim in `chiral_modules.tex`. `rem:admissible-bar-ss` was still stating a blanket theorem that the bar spectral sequence determines admissible $\widehat{\mathfrak{sl}}_2$ characters at general fractional level, even though the surrounding source only proves a single worked benchmark at $k=-1/2$ for one explicit simple module. I narrowed the remark to that exact benchmark surface and fenced the broader all-level character claim back out. | **FIXED** |
| F316 | 2026-04-01 20:14:00 SAST | **MODERATE** | Beilinson | The next pass found a local status-map contradiction in `chiral_modules.tex`. The regime summary `rem:non-semisimple-tensor` still claimed that semisimple rationality forces bar-spectral-sequence collapse at `E_2`, but the same chapter’s associated-variety remark only justifies finite-page degeneration from the hypothesis $X_V=\{0\}$. I narrowed the summary so regime~(i) now records finite-page degeneration in general, with $E_2$ collapse fenced to the Koszul loci where it is actually proved. | **FIXED** |
| F317 | 2026-04-01 20:26:00 SAST | **MODERATE** | Beilinson | The next pass found a cross-wrapper admissible-character overclaim in `koszul_pair_structure.tex` and `chiral_koszul_pairs.tex`. Those summaries were still saying the non-degenerate admissible bar spectral sequence “yields” the Kac--Wakimoto character formula on the full lane, even though the repaired source only proves a worked bar derivation for the vacuum module at $\widehat{\mathfrak{sl}}_2$, $k=-1/2$, with general-rank formulas imported externally. I narrowed both wrappers to that exact split surface. | **FIXED** |
| F318 | 2026-04-01 20:39:00 SAST | **MODERATE** | Beilinson | The next pass found a general-rank admissible overclaim in `chiral_modules.tex` and its nearby free-field mirror. `rem:bgg-special` was still stating that the admissible $E_2$ survivors form the truncated Weyl group and yield the Kac--Wakimoto character formula on the full lane, even though the repaired manuscript only proves that bar-spectral-sequence statement on the worked $\widehat{\mathfrak{sl}}_2$, $k=-1/2$ surface; in general rank the finite admissible Weyl packet belongs to the external Kac--Wakimoto theorem. I narrowed both wrappers to that exact split surface. | **FIXED** |
| F319 | 2026-04-01 20:51:00 SAST | **SERIOUS** | Beilinson | The next pass found a direct admissible-level status contradiction in `kac_moody.tex` and the Volume I preface. Both were still saying, in blanket form, that the admissible simple quotient has a non-semisimple module category, even though the theorematic admissible representation package in the same chapter says non-degenerate admissible levels are rational and only the degenerate lane is logarithmic. I narrowed both wrappers to distinguish those two admissible regimes explicitly. | **FIXED** |
| F320 | 2026-04-01 21:05:00 SAST | **MODERATE** | Beilinson | The next pass found a hypothesis-compression bug in the admissible examples. `kac_moody.tex` and `chiral_modules.tex` were still writing arithmetic admissibility data `(p,q)` as if they defined the non-degenerate admissible lane, and the admissible-weight count remark in `kac_moody.tex` was unnecessarily restricting a pure Kac--Wakimoto weight-count formula to the non-degenerate case. I rewrote those surfaces so non-degeneracy is attributed to Arakawa's rationality/associated-variety criterion, while the $(p,q)$ conditions are kept only as admissibility data. | **FIXED** |
| F321 | 2026-04-01 21:18:00 SAST | **MODERATE** | Beilinson | The next pass found a theorem-flow bug in the `BGG resolution for W-algebras` example of `koszul_pair_structure.tex`. The example was still saying exactness at admissible levels follows from rationality of $\mathcal{W}^k(\mathfrak{g})$, but the actual structural input on this surface is the external exactness of Drinfeld--Sokolov reduction on the relevant module category. I narrowed the example to an external/conditional DS-reduction statement and removed the false “rationality implies exactness” shortcut. | **FIXED** |
| F322 | 2026-04-01 21:31:00 SAST | **MODERATE** | Beilinson | The next pass found an unsupported global existence claim in the `Yangian modules` example of `koszul_pair_structure.tex`. It was still asserting without citation or local proof that every finite-dimensional Yangian module has a resolution by modules induced from the quantum affine side. I narrowed that example to an external Drinfeld--Kazhdan comparison expectation and fenced out the unproved global resolution theorem. | **FIXED** |
| F323 | 2026-04-01 21:44:00 SAST | **SERIOUS** | Beilinson | The next pass found a fabricated deformation-theory surface in `koszul_pair_structure.tex`: a stand-alone theorem claiming formal deformations are in bijection with Maurer--Cartan elements of `\ChirHoch^2(\mathcal A)[[t]]`, plus an explicit `\beta\gamma` Maurer--Cartan deformation family, neither supported elsewhere in the manuscript. I replaced that whole block by a narrow scope remark saying only that the manuscript uses the antisymmetrized bar `L_\infty` package as deformation-theoretic background, and I rewired the one downstream proof in `ym_boundary_theory.tex` to the local boundary-deformation dg Lie definition already present there. | **FIXED** |
| F324 | 2026-04-01 22:06:00 SAST | **SERIOUS** | Beilinson | The next pass found an object-conflation bug in the Maurer--Cartan/Chern--Simons block of `koszul_pair_structure.tex`. Theorems `thm:linf-mc-flatness` and `thm:cs-koszul-general` were still claiming that the bar complex itself carries the antisymmetrized `L_\infty` structure coming from the higher operations `m_n`, but the cited inputs only justify antisymmetrization on a transferred/model-side graded space carrying an `A_\infty` structure induced from bar data. I narrowed both theorems and their explanatory remarks to that transferred-model surface. | **FIXED** |
| F325 | 2026-04-01 22:19:00 SAST | **SERIOUS** | Beilinson | The next pass found a direct object mismatch in `thm:cs-koszul-km` of `koszul_pair_structure.tex`. The theorem statement was placing the classical Maurer--Cartan equation in the Chevalley--Eilenberg/bar complex `C^*(\fg)\otimes\Omega^1(M)`, while the proof correctly used the ordinary dg Lie algebra `\fg\otimes\Omega^*(M)`. I repaired the statement and local interpretation so the classical MC/flatness claim now lives on the ordinary dg Lie side, with the Chevalley--Eilenberg/bar complex retained only as the associated cochain model. | **FIXED** |
| F326 | 2026-04-01 22:33:00 SAST | **MODERATE** | Beilinson | The next pass found a wrapper-level Chern--Simons overclaim in `koszul_pair_structure.tex`. After the Maurer--Cartan theorems were narrowed to transferred-model algebra, the surrounding WRT/Chern--Simons remarks had still drifted back to stronger object-level language: the scope remark spoke of the Maurer--Cartan equation of the Koszul dual coalgebra itself, and the heuristic action/implications block still read as if a deformed Chern--Simons action and level-rank interpretation were already established. I narrowed that whole local surface to the honest one: a transferred-model Maurer--Cartan equation with the formal shape of deformed flatness, plus schematic heuristic Chern--Simons templates whose physical realization remains external or conjectural. | **FIXED** |
| F327 | 2026-04-01 22:47:00 SAST | **MODERATE** | Beilinson | The next pass found a residual export leak in the same Chern--Simons cone. The local BV remark in `koszul_pair_structure.tex` still said too flatly that the master action is the Chern--Simons functional, and the constitution was still summarizing `conj:wrt-conjecture` as “WRT invariants from bar complex,” which is broader and more concrete than the actual conjectural Yangian/quantum-affine Chern--Simons/Koszul dictionary. I narrowed both to the honest heuristic/conjectural surface. | **FIXED** |
| F328 | 2026-04-01 23:01:00 SAST | **MODERATE** | Beilinson | The next pass found a direct status contradiction between the Chern--Simons heuristic block and the Poincar\'e-computations warning. `koszul_pair_structure.tex` and the concordance export were still speaking as if level-rank duality literally is Koszul duality, while `poincare_computations.tex` explicitly warns that operadic Koszul duality is not classical level-rank duality. I narrowed both surviving exports to a level-rank-type Chern--Simons analogy, explicitly heuristic rather than an identification. | **FIXED** |
| F329 | 2026-04-01 23:14:00 SAST | **MODERATE** | Beilinson | The next pass found a scalar-lane Chern--Simons overclaim in `higher_genus_foundations.tex`. The remark `rem:kappa-chern-simons` was still promoting the affine scalar genus series all the way to a literal Witten--Reshetikhin--Turaev / Chern--Simons partition-function asymptotic, and it treated vanishing at critical level as a proved topological-phase statement. I narrowed that remark to the honest surface: the scalar series is linear in the shifted level and supports only a heuristic comparison with Chern--Simons/WRT asymptotics; no theorematic partition-function or topological-phase identification is proved here. | **FIXED** |
| F330 | 2026-04-01 23:29:00 SAST | **MODERATE** | Beilinson | The next pass found a status leak in the Montonen--Olive comparison of `thqg_gravitational_s_duality.tex`. The affine shifted-level involution $k+h^\vee \leftrightarrow -(k+h^\vee)$ was still being called a “genuine strong/weak exchange,” but the manuscript only proves a sign flip of the scalar obstruction parameter, not a literal strong/weak-coupling theorem in the physical sense. I narrowed that analogy to a sign-exchange analogue on the shifted-level parameter. | **FIXED** |
| F331 | 2026-04-01 23:41:00 SAST | **SERIOUS** | Beilinson | The next pass found a theorem-chain overclaim in the same Montonen--Olive comparison. The “particle spectrum duality” bullet of `thqg_gravitational_s_duality.tex` was still promoting gravitational $S$-duality to a blanket module-category equivalence $\cA\text{-mod} \leftrightarrow \cA^!\text{-mod}$, even though the repaired module-theoretic source only proves ordinary module-category duality on the separate quadratic genus-$0$ $\Eone$ complete/conilpotent lane and fences broader descent out of the theorematic surface. I narrowed the bullet to the algebra/bar--cobar level, with the module-category comparison explicitly lane-restricted. | **FIXED** |
| F332 | 2026-04-01 23:55:00 SAST | **MODERATE** | Beilinson | The next pass found a theorem-surface overread in the same Montonen--Olive comparison. The remark was still saying gravitational $S$-duality is an involution on the “space of theories” and is “exact, not conjectural” without qualification, even though the local theorem proves the exact identity only on the universal modular/Koszul datum $\sigma(\Theta_\cA)=\Theta_{\cA^!}$. I narrowed both clauses so the exactness and involutivity are now explicitly tied to that algebraic/modular datum surface rather than a blanket physical theory-level duality statement. | **FIXED** |
| F333 | 2026-04-02 00:06:00 SAST | **MODERATE** | Beilinson | The next pass found a residual scope leak in the subsection heading of `thqg_gravitational_s_duality.tex`. After the local Montonen--Olive comparison had been narrowed to exact symmetry of the modular/Koszul datum, the subsection title was still advertising `$S$-duality as exact symmetry` in a broader unqualified sense. I aligned the heading itself to the honest theorem surface: exact symmetry on the modular datum. | **FIXED** |
| F334 | 2026-04-02 00:22:00 SAST | **MODERATE** | Beilinson | The next pass found a theorem-surface wording leak in `higher_genus_foundations.tex`. `prop:grr-bridge` was still titled and stated on the broad “scalar lane,” even though its proof runs through the repaired all-genera uniform-weight genus-universality theorem together with a separate unconditional genus-$1$ fallback. I narrowed the proposition title and statement to the proved uniform-weight scalar lane and aligned the nearby family-index remark to the same scope. | **FIXED** |
| F335 | 2026-04-02 00:39:00 SAST | **SERIOUS** | Beilinson | The next pass found a theorem-chain overclaim in `higher_genus_modular_koszul.tex`. `thm:conformal-block-reconstruction` was still stated for arbitrary modular Koszul algebras even though parts (i)--(ii) of its proof import the repaired all-genera scalar formula from the uniform-weight genus-universality theorem. I narrowed the theorem and its immediate export remark so the genus-$g$ free-energy and $\hat A$ reconstruction package now live only on the proved uniform-weight scalar lane, with the genus-$1$ scalar specialization kept separate for arbitrary modular Koszul algebras. | **FIXED** |
| F336 | 2026-04-02 01:03:00 SAST | **SERIOUS** | Beilinson | The next pass found a cross-wrapper theorem-surface leak in `introduction.tex` and `concordance.tex`. The primitive-kernel overview was still identifying the general flat bar-cohomology connection with conformal blocks and the KZ/Hitchin package in blanket language, and it even described the Virasoro modular connection as the full Hitchin connection. I narrowed those wrappers so the general statement is only the flat modular connection on bar-cohomology sheaves; the actual conformal-block and KZ/Hitchin recovery is fenced to the integrable affine lane; and the Virasoro line is described only as the modular flat connection driven by the infinite shadow tower. | **FIXED** |
| F337 | 2026-04-02 01:18:00 SAST | **MODERATE** | Beilinson | The next pass found a residual concordance/export lag in `concordance.tex` and `higher_genus_modular_koszul.tex`. A modular-functor conjecture bullet and a status table row were still summarizing the general flat modular connection as “KZ/Hitchin,” and a nearby higher-genus summary remark still said the flat connection recovers `KZ/Hitchin` without the integrable-affine fence. I narrowed all three to the honest surface: one has a general flat modular connection, whose KZ/Hitchin package is recovered only on the integrable affine lane. | **FIXED** |
| F338 | 2026-04-02 01:34:00 SAST | **MODERATE** | Beilinson | The next pass found a theorem-title overclaim in `higher_genus_modular_koszul.tex`. `thm:primitive-flat-descent` was still titled “Descent to the conformal block connection,” even though the statement itself only proves descent to a general flat modular connection on bar cohomology and reserves actual conformal-block recovery for the integrable affine clause. I narrowed the theorem title and index entry to the honest flat-modular-connection surface. | **FIXED** |
| F339 | 2026-04-02 01:49:00 SAST | **MODERATE** | Beilinson | The next pass found a residual bridge-level overclaim in `genus_complete.tex`, together with closely related example wrappers in `w_algebras.tex` and `genus_expansions.tex`. Those surfaces were still identifying the descended flat connection itself with the KZ/Hitchin connection, or treating the Virasoro modular connection as if the KZ/Hitchin comparison were already the theorematic statement. I narrowed them to the honest surface: the general result is a flat modular connection, while KZ/Hitchin is only the integrable affine comparison package / benchmark surface, and the Virasoro line carries only the modular flat connection controlled by the shadow tower. | **FIXED** |
| F340 | 2026-04-02 02:09:00 SAST | **SERIOUS** | Beilinson | The next pass found a theorem-and-wrapper overclaim cluster in `thqg_introduction_supplement_body.tex`. That section was still presenting a general “bundle of conformal blocks” and a general shadow connection transporting derived conformal blocks, while the local imported theorem `thm:shadow-connection-kz` was still titled and phrased as if KZ recovery held on the full shadow-connection surface. I narrowed the whole local packet to the honest split surface: a general bar-cohomological / derived-coinvariant transport package, affine KZ only on the affine Kac--Moody comparison surface, and BPZ only on the Virasoro degenerate-module comparison surface. | **FIXED** |
| F341 | 2026-04-02 02:26:00 SAST | **MODERATE** | Beilinson | The next pass found a compact-wrapper lag in `thqg_introduction_supplement.tex`. The six-fold holographic datum summary was still exporting the modular shadow connection as a flat connection on derived conformal blocks, even though the repaired body had already narrowed the general surface to the derived coinvariant / protected-state package, with conformal blocks appearing only on benchmark comparison surfaces. I aligned that summary layer to the honest split surface. | **FIXED** |
| F342 | 2026-04-02 02:41:00 SAST | **MODERATE** | Beilinson | The next pass found a cross-volume wrapper-level scope leak in `thqg_preface_supplement.tex` and `chiral-bar-cobar-vol2/chapters/connections/thqg_ht_bbl_extensions.tex`. Both surfaces were still describing the full shadow connection as “KZ-type,” even though the repaired source only supports a KZ-like binary leading term together with separate higher-arity and higher-genus corrections. I narrowed both wrappers to that honest split surface. | **FIXED** |
| F343 | 2026-04-02 03:03:00 SAST | **SERIOUS** | Beilinson | The next pass found a cross-wrapper object/surface leak in `introduction.tex`, `higher_genus_modular_koszul.tex`, `genus_complete.tex`, and `concordance.tex`. Those overview layers were still sliding from the general flat modular connection and cohomological modular-functor package to a generic conformal-block object `\operatorname{CB}_g(\cA)`, and concordance still said the flat sections of the general connection “are the conformal blocks.” I narrowed that whole export chain so the general surface is the flat modular connection / cohomological modular-functor package, while actual conformal blocks appear only on the integrable affine `H^0` comparison lane. | **FIXED** |
| F344 | 2026-04-02 03:17:00 SAST | **MODERATE** | Beilinson | The next pass found a theorem-title and remark-level overclaim in `higher_genus_modular_koszul.tex`. `thm:conformal-block-reconstruction` was still titled as a conformal-block reconstruction theorem even though parts (i)--(iii) are scalar free-energy / determinant statements on the proved uniform-weight scalar lane, and only part (iv) reaches conformal blocks on the separate integrable affine lane. The downstream “circle closes” remark was exporting the same stronger slogan via an unconditional `H^0 \to conformal blocks` arrow. I retitled the theorem to the honest scalar-reconstruction surface and fenced the remark’s final comparison to the integrable affine `H^0` lane. | **FIXED** |
| F345 | 2026-04-02 03:29:00 SAST | **MODERATE** | Beilinson | The next pass found an object-level overclaim in `higher_genus_modular_koszul.tex`. Right below the repaired scalar-reconstruction theorem, `conj:conformal-block-half-form` was still calling $L_\cA$ the “conformal block line bundle” on the full modular-Koszul surface, even though the theorematic object there is only the determinant line bundle of the bar family, with conformal-block identification available only on the integrable affine comparison lane. I retitled the conjecture to the determinant-line surface and fenced the conformal-block language to that affine lane. | **FIXED** |
| F346 | 2026-04-02 03:43:00 SAST | **MODERATE** | Beilinson | The next pass found a proof-level overread in `higher_genus_modular_koszul.tex`. After the theorem and surrounding wrappers had already been narrowed, the proof of `thm:conformal-block-reconstruction` was still saying that the space of global flat sections `H^0(\overline{\mathcal M}_{g,n},\nabla_\cA)` *is* the conformal block space. I narrowed that to the honest comparison-surface claim: on the integrable affine lane, those global flat sections identify with the conformal block space. | **FIXED** |
| F347 | 2026-04-02 04:01:00 SAST | **MODERATE** | Beilinson | The next pass found a wrapper/index lag in `genus_complete.tex` and `introduction.tex`. The opener of `genus_complete.tex` was still saying cohomology recovers the classical modular functor of conformal field theory, and the nearby introduction index still filed the primitive-kernel transport under bare “conformal blocks,” even though the repaired source now distinguishes the general cohomological modular-functor package from the separate integrable affine conformal-block comparison. I narrowed both surfaces to that honest split. | **FIXED** |
| F348 | 2026-04-02 04:16:00 SAST | **SERIOUS** | Beilinson | The next pass found a theorem-chain status mismatch in the higher-genus transport cone. `higher_genus_modular_koszul.tex`, `introduction.tex`, `genus_complete.tex`, and `concordance.tex` were still saying the integrable affine monodromy *recovers* or *gives* the KZ/Hitchin package, while the underlying moduli-variation remark in `genus_complete.tex` only says the bar-side flat connection *should match* that package. I narrowed the theorem and surrounding exports to the honest surface: the bar construction furnishes the bar-side comparison surface for KZ/Hitchin on the integrable affine lane. | **FIXED** |
| F349 | 2026-04-02 04:31:00 SAST | **MODERATE** | Beilinson | The next pass found a residual constitution-level lag after F348. The chain-level modular-functor summary in `concordance.tex` was still saying the flat connection on the cohomology sheaf *recovers* the KZ/Hitchin monodromy package on the integrable affine lane, while the repaired theorem and overview layers had already been narrowed to the weaker comparison-surface formulation. I aligned that constitutional export to the honest bar-side comparison surface. | **FIXED** |
| F350 | 2026-04-02 04:45:00 SAST | **MODERATE** | Beilinson | The next pass found the local source of the KZ/Hitchin-status drift in `genus_complete.tex`. The moduli-variation remark still said the affine flat connection “should match” the KZ/Hitchin package on conformal blocks, while the repaired theorem and overview layers had already standardized on the weaker “bar-side comparison surface” language. I aligned the local remark to that same comparison-surface formulation so the theorem chain no longer fights itself. | **FIXED** |
| F351 | 2026-04-02 05:10:00 SAST | **MODERATE** | Beilinson | The next pass found two residual overview-level leaks in the higher-genus transport cone. `genus_complete.tex` was still saying the general bar construction “refines the classical modular functor,” and `concordance.tex` still had one table row and one primitive-kernel summary saying the integrable affine chain *gives* KZ/Hitchin rather than only the bar-side comparison surface for that package. I narrowed all three surfaces to the honest split between the general chain-level modular-functor formalism and the separate integrable affine KZ/Hitchin comparison lane. | **FIXED** |
| F352 | 2026-04-02 05:33:00 SAST | **MINOR** | Beilinson | The next pass found a residual object-level drift in the opener of `genus_complete.tex`. The scalar genus tower was still described as the first Chern class of the determinant line bundle of “the modular functor,” even though the repaired higher-genus theorem chain now treats the theorematic object as the determinant line bundle of the bar family, with classical modular-functor/conformal-block identifications only on comparison lanes. I aligned the opener to that exact bar-family surface. | **FIXED** |
| F353 | 2026-04-02 05:51:00 SAST | **MINOR** | Beilinson | The next pass found a concordance-level object/terminology leak in the higher-genus package. One summary still said Level~3 “recovers the full modular functor,” and a later all-genera lift conjecture still said “the full equivalence is the modular functor of~$\cA$,” conflating the equivalence data with the chain-level modular-functor package it supports. I narrowed both to the honest package-level wording. | **FIXED** |
| F354 | 2026-04-02 06:08:00 SAST | **MINOR** | Beilinson | The next pass found a small scope leak in `introduction.tex`. The primitive-kernel overview was still saying the arity-$3$ corolla $K_{0,3}$ determines the affine KZ equations without naming the affine Kac--Moody comparison surface on which that identification is actually proved. I narrowed the sentence to that exact lane. | **FIXED** |
| F355 | 2026-04-02 06:34:00 SAST | **SERIOUS** | Beilinson | The next pass found a real theorem-surface overclaim in `frontier_modular_holography_platonic.tex`. `thm:sphere-reconstruction` was still stated for arbitrary modular Koszul algebras as if sphere correlators were generically the horizontal sections of the shadow connection, but the proof only justifies that identification on established genus-$0$ comparison surfaces where the Ward-identity package is already known. I narrowed the theorem, its proof, the nearby GZ26 remark, and the exported concordance/preface summaries to that exact comparison-surface lane. | **FIXED** |
| F356 | 2026-04-02 07:02:00 SAST | **MODERATE** | Beilinson | The next pass found a downstream scope leak in `thqg_introduction_supplement_body.tex`. The general higher-genus shadow-connection discussion was still speaking as if the regular-singular extension, the genus-$1$ leading term, and the period-coupling statements were about conformal blocks on the full theorem surface, even though the supplement had already narrowed the generic object to the derived coinvariant / protected-state package. I rewrote that local block so the generic statements stay on the derived-coinvariant surface, with conformal-block language fenced back to benchmark comparison surfaces. | **FIXED** |
| F357 | 2026-04-02 07:18:00 SAST | **MODERATE** | Beilinson | The next pass found a residual object-level leak in `thqg_introduction_supplement_body.tex`. The generic sheaf and generic shadow action were still being described as a “derived coinvariant / conformal-block model” and as acting on the “fiber of conformal blocks,” even though this section’s repaired theorem surface already treats the generic object as the derived coinvariant / protected-state package, with conformal blocks only on comparison lanes. I aligned those definitions to the honest generic surface. | **FIXED** |
| F358 | 2026-04-02 07:31:00 SAST | **MINOR** | Beilinson | The next pass found a residual index/export leak in `thqg_introduction_supplement_body.tex`. The higher-genus subsection was still indexed under “conformal blocks!higher genus” even though its generic content had already been narrowed to the derived coinvariant / protected-state package, with conformal blocks only on benchmark comparison surfaces. I aligned the index layer to the honest generic surface. | **FIXED** |
| F359 | 2026-04-02 07:49:00 SAST | **MINOR** | Beilinson | The next pass found a frontier-side index/export leak in `frontier_modular_holography_platonic.tex`. The generic modular-shadow-connection definition was still indexed under conformal blocks even though the definition itself had already been narrowed to the derived coinvariant / protected-state package, with conformal blocks only on benchmark comparison surfaces. I aligned the index layer to that honest generic surface. | **FIXED** |
| F360 | 2026-04-02 08:04:00 SAST | **MINOR** | Beilinson | The next pass found a residual proof-level object leak in `frontier_modular_holography_platonic.tex`. Even after the sphere-reconstruction theorem had been narrowed to established comparison surfaces, the proof still called the generic notation `\mathcal{V}_{0,n}(\cA;V_1,\dotsc,V_n)` “the space of conformal blocks.” I rewrote that sentence so the conformal-block identification is explicitly comparison-surface-only. | **FIXED** |
| F361 | 2026-04-02 08:21:00 SAST | **MINOR** | Beilinson | The next pass found a residual frontier-synthesis overcompression in `frontier_modular_holography_platonic.tex`. The closing slogan still said “the connection shadow is KZ/BPZ” and listed “conformal blocks” among the five classical theories, even though the repaired theorem chain now treats the generic connection shadow as the modular shadow connection, with KZ/BPZ and conformal blocks only on established comparison surfaces. I narrowed that synthesis line accordingly. | **FIXED** |
| F362 | 2026-04-02 08:39:00 SAST | **MINOR** | Beilinson | The next pass found a residual slogan-level scope leak in the frontier census of `frontier_modular_holography_platonic.tex`. The proved-recovery summary still compressed the benchmark theorem surface to “shadow = KZ/BPZ,” which again made the generic shadow connection sound identified with its affine/Virasoro comparison-surface specializations. I narrowed that slogan to the honest comparison-surface wording. | **FIXED** |
| F363 | 2026-04-02 08:58:00 SAST | **MINOR** | Beilinson | The next pass found a preface-level scope leak in the M2 summary of `preface.tex`. The genus-zero shadow was still being described as if it literally were the KZ/BPZ-type connection for `\mathfrak{psl}(2|2)`, even though this file contains no corresponding proved theorem surface and the repaired frontier chain now consistently treats such statements as benchmark-surface comparison data. I narrowed the preface wording to an expected benchmark-type leading behavior. | **FIXED** |
| F364 | 2026-04-02 09:18:00 SAST | **MINOR** | Beilinson | The next pass found a residual subsection-opener scope leak in `frontier_modular_holography_platonic.tex`. The collision-filtration recovery synopsis was still packaging “the affine genus-$0$ shadow connection recovering KZ” as if that were part of the generic core identifications, even though the local theorem only proves KZ recovery on the affine Kac--Moody comparison surface. I narrowed the opener to that exact comparison-surface formulation. | **FIXED** |
| F365 | 2026-04-02 09:34:00 SAST | **MINOR** | Beilinson | The next pass found a residual object-level terminology leak in the frontier and THQG transport definitions. The generic derived coinvariant / protected-state package was still said to “specialize to derived conformal blocks,” which blurred the package-level comparison surface into an object identification. I aligned those wrappers so they now specialize only to the corresponding derived conformal-block package on benchmark comparison surfaces. | **FIXED** |
| F366 | 2026-04-02 09:53:00 SAST | **MODERATE** | Beilinson | The next pass found a theorem-flow mismatch in `thqg_introduction_supplement_body.tex`. The imported quartic-resonance theorem was still phrased as if it generically gave the obstruction to lifting the KZ connection from genus~$0$ to genus~$1$, even though the sourced result is the general $L_\infty$ obstruction governing the quartic extension problem for the shadow package. I narrowed that theorem to the exact shadow-extension surface. | **FIXED** |
| F367 | 2026-04-02 10:08:00 SAST | **MODERATE** | Beilinson | The next pass found a Drinfeld--Kohno wrapper leak in `thqg_introduction_supplement_body.tex`. The bridge subsection and its summary table were still exporting “the KZ connection is the genus-$0$ shadow connection” as if that were generic, even though the manuscript only proves that identification on the affine Kac--Moody comparison surface. I narrowed both exports to that exact affine comparison lane. | **FIXED** |
| F368 | 2026-04-02 10:22:00 SAST | **MINOR** | Beilinson | The next pass found a residual summary-table scope leak in `thqg_introduction_supplement_body.tex`. The guide table still exported bare “BPZ equations” as if they were a generic shadow output, even though the local theorem surface only recovers BPZ on the Virasoro degenerate-module comparison surface. I narrowed that row to the exact comparison lane. | **FIXED** |
| F369 | 2026-04-02 10:36:00 SAST | **MINOR** | Beilinson | The next pass found a preface-table scope leak in `preface.tex`. A Polyakov/bar-cobar dictionary row still exported bare “BPZ equations” as if the shadow connection produced them generically, even though the repaired manuscript only recovers BPZ on the Virasoro degenerate-module comparison surface. I narrowed that row to the exact comparison lane. | **FIXED** |
| F370 | 2026-04-02 10:49:00 SAST | **MINOR** | Beilinson | The next pass found a concordance-table scope and citation leak in `concordance.tex`. A constitutional summary row still exported bare “BPZ equations” with a generic shadow-connection citation, even though the repaired manuscript only recovers BPZ on the Virasoro degenerate-representation comparison surface and the exact local source is `prop:shadow-connection-bpz`. I narrowed the row and rewired the citation accordingly. | **FIXED** |
| F371 | 2026-04-02 11:02:00 SAST | **MODERATE** | Beilinson | The next pass found a preface-level theorem-flow mismatch in `preface.tex`. The Drinfeld--Kohno summary was still saying the KZ connection is the genus-$0$ arity-$n$ shadow “projected to the scalar sector,” but the cited affine theorem only identifies KZ with the genus-$0$ shadow connection on the affine Kac--Moody comparison surface and says nothing about a scalar-sector projection. I rewrote that slogan to the exact affine comparison-surface statement and removed the unsupported scalar language. | **FIXED** |
| F372 | 2026-04-02 11:17:00 SAST | **MINOR** | Beilinson | The next pass found a remaining constitutional scope leak in `concordance.tex`. The recovery-theorem bullet still used the short title “Shadow connection = KZ” and then appended an unqualified “BPZ recovery for Virasoro,” even though the repaired local theorem surfaces are comparison-lane-specific: affine Kac--Moody for KZ and Virasoro degenerate representations for BPZ. I narrowed that bullet to the exact two comparison surfaces. | **FIXED** |
| F373 | 2026-04-02 11:31:00 SAST | **MODERATE** | Beilinson | The next pass found a preface-slogan scope leak in `preface.tex`. The `(R3)` recovery slogan still said “Shadow = KZ” and then claimed the same extraction produced “the BPZ connection,” even though the repaired theorem surfaces are comparison-lane-specific: affine Kac--Moody for KZ and Virasoro degenerate modules for BPZ. I narrowed the slogan and both follow-on clauses to those exact comparison surfaces. | **FIXED** |
| F374 | 2026-04-02 11:46:00 SAST | **MINOR** | Beilinson | The next pass found a residual frontier-synthesis scope leak in `frontier_modular_holography_platonic.tex`. The recovery census, the five-projection table, and the closing slogan were still compressing the repaired comparison-lane statements to `KZ/BPZ` or `KZ for KM, BPZ for Vir`, even though the local theorem surfaces are more precise: affine Kac--Moody comparison surfaces for KZ and Virasoro degenerate-representation comparison surfaces for BPZ. I narrowed all three exports to that exact wording. | **FIXED** |
| F375 | 2026-04-02 12:01:00 SAST | **MODERATE** | Beilinson | The next pass found a preface projection-table dependency leak in `preface.tex`. The binary genus-$0$ residue row still listed `KZ/Yangian` as if both came directly from the same projection of `\Theta_\cA`, but the repaired source only makes the binary collision residue produce the classical $r$-matrix / Yangian seed; KZ enters later through the genus-$0$ shadow connection on the affine comparison surface. I narrowed that row to the exact residue-level output. | **FIXED** |
| F376 | 2026-04-02 12:17:00 SAST | **MINOR** | Beilinson | The next pass found a residual Virasoro/BPZ scope leak in `frontier_modular_holography_platonic.tex`. The sphere-reconstruction summary and the local proposition title were still phrased as if Virasoro generically “recovers BPZ,” even though the actual statement only reaches BPZ on the degenerate-representation comparison surface. I narrowed both to that exact surface. | **FIXED** |
| F377 | 2026-04-02 12:31:00 SAST | **MINOR** | Beilinson | The next pass found a residual Virasoro summary conflation in `frontier_modular_holography_platonic.tex`. A sphere-reconstruction bullet still spoke of “BPZ conformal Ward identities” on the degenerate-representation lane, even though the local proposition distinguishes the ordinary Virasoro conformal Ward connection from the stronger BPZ null-vector equations produced by higher collision residues on that lane. I separated those two surfaces in the summary. | **FIXED** |
| F378 | 2026-04-02 12:54:00 SAST | **MINOR** | Beilinson | The next pass found a THQG summary-layer comparison leak in `thqg_gravitational_yangian.tex`. A genus-zero shadow bullet and its summary table were still exporting a generic `KZ/BPZ connection`, even though the repaired theorem chain only gives KZ on the affine Kac--Moody comparison surface and BPZ on the Virasoro degenerate-representation comparison surface. I narrowed both exports to those exact lanes and added the missing BPZ citation in the table. | **FIXED** |
| F379 | 2026-04-02 13:08:00 SAST | **MINOR** | Beilinson | The next pass found a Virasoro/BPZ statement conflation in `thqg_soft_graviton_theorems.tex`. The soft-Ward theorem still said the full infinite Virasoro Ward tower “encodes the BPZ conformal Ward identities,” even though the proof only gives the general genus-$0$ Virasoro Ward hierarchy, with BPZ appearing only after imposing degenerate-module comparison-surface conditions. I narrowed the theorem clause to that exact split surface. | **FIXED** |
| F380 | 2026-04-02 13:24:00 SAST | **MINOR** | Beilinson | The next pass found a residual HT-wrapper scope leak in `holomorphic_topological.tex`. The “four recovery theorems” proposition still used the item title “Shadow = KZ,” which compressed the repaired comparison-lane statement into a generic identity. I narrowed that item to the affine Kac--Moody comparison surface and stated the result as a shadow/KZ comparison on that lane. | **FIXED** |
| F381 | 2026-04-02 13:38:00 SAST | **MINOR** | Beilinson | The next pass found a THQG preface-supplement slogan leak in `thqg_preface_supplement.tex`. The recovery slogan still said “Shadow as KZ,” and the follow-up sentence described the KZ equations as a structural consequence of $D^2=0$ without restating that this only holds on the affine Kac--Moody comparison surface. I narrowed both to the exact affine shadow/KZ comparison surface. | **FIXED** |
| F382 | 2026-04-02 13:52:00 SAST | **MINOR** | Beilinson | The next pass found a theorem/proof scope leak in `thqg_soft_graviton_theorems.tex`. The affine clause of the soft-Ward theorem and its proof still said the leading Ward identity “recovers the KZ equation” without restating the affine Kac--Moody comparison surface on which that identification is actually proved. I narrowed both to that exact lane. | **FIXED** |
| F383 | 2026-04-02 14:11:00 SAST | **MODERATE** | Beilinson | The next pass found a theorem-flow overclaim in `kontsevich_integral.tex`. The local quasi-triangular remark still escalated the proved affine genus-$0$ KZ/associator comparison to the claim that the bar-cobar adjunction itself “is” the Reshetikhin--Turaev construction at the chain level. I narrowed the remark to the exact proved surface: the genus-$0$ bar differential identifies with KZ on the affine Kac--Moody comparison surface, and together with the external Drinfeld--Kohno theorem this supplies the chain-level quasi-triangular package underlying the Reshetikhin--Turaev construction. I also aligned the local `What is proved` summary item. | **FIXED** |
| F384 | 2026-04-02 14:26:00 SAST | **MODERATE** | Beilinson | The next pass found a DK/KL scope leak in `thqg_gravitational_yangian.tex`. The meromorphic-vs-genuine braiding remark still said the KL and DK routes “both produce the same genuine braided tensor category, which is the category of representations of the quantum group $U_q(\fg)$,” even though the repaired affine comparison surfaces only support that conclusion on the affine / evaluation-core comparison lane. I narrowed the remark to that exact lane and to the comparison core identified by the Kazhdan--Lusztig / Drinfeld--Kohno package. | **FIXED** |
| F385 | 2026-04-02 14:44:00 SAST | **MODERATE** | Beilinson | The next pass found a cross-wrapper DK compression in `preface.tex`, `introduction.tex`, and `thqg_introduction_supplement_body.tex`. Those summaries still said the monodromy of KZ “equals” the quantum-group $R$-matrix, or that Drinfeld--Kohno is simply the passage from KZ to the quantum group, which collapsed the repaired affine comparison package into a stronger identity. I narrowed all three to the honest surface: the affine Drinfeld--Kohno package compares KZ monodromy with the corresponding quantum-group side on the reduced evaluation comparison surface. | **FIXED** |
| F386 | 2026-04-02 15:03:00 SAST | **MODERATE** | Beilinson | The next pass found an object mismatch and DK overcompression in `preface.tex`. The Drinfeld--Kohno summary wrote the KZ monodromy representation as a map into `\operatorname{Rep}(U_\hbar(\mathfrak g))`, which is a category, and a later summary line compressed the theorem to “monodromy of KZ = quantum group representations.” I rewrote both to the honest affine comparison surface: braid-group monodromy on the KZ side, compared by the affine Drinfeld--Kohno package with the corresponding quantum-group representation category. | **FIXED** |
| F387 | 2026-04-02 15:18:00 SAST | **MINOR** | Beilinson | The next pass found a residual KZ-identity wording drift in `higher_genus_modular_koszul.tex`, `concordance.tex`, and `thqg_preface_supplement.tex`. Those surfaces still said the affine genus-$0$ shadow connection “is” the KZ connection, even though the repaired manuscript now treats that as an affine comparison-surface identification. I aligned all three to the honest comparison language: on the affine Kac--Moody comparison surface, the genus-$0$ shadow connection identifies with KZ. | **FIXED** |
| F388 | 2026-04-02 15:34:00 SAST | **MINOR** | Beilinson | The next pass found a residual DK-comparison compression in `en_koszul_duality.tex`. The `\Eone`-`\Etwo` obstruction remark still summarized Drinfeld--Kohno as “KZ monodromy = $R$-matrix,” even though the repaired manuscript now treats that only as an affine Drinfeld--Kohno comparison package. I narrowed that remark to the exact comparison language. | **FIXED** |
| F389 | 2026-04-02 15:47:00 SAST | **MINOR** | Beilinson | The next pass found a residual equality-slogan import in `thqg_introduction_supplement_body.tex`. The imported affine theorem was still titled “Shadow = KZ connection,” and its body said the shadow connection “recovers” KZ, even though the repaired manuscript now treats this as an affine comparison-surface identification. I narrowed the theorem title and body to explicit shadow/KZ comparison language. | **FIXED** |
| F390 | 2026-04-01 20:41:37 SAST | **MINOR** | Beilinson | The next pass found a theorem-surface scope leak in `chiral_modules.tex`. The general bar-connection proposition still said that for affine Kac--Moody algebras on $\mathbb{P}^1$ it “recovers the KZ connection,” without fencing that identification to the affine Kac--Moody comparison surface. I narrowed the affine specialization clause to that exact comparison-surface identification. | **FIXED** |
| F391 | 2026-04-01 20:43:00 SAST | **MINOR** | Beilinson | The next pass found a higher-genus export leak in `genus_expansions.tex`. The integrable-affine example still said the monodromy of the flat modular connection “recovers the KZ/Hitchin package,” even though the repaired higher-genus theorem chain only supports the weaker comparison-surface statement. I narrowed that export to the honest bar-side comparison language. | **FIXED** |
| F392 | 2026-04-01 20:45:02 SAST | **MINOR** | Beilinson | The next pass found a theorem-and-wrapper equality leak in `frontier_modular_holography_platonic.tex`. The primary affine theorem was still titled “Shadow connection for affine Kac--Moody = KZ,” its body and proof used raw identity language, and the local frontier census/synthesis lines still exported “recovering KZ.” I narrowed the theorem and surrounding summaries to explicit shadow/KZ comparison language on the affine Kac--Moody comparison surface. | **FIXED** |
| F393 | 2026-04-01 20:46:00 SAST | **MINOR** | Beilinson | The next pass found a THQG summary compression in `thqg_gravitational_yangian.tex`. A genus-zero shadow bullet still said `\Sh_{0,n}(\Theta_\cA)` “yields the KZ connection” and then bundled the Virasoro BPZ statement into the same clause, even though the repaired frontier source distinguishes the affine shadow/KZ comparison from the separate higher-collision BPZ null-vector constraints on the degenerate surface. I narrowed the bullet to that exact split surface. | **FIXED** |
| F394 | 2026-04-01 20:47:00 SAST | **MINOR** | Beilinson | The next pass found a Virasoro/BPZ mechanism compression in `thqg_introduction_supplement_body.tex`. The BPZ subsection opener and later higher-genus summary still spoke as if the shadow connection itself produced or recovered the BPZ equations, even though the repaired frontier theorem chain separates the genus-$0$ Virasoro conformal Ward connection from the stronger BPZ null-vector equations supplied by higher collision residues on the degenerate-module surface. I narrowed both exports to that exact split surface. | **FIXED** |
| F395 | 2026-04-01 20:48:00 SAST | **MINOR** | Beilinson | The next pass found a residual affine-comparison wording lag in `thqg_introduction_supplement_body.tex`. The higher-genus summary still said the affine genus-$0$ comparison surface “recovers the KZ connection,” even though the repaired source theorem and mirror summaries now use explicit identification language on that surface. I aligned the summary to that exact comparison-surface identification. | **FIXED** |
| F396 | 2026-04-01 20:49:00 SAST | **MINOR** | Beilinson | The next pass found a preface-level object/scope compression in `preface.tex`. The recovery slogan still said “Shadow recovers KZ,” the affine clause treated the genus-$0$ shadow one-form as if it were the full KZ connection rather than its connection form, and the Virasoro clause skipped the repaired split between the genus-$0$ Ward connection and the higher-collision BPZ constraints. I narrowed that whole block to the exact affine shadow/KZ comparison surface and the degenerate-module Virasoro/BPZ split surface. | **FIXED** |
| F397 | 2026-04-01 21:05:52 SAST | **MINOR** | Beilinson | The next pass found a theorem-surface compression in `thqg_gravitational_s_duality.tex`. The theorem title “KZ duality,” the affine clause of the statement, the proof’s “KZ connection with negated coupling” wording, and the downstream summary row were all exporting the affine Feigin--Frenkel corollary more strongly than the repaired source warrants. I narrowed the theorem and table to the honest surface: generic shadow-connection duality, with affine shadow/KZ duality only on the affine Kac--Moody comparison surface. | **FIXED** |

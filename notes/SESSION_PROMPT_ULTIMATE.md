> **Historical prompt note (March 13, 2026).**
> This file is retained for provenance and should not be treated as a live control document.
> Active doctrine is `notes/SESSION_PROMPT_v23.md` together with `notes/autonomous_state.md`, `notes/VISION.md`, `notes/PROGRAMMES.md`, and `chapters/connections/concordance.tex`.
> Current constitutional status: MC1/MC2 resolved on the printed loci; live frontier MC3/MC4; MC5 downstream; periodicity orthogonal.

# SESSION PROMPT ULTIMATE - Master Execution Prompt
# Launch: "Read notes/SESSION_PROMPT_ULTIMATE.md and execute it."
# Target model: ChatGPT 5.4 Pro / Codex in code environment, extra-high reasoning
# Purpose: execute the monograph's dependency-ordered master backlog without
# status drift, local patching, loss of architectural perspective, or
# analysis-only stalling.

---

## IDENTITY

You are not polishing an isolated TeX project. You are maintaining and extending

**Modular Homotopy Theory for Factorization Algebras on Curves. Volume I: Modular Koszul Duality**

as a definitive dimension-one treatise.

Your job is to convert the current control-layer understanding of the manuscript
into decisive, verified progress on the highest-leverage unresolved item you can
actually move today.

Do not produce atmosphere. Produce mathematics, doctrinal synchronization,
frontier reduction, or repository-operational work that unblocks those.

---

## NON-NEGOTIABLE ACTION BIAS

Orientation exists only to enable action. It is not itself the deliverable.

You must exit analysis rapidly and move exactly one concrete batch.
A run that ends with only control-plane analysis, routing language, or
high-level diagnosis is a failed run unless the repository is externally
blocked and that blocker is evidenced precisely.

By the end of the first substantive work cycle, at least one of the following
must have happened:

1. a theorem statement, proposition, remark, definition, proof segment, or
   chapter opening has been sharpened in source;
2. a compute script, test, or extraction relevant to MC3 / MC4 has been run
   and interpreted against the manuscript;
3. a build or tooling blocker preventing theorem work has been fixed or
   isolated with exact evidence.

The routing card is an internal instrument, not a final product.
Do not spend a turn restating context already present in this prompt unless the
user explicitly asked for a diagnosis.

If several tasks are plausible, choose the one with the shortest path to a real
mathematical or computational move today.

---

## PANORAMIC STATE YOU MUST TREAT AS CURRENT

These are inherited control facts, not optional context:

1. MC1 is resolved for the standard finite-type interacting families:
   affine Kac-Moody, Virasoro, principal finite-type `W_N`.
2. MC2 is resolved:
   the cyclic deformation object `Def_cyc(A)`, the universal class `Theta_A`,
   and the modular-operadic completed-tensor package are part of the proved core.
3. The book already has a concrete modular datum on the present theorem surface:
   H-level, M-level, and S-level data are explicit in Chapter 34.
4. The live structural frontier is MC3 and MC4, not MC1 and not MC2.
5. MC3 means extension beyond the evaluation-generated / polynomial DK core:
   full `O`, KL, completed/coderived, compact-generation, prefundamental,
   Baxter, pro-Weyl, braided upgrade.
6. MC4 means filtered H-level / coefficient-identification comparison for
   `W_infty` and Yangian towers, after the standard M-level completions.
7. The MC4 ledger is exact, not vague:
   prove
   `K^{line}_{a,b}(N)=K^{RTT}_{a,b}(N)` and
   `C^{res}_{s,t;u;m,n}(N)=C^{DS}_{s,t;u;m,n}(N)`,
   and close the finite-detection packets `Delta_{a,0}(N)` and `I_N`.
8. The non-principal orbit frontier is distinct from the standard MC4 tower:
   keep separate the dual-orbit input packet, the orbit-indexed level-shift
   packet, and the paired DS seed-transport/globalization packet.
9. MC5 is downstream:
   genus-0 BV/BRST is theorematic; higher genus remains downstream of MC3/MC4.
10. Periodicity is an orthogonal weak flank:
    contain it, stratify it correctly, and never let it outrun the proof surface.

---

## CONTROL STACK TO READ BEFORE CHANGING ANYTHING

Read in this order, but stop as soon as the route is clear enough to act safely.
Do not bulk-summarize the whole stack back to the user unless asked.

Default minimum before the first edit or computation:

1. `CLAUDE.md`
2. `chapters/connections/concordance.tex`
3. `chapters/theory/introduction.tex`
4. the local file or theorem surface you intend to move

Load the remaining control documents only if the route, status boundary,
dependency order, or verification target is still unclear:

5. `notes/GPT54_CODEX_OPERATING_SYSTEM.md`
6. `notes/SESSION_PROMPT_v25.md`
7. `notes/autonomous_state.md`
8. `notes/VISION.md`
9. `notes/PROGRAMMES.md`
10. any local theorem chapter you intend to edit

If any downstream file conflicts with Chapter 34 / concordance, concordance wins.

---

## MASTER ACTION INVENTORY

Treat the following as the monograph-wide backlog. Do not try to do all of it at
once. Use it to choose the best next decisive batch.

### A. Repository-operational debt

1. Regenerate stale metadata: `census.json`, `claims.jsonl`, `theorem_registry.md`.
2. Fix the hanging DS-heavy test suite.
3. Keep `make fast` usable and diagnose build/tooling interference precisely.
4. Triage the large modified worktree without reverting user work.

### B. Doctrinal propagation and control sync

5. Keep MC1 and MC2 propagated as resolved across all control, summary,
   portrait, and frontier surfaces.
6. Keep the concrete modular datum explicit on H/M/S levels.
7. Prevent local theorem-status drift from concordance.
8. Prevent scalar / spectral / full-package blur.
9. Prevent construction / inversion blur.
10. Prevent `d_fib` / `D_tot` conflation.

### C. Future 3 / categorical completion

11. Complete the factorization-level coderived / contraderived Ran formalism.
12. Strengthen the modular-operadic / categorical statement as far as the proof
    actually goes.
13. Keep the outer categorical lift fenced where not yet proved.

### D. MC3 / derived Drinfeld-Kohno extension

14. Decide the correct ordinary-derived DK domain beyond the current core.
15. Prove or sharply fence the four extension packets:
    Baxter exact triangles, shifted-prefundamental generation,
    pro-Weyl recovery, DK on compacts extended by completion.
16. Complete the KL side:
    periodic CDG, coderived identification, braided monoidal upgrade.
17. Push the lattice route all the way through the remaining
    lattice-to-quantum-group identification.
18. Finish DK-4.
19. Finish DK-5.

### E. MC4 / infinite-generator comparison

20. Construct the filtered H-level `W_infty` target with correct finite quotients.
21. Construct the RTT-adapted / dg-shifted Yangian target with correct finite quotients.
22. Prove the exact Yangian coefficient identities on `Delta_{a,0}(N)`.
23. Prove the exact `W_infty` coefficient identities on `I_N`.
24. Promote finite-detection identities to stagewise comparison theorems.
25. Carry the low-stage exact packets to closure, not just extraction.

### F. Distinct non-principal orbit frontier

26. Determine the dual-orbit input beyond the current type-A seeds.
27. Determine the orbit-indexed level-shift rule `k'(k,f)`.
28. Construct and globalize paired non-principal DS seed transport beyond the
    present hook / subregular theorematic seeds.

### G. Characteristic hierarchy beyond existence

29. Compute `Theta_A`, `H_A`, and the sharper package where existence is already proved.
30. Decide admissible-level scalar saturation / universal rigidity.

### H. Periodicity and spectral flank

31. Finish modular periodicity in its stratified formulation.
32. Keep geometric contribution classified as nilpotent depth, not period.
33. Upgrade spectral discriminant geometry from determinant preservation to
    transport-operator / branch-object preservation if true.

### I. MC5 / physics completion

34. Complete higher-genus BV/BRST.
35. Complete higher-genus path-integral / string-amplitude statements.
36. Advance holography, AGT, HCS/CL, and related physics only after upstream
    MC3/MC4 prerequisites are in place.

### J. Long-range programme surfaces

37. Programme I: critical-level bar = derived opers, beyond `H^0` / `H^1`.
38. Programme III: fusion / monoidality of bar-cobar on the characteristic hierarchy.
39. Programme V: Vassiliev / Kontsevich-integral shadow.
40. Programme VII: noncommutative Hodge as a genuine later programme, not fake closure.
41. Programme IX: computation that directly reduces MC3 / MC4 / non-principal uncertainty.

---

## TASK SELECTION POLICY

Unless the user explicitly narrows the target, choose the highest-leverage item by
the following order:

1. A control-layer inconsistency that would misstate what is proved.
2. A repository-operational blocker preventing sustained theorem work.
3. An upstream theorematic blocker in MC3.
4. An upstream theorematic blocker in MC4.
5. A non-principal packet only if it is not being conflated with MC4.
6. Periodicity / spectral work only if it does not outrun the main chain.
7. Downstream physics only if the upstream mathematical prerequisites are already in place.

Prefer one decisive batch over many cosmetic edits.
Prefer upstream doctrine over downstream portrait patching.
Prefer theorem hardening over rhetorical smoothing.
Prefer the batch that yields a changed source file, compute artifact, or exact
blocker diagnosis in the current run.

Do not ask the user what to work on if the control stack already decides it.

---

## ROUTING CARD

Before editing, determine all of the following in extended reasoning:

1. What is the governing question of the target file or batch?
2. Is the target in Stratum I or Stratum II?
3. Which semantic levels are being touched: H, M, S?
4. Which status vocabulary is legal there: proved, conditional,
   conjectural, programme?
5. Which control document governs the truth conditions for the batch?
6. Is the batch doctrinal propagation, theorem hardening,
   portrait synchronization, frontier reset, compute-supported evidence,
   or repo-operational work?
7. Which single pass type are you executing?

Valid pass types:

- doctrinal propagation
- theorem linearization
- entry-point rewrite
- portrait pass
- frontier pass
- periodicity containment
- compute-surface reduction
- repo-operational unblock

If you cannot answer the routing card, read upward before changing downstream files.
Do the routing card internally. Do not turn it into a standalone essay.

---

## EXECUTION CONTRACT

1. Read the control node and target file together.
2. Determine the governing question, stratum, semantic levels, and legal status.
3. Identify the smallest authoritative file set that must move together.
4. Start the concrete batch immediately: edit source, run the relevant compute,
   or attack the operational blocker.
5. Make theorem-status boundaries explicit before polishing prose.
6. If a theorem is only implied, either prove it or state it as a precise conjecture.
7. Propagate any status change forward to obvious summaries, examples, and ledgers.
8. Run `make fast` after the batch.
9. Run relevant tests when compute or scripts are touched.
10. Report precisely what was changed, what was verified, and what remains open.

Never use the entire run just to classify, summarize, or restate the backlog.
If no safe edit is possible after the minimal read, choose the smallest exact
compute or theorem-shaping packet and move that.

If build infrastructure misbehaves, distinguish clearly between:

- a manuscript/source error,
- a stale build artifact,
- a competing watcher / external process issue.

Do not confuse tooling noise with theorem failure.

---

## ANTI-FAILURE RULES

Do not:

1. reopen MC1 as if finite-type PBW were still the main bottleneck;
2. reopen MC2 as if `Theta_A` were still a target rather than a proved package;
3. collapse the evaluation-generated DK core into the full category `O` / completion frontier;
4. treat the theorematic M-level `W_infty` / Yangian package as if the H-level comparison were done;
5. mix the standard MC4 tower with the separate non-principal orbit frontier;
6. let S-level evidence upgrade H-level claims;
7. let periodicity language outrun the printed proof;
8. use Heisenberg as proof of the full package rather than as the commutative atom;
9. produce elegant local prose that leaves the theorem graph inconsistent;
10. stop at analysis if a concrete edit or computation is feasible.

Also avoid generic prompts to yourself like "improve this section" or
"make it more compelling." Always route through governing question, stratum,
semantic level, and exact theorem/conjecture boundary.

---

## MODEL-SPECIFIC GUIDANCE FOR CHATGPT 5.4 PRO IN CODE ENVIRONMENT

Exploit the model's strengths by being explicit about:

1. the control stack,
2. the exact dependency order,
3. the pass type,
4. the smallest authoritative file set,
5. the required verification step,
6. the distinction between theorematic core and outer programme.

Counter its common failure modes by forcing:

1. classification before rewriting,
2. immediate exit from classification into one decisive batch,
3. explicit theorem/conjecture boundaries,
4. upstream control synchronization before local patching,
5. verification after edits,
6. exact statements of what remains unresolved,
7. a hard ban on analysis-only completion.

The standard high-performance internal questions are:

- What breaks that forces this construction?
- What is already proved on the current theorem surface?
- What exact frontier packet remains?
- Which semantic level am I touching?
- Which upstream document must move before this downstream chapter?
- What is the smallest batch that materially changes the theorem graph?
- What can I physically change, compute, or verify in this run?

Timebox orientation aggressively. If the minimal control read already points to a
safe batch, act. Additional panoramic reading is justified only when it changes
what you will actually do.

---

## DELIVERABLE FORMAT

At the end of the run, deliver:

1. the pass type executed;
2. the governing question of the batch;
3. the files changed;
4. the exact mathematical or doctrinal advance achieved;
5. verification performed (`make fast`, tests, compute, or explicit failure diagnosis);
6. the next dependency-ordered step.

Do not end with vague "future work" language. Name the next exact packet.
A changed file, compute artifact, or exact blocker diagnosis is mandatory unless
an external constraint prevented action.

---

## DEFINITION OF DONE FOR A SESSION

A session is successful if:

1. one meaningful item from the master action inventory has moved;
2. the control layer is at least as accurate as before;
3. status drift has decreased, not increased;
4. any theorematic gain has been propagated to the obvious dependent surfaces;
5. verification has been run or a precise blocker has been diagnosed;
6. the final note names the next exact packet, not a generic horizon.
7. the session did not terminate at the routing / analysis layer when a concrete
   batch was feasible.

Execute now.

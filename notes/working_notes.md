# Working Notes — Session Mar 10-11, 2026

## What was done

### MC3 Category O strategies — two explicit computations added to yangians.tex

**comp:current-algebra-E1** (Strategy IV E_1 page):
The Strategy IV spectral sequence for the Yangian bar complex, filtered by loop degree, has E_0 differential = bar differential of gr^F Y(sl_N) = U(sl_N[t]). The E_1 page is therefore H*(sl_N[t], k).

For sl_2: the Hochschild-Serre spectral sequence for 0 -> sl_2 ⊗ t k[t] -> sl_2[t] -> sl_2 -> 0 gives E_2^{p,q} = H^p(sl_2, H^q(n, k)) where n = sl_2 ⊗ t k[t]. The key point is that n is abelian on the associated graded (bracket raises loop degree), so H^q(n, k) = Λ^q(n^*). Since each copy of n is a copy of V_2 (adjoint, self-dual), the invariants come from Clebsch-Gordan: V_2 ⊗ V_2 = V_4 + V_2 + V_0, giving one invariant per pair of distinct loop degrees.

At each fixed total loop degree p, this is a finite computation. First values: dim E_1^{0,p} = 1, 0, 0, 1, 0, 1 at p = 0,...,5.

Constructive insight: the E_1 growth is at most polynomial in loop degree, confirming that Strategy IV reduces the infinite-dimensional generation problem to a countable family of finite computations.

**comp:sl2-rmatrix-ext** (Strategy I R-matrix extensions):
The Yang R-matrix for the fundamental V_1 of Y(sl_2) is R(u) = (u·id + P)/(u+1). Eigenvalues: +1 on Sym^2(V_1), (u-1)/(u+1) on Λ^2(V_1). Unique pole at u = -1 with residue = projector onto V_0.

This produces Ext^1_O(V_2(c), V_0(c)) = k for all c — exactly one extension class per spectral parameter, generated by the R-matrix residue.

For dominant integral λ, iterating produces the classical BGG resolution enriched with spectral parameters, of length λ+1. For non-integral λ, the chain does not terminate: no lowest weight in the Verma module. This exhibits exactly why Strategy I succeeds on O_poly and fails outside it — consistent with Theorem thm:catO-thick-generation.

### O_poly = DK-relevant O (already proved)

Discovered that cor:dk-poly-catO + rem:poly-O-natural-domain already prove this. The quantum group side Fact_{E_1}(U_q(g))^op has only finite-dimensional reps. Under KL, these correspond exactly to O_poly. So O_poly IS the natural DK domain and factorization DK is unconditionally proved for type A.

### Fourier Seed chapter — complete (681 lines)

All 6 sections written per SESSION_PROMPT_v25:
1. The kernel (logarithmic propagator, Arnold relation with explicit proof)
2. The transform (4 computations: point, P^1/H_k, E_τ/H_k, P^1/g_k)
3. Specialization to a point (bar → classical bar of Loday-Vallette)
4. The four properties (Theorems A-D as Fourier properties + classical analogy table)
5. The landscape (table of B(A), κ(A), c(A) for standard families)
6. Genus as deformation variable (κ proved, Θ_A from MC2)

All Phase 2 integration edits done (abstract, introduction, bar_cobar_construction, heisenberg_frame).

### introduction.tex restored — 238 broken refs fixed

The file chapters/theory/introduction.tex was not included in main.tex (apparently from an incomplete refactoring in commit 246f7e9). It defines 27 labels (conv:hms-levels, conv:regime-tags, thm:central-charge-complementarity, rem:nilpotence-periodicity, etc.) referenced 238 times from 28 compiled chapters.

Fixed by adding \include{chapters/theory/introduction} before fourier_seed in main.tex. Build now clean at 1612pp, 0 errors.

The four most-referenced labels from introduction.tex:
- conv:hms-levels (84 refs) — H/M/S semantic level convention
- conv:regime-tags (54 refs) — proof regime tags
- thm:central-charge-complementarity (23 refs) — central charge complementarity
- rem:nilpotence-periodicity (20 refs) — bar as categorical logarithm

### MC4 coefficient identities — framework assessed

The formal reduction chain is at its terminal point. On the Yangian side: 10+ propositions reduce to a single mixed-tensor residue formula Ξ_a(e_1 ⊗ e_2) = -ℏ(e_2 ⊗ e_1). On the W_∞ side: reduced to C^res_{s,t;u;m,n}(N) = C^DS_{s,t;u;m,n}(N).

The remaining work is constructing the line operator from the bar construction — this is genuine mathematical research requiring the chiral-to-RTT extraction.

### Build

1612pp, 0 TeX errors, 0 undefined control sequences, 11 overfull (cosmetic). Batchmode required due to OOM in nonstopmode on this 1600+ page document.

## Key structural observations

1. The DK ladder is now: DK-0/1 proved (all g), DK-1½ proved (lattice), DK-2/3 proved (fd + O_poly, type A). The "full category O" generation problem is actually a phantom — O_poly is the natural DK domain.

2. Strategy IV (sectorwise finiteness via loop filtration) is the only strategy that could resolve MC3 for all types simultaneously. Its bottleneck — computing H*(g[t], k) and controlling the spectral sequence differentials — connects to the Loday-Quillen-Tsygan theorem.

3. The four MC3 strategies form a coherent picture: Strategies I and III (BGG + KL) attack via representation theory of specific types. Strategy II is proved for O_poly. Strategy IV attacks via bar complex structure for all types.

4. The MC4 terminal reduction to a single residue formula is remarkably clean. The entire chain from "factorization DK/KL bridge" to "check Ξ_a(e_1 ⊗ e_2) = -ℏ(e_2 ⊗ e_1)" is fully formal — only the last step requires extracting coefficients from the chiral bar construction.

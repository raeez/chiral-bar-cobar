**BLOCK — bounded review of Chapter 9, candidate programme-008.**

One required carrier clarification remains. I found no independent sign error or counterexample to the bar resolution, multiplicative derived-endomorphism comparison, or cobar reconstruction in the intended degreewise category. This verdict does not certify the whole programme.

All 14 manifest entries matched their live and frozen SHA-256 values initially and finally. The final recheck reported zero mismatches.

| Artifact | SHA-256 |
|---|---|
| Candidate manifest | `79667c9500899184b8293892900fd0fa7c75b3e0ebb445e4ab795c3697544dc9` |
| `ordered-coefficient-bar.tex` | `269592881db2820e1324dcdbb443c4602c0ade2ca6c90774ca4747a5e8fcc9e3` |
| `ordered-collisions-and-residues.tex` | `96e1f39f364060cd00c15650f672fb4deb66f3843a23d1137827fe58706c93a0` |
| `relations-and-homotopies.tex` | `6c14c43992f786dc4c4eeb9967be9f7e3a993694b7f2504ce30e0c76d8819f77` |
| `completion-and-coefficients.tex` | `28fa96e404fa298b8d160e98d469e339ead3e4c039a726d133089b66ea02b39e` |
| Programme PDF | `89c9ac203ede393d13799afefec28a7afd613952f95efaeaa365ba7a4aa75d00` |

The worktree was `/Users/raeez/mathematics/worktrees/frontier-recovery-astra-ultra-20260913`, branch `intake/frontier-recovery-astra-ultra-20260913`, HEAD `57ee67e7a4be06694931ffc348c17bd461a40c88`. No files or git state were changed.

Requested mathematical controls were `gpt-6-astra`, effort `ultra`. Observed runtime model and effort metadata were unavailable to this specialist and remain **unverified**.

The blocking finding is:

```yaml
id: A008-bar-01
severity: 3
status: valid
lens: carriers_and_completion
target: >
  manuscript/ordered-coefficient-bar.tex:281-307;
  propagated convention at 436-447, 492-498, 570-572;
  PDF pages 162, 164-165
claim: >
  The product of weight duals, described as unrestricted coefficients
  for every word, is the stated graded Hom and derived-endomorphism model.
broken_step: >
  The word-series description does not impose finite support in total
  cochain degree. Its literal unrestricted product therefore admits
  elements absent from the previously defined graded Hom.
evidence_type: counterexample
evidence_ref: >
  I={1}: C=C[z], A=C plus Ca, |a|=0, a^2=0.
  BA has one basis word in degree -r for each r>=0.
  Graded Hom_C(BA,C) is C<alpha>, |alpha|=1.
  The unrestricted weight product also contains sum_{r>=0} alpha^r.
  That series has infinitely many cochain degrees and is not in graded Hom.
files_read:
  - manuscript/ordered-coefficient-bar.tex
  - manuscript/relations-and-homotopies.tex
  - manuscript/completion-and-coefficients.tex
tools_used:
  - exec_command
  - view_image
confidence: high
blast_radius: >
  The explicit algebra identified with graded Hom and RHom;
  the category governing subsequent completed constructions and base change.
minimal_heal: >
  Define every weight completion degreewise and retain a direct sum
  across cochain degrees. Constrain the double-bracket description accordingly.
residual: >
  Frozen008 does not contain this clarification.
deciding_evidence: >
  A new frozen candidate with explicit degreewise formulas, consistent
  completed tensor products, and corresponding rendered prose.
```

This is a carrier-specification failure, not a refutation of the intended degreewise theorem. The first unsupported identification occurs at lines 281–307. “This graded Hom” points toward the correct interpretation, but the accompanying description states only the word-weight restriction and permits the counterexample above.

A precise repair is to state, for weight-graded complexes \(M\),
\[
(\widehat M)^n=\prod_{w\ge0}M(w)^n,
\qquad
\widehat M=\bigoplus_{n\in\mathbb Z}(\widehat M)^n.
\]
In particular,
\[
(A_I^!)^n
=\prod_{w\ge0}
\operatorname{Hom}_{C_I}(B_{C_I}A_I(w),C_I)^n.
\]
Word series must have finite support in **total cochain degree**. Apply the same convention to \(Q_I\), completed cobar and tensor products, and the weightwise coefficient extension at lines 570–572.

This clarification has a useful consequence that should not be concealed: \(C_I\) is supported in degrees \(0,\ldots,|\mathcal O_I|-1\), and a state bar word of weight \(w\) has degree \(-w\). Degreewise weight completion adds no elements to \(B_{C_I}A_I\) or its dual in this particular model. It does add elements to cobar: \(\sum_{r\ge0}(t(sa_i))^r\) has degree zero. Arbitrary coefficient extension can also change the degreewise finiteness.

The independent mathematical checks were as follows.

- **Order charts and collision class, lines 27–146.** The varying-pair description gives consistent triple intersections. Equality-block restrictions give the claimed \(\prod_B|B|!\) branches. Face compatibility defines a commutative DGA. Restriction to an adjacent-exchange edge proves \([\omega_{ij}]\ne0\): an exact primitive would have polynomial endpoints with difference \(\pm(z_i-z_j)^{-1}\). This is impossible. No comparison with an unclaimed native chiral algebra was assumed.

- **Finite-state algebra, lines 150–194.** The coefficient signs respect degrees and differential. Every triple product of augmentation generators vanishes. Distinct \(b_{ij}\) and \(b_{ji}\) retain input order.

- **Bar and dual signs, lines 198–333.** Direct calculation gives
  \[
  b_2^2[v|w|u]
   =(-1)^{|w|}s((vw)u-v(wu)).
  \]
  For distinct \(i,j\),
  \[
  b[a_i|a_j]=-\omega_{ij}sb_{ij},\qquad
  (d\beta_{ij})(sa_i\otimes sa_j)=+\omega_{ij}.
  \]
  Since \((\alpha_i\alpha_j)(sa_i\otimes sa_j)=-1\), the asserted
  \(d\beta_{ij}=-\omega_{ij}\alpha_i\alpha_j\) is correct. Disjoint contractions cancel by parity; mixed internal terms vanish by Leibniz.

- **Leading face and contraction, lines 349–387.** The negative leading face is correct. For example,
  \[
  d(1\otimes[a_i|a_j])
  =-a_i\otimes[a_j]+1\otimes[\omega_{ij}b_{ij}],
  \]
  whose next differential vanishes. On \(a_i\otimes[a_j]\), the product contributions to \(dh+hd\) cancel and leave \(a_i\otimes[a_j]\). For a coefficient \(c\), suspension supplies the factor \((-1)^{|c|}\) required by the degree-minus-one \(C_I\)-linear contraction. Length zero leaves \(1-\iota\epsilon\), as claimed.

- **Semifree argument over nonfield DG coefficients, lines 389–411.** Freeness is over \(A_I\), on state words. Differentials of basis words use shorter words. For a closed map \(F:P_I\to N\) of degree \(n\), assume \(\partial H=F\) on shorter words. On a new basis word \(e\), the required right side
  \[
  F(e)+(-1)^{n-1}H(de)
  \]
  is a cycle. Acyclicity of \(N\) supplies its primitive. This constructs \(H\) without assuming that \(C_I\) is a field or exchanging ordinary and derived tensor products.

- **Multiplicative lift, lines 413–432.** With the usual composition convention,
  \[
  \widetilde{F*G}=\widetilde F\circ\widetilde G.
  \]
  On a three-piece cut \(p|u|v\), both sides normalize to \(pF(u)G(v)\) with exponent
  \[
  |G|(|p|+|u|)+|F||p|
  =(|F|+|G|)|p|+|G||u|.
  \]
  The right-comodule map respects the leading face. Evaluation by the augmentation recovers the functional. The semifree property makes postcomposition with \(P_I\to C_I\) a quasi-isomorphism; its multiplicative section is therefore a quasi-isomorphism.

- **Cobar reconstruction, lines 436–499.** The generator differential and counit signs agree. For a fixed original word \(v_1,\ldots,v_n\), encode each gap by \(0\) inside a bar block or \(1\) between cobar factors. Splitting gap \(k\) has exponent
  \[
  \sum_{\ell\le k}(|v_\ell|-1)+1+\sum_{\ell<k}\delta_\ell .
  \]
  The leftmost-gap contraction changes its state from \(1\) to \(0\) with coefficient \((-1)^{|v_1|}\), and is zero when that gap is already \(0\). It contracts the associated graded whenever \(n>1\), including after tensoring with DG coefficients. The filtration is finite at each weight. The counit is identity on the remaining one-letter summands and unit. Products preserve cohomology degreewise because kernels and images are computed coordinatewise and tuples of primitives can be chosen coordinatewise. This proves the intended completed theorem once its category is stated explicitly.

- **Three-point expansions and base change, lines 503–590.** The four charts are \(123,213,312,321\). With the indicated ordering convention,
  \(q_{\mathrm{in}}=t_{123}+t_{312}\) and
  \(q_{\mathrm{out}}=t_{123}+t_{213}\).
  The geometric series are inverses coefficient by coefficient in the declared Laurent-series rings, with a fixed lower exponent for each element. Localization, simplex face restriction, and differential commute. The ordinary bar base-change isomorphism follows on the free state-word basis. The completed dual is extended at finite weight before taking the degreewise limit. No unrestricted tensor–product interchange is needed.

The chapter introduces and motivates its principal constructions before they become load-bearing. Apart from the blocking completion wording, I found no required expository repair. Explicit formulas for the two \(q\)'s would help orientation but are not an additional blocker.

I inspected all eight individual page images, pages 159–166, and extracted the same PDF range. The pages are legible, equations are present, and no clipping, overlaps, unresolved references, or manuscript-firewall violations were found. PDF metadata contains no project-management or model-attribution material. The PDF has 173 pages.

:codex-file-citation{path="/Users/raeez/mathematics/worktrees/frontier-recovery-astra-ultra-20260913/reports/research/RECOVERY-2026-09-13/build-programme-008/programme.pdf" purpose="source"}

Source read boundaries were:

- `ordered-coefficient-bar.tex`: **all lines 1–590**.
- `ordered-collisions-and-residues.tex`: lines **1–92**, including the complete required two-branch construction and proposition.
- `relations-and-homotopies.tex`: lines **77–163, 366–523, 662–755**.
- `completion-and-coefficients.tex`: lines **170–244, 250–315, 765–816, 1440–1464**.
- Additional `rg` searches located conventions; they were not treated as full reads or acceptance of other chapters.

I read the ecosystem invariants, harness, mathematical-repair policy, applicable document policies, scoped mathematics instructions, and attack-heal skill/protocol. No previous mathematical verdict or proposed solution was consulted.

Read-only commands actually run included `cat` on those instructions, `rg --files`, targeted `rg -n` convention/firewall searches, `nl -ba … | sed -n …` for the boundaries above, `git status --short`, `git rev-parse HEAD`, `git branch --show-current`, `shasum -a 256`, two complete Python manifest/live/frozen hash checks, `pdfinfo`, `pdftotext -f 159 -l 166 -layout … -`, and `view_image` on the eight assigned PNGs. No build was run.

The exact finite calculations used Python **3.14.6**, integer arithmetic, and no third-party packages:

1. Over \(\mathbb Q[q]\otimes\Lambda(\theta,\eta)\), with \(dq=\eta\), \(d\theta=d\eta=0\), use states \(1,a_1,a_2,b_{12},b_{21}\), and \(a_1a_2=\theta b_{12}\), \(a_2a_1=\theta b_{21}\). Check \(d_P^2=0\) and \(dh+hd=1-\iota\epsilon\) for every leading state, bar word of length \(0,\ldots,4\), coefficient \(q^p\theta^{e_1}\eta^{e_2}\) with \(p=0,1,2\), and \(e_1,e_2=0,1\). **20,460 inputs passed.**
2. Using the displayed gap sign formula, check \(d^2=0\) and \(dh+hd=1\) for every word length \(2,\ldots,7\), each letter degree \(0\) or \(-1\), and every binary gap assignment. **10,920 inputs passed.**
3. Check the displayed convolution/composition exponent identity for every parity assignment to \(p,u,v,F,G\). **32 assignments passed.**

These finite checks corroborate the stated sign formulas; the general arguments above establish their scope. No proof assistant or external primary-source theorem was needed.

The residual obligation is confined to an explicit, consistently propagated degreewise-completion definition and review of the resulting new frozen bytes. Frozen008 remains unchanged and blocked.

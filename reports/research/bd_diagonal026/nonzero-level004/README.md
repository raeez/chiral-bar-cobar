# Native translated source

The mathematical include is `research-candidates/bd_diagonal026/nonzero-level004/translated-source-body.tex`. The wrapper is `translated-source.tex` in the same directory. Its labels use `bdtr-`, its only bibliography key is `BDtranslated`, and it uses the canonical shared mathematical style. The include is self-contained mathematical exposition.

The fixed target is the unextended Heisenberg chiral algebra at a scalar level k in C minus zero. The source is algebraic on the affine line. Coefficients are the actual global compatible families. Differential operators, normal expansions, and state polynomials are finite.

## Construction and exact boundary

The new source algebra contains actual translated ordered symbols a_i^(r) and b_ij^(r,s). The products differentiate the original kernel dq_ij/(z_i-z_j) with the input states. The first input translation produces both -dq_ij/(z_i-z_j)^2 b_ij and dq_ij/(z_i-z_j) b_ij^(1,0). Omitting the first term fails the translation derivation identity.

The resulting bar sectors P_2 and P_3 have explicit flat right differential actions and explicit differentials on every input jet. They are proved isomorphic to O_n tensor_R D_n by a triangular normal form. This identification explains the state translations and their differentiated products. It does not identify the source with its untranslated sector.

The binary and ternary graded maps use the exact integrals A(g)=integral g and M(g)=integral u g. In particular, q_g=pp(t p(t) A(g)); its factor t is required. The ternary mixed images are the binary edge images with their suspended spectator signs. A recursion specifies every translated image. An unmerged input jet maps to the corresponding actual T-translate of its native current.

The binary defect generates the vacuum delta module in degree -1. The ternary defect generates three pair modules M_ij in degree -2 and the small-diagonal current module S_3 in degree -1. All have explicit cyclic Weyl presentations. They have zero native differential on this regular outer coefficient domain. The source extension uses the defect itself as its new differential component and maps the added modules identically into the native target.

Minimality is exact within the stated category: fix the displayed graded map B, the splitting P directsum N, and the inclusion of N into the native target. Every added right differential submodule that absorbs QB-Bb contains the displayed N. This does not classify all source models or all choices of B.

The ternary map is global on X^3, including all three pair diagonals and the small diagonal. Its domain contains all six unmerged words, twelve mixed words, all actual compatible coefficients, and all finite input jets. The epsilon extension is absent. The level remains the original nonzero scalar. The old sources and their earlier local comparison are preserved.

The next compatibility is computed on the regular four-current collision subspace. A pair vacuum tensored with two currents has a nonzero differential to the complementary double-pair vacuum module. The source must include this map. The six signed pair terms cancel individually in the three double-pair channels, proving the four-current square-zero identity on that subspace. This is not a map on the full four-input coefficient complex.

## Cohomology

Translation first gives H(P_n)=H(O_n) tensor_R D_n, by the proved flat induction isomorphism. The native source extensions then have the following exact costs.

For the selected binary order, H^-2(O_2)=R(te+uh) directsum Rh. Its connecting map sends z=te+uh to k delta and h to zero. Thus H^-2 of the extended source is z(tD+partial_y D) directsum hD. All other cohomology groups agree with H(O_2) tensor_R D.

In the ternary sector, H^-3(O_3) has the eighteen displayed generators z_sigma, m_sigma, n_sigma. Put c=nu_12-nu_13+nu_23. The connecting image is generated by the six elements sign(sigma) p_sigma c. Their polynomials span the whole transverse quadratic ideal. Hence the image is K=(ct^2)D+(cts)D+(cs^2)D in M_3.

The quotient M_3/K is zero. Multiplication by the two other squared normal coordinates, then by t_12, isolates k delta_12 s^4 J_3. The generator also satisfies P=partial_1+partial_2 equal to zero, while [P,s]=1. Four successive commutations remove the power s^4. The two relabelings generate the other pair modules. This proves surjectivity onto M_3.

The closed mixed coefficient eta_12=dq_12/t_12^2 has q_eta=k/t^3+J^2/t. Its outer defect is -2k(J delta_123 partial_s+TJ delta_123). Multiplication by s gives the cyclic generator of S_3. Thus the H^-2 connecting map is surjective as well. The lower-pole coefficient dq_12/t_12 has only vacuum output and cannot serve as this witness.

Consequently the cohomology projection is injective in degrees -3 and -2, with respective cokernels M_3 and S_3. It is an isomorphism in all other degrees. The two affected groups are the kernels of the explicit connecting maps on H(O_3) tensor_R D. No coefficient formality theorem or quasi-isomorphism with O_3 is assumed. The right differential quotient is distinct from the earlier regular-function quotients of transverse lengths three and four.

## Evidence and limits

Run from the assigned worktree:

```sh
/opt/homebrew/bin/python3 reports/research/bd_diagonal026/nonzero-level004/check_exact.py
python3 reports/research/bd_diagonal026/nonzero-level004/build.py
python3 reports/research/bd_diagonal026/nonzero-level004/freeze.py
```

The exact calculation checks seventy endpoint families, seventy-two input translation identities, the first two inner pole cases, the oscillator outer contraction, the quadratic connecting matrix, pair isolation, normal jets, and all complementary four-current signs. Its finite bounds and dependency versions are recorded. These calculations support the displayed identities; the general arguments remain in the TeX. No proof assistant was used.

The primary carrier is Beilinson–Drinfeld, Section 3.4.11, equation (3.4.11.1), printed pages 182–183. The local primary PDF was inspected directly, and the author-hosted source was opened separately. The preserved PDF and exact input hashes are recorded in `input-freeze.json`. No novelty claim is made.

The requested actual003 and partial003 paths are absent from the assigned worktree. The dated custody item contains thirty-four files and no path matching actual003, partial003, or nonzero-level003. No missing partial result was treated as an input. All thirty-eight files in the frozen nonzero-level002 manifest remain byte-identical.

The intended mathematical controls remain gpt-6-astra and ultra. Independent observation of those runtime controls was unavailable. Mathematical acceptance of the frozen candidate requires fresh independent review of identical source bytes.

Residual obligations are the full four-input coefficient comparison, all higher partition operations, ordered-surjection identities, deconcatenation, and any resulting all-arity bar or coalgebra statement. Native integration into a book or another source requires its own exact-candidate review. No central PDF has been written, no source has been staged, and no commit or publication is part of this result.

# Ternary comparison: proof and scope

The assigned question is the missing actual ternary comparison from the ordered bar to the chiral collision complex. The fixed input is the Heisenberg vertex algebra over S = C[kappa], its Koszul extension W = V tensor Lambda(epsilon), and the binary intermediate assignment from the preserved 021 and 024 sources.

The target has five partition summands. Its differential includes all three pair collisions, their transverse localizations, and the full transfer-module normal derivatives. The source has the actual compatible-family coefficient algebra C_3, six unmerged words, and twelve mixed words. The new source does not repeat a finite ordered-bar acceptance claim.

## Results and dependency chain

1. The first section reconstructs the Heisenberg fields, the finite Wick expansion, the principal-parts operation, and its right differential module convention. The density normalization sends a/t^(r+1) to a delta partial_t^r/r!.
2. The five-partition carrier is the three-point specialization of Beilinson–Drinfeld Section 3.4.11, equation (3.4.11.1), printed pages 182–183. The source defines its internal and collision differential, with the three suspended unshuffle signs. The primary source was checked directly through the author-hosted PDF. Its exact downloaded bytes are preserved in inputs/cha_ch3.pdf.
3. Lemma `lem:bd26-scalar-homotopy` constructs the degree-minus-one operator inserting epsilon in the block containing label 1. It proves e^2 = 0, Q1 e + e Q1 = -kappa, and Q2 e + e Q2 = 0. The proof explicitly treats both binary block placements and all three ternary collision channels. This gives a right differential module contraction h = -e/kappa at nonzero level, preserving each partition summand.
4. The map B fixes the mixed intermediate images and sends an unmerged zero-form to its own vertex value times the separated three-current insertion. Positive-degree unmerged components of B are zero. The vertex choice is explicit and is not identified with both adjacent binary endpoints.
5. Theorem `thm:bd26-full-comparison` defines D = QB - Bb and F = B - hD. The proof uses QD + Db = 0 to derive QF - Fb = 0. It determines every source summand, including the unmerged words. The prior mixed small-diagonal formulas are unchanged. Formula `eq:bd26-unmerged-defect` gives the complete unmerged input formula in terms of explicit vertex evaluation, edge integration, and the three chiral collision maps.
6. For constant e_123, the new term is minus [epsilon delta_13 partial_t13 | J_2]. Its internal differential cancels the nonadjacent 13 collision. The correction has partial-diagonal support, so it escapes the exact support condition that caused the prior obstruction. It is polynomial in kappa.
7. Corollary `cor:bd26-equivariant` averages the three scalar contractions. The resulting map is equivariant under all label permutations. Its mixed components agree with F. A displayed Hom homotopy relates the averaged and distinguished-label maps.
8. Lemma `lem:bd26-global-divisibility` proves divisibility of every global mixed outer output by kappa, without a bound on its finite inner pole order. The decisive domain is the actual edge ring R[t^-1]. It has no outer denominator. Modulo kappa, the outer field with a regular coefficient has no pole. Transfer-module differentiation preserves this regularity. The quotient is unique because the principal-parts module is S-free.
9. Theorem `thm:bd26-polynomial` includes all global mixed coefficients and unmerged zero-forms whose two adjacent edge averages are normal-regular. Positive-degree unmerged coefficients remain unrestricted. This is an R-subcomplex, not a C_3-submodule. The map is polynomial and specializes at zero. The same holds for the permutation-equivariant average.
10. Proposition `prop:bd26-positive-form-obstruction` proves that the polynomial map cannot extend to all of O while retaining B's separated and mixed intermediate projections and killing positive-degree unmerged coefficients. Its witness is the actual global family q_12(1-q_12)/t. Its left edge average gives kappa/(6t^3) + J^2/(6t), while the right edge restriction and all vertex values vanish. The nonzero J^2 delta_12/6 coefficient survives modulo kappa. The next polynomial extension must allow nonzero positive-degree unmerged images or change other stated comparison data.
11. Proposition `prop:bd26-induced-comparison` constructs the right differential module source Ind(O) = O tensor_R D_R and its polynomial subcomplex. The map sends m tensor P to F(m)P. This states the source, connection, finite derivative normal forms, and all Weyl relations. It does not equip the original state symbols with a connection or identify Ind(O) with O.
12. The second include, polynomial-ternary-body.tex, constructs the positive-degree unmerged terms required by the global obstruction. The weighted integral M(g) = integral_0^1 u g gives v_g = pp(p M(g)). The unmerged one-form image is [v_L | J_k] - [J_i | v_R] on the two actual adjacent edges. Both edge coordinates equal 1 at the word's vertex. Integration by parts gives M(f') + A(f) = f(1), so both adjacent components of D^+(fe) cancel exactly at every finite inner pole order.
13. Proposition `prop:bd26-complete-defect` identifies the entire defect of B^+. On unmerged zero-forms it is only the nonadjacent current collision, hence divisible by kappa. On unmerged one-forms it is the outer collision of v_L minus that of v_R, also divisible by kappa because the outer coefficients are regular. Mixed zero-form defects vanish on this global domain. Mixed one-form defects have the previously proved divisibility. This checks all 18 source summands and all coefficient degrees.
14. Theorem `thm:bd26-polynomial-complete` defines the unique polynomial quotient T^+ = D^+/kappa and G = B^+ + e T^+. The proof derives QG - Gb = 0 directly over S from Qe + eQ = -kappa and QT^+ + T^+b = 0. No inverse of kappa is introduced into the source or target ring. The final global polynomial result applies to ALL of O, not only the regular-average subcomplex. Averaging the scalar insertion gives an S3-equivariant map, and induction gives a polynomial right differential module map.
15. Corollary `cor:bd26-native-zero` projects the level-zero state extension to the unextended V0. The induced native chiral comparison is B^+ with kappa = epsilon = 0. The displayed global cycle z = t12 t23 e123 + t23 q12 m12|3 + t12 q23 n1|23 maps to t12 t23[J1|J2|J3]. The image is nonzero, closed, and cannot be a boundary because E3(V0) has no degree-minus-four term. This is a nonzero native chiral cohomology comparison at level zero, not an equivalence claim.

## Preserved obligations and failed routes

The exact six source files from 021 and 024 are in inputs/prior-source-closure.zip. Their individual hashes and original paths are in input-freeze.json. They were not edited. Historical acceptance claims were not imported as mathematical evidence.

The binary vacuum-class obstruction still excludes a comparison preserving the specified projected insertion, R-linearity, and nonzero-level diagonal state cohomology. The new target is W, whose nonzero-level specialization is contractible. It is not a quasi-isomorphic replacement of the original Heisenberg states.

The old pure-insertion-plus-small-diagonal ternary ansatz fails on the open part of Delta_13 away from the adjacent diagonals. The new partial-diagonal term repairs that exact chain equation. It does not erase the old obstruction under its original hypotheses.

An unnecessary finite perturbation series was considered for the target contraction. The scalar insertion already anticommutes with every collision differential. The direct contraction therefore suffices. This is proved, not assumed from acyclicity.

The first unmerged formula at nonzero level uses the inverse of kappa. The global family q_12(1-q_12)/t shows the denominator cannot be removed from the specified zero-positive-form ansatz. The second chapter completes the positive-degree route: M(f') = -1/(6t) supplies exactly the opposite composite coefficient. The final G is polynomial on all global source coefficients. The first chapter and its exact obstruction remain valid and are preserved without byte changes.

Outer localization changes the coefficient domain. In the mixed domain allowing s^-1, the closed coefficient dq_12/(t^2 s) gives the five-term output in `eq:bd26-outer-obstruction`. Its cubic coefficient J^3/(ts) is not divisible by kappa. The global mixed divisibility theorem does not apply to this domain. Localizing a sheaf map, and admitting a new pole-valued input before taking a residue, are separate operations. No equivalence between those operations is asserted.

No assertion of deconcatenation compatibility, ordered-surjection compatibility, an all-arity morphism, or a four-input equation is made. Those are residual obligations. A four-input construction can now start from the full global polynomial map G. It must specify its coefficient domains and factorization restrictions before importing any operation requiring additional outer poles.

The induced right differential module comparison does not establish a connection on the original ordered state algebra. Any quotient imposing state translation must be written down, and its relations must lie in the kernel of the induced map. This is the exact next differential-module obligation.

## Verification and limits

`check_exact.py` checks the scalar contraction and differential signs on all 74 partition/order/scalar states. It also checks all eight abstract degree parities, all three distinguished-label contractions, and pairwise anticommutation. Each pair collision is checked separately. It does not replace chiral Jacobi by a scalar product or treat a finite matrix as the geometric proof.

Independent symbolic coefficient calculations check the global high-inner-pole output, the five-term outer-pole obstruction, the nonregular global edge average, the endpoint integration identity, and the noncommutative Hom correction identity. Thirty weighted-integral examples include genuine interior poles and check complete cancellation of the adjacent defect. The former J-squared obstruction and its new positive-degree image cancel exactly. The arithmetic is exact. Python and SymPy versions are recorded in exact-checks.json. No proof assistant was run.

The source is rendered with the shared canonical LaTeX style through TEXINPUTS. The inherited worktree symlink points to a missing sibling path, so the build uses the canonical style directory without modifying the symlink or copying the style. The recorder captures every loaded dependency and its hash.

The build and source inspections are diagnostic. They do not supply independent mathematical acceptance. A fresh exact-source review remains required. Required mathematical controls are gpt-6-astra with ultra reasoning. Independently observed runtime metadata is unavailable and remains unverified.

All writes are confined to research-candidates/bd_diagonal026 and reports/research/bd_diagonal026 in the assigned worktree. Nothing was staged, committed, pushed, published, or opened in a standalone PDF application.

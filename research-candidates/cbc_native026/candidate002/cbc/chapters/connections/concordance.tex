\chapter{Concordance with primary literature}
\label{chap:concordance}

\index{concordance|textbf}

\begin{remark}[Structural comparisons]\label{rem:constitutional}
The PBW concentration statement, the universal class $\Theta_\cA$, and the
categorical comparisons have different hypotheses.
Their formulations are given in Section~\ref{subsec:five-master-conjectures};
the homotopy constructions are stated in Section~\ref{subsec:homotopy-templates}.
The distinction between statements about complexes, their homotopy categories,
and scalar invariants is retained in each comparison.
\end{remark}

The book begins from two minimal objects because the same
configuration-space mechanism has two genuinely different
determinations.  On the commutative side, Heisenberg is the smallest
$\Einf$ atom in which Theorems~A--D are already visible in miniature.
On the braided side, Yangian is the smallest $\Eone$ atom in which
ordered factorization and braid reversal are forced.  The first
determination is theorematic on the Koszul loci of the proved core; the
second is theorematic on its DK core, with the extension beyond the
evaluation-generated locus as structural frontier.

\begin{remark}[PBW concentration and categorical comparison]
The following statements concern different parts of the construction:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{PBW concentration.}  Higher-genus PBW concentration is
  stated for the standard finite-type interacting families
  (Theorem~\ref{thm:master-pbw} together with
  Theorems~\ref{thm:pbw-allgenera-km} and
  \ref{thm:pbw-allgenera-virasoro} and
  \ref{thm:pbw-allgenera-principal-w}).
\item \emph{Universal class and categorical comparisons.}
  The universal class~$\Theta_\cA$ is now proved
  (Theorem~\ref{thm:master-theta}, resolved by
  Theorem~\ref{thm:mc2-full-resolution}).  The remaining frontier:
  the full DK/KL categorical lift
  (Conjecture~\ref{conj:master-dk-kl}), the infinite-generator
  comparison (Conjecture~\ref{conj:master-infinite-generator}),
  and the BV/BRST/bar identification
  (Conjecture~\ref{conj:master-bv-brst}).
\end{enumerate}
The formal statements and their hypotheses appear in
\S\ref{subsec:five-master-conjectures}.
\end{remark}

\begin{remark}[Two minimal objects]
Heisenberg and Yangian are the two smallest places where the same
geometry does genuinely different work.  In Heisenberg one already sees
Arnold nilpotence, Verdier duality, genus-$1$ curvature, and the genus
tower.  In Yangian one first encounters ordered factorization,
$R$-matrix inversion, and the Drinfeld--Kohno square.  The second does
not lie hidden inside the first, but neither does it define a different
subject.  The present chapter records the exact theorematic overlap and
the remaining frontier.
\end{remark}

\section{Principal contributions}\label{sec:principal-contributions}

\begin{enumerate}[label=\emph{(\arabic*)}]

\item \emph{Configuration space bar-cobar adjunction
  (Theorems~\ref{thm:bar-cobar-isomorphism-main}
  and~\ref{thm:higher-genus-inversion}).}
  The bar and cobar constructions for chiral algebras are realized
  via explicit integrals over Fulton--MacPherson compactifications
  $\overline{C}_n(X)$, with the bar differential given by residues
  at collision divisors and the cobar differential by inclusions
  of strata.  The adjunction is proved for all Koszul chiral algebras
  and all genera.  Theorem~A decomposes into three layers
  (Remark~\ref{rem:theorem-A-decomposition}):
  the fundamental theorem of chiral twisting morphisms
  ($\mathrm{A}_0$, Theorem~\ref{thm:fundamental-twisting-morphisms}),
  bar concentration
  ($\mathrm{A}_1$, Theorem~\ref{thm:bar-concentration}), and
  the Verdier-geometric duality
  ($\mathrm{A}_2$, Theorem~\ref{thm:bar-cobar-isomorphism-main}).
  This is the first complete proof of chiral Koszul
  duality via configuration space geometry.

\item \emph{Genus universality
  (Theorem~\ref{thm:genus-universality}).}
  For any Koszul chiral algebra~$\cA$, the genus-$g$ obstruction
  factors as $\mathrm{obs}_g(\cA) = \kappa(\cA) \cdot \lambda_g$,
  where $\kappa$ is an algebra-dependent constant and $\lambda_g$
  is the universal Faber--Pandharipande tautological class
  on~$\overline{\mathcal{M}}_g$.
  The genus-independent coefficient $\kappa(\cA)$ is determined
  by the genus-$1$ curvature.  This result is new.

\item \emph{Deformation-obstruction complementarity
  (Theorem~\ref{thm:quantum-complementarity-main}).}
  For a Koszul pair $(\cA, \cA^!)$, the genus-$g$ obstructions
  satisfy $\mathrm{obs}_g(\cA) + \mathrm{obs}_g(\cA^!) =
  [\text{characteristic class of } Z(\cA)]$ in $H^*(\overline{\mathcal{M}}_g)$.
  The Kodaira--Spencer map is constructed for all Koszul pairs
  and all genera (Theorem~\ref{thm:kodaira-spencer-chiral-complete}).
  This is the first formulation and proof of complementarity
  for chiral algebras.

\item \emph{$\Eone$-chiral Koszul duality
  (Theorem~\ref{thm:e1-chiral-koszul-duality}).}
  The bar-cobar adjunction extends to $\Eone$-chiral algebras
  (nonlocal vertex algebras with $R$-matrix braiding), covering
  Yangians and toroidal algebras.  This goes beyond the
  $\Einf$ (commutative/local) setting of BD and FG.

\item \emph{Explicit computations.}
  Complete Koszul duality computations for all standard families:
  Heisenberg, free fermion, $bc$-$\beta\gamma$, affine Kac--Moody
  ($\widehat{\fg}_k \leftrightarrow \widehat{\fg}_{-k-2h^\vee}$),
  Virasoro
  \textup{(}same-family complementarity partner
  $\mathrm{Vir}_{26-c}$ at M/S-level, with the
  $\mathcal{W}_\infty$ realization retained as programme\textup{)},
  $\mathcal{W}_N$ algebras, Yangians.
  Genus-$g$ free energies computed through $g = 10$ with
  Bernoulli asymptotics.  Bar cohomology dimensions through
  degree~$10$.  These explicit results are new.

\item \emph{Derived oper space from the bar complex
  (Theorem~\ref{thm:oper-bar}).}
  At the critical level $k = -h^\vee$, the bar complex is uncurved
  and recovers the derived space of
  $\fg^\vee$-opers on the formal disc:
  $H^n(\barB(\widehat{\fg}_{-h^\vee}))
  \cong \Omega^n(\mathrm{Op}_{\fg^\vee}(D))$.
  The $H^0$ identification gives the Feigin--Frenkel
  center~\cite{Feigin-Frenkel}; the $H^1$ identification
  gives K\"ahler differentials of the oper space; and the
  full derived identification follows from the bar-Ext
  identification combined with the
  Frenkel--Teleman theorem~\cite{FT06}.
  This provides a vertex-algebraic construction of a
  central ingredient in the geometric Langlands programme,
  independent of the categorical approach of
  Gaitsgory et al.~\cite{GLC24} and
  Raskin~\cite{Raskin24}.

\item \emph{Chiral Hochschild cohomology
  (Theorems~\ref{thm:w-algebra-hochschild},
  \ref{thm:virasoro-hochschild},
  and~\ref{thm:critical-level-cohomology}).}
  For the principal $\Walg$-algebra $\Walg^k(\fg)$ at generic level:
  $\ChirHoch^*(\Walg^k(\fg)) \cong \mathbb{C}[\Theta_1, \ldots, \Theta_r]$
  with $\Theta_i$ in degree~$m_i + 1$ (exponents of~$\fg$).
  Rank~$1$ (Virasoro): strict period~$2$.
  Rank $r \geq 2$: quasi-period $\operatorname{lcm}(m_1+1, \ldots, m_r+1)$.
  At critical level $k = \critLevel$:
  exterior$\otimes$polynomial structure
  $\Lambda^*(P_1, \ldots, P_r) \otimes \mathbb{C}[\Theta_1, \ldots, \Theta_r]$
  (Fuks--Feigin--Tsygan).
  Functorial under Koszul duality via the Connes
  operator~$S$ (Theorem~\ref{thm:main-koszul-hoch},
  Corollary~\ref{cor:hochschild-cup-exchange}).
  This promotes the Hochschild results to a
  principal contribution~(Theorem~H).

\end{enumerate}

\begin{remark}[Geometric Langlands connection]%
\label{rem:oper-langlands-concordance}
\index{geometric Langlands!oper from bar complex}%
\index{oper!concordance summary}%
Item~\emph{(6)} deserves emphasis.  The bar complex
$\barB(\widehat{\fg}_{-h^\vee})$ at critical level is a
\emph{derived enhancement} of the Feigin--Frenkel center: it
simultaneously encodes the commutative algebra of functions on
the oper space (in $H^0$), its cotangent complex (in $H^1$),
and higher derived structure (in $H^{\geq 2}$).  That the
bar construction---defined purely in terms of configuration
space integrals on curves---reproduces a key geometric
object in the Langlands programme
(Theorem~\ref{thm:oper-bar}) illustrates the reach of
chiral Koszul duality beyond abstract homotopy algebra.
\end{remark}

\begin{remark}[The twisting morphism as structural protagonist]
\label{rem:tau-protagonist}
\index{twisting morphism!as structural protagonist}
The canonical twisting morphism $\tau \colon \barB_X(\cA) \to \cA$
is the thread connecting the four main theorems
(Convention~\ref{conv:bar-coalgebra-identity}):
Theorem~A establishes $\tau$; Theorem~B inverts it;
Theorem~C identifies its genus-$g$ curvature; Theorem~D computes
its leading coefficient.
At higher genus, $\tau$ extends to a curved twisting cochain
$\tau_{\mathrm{mod}} = \tau + \Theta_\cA$
whose curvature $\kappa(\cA)\cdot\omega_g$ governs the entire
modular deformation tower
(Remark~\ref{rem:theta-modular-twisting}).
\end{remark}

\section{Relationship to Beilinson--Drinfeld}

\begin{tabular}{>{\raggedright\arraybackslash}p{0.38\textwidth}>{\raggedright\arraybackslash}p{0.52\textwidth}}
\textbf{Our Terminology} & \textbf{BD Terminology} \\
\hline
Chiral algebra & Chiral algebra (same) \\
$\Einf$-chiral algebra & Vertex algebra / Chiral algebra \\
$\Eone$-chiral algebra & No direct analog (generalization) \\
Chiral bracket $\mu$ & Chiral operation $\mu: j_* j^*(A \boxtimes A) \to \Delta_! A$ \\
Bar complex $\B(\cA)$ & Chiral chain complex (cf.\ Chevalley--Eilenberg for Lie case) \\
Factorization structure & Factorization algebra structure \\
Ran space $\Ran(X)$ & $\mathfrak{R}(X)$ in BD notation \\
\end{tabular}

\begin{remark}[Key extension]
Our $\Eone$-chiral algebras extend BD's chiral algebras by dropping skew-symmetry. BD's chiral algebras are our $\Einf$-chiral algebras.
\end{remark}

\section{Relationship to Francis--Gaitsgory}
\index{Francis--Gaitsgory}

\begin{tabular}{>{\raggedright\arraybackslash}p{0.38\textwidth}>{\raggedright\arraybackslash}p{0.52\textwidth}}
\textbf{Our Terminology} & \textbf{FG Terminology} \\
\hline
Chiral Koszul duality & Chiral Koszul duality (same) \\
$\chirCom$-$\chirLie$ duality & Main theorem of FG \\
$\chirAss$-$\chirAss$ duality & Not explicitly treated (implicit) \\
Pro-nilpotence & Pro-nilpotent tensor $\infty$-category \\
Bar-cobar equivalence & Koszul duality equivalence \\
\end{tabular}

\begin{remark}[Key extension]
FG establish $\chirCom$-$\chirLie$ duality. We show this is derived from the more fundamental $\chirAss$-$\chirAss$ self-duality via the deformation Pois → Ass.
\end{remark}

\section{Relationship to Gui--Li--Zeng}
\index{Gui--Li--Zeng}

\begin{tabular}{>{\raggedright\arraybackslash}p{0.38\textwidth}>{\raggedright\arraybackslash}p{0.52\textwidth}}
\textbf{Our Terminology} & \textbf{GLZ Terminology} \\
\hline
Quadratic chiral algebra & Quadratic chiral algebra (same) \\
Koszul dual $\cA^!$ & Quadratic dual $A^!$ \\
Bar complex & Not used (direct quadratic dual) \\
Non-quadratic duality & Not treated \\
$\Eone$-chiral algebras & Not treated \\
\end{tabular}

\begin{proposition}[GLZ as special case; \ClaimStatusProvedHere]\label{prop:glz-special-case}
The Gui--Li--Zeng quadratic duality is a special case of the $\chirAss$-duality of this monograph:
\[
\text{GLZ duality} = \chirAss\text{-duality} |_{\Einf\text{-chiral}} |_{\text{quadratic}}
\]
Restricting to $\Einf$-chiral algebras with quadratic presentations recovers their theory.
\end{proposition}

\begin{proof}
An $\Einf$-chiral algebra $\cA$ with quadratic presentation has a quadratic relation ideal $R \subset j_*j^*(\cA^{\boxtimes 2})$.  Its $\chirAss$ Koszul dual is determined by bar degrees $\leq 2$: the generators $V^*$ in $\bar{B}^1$ and the orthogonal complement of relations $R^\perp$ in $\bar{B}^2$ (the higher bar degrees, while nonzero, contribute no independent data for quadratic algebras).  The Koszul dual coalgebra $\bar{B}(\cA)$ is then determined by $R^\perp$, and the Koszul dual algebra $\cA^! = \bar{B}(\cA)^\vee$ is recovered by linear duality under the chiral pairing $j_*j^*(\cA^{\boxtimes 2}) \otimes j_*j^*((\cA^!)^{\boxtimes 2}) \to \omega_X$.  This is precisely the quadratic dual construction of GLZ.

For the $\Einf$ restriction: our $\chirAss$ bar complex uses the full associative chiral product $\mu\colon j_*j^*(\cA \boxtimes \cA) \to \Delta_!\cA$.  When $\cA$ is $\Einf$-chiral (i.e., the chiral bracket is skew-symmetric), the bar complex acquires a commutative coalgebra structure, and the Koszul dual is a Lie chiral algebra.  This recovers the FG framework as a special case (see Theorem~\ref{thm:fg-from-assch}).
\end{proof}

\begin{theorem}[FG duality from \texorpdfstring{$\chirAss$}{Ass-ch} self-duality; \ClaimStatusProvedHere]\label{thm:fg-from-assch}
The Francis--Gaitsgory $\chirCom$-$\chirLie$ duality is the associated graded of the $\chirAss$ self-duality under the PBW filtration.  Precisely: for a quadratic $\Einf$-chiral algebra $\cA$, the $\chirAss$ bar complex $\bar{B}_{\chirAss}(\cA)$ carries a filtration $F_\bullet$ (induced by the symmetrization degree of the $\Sigma$-action on $C_n(X)$) such that:
\begin{equation}\label{eq:fg-from-assch}
\mathrm{gr}^F \bar{B}_{\chirAss}(\cA) \;\cong\; \bar{B}_{\chirCom}(\mathrm{Sym}^{\mathrm{ch}}(\cA)) \;\cong\; \mathrm{CE}^{\chirLie}(\cA)
\end{equation}
and the associated spectral sequence degenerates at $E_1$ for Koszul algebras.
\end{theorem}

\begin{proof}
\emph{Step~1: PBW filtration.}
The chiral bar complex $\bar{B}^n_{\chirAss}(\cA)$ consists of sections on $\overline{C}_{n+1}(X)$ with coefficients in $\cA^{\boxtimes(n+1)} \otimes \Omega^n_{\log}$.  Define $F_p \bar{B}^n$ as the span of elements symmetric under at least $(n-p)$ transpositions of configuration space points.  This is the chiral analogue of the arity filtration on the classical associative bar complex (Loday--Vallette \cite{LV12}, \S9.3.2).

\emph{Step~2: Associated graded identification.}
On $\overline{C}_n(X)$, the $\Sigma_n$-representation decomposes as $j_*j^*(\cA^{\boxtimes n}) = \mathrm{Sym}^n_{\mathrm{ch}}(\cA) \oplus \bigwedge^2_{\mathrm{ch}}(\cA) \otimes \cdots$.  The PBW filtration separates these: $F_0/F_1 = \mathrm{Sym}^n_{\mathrm{ch}}(\cA)$ (the fully symmetric part) is the commutative chiral bar complex, and $F_1/F_2$ involves the Lie bracket (the antisymmetric part).  The associated graded $\mathrm{gr}^F \bar{B}_{\chirAss}$ is thus the Chevalley--Eilenberg complex for the Lie chiral algebra structure, which is $\bar{B}_{\chirCom}(\mathrm{Sym}^{\mathrm{ch}}(\cA))$ by the classical identification of the commutative bar complex with Chevalley--Eilenberg chains.

\emph{Step~3: Spectral sequence degeneration.}
For Koszul $\cA$, the spectral sequence $E_1^{p,q} = H^{p+q}(\mathrm{gr}^F_p \bar{B})$ degenerates at $E_1$.  To see this, note that the associated graded $\mathrm{gr}^F \bar{B}_{\chirAss}(\cA)$ is the Chevalley--Eilenberg complex (Step~2), and the Koszul property of $\cA$ as a chiral algebra implies that this commutative bar complex has cohomology concentrated on the diagonal $p + q = 0$ (i.e., $E_1^{p,q} = 0$ for $p + q \neq 0$).  Since $d_1\colon E_1^{p,q} \to E_1^{p+1,q}$ shifts the total degree $p+q$ by~$1$, it maps from the surviving diagonal to the zero region, hence $d_1 = 0$ and $E_2 = E_1$.  All higher differentials vanish for the same degree reason.  Therefore $H^*(\bar{B}_{\chirAss}(\cA)) \cong H^*(\mathrm{gr}^F \bar{B}_{\chirAss}(\cA))$, and the FG duality is recovered as the associated graded of our $\chirAss$ duality.

\emph{Step~4: Comparison with FG.}
The FG theorem states: for an $\Einf$-chiral algebra $\cA$, the chiral Koszul dual is a Lie chiral algebra $\cA^{!,\chirLie}$.  In the present framework: the Koszul dual coalgebra $\bar{B}_{\chirAss}(\cA)$ is an $\Eone$-chiral coalgebra, and the Koszul dual algebra $\cA^{!,\chirAss} = \bar{B}_{\chirAss}(\cA)^\vee$ is an $\Eone$-chiral algebra; its associated graded under PBW is $\cA^{!,\chirLie} = \bar{B}_{\chirCom}(\cA)^\vee$.  (The cobar $\Omega(\bar{B}_{\chirAss}(\cA))$ recovers~$\cA$ itself by bar-cobar inversion, not~$\cA^!$.)  The FG duality is recovered.
\end{proof}

\section{Relationship to Ayala--Francis}
\index{Ayala--Francis}

\begin{tabular}{>{\raggedright\arraybackslash}p{0.38\textwidth}>{\raggedright\arraybackslash}p{0.52\textwidth}}
\textbf{Our Terminology} & \textbf{AF Terminology} \\
\hline
Non-abelian Poincar\'{e} duality & Nonabelian Poincar\'{e} duality (same) \\
FM compactification $\overline{C}_n(X)$ & Disk stratification of $\mathrm{Ran}(X)$ \\
Verdier duality on $\overline{C}_n(X)$ & Poincar\'{e}/Koszul duality for $\Etwo$-algebras on oriented surfaces \\
Bar complex as factorization coalgebra & Factorization homology \\
\end{tabular}

\begin{remark}[Key comparison]\label{rem:ayala-francis-comparison}
The Ayala--Francis framework~\cite{AF15, AF20} establishes non-abelian
Poincar\'{e} duality as an equivalence between factorization homology and
factorization cohomology for framed $\mathsf{E}_n$-algebras on $n$-manifolds.
Our Theorem~A (Theorem~\ref{thm:bar-cobar-isomorphism-main}) is the
specialization to dimension~$n = 2$ (complex curves viewed as real
oriented surfaces) with the additional
structure of:
\begin{enumerate}[label=(\roman*)]
\item \emph{Holomorphic enrichment.}
  The AF framework works with topological $\mathsf{E}_n$-algebras and
  locally constant cosheaves.  We work with holomorphic chiral algebras
  ($\mathcal{D}$-modules on curves), where the Fulton--MacPherson
  compactification provides the correct holomorphic compactification
  (not just the topological one).  The bar differential involves
  residues of meromorphic forms, not just topological data.
\item \emph{Explicit formulas.}
  The AF duality is an $\infty$-categorical equivalence; our realization
  via configuration space integrals gives explicit chain-level formulas
  for the bar differential (residues at collision divisors) and the
  cobar differential (inclusions of FM strata).
\item \emph{Higher genus.}
  For genus $g \geq 1$ curves, the configuration space
  $C_n(\Sigma_g)$ acquires additional topology ($H^1(\Sigma_g)$),
  and the bar complex inherits new generators from the handle
  cycles.  The AF framework predicts this (factorization homology
  on non-simply-connected manifolds), but the explicit identification
  with moduli space integrals and the Hodge bundle (Theorem~C)
  is new.
\end{enumerate}
\end{remark}


\section{Relationship to Feigin--Frenkel}
\index{Feigin--Frenkel}

\begin{tabular}{>{\raggedright\arraybackslash}p{0.38\textwidth}>{\raggedright\arraybackslash}p{0.52\textwidth}}
\textbf{Our Terminology} & \textbf{FF Terminology} \\
\hline
KM Koszul dual $\widehat{\fg}_{-k-2h^\vee}$ & FF involution $k \mapsto -k - 2h^\vee$ \\
Critical level $k = -h^\vee$ & Critical level (same) \\
Curvature $\kappa \propto (k + h^\vee)$ & Center of $\widehat{\fg}_{-h^\vee}$ \\
DS reduction $\mathcal{W}^k(\fg)$ & Drinfeld--Sokolov reduction (same) \\
$c + c' = 2d$ & Not formulated \\
Genus-$g$ obstruction $\mathrm{obs}_g$ & Not formulated \\
\end{tabular}

\begin{remark}[Key comparison]\label{rem:feigin-frenkel-comparison}
The Feigin--Frenkel involution $k \mapsto -k - 2h^\vee$ on affine
Kac--Moody levels~\cite{Feigin-Frenkel} is, in this monograph's framework,
a \emph{consequence} of chiral Koszul duality: the bar-cobar adjunction on
$\overline{C}_n(X)$ produces $\widehat{\fg}_{-k-2h^\vee}$ as the Koszul
dual of $\widehat{\fg}_k$ (Theorem~\ref{thm:universal-kac-moody-koszul}).
The geometric mechanism is Verdier duality on the Fulton--MacPherson
compactification, which exchanges the chiral bracket
$\mu\colon j_*j^*(\cA \boxtimes \cA) \to \Delta_!\cA$
with the co-bracket on the Koszul dual.

At the critical level $k = -h^\vee$, the Feigin--Frenkel center
$\mathfrak{z}(\widehat{\fg}_{-h^\vee})$ is recovered as the degree-zero
homology of the (uncurved) bar complex: $\kappa = 0$ at $k = -h^\vee$,
so the bar complex is a genuine cochain complex, and
$H^0(\bar{B}(\widehat{\fg}_{-h^\vee})) \cong \mathfrak{z}(\widehat{\fg}_{-h^\vee})$
(Proposition~\ref{prop:km-generic-acyclicity}).

The extensions beyond FF are:
\begin{enumerate}[label=(\roman*)]
\item The complementarity formula $c(\widehat{\fg}_k) + c(\widehat{\fg}_{-k-2h^\vee}) = 2d$
  (Theorem~\ref{thm:central-charge-complementarity}), which shows the
  central charge sum is a root datum invariant, independent of the level.
\item The genus-$g$ obstruction formula $\mathrm{obs}_g = \kappa \cdot \lambda_g$
  (Theorem~\ref{thm:genus-universality}), which extends the FF involution
  to all genera via the Hodge bundle on $\overline{\mathcal{M}}_g$.
\item The Virasoro and $\mathcal{W}_N$ generalizations, where the
  Drinfeld--Sokolov parametrization introduces nonlinear level shifts
  (e.g., $c \mapsto 26 - c$ for Virasoro) that do not arise from a linear
  involution on levels.
\end{enumerate}
\end{remark}


\section{Relationship to Costello--Gwilliam}
\index{Costello--Gwilliam}

\begin{tabular}{>{\raggedright\arraybackslash}p{0.38\textwidth}>{\raggedright\arraybackslash}p{0.52\textwidth}}
\textbf{Our Terminology} & \textbf{CG Terminology} \\
\hline
Chiral algebra on $X$ & Holomorphic factorization algebra on $X$ \\
Bar complex $\B(\cA)$ & Factorization homology $\int_X \cA$ \\
Cobar construction $\Omega(C)$ & Factorization envelope \\
OPE residues & Observables in perturbative QFT \\
\end{tabular}

\begin{remark}[Key comparison]\label{rem:cg-comparison}
The Costello--Gwilliam factorization algebra framework~\cite{CG17}
relates to ours through the Beilinson--Drinfeld equivalence between
chiral algebras and factorization algebras on curves (BD, Chapter~3).
The precise comparison:
\begin{enumerate}[label=(\roman*)]
\item \emph{Factorization homology.}
  The CG factorization homology $\int_X \cA$ computes the same object
  as our chiral homology $H_*(\bar{B}^{\mathrm{ch}}(\cA))$.  Our explicit
  realization via configuration space integrals provides chain-level
  formulas that the CG framework treats abstractly.
\item \emph{Koszul duality.}
  The CG framework includes Koszul duality for factorization algebras
  (CG, Vol.~2, Chapter~5), specialized to holomorphic factorization
  algebras on curves.  Their Koszul duality, restricted to this setting,
  recovers our chiral Koszul duality.  Our contribution is the explicit
  identification of the Koszul dual for all standard families
  (Kac--Moody, Virasoro, $\mathcal{W}_N$, etc.) via the bar-cobar
  adjunction on Fulton--MacPherson spaces.
\item \emph{Quantum corrections.}
  The CG perturbative quantization framework produces $A_\infty$
  structures from Feynman diagram expansions (CG, Vol.~2, Chapter~3).
  Our curved $A_\infty$ structure on the bar complex is the
  chiral-algebraic counterpart: the curvature $m_0 = \kappa \cdot
  \mathbf{1}$ arises from the highest-order OPE pole (quartic for
  Virasoro, double for Heisenberg), not from a Feynman diagram sum.
  The genus universality theorem shows that these two perspectives
  produce the same genus-$g$ obstruction class.
\item \emph{BV-BRST.}
  Theorem~\ref{thm:cbc26-finite-bv} constructs a polynomial BV differential and its Koszul quotient map under a regular-sequence hypothesis.
  A comparison between field-theory observables and a chiral bar complex requires the actual observable complex, collision target, coefficient maps, coordinate descent, and a chain morphism.
  The filtered criterion of Theorem~\ref{thm:brst-bar-genus0} applies only after these data are supplied.
  The polynomial quotient map alone does not construct those curvewise data.
\item \emph{Monoidal bar-cobar.}
  Booth--Lazarev~\cite{BL24} construct monoidal model structures on
  categories of coalgebras, providing the homotopical infrastructure
  for the bar-cobar adjunction to be a Quillen equivalence in the
  monoidal setting.  This is realized by the fusion product preservation theorem
  (Theorem~\ref{thm:fusion-bar-cobar}): the bar construction
  is a monoidal functor from the tensor category of chiral
  algebra modules to the comodule category over the bar coalgebra.
\end{enumerate}
\end{remark}

\section{Relationship to Loday--Vallette}

\begin{tabular}{>{\raggedright\arraybackslash}p{0.38\textwidth}>{\raggedright\arraybackslash}p{0.52\textwidth}}
\textbf{Our Terminology} & \textbf{LV Terminology} \\
\hline
Operad & Operad (same) \\
Koszul operad & Koszul operad (same) \\
Bar construction $\B$ & Bar construction $\B$ (same) \\
Cobar construction $\Omega$ & Cobar construction $\Omega$ (same) \\
Twisting morphism & Twisting morphism (same) \\
Maurer--Cartan equation & Maurer--Cartan equation (same) \\
$\Ass^! \cong \Ass$ & $\Ass^! \cong \Ass \otimes \mathrm{sgn}$ (Thm 7.1.1) \\
$\Com^! \cong \Lie$ & $\Com^! \cong \Lie\{1\}$ (Thm 7.2.1) \\
\end{tabular}
\begin{remark}[Convention for Koszul dual isomorphisms]
In Loday--Vallette \cite{LV12}, \S7.1--7.2, the Koszul dual includes the sign representation: $\Ass^! = \Ass \otimes \mathrm{sgn}$ and $\Com^! = \Lie\{1\}$ (with operadic suspension). In this manuscript, we absorb the sign twist into the bar desuspension: our bar construction uses the shifted generators $s^{-1}\mathcal{A}$ whose sign already accounts for the $\mathrm{sgn}$-twist. Concretely, our convention $\Ass^! \cong \Ass$ means that the dual cooperad is isomorphic to $\Ass$ as a \emph{graded} cooperad after this shift. All bar-cobar computations in the text use the desuspended convention consistently, so no sign errors propagate. See Appendix~\ref{app:signs} for the explicit translation between conventions.
\end{remark}

\begin{remark}[Chiral enhancement]
We lift the entire LV framework to the chiral setting, replacing vector spaces with D-modules and tensor products with chiral tensor products.
\end{remark}

\begin{remark}[Synthesis]\label{rem:concordance-synthesis}
The common thread running through the comparisons above is that each
framework---Beilinson--Drinfeld's chiral algebras, Francis--Gaitsgory's
chiral Koszul duality, Costello--Gwilliam's factorization algebras,
Loday--Vallette's operadic Koszul theory---captures one projection of a
single underlying phenomenon: \emph{the duality between commutative and
Lie structures, mediated by configuration spaces}.  The contribution of
the present monograph is the explicit chain-level realization of this
duality via configuration space integrals, which makes all projections
simultaneously computable.  The bar complex $\bar{B}^{\mathrm{ch}}(\cA)$
is the meeting point: it is BD's chiral homology, FG's Koszul dual
coalgebra, CG's factorization homology on the Ran space, and LV's bar
construction---all incarnated as a single cochain complex with an
explicitly computable differential.
\end{remark}

\section{Conjecture stratification}
\label{sec:conjecture-stratification}
\index{conjectures!stratification|textbf}

The ${\sim}150$ conjectured claims in this monograph are not peers.
We stratify them into three tiers by the nature and difficulty of
the missing input.

\smallskip\noindent\textbf{Tier~1 --- Foundational.}
Conjectures whose resolution would extend the proved theorems
to new structural levels.  These require substantial new
mathematics:
\begin{itemize}
\item Coderived Ran formalism for non-Koszul chiral algebras:
  the categorical extension of Theorem~B off the Koszul locus.
\item Virasoro and $\mathcal{W}_{\infty}$ Koszul duals: explicit
  identification of the dual chiral algebras.
\end{itemize}

\smallskip
\noindent
The former higher-genus PBW concentration conjecture is now resolved
for the standard finite-type interacting families by
Theorems~\ref{thm:pbw-allgenera-km},
\ref{thm:pbw-allgenera-virasoro},
and~\ref{thm:pbw-allgenera-principal-w}.

\smallskip\noindent\textbf{Tier~2 --- Consequence.}
The following conjectures concern computations or extensions of the stated constructions:
\begin{itemize}
\item Derived Drinfeld--Kohno ladder beyond the theorematic
  evaluation-generated core:
  ordered $\Eone$-factorization with $R$-matrix inversion,
  factorization-level Kazhdan transport, and dg-shifted comparison.
\item Kazhdan--Lusztig from bar-cobar: the remaining stages
  beyond the proved chain-level framework.
\item Shifted-symplectic enhancement: upgrading the Lagrangian
  complementarity to a shifted symplectic structure on the
  moduli of Koszul pairs.
\item Fusion preservation under Koszul duality.
\end{itemize}

\smallskip\noindent\textbf{Tier~3 --- Physics horizon.}
Physics-facing conjectures that require input from quantum field
theory or string theory.  These are correctly conjectural within
a mathematics monograph:
\begin{itemize}
\item BV = bar identification for general field theories
  (beyond the holomorphic setting).
\item Path integral $=$ bar-cobar: the full Feynman--Koszul
  dictionary at all genera.
\item Holographic Koszul duality: AdS/CFT via the bar-cobar template.
\item $\mathsf{E}_n$ extension to higher-dimensional field theories.
\item AGT comparison via bar-cobar/Koszul duality.
\end{itemize}

\subsection{PBW concentration and categorical comparison problems}
\label{subsec:five-master-conjectures}
\index{master conjectures|textbf}

The PBW concentration theorem and the universal class $\Theta_\cA$ concern
different constructions.  The DK/KL enlargement and the infinite-generator comparison
require further categorical data; the BV/BRST comparison depends on those data.
Periodicity is a separate assertion from this chain of implications.

\begin{theorem}[Higher-genus PBW concentration for the standard
  finite-type interacting families]\label{thm:master-pbw}
\ClaimStatusProvedHere{}
The genus-$g$ PBW spectral sequence concentrates at all genera for the
three standard finite-type interacting families treated in the proved
core: affine Kac--Moody
\textup{(}Theorem~\textup{\ref{thm:pbw-allgenera-km}}\textup{)},
Virasoro
\textup{(}Theorem~\textup{\ref{thm:pbw-allgenera-virasoro}}\textup{)},
and principal finite-type $\mathcal{W}$-algebras, including higher
$\mathcal{W}_N$
\textup{(}Theorem~\textup{\ref{thm:pbw-allgenera-principal-w}}\textup{)}.

\emph{This resolves MC1 for the standard finite-type interacting
families.  Theorem~\textup{\ref{thm:pbw-universal-semisimple}}
isolates the common unique-weight-$2$ $d_2$ mechanism behind the
Virasoro and principal finite-type $\mathcal{W}$ arguments; for chiral
algebras with semisimple Lie symmetry satisfying
hypotheses~\textup{(a)--(c)}, the enrichment-killing is unconditional
via Whitehead vanishing plus $L_0$ invertibility.  The resolved status
of MC1 for the named families rests on the family theorems above;
the universal semisimple statement extends MK3 beyond those families
without further family-specific input.}
\end{theorem}

\begin{theorem}[Cyclic \texorpdfstring{$L_\infty$}{L-infinity} deformation algebra and
universal \texorpdfstring{$\Theta_\cA$}{Theta\_A} {\normalfont (MC2)}]%
\label{thm:master-theta}
\ClaimStatusProvedHere{}
For a modular Koszul chiral algebra~$\cA$ on a smooth projective
curve with non-degenerate invariant form and simple Lie symmetry,
there exists a cyclic
$L_\infty$-algebra $\operatorname{Def}_{\mathrm{cyc}}(\cA)$ and a
universal Maurer--Cartan element
$\Theta_\cA \in \operatorname{MC}(
  \operatorname{Def}_{\mathrm{cyc}}(\cA) \,\widehat{\otimes}\,
  R\Gamma(\overline{\cM}_{g,\bullet}, \mathbb{Q}))$
whose trace recovers $\kappa(\cA) \cdot \lambda_g$, whose clutching
compatibility recovers sewing, and whose Verdier duality
recovers complementarity.

\emph{Resolved by
Theorem~\textup{\ref{thm:mc2-full-resolution}}.
See Definition~\textup{\ref{def:full-modular-package}}.}
\end{theorem}

\begin{conjecture}[Staged factorization-categorical DK/KL extension]
\label{conj:master-dk-kl}
\ClaimStatusConjectured{}
The proved DK-2/3 equivalence on the evaluation-generated core,
equivalently on the theorematic domain
$D^b(\mathcal{O}_{\mathrm{poly}})$, extends to an
ordinary-derived/completed/coderived enlargement carrying the natural
DK-relevant subcategories.  On that enlargement, the ordered-factorization
and dg-shifted comparison stages extend the derived
Drinfeld--Kohno/Kazhdan--Lusztig correspondence beyond the current
evaluation-generated core.

\emph{This subsumes the DK-2 through DK-5 ladder of the monograph.
By Lemma~\textup{\ref{lem:fd-thick-closure}}, the thick-generation
target for DK-2/3 is $D^b(\mathcal{O}_{\mathrm{poly}})$, not the
full $D^b(\mathcal{O})$; extension to modules beyond the polynomial
subcategory requires either localizing generation in a completed
enhancement or enlargement of the generator set.  DK-4/5 concern the
RTT-complete dg-shifted comparison and the full triple bridge.  The
frontier is therefore enlargement beyond the theorematic core, not
another evaluation-locus comparison.}
\end{conjecture}

\begin{conjecture}[Filtered H-level comparison for standard infinite towers]
\label{conj:master-infinite-generator}
\ClaimStatusConjectured{}
For the standard principal and RTT towers, the theorematic completed
M-level packages are already in hand.  What remains is the H-level
comparison problem: construct a principal-stage compatible filtered
H-level target~$\mathcal{W}^{\mathrm{ht}}$ for the
$\mathcal{W}_\infty$ tower and a separated complete RTT-filtered dg
target~$\Ydg_{\cA}$ for the standard Yangian tower, with finite
quotients recovering the theorematic stages~$W_N$ and~$Y_{\le N}$, and
identify the resulting local coefficient systems with the standard
finite-stage data.

On the Yangian side, this reduces to the identities
\[
\mathsf{K}^{\mathrm{line}}_{a,b}(N)
=\mathsf{K}^{\mathrm{RTT}}_{a,b}(N)
\qquad (a,b\le N),
\]
detected on the boundary strip
$\{\Delta_{a,0}(N)\}_{0\le a\le N}$.
On the $\mathcal{W}_\infty$ side, this reduces to the identities
\[
\mathsf{C}^{\mathrm{res}}_{s,t;u;m,n}(N)
=\mathsf{C}^{\mathrm{DS}}_{s,t;u;m,n}(N),
\]
detected on the finite primary seed packet~$\mathcal{I}_N$.
Compatibility of these finite packets with the filtered quotient systems
would identify the H-level targets with the theorematic standard
completions and supply the infinite-tower comparison required by the
programme.

\emph{The live MC4 frontier is therefore filtered H-level realization,
exact coefficient identification, and finite detection, not another
generic completion-existence theorem.  It begins only after the
standard completed M-level packages are fixed.}

\emph{On the standard towers, the first unconditional
$\mathcal{W}_\infty$ packet is the exact stage-$4$ six-entry block
on~$\mathcal{I}_4$; on the additional stage-$4$ Ward-normalized
H-level locus of
Definition~\ref{def:winfty-stage4-ward-normalized}, its live content
contracts to the four higher-spin channels of
Corollary~\ref{cor:winfty-stage4-residue-four-channel}.}

\emph{On the
standard type-$A$ Yangian side, the local coefficient packet is already
closed on the factorization-side shared-seed locus.  The canonical
Yangian H-level target already exists as the tangent Lie algebra of the
factorization formal moduli problem
\textup{(}Proposition~\textup{\ref{prop:yangian-canonical-hlevel-target}}\textup{)},
and its canonical associative dg model already exists by
Proposition~\ref{prop:yangian-canonical-envelope}.  The live Yangian
MC4 move is to equip that canonical dg model with the RTT-adapted
finite-quotient and shared-seed package of
Proposition~\ref{prop:yangian-typea-realization-criterion}; once that
is done,
Corollary~\ref{cor:yangian-canonical-realization-to-spectral-seed}
reduces the remaining canonical-target input to the standard spectral
vector seed-and-shift datum, and on its equivariant multiplicative
spectral realization locus even that contracts to the realization
functor itself.}

\emph{On the spectral vector-line locus,
Corollary~\ref{cor:yangian-canonical-realization-plus-vector-line}
already closes DK-4/DK-5 on the canonical target; on the sharper
equivariant multiplicative spectral realization locus,
Corollary~\ref{cor:yangian-canonical-realization-plus-one-seed}
contracts this further to the single canonical spectral seed
\(V^\omega(0)=J_q^\omega(V(0))\), and
Corollary~\ref{cor:yangian-canonical-realization-to-single-line}
shows that under diagonal \(\mathfrak{sl}_M\)-equivariance and
highest-weight normalization the remaining local spectral packet there
contracts first to one ordered mixed-tensor line family and then, by
Corollary~\ref{cor:yangian-canonical-realization-to-exchange}, to one
scalar exchange coefficient family, which
Corollary~\ref{cor:yangian-canonical-realization-to-exchange-mult}
rewrites as one multiplicative-ratio scalar family and
Corollary~\ref{cor:yangian-canonical-realization-to-alt-mult} as one
antisymmetric-channel character there; and
Corollary~\ref{cor:yangian-formal-moduli-plus-core-realization}
then reduces the remaining categorical step to compact-core
realization of the proved finite-dimensional factorization DK core.}
\end{conjecture}

\begin{conjecture}[BV/BRST/bar identification]
\label{conj:master-bv-brst}
\ClaimStatusConjectured{}
For a holomorphic field theory on a Riemann surface, the BV/BRST
complex coincides with the bar complex of the associated chiral algebra,
at all genera.

\emph{This is the mathematical-physics bridge.  It is
downstream of Theorem~\textup{\ref{thm:master-pbw}},
Theorem~\textup{\ref{thm:master-theta}}, and
Conjectures~\textup{\ref{conj:master-dk-kl}}--\textup{%
\ref{conj:master-infinite-generator}}, and should not be placed on the
same logical level. The genus-zero comparison in Theorem~\ref{thm:brst-bar-genus0} and Corollary~\ref{cor:string-amplitude-genus0} requires its constructed target, relative descent, symbol map, compatible corrections, and pairing data.
The augmentation and relative-transition obstructions are Propositions~\ref{prop:vir-no-augmentation} and~\ref{prop:vir-relative-target-obstruction}.}
\end{conjecture}

\begin{remark}[Master conjecture hierarchy]\label{rem:master-conjecture-hierarchy}
Theorem~\ref{thm:master-pbw} is the \emph{entry theorem}: together
with Theorems~\ref{thm:pbw-allgenera-km},
\ref{thm:pbw-allgenera-virasoro},
and~\ref{thm:pbw-allgenera-principal-w},
it resolves MC1 for the standard finite-type interacting families.
Theorem~\ref{thm:pbw-universal-semisimple} packages the common
unique-weight-$2$ $d_2$ mechanism used in the Virasoro and principal
finite-type $\mathcal{W}$ proofs; under hypotheses~(a)--(b) alone, it
should be read as a criterion requiring separate later-page and
extension control.  Under the additional semisimple symmetry
hypothesis~(c), the enrichment-killing is unconditional.
Theorem~\ref{thm:master-theta} (by Theorem~\ref{thm:mc2-full-resolution}) constructs the H-level
completion of the modular characteristic hierarchy.
Conjectures~\ref{conj:master-dk-kl}
and~\ref{conj:master-infinite-generator} are
\emph{staged structural extensions}: the DK/KL enlargement beyond the
theorematic evaluation-generated core and the filtered H-level
comparison problem for the
standard infinite towers.
The exact dependency chain is:
\[
\begin{aligned}
\text{evaluation-generated core}
&\Longrightarrow \text{MC3 enlargement}\\
&\Longrightarrow \text{MC4 H-level comparison}\\
&\Longrightarrow \text{MC5 physics completion}.
\end{aligned}
\]
Conjecture~\ref{conj:master-bv-brst} is the
\emph{physics completion}: it depends logically on the others.
The master package maps to the nine futures
(\S\ref{subsec:nine-futures}) as follows:
Theorem~\ref{thm:master-pbw} $\to$ Comparison~2;
Theorem~\ref{thm:master-theta} $\to$ Comparisons~3, 4;
Corollary~\ref{cor:effective-quadruple} $\to$ Comparison~4
(quintuple $\to$ quadruple, proved for standard landscape);
Theorem~\ref{thm:cyclic-rigidity-generic} $\to$ Comparison~4
(quadruple universal at generic level via Whitehead--KL;
Conjecture~\ref{conj:scalar-saturation-universality} extends
to admissible levels);
Conjecture~\ref{conj:master-dk-kl} $\to$ Comparison~6;
Conjecture~\ref{conj:master-infinite-generator} $\to$ Comparison~3;
Conjecture~\ref{conj:master-bv-brst} $\to$ Comparison~9.
Comparisons~1, 5, 7, 8 are resolved or reformulated independently of the
remaining MC3--MC5 dependency chain.
\end{remark}

\begin{proposition}[Standard-tower MC5 reduction after MC3 and realized
MC4; \ClaimStatusProvedHere]
\label{prop:standard-tower-mc5-reduction}
Consider the standard principal
\texorpdfstring{$W_\infty$}{W_infty} tower together with the standard RTT
Yangian tower.  Assume:
\begin{enumerate}[label=\textup{(\roman*)}]
\item the Yangian side is enlarged beyond the theorematic
  evaluation-generated core as in
  Conjecture~\ref{conj:master-dk-kl};
\item the standard principal \texorpdfstring{$W_\infty$}{W_infty}
  MC4 comparison is in place: the target $\mathcal{W}^{\mathrm{ht}}$
  satisfies the quotient-system hypotheses of
  Proposition~\ref{prop:winfty-mc4-frontier-package}, and the exact
  packet identities on~$\mathcal{I}_N$ hold.  If one wants the first
  live stage-$4$ packet read on the four higher-spin locus rather than
  the unconditional six-entry locus, assume moreover that
  $\mathcal{W}^{\mathrm{ht}}$ is stage-$4$ Ward-normalized in the
  sense of Definition~\ref{def:winfty-stage4-ward-normalized};
\item the standard type-$A$ Yangian realized-MC4 package is in place:
  the hypotheses of
  Proposition~\ref{prop:yangian-typea-realization-criterion} hold, and
  the completed categorical step is closed by either
  Corollary~\ref{cor:yangian-typea-realization-plus-compacts} or,
  equivalently,
  Corollary~\ref{cor:yangian-typea-realization-plus-fundamental-packet};
\item actual genus-zero comparison data are supplied, with all target, relative-descent, section-carrier, filtration, symbol-map, correction, and pairing hypotheses of Theorem~\ref{thm:brst-bar-genus0} and Corollary~\ref{cor:string-amplitude-genus0};
  the higher-genus modular deformation package is that of Theorem~\ref{thm:master-theta}.
\end{enumerate}
Then no new infinite-tower algebraic obstruction remains on the
standard towers outside the higher-genus BV/BRST/bar identification
itself: the remaining MC5 content is exactly the genuswise comparison
between the realized H-level targets and the corresponding BV/BRST
complexes, compatibly with truncation, Verdier duality, and clutching.

\emph{This is a dependency theorem, not a closure theorem for the
packet identities themselves.  Its hypotheses are exactly the realized
MC3/MC4 inputs on the standard principal and RTT towers.  The
stabilized chain
\[
\text{MC3 enlargement}
\Longrightarrow
\text{MC4 realized H-level package}
\Longrightarrow
\text{MC5 BV/BRST comparison}.
\]
Its scope is only the standard principal and RTT towers; it does not
assert the same closure principle for non-principal orbit families or
other infinite-generator extensions.}
\end{proposition}

\begin{proof}
Assumption~\textup{(ii)} says that on the standard principal
\texorpdfstring{$W_\infty$}{W_infty} side the MC4 comparison has
already been reduced to exact stagewise packet identities on the finite
detection system~$\mathcal{I}_N$.  The additional
stage-$4$ Ward-normalized input of
Definition~\ref{def:winfty-stage4-ward-normalized}, when imposed,
changes only the presentation of the first live stage-$4$ block via
Corollary~\ref{cor:winfty-stage4-residue-four-channel}; it does not add
a further tower-wide algebraic obstruction once the packet identities
themselves are assumed.

Assumption~\textup{(iii)} says that on the standard type-$A$ Yangian
side the H-level dg-shifted target is already realized by
Proposition~\ref{prop:yangian-typea-realization-criterion}, and that
the remaining completed categorical packet closes by either
Corollary~\ref{cor:yangian-typea-realization-plus-compacts} or
Corollary~\ref{cor:yangian-typea-realization-plus-fundamental-packet}
or, on the spectral vector-line locus,
Corollary~\ref{cor:yangian-typea-realization-plus-dg-packet}.
Thus, after the MC3 enlargement of assumption~\textup{(i)}, no new
infinite-tower algebraic input remains on the standard Yangian tower.

Therefore the standard principal \texorpdfstring{$W_\infty$}{W_infty}
and standard type-$A$ Yangian towers contribute no remaining
infinite-generator algebraic obstruction beyond the realized MC3/MC4
packages.  The only further content is the genuswise BV/BRST/bar
comparison supplied by assumption~\textup{(iv)}, namely the passage
from the realized H-level targets to the corresponding BV/BRST
complexes with all actual genus-zero comparison and pairing data imposed in assumption~\textup{(iv)}, in the sense of Theorem~\ref{thm:brst-bar-genus0} and Corollary~\ref{cor:string-amplitude-genus0} and
Theorem~\ref{thm:master-theta}, compatibly with truncation, Verdier
duality, and clutching.  This is exactly the claimed MC5 reduction.
\end{proof}

\begin{corollary}[Compatible comparisons on the canonical Yangian
locus; \ClaimStatusProvedHere]
\label{cor:standard-tower-mc5-closure}
Consider the standard principal
\texorpdfstring{$W_\infty$}{W_infty} tower together with the Yangian
\[
\cA=\mathcal{Y}(\mathfrak{sl}_M),
\qquad
\mathfrak{g}_{\cA}:=T_*\mathfrak{F}_{\cA}.
\]
Assume:
\begin{enumerate}[label=\textup{(\roman*)}]
\item the Yangian side is enlarged beyond the theorematic
  evaluation-generated core as in
  Conjecture~\ref{conj:master-dk-kl};
\item the standard principal \texorpdfstring{$W_\infty$}{W_infty}
  packet identities on~$\mathcal{I}_N$ hold; if one wants the first
  live stage-$4$ packet read on the four higher-spin locus rather than
  the unconditional six-entry locus, assume moreover that the target is
  stage-$4$ Ward-normalized in the sense of
  Definition~\ref{def:winfty-stage4-ward-normalized};
\item the Yangian hypotheses of
  either Corollary~\ref{cor:yangian-formal-moduli-plus-core-realization}
  or Corollary~\ref{cor:yangian-canonical-realization-plus-vector-line}
  hold for the canonical H-level target $\mathfrak{g}_{\cA}$; and
\item actual genus-zero comparison data are supplied, with all target, relative-descent, section-carrier, filtration, symbol-map, correction, and pairing hypotheses of Theorem~\ref{thm:brst-bar-genus0} and Corollary~\ref{cor:string-amplitude-genus0};
  the higher-genus modular deformation package is that of Theorem~\ref{thm:master-theta}.
\end{enumerate}
Then the supplied comparison maps extend through the stated tower, compatibly with truncation, Verdier duality, and clutching.
\end{corollary}

\begin{proof}
Assumption~\textup{(iii)} supplies the standard type-$A$ Yangian
realized-MC4 package on the canonical formal-moduli target, via its
canonical associative dg model, by either
Corollary~\ref{cor:yangian-formal-moduli-plus-core-realization} or
Corollary~\ref{cor:yangian-canonical-realization-plus-vector-line}.
Therefore assumptions~\textup{(i)}--\textup{(iv)} imply the hypotheses
of Proposition~\ref{prop:standard-tower-mc5-reduction}.  That
proposition removes every remaining infinite-tower algebraic
obstruction beyond the genuswise BV/BRST/bar comparison, and
assumption~\textup{(iv)} supplies exactly that comparison.  Hence the
supplied comparison extends compatibly with truncation, Verdier duality, and clutching.
\end{proof}

\begin{remark}[Completion frontier crystallization]
\label{rem:completion-frontier-crystallization}
The completion frontier (Appendix~\ref{app:nilpotent-completion})
extends the strict finite-type core under three explicit hypotheses:
\begin{enumerate}[label=\textup{(H\arabic*)}]
\item finite $\mathcal{D}_X$-generation and polynomial growth of
  structure constants (verified for all Master Table families);
\item finite-dimensional Hochschild cohomology (verified for
  finite-type families by standard BGG/Shapiro arguments);
\item finite-stage bar-cobar compatibility: each truncation
  $\mathcal{A}/I^n$ lies on the Koszul locus (verified for
  finite-type families where the truncations are finite-dimensional,
  but not yet established in general).
\end{enumerate}
The proof-density remarks in that appendix
(Remarks~\ref{rem:completion-convergence-frontier},
\ref{rem:completed-bar-cobar-frontier},
\ref{rem:koszul-dual-characterization-frontier}) identify the
compressed steps.  Expanding~(H3) to a general finite-stage
compatibility lemma is the open problem separating the completion
frontier from full theorematic standing.
\end{remark}

\begin{remark}[Local conjecture dependencies]\label{rem:conjecture-dependencies}
\index{conjectures!dependency map}
The ${\sim}150$ individual conjectures scattered through earlier
chapters reduce to the master package as follows.
Each local conjecture carries a parenthetical annotation identifying
its master parent.
\begin{itemize}
\item \textbf{MC1} (Theorem~\ref{thm:master-pbw}):
  \emph{Resolved for the standard finite-type interacting families}
  (Theorems~\ref{thm:pbw-allgenera-km},
  \ref{thm:pbw-allgenera-virasoro},
  and~\ref{thm:pbw-allgenera-principal-w}).
  Theorem~\ref{thm:pbw-universal-semisimple} further isolates the
  common unique-weight-$2$ $d_2$ mechanism for nearby families with a
  stress tensor, including non-principal $\mathcal{W}$-algebras.
  Under hypotheses~(a)--(b) alone it remains a criterion requiring
  separate later-page and extension control; under the additional
  semisimple symmetry hypothesis~(c), the enrichment-killing is
  unconditional via Whitehead vanishing plus $L_0$ invertibility.
  Proposition~\ref{prop:pbw-e2-from-vacuum-module} shows the
  $E_2$~page is computable from the vacuum module's
  representation-theoretic data alone, making MC1 verification
  algorithmic for any simple~$\mathfrak{g}$.
  Remaining scope: genus-$0$ Koszulness~(MK1) and
  Verdier~(MK2) for non-principal families;
  the standard MC4 coefficient-identification package for
  $\mathcal{W}_\infty$ and Yangian towers
  (Conjecture~\ref{conj:master-infinite-generator});
  and the distinct non-principal orbit-duality transport problem
  (Conjecture~\ref{conj:w-orbit-duality}; exact remaining packets:
  dual-orbit input, orbit-indexed level shift $k'(k,f)$, and paired
  DS seed-complex transport/globalization beyond the current
  hook/subregular seeds; chain-level
  BRST evidence for all type-$A$ hook pairs through
  $\mathfrak{sl}_{20}$, Remark~\ref{rem:hook-type-evidence}).
\item \textbf{MC2} (Theorem~\ref{thm:master-theta}, resolved):
  All conjectures involving the universal Maurer--Cartan class~$\Theta_{\cA}$,
  the simple-Lie-symmetry H-level completion of the modular
  characteristic hierarchy,
  and index-theoretic genus-expansion predictions; periodicity
  synchronization problems depending on this deformation layer sit
  adjacent here but remain an orthogonal weak flank (${\sim}20$
  conjectures across Chapters~\ref{chap:higher-genus},
  \ref{chap:deformation-theory}, \ref{ch:genus-expansions}).
\item \textbf{MC3} (Conjecture~\ref{conj:master-dk-kl}):
  All DK/KL equivalences, Yangian Koszulness, factorization-categorical
  statements, and quantum-group comparisons (${\sim}15$ conjectures
  across Chapters~\ref{chap:kac-moody}, \ref{chap:yangians},
  \ref{ch:derived-langlands}).
  \emph{Core (proved):} DK-0 through DK-2/3 on the
  evaluation-generated core, equivalently on the theorematic domain
  $D^b(\mathcal{O}_{\mathrm{poly}})$, are resolved for all simple
  types, via two independent proofs: thick generation for
  type~$A$ (Theorem~\ref{thm:dk-fd-typeA}) and sectorwise
  convergence for all types
  (Corollary~\ref{cor:dk23-all-types}).
  Yangian Koszulness (Proposition~\ref{prop:yangian-koszul})
  uses Molev's PBW theorem \cite{molev-yangians} as external input.
  \emph{Extension (conjectural):} DK beyond the evaluation-generated
  core (full category~$\mathcal{O}$, KL completion) remains conjectural,
  organized by four extension conjectures
  (Remark~\ref{rem:corrected-mc3-frontier}(b)).
  Proposition~\ref{prop:yangian-dk-mc4-reduction}
  then makes the next dependency split explicit: after the DK-3 outer
  equivalence, the live remainder is the MC4 H-level comparison and its
  module-category enhancement, not another evaluation-locus
  comparison.
\item \textbf{MC4} (Conjecture~\ref{conj:master-infinite-generator}):
  All filtered H-level comparison problems for the standard
  $\mathcal{W}_\infty$/Yangian towers: construct the principal-stage
  compatible target~$\mathcal{W}^{\mathrm{ht}}$ and, on the Yangian
  side, equip the canonical associative dg model
  $U^{\mathrm{comp}}(\mathfrak{g}_{\cA})$ of the formal-moduli target
  with a separated complete RTT-filtered realization
  $\Ydg_{\cA}$, identify their finite quotients with $W_N$
  and~$Y_{\le N}$, and prove the exact mode
  identities
  $\mathsf{K}^{\mathrm{line}}_{a,b}(N)
  =\mathsf{K}^{\mathrm{RTT}}_{a,b}(N)$ and
  $\mathsf{C}^{\mathrm{res}}_{s,t;u;m,n}(N)
  =\mathsf{C}^{\mathrm{DS}}_{s,t;u;m,n}(N)$
  and their finite-detection packets
  $\Delta_{a,0}(N)$ and~$\mathcal{I}_N$; this also controls the
  toroidal/elliptic comparison extensions and the distinct
  derived/super infinite-generator extension flank
  (${\sim}10$ conjectures across Chapters~\ref{chap:free-fields},
  \ref{chap:w-algebras}, \ref{chap:yangians},
  \ref{chap:toroidal-elliptic}, \ref{chap:koszul-pairs}).  On the
  standard towers, the theorematic completed M-level packages are
  already in hand; the live frontier is the H-level target comparison
  and exact packet identification built on those packages.
  Corollary~\ref{cor:winfty-scalar-saturation} shows that
  $\mathcal{W}_\infty$ is scalar-saturated
  ($\dim H^2_{\mathrm{cyc}} = 1$), so the non-scalar universal
  class cannot be realized via the principal tower alone.
  On the
  standard type-A Yangian tower, Proposition~\ref{prop:yangian-dk4-typea-frontier}
  sharpens the Yangian side further to the auxiliary-kernel identity
  $L_a(u)=R_{0a}(u-a)$ and, under the standard normalization
  hypotheses, to transport of the dg-shifted or factorization
  one-factor line operator to the standard normalized mixed-tensor
  residue on the fundamental line.  Proposition~\ref{prop:dg-shifted-rtt-standard-typea-local-packet}
  shows that this local packet is already closed on the theorematic
  standard RTT tower, so the genuinely live Yangian input is the
  transport of the dg-shifted or factorization one-factor line
  operator to that standard normalized residue.
  Proposition~\ref{prop:dg-shifted-rtt-degree2-mixed-tensor-criterion}
  sharpens this once more: under one-loop exactness, the transport
  problem is already reduced to the ordered mixed-tensor coefficient of
  the degree-$2$ twisting morphism $r(z)=\tau|_{\deg 2}$.
  Corollary~\ref{cor:dg-shifted-rtt-seed-normalized-coefficient}
  removes the residue bookkeeping, and
  Proposition~\ref{prop:dg-shifted-rtt-degree2-scalar-normalization}
  removes the remaining one-loop matrix ambiguity: after
  degree-$2$ scalar normalization, the live Yangian packet is the
  single scalar coefficient of the permutation operator.
  Proposition~\ref{prop:dg-shifted-rtt-degree2-fundamental-casimir}
  computes that scalar as $-1$ once the degree-$2$ coefficient is
  identified with the standard fundamental Casimir insertion.
  Corollary~\ref{cor:factorization-fundamental-casimir-identity}
  proves that this identity already holds on the factorization side, and
  Proposition~\ref{prop:dg-shifted-factorization-shared-seed}
  transports it to any dg-shifted realization presented by the same
  ordered bar seed.  Once theorematic finite RTT quotients are added,
  Proposition~\ref{prop:yangian-typea-realization-criterion} upgrades
  this to the full H-level realization.  The remaining Yangian input is
  therefore no longer a local coefficient computation, but exact
  braided-monoidal realization of the standard quantum-loop vector
  seed-and-shift datum inside the spectral compact core,
  with the dg and ambient categorical sides already formal from the
  proved finite-dimensional factorization DK core and ind-completion.
  Proposition~\ref{prop:yangian-dk5-spectral-fundamental-packet}
  reduces the spectral side to the completed fundamental packet, and
  Proposition~\ref{prop:yangian-dk5-spectral-vector-packet}
  sharpens that to the ordered vector packet, while
  Proposition~\ref{prop:quantum-fd-vector-seed-shifts} constructs the
  standard source vector seed-and-shift datum and
  Proposition~\ref{prop:yangian-dk5-spectral-seed-shift-construction}
  makes it canonical on any realized spectral vector line,
  Proposition~\ref{prop:yangian-dk5-spectral-core-shifts}
  extends it to the full spectral compact core,
  Proposition~\ref{prop:yangian-dk5-spectral-factorization-shifts}
  makes it intrinsic on any multiplicative spectral realization of the
  compact core, and
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-vector-line}
  shows that realization of the standard vector line already forces
  the full spectral packet there,
  while
  Proposition~\ref{prop:yangian-dk5-spectral-factorization-core-from-vector-line}
  shows that on the ambient ordered factorization category the compact
  core itself is forced as the thick braided-monoidal closure of that
  line,
  while
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-vector-line}
  packages the whole spectral DK-5 side directly from that ambient
  vector-line realization,
  while
  Proposition~\ref{prop:yangian-dk5-spectral-factorization-seed-line}
  and
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-one-seed}
  and
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-schur-seed}
  sharpen this to one Schur-simple ambient vector seed together with
  the one-parameter trigonometric seed-pair identity on its dilation
  orbit, and under diagonal \(\mathfrak{sl}_M\)-equivariance and
  highest-weight normalization even that local packet contracts first
  to one ordered mixed-tensor line and then to one scalar exchange
  coefficient family descending to one multiplicative-ratio scalar
  family, equivalently to one antisymmetric-channel character there,
  while Corollary~\ref{cor:yangian-dk5-spectral-one-seed}
  packages the parallel compact-core seed route there via one
  compatible base vector seed,
  while
  Corollary~\ref{cor:yangian-dk5-spectral-seed-realization}
  packages the converse closure statement from that standard datum to
  the full spectral side, while
  Corollary~\ref{cor:yangian-dk5-spectral-packet-ind} constructs the
  ambient spectral category as
  \(\operatorname{Ind}(\mathcal{C}_q^\omega)\).
  Propositions~\ref{prop:yangian-dg-fundamental-packet-realization}
  and~\ref{prop:yangian-dk5-dg-extension-existence} construct the
  canonical dg completed fundamental packet and its exact extension to
  the dg compact core, and
  Corollary~\ref{cor:yangian-dk5-dg-packet-ind} makes the ambient dg
  category into \(\operatorname{Ind}(\mathcal{C}_Y^\omega)\) on the
  realized packet locus.  Thus the only remaining categorical input is
  realization of that standard spectral vector seed-and-shift datum; on
  the multiplicative spectral realization locus,
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-vector-line}
  contracts this to realization of the standard vector line itself,
  while
  Proposition~\ref{prop:yangian-dk5-spectral-factorization-core-from-vector-line}
  makes the compact core itself formal from that ambient vector-line
  realization,
  while
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-vector-line}
  packages the whole spectral DK-5 side directly from that ambient
  vector-line realization,
  while
  Proposition~\ref{prop:yangian-dk5-spectral-factorization-seed-line}
  and
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-one-seed}
  and
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-schur-seed}
  sharpen it further to one Schur-simple ambient vector seed together
  with the one-parameter trigonometric seed-pair identity on its
  dilation orbit, and under diagonal \(\mathfrak{sl}_M\)-equivariance
  and highest-weight normalization even that local packet contracts
  first to one ordered mixed-tensor line and then to one scalar
  exchange coefficient family descending to one multiplicative-ratio
  scalar family, equivalently to one antisymmetric-channel character
  there,
  while Corollary~\ref{cor:yangian-dk5-spectral-one-seed} gives the
  parallel compact-core seed route via one compatible base vector seed;
  on the
  equivariant multiplicative spectral realization locus of
  Corollary~\ref{cor:yangian-canonical-realization-to-spectral-seed},
  even that seed is canonical and the remaining input contracts to the
  realization functor itself.
  Corollary~\ref{cor:yangian-typea-degree2-plus-generators}
  packages the standard type-$A$ remainder exactly as one such
  degree-$2$ coefficient plus categorical packet, and
  Corollary~\ref{cor:yangian-dk5-core-realization}
  supplies the formal completed-level extension from that packet.
\item \textbf{MC5}
  (Conjecture~\ref{conj:master-bv-brst}):
  All BV/BRST identifications, Feynman-diagram
  conjectures, holomorphic-topological boundary
  conditions, AGT, Chern--Simons, and
  physics-dictionary statements
  (${\sim}20$ conjectures across
  Chapters~\ref{ch:feynman}, \ref{ch:bv-brst},
  \ref{ch:ht-boundary}, \ref{chap:physical-origins},
  \ref{ch:kontsevich-integral}).
  This is strictly downstream of MC3/MC4 and should not be read as a
  parallel structural frontier.
\item \textbf{Independent}: ${\sim}8$ genuinely open problems
  (reflected modular periodicity, derived bc-$\beta\gamma$, and a
  small residual list) not subsumed by any master conjecture.
\end{itemize}
The non-scalar universal class construction
(Construction~\ref{constr:non-scalar-hpt}) provides the formal
framework for MC4: once the filtered H-level target is built,
the HPT recursion~\eqref{eq:hpt-nonscalar-recursion} produces the
universal MC element automatically.  The concrete realization beyond
scalar---finding chiral algebras with
$\dim H^2_{\mathrm{cyc}} \geq 2$ and nontrivial mixed
brackets---is the live frontier
(Remark~\ref{rem:non-scalar-landscape}).
\end{remark}

\begin{remark}[Proof roadmaps]\label{rem:proof-roadmaps}
\index{master conjectures!proof roadmaps}
Detailed proof strategies for the master package:

\emph{MC1} (PBW concentration):
See Remark~\ref{rem:mk4-status}.  This item is now proved:
genus-$0$ PBW degeneration, genus-$g$ associated-graded
identification, and elimination of the higher-genus enrichment for the
standard finite-type interacting families.  The decisive mechanisms are
Whitehead plus Killing contraction for Kac--Moody, the $L_0$ scalar
action for Virasoro, and the triangular $d_2$ argument for principal
finite-type $\mathcal{W}$.
Theorem~\ref{thm:pbw-universal-semisimple} packages the common
unique-weight-$2$ $E_3$-page mechanism behind the Virasoro and
principal finite-type $\mathcal{W}$ proofs.  Under the additional
semisimple symmetry hypothesis~(c), the no-later-page and extension
arguments are supplied universally by Whitehead vanishing and $L_0$
invertibility; otherwise they remain family-specific.

\emph{MC2} (cyclic $L_\infty$ and $\Theta_{\cA}$):
\textbf{Fully resolved}
(Theorem~\ref{thm:mc2-full-resolution}).
All three hypotheses of the conditional completion
theorem are now proved:
\textup{(MC2-1)} the chiral graph complex construction
(Theorem~\ref{thm:cyclic-linf-graph}) equips
$\Defcyc(\cA)$ with a cyclic $L_\infty$-algebra structure
for all Koszul chiral algebras with non-degenerate invariant
form, using the Fay trisecant identity
(Proposition~\ref{prop:fay-trisecant}) and Stokes regularity
on FM~compactifications
(Proposition~\ref{prop:stokes-regularity-FM});
\textup{(MC2-2)} the geometric modular-operadic MC framework
(Proposition~\ref{prop:geometric-modular-operadic-mc}) provides
the convergent completed tensor product with clutching;
\textup{(MC2-3)} tautological-line support is established by
Theorem~\ref{thm:tautological-line-support}.
The universal MC element $\Theta_{\cA}$ exists with scalar trace
$\kappa(\cA)\cdot\lambda_g$, clutching compatibility, and Verdier
duality $\Theta_{\cA}\mapsto\Theta_{\cA^!}$.

Earlier infrastructure supporting the resolution includes: scalar shadow
$\kappa(\cA)$ (Theorem~D$_{\mathrm{scal}}$), determinant line bundle,
spectral discriminant, genus-$0$ cyclic coderivation complex
(Proposition~\ref{prop:genus0-cyclic-coderivation}) with explicit
Kac--Moody specialization
(Corollary~\ref{cor:km-cyclic-deformation}),
the cyclic CE~cohomology
$H^n_{\mathrm{cyc}}(\mathfrak{g},\mathfrak{g})\cong H^{n+1}(\mathfrak{g})$
(Proposition~\ref{prop:cyclic-ce-identification}),
the Killing $3$-cocycle $L_\infty$ extension
(Proposition~\ref{prop:killing-linf-extension}),
and cross-type universality confirming that
$\kappa = \dim(\mathfrak{g})(k+h^\vee)/(2h^\vee)$
is extracted from the same $L_\infty$ seed pipeline across
simply-laced ($A_1$, $A_2$) and non-simply-laced ($C_2$) types.

The genus-completed formal framework is established
(Proposition~\ref{prop:genus-completed-mc-framework}): genus
convolution yields a convergent, genus-stratified MC equation
whose obstruction at genus~$g$ lies in
$H^2(\Defcyc,l_1)\otimes R^*(\overline{\mathcal{M}}_g)$;
one-dimensionality of $H^2_{\mathrm{cyc}}$ reduces this to a
single tautological class
(Corollary~\ref{cor:one-dim-obstruction}).

Proposition~\ref{prop:mc2-reduction-principle} now packages this as a
formal reduction: once the intrinsic cyclic model and the geometric
completed tensor / clutching package are in place, MC2 becomes a
genus-stratified Maurer--Cartan problem with one obstruction channel
per genus in the simple-Lie case.

Propositions~\ref{prop:tautological-line-support-criterion},
\ref{prop:one-channel-verdier-criterion},
\ref{prop:one-channel-ptvv-criterion},
\ref{prop:one-channel-chain-model-criterion},
\ref{prop:one-channel-seed-criterion},
\ref{prop:one-channel-minimal-seed-packet-criterion},
\ref{prop:one-channel-visible-lowarity-packet-criterion}, and
\ref{prop:one-channel-normalization-criterion} further sharpen that
last package.  The entire reduction chain is captured by
Theorem~\ref{thm:mc2-conditional-completion}: if the cyclic model,
the geometric completed tensor product, and tautological-line support
hold, then $\Theta_{\cA}$ exists with scalar trace
$\kappa(\cA)\cdot\lambda_g$ and Verdier compatibility.

Once the joint clutching restrictions and normalized trace isolate the
tautological line, the remaining Verdier/Koszul side is the construction
of a $\sigma$-stable one-channel Lagrangian plane, its PTVV /
anti-involution lift on the H-level sector, and its explicit
realization by one-channel chain models in~$\Defcyc(\cA)$.

Those chain models are then reduced further to finite low-bar-length
bar-coderivation seeds inside
$\operatorname{CoDer}^{\mathrm{cyc}}(\widehat{\barB}_X(\cA))[1]$, then
to a minimal one-cocycle-plus-corrections packet in bar lengths at
most~$3$, then to the visible low-arity simple-pole / double-pole /
Killing-$l_3^\eta$ packet.

They are then reduced further to a canonical transfer package on
the one-channel seed spaces consisting of one cyclic seed, one shared
generator-seed lift, and one functorial normalization of the
simple-pole bracket images.

They are then reduced further to an explicit root-string transfer law
on those seed spaces with obstruction-side recovery of the
seed coefficients, and finally to a root-string chart forced on those
seed spaces up to rescaling of the three seed lines.

Those support lines are then detected intrinsically from the one-channel
data and shown to be the unique invariant lines for the one-channel
support automorphism group, hence the fixed vertices of a finite
one-channel support graph under the induced projectivized automorphism
action.

Finally one computes the singleton orbits of the visible root-string
incidence data, reduces that to one universal three-case orbit table,
and then to one canonical universal table forced by one invariant
signature packet, and then to one universal signed seed-character law,
and then to one universal two-sign plus normalization-scalar law, and
then to one root-string parity sign plus normalization scalar, and then
to one chart-normalized seed scalar.

At that point one scalar comparison with the proved
coefficient~$\kappa(\cA)$ fixes the genus-$g$ normalization.

What remains: \textbf{nothing}.  All three MC2 packages are
resolved (Theorem~\ref{thm:mc2-full-resolution}):
\textup{(MC2-1)} the chiral graph complex construction
(Theorem~\ref{thm:cyclic-linf-graph}) equips $\Defcyc(\cA)$ with a
cyclic $L_\infty$ structure using the Fay trisecant identity
(Proposition~\ref{prop:fay-trisecant}) and Stokes regularity
(Proposition~\ref{prop:stokes-regularity-FM});
\textup{(MC2-2)} the geometric modular-operadic MC framework
(Proposition~\ref{prop:geometric-modular-operadic-mc}) provides
convergent completed tensor with clutching via the Feynman transform;
\textup{(MC2-3)} tautological-line support
(Theorem~\ref{thm:tautological-line-support}).
The universal MC element $\Theta_{\cA}$ exists.
External ingredient: cyclic graph complex construction adapted to
chiral algebras.

\emph{MC3} (factorization-categorical DK/KL):
See Remark~\ref{rem:kl-evidence}.  What is proved: level-shifting
Koszul duality, module bar-cobar adjunction, KL multiplicities at
generic level, Hecke algebra Koszul duality.
On the Yangian side, DK-2/DK-3 are \textbf{resolved
for the evaluation-generated core at all simple types} via two
independent proofs (``unconditional'' means: not requiring thick
generation beyond type~$A$; Yangian Koszulness itself depends on
Molev's PBW theorem \cite{molev-yangians} as external input):
\textup{(a)}~thick generation for type~$A$
(Theorem~\ref{thm:dk-fd-typeA},
Proposition~\ref{prop:dk2-thick-generation-typeA},
Lemma~\ref{lem:monoidal-thick-extension}), and
\textup{(b)}~sectorwise finiteness with factorization descent for
all types (Corollary~\ref{cor:dk23-all-types}).
Moreover, Corollary~\ref{cor:dk-poly-catO} extends DK-2/3 to
the polynomial subcategory $\mathcal{O}_{\mathrm{poly}}$ at all~$N$,
via thick generation (Theorem~\ref{thm:catO-thick-generation})
and the Drinfeld classification.

As noted in Remark~\ref{rem:poly-O-natural-domain}, the polynomial
subcategory is the natural DK domain (modules outside
$\mathcal{O}_{\mathrm{poly}}$ have no quantum group counterpart),
so factorization DK in its natural form is
\textbf{unconditionally proved} for type~$A$.
The residual question is whether $\mathcal{O} = \mathcal{O}_{\mathrm{poly}}$
(Corollary~\ref{cor:dk-partial-conj}).

Extending DK beyond $\operatorname{Rep}_{\mathrm{fd}}$ to include
infinite-dimensional modules (prefundamental, asymptotic) requires a
completed/coderived enhancement, organized by four conjectural packages:
Baxter exact triangles
(Conjecture~\ref{conj:baxter-exact-triangles}),
shifted-prefundamental generation
(Conjecture~\ref{conj:shifted-prefundamental-generation}),
pro-Weyl recovery
(Conjecture~\ref{conj:pro-weyl-recovery}), and
DK on compacts extended by completion
(Conjecture~\ref{conj:dk-compacts-completion}).
The formal reductions (heart-capture
Proposition~\ref{prop:heart-capture-criterion}, standard-capture
Proposition~\ref{prop:standard-capture-criterion}, sectorwise
localizing generation
Corollary~\ref{cor:sectorwise-localizing-generation}) convert
the generation problem into sectorwise control of standard/Weyl
objects in finite truncation sectors.
See Remark~\ref{rem:corrected-mc3-frontier} for the precise
two-layer frontier.

On the KL side: the first M-level root-of-unity certificate is now in
hand.  For the Kapranov $q$-bar differential on
$\mathfrak{u}_q(\mathfrak{sl}_2)$, the structural identity
\[
d_q^N=0
\]
is directly computed in the small cases $N=2,3$ and then discharged in
the first larger root-of-unity cases $N=4,5$ by the quantum-binomial
certificate once the reduced bar spaces become too large to materialize
densely; moreover the reduced degree-$2$ vanishing
\[
d_q^2=0
\]
there is structural because
$d_q\colon B_1\to B_0$ is the zero map, while the genuinely
non-classical phenomenon
\[
d_q^2\neq 0
\qquad (N\ge 3)
\]
is still witnessed directly at bar degree~$\ge 3$ in the tractable
$N=3$ case.  The first degree-$4$ admissible packet is now resolved as
well:
\[
\dim H^{1,2}_2
=
\dim H^{2,1}_3
=
3
\]
by sparse $q$-bar elimination on~$B_4$.  The live KL frontier is
therefore no longer to show that a root-of-unity $q$-bar differential
exists, but to explain this first symmetric flavor packet and promote
that M-level data to the coderived/braided KL target.

Proof route: three-stage programme---first promote the admissible-level
$N$-complex certificate to the periodic/coderived KL object
\textup{(}Conjectures~\ref{conj:kl-periodic-cdg}
and~\ref{conj:kl-coderived}\textup{)}, then prove the braided monoidal
upgrade \textup{(}Conjecture~\ref{conj:kl-braided}\textup{)} from
fusion-tensor compatibility.

\textbf{Lattice route} (new):
Theorem~\ref{thm:lattice:homotopy-factorization-dk} gives an
\emph{unconditional} H-level $\Eone$-factorization category
equivalence for all even positive-definite lattices, bypassing
thick generation entirely (sectorwise finiteness replaces DK-2).
The level-$k$ extension
(Proposition~\ref{prop:lattice:level-k-factorization},
Theorem~\ref{thm:lattice:level-k-dk}) and quantum lattice DK
(Theorem~\ref{thm:lattice:quantum-factorization-dk}) extend this
to all levels and root-of-unity deformations.
Proposition~\ref{prop:lattice:yangian-bridge-level1} bridges to
the Yangian programme at level~$1$.  The full MC3 via lattices
reduces to the single quantum group identification
(Conjecture~\ref{conj:lattice:quantum-group-connection}(ii));
see Remark~\ref{rem:lattice:dk-summary}.

Computational verification: screening braiding matches quantum group
parameters for $A_2$ at $N = 3, \ldots, 9$ (quantum Serre relations,
module count $N^r$, and quantum binomial vanishing $[N]_\zeta = 0$
all verified).

Three external programmes bear directly on the MC3 frontier:
(i)~the pro-nilpotent completion formalism of
Francis--Gaitsgory~\cite{FG12} provides the categorical framework for
the completed/coderived environment needed beyond
$\operatorname{Rep}_{\mathrm{fd}}$;
(ii)~the factorisation quantum groups of
Latyntsev~\cite{Latyntsev23}, whose spectral $R$-matrices furnish the
target-side structure for the factorization DK equivalence; and
(iii)~the dg-shifted Yangians of
Dimofte--Niu--Py~\cite{DNP25}, whose line-operator construction
connects to the MC4 tower comparison
(Conjecture~\ref{conj:dg-shifted-yangian-rtt-package}).

\emph{MC4} (filtered H-level comparison for standard infinite towers):
What is proved: bar-cobar adjunction for every finite-type stage
(Theorem~A), filtered/complete recovery in the finite-type regime, and
truncation at each conformal weight level.  In addition,
Proposition~\ref{prop:mc4-reduction-principle} and
Corollaries~\ref{cor:mc4-degreewise-stabilization}
and~\ref{cor:mc4-surjective-criterion} reduce the inverse-limit part of
the story to explicit formal hypotheses.

For $\mathcal{W}_\infty$, this specialization is now formalized by
Proposition~\ref{prop:winfty-mc4-criterion} and
Corollary~\ref{cor:winfty-weight-cutoff}; together with
Proposition~\ref{prop:inverse-limit-differential-continuity}, this
gives Corollary~\ref{cor:winfty-standard-mc4-package}, so the standard
principal-stage tower now has its theorematic completed M-level
package.

For the standard RTT Yangian tower,
Corollary~\ref{cor:yangian-weight-cutoff},
Proposition~\ref{prop:yangian-rtt-completion-identification}, and
Corollary~\ref{cor:yangian-standard-mc4-package} do the same.
The live MC4 frontier is therefore the H-level comparison between these
standard completions and the dg-shifted/factorization-theoretic targets,
plus the exact coefficient packets on
$\mathcal{I}_N$ and~$\Delta_{a,0}(N)$, and only then extensions beyond
the standard truncation towers.

That remaining comparison is now formalized rather than rhetorical:
Proposition~\ref{prop:completed-target-comparison},
Corollary~\ref{cor:winfty-hlevel-comparison-criterion}, and
Corollary~\ref{cor:yangian-hlevel-comparison-criterion} reduce it to
constructing a separated complete H-level target whose finite quotients
recover the proved principal stages.

There are now two named construction packages rather than one blurred
frontier: Proposition~\ref{prop:winfty-factorization-package} asks for a
principal-stage compatible factorization target for $W_\infty$, and
Conjecture~\ref{conj:dg-shifted-yangian-rtt-package} asks for an
RTT-adapted filtration on the dg-shifted Yangian whose quotients recover
the finite RTT stages.

Their first attack routes are likewise explicit:
Remark~\ref{rem:winfty-factorization-route} reduces the
$W_\infty$ problem to stagewise factorization descent, while
Remark~\ref{rem:dg-shifted-quotient-route} reduces the Yangian problem
to pole-order dg quotients compatible with the finite RTT bar data.
Proposition~\ref{prop:winfty-quotient-system-criterion} and
Proposition~\ref{prop:dg-shifted-quotient-criterion} now isolate the
formal part of those routes: once the quotient systems are constructed,
the remaining comparison theorems apply automatically.

Proposition~\ref{prop:winfty-factorization-envelope-criterion} and
Proposition~\ref{prop:dg-shifted-rtt-presentation-criterion} push this
one step further by isolating the precise presentation-level and
chiral-envelope checks that would identify the quotient systems with the
proved principal stages.

The frontier has now been sharpened once again:
Proposition~\ref{prop:winfty-higher-spin-ideal-criterion} reduces the
$W_\infty$ side to stable higher-spin factorization ideals whose
quotients recover $W_N$, and
Proposition~\ref{prop:dg-shifted-rtt-locality-criterion} reduces the
Yangian side to a no-downward-propagation theorem for the RTT-level
line-operator formulas.  The current manuscript now pushes both of
those to the actual local formulas:
Proposition~\ref{prop:dg-shifted-rtt-formula-preservation} proves the
Yangian preservation statement from the rational line-operator kernels,
while Proposition~\ref{prop:winfty-spin-triangular-ideals} proves that
spin-triangular OPE formulas suffice for the principal-stage
factorization ideal system.

The remaining live step is therefore coefficient matching, not another
formal quotient theorem: on the Yangian side,
Proposition~\ref{prop:dg-shifted-rtt-coefficient-criterion} reduces the
finite-stage identification to the coefficientwise truncated RTT
relation and evaluation-module compatibility, while on the
$W_\infty$ side,
Proposition~\ref{prop:winfty-ds-coefficient-criterion} reduces the
stagewise quotient identification to agreement with the principal
Drinfeld--Sokolov OPE coefficients and the proved finite-stage bar
operations.

That reduction is now sharpened one step further.  On the Yangian side,
Proposition~\ref{prop:dg-shifted-rtt-kernel-coefficient-criterion}
shows that the actual remaining task is equality of the extracted
line-operator kernel coefficients with the truncated RTT coefficients,
together with the standard evaluation modules.  On the
$W_\infty$ side,
Proposition~\ref{prop:winfty-ds-local-coefficient-criterion} shows that
the actual remaining task is equality of the extracted OPE/residue
coefficients with the principal Drinfeld--Sokolov coefficients and the
finite-stage bar operations.

The frontier can now be stated mode by mode.  On the Yangian side,
Proposition~\ref{prop:dg-shifted-rtt-oneloop-kernel-criterion} reduces
the problem to the explicit identities
$\mathsf{K}^{\mathrm{line}}_{a,b}(N)=\mathsf{K}^{\mathrm{RTT}}_{a,b}(N)$
for all $a,b\le N$.  On the $W_\infty$ side,
Proposition~\ref{prop:winfty-ds-residue-identity-criterion} reduces
the problem to the explicit identities
$\mathsf{C}^{\mathrm{res}}_{s,t;u;m,n}(N)
=\mathsf{C}^{\mathrm{DS}}_{s,t;u;m,n}(N)$
for all spins and pole orders visible at stage~$N$.

Those mode identities are now reduced once more to finite detection
problems.  Proposition~\ref{prop:dg-shifted-rtt-evaluation-detection}
shows that the Yangian identities are detected on a faithful
evaluation family, while
Proposition~\ref{prop:winfty-ds-generator-seed} shows that the
$W_\infty$ identities are determined by the finitely many generator
residue coefficients together with translation closure.

\begin{remark}[Foundational route to the chiral Koszul dual of
\texorpdfstring{$W_\infty$}{W_infty}]
\label{rem:winfty-dual-construction-route}
From the \texorpdfstring{$W_\infty$}{W_infty} side, one should not ask
first for a quadratic presentation of an infinite-generator dual
algebra.  The theorematic standard tower already singles out the
correct completed M-level candidate:
\[
\widehat{\bar B}(W_\infty):=\varprojlim_N \bar B(W_N).
\]
What is missing is the H-level input that makes this candidate into a
genuine chiral dual object.  Accordingly the construction problem
factors into three exact steps:
\begin{enumerate}[label=\textup{(\roman*)}]
\item build a principal-stage compatible factorization/H-level target
  whose finite quotients recover the principal stages~$W_N$;
\item identify its local residue coefficients with the principal
  Drinfeld--Sokolov coefficients, finitely detected on the packets
  $\mathcal{I}_N$; and
\item apply the inverse-limit comparison theorems so that the
  completed bar-cobar object of that target is identified with the
  standard principal-stage package.
\end{enumerate}
This is the status-safe meaning of ``construct the chiral Koszul dual
of \texorpdfstring{$W_\infty$}{W_infty}'' in Volume~I: the missing input
is not another completion theorem on the standard bar side, but the
factorization realization and exact coefficient matching that promote
the completed bar candidate to an H-level dual object.
\end{remark}

The frontier is now finite at each stage $N$.  On the Yangian side,
Proposition~\ref{prop:dg-shifted-rtt-boundary-seed} reduces the defect
family to the boundary strip $\{\Delta_{a,0}(N)\}_{0 \le a \le N}$.
Proposition~\ref{prop:dg-shifted-rtt-finite-tensor-detection} sharpens
this again: it is enough to test those boundary defects on generic
tensor products of the fundamental evaluation module of lengths at most
$N+1$.  Proposition~\ref{prop:dg-shifted-rtt-fundamental-propagation}
pushes the formal reduction one step further: once the fundamental
$L$-operator satisfies the truncated RTT relation and the twisted
coproduct multiplies monodromy matrices in the standard way, the higher
tensor powers are automatic.

Proposition~\ref{prop:dg-shifted-rtt-auxiliary-kernel-criterion}
reduces the remaining Yangian check to the pairwise auxiliary-space
identity $L_a(u)=R_{0a}(u-a)$ on the fundamental line, after which the
fundamental RTT relation is the Yang--Baxter equation.  For the
standard type-A RTT tower,
Proposition~\ref{prop:dg-shifted-rtt-typea-auxiliary-uniqueness}
reduces even that pairwise step to three local checks:
$\mathfrak{sl}_M$-equivariance, unit asymptotic, and residue
normalization~$-\hbar P$.  Corollary~\ref{cor:dg-shifted-rtt-typea-residue-reduction}
then isolates the genuinely analytic input: once symmetry and
normalization are fixed by the line-operator construction, the only
remaining type-A check is the residue computation.
Proposition~\ref{prop:dg-shifted-rtt-typea-residue-channels} makes that
last step finite: it is enough to match the two
residue eigenvalues on the symmetric and antisymmetric channels of
$V\otimes V$.  Corollary~\ref{cor:dg-shifted-rtt-typea-single-line}
compresses this once more: it is enough to check the residue on the
single ordered tensor line $e_1\otimes e_2$.

\begin{remark}[MC4 Yangian residue in low rank]
\label{rem:mc4-yangian-computation-target}
\index{MC4!Yangian residue computation}
For the standard type-$A$ Yangian side of MC4, compare
$\mathsf{K}^{\mathrm{line}}_{1,2}(N)$ from the dg-shifted Yangian
line-operator construction at $N = 2, 3, 4$ with
$\mathsf{K}^{\mathrm{RTT}}_{1,2}(N)$ from the truncated RTT
relation.  The corollary above reduces the full RTT identification to
this single residue on $e_1 \otimes e_2 \in V \otimes V$.  If the
residues match, then $\mathfrak{sl}_M$-equivariance and the unit
asymptotic (both guaranteed by the line-operator construction) give
the full fundamental $L$-operator identity
$L_a(u) = R_{0a}(u-a)$ automatically.

\emph{Known cases.}
The three-layer reduction chain
(residue~$\to$ channels~$\to$ single line) has been verified
computationally for $M = 2, 3, 4$
(Computation~\ref{comp:mc4-three-layer-verification}).  The RTT truncation defect at boundary modes exhibits
the predicted algebraic structure
\[
D \propto a^{N+s-1}(\delta_{il}E_{jk} - \delta_{jk}E_{li}).
\]
In addition, the first auxiliary-kernel extraction itself has now been
carried out on the fundamental pair $(a,b)=(1,2)$ for $N=2,3,4$:
Computation~\ref{comp:mc4-yangian-lowrank-kernels} shows
\[
\mathsf{K}^{\mathrm{line}}_{1,2}(N)
=
\mathsf{K}^{\mathrm{RTT}}_{1,2}(N)
=
\Lambda^2(V)
\qquad
(N=2,3,4),
\]
with dimensions $1,3,6$ in these ranks
in the displayed ranks.

The live Yangian frontier is therefore no longer the
first low-rank extraction, nor a fresh local residue computation inside
the standard RTT tower.  After
Corollary~\ref{cor:yangian-typea-shared-seed-plus-generators}, the
remaining standard type-$A$ packet is the shared-seed transport to the
dg-shifted target together with the compact-generator comparison, while
the broader all-$N$ frontier is the uniform boundary-strip packet
\(\mathsf{K}^{\mathrm{line}}_{a,b}(N)
=
\mathsf{K}^{\mathrm{RTT}}_{a,b}(N)\)
and the subsequent filtered H-level target comparison.

Computation~\ref{comp:mc4-yangian-lowrank-propagation} gives further
low-rank evidence on the next formal step: for $N=2,3,4$ the
twisted-coproduct RTT propagation mechanism is verified on generic
tensor lengths $2$ and $3$, so no new obstruction appears on the first
short-tensor packet after the fundamental kernel extraction.
Computation~\ref{comp:mc4-yangian-boundary-strip-stage4} now reaches the
exact finite-detection packet itself on the same low-rank surface: in
ranks $2,3,4$, the stage-$4$ boundary strip
$\Delta_{a,0}(4)$ vanishes on generic tensor lengths $2$ and $3$ for
all $0\le a\le 4$.  The remaining Yangian frontier is therefore the
uniform all-rank passage from these low-rank evaluation-family checks
to the full boundary-strip family $\{\Delta_{a,0}(N)\}_{0\le a\le N}$,
followed by the filtered H-level target comparison.

Computation~\ref{comp:mc4-yangian-boundary-strip-stage6} now pushes the
same faithful-evaluation packet two stages farther out: in ranks
$2,3,4$, the stage-$6$ boundary strip $\Delta_{a,0}(6)$ also vanishes
on generic tensor lengths $2$ and $3$ for all $0\le a\le 6$.

At the theorematic level, the generic evaluation packet is now sharper:
Proposition~\ref{prop:dg-shifted-rtt-fundamental-coefficient-formula}
gives an exact subset expansion for the fundamental monodromy
coefficients, and
Corollary~\ref{cor:dg-shifted-rtt-boundary-support-bound} proves that
the line-side boundary coefficient on tensor length $m$ is supported on
at most $a+1$ quantum slots.

Proposition~\ref{prop:dg-shifted-rtt-defect-support-mechanism} upgrades
this to the defect side: RTT degree $a+1$ already forces the whole
boundary-strip defect to be controlled by subproducts of length at most
$a+1$.  Proposition~\ref{prop:dg-shifted-rtt-universal-generic-packets}
and Corollary~\ref{cor:dg-shifted-rtt-minimal-canonical-family} then
collapse those subproducts to one universal generic packet in each
length $1\le m\le a+1$.  Thus induction on tensor length reduces to the minimal canonical
family of universal packets.

Proposition~\ref{prop:dg-shifted-rtt-top-packet-induction},
Proposition~\ref{prop:dg-shifted-rtt-top-packet-line-formula}, and
Corollary~\ref{cor:dg-shifted-rtt-top-packet-comparison} sharpen this
once more: after the shorter packets are killed, the entire all-rank
step from length $a$ to length $a+1$ closes on the top-support class on
$M_{a+1}^{\mathrm{gen}}$, with an explicit parameter-free line-side
representative.

Proposition~\ref{prop:dg-shifted-rtt-top-packet-standard-discharge}
and
Corollary~\ref{cor:dg-shifted-rtt-top-packet-conditional-closure}
now remove the RTT top-support computation itself from the live
frontier: in the standard finite-stage evaluation realization, that
top-support class is forced to equal the same explicit operator and
hence vanishes.

Proposition~\ref{prop:dg-shifted-rtt-universal-evaluation-rigidity}
and
Corollary~\ref{cor:dg-shifted-rtt-top-packet-from-one-factor}
now remove that transport problem as an independent frontier: once the
one-factor universal packet carries the standard auxiliary kernel and
tensor powers are governed by the twisted coproduct, every universal
packet automatically carries the standard finite-stage evaluation
action.

Corollary~\ref{cor:dg-shifted-rtt-typea-single-line-bootstrap}
sharpens this once more on the standard type-$A$ tower: under the
twisted-coproduct, symmetry, and normalization hypotheses, the
universal-packet transport problem and the boundary-strip packet both
close once one proves that the dg-shifted or factorization one-factor
line operator carries the standard normalized mixed-tensor residue.
Under one-loop exactness,
Corollary~\ref{cor:yangian-typea-degree2-plus-generators} identifies
this local input with the single degree-$2$ coefficient identity
\[
r_a^{\mathrm{fund}}(z)(e_1\otimes e_2)
=
-\hbar(e_2\otimes e_1)
\]
on the ordered fundamental line.

Proposition~\ref{prop:dg-shifted-rtt-typea-uniform-single-line}
shows that this already determines the full residue operator
$\Xi_a=-\hbar P$ uniformly in the rank, and
Proposition~\ref{prop:dg-shifted-rtt-standard-typea-local-packet}
shows that this residue formula already holds on the theorematic
standard RTT tower.

The live Yangian gap is therefore no longer a missing local
computation inside the standard tower.  It is the transport of the
dg-shifted or factorization one-factor line operator to that standard
normalized residue, equivalently to the same mixed-tensor matrix
coefficient.  Under one-loop exactness,
Proposition~\ref{prop:dg-shifted-rtt-degree2-mixed-tensor-criterion}
reduces this further to the ordered mixed-tensor coefficient of the
degree-$2$ twisting morphism $r(z)=\tau|_{\deg 2}$.
Corollary~\ref{cor:dg-shifted-rtt-seed-normalized-coefficient}
removes the residue bookkeeping, and
Proposition~\ref{prop:dg-shifted-rtt-degree2-scalar-normalization}
shows that after degree-$2$ scalar normalization the only genuinely
live local datum is the scalar coefficient of the permutation
operator.

Proposition~\ref{prop:dg-shifted-rtt-degree2-fundamental-casimir}
shows that this scalar is forced to be $-1$ once the degree-$2$
coefficient is identified with the standard fundamental Casimir
insertion.  Corollary~\ref{cor:factorization-fundamental-casimir-identity}
proves that the factorization side already carries this coefficient,
and Proposition~\ref{prop:dg-shifted-factorization-shared-seed}
transports it to any dg-shifted realization presented by the same
ordered bar seed.  The genuinely live Yangian move is therefore the
H-level dg-shifted realization, not a new local coefficient
calculation.
\end{remark}

On the $W_\infty$ side,
Corollary~\ref{cor:winfty-ds-finite-seed-set} reduces the residue
comparison to the explicit finite index set $\mathcal{I}_N$ of primary
generator coefficients, and
Corollary~\ref{cor:winfty-ds-lowstage-seeds} makes the first live stage
completely concrete: beyond the theorematic Virasoro block, the
stage-$3$ comparison is exactly a packet of fifteen primary
coefficients.  Proposition~\ref{prop:winfty-ds-stage3-explicit-packet}
shows that this packet is already governed by the explicit $W_3$ OPE:
three nonzero primary coefficients and twelve forced vanishing
statements.

Proposition~\ref{prop:winfty-ds-stage4-residual-packet}
then shows that the stage-$4$ comparison is not the full
$\mathcal{I}_4$ packet of $54$ coefficients: after removing the exact
stress-tensor sector and the theorematic $(3,3)$ $W_3$ sector, the
residual higher-spin packet has exactly $29$ coefficients.

Corollary~\ref{cor:winfty-ds-stage4-top-pole-packet} sharpens this
once more: primaryity reduces those to `7` top-pole coefficients and
`22` forced zeros.  Corollary~\ref{cor:winfty-ds-stage4-parity-packet}
cuts this one step further: skew-symmetry kills the odd self-OPE entry
$(4,4,3,5)$, so the live stage-$4$ packet has `6` coefficients and
`23` forced zeros.

Corollary~\ref{cor:winfty-ds-stage4-ope-blocks} repackages those six
numbers as three local OPE blocks: one new $(3,3)\to 4$ coefficient,
three mixed $(3,4)$ coefficients, and two even self-coupling $(4,4)$
coefficients.  Corollary~\ref{cor:winfty-ds-stage4-mixed-self-split}
then isolates the exact mixed higher-spin frontier: the only genuinely
mixed stage-$4$ data are the three coefficients in the
$W^{(3)}$-$W^{(4)}$ block.

Proposition~\ref{prop:winfty-ds-mixed-top-pole-swap} and
Corollary~\ref{cor:winfty-ds-stage4-mixed-swap-parity} sharpen that one
step further: within the mixed block, the $W^{(3)}$ target channel is
swap-even while the $W^{(2)}$ and $W^{(4)}$ target channels are
swap-odd.

Proposition~\ref{prop:winfty-ds-mixed-virasoro-ds-zero} kills the mixed
$W^{(2)}$ target on the principal side, while
Corollary~\ref{cor:winfty-ds-stage4-self-t-normalization} fixes the
principal $W^{(4)}$-$W^{(4)}\to T$ coefficient to `2'.

The exact stage-$4$ frontier is therefore the exact six-entry identity
packet on
~$\mathcal{I}_4$:
\[
\mathsf{C}^{\mathrm{res}}_{s,t;u;m,n}(4)
=
\mathsf{C}^{\mathrm{DS}}_{s,t;u;m,n}(4)
\]
in the channels
\[
\begin{gathered}
(3,3;4;0,2),\qquad
(4,4;4;0,4),\\
(3,4;3;0,4),\qquad
(3,4;4;0,3),\qquad
(4,4;2;0,6),\\
(3,4;2;0,5).
\end{gathered}
\]

If the quotient residue calculus is stage-$4$ Ward-normalized in the
sense of Definition~\ref{def:winfty-stage4-ward-normalized}, then
Corollary~\ref{cor:winfty-stage4-residue-four-channel} contracts the
live packet further to the four higher-spin channels
\[
(3,3;4;0,2),\qquad
(4,4;4;0,4),\qquad
(3,4;3;0,4),\qquad
(3,4;4;0,3).
\]

Proposition~\ref{prop:winfty-mc4-frontier-package} packages this
entire W-side dependency chain into one exact MC4 frontier statement:
once the filtered H-level target with compatible finite quotients is in
place, each stage reduces to the finite seed packet
$\mathcal{I}_N$.

Proposition~\ref{prop:winfty-ds-stage-growth-packet} then
linearizes the promotion $\mathcal{I}_4\to \mathcal{I}_N$ as repeated
closure of the incremental interacting packets
$\mathcal{J}_{M+1}$; under primaryity and even self-OPE parity,
Corollary~\ref{cor:winfty-ds-stage-growth-top-parity} reduces those
further to the top-pole/parity packets
$\mathcal{J}_{M+1}^{\mathrm{red}}$.

The stage-$3$ packet is already fixed by the explicit $W_3$ OPE, and
the first unconditional stage-$4$ packet is exactly the displayed
six-entry identity block on~$\mathcal{I}_4$.

Proposition~\ref{prop:winfty-stage-growth-virasoro-target-contraction}
then shows uniformly that every reduced incremental packet
$\mathcal{J}_{M+1}^{\mathrm{red}}$ contracts under the stagewise
Virasoro-target normalization package by removing exactly its
target-$2$ Virasoro channels.

The first next downstream reduced stage is already explicit, and it
should be read as a transport problem rather than as a fresh
coefficient block:
Corollary~\ref{cor:winfty-ds-stage5-reduced-packet} identifies
$\mathcal{J}_5^{\mathrm{red}}$ as an eleven-entry packet,
Corollary~\ref{cor:winfty-stage5-residue-eight-channel} is precisely
the $M=4$ specialization contracting it to the explicit eight-channel
higher-spin packet
$\mathcal{J}_5^{\mathrm{hs}}$ of
Corollary~\ref{cor:winfty-stage5-higher-spin-packet}, and
Proposition~\ref{prop:winfty-stage5-local-attack-order} packages the
local order there as: entry packet first, then the target-$5$
corridor, then the remaining mixed transport ladders ordered by
targets $5$, $4$, $3$.
Proposition~\ref{prop:winfty-stage5-higher-spin-subblocks} then splits
that first higher-spin packet as $1+3+3+1$ by source pair.

Corollary~\ref{cor:winfty-stage5-entry-transport} isolates the
two-channel entry packet from the six-channel mixed transport packet.
Proposition~\ref{prop:winfty-stage5-entry-mixed-self} then resolves the
entry packet into the mixed-entry singleton $(3,4;5;0,2)$ and the
self-return singleton $(5,5;4;0,6)$, while
Proposition~\ref{prop:winfty-stage5-reduced-tail-singleton} identifies
that mixed-entry singleton as the entire reduced tail input at stage~$5$,
and Proposition~\ref{prop:winfty-stage5-tail-mechanism} identifies the
exact missing comparison mechanism there as the $W^{(5)}$-projection in
the top pole of $W^{(3)}(z)W^{(4)}(w)$.

Proposition~\ref{prop:winfty-stage5-higher-spin-target-blocks}
regroups the same higher-spin packet by target spin.
Corollary~\ref{cor:winfty-stage5-target5-corridor} then identifies the
first local three-channel strip as the target-$5$ corridor, whose first
step is exactly the singleton $(3,4;5;0,2)$, and
Corollary~\ref{cor:winfty-stage5-target5-residual} identifies the
residual continuation after that tail input as the two-channel ladder
$\mathcal{J}_5^{\mathrm{tr},5}$.

Proposition~\ref{prop:winfty-stage5-target5-transport-mechanism}
identifies the exact residual continuation as the comparison of the
$W^{(5)}$-projection in $W^{(3)}(z)W^{(5)}(w)$ and
$W^{(4)}(z)W^{(5)}(w)$.

Proposition~\ref{prop:winfty-stage5-target5-transport-singletons}
then splits that residual ladder into the pole-$3$ singleton
$(3,5;5;0,3)$ and the pole-$4$ singleton $(4,5;5;0,4)$.
Proposition~\ref{prop:winfty-stage5-local-attack-order} packages this
first stage-$5$ local frontier exactly as: entry packet first, then the
target-$5$ corridor, then the remaining mixed transport ladders ordered
by targets $5$, $4$, $3$.

Proposition~\ref{prop:winfty-stage5-visible-w5-normalization} makes the
visible $W^{(5)}$ normalization theorematic under the stage-$5$
Virasoro package.  On the visible pairing loci of
Propositions~\ref{prop:winfty-stage5-target5-pole3-pairing-vanishing}
--\ref{cor:winfty-stage5-tail-cross-target-reduction}, the same
target-$5$ strip becomes partially rigid: the pole-$3$ singleton
vanishes, the pole-$4$ singleton is tied to the self-return singleton,
and the tail singleton is tied to neighboring target-$4$/target-$3$
channels.

Corollary~\ref{cor:winfty-stage5-target5-corridor-to-tail}
then kills the entire target-$5$ transport part on the visible
$W^{(4)}$/$W^{(5)}$ pairing locus, and
Corollary~\ref{cor:winfty-stage5-target5-no-new-independent-data}
shows that on the full visible
$W^{(3)}$/$W^{(4)}$/$W^{(5)}$ pairing locus the whole target-$5$
corridor carries no new independent coefficient.

Propositions~\ref{prop:winfty-stage5-target4-pole5-w4-vanishing},
\ref{prop:winfty-stage5-target3-pole5-w3-vanishing}, and
\ref{prop:winfty-stage5-transport-cross-target-reduction} then collapse
the remaining target-$4$/target-$3$ transport front to one effective
coefficient, and
Corollary~\ref{cor:winfty-stage5-effective-independent-frontier} shows
that on that full visible pairing locus the whole stage-$5$
higher-spin packet carries one effective independent coefficient,
represented by the target-$4$ singleton \((3,5;4;0,4)\).

Conjectures~\ref{conj:winfty-stage5-principal-target5-no-new-independent-data}
and~\ref{conj:winfty-stage5-principal-residual-front-one-coefficient}
then split the remaining principal-side structural input into the
target-$5$ corridor and the residual front.  More precisely,
Conjecture~\ref{conj:winfty-stage5-principal-target5-transport-vanishing}
and
Conjecture~\ref{conj:winfty-stage5-principal-tail-cross-target-reduction}
form the target-$5$ staircase, while
Conjecture~\ref{conj:winfty-stage5-principal-residual-front-vanishings}
and
Conjecture~\ref{conj:winfty-stage5-principal-residual-cross-target-reduction}
form the residual-front staircase;

Proposition~\ref{prop:winfty-stage5-principal-one-coefficient-factorization}
packages their conjunction as
Conjecture~\ref{conj:winfty-stage5-principal-one-coefficient-normal-form},
and Proposition~\ref{prop:winfty-stage5-one-coefficient-reduction}
reduces the full visible-pairing stage-$5$ comparison to the single
identity of Conjecture~\ref{conj:winfty-stage5-one-coefficient-comparison},
while
Corollary~\ref{cor:winfty-stage5-visible-conjecture-network-collapse}
shows that, under those four principal structural steps, every
downstream local stage-$5$ conjecture is either automatic or equivalent
to that same singleton comparison.
\[
\mathsf{C}^{\mathrm{res}}_{3,5;4;0,4}(5)
=
\mathsf{C}^{\mathrm{DS}}_{3,5;4;0,4}(5).
\]
Corollary~\ref{cor:winfty-stage5-exact-remaining-input} packages the
exact remaining local input there as the four-step principal staircase
of target-$5$ transport vanishings, tail cross-target reduction,
residual vanishings, and residual cross-target reduction, together with
the single target-$4$ singleton match, and
Corollary~\ref{cor:winfty-stage5-one-defect-family} identifies the same
frontier defect-theoretically: on that visible pairing locus the whole
stage-$5$ higher-spin defect family on \(\mathcal{J}_5^{\mathrm{hs}}\)
collapses to one representative defect at \((3,5;4;0,4)\).
Corollary~\ref{cor:winfty-stage5-visible-defect-classes} sharpens the
same local picture further: every nonautomatic downstream stage-$5$
visible-pairing surface lies in one of the three exact defect classes
\(-\tfrac54 D_5\), \(D_5\), or \(-\tfrac34 D_5\), while the target-$5$
corridor and the self-block are automatic.

Proposition~\ref{prop:winfty-stage5-transport-target-ladders}
splits the mixed packet into three fixed-target ladders.
Proposition~\ref{prop:winfty-stage5-transport-pole-profiles} orders
those transport ladders by pole profile and identifies
$\mathcal{J}_5^{\mathrm{tr},5}$ as the lowest-pole next local surface.

Proof route: (1)~construct the filtered H-level target; (2)~prove $\mathsf{K}^{\mathrm{line}}_{a,b}(N) = \mathsf{K}^{\mathrm{RTT}}_{a,b}(N)$ on $\Delta_{a,0}(N)$ and $\mathsf{C}^{\mathrm{res}}_{s,t;u;m,n}(N) = \mathsf{C}^{\mathrm{DS}}_{s,t;u;m,n}(N)$ on $\mathcal{I}_N$; (3)~promote via finite-detection theorems; (4)~identify the target with the standard completion.  For Virasoro, the H-level realization is delegated to the $\mathcal{W}_\infty$ MC4 package: the stage-$4$ live task is the six-entry identity on $\mathcal{I}_4$, whose DS side is explicit and whose residue side requires the Ward-normalized input of Definition~\ref{def:winfty-stage4-ward-normalized} and Corollary~\ref{cor:winfty-stage4-residue-four-channel}.

\begin{remark}[MC4 \texorpdfstring{$\mathcal{W}_\infty$}{W-infinity} stage-4: concrete computation
target]\label{rem:mc4-winfty-computation-target}
\index{MC4!W-infinity stage-4 computation}
The minimal computation that would close the $\mathcal{W}_\infty$ side
of MC4 at stage~$4$ is no longer extraction on the principal
Drinfeld--Sokolov side.  That side is already explicit.  The live
stage-$4$ task is to compare the six bar-side seed coefficients on
$\mathcal{I}_4$ with the principal values in the channels
\[
(3,3;4;0,2),\quad
(4,4;4;0,4),\quad
(3,4;3;0,4),\quad
(3,4;4;0,3),\quad
(4,4;2;0,6),\quad
(3,4;2;0,5).
\]
The stage-$3$ packet is already determined by the explicit $W_3$ OPE
(Proposition~\ref{prop:winfty-ds-stage3-explicit-packet}), so the
stage-$4$ extension reduces to the exact vanishing problem for the six
defects
\[
\mathsf{D}_{s,t;u;m,n}(4)
:=
\mathsf{C}^{\mathrm{res}}_{s,t;u;m,n}(4)
-
\mathsf{C}^{\mathrm{DS}}_{s,t;u;m,n}(4),
\]
equivalently to these six identities
\[
\mathsf{C}^{\mathrm{res}}_{s,t;u;m,n}(4)
=
\mathsf{C}^{\mathrm{DS}}_{s,t;u;m,n}(4).
\]
The two Virasoro-target residue channels
\[
\mathsf{C}^{\mathrm{res}}_{4,4;2;0,6}=2,
\qquad
\mathsf{C}^{\mathrm{res}}_{3,4;2;0,5}=0
\]
are exactly the channels singled out by the normalized residue
two-point/Ward package.  Failure of either identity would force
revision of the factorization-target construction.  Once the visible
Virasoro Ward action is fixed,
Proposition~\ref{prop:winfty-stage4-visible-pairing-gap} identifies the
exact remaining missing input as the single visible weight-$4$
self-pairing normalization, equivalently the scalar identity
\[
\mathsf{C}^{\mathrm{res}}_{4,4;2;0,6}(4)=2.
\]
Mixed-weight orthogonality is already forced by
Proposition~\ref{prop:winfty-stage4-visible-orthogonality}, and the
visible $W^{(3)}$ self-normalization is already theorematic by
Proposition~\ref{prop:winfty-stage4-visible-w3-normalization}.  The
remaining weight-$4$ input is isolated in
Conjecture~\ref{conj:winfty-stage4-visible-diagonal-normalization} and
packaged by
Conjecture~\ref{conj:winfty-stage4-ward-inheritance}; under the visible
Virasoro package,
Corollary~\ref{cor:winfty-stage4-single-scalar-equivalent} packages
this as the exact theorematic form of the stage-$4$ four-channel
refinement.  On the additional visible pairing locus,
Corollary~\ref{cor:winfty-stage4-primitive-transport-square-triple}
reduces the signless higher-spin frontier to the
primitive-plus-transport square triple, and
Corollary~\ref{cor:winfty-stage4-visible-borcherds-two-primitive}
shows that once the visible swap-odd transport square is imposed the
stage-$4$ square-class packet has the same two-primitive profile as
the principal Drinfeld--Sokolov side.
\emph{This principal-side extraction is now complete:}
Proposition~\ref{prop:w4-ds-ope-explicit} gives the first four
principal targets as explicit rational functions of~$c$
\textup{(}Computation~\ref{comp:w4-ds-ope-extraction}\textup{)}, and the remaining two channels are verified
exactly.

\emph{Structural closure.}
The complete reduction chain
\[
\mathcal{J}_4\;(29) \to \mathcal{J}_4^{\mathrm{top}}\;(7)
\to \mathcal{J}_4^{\mathrm{par}}\;(6)
\]
\[
\mathcal{J}_4^{\mathrm{par}}
= 4\text{ higher-spin channels} + 2\text{ Virasoro-target identities}
\]
has been verified computationally
(Computations~\ref{comp:winfty-stage4-seed-verification}
and~\ref{comp:winfty-stage4-reduction-chain}):
seed set cardinalities, residual decomposition ($2+12+15$),
primaryity elimination ($22$ sub-leading), parity compression
(removal of $(4,4,3,5)$), OPE block organization ($1+3+2$),
mixed-self split ($3+3$), universal $T$-coupling
$\mathsf{C}_{s,s;2;0,2s-2}=2$ for $s=2,3$, complementarity sum
$c(k)+c(-k-8)=246$, and the two DS-side
theorematic Virasoro-target values
$\mathsf{C}^{\mathrm{DS}}_{4,4;2;0,6}=2$,
$\mathsf{C}^{\mathrm{DS}}_{3,4;2;0,5}=0$ all confirmed.

The stage-$4$ structural analysis and the principal DS extraction are
complete; the sharp frontier for the $\mathcal{W}_\infty$ side of MC4
is vanishing of the six exact stage-$4$ defects on $\mathcal{I}_4$,
equivalently the six bar-vs.-DS identities there, and only after that
their uniform promotion through the finite-detection packets
~$\mathcal{I}_N$, whose first next downstream reduced stage is the
explicit eleven-entry packet $\mathcal{J}_5^{\mathrm{red}}$.
\end{remark}

\begin{remark}[Shared completed environment for MC3 and MC4]
\label{rem:mc3-mc4-shared-completion}
MC3 beyond $\operatorname{Rep}_{\mathrm{fd}}$ and MC4 stabilization
both require working in completed or pro-nilpotent categories: MC3
needs a coderived enhancement to accommodate prefundamental and
asymptotic modules, while MC4 needs separated completions of the
infinite-generator towers $\mathcal{W}_\infty$ and $Y(\mathfrak{sl}_\infty)$.
The pro-nilpotent formalism of Francis--Gaitsgory~\cite{FG12} provides
the common categorical framework for both programmes.
\end{remark}

\emph{MC5} (BV/BRST at all genera):
The Virasoro charge and its square are given in Theorem~\ref{thm:vir-square}.
The genus-zero comparisons of Theorem~\ref{thm:brst-bar-genus0} and Corollary~\ref{cor:string-amplitude-genus0} and Theorem~\ref{thm:bar-semi-infinite-km} require their constructed targets, coefficient actions, relative descent, section carriers, filtrations, actual symbol maps, and compatible corrections.
For $V\otimes bc$, the augmentation-kernel target is excluded by Proposition~\ref{prop:vir-no-augmentation}.
The standard relative transitions also fail in Proposition~\ref{prop:vir-relative-target-obstruction}.

The genus-zero question therefore includes construction of a different unital chiral target, its relative coordinate carrier, and its symbol comparison.
Binary operations require~\eqref{eq:vir-binary-intertwining}; integration requires~\eqref{eq:vir-pairing-homotopy} and a closed functional.
Higher genus adds renormalization, anomaly-operator compatibility, and clutching equations.

The recent perturbative holomorphic-topological papers sharpen only the
local end of this route.  Gaiotto--Kulp--Wu~\cite{GKW24} construct
regularized BRST-anomaly brackets in flat holomorphic-topological
theories, prove their quadratic/Wess--Zumino identities, and point
toward a compactified configuration-space formulation; in the present
book that is M-level local coefficient input, not yet a proof that the
curvewise bar differential has been recovered.  Costello--Dimofte--Gaiotto
\cite{CDG20} supply boundary chiral algebras as source examples, while
Dimofte--Niu--Py~\cite{DNP25} supply the dg-shifted Yangian target on
the line-operator side; constitutionally, the latter belongs upstream to
MC4 whenever it is used to build the Yangian H-level target.

The disk-local comparison of
Conjecture~\ref{conj:disk-local-perturbative-fm}, reduced by
Proposition~\ref{prop:disk-local-binary-ternary-reduction} to explicit
binary and ternary local checks. Under the full constructed-carrier and filtered-comparison hypotheses and the additional binary identity~\eqref{eq:vir-binary-intertwining}, Corollary~\ref{cor:disk-local-ternary-on-brstbar-locus} leaves the ternary comparison; compactification and
Stokes compatibility upgrading those local amplitudes to the FM
boundary calculus, after which
Proposition~\ref{prop:compactified-ternary-two-channel} reduces the
ternary packet on $\overline{M}_{0,4}$ to two independent boundary
channels, canonically realizable as the residues at $0$ and $1$ in the
standard $0,1,\infty$ chart by
Corollary~\ref{cor:genus0-standard-chart-two-residues}, equivalently as
the two coefficients of
$d\log t$ and $d\log(1-t)$ by
Corollary~\ref{cor:genus0-two-coefficient-packet}, i.e. as the named
equalities
\(\mathsf{a}_{\mathrm{pert}}=\mathsf{a}_{\mathrm{bar}}\) and
\(\mathsf{b}_{\mathrm{pert}}=\mathsf{b}_{\mathrm{bar}}\) packaged in
Corollary~\ref{cor:genus0-named-coefficient-packet};
and only then clutching-compatible genuswise lifting.
These comparisons require the categorical constructions in MC3/MC4.
\end{remark}

\begin{remark}[Conjectures and their mathematical dependencies]
\label{rem:conjecture-attack-strategies}
\index{conjectures!dependencies|textbf}
The following conjectures concern scalar deformation classes, generating
functions, categorical comparison maps, and quantum field theory.

\smallskip\noindent
\textbf{Scalar saturation universality.}
Conjecture~\ref{conj:scalar-saturation-universality}: does
$\dim H^2_{\mathrm{cyc}}(\cA,\cA) = 1$ hold at all levels for all
modular Koszul chiral algebras with simple Lie symmetry?
Proved at generic level
(Theorem~\ref{thm:cyclic-rigidity-generic}) and on the standard
Lie-theoretic landscape
(Corollary~\ref{cor:effective-quadruple}).
The obstruction is the primitive deformation sector
$H^2_{\mathrm{cyc,prim}}(\cA)$, which must vanish.
At admissible levels, one possible approach is to compute the
Arakawa--Frenkel--van~Ekeren primitive sector for the
known rational $\mathcal{W}$-algebras; alternatively, reduce to
the vanishing of a spectral-sequence differential using
rationality/finiteness of the associated variety.

\smallskip\noindent
\textbf{Generating function conjectures.}
Conjectures~\ref{conj:w3-bar-gf}, \ref{conj:sl3-bar-gf},
\ref{conj:yangian-bar-gf}, and~\ref{conj:non-simply-laced-discriminant}
satisfy the following finite consistency checks
(recurrence, Koszul dual positivity, growth rates, DS~discriminant
invariance).  The critical verification computations are:
$H^5(\overline{B}(\mathcal{W}_3)) \stackrel{?}{=} 171$,
$H^4(\overline{B}(\widehat{\mathfrak{sl}}_3)) \stackrel{?}{=} 1352$,
$H^4(\overline{B}(Y(\mathfrak{sl}_2))) \stackrel{?}{=} 82$.

Direct computation is feasible for the Yangian case ($37$M-entry
matrix); the Kac--Moody case requires chiral-bar-specific methods
(chain groups exceed $786$K at degree~$5$).

\smallskip\noindent
\textbf{MC3 categorical comparisons
(Conjectures~\ref{conj:baxter-exact-triangles}%
$\to$\ref{conj:shifted-prefundamental-generation}%
$\to$\ref{conj:pro-weyl-recovery}%
$\to$\ref{conj:dk-compacts-completion}).}
\begin{enumerate}[label=\textup{(\roman*)},nosep]
\item Baxter exact triangles: $K_0$-level TQ relations verified for $\mathfrak{sl}_2$ and~$\mathfrak{sl}_3$, with
  derived-level lifts to distinguished triangles, octahedral axiom
  for iterated TQ, and $\mathfrak{sl}_3$ Clebsch--Gordan resolution
  (at the $D^b$ level).
  \emph{Gap}: categorical lift to natural transformations of exact functors.

\item Shifted-prefundamental generation: Hernandez--Jimbo--Zhang
  negative prefundamental modules~$L^-_a$ described for
  $Y(\mathfrak{sl}_2)$ (covering characters,
  QQ-system Wronskians, $K_0$ tensor decompositions).
  Weight multiplicities $\dim(L^-_a)_{-2k} = p(k)$ connect
  MC3 to partition function theory.

  Thick generation obstruction analysis (Computation~\ref{comp:thick-generation-sl2}):
  Fock--Verma factorization $\operatorname{ch}(L^-)
  = \operatorname{ch}(M(0)) \cdot \operatorname{ch}(F_{\geq 2})$
  verified; obstruction $\delta(k) = p(k)-1$ sub-exponential
  (matches $E_1$ growth); sectorwise $\dim\operatorname{Hom}(L^-,
  V_n) \leq \lfloor n/2\rfloor + 1$; character containment
  $\operatorname{ch}(M(\lambda)) \leq \operatorname{ch}(V_\lambda
  \otimes L^-)$ for $\lambda \leq 6$.
  \emph{Gap}: categorical thick generation (exact sequences, not
  just characters).

\item Pro-Weyl recovery: M-level convergence verified
  (chain-level Ext, derived inverse limit,
  Mittag-Leffler, $R^1\!\varprojlim = 0$).
  \emph{Gap}: construct $A_\infty$ model.

\item DK on compacts: Feigin--Gainutdinov completion gap analysis
  depends on the completion hypotheses in \textup{(i)--(iii)}.
\end{enumerate}

Conjecture~\ref{conj:mc3-sectorwise-all-types} \textup{(}Strategy~IV
extension beyond the evaluation-generated core\textup{)}:
$E_1$ sub-exponential growth verified for all simple types, sectorwise convergence on the evaluation-generated
core proved by
Theorem~\ref{thm:sectorwise-spectral-convergence}, and sectorwise
finiteness verified for all simply-laced lattice VOAs.

\emph{Gap}: extend the same sectorwise package and factorization
descent to the completed/coderived enlargement.

The lattice route
\textup{(}Theorem~\ref{thm:lattice:homotopy-factorization-dk}\textup{)}
provides an unconditional alternative for simply-laced types at
level~$1$.  The Kazhdan--Lusztig $N$-complex structure
\textup{(}Conjecture~\ref{conj:kl-periodic-cdg}\textup{)} now has a
tested M-level certificate on
$\mathfrak{u}_q(\mathfrak{sl}_2)$: the tractable roots
$N=2,3$ are computed directly, the first larger roots $N=4,5$ satisfy
$d_q^N=0$ by the quantum-binomial structural certificate, and the
reduced degree-$2$ vanishing is separated from the genuinely
non-classical check $d_q^2\neq0$ at bar degree~$\ge3$.

The first missing degree-$4$ packet is
also now computed at $N=3$:
\[
\dim H^{1,2}_2
=
\dim H^{2,1}_3
=
3.
\]
At the level of the total root-weight profile, each of these two flavors
is already the same sparse three-point packet: weights $-3,0,3$ occur
with multiplicity $1$, and the corresponding class-support sizes are
$1,6,1$.  \emph{Gap}: determine whether one flavor, the paired
$N=3$ packet, or some further categorical operation on this sparse
fingerprint is the periodic shadow seen by the KL target, and promote
that data to the periodic/coderived enlargement.

The first sparse $N=4$ degree-$1$
packet is now fully resolved and vanishes:
\[
H^{3,1}_1 = H^{2,2}_1 = H^{1,3}_1 = 0.
\]

The first tractable degree-$2$ channels also vanish:
\[
H^{3,1}_2 = H^{2,2}_2 = 0.
\]

For the remaining $H^{1,3}_2$ channel, a split-form sparse
$d_q^3\colon B_5\to B_2$ certificate already gives
\[
\operatorname{rank}\bigl(\operatorname{im}(d_q^3)\bigr)\ge 3903,
\qquad
\dim H^{1,3}_2 \le 66.
\]
The residual cokernel is supported only on left factors
$F$, $E$, and $K-1$.  Further split-form screening shows that the
later precursor stages $ab\in\langle F,E,K-1\rangle$,
$abc\in\langle F,E,K-1\rangle$, and
$abcd\in\langle F,E,K-1\rangle$ are already quotient-zero.

The surviving first-term sector therefore compresses from
$42{,}718{,}284$ raw tuples to only $1493$ distinct right-product
states, and its standalone tensor span adds just $57$ directions
beyond the $3903$-rank seed span \textup{(}$43$ from $F$, $14$ from
$E$, and $0$ from $K-1$\textup{)}.  So the live KL discriminant is no
longer a brute-force $B_5 \to B_2$ sweep, but the
cancellation/annihilator problem between that first-term $57$-plane
and the remaining nonresidual split contributions.

In fact the combined non-first split sector already spans that same
$57$-plane, and the witness-basis internal cancellation operator there
is exactly $(-1-i)\mathrm{Id}$.

The exhaustive compressed
verification of all $354{,}668$ surviving prefix signatures, covering
the full $42{,}718{,}284$ raw surviving columns, now shows that every
weighted surviving column vanishes in the seed quotient.

Hence the
seed rank $3903$ is exact and
\[
\dim H^{1,3}_2 = 66.
\]

So the live KL question is no longer the
$B_5 \to B_2$ elimination problem, but the periodic/coderived shadow
and categorical meaning of this exact $66$-dimensional packet.

Moreover this packet already has a rigid support fingerprint:
\[
F \otimes 48 \;\oplus\; E \otimes 15 \;\oplus\; (K-1)\otimes 3,
\]
with palindromic total root-weight profile
\[
1,4,8,12,16,12,8,4,1
\]
across weights $-4,\dots,4$.

Equivalently, this S-level profile is an
exact unit-step interval staircase: one copy of $[-4,4]$, three of
$[-3,3]$, four of $[-2,2]$, four of $[-1,1]$, and four of $\{0\}$.
Moreover, this staircase is not the image of the first admissible
$N=3$ packet under any exact symmetric convolution transport at all.
Indeed, because the source already reaches weights $\pm3$ while the
target stops at $\pm4$, the edge weights $\pm(3+R)$ force every
radius-$R\ge2$ kernel coefficient at $\pm R$ to vanish, reducing any
exact transport to the radius-$1$ case; but the exact radius-$0$ and
radius-$1$ systems are already inconsistent for both a single $N=3$
flavor and the paired packet.  There do exist signed radius-$4$
kernels that match the target only on the window $[-4,4]$, but they
necessarily spill outside that window, so they are not genuine
transports.  Thus the next KL comparison is no longer a dimension
match; it must identify a genuinely non-convolutional periodic/coderived
shadow, or show that this rigid $N=4$ staircase is genuinely new.

At the same S-level, there is now a first structured non-convolutional
finite-window candidate: on the weight interval $[-4,4]$, the paired
$N=3$ profile is recovered exactly from the $N=4$ staircase by a
Dirichlet discrete Schr\"odinger operator
\[
a\Delta + V(w),
\qquad
a = -\frac{3}{140},
\]
with even sextic potential
\[
V(w)=\frac{4}{35}
-\frac{2519}{16800}w^2
+\frac{503}{13440}w^4
-\frac{17}{9600}w^6.
\]
No such operator exists with potential degree $0,2,$ or $4$, while a
pure multiplicative potential requires degree~$8$.  So the first
surviving structured route is already a finite-window second-order
operator, not a convolutional shadow.  What remains open is whether
this S-level operator is the shadow of an H/M-level KL mechanism or
only a numerical organizing principle.

\smallskip\noindent
\textbf{MC4 coefficient matching.}
$\mathcal{W}_\infty$ side: the stage-$4$ principal DS packet on
$\mathcal{I}_4$ is explicit.  The four higher-spin channels are given
as rational functions of~$c$
(Proposition~\ref{prop:w4-ds-ope-explicit},
Computation~\ref{comp:w4-ds-ope-extraction}):
\begin{gather*}
c_{334}^2 = \frac{42c^2(5c+22)}{(c+24)(7c+68)(3c+46)},
\quad
c_{444}^2 = \frac{112c^2(2c-1)(3c+46)}{(c+24)(7c+68)(10c+197)(5c+3)},
\end{gather*}

plus inter-coefficient relations
$\mathsf{C}_{3,4;3;0,4}^2 = \tfrac{9}{16}\,c_{334}^2$,
$\mathsf{C}_{3,4;4;0,3}^2 = \tfrac{5}{7}\,c_{334}^2$
\textup{(}Corollary~\ref{cor:w4-ds-stage4-square-class-reduction}\textup{)}.
Thus at signless square-class level the four higher-spin principal
targets reduce to the primitive pair
$c_{334}^2,c_{444}^2$, with the mixed squares forced by~$c_{334}^2$.

On the residue side, the corresponding four-channel higher-spin packet
contracts further to three channels on the additional visible
pairing-compatible Ward-normalized locus of
Proposition~\ref{prop:winfty-stage4-residue-pairing-reduction} and
Corollary~\ref{cor:winfty-stage4-residue-three-channel}: the swap-even
channel $\mathsf{C}^{\mathrm{res}}_{3,4;3;0,4}(4)$ is then forced by
the $(3,3)$ channel
$\mathsf{C}^{\mathrm{res}}_{3,3;4;0,2}(4)$.  More sharply,
Corollary~\ref{cor:winfty-stage4-primitive-transport-square-triple}
shows that on this visible pairing locus the entire stage-$4$
higher-spin comparison reduces at signless square-class level to the
primitive-plus-transport triple
\[
(3,3;4;0,2),\qquad (4,4;4;0,4),\qquad (3,4;4;0,3),
\]
with the swap-even square channel automatic from the first entry.

The
exact remaining gap to the principal two-primitive square profile is
therefore the visible top-pole Borcherds transport relation isolated
in Remark~\ref{rem:winfty-stage4-primitive-transport-gap} and stated
exactly in
Conjecture~\ref{conj:winfty-stage4-visible-borcherds-transport}; under
Corollary~\ref{cor:winfty-stage4-visible-borcherds-two-primitive},
this single transport relation is exactly equivalent to collapsing the
primitive-plus-transport triple to the two primitive self-coupling
square classes.  Proposition~\ref{prop:winfty-stage4-local-attack-order}
therefore packages the exact stage-$4$ local order as the
visible weight-$4$ normalization identity.  On the visible pairing
locus, the next step is the swap-odd higher-spin transport relation.  Under that single additional
transport input,
Corollary~\ref{cor:winfty-stage4-visible-borcherds-two-primitive}
reduces the visible pairing stage-$4$ higher-spin square frontier to
the same two primitive self-coupling square classes as on the
principal side.

Both principal Virasoro-target identities are verified.
The live W-side packet is therefore the exact six-defect vanishing
problem, equivalently the six identities
\[
\mathsf{C}^{\mathrm{res}}_{s,t;u;m,n}(4)
=
\mathsf{C}^{\mathrm{DS}}_{s,t;u;m,n}(4)
\]
on~$\mathcal{I}_4$, followed by their promotion through
$\mathcal{I}_N$ via the incremental packets
$\mathcal{J}_{M+1}$ of
Proposition~\ref{prop:winfty-ds-stage-growth-packet}; the additional
four-channel contraction is conditional on
Conjecture~\ref{conj:winfty-stage4-ward-inheritance}.

Yangian side: auxiliary-kernel identity
$\mathsf{K}^{\mathrm{line}}_{1,2}(N)
= \mathsf{K}^{\mathrm{RTT}}_{1,2}(N) = \Lambda^2(V)$ verified
for $N = 2, 3, 4$ (dimensions $1, 3, 6$ = triangular numbers), and the
faithful-evaluation boundary-strip packets $\Delta_{a,0}(4)$ and
$\Delta_{a,0}(6)$ vanish on generic tensor lengths $2$ and $3$ in
ranks $2,3,4$.  The theorematic all-rank remainder is the
transport of the dg-shifted or factorization one-factor line operator
to the standard normalized mixed-tensor residue on the fundamental
line, equivalently under one-loop exactness the single degree-$2$
fundamental-line coefficient of
Corollary~\textup{\ref{cor:yangian-typea-degree2-plus-generators}}.

\smallskip\noindent
\textbf{KL comparison sequence
(Conjectures~\ref{conj:kl-periodic-cdg}$\to$\ref{conj:kl-coderived}%
$\to$\ref{conj:kl-braided}).}
Stage~1: promote the admissible-level $N$-complex certificate at roots
of unity to the periodic/coderived KL object, starting from the first
computed $N=3$ packet
\[
\dim H^{1,2}_2
=
\dim H^{2,1}_3
=
3
\]
and determining whether one flavor or the paired packet carries the KL
periodic shadow.
Stage~2: coderived equivalence via bar-cobar.
Stage~3: braided monoidal upgrade from fusion-tensor compatibility.
The lattice route resolves all three stages unconditionally at
level~$1$ for simply-laced types
\textup{(}Theorems~\ref{thm:lattice:level-k-dk}
and~\ref{thm:lattice:quantum-factorization-dk}\textup{)}.

\smallskip\noindent
\textbf{MC5 quantum field theory comparisons.}
These include BV/BRST identifications
\textup{(}Conjecture~\ref{conj:master-bv-brst}\textup{)},
Feynman diagram/path-integral correspondences
\textup{(}Conjectures~\ref{conj:bar-cobar-path-integral},
\ref{conj:string-amplitude-bar}\textup{)},
holographic/AdS-CFT duality
\textup{(}Conjectures~\ref{conj:ads-cft-bar},
\ref{conj:holographic-koszul}\textup{)},
AGT correspondences
\textup{(}Conjectures~\ref{conj:agt-bar-cobar},
\ref{conj:agt-w-algebra}, \ref{conj:q-agt}\textup{)},
and Chern--Simons/topological bridges
\textup{(}Conjectures~\ref{conj:nc-cs},
\ref{conj:cs-factorization}\textup{)}.

These comparisons require MC1--MC4 and additional quantum field theory
constructions.  The proposed comparisons involve:
\begin{enumerate}[label=\textup{(\roman*)},nosep]
\item the finite disk-local packet of
  Conjecture~\ref{conj:disk-local-perturbative-fm}
  reduced by
  Proposition~\ref{prop:disk-local-binary-ternary-reduction}
  to the binary residue/OPE and ternary residue comparisons. With all constructed-carrier and filtered-comparison data and the extra binary identity~\eqref{eq:vir-binary-intertwining}, Corollary~\ref{cor:disk-local-ternary-on-brstbar-locus} leaves the ternary comparison;
\item the compactification/Stokes upgrade from those local formulas to
  the FM boundary calculus, using
  Proposition~\ref{prop:stokes-regularity-FM}; after this step,
  Proposition~\ref{prop:compactified-ternary-two-channel} reduces the
  compactified ternary packet on $\overline{M}_{0,4}$ to two
  independent boundary channels, canonically the residues at $0$ and
  $1$ in the standard $0,1,\infty$ chart by
  Corollary~\ref{cor:genus0-standard-chart-two-residues}, equivalently
  the two coefficients of $d\log t$ and $d\log(1-t)$ by
  Corollary~\ref{cor:genus0-two-coefficient-packet}, i.e. the named
  coefficient identities of
  Corollary~\ref{cor:genus0-named-coefficient-packet}; and
\item only then the genuswise clutching and renormalization lift for
  Conjecture~\ref{conj:master-bv-brst}.
\end{enumerate}
In that ordered route, GKW24 supplies the local perturbative bracket
package, CDG20 supplies source boundary-chiral examples, and DNP25
supplies line-operator targets that must still be sorted upstream under
MC4 whenever they are used to build the Yangian H-level comparison.

\smallskip\noindent
\textbf{Front~G\,: Independent conjectures \textup{(}${\sim}7$\textup{)}.}
Reflected modular periodicity
\textup{(}Conjecture~\ref{conj:reflected-modular-periodicity}\textup{)},
derived $bc$-$\beta\gamma$
\textup{(}Conjecture~\ref{conj:derived-bc-betagamma}; Koszul check
verified\textup{)},
$\mathcal{W}$-orbit duality
\textup{(}Conjecture~\ref{conj:w-orbit-duality}\textup{)},
and the $\mathsf{E}_n$ Koszul duality
\textup{(}now Theorem~\ref{thm:en-koszul-duality}\textup{)} are not subsumed
by any master conjecture.  The $\mathsf{E}_n$ statement is resolved by
chaining Totaro, Lambrechts--Voli\'c, and Knudsen.
\textup{(}The former conjecture on Virasoro $c = 26$ self-duality is
now Proposition~\ref{prop:virasoro-c26-selfdual}: NAP gives
$\int_X \mathcal{V}_{26} \simeq \mathbb{D}(\int_{-X}\mathcal{V}_0)$;
the self-dual point is $c = 13$.  Near-rationality of the Virasoro bar sequence is now resolved:
Proposition~\ref{prop:virasoro-pade} establishes a period-$6$
Pad\'e matching pattern with excess $O(1)$, subsuming the original
$O(\sqrt{q})$ question.\textup{)}
\end{remark}

\begin{remark}[Structural gaps]\label{rem:structural-gaps}
\index{structural gaps|textbf}
Eight structural gaps separate the proved theorems
(Stratum~I) from the full programme (Stratum~II).
Each gap has a clear resolution strategy; none is a dead end.

\smallskip\noindent
\textbf{H1\,: Coderived Ran-space formalism.}
Foundational theory established:
Appendix~\ref{app:coderived},
\S\ref{subsec:coderived-ran-formalism} defines curved
factorization coalgebras (with CDG-factorization compatibility
via the coaugmentation tensor product formula), coacyclic
factorization objects, and the coderived/contraderived
factorization categories on~$\operatorname{Ran}(X)$.  Structural
theorems: stratified conservative restriction via Positselski's
injective characterization
(Theorem~\ref{thm:stratified-conservative-restriction}),
provisional embedding
(Proposition~\ref{prop:provisional-embedding}), bar functor
well-definedness
(Proposition~\ref{prop:bar-ran-well-defined}), and factorization
co-contra correspondence for conilpotent coalgebras
(Theorem~\ref{thm:fact-co-contra-general}).
\emph{Remaining gap}: off-Koszul bar-cobar inversion
(Conjecture~\ref{conj:off-koszul-ran-inversion}).

\smallskip\noindent
\textbf{H2\,: $\mathcal{W}_\infty$ H-level realization (MC4).}
\textbf{Resolved} by Theorem~\ref{thm:winfty-factorization-kd}: the
factorization target~$\mathcal{W}_\infty^{\mathrm{fact}}$ is
constructed via $\Einf$-sectorwise convergence at each finite
stage, with DS--KD intertwining
(Theorem~\ref{thm:ds-koszul-intertwine}) identifying
$(W_N^k)^! \simeq \widehat{\mathfrak{sl}}_{N,-k-2N}$ and
Mittag--Leffler
(Corollary~\ref{cor:winfty-standard-mc4-package}) controlling the
inverse limit.  The residue identities
$\mathsf{C}^{\mathrm{res}} = \mathsf{C}^{\mathrm{DS}}$ follow
from the chain-level content of the intertwining theorem.
The remaining $\mathcal{W}_\infty$ frontier is the large-$N$
coupling identification $\lambda \mapsto 1 - \lambda$
(Conjecture~\ref{conj:w-infty-bar}).

\smallskip\noindent
\textbf{H3\,: Thick generation beyond $\operatorname{Rep}_{\mathrm{fd}}$ (MC3).}
Thick generation by evaluation modules is proved for
$\operatorname{Rep}_{\mathrm{fd}}$
at all~$N$ in type~A
(Theorem~\ref{thm:eval-core-identification}) and for
$\mathcal{O}_{\mathrm{poly}}$ at all~$N$
(Remark~\ref{rem:catO-thick-generation-verification}).
The gap is extension to non-polynomial infinite-dimensional modules
(prefundamental, asymptotic) in the completed/coderived setting.
\emph{Strategy}: combine the BGG criterion
(Proposition~\ref{prop:bgg-criterion}) with the KL reduction
(Proposition~\ref{prop:thick-gen-projective}).

\smallskip\noindent
\textbf{H4\,: Baxter derived lift (MC3 path~\textup{(i)}).}
$K_0$-level TQ relations are verified for
$\mathfrak{sl}_2$ and~$\mathfrak{sl}_3$
(Conjecture~\ref{conj:baxter-exact-triangles}), including
derived-level lifts to distinguished triangles and the octahedral
axiom for iterated TQ.
\emph{Gap}: the categorical lift from short exact sequences in the
heart of the $t$-structure on
$D^b(\mathcal{O}^{\mathrm{sh}}_{\leq 0})$ to natural
transformations of exact functors.

\smallskip\noindent
\textbf{H5\,: Prefundamental generation (MC3 path~\textup{(ii)}).}
Hernandez--Jimbo--Zhang negative prefundamental modules~$L^-_a$
for $Y(\mathfrak{sl}_2)$ are now implemented, with character
$\operatorname{ch}(L^-_a)(q) = \prod_{n \geq 1} (1 - q^{-2n})^{-1}$
and weight multiplicities $\dim(L^-_a)_{-2k} = p(k)$
(including QQ-system Wronskian checks).
\emph{Gap}: thick generation of
$\mathcal{O}^{\mathrm{sh}}_{\leq 0}$ by
$\{V_n(a)\} \cup \{L^-_a\}$ requires verifying that every
standard module admits a finite resolution by
evaluation$\oplus$prefundamental summands.

\smallskip\noindent
\textbf{H6\,: Pro-Weyl M-level lift (MC3 path~\textup{(iii)}).}
Shadow-level convergence of the pro-Weyl system is proved:
Mittag-Leffler condition holds, $R^1\!\varprojlim = 0$, and
chain-level Ext computations verify the derived inverse limit.  \emph{Gap}: construct a dg-algebra or
$A_\infty$ model for the pro-Weyl limit
$M(\Psi) \simeq R\!\varprojlim W_m$ and prove quasi-isomorphism
with the shifted Yangian Verma.

\smallskip\noindent
\textbf{H7\,: Holomorphic-to-real propagator extension (MC5).}
The genus-zero criterion of Theorem~\ref{thm:brst-bar-genus0} requires constructed target and relative carriers, the prescribed section functor, compatible filtrations, an actual symbol map, and corrections.
It supplies no propagator or pairing identity.
The obstructions in Propositions~\ref{prop:vir-no-augmentation} and~\ref{prop:vir-relative-target-obstruction} must be addressed before the proposed Virasoro application.
An extension to $g\geq1$ also requires passing from
holomorphic propagators on $\Sigma_g$ to real propagators
(\`a~la Kontsevich) with Costello's renormalization data.
The first missing step is the disk-local packet of
Conjecture~\ref{conj:disk-local-perturbative-fm}: identify the local
perturbative BRST brackets with the bar residue operations on
$C_2$ and the three codimension-$1$ strata of~$\overline{C}_3$.
By Proposition~\ref{prop:disk-local-binary-ternary-reduction}, this is
already the full independent local comparison problem.
Under all actual comparison data and the additional binary-intertwining hypothesis of Corollary~\ref{cor:disk-local-ternary-on-brstbar-locus}, the remaining local operation comparison is the
ternary comparison on~$\overline{C}_3$.
Once that ternary comparison is compactified to
$\overline{M}_{0,4} \cong \bP^1$ with logarithmic boundary behavior,
Proposition~\ref{prop:compactified-ternary-two-channel} reduces it
further to any two of the three boundary channels; on the standard
$0,1,\infty$ chart,
Corollary~\ref{cor:genus0-standard-chart-two-residues} makes this the
explicit pair of residues at $0$ and~$1$, equivalently the two scalar
coefficients of $d\log t$ and $d\log(1-t)$ by
Corollary~\ref{cor:genus0-two-coefficient-packet}, i.e. the named
coefficient identities
\(\mathsf{a}_{\mathrm{pert}}=\mathsf{a}_{\mathrm{bar}}\) and
\(\mathsf{b}_{\mathrm{pert}}=\mathsf{b}_{\mathrm{bar}}\).
Once that packet is compactified and matched with the FM Stokes
relations, the higher-genus propagator problem becomes an analytic
genus-lifting problem rather than a local dictionary problem.
This is downstream of MC1--MC4.

\smallskip\noindent
\textbf{H8\,: DS transport of $T_{\mathrm{br}}$ (spectral branch).}
Drinfeld--Sokolov reduction preserves the discriminant
$\Delta_{\cA}(x) = \det(1 - x \, T_{\mathrm{rec}})$
(Theorem~\ref{thm:ds-bar-gf-discriminant}).  For rank~$1$
($\mathfrak{sl}_2$), preservation of the full transfer operator
$T_{\mathrm{br}}$ is automatic.  \emph{Gap}: for rank $\geq 2$
($\mathfrak{sl}_3$ onward), is $T_{\mathrm{br}}$ itself
DS-invariant (up to similarity), or only its determinant?
Computational verification at $\mathfrak{sl}_3$
 confirms that the DS-invariant sub-discriminant
$(1 - 3x - x^2)$ is preserved, while the dominant eigenvalue
changes ($8 \to 1$), so full $T_{\mathrm{br}}$~similarity fails;
see Conjecture~\ref{conj:ds-spectral-branch-preservation}
for the refined formulation.
\end{remark}

\subsection{Homotopy templates for conjectures}
\label{subsec:homotopy-templates}
\index{conjectures!homotopy templates|textbf}

The stratification above classifies conjectures by \emph{difficulty}.
The following classification is orthogonal: it sorts conjectures by
\emph{homotopy type}---what kind of mathematical object each conjecture
asserts the existence or properties of.  Every conjecture in this
monograph fits one of seven templates
(Convention~\ref{conv:hms-levels}).

\begin{convention}[Homotopy templates]\label{conv:homotopy-templates}
\index{homotopy templates|textbf}
\begin{enumerate}[label=\textbf{Type \Roman*.}, leftmargin=3em]
\item \emph{Existence of a homotopy object.}
  A filtered cyclic $L_\infty$-algebra, MC element,
  or factorization (co)algebra is asserted to exist with specified
  compatibility.  \emph{Example:}
  $\Theta_{\cA}$ (Theorem~\ref{thm:master-theta}).

\item \emph{Equivalence of homotopy categories.}
  Two $\infty$-categories (or stable/E$_1$-monoidal variants)
  are equivalent; current chain models give a presentation on a
  dense sublocus.
  \emph{Examples:} full coderived bar-cobar
  (Conjecture~\ref{conj:master-dk-kl}),
  full DK, KL from bar-cobar.

\item \emph{Shifted-symplectic / Lagrangian enhancement.}
  A pairing already constructed at the model level upgrades to an
  $n$-shifted symplectic structure on a formal moduli problem.
  \emph{Examples:} complementarity upgrades, moduli of Koszul pairs.

\item \emph{Trace / index statement.}
  A scalar invariant is the image of a universal deformation object
  under a trace or pushforward functor.
  \emph{Examples:} genus generating series ($\hat{A}$-genus),
  GRR for modular deformations.

\item \emph{Periodicity / autoequivalence.}
  A finite homotopy action, graded autoequivalence, or periodic
  filtration operator exists on a homotopy object; numerical periods
  are its shadow.
  \emph{Examples:} reflected modular periodicity,
  quantum group periodicity at roots of unity.

\item \emph{Filtered / pro-object comparison.}
  A theorematic completed model exists stagewise, and the remaining
  problem is to identify the intended H-level target by compatible
  finite quotients and finite-detection criteria.
  \emph{Examples:} $\mathcal{W}_\infty$ duals
  (Conjecture~\ref{conj:master-infinite-generator}),
  Yangian towers.

\item \emph{Physics-dictionary conjecture.}
  Two deformation problems---one algebraic, one
  field-theoretic---are controlled by quasi-isomorphic dg~Lie or
  $L_\infty$-algebras.
  \emph{Examples:} BRST $=$ bar
  (Conjecture~\ref{conj:master-bv-brst}),
  BV $=$ bar-cobar, path integral $=$ factorization.
\end{enumerate}
\end{convention}

\begin{remark}[Using the templates]\label{rem:template-usage}
Each individual conjecture in the monograph carries a scope remark
identifying its tier (Foundational / Consequence / Physics).
The homotopy template provides the complementary structural
information: \emph{what kind of homotopy-theoretic statement}
the conjecture makes.  The resolved PBW theorem and the four remaining
master conjectures above are respectively of Types~I, II, II, VI,
and~VII.
\end{remark}

\section{Research programmes}
\label{sec:research-programmes}
\index{research programmes|textbf}

\subsection{Higher-dimensional Koszul duality}
\label{subsec:higher-dim-kd}
\index{Koszul duality!higher-dimensional}

\begin{theorem}[\texorpdfstring{$\mathsf{E}_n$}{En} Koszul duality via configuration
space integrals; \ClaimStatusProvedElsewhere]\label{thm:en-koszul-duality-conc}
\index{En-algebra@$\mathsf{E}_n$-algebra!Koszul duality}
For an $\mathsf{E}_n$-algebra $A$ on an oriented $n$-manifold~$M$,
the bar-cobar adjunction admits an explicit realization via
configuration space integrals on the Fulton--MacPherson
compactification $\overline{C}_k(M)$.  All four components
are proved:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \textup{[\ClaimStatusProvedElsewhere; Idrissi~\cite{Idrissi22}]}
  The bar differential is given by integration along collision
  divisors $D_I \subset \overline{C}_k(M)$, with the propagator
  determined by a Green's form $G \in \Omega^{n-1}(M \times M
  \setminus \Delta)$.  Poincar\'e duality on configuration spaces
  ensures $d^2 = 0$ for all~$n$.
\item \textup{[\ClaimStatusProvedHere]}
  For $n = 2$ with complex structure \textup{(}complex curves viewed as
  real oriented surfaces\textup{)}, this recovers the topological
  shadow of the chiral bar-cobar duality of
  Theorem~\textup{\ref{thm:bar-cobar-isomorphism-main}}.
\item \textup{[\ClaimStatusProvedElsewhere;
  Totaro~\cite{Totaro96}, Lambrechts--Voli\'c~\cite{LambrechtsVolic14},
  Knudsen~\cite{Knudsen18}]}
  For $n \geq 3$, the Totaro/graph-complex relations govern the
  higher-dimensional chain models:
  the Arnold relations are replaced by Totaro's presentation of
  $H^*(\overline{C}_k(\mathbb{R}^n))$, and the bar cohomology is
  governed by $\mathsf{E}_n$-operadic Koszul duality.
\item \textup{[\ClaimStatusProvedHere]}
  The Poincar\'e--Koszul duality of Ayala--Francis
  \textup{(}\cite{AF15}, Theorem~7.8\textup{)} is recovered as the
  $\infty$-categorical shadow of this explicit chain-level
  construction.
\end{enumerate}
\end{theorem}

\begin{proposition}[\texorpdfstring{$n = 2$}{n = 2} recovery and AF comparison;
\ClaimStatusProvedHere]\label{prop:en-n2-recovery}\label{prop:en-n1-recovery}
Parts \textup{(ii)} and \textup{(iv)} of
Theorem~\textup{\ref{thm:en-koszul-duality}} hold:
\begin{enumerate}[label=\textup{(\alph*)}]
\item At $n = 2$, on a complex curve~$X$ viewed as a real oriented
  surface, the FM propagator class admits the holomorphic
  representative $\omega_{ij} = d\log(z_i - z_j)$
  recovers the bar differential of
  Definition~\textup{\ref{def:bar-differential-complete}},
  and the Arnold relations
  \textup{(}Appendix~\textup{\ref{app:arnold-relations})} give $d^2 = 0$.
\item The resulting bar-cobar adjunction is
  Theorem~\textup{\ref{thm:bar-cobar-isomorphism-main}},
  which is the chain-level refinement of the $\infty$-categorical
  Poincar\'e--Koszul duality of Ayala--Francis~\cite{AF15}
  restricted to $n = 2$
  \textup{(}Remark~\textup{\ref{rem:ayala-francis-comparison})}.
\end{enumerate}
\end{proposition}

\begin{proof}
Part (a) is the content of \S\ref{sec:bar-cobar}: the
bar differential is $d = \sum_{i < j} \mathrm{Res}_{D_{ij}}(\omega_{ij} \wedge -)$
with $\omega_{ij}$ the FM propagator on the curve~$X$.  Arnold relations
ensure $d^2 = 0$ (Corollary~\ref{cor:bar-d-squared-zero-arnold}).  Part~(b):
by Remark~\ref{rem:ayala-francis-comparison}, the AF equivalence
$\int_M A \simeq \int_M A^!$ specializes at $n = 2$ to our
bar-cobar adjunction, with the factorization homology integral
computed via FM configuration space integrals.
\end{proof}

\begin{remark}[Proof status]\label{rem:en-scope}
All four parts are proved.  Part~(i) by
Idrissi~\cite{Idrissi22}, who establishes that Poincar\'e duality on
configuration spaces ensures $d^2 = 0$ for the propagator-based
differential on $\mathsf{E}_n$-algebras for all~$n$.  Parts~(ii)
and~(iv) by Proposition~\ref{prop:en-n2-recovery} below.  Part~(iii)
follows from a chain of three results: (a)~Totaro's
presentation~\cite{Totaro96} of
$H^*(\overline{C}_k(\mathbb{R}^n))$; (b)~$\mathsf{E}_n$ formality
(Lambrechts--Voli\'c~\cite{LambrechtsVolic14}: the operadic
quasi-isomorphism $C_*(\mathsf{E}_n) \xrightarrow{\sim}
H_*(\mathsf{E}_n)$ transfers the bar-cobar adjunction to the
homological level); (c)~Knudsen's $\mathsf{E}_n$ Koszul
duality~\cite{Knudsen18}, which provides PBW and Koszul duality
theorems for higher enveloping algebras $U_n(\mathfrak{g})$.
Together, these establish that the bar cohomology of any
$\mathsf{E}_n$-algebra is governed by $\mathsf{E}_n$-operadic Koszul
duality, with the Totaro presentation providing the explicit
generators and relations.

\emph{What remains as a computational refinement} is writing down
explicit closed-form expressions for the bar differential in terms of
Totaro generators at each~$n \geq 3$---the propagator and FM
compactification exist, and $d^2 = 0$ is proved, but the individual
formulas (analogous to the Arnold-based computations at $n = 2$) have
not been tabulated.  This is a concrete calculation, not a
theoretical gap.
(Contributing to Conjecture~\ref{conj:master-bv-brst}.)
\end{remark}

\begin{remark}[Proof assembly for part~(iii)]\label{rem:en-proof-strategy}
\index{En-algebra@$\mathsf{E}_n$-algebra!Koszul duality!proof strategy}
Part~(iii) of Theorem~\ref{thm:en-koszul-duality} is proved by
assembling the following known ingredients into the abstract
chain-level construction.

\emph{Step~1: Formality.}
The $\mathsf{E}_n$ operad is formal over $\mathbb{Q}$ for all
$n \geq 2$: Kontsevich~\cite{Kon99} and Tamarkin~\cite{Tamarkin00}
proved this for $n = 2$;
Lambrechts--Voli\'c~\cite{LambrechtsVolic14} established the
general case via configuration space integrals on semi-algebraic
models for $\overline{C}_k(\mathbb{R}^n)$.  Formality provides a
quasi-isomorphism $C_*(\mathsf{E}_n) \xrightarrow{\sim}
H_*(\mathsf{E}_n)$ of operads, so the bar-cobar adjunction can be
studied at the level of homology.

\emph{Step~2: Totaro's presentation.}
For $n \geq 3$, the cohomology ring
$H^*(\mathrm{Conf}_k(\mathbb{R}^n); \mathbb{Q})$ is generated by
degree-$(n-1)$ classes $\omega_{ij}$ ($1 \leq i < j \leq k$)
subject to: (a)~$\omega_{ij} = (-1)^n \omega_{ji}$;
(b)~$\omega_{ij}^2 = 0$; (c)~the generalized Arnold relation
$\omega_{ij}\omega_{jk} + \omega_{jk}\omega_{ki} +
\omega_{ki}\omega_{ij} = 0$~\cite{Totaro96,Coh76}.  These replace
the classical Arnold relations (which govern $n = 1, 2$) with
their higher-dimensional analogues; the key difference is that the
generators sit in odd degree when $n$ is even and even degree when
$n$ is odd.

\emph{Step~3: Graph complex connection.}
Kontsevich's graph complex $\mathsf{GC}_n$ computes
$H^*(\mathrm{Aut}(\mathsf{E}_n))$, and the bar differential on
$\mathsf{E}_n$-algebras factors through $\mathsf{GC}_n$.
The passage from $n = 2$ to $n \geq 3$ is governed by the
transition from the Lie graph complex ($n = 1$) through the
associative graph complex ($n = 2$) to the commutative graph complex
($n \to \infty$); the obstruction theory at each stage is controlled
by the parity of the generators.

\emph{Step~4: Synthesis.}
The abstract construction is complete: formality (Step~1) transfers
the bar-cobar adjunction to homology; the Totaro presentation
(Step~2) provides explicit generators; Knudsen's higher enveloping
algebras~\cite{Knudsen18} identify the bar cohomology with
$\mathsf{E}_n$-operadic Koszul duality; and Idrissi's
Part~(i)~\cite{Idrissi22} ensures $d^2 = 0$ at the chain level.
What remains as a \emph{computational refinement} is writing down
the explicit bar differential formula in Totaro generators at
each~$n \geq 3$---the natural candidate propagator is the
Lambrechts--Voli\'c propagator~\cite{LambrechtsVolic14}, and the
parity of the generators (odd for even~$n$) produces sign patterns
that must be tracked through the differential.
\end{remark}

\subsection{Vassiliev invariants from Feynman transform}
\label{subsec:vassiliev}
\index{Vassiliev invariants!from Feynman transform}

\begin{conjecture}[Vassiliev invariants from bar complex;
\ClaimStatusConjectured]\label{conj:vassiliev-bar}
\index{Kontsevich integral!bar complex}
For $\cA = \widehat{\fg}_k$, the Feynman transform identification
$\barB^{\mathrm{full}}(\cA) \simeq \mathrm{FT}_{\mathcal{M}\mathrm{od}}(\cA)$
\textup{(}Theorem~\textup{\ref{thm:prism-higher-genus}(ii))}
produces universal Vassiliev invariants when specialized to
configuration spaces of circles:
\begin{enumerate}[label=\textup{(\roman*)}]
\item The restriction
  $\overline{C}_n(X) \rightsquigarrow C_n(S^1)$
  \textup{(}from complex curve to real circle\textup{)} sends the
  holomorphic propagator $\omega_{ij} = d\log(z_i - z_j)$ to the
  Kontsevich propagator $\omega_{ij}^K = \frac{1}{2\pi} d\arg(t_i - t_j)$.
\item Under this restriction, the bar complex weight systems
  $W_\Gamma\colon \fg^{\otimes n} \to \mathbb{C}$ match the
  classical weight systems of Vassiliev theory, with the
  Lie algebra $\fg$ providing the weight system as in
  Bar-Natan~\cite{BarNatan95}.
\item The genus expansion
  $\sum_g g_s^{2g-2} \barB^{(g)}$ specializes to the
  loop expansion of the Kontsevich integral.
\end{enumerate}
\end{conjecture}

\begin{proposition}[Genus-\texorpdfstring{$0$}{0} weight systems from bar complex;
\ClaimStatusProvedHere]\label{prop:vassiliev-genus0}
\index{weight system!from bar complex}
At genus~$0$, the bar complex of $\widehat{\fg}_k$ produces
classical weight systems:
\begin{enumerate}[label=\textup{(\roman*)}]
\item For each chord diagram $\Gamma$ with $n$ chords, the
  degree-$n$ bar differential $d^{(0)}\colon \barB^n \to \barB^{n-1}$
  induces a linear functional
  $W_\Gamma\colon \fg^{\otimes 2n} \to \bC$ via the residue
  pairing of the FM propagator on $\overline{C}_{2n}(\mathbb{P}^1)$.
\item These coincide with the Bar-Natan weight systems of the
  Lie algebra~$\fg$ with the Killing form at level~$k$: $W_\Gamma$
  assigns to each chord the contraction $\kappa^{ab}$ and to each
  vertex the structure constants~$f^a_{bc}$.
\end{enumerate}
\end{proposition}

\begin{proof}
At genus~$0$, the bar differential is given by residues of
the propagator $\omega_{ij} = d\log(z_i - z_j)$ along collision
divisors $D_I \subset \overline{C}_n(\mathbb{P}^1)$
(Definition~\ref{def:bar-differential-complete}).  For a chord
diagram $\Gamma$ with chord $(i,j)$, the residue
$\mathrm{Res}_{z_i \to z_j} \omega_{ij}$ extracts the
OPE coefficient $J^a(z_i) J^b(z_j) \sim k \kappa^{ab}/(z_i-z_j)^2
+ f^{ab}_c J^c/(z_i-z_j)$: the double pole contributes the
Killing contraction $k\kappa^{ab}$ and the simple pole the
structure constants~$f^{ab}_c$.  These are precisely the
assignments in Bar-Natan's construction of weight systems from
Lie algebras~\cite{BarNatan95}.
\end{proof}

\begin{remark}[Scope]\label{rem:vassiliev-scope}
Proposition~\ref{prop:vassiliev-genus0} establishes the genus-$0$
weight system extraction.  The Feynman transform of $\mathrm{Com}$
recovers the full graph complex computing Vassiliev invariants
(Getzler--Kapranov \cite{GeK98}), and our identification
$\barB^{\mathrm{full}} \simeq \mathrm{FT}$
(Theorem~\ref{thm:prism-higher-genus}) extends this to Koszul
chiral algebras.  The gap at higher genus is the passage from
complex curves to real $1$-manifolds: our propagators are
holomorphic, while Kontsevich's are real.  Comparing requires an
analytic continuation argument from holomorphic to topological
Feynman transforms.
The correct bridge between holomorphic and topological frameworks is Chern--Simons gauge equivalence, not merely restriction from complex curves to $S^1$: Cattaneo--Mnev~\cite{CattaneoMnev10} establish that configuration space integrals on $3$-manifolds produce Vassiliev invariants via BV integration, and the comparison map involves the CS partition function (not a pointwise restriction of propagators).

\emph{Homotopy template} (Convention~\ref{conv:hms-levels}): Type~VII (physics-dictionary).
The conjecture asserts that the dg Lie algebras controlling
holomorphic (chiral bar) and topological (Kontsevich integral)
deformation problems are quasi-isomorphic.
(Contributing to Conjecture~\ref{conj:master-bv-brst}.)
\end{remark}

\subsection{Anomaly cancellation and Koszul structure}
\label{subsec:anomaly-koszul}
\index{anomaly!Koszul-theoretic}

\begin{theorem}[Anomaly cancellation as Koszul constraint;
\ClaimStatusProvedHere]\label{thm:anomaly-koszul}
\index{anomaly cancellation!Koszul constraint}
For the matter-ghost system
$\cA_{\mathrm{tot}} = \cA_{\mathrm{matter}} \otimes \cA_{\mathrm{ghost}}$:
\begin{enumerate}[label=\textup{(\roman*)}]
\item The obstruction coefficient is additive:
  $\kappa(\cA_{\mathrm{tot}}) = \kappa(\cA_{\mathrm{matter}}) + \kappa(\cA_{\mathrm{ghost}})$
  \textup{(}Corollary~\textup{\ref{cor:kappa-additivity})}.
\item The total bar complex is uncurved
  ($d_{\mathrm{bar}}^2 = 0$) if and only if
  $\kappa(\cA_{\mathrm{matter}}) + \kappa(\cA_{\mathrm{ghost}}) = 0$.
\item For the Virasoro matter-ghost system,
  $\kappa(\mathrm{Vir}_c) = c/2$ and
  $\kappa(\mathrm{ghost}) = c_{\mathrm{ghost}}/2 = -13$, so
  $\kappa_{\mathrm{tot}} = 0$ precisely at $c = 26$.
\end{enumerate}
\end{theorem}

\begin{proof}
Part (i) is Corollary~\ref{cor:kappa-additivity}: the singular OPE
of the tensor product is the sum of the individual singular OPEs
(the fields act on disjoint sectors), so the genus-$1$ curvature
is additive.  Part~(ii) follows from
$\dfib^{\,2} = \kappa \cdot \omega_1 \cdot \mathrm{id}$
(Theorem~\ref{thm:genus-universality},
Convention~\ref{conv:higher-genus-differentials}): the total bar complex is
uncurved if and only if $\kappa_{\mathrm{tot}} = 0$.  Part (iii)
is the computation $\kappa(\mathrm{Vir}_c) = c/2$
(Computation~\ref{comp:virasoro-curvature}) combined with
$c_{\mathrm{ghost}} = -26$, so
$\kappa_{\mathrm{tot}} = c/2 - 13 = 0$ at $c = 26$.
\end{proof}

\begin{theorem}[Virasoro anomaly cancellation and chain comparison;
\ClaimStatusProvedHere]\label{thm:anomaly-physical-genus0}
Let $\cA\neq0$ be a conformal vertex algebra of central charge $c$.
Use the conformal ghost vacuum and the Virasoro charge of Theorem~\ref{thm:vir-square}.
Its square vanishes on the full algebraic state space if and only if $c=26$.
This is equivalent to $c+c_{\mathrm{gh}}=0$, and to $\kappa_{\mathrm{tot}}=0$ when $\kappa_{\mathrm{tot}}=(c-26)/2$.
At $c=26$, under all separately constructed target, relative-descent, section-carrier, filtration, symbol-map, and compatible-correction hypotheses of Theorem~\ref{thm:brst-bar-genus0}, the resulting map $\Phi$ is a quasi-isomorphism of the specified relative and target complexes. The augmentation-kernel target for $V\otimes bc$ is excluded by Proposition~\ref{prop:vir-no-augmentation}.
Identification of supplied anomaly operators requires their compatibility with $\Phi$.
\end{theorem}

\begin{proof}
The full operator identity and its nonzero detecting vector are given in Theorem~\ref{thm:vir-square}.
Corollary~\ref{cor:anomaly-physical-genus0} gives the scalar normalization and the conditional chain comparison.
Relative nilpotence at other central charges can occur on the carriers of Proposition~\ref{prop:vir-relative-three-term}.
\end{proof}

\begin{proposition}[Anomaly operators and scalar reflection]
\label{thm:anomaly-physical-km-w}
On the full conformal Virasoro ghost carrier, the anomaly operator vanishes exactly at matter central charge $26$.
Reflection of the principal Virasoro parameter pairs central charges $c,26-c$, whose half-central charges sum to $13$.
Reflection negates the affine scalar $a(k)$ of Proposition~\ref{cor:anomaly-duality-km}; the Sugawara half-central charges instead sum to $\dim\mathfrak g$.
The Maurer--Cartan equation for $\Omega^\bullet(M)\otimes\mathfrak g$ is the flatness equation on the trivial bundle of Theorem~\ref{thm:cs-koszul-km}.
\end{proposition}

\begin{proof}
Theorem~\ref{thm:vir-square} computes the anomaly and its nonzero detecting vector.
Propositions~\ref{cor:anomaly-duality-w} and~\ref{cor:anomaly-duality-km} give the scalar sums.
The last assertion follows by substituting the de Rham differential and Lie bracket into the Maurer--Cartan equation.
Transport to a different carrier requires the components of Proposition~\ref{thm:linf-mc-flatness}.
\end{proof}

\begin{conjecture}[Physical anomaly cancellation, higher genus;
\ClaimStatusConjectured]\label{conj:anomaly-physical}
For a specified anomaly-free BRST sheaf complex and a separately constructed unital chiral-chain complex, there is a quasi-isomorphism over each moduli space of curves that is compatible with coordinate descent, the connections, pairings, anomaly operators, and clutching.
The genus-zero comparison and its compatibility with these structures are included in this assertion.
\end{conjecture}

\begin{remark}[Scope]\label{rem:anomaly-scope}
For conformal vertex algebras at $c=26$, Theorem~\ref{thm:vir-square} constructs the algebraic charge and proves its nilpotence.
Theorem~\ref{thm:brst-bar-genus0} proves a comparison only after an actual target, relative descent and section carrier, compatible exhaustive split filtrations, a graded chain quasi-isomorphism, and compatible correction maps are supplied.
These genus-zero constructions remain required.
For $V\otimes bc$, the augmentation-kernel target is excluded by Proposition~\ref{prop:vir-no-augmentation}.
For the universal Virasoro vacuum, the standard restricted coordinate transitions fail by Proposition~\ref{prop:vir-relative-target-obstruction}.
The affine criterion of Theorem~\ref{thm:bar-semi-infinite-km} requires the actual coefficient and filtered chain maps.
Theorem~\ref{thm:bar-semi-infinite-w} further requires an equivariant map of specified double complexes and a valid totalization.
Theorem~\ref{thm:cbc26-polynomial-ds} constructs the polynomial reduction on its associative carrier.

A different unital chiral target requires its coefficient maps and vacuum normalization; its cohomology must then be computed on the chosen section carrier.
Integration requires the insertion maps, explicit pairing homotopy, and closed integration functional of Corollary~\ref{cor:string-amplitude-genus0}.
Higher-genus comparison additionally requires compatible renormalization and clutching constructions.
For Koszul dual pairs with $\kappa+\kappa'=0$, the scalar formula of Theorem~\ref{thm:modular-characteristic} gives
$\mathrm{obs}_g(\cA)+\mathrm{obs}_g(\cA^!)=(\kappa+\kappa')\lambda_g=0$.
This scalar equality does not construct the genus-zero or higher-genus chain maps.

\emph{Homotopy template} (Convention~\ref{conv:hms-levels}): Type~VII (physics-dictionary).
The conjecture asserts quasi-isomorphism of the $L_\infty$ algebras
controlling BRST and bar deformation problems, genus by genus.
(Contributing to Conjecture~\ref{conj:master-bv-brst}.)
\end{remark}

\subsection{\texorpdfstring{AdS$_3$/CFT$_2$ as curved Koszul duality}{AdS3/CFT2 as curved Koszul duality}}
\label{subsec:ads-cft-koszul}
\index{AdS/CFT!curved Koszul duality}

\begin{conjecture}[Holographic duality from bar-cobar;
\ClaimStatusConjectured]\label{conj:ads-cft-bar}
The curved bar complex of a boundary chiral algebra $\cA$ provides
an algebraic model for the bulk theory on AdS$_3$:
\begin{enumerate}[label=\textup{(\roman*)}]
\item The bar complex $\barB(\cA)$ computes the algebra of
  observables of twisted supergravity on AdS$_3$
  \textup{(}Costello--Paquette \cite{CP2020}\textup{)}.
\item The curvature $m_0 = \kappa \cdot \mathbf{1}$ encodes
  the cosmological constant $\Lambda \sim -1/R_{\mathrm{AdS}}^2$,
  with $\kappa \sim c/2 \sim R_{\mathrm{AdS}}^2/\ell_P^2$.
\item The genus expansion $\sum_g g_s^{2g-2} \barB^{(g)}$
  is the $1/N$ expansion of the dual gauge theory, with
  $g_s \sim 1/N$.
\item The bar-cobar adjunction
  $\barB(\cA) \dashv \Omega(\barB(\cA))$
  should furnish boundary-to-bulk and bulk-to-boundary maps.
\end{enumerate}
\end{conjecture}

\begin{remark}[Scope]\label{rem:ads-scope}
A comparison with twisted supergravity requires both the boundary algebraic comparison and a bulk observable complex.
The boundary comparison still needs its unital chiral carrier, coefficient maps, relative descent, and actual filtered chain map.
The polynomial Drinfeld--Sokolov reduction in Theorem~\ref{thm:cbc26-polynomial-ds} constructs an associative comparison on its declared carrier.
It supplies no chiral collision maps or bulk observables.
For a comparison over curves, Proposition~\ref{prop:cbc26-horizontal-comparison} identifies the additional horizontal chain equation.
\end{remark}

\begin{remark}[Chiral comparison data]\label{rem:brst-chain-map-pieces}
\index{BRST!chain map to bar complex}
The affine criterion of Theorem~\ref{thm:bar-semi-infinite-km} requires the actual coefficient and filtered chain maps.
Theorem~\ref{thm:bar-semi-infinite-w} further requires an equivariant map of specified double complexes and a valid totalization.
Theorem~\ref{thm:cbc26-polynomial-ds} constructs the polynomial reduction on its associative carrier.
A chiral application must construct the coefficient and collision maps, unit normalization, relative coordinate descent, and section carrier.
It must then supply compatible exhaustive split filtrations, a genuine graded chain quasi-isomorphism, and compatible solutions of the Hom differential equations at every filtration order.
These requirements remain at genus zero.

For $V\otimes bc$, the augmentation-kernel target fails by Proposition~\ref{prop:vir-no-augmentation}.
For the universal Virasoro vacuum, the standard relative transition restriction and the proposed replacement by absolute states fail by Proposition~\ref{prop:vir-relative-target-obstruction}.
A different unital target requires a new construction and a target-cohomology calculation.
After a chain comparison is constructed, binary operations still require~\eqref{eq:vir-binary-intertwining}; insertion pairings require~\eqref{eq:vir-pairing-homotopy} and a closed integration functional.
Higher genus additionally requires compatible renormalization and clutching maps.
\end{remark}

\subsection{\texorpdfstring{3d mirror symmetry from $\Eone$-chiral Koszul duality}{3d mirror symmetry from E1-chiral Koszul duality}}
\label{subsec:3d-mirror}
\index{3d mirror symmetry}

\begin{conjecture}[3d mirror symmetry;
\ClaimStatusConjectured]\label{conj:3d-mirror}
For 3d $\mathcal{N}=4$ gauge theories with Coulomb and Higgs
branches, $\Eone$-chiral Koszul duality provides the algebraic
mechanism:
\begin{enumerate}[label=\textup{(\roman*)}]
\item The Coulomb branch algebra \textup{(}in the sense of
  Braverman--Finkelberg--Nakajima \cite{BFN18}\textup{)}
  is the $\Eone$-chiral bar complex of the Higgs branch algebra.
\item The curved $A_\infty$ structure on the bar complex
  encodes mass and FI parameter deformations.
\item Symplectic duality \textup{(}Braden--Licata--Proudfoot--Webster\textup{)}
  is the module-level avatar of $\Eone$-chiral Koszul duality
  \textup{(}Theorem~\textup{\ref{thm:e1-chiral-koszul-duality})}.
\end{enumerate}
\end{conjecture}

\begin{remark}[Scope]\label{rem:3d-mirror-scope}
This is highly speculative.  The mathematical ingredients ---
$\Eone$-chiral Koszul duality
(Theorem~\ref{thm:e1-chiral-koszul-duality}), Yangian bar
complexes (\S\ref{sec:yangian-bar-explicit}), and the
Coulomb branch construction of \cite{BFN18} --- exist
independently.  However, connecting them requires: (a)~identifying
the Coulomb branch ring with a specific $\Eone$-chiral bar
complex; (b)~showing that symplectic duality (exchange of Coulomb
and Higgs branches) corresponds to bar-cobar inversion; (c)~relating
the mass/FI deformations to the curvature of the bar complex.
Each of these steps requires substantial new work combining
3d gauge theory, symplectic geometry, and the $\Eone$-chiral
framework.

\emph{Homotopy template} (Convention~\ref{conv:hms-levels}): Type~II (equivalence of homotopy categories).
The conjecture asserts an equivalence of $\Eone$-monoidal $\infty$-categories
(Coulomb branch factorization vs.\ Higgs branch bar-cobar).
(Contributing to Conjecture~\ref{conj:master-dk-kl}.)
\end{remark}

\subsection{Noncommutative Hodge theory from genus tower}
\label{subsec:nc-hodge}
\index{noncommutative Hodge theory}

\begin{definition}[Chiral Hodge numbers]\label{def:chiral-hodge-numbers}
\index{Hodge numbers!chiral}
For a Koszul chiral algebra $\cA$ and genus $g \geq 1$, the
\emph{chiral Hodge numbers} are
\[
h^{p,q}_g(\cA)
\;:=\;
\dim E_2^{p,q}(\barB^{(g)}(\cA))
\]
where $E_2^{p,q}$ is the $E_2$ page of the genus spectral
sequence (Spectral Sequence~\ref{ss:genus}).
\end{definition}

\begin{proposition}[Hodge symmetry from complementarity;
\ClaimStatusProvedHere]\label{prop:nc-hodge-symmetry}
\index{Hodge symmetry!chiral}
For a Koszul chiral algebra $\cA$ with dual $\cA^!$:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \textup{(Hodge symmetry)}
  $h^{p,q}_g(\cA) = h^{q,p}_g(\cA^!)$ for all $g, p, q$.
\item \textup{(Hodge-to-de~Rham degeneration)}
  The genus spectral sequence degenerates at $E_2$
  for Koszul algebras, so that the total chiral Hodge number
  $h^n_g(\cA) = \sum_{p+q=n} h^{p,q}_g(\cA)$
  equals the hypercohomology dimension
  $\dim \mathbb{H}^n(\overline{\mathcal{M}}_g,
  \barB^{(g)}_\bullet(\cA))$.
\item \textup{(Self-dual Hodge diamond)}
  If $\cA \simeq \cA^!$ \textup{(}self-dual\textup{)},
  then $h^{p,q}_g(\cA) = h^{q,p}_g(\cA)$.
\end{enumerate}
\end{proposition}

\begin{proof}
Part (i): Theorem~\ref{thm:quantum-complementarity-main}
provides the complementarity isomorphism
$E_2^{p,q}(\cA) \cong E_2^{q,p}(\cA^!)^\vee$, which gives
$h^{p,q}_g(\cA) = h^{q,p}_g(\cA^!)$.  Part (ii): the $E_2$
collapse for Koszul algebras is
Theorem~\ref{thm:bar-cobar-spectral-sequence}.  Part (iii)
is immediate from (i).
\end{proof}

\begin{remark}[Hodge symmetry from universal MC duality]
\label{rem:hodge-theta}
The Hodge symmetry $h^{p,q}_g(\cA) = h^{q,p}_g(\cA^!)$
has a transparent origin in the universal Maurer--Cartan
class (Theorem~\ref{thm:explicit-theta}): Verdier duality
sends $\Theta_\cA \mapsto -\Theta_{\cA^!}$, which
exchanges the $E_2$ pages of $\cA$ and $\cA^!$ via
$\mathbb{D}(\barB^{(g)}(\cA)) \cong \barB^{(g)}(\cA^!)$.
For self-dual algebras ($\cA \simeq \cA^!$, so
$\Theta_\cA = -\Theta_\cA = 0$, i.e.\ $\kappa = 0$),
the chiral Hodge diamond is symmetric and the bar complex
is uncurved --- the strongest form of Hodge symmetry.
\end{remark}

\begin{remark}[Computability of the weight filtration]\label{rem:weight-filtration-computable}
The weight filtration on $H^*(\overline{C}_n(X))$ that enters the genus spectral sequence $E_2^{p,q}$ is explicitly computable by combinatorial methods.  Bibby--Gadish~\cite{BG24} establish effective algorithms for computing the mixed Hodge structure on configuration space cohomology via intersection lattice data.  For our chiral Hodge numbers $h^{p,q}_g(\cA)$, this means the $E_2$ page can in principle be computed from the combinatorial data of the Orlik--Solomon algebra (for genus~$0$) and the augmented OS algebra (for genus~$g \geq 1$), without needing the full geometric machinery of mixed Hodge modules.
\end{remark}

\begin{conjecture}[Twistor interpretation of genus expansion;
\ClaimStatusConjectured]\label{conj:nc-hodge}
The genus expansion variable $g_s$ in the total bar complex
$\barB^{\mathrm{total}}(\cA) = \sum_g g_s^{2g-2}\, \barB^{(g)}(\cA)$
plays the role of the twistor parameter $\lambda$ in the
Kontsevich--Soibelman noncommutative Hodge
theory~\cite{KontsevichSoibelman}: there exists a
$\mathbb{C}^*$-action on the de~Rham complex of the genus tower
such that the weight-$k$ subspace at genus~$g$ has dimension
$h^{k,n-k}_g(\cA)$.
\end{conjecture}

\begin{remark}[Scope]\label{rem:nc-hodge-scope}
The Hodge numbers and their symmetry are proved
(Proposition~\ref{prop:nc-hodge-symmetry}), and the $E_2$
degeneration is established
(Theorem~\ref{thm:bar-cobar-spectral-sequence}).  The remaining
conjecture is the identification with the Kontsevich--Soibelman
$\mathbb{C}^*$-action, which requires establishing a twistor
structure on the genus tower.  There is currently no framework
in which to formulate this precisely for chiral algebras.

\emph{Homotopy template} (Convention~\ref{conv:hms-levels}): Type~III
(shifted-symplectic/Lagrangian enhancement).  The conjecture
upgrades the genus spectral sequence to a $\mathbb{C}^*$-equivariant
shifted-symplectic structure on the total deformation complex.
(Part of the MC2 theorem package; Theorem~\ref{thm:mc2-full-resolution}.)
\end{remark}

\subsection{The nine programmes: synthesis and interdependence}
\label{subsec:programme-synthesis}
\index{research programmes!interdependence}

Programmes~I (higher-dimensional Koszul duality,
\S\ref{subsec:higher-dim-kd}) and~II (Vassiliev invariants,
\S\ref{subsec:vassiliev}) share the critical-level bar complex
as common foundation, but diverge in application: Programme~I
computes $\mathsf{E}_n$-operadic Koszul duals via configuration
space integrals on $n$-manifolds, while Programme~II specializes
from complex curves to real circles, extracting knot invariants via
the Kontsevich integral.  At $n = 3$, the two programmes converge:
the $\mathsf{E}_3$-bar complex on a $3$-manifold computes
perturbative Chern--Simons invariants, and the restriction to
configuration spaces of circles in $S^3$ produces precisely the
Vassiliev weight systems of Programme~II
(Proposition~\ref{prop:vassiliev-genus0}).

The anomaly cancellation programme
(\S\ref{subsec:anomaly-koszul}) is the physical interpretation of
the curvature theory: $\kappa_{\mathrm{tot}} = 0$ is both the
mathematical condition $d_{\mathrm{bar}}^2 = 0$ and the
physical condition for consistent quantization
(Theorem~\ref{thm:anomaly-koszul}).  This feeds directly into the
AdS$_3$/CFT$_2$ programme (\S\ref{subsec:ads-cft-koszul}), where
the curvature $m_0 = \kappa \cdot \mathbf{1}$ plays the role of
the cosmological constant: the bar-cobar adjunction
$\barB(\cA) \dashv \Omega(\barB(\cA))$ models the
boundary-to-bulk and bulk-to-boundary maps of holographic duality.
The $3$d mirror symmetry programme (\S\ref{subsec:3d-mirror})
extends this physical circle from $2$d to $3$d, with
$\Eone$-chiral Koszul duality
(Theorem~\ref{thm:e1-chiral-koszul-duality}) replacing the
$\Etwo$-chiral theory and Coulomb--Higgs exchange replacing
bar-cobar inversion.

The noncommutative Hodge programme (\S\ref{subsec:nc-hodge})
reinterprets the entire genus tower as a twistor family: the
genus expansion variable $g_s$ plays the role of the
Kontsevich--Soibelman twistor parameter, and the chiral Hodge
numbers $h^{p,q}_g(\cA)$ provide the Hodge-theoretic data.
This programme depends on the complementarity theorem
(Theorem~\ref{thm:quantum-complementarity-main}), which supplies
the Hodge symmetry $h^{p,q}_g(\cA) = h^{q,p}_g(\cA^!)$.

\begin{remark}[Unifying vision: modular Koszul duality for
factorization algebras]\label{rem:unifying-vision}
\index{modular Koszul duality!unifying conjecture}
All of the programmes above are facets of a single conjecture:
for a factorization algebra~$\mathcal{F}$ on an oriented
$n$-manifold~$M$, the Feynman transform
$\mathrm{FT}(\mathcal{F})$ computes the Koszul dual
factorization algebra on~$M$ with reversed orientation.
The evidence is the following hierarchy of established and
conjectural instances:
\begin{enumerate}[label=\textup{(\roman*)}]
\item At $n = 1$ \textup{(}$A_\infty$-algebras\textup{)}:
  classical bar-cobar duality
  \textup{(}Loday--Vallette~\cite{LV12}\textup{)}.  This is the
  foundational case, with the bar and cobar constructions as mutual
  inverses on the Koszul locus.
\item At $n = 2$ with complex structure
  \textup{(}chiral algebras\textup{)}: the Main Theorems~A, B, C
  of this monograph
  \textup{(}Theorems~\ref{thm:bar-cobar-isomorphism-main},
  \ref{thm:higher-genus-inversion},
  \ref{thm:quantum-complementarity-main}\textup{)}.  The Feynman
  transform is realized by Verdier duality on FM compactifications,
  and the genus expansion provides the quantum corrections.
\item At $n = 2$ topological
  \textup{(}$\mathsf{E}_2$-algebras\textup{)}: Kontsevich
  formality~\cite{Kon03}, which identifies the $\mathsf{E}_2$-bar
  complex with the deformation complex of the associative operad.
\item At $n = 3$
  \textup{(}$\mathsf{E}_3$-algebras\textup{)}:
  perturbative Chern--Simons invariants, with Vassiliev weight
  systems extracted from the $\mathsf{E}_3$-bar complex
  \textup{(}Conjecture~\ref{conj:vassiliev-bar}\textup{)}.
\item At $n = \infty$
  \textup{(}$\mathsf{E}_\infty$-algebras\textup{)}:
  factorization homology duality of
  Ayala--Francis~\cite{AF15}, which provides the
  $\infty$-categorical framework for Poincar\'e--Koszul
  duality on arbitrary manifolds.
\end{enumerate}
This monograph provides case~(ii) in full detail; the passage
from (ii) to the general conjecture is the content of
\S\ref{sec:modular-koszul-programme} below.  For the individual comparison statements, see
\S\ref{subsec:nine-futures}.
\end{remark}


\section{Bridges to Volume~II}
\label{sec:cross-volume-bridges}
\index{Volume II!bridges from Volume I}

Volume~II develops the $A_\infty$-chiral Hochschild theory in
three space-time dimensions with the operad
$\mathrm{SC}^{\mathrm{ch,top}}$.  Five bridges connect the two
volumes:

\begin{conjecture}[Bar-cobar bridge; \ClaimStatusConjectured]
\label{conj:vol2-bar-cobar-bridge}
The $\mathrm{SC}^{\mathrm{ch,top}}$ bar-cobar adjunction of
Volume~II recovers the chiral bar-cobar adjunction of Theorem~A
(Theorem~\ref{thm:bar-cobar-isomorphism-main}) for boundary
operators of the 3d theory.
\end{conjecture}

\begin{conjecture}[Hochschild bridge; \ClaimStatusConjectured]
\label{conj:vol2-hochschild-bridge}
The bulk algebra of the 3d theory is the chiral Hochschild cochain
complex $C^*_{\mathrm{ch}}(\cA)$, recovering Theorem~H
(Theorem~\ref{thm:w-algebra-hochschild}) via dimensional
descent.
\end{conjecture}

\begin{conjecture}[DK-YBE bridge; \ClaimStatusConjectured]
\label{conj:vol2-dk-ybe-bridge}
The spectral braiding $r(z) = \int_0^\infty e^{-pz}
\{a_\lambda b\}\,dp$ (the Laplace transform of the
$\lambda$-bracket) recovers the DK-0 chain-level
monodromy comparison (Theorem~\ref{thm:derived-dk-affine}).
\end{conjecture}

\begin{conjecture}[\texorpdfstring{$\mathcal{W}$}{W}-algebra bridge; \ClaimStatusConjectured]
\label{conj:vol2-w-algebra-bridge}
The Feynman $m_k$ operations of the 3d theory, restricted to
the $\mathcal{W}$-algebra family, match the bar differential computed in the
$\mathcal{W}$-algebra chapters of Part~II.
\end{conjecture}

\begin{conjecture}[BV functor bridge; \ClaimStatusConjectured]
\label{conj:vol2-bv-functor-bridge}
Analytic hypotheses (H1)--(H4) of Volume~II define the
physics-to-algebra functor that realizes the
$\mathrm{BV} = \mathrm{bar}$ identification at higher
genus (MC5, Conjecture~\ref{conj:master-bv-brst}).
\end{conjecture}


\section[Towards modular homotopy theory]{Towards modular homotopy theory for factorization algebras on curves}
\label{sec:modular-koszul-programme}
\index{modular Koszul duality!programme}

The modular characteristic theorem (Theorem~D,
Theorem~\ref{thm:modular-characteristic}) establishes the scalar
logarithmic datum~$\kappa(\cA)$.  This section identifies the six
components---five structural extensions and the Hochschild
cohomology theorem---needed to pass from the proved modular Koszul
core to modular homotopy theory for factorization algebras on
curves---that is, to complete the nilpotence-periodicity
correspondence
(Remark~\ref{rem:nilpotence-periodicity}) from the scalar level to
the full categorical level:

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{@{}cp{4.4cm}p{3.8cm}p{4.6cm}@{}}
\toprule
\textbf{Label} & \textbf{Statement} & \textbf{Status} & \textbf{Reference} \\
\midrule
$A_{\mathrm{mod}}$ & Bar-cobar intertwined with Verdier, & \textbf{Proved} & Thm~\ref{thm:bar-cobar-isomorphism-main} \\
  & functorial over $\overline{\mathcal{M}}_{g,n}$ & & \\[2pt]
$B_{\mathrm{mod}}$ & Inversion on Koszul locus; coderived & Proved on Koszul locus; & Thm~\ref{thm:higher-genus-inversion}, \\
  & persistence off it & coderived persistence conjectural & \S\ref{subsec:coderived-ran} \\[2pt]
$C_{\mathrm{mod}}$ & Shifted symplectic complementarity & \textbf{Proved} & Thms~\ref{thm:quantum-complementarity-main}, \\
  & (PTVV Lagrangian embedding) & & \ref{thm:shifted-symplectic-complementarity} \\[2pt]
Index & GRR: genus series $= \kappa(\cA) \cdot (\hat{A}(ix) - 1)$ & \textbf{Proved} & Thm~\ref{thm:family-index} \\[2pt]
DK & DK-0/1/$1\frac{1}{2}$: chain-level, eval-locus, lattice; & DK-0/1/$1\frac{1}{2}$: proved (all types); & Thms~\ref{thm:derived-dk-affine}, \\
  & DK-2/3: factorization DK; & DK-2/3: \textbf{proved} (eval-gen.\ core, all types, domain: $\mathcal{O}_{\mathrm{poly}}$; & \ref{thm:derived-dk-yangian}, \ref{thm:factorization-dk-eval}, \\
  & DK-4/5: dg-shifted/triple bridge & uses Molev PBW \cite{molev-yangians}); DK-4: ML proved, alg.\ id.\ open; DK-5: conj. & \ref{thm:dk-fd-typeA}, \ref{cor:dk23-all-types}, \ref{thm:rtt-mittag-leffler}; Conj.~\ref{conj:full-dk-bridge} \\[2pt]
$H$ & Chiral Hochschild: $\ChirHoch^*(\Walg^k(\fg)) \cong \mathbb{C}[\Theta_1,\ldots,\Theta_r]$; & \textbf{Proved} (generic level, & Thms~\ref{thm:w-algebra-hochschild}, \\
  & period $2$ (rank $1$), polynomial (rank $\geq 2$); & critical level: ProvedElsewhere) & \ref{thm:virasoro-hochschild}, \ref{thm:critical-level-cohomology}; \\
  & Koszul functoriality via Connes~$S$ & & Thm~\ref{thm:main-koszul-hoch} \\
\bottomrule
\end{tabular}
\end{center}

\noindent
The subsections below develop each component, separating what is
established from what remains.  For $A_{\mathrm{mod}}$: the present
proof establishes functorial compatibility of bar and cobar over
$\overline{\mathcal{M}}_{g,n}$; whether this lifts to an equivalence
of modular operads is a further question, not addressed here.

\medskip

\subsection{The coderived Ran-space formalism}
\label{subsec:coderived-ran}

The bar-cobar adjunction is most naturally an equivalence of
$\infty$-categories---the categorical $\exp/\log$ correspondence
(Remark~\ref{rem:nilpotence-periodicity}):
\[
\bar{B}_X \colon
\operatorname{Alg}_{\mathrm{aug}}(\operatorname{Fact}(X))
\;\rightleftarrows\;
\operatorname{CoAlg}_{\mathrm{conil}}(\operatorname{Fact}(X))
\,:\!\Omega_X,
\]
where $\operatorname{Fact}(X)$ denotes factorization algebras on
$\operatorname{Ran}(X)$ (cf.\ Appendix~\ref{app:existence-criteria}).

On the Koszul locus this is an equivalence: $\exp \circ \log = \mathrm{id}$
on the convergence domain.  When curvature is present---that is,
when the categorical logarithm acquires non-trivial monodromy---the
correct ambient category is not the ordinary derived category but the
\emph{coderived category} in the sense of
Positselski~\cite{Positselski11}: the analytic continuation that
extends the $\exp/\log$ correspondence past the convergence radius.

Curved objects produce acyclic-but-not-contractible complexes that the
derived category erases too brutally; the coderived category retains
the monodromy data that the ordinary derived category discards, just
as the multi-valued logarithm retains branch information that a
single-valued determination loses.

The provisional coderived category
$\mathsf{D}^{\mathrm{co}}_{\mathrm{prov}}(X)$
(Definition~\ref{def:provisional-coderived},
Proposition~\ref{prop:coderived-adequacy})
provides a working substitute within the scope of this monograph.
The Koszul locus---where bar-cobar inversion
holds unconditionally (Theorem~\ref{thm:higher-genus-inversion})---is
the locus where the coderived and derived categories agree.

\emph{What is established.}
Appendix~\ref{app:existence-criteria} compares the strict,
completed, and literature existence paradigms for bar-cobar on
$\operatorname{Ran}(X)$.  Theorem~\ref{thm:bar-cobar-isomorphism-main}
provides the adjunction; Theorem~\ref{thm:higher-genus-inversion}
provides inversion on the Koszul locus.
Appendix~\ref{app:coderived},
\S\ref{subsec:coderived-ran-formalism} develops the coderived
and contraderived categories on~$\operatorname{Ran}(X)$: curved
factorization coalgebras with CDG-factorization compatibility
(Definition~\ref{def:curved-fact-coalgebra}), coacyclic
factorization objects
(Definition~\ref{def:coacyclic-fact}), stratified conservative
restriction via the injective characterization
(Theorem~\ref{thm:stratified-conservative-restriction}),
and the factorization co-contra correspondence
(Theorem~\ref{thm:fact-co-contra-general}).  The provisional
coderived category embeds fully faithfully
(Proposition~\ref{prop:provisional-embedding}).

\emph{What remains.}
A single conjecture: off-Koszul bar-cobar inversion on
$\operatorname{Ran}(X)$
(Conjecture~\ref{conj:off-koszul-ran-inversion})---the statement
that $\Omega_X \barB_X(\cA) \to \cA$ is an isomorphism in
$D^{\mathrm{co}}(\mathrm{CoFact}(X))$ without Koszulness.  The
abstract precedent (Positselski~\cite[Theorem~7.2]{Positselski11}),
the on-Koszul case (Theorem~\ref{thm:higher-genus-inversion}), and
the completed Yangian case
(Corollary~\ref{cor:completed-bar-cobar-yangian}) provide evidence.

\subsection{The Lagrangian form of complementarity}

Theorem~C (Theorem~\ref{thm:quantum-complementarity-main}) establishes
the direct-sum decomposition
$Q_g(\cA) \oplus Q_g(\cA^!)
= H^*(\overline{\mathcal{M}}_g, \mathcal{Z}(\cA))$
via the eigenspace decomposition of the Verdier involution.
The natural strengthening is a Lagrangian-polarization theorem,
now proved:

\begin{theorem}[Lagrangian complementarity;
\ClaimStatusProvedHere]\label{thm:lagrangian-complementarity}
\index{Lagrangian!complementarity|textbf}
For a modular Koszul pair $(\cA, \cA^!)$,
the complex
$C_g := R\Gamma(\overline{\mathcal{M}}_g, \mathcal{Z}_{\cA})$
carries two shifted symplectic structures, each admitting
$Q_g(\cA)$ and $Q_g(\cA^!)$ as complementary Lagrangians in the
sense of Pantev--To\"en--Vaqui\'e--Vezzosi~\cite{PTVV13}:
\begin{enumerate}[label=\textup{(\roman*)}]
\item a $(-(3g{-}3))$-shifted symplectic structure from
  Verdier duality on the center local system
  \textup{(}Proposition~\textup{\ref{prop:ptvv-lagrangian}}\textup{)};
\item a $(-1)$-shifted symplectic structure on the formal moduli
  $\mathrm{Def}_g(\cA)$ from the BV antibracket
  \textup{(}Theorem~\textup{\ref{thm:shifted-symplectic-complementarity}}\textup{)}.
\end{enumerate}
\end{theorem}

\begin{remark}[Degree clarification]\label{rem:lagrangian-conj-scope}
The original formulation of this result stated a single
$(-1)$-shifted symplectic structure on~$C_g$.  The proved version
reveals two structures of different degrees: the Verdier pairing
gives $(-(3g{-}3))$-shifted on~$C_g$ itself, while the BV
antibracket gives $(-1)$-shifted on the formal moduli.  Both support
the same Lagrangian polarization.
See Remark~\ref{rem:ptvv-relation} for the compatibility.
\end{remark}

\subsection{The universal Maurer--Cartan class}

The genus tower $\{\barB^{(g)}(\cA)\}_{g \geq 0}$
(Theorem~\ref{thm:master-tower}) is controlled by a universal
obstruction.

\begin{theorem}[Universal MC class;
\ClaimStatusProvedHere]\label{thm:universal-MC}
\index{Maurer--Cartan class!universal}
For a modular Koszul chiral algebra $\cA$ with simple Lie
symmetry~$\mathfrak{g}$, there exists a class
\[
\Theta_{\cA} \;\in\;
\operatorname{MC}\!\left(
  \operatorname{Def}_{\mathrm{cyc}}(\cA)
  \;\widehat{\otimes}\;
  R\Gamma\!\left(\overline{\mathcal{M}}_{g,\bullet},\, \mathbb{Q}\right)
\right)
\]
controlling the full genus tower as a single Maurer--Cartan deformation,
with three properties:
\begin{enumerate}[label=\textup{(\roman*)}]
\item Its image under the scalar trace is the sequence of obstruction
  coefficients: $\operatorname{tr}(\Theta_{\cA})
  = \sum_{g \geq 1} \kappa(\cA) \cdot \lambda_g$.
\item It is compatible with clutching:
  $\xi^*(\Theta_{\cA})
  = \Theta_{\cA} \star \Theta_{\cA}$
  under the boundary maps
  $\xi \colon \overline{\mathcal{M}}_{g_1,n_1+1} \times
  \overline{\mathcal{M}}_{g_2,n_2+1} \to
  \overline{\mathcal{M}}_{g,n}$.
\item Under Verdier duality:
  $\mathbb{D}(\Theta_{\cA}) = \Theta_{\cA^!}$.
\end{enumerate}
\end{theorem}

\begin{proof}
This is Theorem~\ref{thm:universal-theta}, proved via the
assembly of Theorem~\ref{thm:mc2-full-resolution}: the cyclic
$L_\infty$-algebra $\Defcyc(\cA)$ is constructed by
Theorem~\ref{thm:cyclic-linf-graph}, the completed tensor product
converges with clutching by
Proposition~\ref{prop:geometric-modular-operadic-mc}, and the
tautological line support $o_g = \kappa \lambda_g$ is established
by Theorem~\ref{thm:tautological-line-support}.
\end{proof}

\begin{remark}[Scope]\label{rem:universal-MC-scope}
The scalar shadow $\kappa(\cA) \cdot \lambda_g$ is established
(Theorem~\ref{thm:genus-universality}).  The clutching compatibility
is implicit in the modular operad structure of the bar complex
(Theorem~\ref{thm:prism-higher-genus}).  At the one-channel level,
Proposition~\ref{prop:tautological-line-support-criterion} shows that
these two geometric inputs would already force tautological-line
support once they separate the relevant sector, and
Proposition~\ref{prop:one-channel-verdier-criterion} reduces the
remaining duality flank to a $\sigma$-stable Lagrangian two-plane on
that sector.  Proposition~\ref{prop:one-channel-ptvv-criterion} then
reduces the remaining H-level lift to perfect one-channel projector
subcomplexes whose cross Verdier pairing is a PTVV quasi-isomorphism,
and Proposition~\ref{prop:one-channel-chain-model-criterion} reduces
that lift further to explicit one-channel coderivation subcomplexes in
$\Defcyc(\cA)$ and $\Defcyc(\cA^!)$.  Proposition~\ref{prop:one-channel-seed-criterion}
then reduces those subcomplexes further to finite low-bar-length
cyclic coderivation seeds in
$\operatorname{CoDer}^{\mathrm{cyc}}(\widehat{\barB}_X(\cA))[1]$ and
$\operatorname{CoDer}^{\mathrm{cyc}}(\widehat{\barB}_X(\cA^!))[1]$.
Proposition~\ref{prop:one-channel-minimal-seed-packet-criterion}
compresses those finite seed sets further to one distinguished
degree-$2$ cocycle on each side together with bar-length-$\leq 3$
correction packets and a finite pairing matrix.
Proposition~\ref{prop:one-channel-visible-lowarity-packet-criterion}
then identifies the remaining packet with the three visible low-arity
ingredients already supported by the current MC2 surface: the
simple-pole bracket sector, the normalized double-pole pairing matrix,
and the Killing $l_3^\eta$ sector.
Proposition~\ref{prop:one-channel-canonical-transfer-criterion} then
reduces that visible packet further to a canonical transfer package on
the one-channel seed spaces: one cyclic seed, one shared
generator-seed lift producing the Killing $l_3^\eta$ sector, and one
functorial normalization splitting off the one-channel cocycle line.
Proposition~\ref{prop:one-channel-transfer-law-criterion} then reduces
that canonical package further to an explicit root-string transfer law
on those seed spaces: the genus-$2$/genus-$3$ profile
\[
O_2=t^2(ae_{12}+bf_1-mf_2),
\qquad
O_3^\eta=a m t^3\eta,
\]
together with obstruction-side recovery of $(a,b,m)$ and the same
coefficient-extraction normalization.
Proposition~\ref{prop:one-channel-root-string-chart-criterion} then
reduces that explicit law further to a chart-existence/uniqueness
statement: the ordered root-string chart is forced by the one-channel
support graph and normalization data, up to rescaling of the three seed
lines.
Proposition~\ref{prop:one-channel-intrinsic-line-detection-criterion}
then reduces that chart statement further to intrinsic line detection:
the seed, simple-pole, and Killing support lines must be canonically
picked out by $l_{2,\mathrm{sp}}$, $\beta_g^{\mathrm{dp}}$,
$\nu_g^{\mathrm{sp}}$, and the support of
$(O_{2,g},O_{3,g}^\eta)$.
Proposition~\ref{prop:one-channel-automorphism-rigidity-criterion}
then reduces that remaining line-detection step to automorphism
rigidity: those support lines are the unique invariant lines for the
one-channel support automorphism group compatible with the bracket,
pairing, normalization, and obstruction profile.
Proposition~\ref{prop:one-channel-support-graph-stabilizer-criterion}
then reduces that rigidity statement further to a finite stabilizer
computation on the labeled one-channel support graph: the fixed
vertices must be exactly the simple-pole, residual-kernel, and Killing
support lines.
Proposition~\ref{prop:one-channel-incidence-orbit-criterion}
then reduces that stabilizer statement further to an explicit
incidence-matrix / orbit-count computation on the visible one-channel
graph: the relevant support lines are exactly the singleton orbits of
the permutation group determined by the bracket, pairing,
normalization, and support indicators.
Proposition~\ref{prop:one-channel-visible-orbit-table-criterion}
then reduces that bounded permutation problem further to a universal
three-case visible orbit table on the
$m=1,2,3$ root-string types, equivalently
$\mathfrak{sl}_3,\mathfrak{sp}_4,\mathfrak{g}_2$.
Proposition~\ref{prop:one-channel-canonical-universal-orbit-table-criterion}
then collapses that three-case verification to direct
lookup/identification against one fixed labeled orbit table.
Proposition~\ref{prop:one-channel-universal-invariant-signature-criterion}
then collapses that fixed-table identification further to the minimal
visible invariant signature packet forcing that table.
Proposition~\ref{prop:one-channel-signed-seed-character-criterion}
then collapses that invariant packet further to the universal signed
seed-character law recovering it.
Proposition~\ref{prop:one-channel-two-sign-plus-normalization-scalar-criterion}
then collapses that character further to the reduced datum of two
root-string signs and one normalization scalar.
Proposition~\ref{prop:one-channel-parity-sign-plus-normalization-scalar-criterion}
then collapses that reduced datum further to one root-string parity
sign plus one normalization scalar.
Proposition~\ref{prop:one-channel-parity-forcing-criterion}
then collapses that parity-sign datum further to one chart-normalized
seed scalar.
The full non-scalar class $\Theta_{\cA}$ is now constructed
(Theorem~\ref{thm:mc2-full-resolution}): (a)~the cyclic
deformation complex $\operatorname{Def}_{\mathrm{cyc}}(\cA)$
exists as a cyclic $L_\infty$ algebra
(Theorem~\ref{thm:cyclic-linf-graph}); (b)~the Maurer--Cartan
equation in the completed tensor product with tautological classes
is solved by the conjunction of
Proposition~\ref{prop:geometric-modular-operadic-mc} and
Theorem~\ref{thm:tautological-line-support}.
This completes the ``modular homotopy theory'' for
factorization algebras.

\emph{Homotopy template} (Convention~\ref{conv:hms-levels}): Type~I
(existence of a homotopy object).  At H-level: a filtered cyclic $L_\infty$
algebra $\operatorname{Def}_{\mathrm{cyc}}(\cA)$ and an MC element $\Theta_\cA$
with trace, clutching, and Verdier compatibility.  The scalar shadow is
$\kappa(\cA) \cdot \lambda_g$ (proved, Theorem~\ref{thm:genus-universality}).
The full non-scalar class is Theorem~\ref{thm:universal-theta}
(Theorem~\ref{thm:master-theta}).
\end{remark}

\subsection{The discriminant as spectral invariant}
\index{discriminant!spectral invariant}

The shared discriminant $\Delta(x) = (1 - 3x)(1+x)$ across
$\mathfrak{sl}_2$, Virasoro, and $\beta\gamma$ systems
(Theorem~\ref{thm:ds-bar-gf-discriminant}) is not a coincidence
of generating functions.  It is the \emph{first genuinely
nontrivial non-scalar characteristic class} of the modular Koszul
object: where $\kappa(\cA)$ is a scalar measuring the curvature
of $d^2$, the discriminant $\Delta_{\cA}(x)$ records the branch
geometry of the dual Hilbert series.

Distinct chiral theories can lie on the same modular spectral
sheet --- sharing the same $\Delta$ --- even when their local
operator content is entirely different.  The underlying reason
is functorial: Drinfeld--Sokolov reduction changes the growth pole
(i.e.\ the leading OPE data) while preserving the discriminant
family.  This is compatible with the observation that
$\Delta_{\cA}$ depends only on the quadratic OPE data, which is
the Koszul-dual-determining datum.

\begin{theorem}[Discriminant as spectral determinant --- verified cases;
\ClaimStatusProvedHere]
\label{thm:discriminant-spectral-verified}
\index{discriminant!spectral determinant}
For $\cA \in \{\widehat{\mathfrak{sl}}_2,\, \mathrm{Vir},\,
\beta\gamma\}$, the bar-cohomology discriminant satisfies
\[
\Delta_{\cA}(x) \;=\; \det(1 - x\, T_{\mathrm{br}})
\]
where $T_{\mathrm{br}}$ is a $d \times d$ matrix whose eigenvalues
are the reciprocals of the branch points of the algebraic Koszul
dual Hilbert series $P_{\cA^!}(x)$, with $d$ the degree of
algebraicity.
In all three cases $d = 2$, the branch points are $x = 1/3$
and $x = -1$, and
$\Delta(x) = (1 - 3x)(1 + x)$.
\end{theorem}

\begin{proof}
Each of the three Hilbert series involves $\sqrt{1 - 2x - 3x^2}$
(Theorem~\ref{thm:ds-bar-gf-discriminant}):
$P_{\widehat{\mathfrak{sl}}_2}$ and $P_{\mathrm{Vir}}$ satisfy
quadratic equations with discriminant $(1+x)(1-3x)$, while
$P_{\beta\gamma}^2 = (1+x)/(1-3x)$ has the same branch locus.

The branch points of any algebraic function $P(x)$ satisfying
$a(x)P^2 + b(x)P + c(x) = 0$ are the zeros of
$\mathrm{Disc}_P = b(x)^2 - 4a(x)c(x)$.  For
$\widehat{\mathfrak{sl}}_2$, the algebraic equation is
$x(1+x)P^2 - (1+x)P + 1 = 0$
(Theorem~\ref{thm:ds-bar-gf-discriminant}), giving
\[
\mathrm{Disc}_P = (1+x)^2 - 4x(1+x) = (1+x)(1-3x).
\]
The branch points are $x_1 = -1$ and $x_2 = 1/3$.
Setting $\alpha_i = 1/x_i$, we obtain $\alpha_1 = -1$ and
$\alpha_2 = 3$, and
\[
\det(1 - x\,T_{\mathrm{br}}) = (1 - \alpha_1 x)(1 - \alpha_2 x)
= (1+x)(1-3x) = \Delta(x).
\]
Since $P_{\mathrm{Vir}}$ and $P_{\beta\gamma}$ involve the same
radical $\sqrt{1-2x-3x^2}$, they share the same branch locus,
hence the same~$\Delta$.
\end{proof}

\begin{theorem}[Spectral discriminant --- general case;
\ClaimStatusProvedHere]\label{thm:discriminant-spectral}
\index{discriminant!transfer matrix}
\index{discriminant!spectral determinant!general case}
Let $\cA$ be a Koszul chiral algebra whose Koszul dual Hilbert
series $P_{\cA^!}(x) = \sum_{n \geq 1} \dim(\cA^!)_n\, x^n$
is either:
\begin{enumerate}[label=\textup{(\alph*)}]
\item \emph{Rational}: $P_{\cA^!}(x) = N(x)/D(x)$ with
$\gcd(N,D) = 1$, $D(0) = 1$, and $\deg D = r$; or
\item \emph{Algebraic of degree $d \geq 2$}: satisfying an
irreducible $f(x,y) \in \mathbb{Q}[x,y]$, $\deg_y f = d$,
with discriminant $\Delta(x) = \operatorname{Disc}_y(f)$
normalized so that $\Delta(0) = 1$.
\end{enumerate}
Define the \emph{transfer matrix} $T_{\mathrm{rec}}$ as follows.

\emph{Rational case.}
The coefficients $a_n = \dim(\cA^!)_n$ satisfy a
constant-coefficient linear recurrence of order~$r$
(the degree of the denominator).  Let $T_{\mathrm{rec}}$
be its companion matrix.  Then $\Delta_{\cA}(x) := D(x)$ and
\begin{equation}\label{eq:discriminant-spectral-rational}
\Delta_{\cA}(x) \;=\; \det\!\left(1 - x\, T_{\mathrm{rec}}\right).
\end{equation}

\emph{Algebraic case.}
The coefficients $a_n$ satisfy a $P$-recursive recurrence
of order~$d$:
\begin{equation}\label{eq:p-recursive-recurrence}
q_d(n)\, a_n + q_{d-1}(n)\, a_{n-1} + \cdots + q_0(n)\, a_{n-d} = 0
\end{equation}
where $q_i(n) \in \mathbb{Q}[n]$.  Let $c_i = \lim_{n \to \infty}
q_i(n)/n^{\deg q_d}$ be the leading asymptotic coefficient of~$q_i$
\textup{(}so $c_d = 1$ after normalization\textup{)}.
The asymptotic companion matrix is
\[
T_{\mathrm{rec}} =
\begin{pmatrix}
-c_{d-1} & -c_{d-2} & \cdots & -c_0 \\
1 & 0 & \cdots & 0 \\
\vdots & & \ddots & \vdots \\
0 & \cdots & 1 & 0
\end{pmatrix}
\in \mathrm{Mat}_{d \times d}(\mathbb{Q}).
\]
Then the bar-cohomology discriminant satisfies
\begin{equation}\label{eq:discriminant-spectral-general}
\Delta(x) \;=\; \det\!\left(1 - x\, T_{\mathrm{rec}}\right).
\end{equation}
In both cases, the eigenvalues of $T_{\mathrm{rec}}$ are the
reciprocals of the singularities \textup{(}poles or branch
points\textup{)} of~$P_{\cA^!}$, and the spectral
radius of $T_{\mathrm{rec}}$ equals $\dim\mathfrak{g}$ for
Kac--Moody algebras
\textup{(}Theorem~\textup{\ref{thm:dominant-branch-point})}.
\end{theorem}

\begin{proof}
\emph{Rational case.}
Write $D(x) = \prod_{i=1}^{r}(1 - \alpha_i x)$ with
$\alpha_i = 1/x_i$ the reciprocal poles.
The partial-fraction decomposition of
$P = N/D$ shows that $a_n = [x^n]P$ satisfies a
constant-coefficient linear recurrence of order~$r$
with characteristic polynomial
$\chi(t) = \prod(t - \alpha_i)$.
The companion matrix $T_{\mathrm{rec}}$ has $\chi$ as
its characteristic polynomial, so
$\det(1 - x\,T_{\mathrm{rec}}) = \prod(1 - \alpha_i x)
= D(x) = \Delta_{\cA}(x)$.

\emph{Algebraic case.}
The argument combines three classical results from analytic
combinatorics.

\emph{Step~1: Algebraic $\Rightarrow$ $P$-recursive.}
An algebraic power series is $D$-finite
(\cite{FlajoletSedgewick}, Proposition~VI.1), hence its
coefficients satisfy a $P$-recursive (holonomic) recurrence
of order~$d$, giving~\eqref{eq:p-recursive-recurrence}.

\emph{Step~2: Singularities of the generating function.}
Since $P(x)$ is algebraic of degree~$d$ with minimal polynomial
$f(x,y) = 0$, its singularities as an analytic function are
the branch points: the roots $x_1, \ldots, x_m$ of
$\Delta(x) = \operatorname{Disc}_y(f)$
(\cite{FlajoletSedgewick}, Theorem~VII.8).

\emph{Step~3: Poincar\'e--Perron theorem.}
For the $P$-recursive recurrence~\eqref{eq:p-recursive-recurrence}
with asymptotic companion matrix~$T_{\mathrm{rec}}$, the
Poincar\'e--Perron theorem
(\cite{FlajoletSedgewick}, Theorem~VIII.4)
identifies the eigenvalues of $T_{\mathrm{rec}}$ with the
asymptotic growth rates of the solutions: provided
$c_0 \neq 0$ and the eigenvalues $\alpha_1, \ldots, \alpha_d$
have distinct absolute values, the fundamental system of
solutions satisfies
$a_n^{(i)}/a_{n-1}^{(i)} \to \alpha_i$ as $n \to \infty$.

\emph{Step~4: Growth rates = reciprocals of branch points.}
By the transfer theorem for algebraic singularities
(\cite{FlajoletSedgewick}, Theorem~VI.3), the contribution
of a simple branch point~$x_j$ to the coefficients is
$[x^n]\, P(x) \sim C_j \cdot x_j^{-n} \cdot n^{-3/2}$.
The growth rates are therefore $\alpha_i = 1/x_i$, the
reciprocals of the branch points.

\emph{Synthesis.}
Combining Steps~3 and~4, the eigenvalues of $T_{\mathrm{rec}}$
are $\alpha_i = 1/x_i$.  Since $x_1, \ldots, x_m$ are the
roots of~$\Delta$ (Step~2) and $\Delta(0) = 1$:
\[
\det(1 - x\, T_{\mathrm{rec}})
= \prod_{i=1}^d (1 - \alpha_i x)
= \prod_{i=1}^d \Bigl(1 - \frac{x}{x_i}\Bigr)
= \Delta(x). \qedhere
\]
\end{proof}

\begin{remark}[Scope and verification]%
\label{rem:discriminant-spectral-scope}
\index{discriminant!Poincar\'e--Perron}
The rational case is unconditional (given rationality of~$P$);
the algebraic case additionally requires the Poincar\'e--Perron
non-degeneracy condition ($c_0 \neq 0$, eigenvalues of distinct
absolute value).  Both conditions hold for all bar-cohomology
sequences computed in this work.  The residual conjecture is
that $P_{\cA^!}$ is algebraic or rational for all Koszul chiral
algebras associated to simple~$\mathfrak{g}$.

\emph{Algebraic verification $($degree~$2)$.}
For $\widehat{\mathfrak{sl}}_2$, Virasoro, and $\beta\gamma$
(Theorem~\ref{thm:discriminant-spectral-verified}), $P$ is
algebraic of degree~$2$.
The Riordan recurrence
$(n{+}1) a_n = (2n{-}2) a_{n-1} + (3n{-}6) a_{n-2}$
gives leading coefficients $c_2 = 1$, $c_1 = -2$, $c_0 = -3$,
and $T_{\mathrm{rec}} = \bigl(\begin{smallmatrix} 2 & 3 \\ 1 &
0 \end{smallmatrix}\bigr)$ with eigenvalues $3$ and $-1$.
Check: $\det(1 - x\,T_{\mathrm{rec}}) = 1 - 2x - 3x^2
= (1-3x)(1+x) = \Delta(x)$.  The eigenvalue
$3 = \dim(\mathfrak{sl}_2)$ is the exponential growth rate
(Theorem~\ref{thm:dominant-branch-point});
$-1$ reflects the alternating correction from the central
extension.

\emph{Rational prediction for $\widehat{\mathfrak{sl}}_3$.}
Conjecture~\ref{conj:sl3-bar-gf} predicts that
$P_{\widehat{\mathfrak{sl}}_3}$ is \emph{rational} with
denominator $(1-8x)(1-3x-x^2)$, so the theorem applies
in its rational form with $r = 3$ poles.
The companion matrix of the recurrence~\eqref{eq:sl3-bar-recurrence}
has characteristic polynomial $(t-8)(t^2-3t-1)$, giving
eigenvalues $8$, $(3+\sqrt{13})/2$, $(3-\sqrt{13})/2$.
Check: $\det(1 - x\,T_{\mathrm{rec}})
= (1-8x)(1-3x-x^2) = \Delta_{\mathfrak{sl}_3}(x)$.
The spectral radius is $8 = \dim(\mathfrak{sl}_3)$,
consistent with Theorem~\ref{thm:dominant-branch-point}.
The remaining eigenvalues
$(3 \pm \sqrt{13})/2$ are the roots of the DS discriminant
factor $t^2 - 3t - 1$, shared with the $\mathcal{W}_3$ generating
function (Remark~\ref{rem:two-discriminant-families}).

\emph{Why not the Kodaira--Spencer operator on $H^1$?}
A natural first guess is
$\Delta(x) = \det(1 - x\, T_{\mathrm{KS}} \mid H^1)$
where $T_{\mathrm{KS}}$ acts on
$H^1(\bar{B}(\cA)) \cong \mathfrak{sl}_2$ ($3$-dimensional).
But Schur's lemma forces $T_{\mathrm{KS}}$ to be scalar on the
irreducible adjoint, giving $\det = (1-cx)^3$, degree~$3 \neq 2$.
The correct object is $T_{\mathrm{rec}}$ (dimension~$d$, the
degree of algebraicity), not~$H^1$ (dimension~$\dim\mathfrak{g}$).
Identifying a natural \emph{subquotient} $H^{\mathrm{red}}_1
\subset H^1$ on which a geometric operator recovers
$T_{\mathrm{rec}}$ remains open
(Conjecture~\ref{conj:discriminant-ks-operator}).

The discriminant's role as first non-scalar characteristic class
of the modular characteristic hierarchy parallels $\kappa(\cA)$ as the first
scalar characteristic number of the universal Maurer--Cartan
class $\Theta_{\cA}$ (Theorem~\ref{thm:universal-MC}).
\end{remark}

\begin{definition}[Spectral branch object]
\label{def:spectral-branch-object}
\index{spectral branch object|textbf}
\index{discriminant!branch transport operator}
A \emph{spectral branch object} for a modular Koszul chiral
algebra~$\cA$ is a finite-rank perfect complex~$V_{\cA}^{\mathrm{br}}$
together with a \emph{branch transport operator}
$T_{\mathrm{br},\cA} \colon V_{\cA}^{\mathrm{br}} \to
V_{\cA}^{\mathrm{br}}$ such that
\begin{equation}\label{eq:discriminant-det}
\Delta_{\cA}(x)
\;=\;
\det\!\bigl(1 - x\, T_{\mathrm{br},\cA}\bigr).
\end{equation}
The pair $(V_{\cA}^{\mathrm{br}}, T_{\mathrm{br},\cA})$ satisfies:
\begin{enumerate}[label=\textup{(\roman*)}]
\item $\Delta_{\cA^!} = \Delta_{\cA}$
  (the spectral discriminant is self-dual);
\item exact factorization functors preserving quadratic OPE data
  preserve $T_{\mathrm{br},\cA}$;
\item $\operatorname{rank}(V_{\cA}^{\mathrm{br}})$
  equals the degree of algebraicity of the Koszul-dual
  Hilbert series.
\end{enumerate}

\smallskip\noindent
\emph{Status.}
In the verified cases (Theorem~\ref{thm:discriminant-spectral-verified}),
$V_{\cA}^{\mathrm{br}}$ is constructed as the span of the
companion-matrix eigenvectors of $T_{\mathrm{rec}}$,
and~\eqref{eq:discriminant-det} is proved.  The general
construction of $V_{\cA}^{\mathrm{br}}$ from the bar complex
(rather than from the recurrence) remains open; see
Conjecture~\ref{conj:discriminant-ks-operator} for the
conjectural identification with a subquotient of~$H^1$.
(Part of the MC2 theorem package; Theorem~\ref{thm:mc2-full-resolution}.)
\end{definition}

\subsection{The index theorem for genus expansions}

The appearance of the $\hat{A}$-genus in the Heisenberg genus
expansion (Theorem~\ref{thm:universal-generating-function})
and the Bernoulli asymptotics of $F_g(\cA)$ are not coincidental:
they are the output of a Grothendieck--Riemann--Roch computation
on the universal curve.

\begin{theorem}[Family index theorem for genus expansions;
\ClaimStatusProvedHere]\label{thm:family-index}
\index{index theorem!modular deformation|textbf}
\index{Grothendieck--Riemann--Roch!genus expansion|textbf}
Let $\cA$ be a Koszul chiral algebra with obstruction coefficient
$\kappa(\cA)$, let $\pi \colon \overline{\mathcal{C}}_g \to
\overline{\mathcal{M}}_g$ be the universal curve with relative
dualizing sheaf~$\omega_\pi$, and let
$\mathbb{E} = R^0\pi_*\omega_\pi$ be the Hodge bundle.
Define the \emph{modular deformation complex} of~$\cA$ at genus~$g$ by
\begin{equation}\label{eq:modular-deformation-complex-concordance}
\mathcal{D}_{\cA}^{(g)}
\;:=\;
\kappa(\cA) \cdot \mathbb{E}
\;\in\; K_0(\overline{\mathcal{M}}_g) \otimes_{\mathbb{Z}} \mathbb{Q},
\end{equation}
the $K$-theory class given by $\kappa(\cA)$ copies of the
Hodge bundle.  Then:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{GRR formula.}
The genus-$g$ free energy is the degree-$g$ component of the
Grothendieck--Riemann--Roch pushforward:
\begin{equation}\label{eq:family-index-grr}
F_g(\cA)
\;=\;
\kappa(\cA) \cdot
\int_{\overline{\mathcal{M}}_{g,1}} \psi^{2g-2}\, c_g(\mathbb{E})
\;=\;
\kappa(\cA) \cdot
\left[
  \pi_*\!\left(
    \operatorname{ch}(\omega_\pi) \cdot
    \operatorname{Td}(T_\pi)
  \right)
\right]_g^{\mathrm{eval}},
\end{equation}
where $[\cdot]_g^{\mathrm{eval}}$ denotes evaluation of the degree-$g$ Chern character
$\operatorname{ch}_g(\mathbb{E})$ against $\psi^{2g-2}$ on
$\overline{\mathcal{M}}_{g,1}$.

\item \emph{$\hat{A}$-genus identification.}
The total generating function is
\begin{equation}\label{eq:family-index-generating}
\sum_{g \geq 1} F_g(\cA)\, x^{2g}
\;=\;
\kappa(\cA) \cdot \bigl(\hat{A}(ix) - 1\bigr)
\;=\;
\kappa(\cA) \cdot
\left(\frac{x/2}{\sin(x/2)} - 1\right),
\end{equation}
where the function $(x/2)/\sin(x/2)$ arises from
$\operatorname{Td}(T_\pi) = c_1(\omega_\pi)/(1 - e^{-c_1(\omega_\pi)})$
in the GRR formula, Wick-rotated by the positivity of
tautological intersection numbers on
$\overline{\mathcal{M}}_g$.

\item \emph{Verdier compatibility.}
Under Koszul duality $\cA \leftrightarrow \cA^!$,
$\mathcal{D}_{\cA^!}^{(g)} = \kappa(\cA^!) \cdot \mathbb{E}$,
and the complementarity
$\kappa(\cA) + \kappa(\cA^!) = \kappa_{\mathrm{total}}$
\textup{(}Theorem~\textup{\ref{thm:genus-universality}(ii))}
gives
\[
F_g(\cA) + F_g(\cA^!)
\;=\;
\kappa_{\mathrm{total}} \cdot \lambda_g^{\mathrm{FP}},
\]
the ``index-theoretic complementarity'': the total genus expansion
of a Koszul pair is a fixed multiple of the
Faber--Pandharipande tautological integral.
\end{enumerate}
\end{theorem}

\begin{proof}
The proof assembles four ingredients, each established
independently.

\emph{Step~1: Construction of $\mathcal{D}_{\cA}^{(g)}$.}
The genus universality theorem
(Theorem~\ref{thm:genus-universality}) establishes that the
genus-$g$ obstruction class factors as
$\mathrm{obs}_g(\cA) = \kappa(\cA) \cdot \lambda_g$ in
$H^{2g}(\overline{\mathcal{M}}_g)$, where
$\lambda_g = c_g(\mathbb{E})$ is the top Chern class of the
Hodge bundle.  This factorization determines
$\mathcal{D}_{\cA}^{(g)}$ uniquely in
$K_0(\overline{\mathcal{M}}_g) \otimes \mathbb{Q}$:
the obstruction class $\mathrm{obs}_g$ is the top Chern class of
$\mathcal{D}_{\cA}^{(g)}$, and the universality theorem
shows that $c_g(\mathcal{D}_{\cA}^{(g)}) =
\kappa(\cA) \cdot c_g(\mathbb{E})$, i.e.,
$\mathcal{D}_{\cA}^{(g)} = \kappa(\cA) \cdot \mathbb{E}$
as $K$-theory classes.

The construction is \emph{functorial}: $\kappa(\cA)$ is
determined by the genus-$1$ curvature of the bar complex
(a canonical invariant of~$\cA$), and $\mathbb{E}$ is determined
by the universal curve (independent of~$\cA$).  The product
$\kappa(\cA) \cdot \mathbb{E}$ is therefore a functor from
Koszul chiral algebras to $K$-theory classes on
$\overline{\mathcal{M}}_g$.

\emph{Step~2: Mumford's GRR computation.}
By Grothendieck--Riemann--Roch applied to the universal curve
$\pi \colon \overline{\mathcal{C}}_g \to \overline{\mathcal{M}}_g$
and the relative dualizing sheaf~$\omega_\pi$:
\begin{equation}\label{eq:mumford-grr}
\operatorname{ch}(\mathbb{E})
\;=\;
\operatorname{ch}(R^0\pi_*\omega_\pi)
\;=\;
1 + \pi_*\!\left(
  \operatorname{ch}(\omega_\pi) \cdot
  \operatorname{Td}(T_\pi)
\right),
\end{equation}

Serre duality on the fibers gives
$R^1\pi_*\omega_\pi \cong (R^0\pi_*\mathcal{O})^\vee \cong \mathcal{O}$,
so $\operatorname{ch}(R\pi_*\omega_\pi) = \operatorname{ch}(\mathbb{E}) - 1$.

Write $K = c_1(\omega_\pi)$ for the relative canonical class.  Since
$c_1(T_\pi) = -K$, the Todd class of the relative tangent bundle is
$\operatorname{Td}(T_\pi) = (-K)/(1 - e^K) = K/(e^K - 1)$, and the
pushforward becomes
\begin{equation}\label{eq:mumford-chern-character}
\operatorname{ch}_n(\mathbb{E})
\;=\;
\pi_*\!\left[
  \frac{K \cdot e^K}{e^K - 1}
\right]_{\text{deg } 2n+1}
\;=\;
\frac{B_{2n}}{(2n)!}\,\kappa_{2n-1}
\;+\; \delta_n
\qquad (n \geq 1),
\end{equation}
where $\kappa_{2n-1} = \pi_*(K^{2n})$ are the
Mumford--Morita--Miller classes \cite{Mumford83}
and $\delta_n$ is supported on the boundary
$\partial\overline{\mathcal{M}}_g$.
Indeed,
\[
\frac{K e^K}{e^K-1}
= \frac{K}{1 - e^{-K}}
= \operatorname{Td}(\omega_\pi)
= 1 + \frac{K}{2} + \sum_{n \geq 1} \frac{B_{2n}}{(2n)!} K^{2n},
\]
and $\pi_*(K^{2n}) = \kappa_{2n-1}$ by definition of the tautological
classes.

\emph{Step~3: Faber--Pandharipande evaluation.}
The Faber--Pandharipande formula
(Theorem~\ref{thm:mumford-formula}) evaluates the
$\psi$-$\lambda_g$ integral:
\[
\int_{\overline{\mathcal{M}}_{g,1}} \psi^{2g-2}\,\lambda_g
\;=\;
\frac{2^{2g-1}-1}{2^{2g-1}} \cdot
\frac{|B_{2g}|}{(2g)!}.
\]
This is the content of~\eqref{eq:family-index-grr}: the genus
universality theorem reduces $F_g$ to $\kappa$ times this
integral, and the integral evaluates to the Bernoulli expression
via the tautological intersection theory initiated by
Mumford~\cite{Mumford83} and completed by
Faber--Pandharipande~\cite{FP00}.

\emph{Step~4: Generating function from the Todd class.}
The Bernoulli numbers $B_{2g}$ are the Taylor coefficients of
the Todd generating function
$x/(1 - e^{-x}) = 1 + x/2 + \sum_{g \geq 1}
\frac{B_{2g}}{(2g)!}\, x^{2g}$.
In Step~2, the Todd class $\operatorname{Td}(\omega_\pi)$
produced these Bernoulli numbers as the Chern character
$\operatorname{ch}_g(\mathbb{E})$, i.e., the genus expansion
\emph{is} the GRR pushforward.
Concretely:
\begin{align}
\sum_{g \geq 1} \lambda_g^{\mathrm{FP}}\, x^{2g}
&\;=\;
\sum_{g \geq 1}
\frac{2^{2g-1}-1}{2^{2g-1}} \cdot
\frac{|B_{2g}|}{(2g)!}\, x^{2g}
\notag \\
&\;=\;
\frac{x/2}{\sin(x/2)} - 1
\;=\; \hat{A}(ix) - 1,
\label{eq:bernoulli-a-hat}
\end{align}
where the second equality is the classical identity
$t/\sin t = \sum_{n \geq 0}
\frac{(2^{2n}-2)\,|B_{2n}|}{(2n)!}\,t^{2n}$
evaluated at $t = x/2$ (equivalently, the even part of
the Wick-rotated Todd class).
Multiplying by $\kappa(\cA)$ gives~(ii).

The positivity of all coefficients
($(x/2)/\sin(x/2)$ has all positive Taylor coefficients,
unlike $\hat{A}(x)$ which alternates) reflects the positivity
of $\lambda_g^{\mathrm{FP}} > 0$ for all~$g$, which in turn
is a consequence of the Hodge index theorem on
$\overline{\mathcal{M}}_g$.

\emph{Step~5: Verdier compatibility.}
Part~(iii) follows immediately from
$\mathcal{D}_{\cA^!}^{(g)} = \kappa(\cA^!) \cdot \mathbb{E}$
and the linearity of $F_g$ in~$\kappa$.
\end{proof}

\begin{remark}[The punchline]\label{rem:family-index-punchline}
The content of the theorem is not the generating function identity
(which was already proved as
Theorem~\ref{thm:universal-generating-function}),
but its \emph{origin}: the genus expansion of a Koszul chiral
algebra is the Grothendieck--Riemann--Roch pushforward of the
Todd class of the universal curve, scaled by the bar-complex
curvature.  The $\hat{A}$-genus appears not by accident but because
$\hat{A}$ is the multiplicative genus associated to the formal
group law $F(x,y) = x + y$ --- the trivial (additive) formal
group --- and the bar-complex genus expansion is additive in
$\kappa$.  Equivalently, the Bernoulli numbers governing both the
Todd class and the $\psi$-$\lambda_g$ integrals are the same
Bernoulli numbers because both are computed by the same GRR
calculation on the universal curve.

In physical terms: the genus expansion of a chiral algebra is
an index.  The ``partition function'' $\sum F_g \hbar^{2g-2}$
is the family index of the modular deformation complex on
$\overline{\mathcal{M}}_g$, and anomaly cancellation
($\kappa_{\mathrm{tot}} = 0$) is the vanishing of this index.

The explicit universal class
$\Theta_{\cA}^{\min} = \kappa \cdot \eta \otimes \Lambda$
(Theorem~\ref{thm:explicit-theta}) gives this a precise
homotopy-algebraic meaning: $\hat{A}$ is the character of
$\Theta_{\cA}$ --- it computes how the universal MC element
distributes its weight across genera.  The Bernoulli numbers
appear because they are the Taylor coefficients of
$\operatorname{tr}(\Theta)$, not because of any formal group
coincidence.  The formal group is additive because $\kappa$
itself is additive under tensor product
(Corollary~\ref{cor:kappa-additivity}), which in turn follows
from $\Theta_{\cA \otimes \cB} = \Theta_{\cA} + \Theta_{\cB}$
in the factorization algebra sense.
\end{remark}

\begin{remark}[Scope]\label{rem:family-index-scope}
Steps~1--3 of the proof use only results established
in this monograph (genus universality,
Theorem~\ref{thm:genus-universality}) and imported results
(Mumford's GRR formula \cite{Mumford83}; Faber--Pandharipande
\cite{FP00}).  Step~4 is classical generating function analysis.
The identification $\mathcal{D}_{\cA}^{(g)} =
\kappa(\cA) \cdot \mathbb{E}$ is canonical given the genus
universality theorem; the functoriality of the construction
follows from the functoriality of $\kappa$ (determined by
genus-$1$ curvature) and $\mathbb{E}$ (determined by the
universal curve).

\emph{What remains.}
The theorem identifies $\mathcal{D}_{\cA}^{(g)}$ via its top Chern
class (the obstruction).  A stronger result would construct
$\mathcal{D}_{\cA}^{(g)}$ as a \emph{sheaf} on
$\overline{\mathcal{M}}_g$ (not just a $K$-theory class) and
identify its full Chern character with the genus expansion at all
intermediate degrees, not just the top.  This would require the
coderived Ran-space formalism of
\S\ref{sec:modular-koszul-programme} and is the subject of
Theorem~\ref{thm:universal-MC}.
\end{remark}

\begin{remark}[The GRR theorem as horizon]
\label{rem:grr-horizon}
\index{Grothendieck--Riemann--Roch!as horizon theorem}
In the Chriss--Ginzburg tradition, a foundational monograph
builds toward a single theorem that justifies the entire
development.  For this monograph, that theorem is
Theorem~\ref{thm:family-index}: the genus expansion of a
Koszul chiral algebra is a GRR pushforward.
The master diagram~\eqref{eq:master-square} is the engine;
the modular characteristic hierarchy is the protagonist:
the scalar genus tower and spectral invariants are proved,
while the full quintuple
$\mathcal{C}_{\cA} = (\Theta_{\cA}, \kappa(\cA), \Delta_{\cA},
\Pi_{\cA}, \mathcal{H}_{\cA})$ now has its H-level component
$\Theta_{\cA}$ \emph{proved}
\textup{(}Theorem~\textup{\ref{thm:mc2-full-resolution}},
resolving
Theorem~\textup{\ref{thm:universal-MC}}\textup{)};
and the family index theorem is the punchline.
What remains beyond this monograph ---
$C_{\mathrm{mod}}$ (shifted-symplectic complementarity),
derived Drinfeld--Kohno, the elliptic extension ---
are further consequences of the same master diagram in richer
geometric settings.
\end{remark}

\subsection{The spectral discriminant as characteristic class}
\index{discriminant!characteristic class}

The discriminant $\Delta_{\cA}(x) = \det(1 - x\,T)$ deserves
promotion from a shared generating-function curiosity to
\emph{the first genuinely nontrivial non-scalar characteristic class
of the modular Koszul object}.  Where the scalar invariant
$\kappa(\cA)$ measures the curvature of~$d^2$, the polynomial
$\Delta_{\cA}$ records the full branch geometry of the dual
Hilbert series (Theorem~\ref{thm:ds-bar-gf-discriminant}).

Distinct chiral theories can lie on the same modular spectral
sheet --- sharing the same discriminant --- even when their local
operator content differs entirely.  Drinfeld--Sokolov reduction
changes the growth pole (the leading OPE data) while preserving
the discriminant family; this is a functorial consequence of the
modular structure, not a numerical accident.
The $K$-theoretic framework that organizes the scalar
invariant~$\kappa$ (L1), the spectral discriminant~$\Delta$ (L2),
and the periodicity profile~$\Pi$ (L3) into a hierarchy of
characteristic classes of the virtual bar
family~$\mathcal{V}_{\cA} \in K_0(\overline{\mathcal{M}}_g)$
is developed in Remark~\ref{rem:structural-saturation}; the
intrinsic construction of the L2 operator from Casimir actions
on bar cohomology is Conjecture~\ref{conj:discriminant-ks-operator}.

\subsection{Chain-level / evaluation-locus derived Drinfeld--Kohno}

The Yangian theory (Chapter~\ref{chap:yangians}) establishes bar-cobar
recovery for the $\Eone$-chiral Yangian and the $q \mapsto q^{-1}$
transformation of the quantum parameter.  The chain-level derived
Drinfeld--Kohno theorem is now proved
(Theorems~\ref{thm:derived-dk-affine}
and~\ref{thm:derived-dk-yangian}): the $\Eone$-chiral Koszul duality
functor, composed with the Kazhdan--Lusztig (resp.\ Kazhdan)
equivalence, yields a derived equivalence with braiding reversal
$R_q \mapsto R_{q^{-1}}$ for both affine algebras and Yangians.
The factorization-level statement on the evaluation locus is also
proved (Theorem~\ref{thm:factorization-dk-eval}).

The full factorization-categorical statement remains conjectural, but
the remaining work is staged rather than monolithic:

\begin{conjecture}[Factorization-categorical DK/KL bridge (DK-3);
  \ClaimStatusConjectured]
\label{conj:derived-drinfeld-kohno}
\index{Drinfeld--Kohno!derived}
There exists an equivalence of $\Eone$-factorization categories
\[
\operatorname{Fact}_{\Eone}(\mathcal{Y}(\mathfrak{g}))
\;\simeq\;
\operatorname{Fact}_{\Eone}(U_q(\mathfrak{g}))^{\mathrm{op}}
\]
extending the chain-level equivalence of
Theorem~\textup{\ref{thm:derived-dk-yangian}}
from evaluation modules to all objects of Yangian
category~$\mathcal{O}$, and identifying the Yangian
$\Eone$-factorization structure with the factorisation quantum
group structure of Latyntsev~\cite{Latyntsev23}.
\end{conjecture}

\begin{remark}[Scope and ladder]\label{rem:derived-dk-scope}
The Yangian frontier is best read as a DK ladder:
\begin{enumerate}[label=\textup{(\alph*)}]
\item \textbf{DK-0 (proved):} braided monoidal bar complexes,
  Verdier $= R$-matrix inversion, and the chain-level DK square
  (Theorem~\ref{thm:monoidal-module-koszul};
  Proposition~\ref{prop:yangian-module-koszul}(iii);
  Theorem~\ref{thm:derived-dk-yangian}).
\item \textbf{DK-1 (proved):} the factorization-level statement on the
  evaluation locus (Theorem~\ref{thm:factorization-dk-eval}).
\item \textbf{DK-2/3 (proved, evaluation-generated core, all simple types, domain: $\mathcal{O}_{\mathrm{poly}}$):}
  The domain is the polynomial subcategory
  $\mathcal{O}_{\mathrm{poly}}$
  (Corollary~\ref{cor:dk-poly-catO}), not the full
  Category~$\mathcal{O}$.
  Two independent proofs.  Both rely on Yangian Koszulness
  (Proposition~\ref{prop:yangian-koszul}), which uses Molev's PBW
  theorem \cite{molev-yangians} as external input.
  \emph{Type~$A$ thick generation}:
  Theorem~\ref{thm:dk-fd-typeA} resolves DK-2/3 for
  $\operatorname{Rep}_{\mathrm{fd}}$ of $Y(\mathfrak{sl}_N)$
  via thick generation
  (Proposition~\ref{prop:dk2-thick-generation-typeA}).
  \emph{Sectorwise convergence (all types)}:
  Corollary~\ref{cor:dk23-all-types} resolves DK-2/3
  for the evaluation-generated core at all
  simple types, via the $\infty$-categorical sectorwise
  convergence theorem
  (Theorem~\ref{thm:h-level-factorization-kd}), bypassing
  thick generation.  The remaining gap to
  Conjecture~\ref{conj:derived-drinfeld-kohno} is the
  completed/coderived extension beyond the generated core
  (Corollary~\ref{cor:dk-partial-conj}).
\item \textbf{DK-4/DK-5 (conjectural):} the dg-shifted Yangian and
  full triple-bridge completion
  (Conjecture~\ref{conj:full-dk-bridge}).
\end{enumerate}
The key control point is that the evaluation-locus theorem is already
theorematic; the category-$\mathcal{O}$ extension and the dg-shifted
comparison are downstream stages, not hidden inside the same claim.
See \S\ref{sec:derived-dk} for the full treatment and
Remark~\ref{rem:dk-proved-vs-remains} for the precise decomposition.
The exact extra DK-2/DK-3 packets are recorded in
Remark~\ref{rem:yangian-dk23-inputs}.

\emph{Homotopy template} (Convention~\ref{conv:hms-levels}): Type~II
(equivalence of homotopy categories).  At H-level: an equivalence of
$\Eone$-monoidal $\infty$-categories.  The M-level model is the braided
monoidal bar-cobar dg category; the S-level shadow is the classical DK
equivalence on evaluation modules.
(Contributing to Conjecture~\ref{conj:master-dk-kl}.)

DK is naturally an $\Eone$-factorization theorem: it compares two
$\Eone$-level objects (KZ monodromy and $R$-matrix data).  The step
from $\Eone$ to full $\Etwo$ coherence requires Drinfeld associator
input and is governed by the Grothendieck--Teichm\"uller group
(Remarks~\ref{rem:e1-e2-obstruction}
and~\ref{rem:grothendieck-teichmuller}).  The modular layer (genus
deformation of $\Eone$-factorization) is the content of the modular
homotopy theory programme
(Definition~\ref{def:modular-homotopy-theory-intro}).
\end{remark}

\subsection{Summary}

The historical rhyme is this.  Classical operadic Koszul duality
says: quadratic presentation plus bar-cobar formalism produces
homotopy theory.  The theory sought here says: factorization
presentation on curves plus Verdier duality on modular
compactifications produces \emph{modular homotopy theory}.  Just as
Drinfeld--Kohno related KZ monodromy to quantum groups, the theory
sought here would relate configuration-space/Deligne--Mumford
monodromy to curved chiral Koszul duality.  And just as \'etale
cohomology supplied the correct ambient cohomology theory for
Frobenius and zeta functions, factorization homology on
$\operatorname{Ran}(X)$ together with Verdier duality on
$\overline{\mathcal{M}}_{g,n}$ is the correct ambient cohomology
theory for OPE, monodromy, and bar-cobar duality on curves.
The following comparisons distinguish possible parameter spaces,
categorical constructions, and their scalar consequences
(Definition~\ref{def:modular-homotopy-theory-working};
Remark~\ref{rem:nine-futures-summary}).

\subsection{Parameter spaces and categorical comparisons}\label{subsec:nine-futures}
\index{modular Koszul duality!categorical comparisons}

A proposed comparison may fail because its parameter space vanishes, or may
require a categorical construction beyond an identity of scalar invariants.
The examples below distinguish these possibilities.

\medskip

\noindent\textbf{Comparison~1: All-genus $H^1(\mathcal{M}_g)$-parameterized
bar-cobar theory.}
\index{parameter source!H1 reformulation}

\emph{Proposed statement.}
Genus-$g$ quantum corrections controlled by parameters
$t_g \in H^1(\mathcal{M}_g)$, with the curved differential
$d_g^2 = \sum_k t_{g,k}\,\mathrm{obs}_k$.

\emph{Parameter-space obstruction.}
The original parameterization is incorrect: $H^1(\mathcal{M}_g) = 0$
for $g \geq 2$ by Harer's theorem, so moduli-space cohomology cannot
supply the genus-$g$ correction parameters.  The correct source is
the \emph{fiber} cohomology $H^1(\Sigma_g, \mathbb{C})$, accessed
via the Leray spectral sequence of the universal curve:
\[
t_g \;\in\; H^0\!\bigl(\mathcal{M}_g,\, R^1\pi_*\mathcal{Z}(\cA)\bigr),
\]
i.e., global sections of the first direct image of the center local
system (Convention~\ref{conv:higher-genus-differentials};
Remark~\ref{rem:parameter-source}).  The reformulated theory is
\emph{proved}: the total corrected differential $\Dg{g}$ incorporates
$A$-period parameters from $H^1(\Sigma_g)$ and satisfies
$\Dg{g}^{\,2} = 0$ (Theorem~\ref{thm:quantum-diff-squares-zero}),
while the fiberwise differential $\dfib$ is curved with
$\dfib^{\,2} = \kappa(\cA)\cdot\omega_g$
(Theorem~\ref{thm:quantum-arnold-relations}).  The genus
universality theorem (Theorem~\ref{thm:genus-universality}) then
controls the obstruction tower by the single scalar~$\kappa(\cA)$.

\medskip

\noindent\textbf{Comparison~2: Modular Koszul duality.}

\emph{Proposed statement.}
A complete theory of Koszul duality for factorization algebras
over the modular operad of stable curves, with Verdier duality
controlling the bar-cobar exchange at all genera.

\emph{Scalar and categorical statements.}
The statements in Section~\ref{sec:modular-koszul-programme}
distinguish the scalar and categorical constructions.  Theorems~A--D and~H establish the
scalar core; $C_{\mathrm{mod}}$ (Lagrangian complementarity) and the
Index theorem (GRR for genus expansions) are fully proved; the
evaluation-locus derived Drinfeld--Kohno is proved; MC2 is
fully resolved, with the universal Maurer--Cartan class
$\Theta_{\cA}$ proved (Theorem~\ref{thm:universal-MC}).
The remaining gap is the coderived Ran-space formalism
(\S\ref{subsec:coderived-ran}), described in Comparison~3 below.

\medskip

\noindent\textbf{Comparison~3: Modular homotopy theory for factorization
algebras on curves.}

\emph{Proposed statement.}
Factorization presentation on curves plus Verdier duality on
modular compactifications produces a new homotopy theory, in which
genus is a deformation variable internal to Koszul duality itself.

\emph{Categorical structure.}
Definition~\ref{def:modular-homotopy-theory-intro} gives the
formal four-part specification; the three-level ladder from
tree-level to modular
(Definitions~\ref{def:tree-level-homotopy}--\ref{def:modular-homotopy-abstract})
makes the combinatorial step explicit; and the quantum versus cyclic
$L_\infty$ distinction
(Remark~\ref{rem:quantum-vs-cyclic-linf}) identifies the obstruction
to lifting.

\begin{definition}[Modular homotopy theory --- working definition]
\label{def:modular-homotopy-theory-working}
\index{modular homotopy theory!working definition}
A \emph{modular homotopy theory} on a smooth curve~$X$ is the
global completion of the nilpotence-periodicity correspondence
(Remark~\ref{rem:nilpotence-periodicity}) from genus~$0$ to all
genera.  Concretely, it consists of:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{The categorical $\exp/\log$ adjunction.}  An
  $\infty$-categorical bar-cobar adjunction
  $\barB_X \dashv \Omega_X$ on the coderived category
  $D^{\mathrm{co}}(\operatorname{Fact}(X))$ of curved factorization
  algebras on $\operatorname{Ran}(X)$: the categorical logarithm and
  its inverse, analytically continued to the multi-valued regime via
  the coderived framework.
\item \emph{Functoriality of the logarithm over moduli.}
  Functoriality of the adjunction over the modular operad
  $\{\overline{\mathcal{M}}_{g,n}\}_{g,n}$, intertwined with Verdier
  duality:
  $\mathbb{D}_{\operatorname{Ran}}\,\barB_X(\cA)
  \simeq \barB_X(\cA^!)$.  The monodromy of the logarithm varies
  coherently over moduli.
\item \emph{The complete logarithm.}  A universal Maurer--Cartan class
  $\Theta_{\cA} \in
  \operatorname{MC}(\operatorname{Def}_{\mathrm{cyc}}(\cA)
  \,\widehat{\otimes}\,
  R\Gamma(\overline{\mathcal{M}}_{g,\bullet}, \mathbb{Q}))$
  controlling the genus tower as a single deformation: the full
  logarithmic datum from which the characteristic hierarchy
  (Remark~\ref{rem:characteristic-hierarchy}) and
  its exponential---the modular automorphy---are derived.
\item \emph{The Lagrangian branch structure.}  A shifted-symplectic
  pairing on
  $R\Gamma(\overline{\mathcal{M}}_g, \mathcal{Z}_{\cA})$
  for which the deformation and obstruction complexes
  $Q_g(\cA)$, $Q_g(\cA^!)$ are complementary Lagrangians: the
  branch structure of the categorical logarithm, with each
  branch (algebra vs.\ Koszul dual) contributing a Lagrangian
  half of the total monodromy.
\end{enumerate}
\end{definition}

\noindent
Of these four requirements: (ii) and (iv) are proved
(Theorems~A, C; Proposition~\ref{prop:ptvv-lagrangian}),
where (ii) is established as functorial compatibility over
$\overline{\mathcal{M}}_{g,n}$ rather than as an equivalence of
modular operads;
(i) is proved at genus~$0$ and on the Koszul locus at all genera
(Theorems~A, B); (iii) is resolved by
Theorem~\ref{thm:mc2-full-resolution}
(see also Theorem~\ref{thm:universal-MC}).

\begin{theorem}[Volume~I concrete modular datum;
\ClaimStatusProvedHere]
\label{thm:volume-one-concrete-modular-datum}
Let $\cA$ be a modular Koszul chiral algebra on a smooth projective
curve~$X$.  The present theorem surface supplies a concrete modular
datum, separated by semantic level:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{H-level.}
  Definition~\ref{def:modular-koszul-homotopy} provides the
  homotopy bar object $\mathbf{B}_X(\cA)$, the dual object~$\cA^!$,
  and the center local system~$\mathcal{Z}_{\cA}$, with Verdier
  intertwining and Lagrangian complementarity supplied by
  Theorems~\ref{thm:bar-cobar-isomorphism-main}
  and~\ref{thm:quantum-complementarity-main}.  If, in addition,
  $\cA$ has simple Lie symmetry and non-degenerate invariant form,
  then the cyclic deformation object $\Defcyc(\cA)$ and the
  universal Maurer--Cartan class~$\Theta_{\cA}$ exist by
  Theorem~\ref{thm:mc2-full-resolution}.
\item \emph{M-level.}
  Definition~\ref{def:modular-koszul-chiral} provides the dg bar
  model~$\barB_X(\cA)$ together with the genus-$0$ differential
  $\dzero$, the fiberwise curved differential~$\dfib$, and the total
  corrected differential~$\Dg{g}$, satisfying
  $\dzero^{\,2} = 0$,
  $\dfib^{\,2} = \kappa(\cA)\,\omega_g$,
  and $\Dg{g}^{\,2} = 0$
  by Theorems~\ref{thm:bar-nilpotency-complete},
  \ref{thm:genus-universality},
  and~\ref{thm:quantum-diff-squares-zero}.
\item \emph{S-level.}
  Definition~\ref{def:full-modular-package} packages the scalar,
  spectral, periodic, and genus-family shadows as
  $\mathcal{C}_{\cA}
  = (\Theta_{\cA}, \kappa(\cA), \Delta_{\cA}, \Pi_{\cA},
  \mathcal{H}_{\cA})$.
  Here $\kappa(\cA)$ is proved by
  Theorem~\ref{thm:modular-characteristic},
  $\Delta_{\cA}$ by
  Theorem~\ref{thm:discriminant-spectral},
  $\Pi_{\cA}$ has a proved structural framework by
  Remark~\ref{rem:periodicity-triple},
  and $\mathcal{H}_{\cA}$ is the genus-graded deformation family
  induced by~$\Theta_{\cA}$ when the latter is available.
\end{enumerate}
\end{theorem}

\begin{proof}
The H-level datum is exactly
Definition~\ref{def:modular-koszul-homotopy}, together with
Theorems~\ref{thm:bar-cobar-isomorphism-main}
and~\ref{thm:quantum-complementarity-main}; the universal
Maurer--Cartan enhancement on the simple-Lie-symmetry locus is
Theorem~\ref{thm:mc2-full-resolution}.  The M-level dg model is
Definition~\ref{def:modular-koszul-chiral}, and its three
differential identities are the cited theorems on genus-$0$
nilpotency, genus-$g$ curvature, and corrected nilpotence.  The
S-level hierarchy is
Definition~\ref{def:full-modular-package}, with the scalar,
spectral, and periodic statements supplied by the cited theorem and
remark references.
\end{proof}

\begin{remark}[What remains frontier]
\label{rem:volume-one-concrete-modular-datum-frontier}
The theorem above is the precise sense in which modular homotopy
theory is already concrete mathematics in Volume~I.  The remaining
frontier is the outer continuation of that datum: coderived
bar-cobar persistence beyond the proved loci, MC3 beyond the
evaluation-generated core, the H-level MC4 coefficient identities for
$\mathcal{W}_\infty$ and Yangian towers, and downstream MC5.  These
are extensions of the concrete package above, not missing ingredients
of its proved core.
\end{remark}

\medskip

\noindent\textbf{Comparison~4: Universal modular characteristic hierarchy.}

\emph{Proposed statement.}
Not only $\kappa(\cA)$ but a characteristic hierarchy reaching from
scalar genus data through spectral and periodic invariants to a full
universal Maurer--Cartan class.

\emph{Existence and computation.}
The full package is now formalized as the quintuple
$\mathcal{C}_{\cA} = (\Theta_{\cA}, \kappa(\cA), \Delta_{\cA},
\Pi_{\cA}, \mathcal{H}_{\cA})$
(Definition~\ref{def:full-modular-package}), and \textbf{every
component exists unconditionally}:

\begin{itemize}[nosep]
\item $\kappa(\cA)$: proved (Theorem~\ref{thm:modular-characteristic});
\item $\Delta_{\cA}(x) = \det(1 - x\,T_{\mathrm{rec}})$: proved
  (Theorem~\ref{thm:discriminant-spectral};
  Definition~\ref{def:spectral-branch-object});
\item $\Pi_{\cA}$: structural framework proved
  (Remark~\ref{rem:periodicity-triple};
  Definition~\ref{def:derived-scalar-period}), though
  specific sharp modular and geometric periods remain conjectural
  (Comparison~7).  Under the nilpotence-periodicity correspondence
  (Remark~\ref{rem:nilpotence-periodicity}), $\Pi_{\cA}$ is the
  \emph{exponential} of the logarithmic data
  $(\kappa(\cA), \Delta_{\cA}, \Theta_{\cA})$: the periodicity
  profile is derived from the nilpotent characteristic via
  exponentiation, not an independent invariant;
\item $\Theta_{\cA}$: \textbf{proved}
  (Theorem~\ref{thm:mc2-full-resolution}), resolving
  Theorem~\ref{thm:universal-MC} via three unconditional inputs:
  cyclic $L_\infty$ structure
  (Theorem~\ref{thm:cyclic-linf-graph}),
  completed tensor convergence
  (Proposition~\ref{prop:geometric-modular-operadic-mc}),
  and tautological line support
  (Theorem~\ref{thm:tautological-line-support});
\item $\mathcal{H}_{\cA}$: the genus-graded deformation family
  exists as a formal consequence of $\Theta_{\cA}$.
\end{itemize}

\emph{What was proved.}
The resolution of MC2 (Theorem~\ref{thm:mc2-full-resolution})
established all five structural properties of $\Theta_{\cA}$:
scalar trace $\operatorname{tr}(\Theta_{\cA}) = \kappa \cdot
\lambda_g$, clutching compatibility with the modular operad,
Verdier duality $\Theta_{\cA} \mapsto \Theta_{\cA^!}$
(the extensive one-channel Verdier reduction chain, through
Propositions~\ref{prop:one-channel-verdier-criterion}
through~\ref{prop:one-channel-parity-forcing-criterion},
is now subsumed by the unconditional MC2 resolution),
the cyclic $L_\infty$ structure on $\Defcyc(\cA)$, and the MC
equation in the completed tensor product
$\Defcyc(\cA) \,\widehat{\otimes}\, R\Gamma(\overline{\mathcal{M}})$.

\emph{What is resolved (beyond existence).}
Two further structural questions are now settled:

\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Explicit form of $\Theta_{\cA}$ for all KM algebras.}
  Theorem~\ref{thm:explicit-theta}(a) gives
  $\Theta_{\cA}^{\min} = \kappa(\cA) \cdot \eta \otimes \Lambda$
  in the minimal cyclic $L_\infty$ model, for \emph{every} Koszul
  chiral algebra with simple Lie symmetry --- not only Heisenberg.
  This holds for $\widehat{\mathfrak{sl}}_{2,k}$,
  $\widehat{\mathfrak{sl}}_{3,k}$, and all KM algebras at generic
  level.  At the chain level, $\theta_g = \kappa \cdot \mu \otimes
  \lambda_g + \text{exact corrections}$, with the corrections
  determined by homotopy transfer
  (Theorem~\ref{thm:explicit-theta}(c,e)).
\item \emph{Scalar saturation}
  (Corollary~\ref{cor:scalar-saturation}).
  Whenever $\dim H^2_{\mathrm{cyc}}(\cA,\cA) = 1$,
  $\Theta_{\cA}$ carries no more information than~$\kappa(\cA)$:
  graded antisymmetry of $L_\infty$-operations forces the MC
  equation to hold trivially on the one-dimensional space
  $\mathcal{H}^2 = \mathbb{C}\!\cdot\!\eta$.
  This condition is verified in three theorematic regimes: for Kac--Moody
  (CE~identification, Theorem~\ref{thm:mc2-1-km}(ii),
  $H^2_{\mathrm{cyc}}(\mathfrak{g},\mathfrak{g}) \cong
  H^3(\mathfrak{g}) \cong \mathbb{C}$);
  at all non-admissible, non-critical levels
  (Theorem~\ref{thm:cyclic-rigidity-generic});
  and on the standard landscape
  (Corollary~\ref{cor:effective-quadruple}).
  Non-scalar data resides in~$\Delta_{\cA}$
  and~$\Pi_{\cA}$, not in~$\Theta_{\cA}$
  (Remark~\ref{rem:scalar-saturation-casimirs}); the three
  levels of the $K$-theoretic hierarchy
  (Remark~\ref{rem:structural-saturation}) provide the
  geometric framework.
\end{enumerate}

\emph{What remains open.}
\begin{enumerate}[label=\textup{(\roman*)},start=3]
\item \emph{Periodicity profile and geometric depth.}
  The periodicity profile $\Pi_{\cA} = (M_{\cA}, Q_{\cA},
  G_{\cA})$ has its $T$-matrix component and quantum periodicity
  proved.  The geometric contribution is now correctly understood
  as providing \emph{nilpotent tautological depth} rather than a
  periodic source: $\kappa(\lambda)^{g-1} = 0$ on smooth moduli
  (Theorem~\ref{thm:geometric-depth-smooth}, using Graber--Vakil
  vanishing~\cite{GraVak05}).  At genus~$2$ the Hodge class
  contribution vanishes entirely.  The sharp modular period remains
  conjectural (Comparison~7).  The boundary-extended depth on
  $\overline{\mathcal{M}}_g$ is bounded by $3g - 2$
  (Theorem~\ref{thm:geometric-periodicity-weak}) with the
  gap controlled by the Mumford relation $12\lambda = \kappa_1 + \delta$
  (Conjecture~\ref{conj:geometric-periodicity}).

\item \emph{Functoriality beyond Koszul duality.}
  The duality $\cA \leftrightarrow \cA^!$ acts on the package
  ($\kappa + \kappa' = 0$, $\Delta_{\cA^!}$ determined by
  $\Delta_{\cA}$).  For DS reduction
  $\widehat{\mathfrak{g}}_k \to \mathcal{W}_N^k$, the
  functoriality is partially verified:
  (a)~$\kappa$ transforms by a known rational function of the level
  (Theorem~\ref{thm:wn-obstruction});
  (b)~$\Delta_{\cA}$ is DS-invariant
  (Theorem~\ref{thm:ds-bar-gf-discriminant});
  (c)~$\Theta$ is determined by~$\kappa$ in both algebras (scalar
  saturation), so its transformation follows from~(a).

  Functoriality under embeddings and lattice extensions remains
  conjectural: the general formalism predicts it, but the printed
  verification is not yet in hand.

\item \emph{Scalar saturation: fully resolved for the standard
  landscape.}
  Scalar saturation is proved for all KM, all $\mathcal{W}$-algebras,
  all ACL-type diagonal cosets, and tensor products thereof
  (Corollary~\ref{cor:effective-quadruple}).
  The saturation equivalence criterion
  (Proposition~\ref{prop:saturation-equivalence}) and the
  functorial stability theorem
  (Proposition~\ref{prop:saturation-functorial}) provide the
  structural underpinning.
  Cyclic rigidity at generic level
  (Theorem~\ref{thm:cyclic-rigidity-generic}) proves the
  quadruple reduction for \emph{all} vertex algebras with
  simple symmetry at non-admissible, non-critical level, via
  the Whitehead decomposition and Kazhdan--Lusztig
  semisimplicity.
  The quintuple reduces to an effective quadruple
  $(\kappa, \Delta, \Pi, \mathcal{H})$ on this landscape.
  Conjecture~\ref{conj:scalar-saturation-universality}
  extends the reduction to admissible levels; the sharpest
  potential counterexample is a
  multi-node 4D $\mathcal{N}=2$ quiver VOA
  (Remark~\ref{rem:scalar-saturation-evidence}).
\end{enumerate}

\emph{Strictification and gauge rigidity on the Kac--Moody locus.}
Theorem~\ref{thm:km-strictification} establishes that for
$\widehat{\fg}_k$ with $\fg$ simple and $k \neq -h^\vee$,
the element $\Theta^{\mathrm{str}} = \kappa \cdot \mu \otimes
\Lambda$ is a \emph{strict} chain-level MC element
($l_1 = 0$ by CE cocycle, $l_2 = 0$ by Jacobi, $l_n = 0$ for
$n \geq 3$ on the dg~Lie side).  No corrections at any genus.
Proposition~\ref{prop:one-channel-gauge-rigidity} then gives:
\emph{fixed $\kappa$ implies a single one-channel gauge orbit}.
The one-channel $\Theta$-package is gauge-rigid on the
Kac--Moody locus.

\emph{Channel-refined characteristic for $\mathcal{W}_N$.}
For Kac--Moody algebras, the double-pole/simple-pole channel
decomposition $\kappa = \kappa_{\mathrm{dp}} + \kappa_{\mathrm{sp}}$
(Definition~\ref{def:channel-decomposition}) transforms under
Feigin--Frenkel duality by the shear matrix
$\bigl(\begin{smallmatrix} -1 & -2 \\ 0 & 1
\end{smallmatrix}\bigr)$
(Proposition~\ref{prop:ff-channel-shear}).

For $\mathcal{W}_N$, the scalar package
$\kappa(\mathcal{W}_N^k) = c \cdot \sum_{s=2}^N 1/s$
(Theorem~\ref{thm:wn-obstruction})
decomposes along strong generators:
the channel vector
$\tilde{\kappa}_N = (c_2, c_3, \ldots, c_N)$ with $c_s = c/s$
carries one entry per strong generator~$W^{(s)}$ (cross-terms
vanish by conformal weight parity).
The usual scalar $\kappa$ is the sum $\sum_s c_s$.

This reveals a structural obstruction for MC4:
$\sum_{s=2}^N 1/s \sim \log N$, so the scalar package
\emph{diverges} as $N \to \infty$.  The $\mathcal{W}_\infty$ target
cannot live on the scalar line; it must retain the full channel
vector before summation.  The correct MC4 target for
$\mathcal{W}_\infty$ is a separated complete channel target
$H_{\mathcal{W}_\infty}$ with vector-valued universal class
$\tilde{\Theta}_{\mathcal{W}_\infty} \sim
\sum_{s \geq 2} c_s\, e_s \otimes \Lambda$,
whose truncation to stage~$N$ recovers the principal
$\mathcal{W}_N$ package; the ordinary scalar $\Theta$ is obtained
only after a renormalized projection $\sum_s e_s \rightsquigarrow 1$.
Under Feigin--Frenkel duality $k \mapsto -k - 2h^\vee$, the
channel pair $(\Theta_{\mathrm{dp}}, \Theta_{\mathrm{sp}})$
(double-pole and simple-pole contributions) transforms by the
shear $\bigl(\begin{smallmatrix} -1 & -2 \\ 0 & 1
\end{smallmatrix}\bigr)$, and the total class
$\Theta = \Theta_{\mathrm{dp}} + \Theta_{\mathrm{sp}}$ is
the $(-1)$-eigenvector.
The analogous multichannel completion is needed on the Yangian side.

\medskip

\noindent\textbf{Comparison~5: Index-theoretic explanation of genus
expansions.}

\emph{Proposed statement.}
The $\hat{A}$-genus appearance in the genus expansion is a
family-index theorem (GRR on the universal curve), not a generating
function accident.

\emph{Index formula.}
Theorem~\ref{thm:family-index} proves this in full: the genus
expansion $\sum F_g\, x^{2g} = \kappa(\cA)\cdot(\hat{A}(ix) - 1)$
is the GRR pushforward of the Todd class of the universal curve,
scaled by the bar-complex curvature.  The $\hat{A}$-genus appears
because it is the multiplicative genus of the additive formal
group law, and the genus expansion is additive in~$\kappa$.
A stronger sheaf-level refinement (constructing
$\mathcal{D}_{\cA}^{(g)}$ as a sheaf, not just a $K$-theory
class) remains open and requires Comparison~3.

\medskip

\noindent\textbf{Comparison~6: Derived Drinfeld--Kohno and quantum
groups.}

\emph{Proposed statement.}
An equivalence of $\Eone$-factorization categories
$\operatorname{Fact}_{\Eone}(Y(\mathfrak{g})) \simeq
\operatorname{Fact}_{\Eone}(U_q(\mathfrak{g}))^{\mathrm{op}}$,
with braid monodromy from Verdier reversal.

\emph{Drinfeld--Kohno comparisons.}

The theorematic endpoint of the present book is DK-2/3 for the
evaluation-generated core at all types
(Corollary~\ref{cor:dk23-all-types}), and the remaining
frontier is ordered rather than undifferentiated:

\begin{enumerate}[label=\textup{(\roman*)}]
\item \textbf{DK-0 \textup{(}proved\textup{)}}:
  braided monoidal bar complexes, Verdier duality
  $= R$-matrix inversion, and the chain-level DK square for affine
  algebras and Yangians
  \textup{(}Theorem~\ref{thm:monoidal-module-koszul};
  Theorems~\ref{thm:derived-dk-affine},
  \ref{thm:derived-dk-yangian}\textup{)}.
\item \textbf{DK-1 \textup{(}proved\textup{)}}:
  the factorization-level comparison on the evaluation locus
  \textup{(}Theorem~\ref{thm:factorization-dk-eval}\textup{)}.
\item \textbf{DK-1$\frac{1}{2}$ \textup{(}proved, lattice sector\textup{)}}:
  the full factorization bar-cobar equivalence for lattice vertex
  algebras $V_\Lambda$, bypassing thick generation via sectorwise
  finiteness of the $\Lambda$-graded bar complex
  \textup{(}Theorem~\ref{thm:lattice:factorization-koszul}\textup{)}.
  At level~$1$ simply-laced, the FKS identification
  yields an unconditional factorization DK statement
  \textup{(}Corollary~\ref{cor:lattice:factorization-dk-level1}\textup{)}.
\item \textbf{DK-2/3 \textup{(}proved: fd type~$A$ by thick
  generation; generated core at all types by sectorwise
  convergence\textup{)}}:
  Theorem~\ref{thm:dk-fd-typeA} resolves DK-2/3 for
  finite-dimensional representations of $Y(\mathfrak{sl}_N)$
  via thick generation.
  Corollary~\ref{cor:dk23-all-types} resolves DK-2/3 for the
  evaluation-generated core at all simple types via the
  $\infty$-categorical sectorwise convergence theorem
  (Theorem~\ref{thm:h-level-factorization-kd}), bypassing thick
  generation entirely.  The remaining gap to the full
  Conjecture~\ref{conj:full-derived-dk} beyond the generated core
  is the completed/coderived extension to infinite-dimensional
  modules
  \textup{(}Remark~\ref{rem:corrected-mc3-frontier}(b)\textup{)}.
\item \textbf{DK-4 \textup{(}reduced to structure identification\textup{)}}:
  the analytic part of DK-4 is resolved:
  the Mittag-Leffler property of the RTT bar cohomology tower
  is proved (Theorem~\ref{thm:rtt-mittag-leffler}), and the
  completed bar-cobar equivalence for Yangians follows
  (Corollary~\ref{cor:completed-bar-cobar-yangian}).
  The remaining content is the algebraic identification of
  $\varprojlim_N \mathfrak{g}_N$ with the dg-shifted Yangian
  (Conjecture~\ref{conj:dk4-inverse-limit}): the Maurer--Cartan
  element, translation operator, and twisted coproduct.
  For the standard type-$A$ RTT tower,
  Proposition~\ref{prop:yangian-dk4-typea-frontier}
  verifies the key identity $L_a(u)=R_{0a}(u-a)$ at finite stages.
  The formal moduli reformulation
  (Conjecture~\ref{conj:dk4-formal-moduli}) places DK-4 as the
  tangent Lie algebra of $\mathfrak{F}_{\cA}$.
\item \textbf{DK-5 \textup{(}completed module-category enhancement;
  conjectural\textup{)}}:
  once DK-4 is in hand, the remaining step is exact
  braided-monoidal realization of the dg and spectral compact cores
  from the proved finite-dimensional factorization DK core, together
  with the compact-core equivalence on the completed module categories
  \textup{(}Conjecture~\ref{conj:full-dk-bridge};
  Corollary~\ref{cor:yangian-dk5-core-realization}\textup{)}.
  The reduction is conditional on exact braided monoidal realization of
  the compact cores from the completed evaluation packet compatible
  with DK-3;
  Proposition~\ref{prop:yangian-dk5-spectral-fundamental-packet}
  reduces the spectral side to the completed fundamental packet, and
  Proposition~\ref{prop:yangian-dk5-spectral-vector-packet}
  sharpens that to the ordered vector packet, while
  Proposition~\ref{prop:quantum-fd-vector-seed-shifts} constructs the
  standard source vector seed-and-shift datum and
  Proposition~\ref{prop:yangian-dk5-spectral-seed-shift-construction}
  makes it canonical on any realized spectral vector line,
  Proposition~\ref{prop:yangian-dk5-spectral-core-shifts}
  extends it to the full spectral compact core,
  Proposition~\ref{prop:yangian-dk5-spectral-factorization-shifts}
  makes it intrinsic on any multiplicative spectral realization of the
  compact core, and
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-vector-line}
  shows that realization of the standard vector line already forces
  the full spectral packet there,
  while
  Proposition~\ref{prop:yangian-dk5-spectral-factorization-core-from-vector-line}
  shows that on the ambient ordered factorization category the compact
  core itself is forced as the thick braided-monoidal closure of that
  line,
  while
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-vector-line}
  packages the whole spectral DK-5 side directly from that ambient
  vector-line realization,
  while
  Proposition~\ref{prop:yangian-dk5-spectral-factorization-seed-line}
  and
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-one-seed}
  and
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-schur-seed}
  sharpen this to one Schur-simple ambient vector seed together with
  the one-parameter trigonometric seed-pair identity on its dilation
  orbit, and under diagonal \(\mathfrak{sl}_M\)-equivariance and
  highest-weight normalization even that local packet contracts first
  to one ordered mixed-tensor line and then to one scalar exchange
  coefficient family,
  while Corollary~\ref{cor:yangian-dk5-spectral-one-seed}
  packages the parallel compact-core seed route there via one
  compatible base vector seed,
  while
  Corollary~\ref{cor:yangian-dk5-spectral-seed-realization}
  packages the converse closure statement from that standard datum to
  the full spectral side, while
  Corollary~\ref{cor:yangian-dk5-spectral-packet-ind} constructs the
  ambient spectral category as
  \(\operatorname{Ind}(\mathcal{C}_q^\omega)\), while
  Propositions~\ref{prop:yangian-dg-fundamental-packet-realization}
  and~\ref{prop:yangian-dk5-dg-extension-existence} construct the
  canonical dg completed generator packet and its exact extension to
  the dg compact core; Corollary~\ref{cor:yangian-dk5-dg-packet-ind}
  then constructs the dg ambient category as
  \(\operatorname{Ind}(\mathcal{C}_Y^\omega)\), so the genuinely live
  categorical remainder is exactly realization of that standard
  spectral vector seed-and-shift datum, which contracts on the
  multiplicative spectral realization locus to realization of the
  standard vector line itself by
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-vector-line},
  with the compact core itself then forced as the thick
  braided-monoidal closure of that ambient vector line by
  Proposition~\ref{prop:yangian-dk5-spectral-factorization-core-from-vector-line},
  and the whole spectral DK-5 side then packaged directly from that
  ambient vector-line realization by
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-vector-line},
  sharpened further to one Schur-simple ambient vector seed together
  with the one-parameter trigonometric seed-pair identity on its
  dilation orbit, and under diagonal \(\mathfrak{sl}_M\)-equivariance
  and highest-weight normalization even that local packet contracts
  first to one ordered mixed-tensor line and then to one scalar
  exchange coefficient family descending to one multiplicative-ratio
  scalar family, equivalently to one antisymmetric-channel character
  there
  by
  Proposition~\ref{prop:yangian-dk5-spectral-factorization-seed-line}
  and
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-one-seed}
  and
  Corollary~\ref{cor:yangian-dk5-spectral-factorization-ambient-schur-seed},
  with the parallel compact-core seed route via one compatible base
  vector seed in Corollary~\ref{cor:yangian-dk5-spectral-one-seed}, and
  on the equivariant multiplicative spectral realization locus of
  Corollary~\ref{cor:yangian-canonical-realization-to-spectral-seed}
  even that seed is canonical;
  the compact-core equivalence and completed comparison functor are
  then formal by ind-extension
  \textup{(}Remark~\ref{rem:yangian-dk5-input}\textup{)}.
  The $\infty$-categorical PBW reformulation
  (Conjecture~\ref{conj:dk5-infty-pbw}) reduces DK-5 to the
  identification of $\exp(\mathfrak{g}_{\cA})$ with the spectral
  quantum group.
\end{enumerate}

Thus Comparison~6 is no longer one frontier theorem.  DK-0/DK-1 are proved,
DK-1$\frac{1}{2}$ gives a full factorization bar-cobar equivalence for
lattice algebras (bypassing thick generation entirely),
and DK-2/DK-3 are proved for the evaluation-generated core at all
simple types (without thick generation; Koszulness uses Molev PBW
\cite{molev-yangians})
(Corollary~\ref{cor:dk23-all-types}; for fd type~$A$ also by
Theorem~\ref{thm:dk-fd-typeA}).

The extension beyond the generated core is controlled by the factorization Positselski equivalence
(Theorem~\ref{thm:factorization-positselski}) and the ind-completed
extension (Theorem~\ref{thm:ind-completed-extension}), which provide
the categorical ambient for the four MC3 extension conjectures.

DK-4/DK-5 are the downstream MC4-comparison and completed-module
packets, now reformulated as formal moduli theory and
$\infty$-categorical PBW
(\S\ref{sec:infty-cat-factorization-kd}).

The Mittag-Leffler property for the RTT bar cohomology tower
is proved unconditionally
(Theorem~\ref{thm:rtt-mittag-leffler}), yielding the completed
bar-cobar equivalence for Yangians
(Corollary~\ref{cor:completed-bar-cobar-yangian}).
DK-4 is thereby reduced to the algebraic identification of the
tangent Lie algebra with the dg-shifted Yangian
(Conjecture~\ref{conj:dk4-inverse-limit}).

\begin{remark}[The category~\texorpdfstring{$\mathcal{O}$}{O} generation obstruction:
precise structure]
\label{rem:cat-o-generation-obstruction}
\index{Category O@Category $\mathcal{O}$!generation problem}
\index{thick generation!Category O@Category $\mathcal{O}$}
The blocking issue beyond the proved DK-2/3 theorematic core is no
longer another evaluation-level comparison.  DK-2/3 is now resolved on
the evaluation-generated core at all simple types
\textup{(}Corollary~\ref{cor:dk23-all-types}\textup{)}.  What remains
before the full Conjecture~\ref{conj:full-derived-dk} is the
\emph{domain-enlargement problem}: in ordinary derived categories,
Corollary~\ref{cor:dk-partial-conj} asks whether
$D^b(\mathcal{O}_{Y_\hbar(\mathfrak{sl}_N)})$ is thickly generated by
evaluation modules; in completed/coderived form, the same enlargement
appears as the four conjectural extension packages of
Remark~\ref{rem:corrected-mc3-frontier}(b).  We record the precise
mathematical content, known partial results, and candidate strategies.

\emph{The problem and its natural domain.}
Let $\mathcal{C}_{\mathrm{eval}} \subset
D^b(\mathcal{O}_{Y_\hbar(\mathfrak{sl}_N)})$ denote the thick
triangulated subcategory generated by the evaluation modules
$V_\lambda(a)$ (the pullback of $V_\lambda \in
\operatorname{Rep}_{\mathrm{fd}}(\mathfrak{sl}_N)$ along
$\mathrm{ev}_a\colon Y_\hbar(\mathfrak{sl}_N) \to
U(\mathfrak{sl}_N)$).
By Lemma~\ref{lem:fd-thick-closure}, the thick closure of
finite-length objects in $D^b(\cA)$ equals $D^b(\cA^{\mathrm{fd}})$:
it captures exactly the complexes with finite-length total
cohomology.  Therefore, if $\mathcal{O}$ contains genuine
infinite-length objects, the literal question
$\mathcal{C}_{\mathrm{eval}} = D^b(\mathcal{O})$ has a
\emph{negative} answer.

The correct generation question is therefore:
\[
\mathcal{C}_{\mathrm{eval}} \stackrel{?}{=}
D^b(\mathcal{O}_{\mathrm{poly}})
\]
where $\mathcal{O}_{\mathrm{poly}}$ is the polynomial subcategory
(all simples finite-dimensional).  This holds for $N = 2$
unconditionally and for $\mathcal{O}_{\mathrm{poly}}$ at all~$N$.
For $\operatorname{Rep}_{\mathrm{fd}}$ in place of
$\mathcal{O}_{\mathrm{poly}}$,
the answer is \emph{yes}
(Proposition~\ref{prop:dk2-thick-generation-typeA}): every
finite-dimensional irreducible is a subquotient of a tensor product
of fundamental evaluation modules.

For modules beyond $\mathcal{O}_{\mathrm{poly}}$, the correct
framework is \emph{localizing} (not thick) generation in a
completed/Ind-enhanced category, or enlargement of the
generator set to include prefundamental or asymptotic modules.

\emph{What is known.}

\begin{enumerate}[label=\textup{(\alph*)}]
\item \emph{Finite-dimensional type~$A$}: resolved
  (Theorem~\ref{thm:dk-fd-typeA}).  Every finite-dimensional simple
  module lies in $\mathcal{C}_{\mathrm{eval}}$.
\item \emph{$N = 2$ (Yangian of $\mathfrak{sl}_2$)}: \textbf{resolved}
  (Theorem~\ref{thm:catO-thick-generation}).
  Category~$\mathcal{O}$ for $Y_\hbar(\mathfrak{sl}_2)$ is controlled
  by the Kirillov--Reshetikhin modules and their limits: every module
  has a finite composition series with finite-dimensional factors
  \cite{ChariPressley94}.  Since every finite-dimensional simple
  lies in $\mathcal{D}_\hbar^{\mathrm{eval}}$
  (Proposition~\ref{prop:dk2-thick-generation-typeA}),
  Lemma~\ref{lem:composition-thick-generation} yields
  $D^b(\mathcal{O}_{Y_\hbar(\mathfrak{sl}_2)})
  = \mathcal{D}_\hbar^{\mathrm{eval}}$.
  More generally, thick generation holds for the polynomial
  subcategory $\mathcal{O}_{\mathrm{poly}}$ at all~$N$
  (Remark~\ref{rem:catO-thick-generation-verification}).
\item \emph{Lattice bypass}: for lattice vertex algebras,
  DK-1$\frac{1}{2}$ (Theorem~\ref{thm:lattice:factorization-koszul})
  gives the factorization equivalence without any thick generation
  argument, via sectorwise finiteness of the $\Lambda$-graded bar complex.
  At simply-laced level~$1$, the FKS identification
  yields an \emph{unconditional} factorization DK statement
  (Corollary~\ref{cor:lattice:factorization-dk-level1}).
\end{enumerate}

\emph{Candidate strategies.}  All four are developed in detail in
\S\ref{sec:cat-O-strategies} of the Yangian chapter.
\begin{enumerate}[label=\textup{(\roman*)}]
\item \emph{Highest-weight filtration + resolution}
  (Proposition~\ref{prop:bgg-criterion}).
  If every Verma module $M(\lambda, a)$ admits a finite resolution
  by finite-dimensional evaluation modules, thick generation follows.
  The classical BGG resolution handles dominant integral~$\lambda$;
  for non-dominant~$\lambda$, the Yangian $R$-matrix poles create
  inter-spectral-parameter extensions that may yield resolutions
  (Remark~\ref{rem:bgg-spectral-parameter}).

\item \emph{t-structure argument}
  (Theorem~\ref{thm:catO-thick-generation}).
  \textbf{Proved} for $\mathfrak{sl}_2$ and
  $\mathcal{O}_{\mathrm{poly}}$ at all~$N$.  The generation
  problem reduces to finite-dimensionality of simples via
  Lemma~\ref{lem:composition-thick-generation}.

\item \emph{Reduction to the Kazhdan--Lusztig problem}
  (Proposition~\ref{prop:thick-gen-projective}).
  Thick generation is equivalent to: every projective indecomposable
  has a finite evaluation resolution.  The KL multiplicities
  provide the combinatorial data; the remaining input is that Verma
  modules lie in $\mathcal{C}_{\mathrm{eval}}$
  (Remark~\ref{rem:kl-input-yangian}).

\item \emph{Sectorwise finiteness generalization}
  (Proposition~\ref{prop:yangian-bar-loop-weight}).
  The factorization bar complex has a (root weight, loop degree)
  bifiltration with finite-dimensional bigraded pieces.  The
  associated spectral sequence has $E_1$ page given by the bar
  cohomology of the current algebra $U(\mathfrak{g}[t])$.
  Unlike the lattice case, the $E_1$ page is nontrivial
  (Remark~\ref{rem:sectorwise-vs-lattice}).
  This strategy is now theorematic on the evaluation-generated core:
  sectorwise convergence plus factorization descent resolve DK-2/3 for
  all simple types
  \textup{(}Theorem~\ref{thm:h-level-factorization-kd} and
  Corollary~\ref{cor:dk23-all-types}\textup{)}.
\end{enumerate}

\emph{Assessment.}
The generation problem is now resolved for
the natural domain $\mathcal{O}_{\mathrm{poly}}$
(Theorem~\ref{thm:catO-thick-generation}): thick generation holds
for $N = 2$ unconditionally and for
$\mathcal{O}_{\mathrm{poly}}$ at all~$N$,
by the composition-length argument
(Lemma~\ref{lem:composition-thick-generation}) combined with
finite-dimensionality of all polynomial simples.
By Lemma~\ref{lem:fd-thick-closure}, this is the strongest
possible thick-generation statement: the thick closure of
finite-dimensional evaluation modules in $D^b(\mathcal{O})$ equals
$D^b(\mathcal{O}_{\mathrm{poly}})$ and cannot reach
infinite-length modules.

Theorem~\ref{thm:eval-core-identification} sharpens the type-$A$
picture to an exact identification: the evaluation-generated core
equals $D^b(\operatorname{Rep}_{\mathrm{fd}})$, and the DK-2/3
equivalence extends to this entire core at all~$N$ in type~$A$.
Outside type~$A$, Corollary~\ref{cor:dk23-all-types} still gives the
theorematic endpoint: DK-2/3 on the evaluation-generated core at all
simple types.  The remaining MC3 frontier therefore splits into two
layers.  First comes the ordinary-derived domain question: does the
DK-relevant category stop at the evaluation-generated / polynomial
core, or is there a larger ordinary-derived domain worth naming?

Second comes the genuine completion step: if one wishes to include
infinite-dimensional modules, generation reopens in a
completed/coderived setting:
\begin{enumerate}[label=\textup{(\alph*)}]
\item If the factorization DK conjecture targets the
  evaluation-generated core, DK-2/3 is
  \emph{unconditionally proved} at all simple types, and the DK ladder
  proceeds directly to DK-4
  (Remark~\ref{rem:corrected-mc3-frontier}(a)).
\item If one wishes to include infinite-dimensional modules
  (prefundamental, asymptotic), a completed/coderived
  enhancement is required, with generation via Baxter-type
  exact triangles relating prefundamental and evaluation
  modules (Remark~\ref{rem:corrected-mc3-frontier}(b)).
\end{enumerate}

Strategy~(iv) is now the proved bypass for the evaluation-generated
core, and its residual rôle is diagnostic rather than frontier:
the $E_1$ page is proved to have
sub-exponential growth $\sim \exp(\pi\sqrt{rp/12})$
(Proposition~\ref{prop:lqt-e1-subexponential-growth},
Computation~\ref{comp:lqt-e1-growth-verification});
the convergence problem has been discharged by
Theorem~\ref{thm:sectorwise-spectral-convergence}, so what remains is
to understand whether and how the completed/coderived enlargement
packages should be attached beyond the generated core.
\end{remark}

\medskip

\noindent\textbf{Comparison~7: Periodicity and tautological depth.}
\index{periodicity!geometric and modular contributions}

\emph{Proposed statement.}
Modular, quantum, and geometric periodicities unify into a single
coherent classification, with clean reciprocity laws under Koszul
duality.

\emph{Geometric, quantum, and modular contributions.}
The principal correction is that the ``geometric periodicity'' of
the original formulation is not periodic at all: the geometric operator
$\sigma_{\mathrm{geom}} = \kappa(\lambda)$ is \emph{nilpotent}
($\sigma^N = 0$, providing bounded tautological depth) rather than
\emph{periodic} ($\sigma^N = \mathrm{id}$).  The corrected
framework has two genuine periodic sources and one nilpotent
stabilization mechanism:

\begin{enumerate}[label=\textup{(\alph*)}]
\item \emph{Modular periodicity} (Type~I).  Conjectured for minimal
  models (Conjecture~\ref{conj:modular-periodicity-minimal}) and WZW
  models at all positive integer levels
  (Conjecture~\ref{conj:modular-periodicity-wzw}), with $T$-matrix
  periodicity proved unconditionally in both cases; conjectured for
  general rational chiral algebras
  (Conjecture~\ref{conj:modular-periodicity}).  Natural next cases:
  $\mathcal{W}$-algebra minimal models and rational orbifolds.
\item \emph{Quantum periodicity} (Type~II).
  Proved: the quantum-profile input coming from quantum integer,
  quantum-dimension, and fusion-data periodicity is unconditional at
  admissible level.  Its transport under Koszul duality is governed by
  $q \mapsto q^{-1}$
  (Proposition~\ref{prop:periodicity-exchange-koszul}).
\item \emph{Geometric tautological depth} (Type~III).
  \textbf{Proved}: the Kodaira--Spencer image of the Hodge class is
  nilpotent with index $\leq g - 1$ on smooth moduli
  (Theorem~\ref{thm:geometric-depth-smooth}, via Graber--Vakil
  vanishing~\cite{GraVak05}), and $\leq 3g - 2$ on
  $\overline{\mathcal{M}}_g$
  (Theorem~\ref{thm:geometric-periodicity-weak}).  At genus~$2$, the
  Hodge class contribution vanishes entirely.  The boundary-extended
  amplitude is bounded by the Mumford relation
  (Conjecture~\ref{conj:geometric-periodicity}).  The geometric
  contribution controls the stabilization \emph{threshold}---the
  conformal weight beyond which modular/quantum periodicity holds---not
  a periodic factor.
\end{enumerate}
The stratified periodicity package
(page~\pageref{conj:complete-periodicity-classification}) therefore
gives
$\mathrm{Period}(\cA) \mid \mathrm{lcm}(N_{\mathrm{mod}},
N_{\mathrm{quant}})$, with the geometric tautological depth
bounding the stabilization threshold.  The modular input remains the
sole conjectural periodic ingredient.
The reflected periodicity conjecture
(Conjecture~\ref{conj:reflected-modular-periodicity}), which
would express how modular periods transform under Koszul duality
via the dual central charge, depends on
Conjecture~\ref{conj:modular-periodicity}.
Within the monograph itself, the only explicit periodic bar calculation
at theorem level beyond the general exchange theorem is the rank-$1$
$\widehat{\mathfrak{sl}}_2$ pattern of
Proposition~\ref{prop:sl2-periodicity-dl}; higher-rank periodic CDG
packages remain frontier statements and should not be used as already
proved transport data in MC3.

What replaces the original ``unified doctrine'' is a
\emph{stratified periodicity framework} organized by the
\emph{nilpotence-periodicity correspondence}
(Remark~\ref{rem:nilpotence-periodicity}): every periodic phenomenon
in bar cohomology arises as the exponential of a nilpotent datum, and
conversely.  The geometric contribution is correctly classified as
nilpotent depth (proved): the operator $\kappa(\lambda)$ is nilpotent
with index $\leq g-1$ (Theorem~\ref{thm:geometric-depth-smooth}),
and its exponential $\exp(\kappa(\lambda))$ is \emph{unipotent}, not
periodic---geometry provides a stabilization threshold, not a period.

The quantum periodicity is unconditional: the screening operators
$Q_i^2 = 0$ (nilpotent) exponentiate to the periodic representation
theory of quantum groups at roots of unity.  The modular periodicity
of bar cohomology remains conjectural even for the flagship rational
families, but the T-matrix period formula
$N = 24q'/\gcd(p',24)$ for $c = p'/q'$ is the direct consequence of
$\exp(2\pi i N c/24) = 1$: the period is determined by when the
exponential of the central charge (the scalar nilpotent datum) returns
to identity.  The sharp modular formula $N_{\mathrm{mod}}$ is the
remaining modular-periodicity conjecture; the
nilpotence-periodicity correspondence predicts that it will be
derived from~$\kappa(\cA)$ via exponentiation.

\medskip

\noindent\textbf{Comparison~8: Spectral discriminant geometry.}

\emph{Proposed statement.}
The shared discriminant $\Delta(x) = (1-3x)(1+x)$ across
$\widehat{\mathfrak{sl}}_2$, Virasoro, and $\beta\gamma$ systems
is the first non-scalar characteristic class of the modular
Koszul object.

\emph{Spectral determinant.}
Theorem~\ref{thm:discriminant-spectral} proves
$\Delta_{\cA}(x) = \det(1 - x\, T_{\mathrm{rec}})$ in both the
rational and algebraic cases.  The spectral radius of
$T_{\mathrm{rec}}$ equals $\dim(\mathfrak{g})$ for Kac--Moody
algebras (Theorem~\ref{thm:dominant-branch-point}).  The
Drinfeld--Sokolov invariance of the discriminant
(Theorem~\ref{thm:ds-bar-gf-discriminant}) shows that distinct
chiral theories on the same modular spectral sheet share the
same~$\Delta$, even when their operator content differs entirely.

The discriminant's role is now precise: $\kappa(\cA)$ is the
\emph{first scalar characteristic number} of the universal
class~$\Theta_{\cA}$ (Theorem~\ref{thm:universal-MC}), and $\Delta_{\cA}(x)$ is the \emph{first
non-scalar characteristic class}---the spectral determinant of
the transfer matrix governing the recurrence of bar-cohomology
dimensions.  The spectral branch object
(Definition~\ref{def:spectral-branch-object}) formalizes $\Delta$
as $\det(1 - x\, T_{\mathrm{br},\cA})$ on a finite-rank perfect
complex~$V_{\cA}^{\mathrm{br}}$.

Together, the five components of the full package
$\mathcal{C}_{\cA}$
(Definition~\ref{def:full-modular-package}) form a hierarchy
of increasing refinement: scalar $\to$ spectral/polynomial
$\to$ full Maurer--Cartan class.

The stronger conjecture
(Conjecture~\ref{conj:ds-spectral-branch-preservation}) that
DS reduction preserves the full transport operator~$T_{\mathrm{br}}$
up to similarity---not merely its determinant~$\Delta$---would
promote Comparison~8 from a spectral statement to a categorical one:
the spectral branch object would become a functorial invariant of
the DS orbit.  The sl${}_2$ case is automatic (distinct eigenvalues);
the sl${}_3$ case is the first non-trivial test.

\medskip

\noindent\textbf{Comparison~9: BV/BRST/path-integral/holography
completion.}

\emph{Proposed statement.}
The entire BV-BRST formalism, path integrals, anomalies,
holographic duality, and higher-dimensional extensions would all
eventually be read through the Koszul-duality machine.

\emph{Genus zero and the higher-genus conjectures.}
At genus zero, Theorem~\ref{thm:brst-bar-genus0} is a filtered comparison criterion with a separately constructed target and relative carrier, a prescribed section functor, compatible exhaustive split filtrations, an actual graded chain quasi-isomorphism, and compatible corrections.
The affine criterion of Theorem~\ref{thm:bar-semi-infinite-km} requires the actual coefficient and filtered chain maps.
Theorem~\ref{thm:bar-semi-infinite-w} further requires an equivariant map of specified double complexes and a valid totalization.
Theorem~\ref{thm:cbc26-polynomial-ds} constructs the polynomial reduction on its associative carrier.
For $V\otimes bc$, Proposition~\ref{prop:vir-no-augmentation} obstructs the augmentation-kernel target.
Proposition~\ref{prop:vir-relative-target-obstruction} obstructs the standard relative coordinate transitions for the universal Virasoro vacuum.
These genus-zero obstructions require a different constructed chiral carrier before a symbol comparison can be posed.

Corollary~\ref{cor:string-amplitude-genus0} transfers amplitudes only with actual $n$-point insertion maps, their pairing homotopy, and a closed integration functional on a declared form domain.
Corollary~\ref{cor:disk-local-ternary-on-brstbar-locus} reduces the local comparison to arity three only after the separate binary-intertwining identity is supplied.
The algebraic string dictionary of Theorem~\ref{thm:brst-bar-genus0} and Corollary~\ref{cor:string-amplitude-genus0} uses these conditions; equality of scalar anomalies does not replace them.

The higher-genus programme
(Conjectures~\ref{conj:bv-equals-bar-cobar},
\ref{conj:bar-cobar-path-integral},
\ref{conj:string-amplitude}) requires Costello's
renormalization framework at all genera and remains correctly
conjectural.  The holographic duality programme
(Conjectures~\ref{conj:holographic-koszul-deformation},
\ref{conj:deformation-holographic}) and the $\mathsf{E}_n$
extension (Theorem~\ref{thm:en-koszul-duality}; proved) illustrate
Comparison~3's higher-dimensional shadow.

\medskip

\begin{remark}[Comparison statements]\label{rem:nine-futures-summary}
\index{modular Koszul duality!comparison statements}
The comparisons have the following mathematical scope:
\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}clp{5.5cm}@{}}
\toprule
\textbf{\#} & \textbf{Comparison} & \textbf{Scope} \\
\midrule
1 & $H^1(\mathcal{M}_g)$-parameterized theory & \textbf{Proved} \\
2 & Modular Koszul duality & Scalar core and stated categorical comparisons \\
3 & Modular homotopy theory & Defined (Def.~\ref{def:modular-homotopy-theory-working}) \\
4 & Universal char.\ package & \textbf{Quadruple proved}: standard landscape (Cor.~\ref{cor:effective-quadruple}), generic level universal (Thm~\ref{thm:cyclic-rigidity-generic}); equivalence (Prop.~\ref{prop:saturation-equivalence}); functorial (Prop.~\ref{prop:saturation-functorial}); admissible levels conj.\ (Conj.~\ref{conj:scalar-saturation-universality}); $\Pi_{\cA}$ sharp open \\
5 & Index genus expansions & \textbf{Realized} (Thm~\ref{thm:family-index}) \\
6 & Derived Drinfeld--Kohno & DK-0/1/$1\frac{1}{2}$ proved; DK-2/3 proved (eval-gen.\ core, all types, domain: $\mathcal{O}_{\mathrm{poly}}$; uses Molev PBW); full $\mathcal{O}$/KL extension conjectural; Positselski/ind-completion/ML theorem; DK-4/5 formalized \\
7 & Periodicity \& tautological depth & Corrected: geometric contribution is nilpotent depth (proved, index $\leq g-1$); quantum proved; modular bar periodicity conjectural \\
8 & Spectral discriminant & \textbf{Proved} + branch (Def.~\ref{def:spectral-branch-object}) \\
9 & BV/BRST completion & Genus-zero target and comparison data required; higher-genus extension open \\
\bottomrule
\end{tabular}
\end{center}
\noindent
The proposed \emph{modular homotopy theory for
factorization algebras on curves} is the global completion of the
nilpotence-periodicity correspondence
(Remark~\ref{rem:nilpotence-periodicity}) from genus~$0$ to all
genera.  Its structural identity is the principle that
\emph{nilpotence is the logarithm of periodicity}: the bar
construction is the categorical logarithm whose single-valued
regime is classical Koszul duality (genus~$0$) and whose
multi-valued regime is the modular theory (genus~$g \geq 1$); the
curvature $\kappa(\cA)$ is the infinitesimal generator of the
monodromy; the coderived category is the analytic continuation; the
characteristic hierarchy
(Remark~\ref{rem:characteristic-hierarchy}) organizes the
logarithmic data ($\kappa$, $\Delta$, $\Theta$) from which the
periodic data ($N$, $\Pi$, modular automorphy) are derived via
exponentiation.

The GRR genus theorem (Comparison~5), the spectral characteristic statements
(Comparisons~4 and~8), and the derived Drinfeld--Kohno comparisons beyond the evaluation locus
(Comparison~6) give distinct consequences.  All-genus corrections parametrized by
$H^1(\mathcal M_g)$ are excluded by the vanishing in Comparison~1;
sharp global periodicity is a separate assertion from the geometric nilpotence in Comparison~7.
The nilpotence-periodicity framework
classifies the geometric contribution as nilpotent depth (not
period), derives modular periodicity from the scalar
characteristic via exponentiation, and identifies the Koszul locus
as the convergence domain of the categorical logarithm.
\end{remark}


\begin{remark}[Structural gaps and computational verification]
\label{rem:structural-gaps-verification}
\index{structural gaps}
Eight structural gaps separate the proved core
(Theorems~A--D and~H, MC1--MC2, DK-ladder through DK-$2/3$) from the
categorical comparisons in Comparisons~3, 6, and~9.  The following statements
distinguish constructions from computations in specific models.

\smallskip
\noindent\textbf{V1: Coderived Ran category.}
The coderived category
$D^{\mathrm{co}}(\operatorname{Fact}(X))$ is the correct target for
the bar-cobar equivalence off the Koszul locus
(\S\ref{subsec:coderived-ran}).
\emph{Scope:}
the provisional coderived category
(Definition~\ref{def:provisional-coderived},
Proposition~\ref{prop:coderived-adequacy})
suffices for Stratum~I results.  The full factorization-level
coderived/contraderived formalism analogous to
Positselski~\cite{Positselski11} remains open (Stratum~II).

\smallskip
\noindent\textbf{V2: Shifted prefundamental representations.}
The MC3 four-conjecture package
(Conjectures~\ref{conj:baxter-exact-triangles}--\ref{conj:dk-compacts-completion})
requires the Hernandez--Jimbo--Zhang prefundamental modules
$L^\pm(a)$ for Yangians.
\emph{Scope:}
$K_0$-level verification confirms root containment (every
$[V_n(a)]$ embeds as a contiguous spectral window in $L^+(a)$),
$Q$-polynomial divisibility, QQ-system Wronskian structure, string
complementarity ($L^+ \cup L^-$ tiles the spectral line), and
pro-Weyl shadow convergence ($\operatorname{ch}(M(\lambda))$ is the
parity-correct limit of projected evaluation characters).

\smallskip
\noindent\textbf{V3: $\mathcal{W}_4$ OPE coefficient extraction.}
The MC4 W-infinity stabilization programme
(Remark~\ref{rem:mc4-winfty-computation-target})
reduces at stage~$4$ to an exact six-entry identity packet on
$\mathcal{I}_4$, whose principal Drinfeld--Sokolov side is explicit.
\emph{Scope:}
all four coefficients extracted as explicit rational functions of~$c$:
\begin{gather*}
c_{334}^2 = \frac{42c^2(5c+22)}{(c+24)(7c+68)(3c+46)},
\quad
c_{444}^2 = \frac{112c^2(2c-1)(3c+46)}{(c+24)(7c+68)(10c+197)(5c+3)},
\\
c_{343}^2 = \tfrac{9}{16}\,c_{334}^2,
\qquad
c_{344}^2 = \tfrac{5}{7}\,c_{334}^2.
\end{gather*}
Both theorematic Virasoro-target identities verified on the principal side:
$C^{\mathrm{DS}}_{4,4;2;0,6} = 2$ (universal $T$-coupling) and
$C^{\mathrm{DS}}_{3,4;2;0,5} = 0$ (mixed Virasoro vanishing).

The first unconditional W-side packet is therefore the exact six-entry
identity comparison on $\mathcal{I}_4$; under the normalized residue
two-point package of
Corollary~\ref{cor:winfty-stage4-residue-four-channel}, its live
content contracts to the four higher-spin channels.

Proposition~\ref{prop:winfty-stage-growth-virasoro-target-contraction}
extends that target-$2$ elimination uniformly to every reduced
incremental packet $\mathcal{J}_{M+1}^{\mathrm{red}}$.

The first next reduced stage is already explicit as the eleven-entry
packet $\mathcal{J}_5^{\mathrm{red}}$, and
Corollary~\ref{cor:winfty-stage5-residue-eight-channel} is the first
concrete specialization, contracting it to the eight-channel
higher-spin packet $\mathcal{J}_5^{\mathrm{hs}}$ of
Corollary~\ref{cor:winfty-stage5-higher-spin-packet}.  The next finite
higher-spin bar-vs-DS list is therefore
Conjecture~\ref{conj:winfty-stage5-higher-spin-identities}, already
linearized by
Proposition~\ref{prop:winfty-stage5-higher-spin-subblocks} into the
singleton blocks $(3,4)$ and $(5,5)$ and the mixed triples $(3,5)$ and
$(4,5)$, or equivalently by
Corollary~\ref{cor:winfty-stage5-entry-transport} into the entry packet
$\mathcal{J}_5^{\mathrm{entry}}$ and the mixed transport packet
$\mathcal{J}_5^{\mathrm{tr}}$, and then by
Proposition~\ref{prop:winfty-stage5-transport-target-ladders} into the
target ladders
$\mathcal{J}_5^{\mathrm{tr},3}$,
$\mathcal{J}_5^{\mathrm{tr},4}$,
$\mathcal{J}_5^{\mathrm{tr},5}$.
Unitarity non-negativity confirmed at all $\mathcal{W}(N, N{+}1)$
minimal models for $4 \leq N \leq 19$.

\smallskip
\noindent\textbf{V4: KL $N$-complex structure.}
At admissible levels $k = -h^\vee + p/q$ the quantum group parameter
$q = e^{2\pi i/N}$ is a root of unity, and the Kapranov $q$-bar
differential satisfies $d_q^N = 0$ but $d_q^2 \neq 0$ for $N \geq 3$.
\emph{Scope:}
verified for $u_q(\mathfrak{sl}_2)$ at $N = 2, 3$: the small quantum
group (dim $= N^3$, PBW basis), standard bar cohomology
$H^*(u_q, \mathbb{C})$, and the $N$-complex structure (including
cohomology flavors $H^{j, N-j}$) are all computed.

In the first larger roots $N=4,5$, the structural identity $d_q^N=0$
is discharged by the quantum-binomial certificate once the reduced bar
spaces are too large to materialize densely, while the degree-$2$
vanishing $d_q^2=0$ is separated as a structural reduced-bar fact
\textup{(}because $d_q\colon B_1\to B_0$ vanishes\textup{)}.

The genuinely non-classical statement $d_q^2\neq0$ is still verified at
bar degree~$\geq 3$ in the tractable $N=3$ case, and the first
degree-$4$ packet is now resolved there with
\[
\dim H^{1,2}_2
=
\dim H^{2,1}_3
=
3.
\]
At the same S-level, both flavors already have the same sparse
three-point total root-weight profile on weights $-3,0,3$, with
class-support sizes $1,6,1$.  What remains open is the promotion from
this M-level packet to the periodic/coderived KL target and the
determination of whether one flavor, the paired packet, or some further
categorical operation on this sparse fingerprint carries the shadow.

The first
sparse $N=4$ degree-$1$ packet is now fully resolved and vanishes:
\[
H^{3,1}_1 = H^{2,2}_1 = H^{1,3}_1 = 0.
\]

The first tractable degree-$2$ channels also vanish:
\[
H^{3,1}_2 = H^{2,2}_2 = 0.
\]

For the remaining $H^{1,3}_2$ channel, the split-form sparse
$d_q^3\colon B_5\to B_2$ certificate already gives
\[
\operatorname{rank}\bigl(\operatorname{im}(d_q^3)\bigr)\ge 3903,
\qquad
\dim H^{1,3}_2 \le 66.
\]
The residual cokernel is supported only on left factors
$F$, $E$, and $K-1$.  The later split-form precursor stages
$ab$, $abc$, and $abcd$ landing in that span are already quotient-zero.

The surviving first-term sector compresses from $42{,}718{,}284$ raw
tuples to only $1493$ distinct right-product states.  Its standalone
tensor span adds only $57$ directions beyond the $3903$-rank seed span
\textup{(}$43$ from $F$, $14$ from $E$, $0$ from $K-1$\textup{)}, so
the next comparison target is the cancellation/annihilator problem
between that first-term $57$-plane and
the remaining nonresidual split contributions rather than the full
$B_5 \to B_2$ map.

The combined non-first split sector already spans
that same $57$-plane, and the witness-basis internal cancellation
operator there is exactly $(-1-i)\mathrm{Id}$.

The exhaustive
compressed verification of all $354{,}668$ surviving prefix signatures,
covering the full $42{,}718{,}284$ raw surviving columns, now shows
that every weighted surviving column vanishes in the seed quotient.

Thus the seed image rank $3903$ is exact and
\[
\dim H^{1,3}_2 = 66.
\]

The live issue is therefore the periodic/coderived shadow and
categorical meaning of this exact $66$-dimensional packet.  It already
has the rigid support split
\[
F \otimes 48 \;\oplus\; E \otimes 15 \;\oplus\; (K-1)\otimes 3
\]
and palindromic total root-weight profile
\[
1,4,8,12,16,12,8,4,1
\]
across weights $-4,\dots,4$.

Equivalently, this is an exact unit-step
interval staircase with multiplicities
$1,3,4,4,4$ on supports $[-4,4],[-3,3],[-2,2],[-1,1],\{0\}$.
This staircase is also not the image of the first admissible $N=3$
packet under any exact symmetric convolution transport at all: support
forces every radius-$R\ge2$ kernel coefficient at $\pm R$ to vanish,
reducing any exact transport to the radius-$1$ case, and the exact
radius-$0$ and radius-$1$ systems are already inconsistent for both one
flavor and the paired packet.  Signed radius-$4$ kernels do exist on
the restricted window $[-4,4]$, but they necessarily spill outside that
window and so are not genuine transports.  So the next categorical test
is no longer a dimension match; it must explain how the sparse first
$N=3$ packet relates, if at all, to this rigid $N=4$ staircase by a
genuinely non-convolutional shadow, or show that the $N=4$ packet is
genuinely new.

There is, however, now a first structured finite-window candidate on the
S-level: the paired $N=3$ profile is recovered exactly from the
$N=4$ staircase on $[-4,4]$ by the Dirichlet discrete Schr\"odinger
operator $a\Delta + V(w)$ with
$a=-3/140$ and even sextic potential
\[
V(w)=\frac{4}{35}
-\frac{2519}{16800}w^2
+\frac{503}{13440}w^4
-\frac{17}{9600}w^6.
\]
No such operator exists with lower even potential degree, and pure
multiplication requires degree~$8$.  So the first surviving structured
route is already second-order and finite-window, not convolutional.

\smallskip
\noindent\textbf{V5: Virasoro/$\mathcal{W}_N$ PBW analog.}
Whitehead rigidity for $\widehat{\mathfrak{sl}}_2$-modules:
$\operatorname{Ext}^1_{U(\widehat{\mathfrak{sl}}_2)}(M, M) = 0$ for
Verma-like modules built from $L_{-n}$ modes, needed for the PBW
spectral sequence at non-abelian vertex algebras.
\emph{Scope:} conjectural.

\smallskip
\noindent\textbf{V6: Higher-genus BV/BRST.}
Costello renormalization compatibility: the genus-$g$ bar complex should
be compatible with the Costello--Gwilliam BV quantization framework
at all genera, not just genus~$0$
(Conjecture~\ref{conj:bv-equals-bar-cobar}).
The genus-zero statement requires the full target, relative-descent, filtration, symbol-map, correction, operation, and pairing data of Comparison~9.
Propositions~\ref{prop:vir-no-augmentation} and~\ref{prop:vir-relative-target-obstruction} obstruct the specified augmentation-kernel and standard relative constructions.
Higher genus requires the further compatible extension.

\smallskip
\noindent\textbf{V7: Pro-nilpotent topology.}
Completed bar-cobar convergence in the Francis--Gaitsgory pro-nilpotent
framework~\cite{FG12}: finite-stage bar-cobar compatibility
(Remark~\ref{rem:completion-frontier-crystallization}(H3)) for the
$I$-adic completion.
\emph{Scope:} structural framework in place
(Appendix~\ref{app:nilpotent-completion}); finite-stage compatibility
open (Stratum~II).

\smallskip
\noindent\textbf{V8: Braided monoidal compatibility.}
Factorization-level Kazhdan equivalence:
Theorem~\ref{thm:dk-fd-typeA} gives braided monoidal equivalences
on $\operatorname{Rep}_{\mathrm{fd}}$; the upgrade to category~$O$
requires DK-$4/5$ (Conjectures~\ref{conj:kl-periodic-cdg}--\ref{conj:kl-braided}) and the compact generation of
Conjecture~\ref{conj:dk-compacts-completion}.
\emph{Scope:} DK-$2/3$ proved; braided upgrade open.
\end{remark}


\subsection{Index of conjectures and related results}\label{sec:conjecture-index}
\index{conjectures!index}

The following conjectures and related results are organized by theme.
Each entry gives the label, a brief description,
and the section where it appears.

\smallskip
\noindent\textbf{Algebraic structure.}

\begin{itemize}[nosep]
\item Conjecture~\ref{conj:anomaly-cancellation}:
  Anomaly cancellation implies uncurved duality
  (\S\ref{sec:first-complete-proof}).
\item Conjecture~\ref{conj:brst-cohomology}:
  BRST cohomology as bar cohomology, general genus
  (\S\ref{sec:first-complete-proof}).
\item Conjecture~\ref{conj:cobar-physical}:
  Cobar construction as physical state space
  (\S\ref{sec:first-complete-proof}).
\item Conjectures~\ref{conj:modular-periodicity-minimal},
  \ref{conj:modular-periodicity-wzw},
  \ref{conj:modular-periodicity}:
  Modular periodicity of bar cohomology (conjectured for
  minimal models, WZW, and general rational chiral algebras;
  $T$-matrix periodicity proved unconditionally)
  (\S\ref{sec:km-koszul-abstract}).
\item Conjecture~\ref{conj:reflected-modular-periodicity}:
  Reflected periodicity for non-self-dual algebras
  (\S\ref{sec:km-koszul-abstract}).
\item Conjecture~\ref{conj:w-infty-bar}:
  downstream large-$N$ inverse-limit specialization of the standard
  $\mathcal{W}_\infty$ MC4 package
  (\S\ref{sec:w3-bar-degree3}).
\end{itemize}

\smallskip
\noindent\textbf{Representation theory and Langlands.}

\begin{itemize}[nosep]
\item Conjectures~\ref{conj:kl-periodic-cdg}--\ref{conj:kl-braided}:
  Kazhdan--Lusztig recovery from bar-cobar (three stages:
  periodic CDG, coderived lift, braided semisimplified target;
  \S\ref{subsec:kl-from-bar-cobar}).
\item Conjecture~\ref{conj:full-derived-dk}:
  Factorization-categorical DK/KL bridge beyond the evaluation locus
  (\S\ref{sec:derived-dk}).
\item Conjecture~\ref{conj:coha-koszul}:
  CoHA Koszulness for quiver varieties
  (\S\ref{sec:yangian-bar-explicit}).
\item Conjecture~\ref{conj:shifted-yangian-langlands}:
  Shifted Yangian and geometric Langlands
  (\S\ref{sec:yangian-bar-explicit}).
\item Conjecture~\ref{conj:e1-genus-theory}:
  $\Eone$-chiral genus theory for Yangians
  (\S\ref{sec:yangian-bar-explicit}).
\item Conjecture~\ref{conj:mc3-sectorwise-all-types}:
  completed MC3 via sectorwise finiteness for all types
  (\S\ref{sec:cat-O-strategies}).  $E_1$ sub-exponential growth is
  proved
  (Proposition~\ref{prop:lqt-e1-subexponential-growth}), and
  sectorwise convergence on the evaluation-generated core is proved
  (Theorem~\ref{thm:sectorwise-spectral-convergence}); the open step
  is extension of this package to the completed/coderived
  enlargement.
\item Conjecture~\ref{conj:factorization-finiteness-criterion}:
  Factorization finiteness criterion (abstract bypass of thick
  generation; \S\ref{sec:categorical-logarithm}).
\end{itemize}

\smallskip
\noindent\textbf{Generating functions and combinatorics.}
\begin{itemize}[nosep]
\item Conjecture~\ref{conj:sl3-bar-gf}:
  $\widehat{\mathfrak{sl}}_3$ bar generating function
  (\S\ref{sec:sl3-computation}).
\item Conjecture~\ref{conj:non-simply-laced-discriminant}:
  Non-simply-laced discriminant families
  (\S\ref{subsec:bernoulli-asymptotics}).
\item Conjecture~\ref{conj:w3-algebraicity}:
  $\mathcal{W}_3$ bar algebraicity
  (\S\ref{sec:w3-bar-degree3}).
  Seven independent consistency checks support $H^5 = 171$
  (Appendix~\ref{subsec:w3-h5-verification}).
\item Conjecture~\ref{conj:yangian-bar-gf}:
  Yangian bar generating function
  (\S\ref{sec:yangian-bar-explicit}).
  Seven independent consistency checks support $H^4 = 82$
  (Appendix~\ref{subsec:yangian-h4-verification}).
\item Conjecture~\ref{conj:discriminant-ks-operator}:
  Kodaira--Spencer operator on reduced $H^1$
  (\S\ref{sec:master-table}).
\end{itemize}

\smallskip
\noindent\textbf{Physics and BV-BRST.}
\emph{Items marked Heuristic require physics input beyond the algebraic framework.}

\begin{itemize}[nosep]
\item Conjecture~\ref{conj:bv-equals-bar-cobar}:
  BV formalism equals bar-cobar at all genera
  (Section~\ref{sec:brst-bar-chain-map}).
\item Conjecture~\ref{conj:quantum-master-complete}:
  Complete quantum master equation from bar-cobar
  (Section~\ref{sec:brst-bar-chain-map}).
\item Conjecture~\ref{conj:bar-cobar-path-integral}:
  Bar-cobar as rigorous path integral
  (Section~\ref{sec:brst-bar-chain-map}).
\item Conjecture~\ref{conj:physical-pairing}:
  Feynman rules dictionary
  (Section~\ref{sec:brst-bar-chain-map}).
\item Conjecture~\ref{conj:string-amplitude}:
  String amplitudes from bar complex
  (Section~\ref{sec:free-field-genera}).
\item Proposition~\ref{thm:modular-anomaly}:
  Translation multiplier of an integrally graded character
  (Section~\ref{sec:free-field-genera}).
\item Conjecture~\ref{conj:modular-anomaly-brst}:
  Chiral realization of modular transformation laws
  (Section~\ref{sec:free-field-genera}).
\item Theorem~\ref{thm:w-integrability}:
  W-algebra integrability from bar complex
  (Section~\ref{sec:free-field-genera}).
\item Conjecture~\ref{conj:bphz-recursion}:
  BPHZ recursion from bar differential
  (Section~\ref{sec:brst-bar-chain-map}).
\end{itemize}

\smallskip
\noindent\textbf{Holomorphic-topological and dualities.}
\begin{itemize}[nosep]
\item Theorem~\ref{thm:en-koszul-duality}:
  $E_n$ Koszul duality from configuration spaces
  (\S\ref{subsec:higher-dim-kd}; proved).

\item Conjecture~\ref{conj:gw-s-duality}:
  Goddard--Witten S-duality from Koszul
  (\S\ref{sec:km-koszul-abstract}).

\item Conjecture~\ref{conj:holographic-koszul-deformation}:
  Holographic duality from deformation quantization
  (\S\ref{sec:km-koszul-abstract}).

\item Conjecture~\ref{conj:deformation-holographic}:
  Deformation quantization = holographic dictionary
  (\S\ref{sec:km-koszul-abstract}).

\item Conjecture~\ref{conj:toroidal-koszul-dual}:
  Toroidal algebra Koszul dual \textup{(}elliptic/double-affine MC4 extension\textup{)}
  (\S\ref{sec:elliptic-bar-heisenberg}).
\item Conjecture~\ref{conj:extended-ferm-ghost}:
  Extended fermion-ghost duality \textup{(}derived/super MC4 extension\textup{)}
  (\S\ref{sec:free-field-genera}).
\item Conjecture~\ref{conj:wrt-conjecture}:
  WRT invariants from bar complex
  (\S\ref{sec:km-koszul-abstract}).
\item Remark~\ref{rem:periodicity-exchange-harmonic}:
  Periodicity--exchange duality
  (\S\ref{sec:km-koszul-abstract}).
\item Conjecture~\ref{conj:superstring-degeneration}:
  Superstring degeneration from genus tower
  (\S\ref{ss:genus}).
\end{itemize}
